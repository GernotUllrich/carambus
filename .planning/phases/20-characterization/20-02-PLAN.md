---
phase: 20-characterization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/models/league_test.rb
  - test/models/league_standings_test.rb
  - test/models/league_scraping_test.rb
autonomous: true
requirements: [CHAR-01, CHAR-02]

must_haves:
  truths:
    - "League standings_table_karambol ranks teams correctly by match points"
    - "League standings_table_snooker produces ranking output for known input"
    - "League standings_table_pool produces ranking output for known input"
    - "League reconstruct_game_plan_from_existing_data is expanded beyond smoke tests"
    - "League scraping methods (scrape_leagues_from_cc, scrape_league_optimized) are pinned with WebMock stubs"
    - "A behavior change in any standings or game plan method causes at least one test to fail"
  artifacts:
    - path: "test/models/league_test.rb"
      provides: "Expanded game plan reconstruction tests"
    - path: "test/models/league_standings_test.rb"
      provides: "Standings table characterization for all three disciplines"
    - path: "test/models/league_scraping_test.rb"
      provides: "Scraping pipeline characterization with WebMock stubs"
  key_links:
    - from: "test/models/league_standings_test.rb"
      to: "app/models/league.rb"
      via: "standings_table_karambol/snooker/pool assertions"
    - from: "test/models/league_scraping_test.rb"
      to: "app/models/league.rb"
      via: "WebMock stubbed scrape_* method assertions"
---

<objective>
Pin League's three behavior clusters (standings tables, game plan reconstruction, scraping pipeline) with characterization tests.

Purpose: League is 2219 lines with no AASM (per D-01). The three clusters identified in D-02 represent the bulk of extractable logic. Pinning their behavior now ensures Phase 21 extraction preserves correctness. The existing league_test.rb has only 3 smoke tests for reconstruct_game_plan — this plan expands that and adds standalone test files for standings and scraping.
Output: Three test files (one expanded, two new), all tests green.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-characterization/20-CONTEXT.md
@.planning/phases/20-characterization/20-RESEARCH.md
@app/models/league.rb
@test/models/league_test.rb
@test/fixtures/leagues.yml
@test/fixtures/regions.yml
@test/fixtures/seasons.yml
@test/fixtures/disciplines.yml
@test/support/scraping_helpers.rb
@test/support/vcr_setup.rb
</context>

<tasks>

<task type="auto">
  <name>Task 1: League standings + game plan characterization tests</name>
  <files>test/models/league_standings_test.rb, test/models/league_test.rb</files>
  <read_first>app/models/league.rb, test/models/league_test.rb, test/fixtures/leagues.yml, test/fixtures/disciplines.yml, app/models/league_team.rb</read_first>
  <action>
**Part A: Standings tests (per D-02 cluster 1)**

Create `test/models/league_standings_test.rb` with `frozen_string_literal: true`.

Read `app/models/league.rb` lines 1432-1600 to understand the exact standings_table_karambol, standings_table_snooker, and standings_table_pool implementations. Pay attention to:
- What columns the method reads from party.data (e.g., "result" key, how it splits "3:1")
- What the return structure looks like (array of hashes with :team, :platz, :punkte, :diff, etc.)
- How sorting/ranking works (by match points, then by difference, etc.)

Setup: Create league, league_teams, and parties programmatically using local IDs (>= 50_000_000). Use TEST_ID_BASE = 50_000_000 with offset 50_000 for this test file.

```ruby
# Pattern from RESEARCH.md:
TEST_ID_BASE = 50_000_000
def create_league_with_teams_and_parties(discipline_fixture:, result_data:)
  @@counter ||= 0
  @@counter += 1
  base = TEST_ID_BASE + 50_000 + (@@counter * 100)
  league = League.create!(id: base, name: "Standings Test", shortname: "ST",
    organizer: regions(:nbv), organizer_type: "Region",
    season: seasons(:current), discipline: disciplines(discipline_fixture))
  team_a = LeagueTeam.create!(id: base+1, league: league, name: "TeamA", cc_id: 1)
  team_b = LeagueTeam.create!(id: base+2, league: league, name: "TeamB", cc_id: 2)
  Party.create!(id: base+3, league: league, league_team_a: team_a, league_team_b: team_b,
    data: result_data)
  { league: league, team_a: team_a, team_b: team_b }
end
```

Tests for `standings_table_karambol`:
1. `test "standings_table_karambol ranks winning team first"` — create league with carom_3band discipline, party with result "3:1", assert first entry has higher punkte than second
2. `test "standings_table_karambol returns empty for league without parties"` — create league with no parties, assert empty or no-error result
3. `test "standings_table_karambol handles draw result"` — party with result "2:2", verify both teams get draw points

Tests for `standings_table_snooker`:
4. `test "standings_table_snooker produces ranking for snooker discipline"` — Read the method to determine the correct discipline fixture name. Create appropriate test data.

Tests for `standings_table_pool`:
5. `test "standings_table_pool produces ranking for pool discipline"` — Read the method to determine expected discipline and result format.

Additional standings tests:
6. `test "standings_table_karambol with multiple parties ranks correctly"` — Two parties (A beats B, then B beats A), verify tie-breaking logic

**Part B: Expand game plan tests (per D-02 cluster 2)**

Edit `test/models/league_test.rb`. Keep existing 3 tests. Add:

7. `test "analyze_game_plan_structure returns structure hash"` — Read league.rb around line 2032 for `analyze_game_plan_structure`. Call on league with parties, assert return type is meaningful (Hash, Array, or specific structure).

8. `test "reconstruct_game_plans_for_season handles league with no season"` — Read league.rb around line 2111. Create league without season, call method, assert no error and expected nil/empty return.

9. `test "reconstruct_game_plan_from_existing_data with parties returns game plan"` — Expand existing test: create league with actual parties (using local IDs), call `send(:reconstruct_game_plan_from_existing_data)`, assert return value structure more specifically than just "nil or GamePlan".

Verify: `bin/rails test test/models/league_standings_test.rb test/models/league_test.rb`
  </action>
  <verify>
    <automated>bin/rails test test/models/league_standings_test.rb test/models/league_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class LeagueStandingsTest" test/models/league_standings_test.rb
    - grep -q "frozen_string_literal: true" test/models/league_standings_test.rb
    - grep -q "standings_table_karambol" test/models/league_standings_test.rb
    - grep -q "standings_table_snooker" test/models/league_standings_test.rb
    - grep -q "standings_table_pool" test/models/league_standings_test.rb
    - grep -q "analyze_game_plan_structure" test/models/league_test.rb
    - grep -q "reconstruct_game_plans_for_season" test/models/league_test.rb
  </acceptance_criteria>
  <done>League standings test file has 6+ tests covering all three standings methods (karambol, snooker, pool) with winning/losing/draw scenarios. League test file expanded with 3+ new tests for game plan reconstruction and analysis. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: League scraping pipeline characterization tests</name>
  <files>test/models/league_scraping_test.rb</files>
  <read_first>app/models/league.rb, test/support/scraping_helpers.rb, test/support/vcr_setup.rb, test/fixtures/leagues.yml, test/fixtures/regions.yml</read_first>
  <action>
Create `test/models/league_scraping_test.rb` with `frozen_string_literal: true` (per D-02 cluster 3).

Read `app/models/league.rb` at these line ranges to understand each scraping method:
- Lines 303-400: `scrape_leagues_from_cc` — class method? instance method? what URL does it hit? what does it parse?
- Lines 405-540: `scrape_leagues_optimized` — what params? what HTTP calls?
- Lines 539-570: `scrape_league_optimized`, `scrape_league_teams_optimized`, `scrape_party_games_optimized`

Also read `app/models/region.rb` to understand how `organizer.public_cc_url_base` provides the base URL for scraping.

Read `test/support/scraping_helpers.rb` to use existing `mock_clubcloud_html` and `stub_request` patterns.

**Strategy:** Use WebMock `stub_request` with minimal HTML stubs (not VCR cassettes — per Claude's Discretion from CONTEXT.md). The goal is to pin that the methods can be called without error and produce expected side effects, not to test parsing accuracy (that's extraction phase work).

Tests (adjust based on what the code read reveals about method signatures):

1. `test "scrape_leagues_from_cc returns without error when HTTP stubbed"` — Stub the expected ClubCloud URL pattern. Call the method. Assert it doesn't raise. Pin the return type.

2. `test "scrape_leagues_from_cc handles HTTP error gracefully"` — Stub URL to return 500. Assert no unhandled exception (per CLAUDE.md error handling pattern: broad rescue).

3. `test "scrape_league_optimized processes stubbed HTML without error"` — Stub the league-specific URL. Call on a league instance with organizer that has a cc_url_base. Assert no crash.

4. `test "scrape_league_teams_optimized processes stubbed HTML without error"` — Same pattern.

5. `test "scrape_party_games_optimized processes stubbed HTML without error"` — Same pattern.

6. `test "scraping methods handle timeout gracefully"` — Stub URL to timeout. Assert method catches the error (per Pitfall 6: WebMock blocks, broad rescue expected).

For each stub, create minimal HTML that matches the expected structure. If the method parses Nokogiri HTML, provide a minimal valid HTML document with the expected CSS selectors. Read the method body to identify what selectors/patterns it looks for.

Use `stub_request(:get, /club-cloud/)` with a regex pattern matching ClubCloud URLs.

Important: If any scraping method is a class method (self.scrape_leagues_from_cc), call it on League class. If instance method, create a league with organizer region that has public_cc_url_base set.

Verify: `bin/rails test test/models/league_scraping_test.rb`
  </action>
  <verify>
    <automated>bin/rails test test/models/league_scraping_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class LeagueScrapingTest" test/models/league_scraping_test.rb
    - grep -q "frozen_string_literal: true" test/models/league_scraping_test.rb
    - grep -q "scrape_leagues_from_cc" test/models/league_scraping_test.rb
    - grep -q "scrape_league_optimized" test/models/league_scraping_test.rb
    - grep -q "stub_request" test/models/league_scraping_test.rb
    - grep -q "scrape_party_games_optimized" test/models/league_scraping_test.rb
  </acceptance_criteria>
  <done>League scraping test file has 5+ tests covering all scraping pipeline methods with WebMock stubs. Tests verify no-crash behavior, HTTP error handling, and timeout handling. All tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — test-only phase, no production code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | n/a | n/a | accept | Test-only phase — no new attack surface |
</threat_model>

<verification>
```bash
bin/rails test test/models/league_standings_test.rb test/models/league_test.rb test/models/league_scraping_test.rb
```
All tests green, 0 failures, 0 errors.
</verification>

<success_criteria>
- League standings test file covers all 3 standings methods (karambol, snooker, pool) with win/loss/draw scenarios
- League test file expanded from 3 to 6+ tests covering game plan reconstruction and analysis
- League scraping test file covers all scrape_* methods with WebMock stubs
- A behavior change in any standings method would cause at least one test to fail
- All new tests pass
- Full test suite remains green (751+ runs, 0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/20-characterization/20-02-SUMMARY.md`
</output>
