---
phase: 41
plan: 05
type: execute
wave: 3
depends_on: [41-01, 41-02, 41-03, 41-04]
files_modified:
  - test/system/devise_flows_test.rb
  - test/system/user_authentication_test.rb
autonomous: false
requirements:
  - REQ-41-15   # E2E Sign-up -> Confirmation-Mail-Click -> Logged-in
  - REQ-41-16   # E2E Forgot -> Reset-Mail-Click -> Neues PW -> Login funktioniert (JWT-Revoke ueber Plan-03/04 abgesichert)
  - REQ-41-17   # E2E Change-Password eingeloggt -> Notification-Mail -> Re-Login Pflicht
  - REQ-41-18   # E2E Email-Change -> Reconfirm-Mail an neue Adresse -> Bestaetigen -> email gesetzt
threat_refs:
  - T-41-05-01  # E2E-Test-Daten-Leak via persistente Browser-Cookies (Capybara-Session-Cleanup)
must_haves:
  truths:
    - "Capybara-System-Test fuer Registrierung+Confirmation: 1 Browser-Flow von /users/sign_up bis eingeloggt"
    - "Capybara-System-Test fuer Forgot-Password+Reset: vollstaendiger Token-Click-Through"
    - "Capybara-System-Test fuer Change-Password: nach Klick auf 'Update' wird User zu Login-Seite umgeleitet (D-41-C)"
    - "Capybara-System-Test fuer Email-Change-Reconfirmation: confirmation_instructions an neue Adresse, Click bestaetigt"
    - "Bestehende user_authentication_test.rb Tests laufen weiter (kein Regress)"
  artifacts:
    - path: test/system/devise_flows_test.rb
      provides: "E2E System-Tests fuer alle 4 Devise-Flows mit ActionMailer::Base.deliveries Token-Extraktion"
      min_lines: 150
  key_links:
    - from: test/system/devise_flows_test.rb
      to: test/support/mail_helpers.rb (Plan-01)
      via: "include MailHelpers via test_helper.rb (sauberer Include in ApplicationSystemTestCase, kein extend-im-setup)"
      pattern: "extract_confirmation_url\\|extract_reset_password_url"
    - from: test/system/devise_flows_test.rb
      to: app/views/devise/* (Forms)
      via: "Capybara fill_in + click_button gegen Devise-Default-Views"
      pattern: "fill_in.*click_button"
---

<objective>
Wave-3 — finaler E2E-Test-Layer (D-41-A Layer 4). System-Tests via Capybara + Selenium simulieren echte Browser-Flows fuer alle 4 D-41-D-Flows:
1. Sign-up → Confirmation-Mail → Click Confirm-Link → Logged-in
2. Forgot-Password → Reset-Mail → Click Reset-Link → Neues PW setzen → Login funktioniert
3. Eingeloggt → Account-Edit → PW-Change → Re-Login-Redirect → Notification-Mail vorhanden
4. Eingeloggt → Account-Edit → Email-Change → Confirmation-Mail an neue Adresse → Click bestaetigt → email gesetzt

Mail-Empfang erfolgt im Test-Adapter (ActionMailer::Base.deliveries) — keine echte SMTP-Verbindung. Token wird aus Mail-Body via Plan-01-Helper extrahiert, Capybara navigiert zur extrahierten URL. Letter_opener_web wird NICHT in System-Tests verwendet (Test-Env nutzt :test-Adapter, nicht :letter_opener_web).

WICHTIG: MailHelpers ist via Plan-01 test_helper.rb in ApplicationSystemTestCase included (BLOCKER-3-Fix). KEIN extend-im-setup-Workaround mehr noetig — `last_email`, `extract_confirmation_url(mail)`, `extract_reset_password_url(mail)` sind Instance-Methods auf jedem DeviseFlowsTest verfuegbar.

Final-Checkpoint (Human-Verify): Production-Smoke-Test mit echter Gmail-Inbox NACH Deploy — gemaess CONTEXT.md Anti-Pattern "Don't ship without echtem Mail-Roundtrip-Verifikation".

Purpose: D-41-A Layer-4 schliesst die 4-Layer-Pyramide. Diese Tests sind die Regression-Garantie fuer Devise-/Rails-Upgrades.

Output: test/system/devise_flows_test.rb mit 4 Browser-getriebenen E2E-Tests; Human-Verify-Checkpoint fuer Production-SMTP-Roundtrip nach Deploy.
</objective>

<execution_context>
@/Users/gullrich/DEV/carambus/carambus_master/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gullrich/DEV/carambus/carambus_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/CONTEXT.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-RESEARCH.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-01-PLAN.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-02-PLAN.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-03-PLAN.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-04-PLAN.md

@test/system/user_authentication_test.rb
@test/application_system_test_case.rb
@test/support/mail_helpers.rb
@app/views/devise/registrations/new.html.erb
@app/views/devise/passwords/new.html.erb
@app/views/devise/passwords/edit.html.erb
@app/views/devise/confirmations/new.html.erb

<interfaces>
<!-- Capybara System-Test Convention (aus user_authentication_test.rb): -->
```ruby
require "application_system_test_case"

class DeviseFlowsTest < ApplicationSystemTestCase
  # Capybara Standard-API:
  # visit "/path"
  # fill_in "Label or ID", with: "value"
  # check "Label", uncheck "Label"
  # click_button "Submit"
  # assert_text "Welcome"
  # assert_current_path "/some/path"
end
```

<!-- ActionMailer::Base.deliveries in System-Tests: -->
```ruby
# In ApplicationSystemTestCase (Test-Env) ist delivery_method = :test.
# letter_opener_web ist NUR development, NICHT test.
# Mail-Empfang via Plan-01-Helper:
clear_mail_queue
# ... user-action ...
url = extract_confirmation_url(last_email)
visit url  # Capybara navigiert zur extrahierten URL
```

<!-- Devise-Default Form-Fields (DE-Locale via devise.de.yml): -->
```
/users/sign_up:
  - Email           -> input[name="user[email]"]
  - Password        -> input[name="user[password]"]
  - Password confirmation -> input[name="user[password_confirmation]"]
  - Terms checkbox  -> input[name="user[terms_of_service]"][type="checkbox"]
  - Submit          -> input[type="submit"] mit value "Sign up" oder "Registrieren"

/users/password/new:
  - Email -> input[name="user[email]"]
  - Submit "Send me reset password instructions" / "Anweisungen zum Zuruecksetzen senden"

/users/password/edit?reset_password_token=...:
  - Password / Password confirmation
  - Submit "Change my password" / "Mein Passwort aendern"

/users/edit:
  - Current password (Pflicht)
  - Email / Password / Password confirmation
  - Submit "Update" / "Aktualisieren"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E2E System-Tests fuer alle 4 Devise-Flows</name>
  <files>test/system/devise_flows_test.rb</files>
  <behavior>
    4 System-Tests, jeder simuliert vollstaendigen Browser-Flow inkl. Mail-Token-Click-Through. Tests laufen mit :rack_test bzw. :headless_chrome (ApplicationSystemTestCase-Default des Projekts).

    Plan-01 Task-4 hat das Skeleton `test/system/devise_flows_test.rb` mit leerer DeviseFlowsTest-Klasse erstellt; dieser Task FUELLT die 4 Tests in das vorhandene Skeleton.

    BLOCKER-3-Fix: MailHelpers ist via test_helper.rb in ApplicationSystemTestCase included — KEIN extend-im-setup-Workaround. Methods `last_email`, `clear_mail_queue`, `extract_confirmation_url`, `extract_reset_password_url` sind direkt verfuegbar.
  </behavior>
  <read_first>
    - /Users/gullrich/DEV/carambus/carambus_master/test/application_system_test_case.rb (gesamtes File — verstehen welcher Driver konfiguriert ist + Test-Helpers)
    - /Users/gullrich/DEV/carambus/carambus_master/test/test_helper.rb (Plan-01 Task 2 hat `include MailHelpers` in ApplicationSystemTestCase eingefuegt — verifizieren)
    - /Users/gullrich/DEV/carambus/carambus_master/test/system/user_authentication_test.rb (gesamtes File — Vorbild fuer Sign-up-Flow + Locale-Konvention)
    - /Users/gullrich/DEV/carambus/carambus_master/test/support/mail_helpers.rb (aus Plan-01 — extract_confirmation_url / extract_reset_password_url)
    - /Users/gullrich/DEV/carambus/carambus_master/test/system/devise_flows_test.rb (Skeleton aus Plan-01 Task-4 — leere Klasse vorhanden, hier fuellen)
    - /Users/gullrich/DEV/carambus/carambus_master/app/views/devise/registrations/new.html.erb (Form-Field-IDs/Labels — fuer Capybara fill_in-Selektoren)
    - /Users/gullrich/DEV/carambus/carambus_master/app/views/devise/registrations/edit.html.erb (Form-Felder Account-Edit)
    - /Users/gullrich/DEV/carambus/carambus_master/app/views/devise/passwords/new.html.erb (Forgot-Form)
    - /Users/gullrich/DEV/carambus/carambus_master/app/views/devise/passwords/edit.html.erb (Reset-Form)
    - /Users/gullrich/DEV/carambus/carambus_master/test/fixtures/users.yml — Klartext-Password verifizieren:
      pruefen welcher Klartext-Password den fixture-Hash erzeugt. Falls Standard-Devise-Fixture vorhanden mit
      `password_digest: <%= User.new(password: 'password').password_digest %>` (test-env stretches=1) → Klartext = "password".
      Sonst expliziter Override im Test-setup: `@user.update!(password: 'TestPasswort123!')` und im Test mit
      `current_password: 'TestPasswort123!'` arbeiten.
  </read_first>
  <action>
    1. test/system/devise_flows_test.rb fuellen (Skeleton aus Plan-01 Task-4 existiert mit leerer Klasse). Sprache: Englisch fuer Test-Method-Namen (Standard-Rails-Konvention), aber Kommentare und Strings in mixed DE/EN je nach Locale-Pfad. Die Klasse `class DeviseFlowsTest < ApplicationSystemTestCase` ist bereits vorhanden — Inhalt einfuegen.
       ```ruby
       # frozen_string_literal: true

       require "application_system_test_case"

       # D-41-A Layer 4: E2E Browser-Tests fuer alle 4 Devise-Flows.
       # Mail-Empfang via ActionMailer::Base.deliveries (Test-Adapter) + Token-Extraktion
       # ueber Plan-01-Helper. KEIN echter SMTP-Aufruf in CI/Test.
       #
       # Locale: Tests pinnen sich auf :en (Sign-up-Form hat hartkodierten EN-Text in
       # Plan-02 ueber t() ersetzt — i18n-Wert ist exakt "I accept the Terms of Service").
       #
       # MailHelpers (`last_email`, `clear_mail_queue`, `extract_confirmation_url`,
       # `extract_reset_password_url`) ist via test_helper.rb (Plan-01) in
       # ApplicationSystemTestCase included — direkt aufrufbar, kein extend noetig.
       class DeviseFlowsTest < ApplicationSystemTestCase
         setup do
           clear_mail_queue
         end

         # ============================================================
         # Flow 1: Sign-up + Confirmation + Login
         # ============================================================
         test "user registers and confirms via email link" do
           email = "e2e-signup-#{SecureRandom.hex(4)}@example.test"
           password = "E2EPasswort123!"

           visit new_user_registration_path(locale: :en)

           fill_in "user[email]", with: email
           fill_in "user[password]", with: password
           fill_in "user[password_confirmation]", with: password
           check "I accept the Terms of Service"
           # FIXME: ggf. first_name/last_name auch noetig — pruefen via Capybara has_field?
           click_button(class: /submit|btn/) rescue click_on "Sign up"

           # Devise sendet confirmation_instructions
           mail = last_email
           refute_nil mail, "Confirmation-Mail muss versendet werden"
           assert_equal [email], mail.to

           confirmation_url = extract_confirmation_url(mail)
           refute_nil confirmation_url, "Confirmation-URL muss aus Mail-Body extrahierbar sein"

           # Im Test-Env hat die URL host:port aus default_url_options — Capybara visit
           # akzeptiert sowohl absolute URL als auch relative. Extrahiere Pfad+Query:
           uri = URI(confirmation_url)
           visit "#{uri.path}?#{uri.query}"

           # Nach Confirmation: User ist eingeloggt ODER zu Login-Seite redirected.
           # Devise-Default-Verhalten: redirect zu sign_in mit Flash "Your email address has been successfully confirmed."
           assert_text(/confirmed|bestaetigt/i, wait: 5)
         end

         # ============================================================
         # Flow 2: Forgot-Password + Reset + Login
         # ============================================================
         test "user resets password via forgot-password email link" do
           user = users(:valid)
           old_jti = user.jti

           visit new_user_password_path(locale: :en)
           fill_in "user[email]", with: user.email
           click_on "Send me reset password instructions"

           mail = last_email
           refute_nil mail, "Reset-Mail muss versendet werden"

           reset_url = extract_reset_password_url(mail)
           refute_nil reset_url
           uri = URI(reset_url)
           visit "#{uri.path}?#{uri.query}"

           fill_in "user[password]", with: "NeuesE2EPasswort123!"
           fill_in "user[password_confirmation]", with: "NeuesE2EPasswort123!"
           click_on "Change my password"

           # Erfolgreicher Reset: redirect + Flash + JTI rotiert (Plan-03 Callback)
           user.reload
           assert_not_equal old_jti, user.jti,
             "Plan-03 Callback muss feuern (E2E-Verifikation der Layer-1+2 Tests)"
         end

         # ============================================================
         # Flow 3: Change-Password eingeloggt + Re-Login Pflicht
         # ============================================================
         test "user changes password and is forced to re-login" do
           user = users(:valid)
           # NOTE Klartext-Password aus Fixture: siehe read_first. Falls fixture-Klartext
           # NICHT "password" ist, hier vor dem sign_in_via_form eine bekannte Variante
           # setzen: user.update!(password: "TestPasswort123!", password_confirmation: "TestPasswort123!")
           # und unten "password" durch "TestPasswort123!" ersetzen.
           sign_in_via_form(user.email, "password")
           old_jti = user.jti

           visit edit_user_registration_path(locale: :en)
           fill_in "user[current_password]", with: "password"
           fill_in "user[password]", with: "ChangedE2EPasswort123!"
           fill_in "user[password_confirmation]", with: "ChangedE2EPasswort123!"
           click_on "Update"

           # D-41-C Plan-04: sign_in_after_change_password=false → User ist abgemeldet
           # Folge-Request auf geschuetzte Seite redirected zu Login
           visit edit_user_registration_path(locale: :en)
           assert_current_path %r{/users/sign_in}, wait: 5

           # JTI rotiert (Plan-03 Callback feuert auch hier)
           user.reload
           assert_not_equal old_jti, user.jti

           # password_change-Notification wurde versendet
           change_mail = ActionMailer::Base.deliveries.find do |m|
             m.to == [user.email] && m.subject.to_s.match?(/password|passwort/i)
           end
           refute_nil change_mail, "password_change Notification-Mail muss vorhanden sein"
         end

         # ============================================================
         # Flow 4: Email-Change + Reconfirmation
         # ============================================================
         test "user changes email and confirms via reconfirmation link" do
           user = users(:valid)
           original_email = user.email
           new_email = "e2e-newemail-#{SecureRandom.hex(4)}@example.test"
           # NOTE Klartext-Password aus Fixture: siehe read_first. Ggf. Override im Setup.
           sign_in_via_form(original_email, "password")

           visit edit_user_registration_path(locale: :en)
           fill_in "user[email]", with: new_email
           fill_in "user[current_password]", with: "password"
           click_on "Update"

           user.reload
           # reconfirmable: alte email bleibt; neue in unconfirmed_email
           assert_equal original_email, user.email
           assert_equal new_email, user.unconfirmed_email

           # confirmation_instructions an NEUE Adresse
           confirm_mail = ActionMailer::Base.deliveries.find { |m| m.to == [new_email] }
           refute_nil confirm_mail, "Reconfirmation-Mail an neue Adresse fehlt"

           confirm_url = extract_confirmation_url(confirm_mail)
           uri = URI(confirm_url)
           visit "#{uri.path}?#{uri.query}"

           user.reload
           # Nach Confirm-Click: unconfirmed_email -> email
           assert_equal new_email, user.email,
             "Nach Reconfirmation-Click muss email auf neuen Wert gesetzt sein"
           assert_nil user.unconfirmed_email
         end

         private

         # Helper: programmatisches Sign-in via Login-Form (Capybara).
         # Wird verwendet um Tests deterministisch zu starten — sign_in-Devise-Helper
         # ist in System-Tests nicht direkt verfuegbar wie in Integration-Tests.
         def sign_in_via_form(email, password)
           visit new_user_session_path(locale: :en)
           fill_in "user[email]", with: email
           fill_in "user[password]", with: password
           click_on "Log in"
           # Warten bis Login-Redirect abgeschlossen
           assert_no_current_path %r{/users/sign_in}, wait: 5
         end
       end
       ```

       **WICHTIGE Adaptionen waehrend Implementierung (Executor klaert via Read von Existing-Files):**
       - Exakte Button-Texte aus devise.en.yml (`devise.passwords.new.send_me_reset_password_instructions` etc.) verifizieren — wenn YAML andere Strings hat (z.B. "Reset password"), den Test-String anpassen
       - Falls fixture-Passwort != "password" — siehe read_first: in `test/fixtures/users.yml` den `password_digest`-ERB-Block lesen; bei Bedarf im setup oder per Test `user.update!(password: "TestPasswort123!", password_confirmation: "TestPasswort123!")` + "password"-Literale durch konkreten Wert ersetzen
       - Falls Capybara-Driver Probleme mit URL-Visit hat (z.B. Selenium-Chrome blockt redirects): Test-Helper-Methode anpassen — Pfad nur statt absolute URL nutzen
       - Falls Tests durch concurrent transactional fixtures Issues haben: Hinweis in CLAUDE.md "no global use_transactional_tests=false" beachten — also keine globale Anpassung machen; statt dessen `user.update_columns(...)` fuer State-Resets nutzen
       - MailHelpers-Methods sind via Plan-01 test_helper.rb in ApplicationSystemTestCase included → `last_email`, `extract_confirmation_url(mail)`, `extract_reset_password_url(mail)` direkt aufrufbar. KEIN extend, kein Bind-Workaround.

    2. Lokal verifizieren: einzelner Test-Run zuerst:
       ```
       bin/rails test test/system/devise_flows_test.rb -n test_user_registers_and_confirms_via_email_link 2>&1 | tail -50
       ```
       Bei Failures: iterativ Form-Felder + Button-Texte korrigieren.
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_master && bin/rails test test/system/devise_flows_test.rb 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - test/system/devise_flows_test.rb enthaelt 4 `test "..."`-Blocks
    - Alle 4 System-Tests GREEN (exit 0)
    - Test "user registers and confirms via email link" — visit signup → fill terms_of_service → submit → Mail extrahiert → confirmation-URL navigiert → assert confirmed
    - Test "user resets password" — JTI-Rotation assertet (E2E-Verifikation Plan-03)
    - Test "user changes password and is forced to re-login" — assert_current_path /users/sign_in (E2E-Verifikation Plan-04 sign_in_after_change_password=false)
    - Test "user changes email and confirms via reconfirmation link" — assert email == new_email nach Confirm-Click
    - KEINE Regression in test/system/user_authentication_test.rb
    - Gesamte System-Test-Laufzeit < 90s
    - KEIN `extend MailHelpers`-Aufruf im File (sauberes Include via test_helper.rb statt extend-im-setup-Workaround)
  </acceptance_criteria>
  <done>E2E-Layer 4 vollstaendig: alle 4 Devise-Flows von Browser-Action bis Mail-Click-Through automated getestet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final Human-Verify — Production-Roundtrip nach Deploy (CONTEXT.md Anti-Pattern)</name>
  <files>(human-verify checkpoint — no files modified; deferred until after Production-Deploy)</files>
  <read_first>
    - /Users/gullrich/DEV/carambus/carambus_master/.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/CONTEXT.md (Anti-Patterns Section: "Don't ship without echtem Mail-Roundtrip-Verifikation")
    - /Users/gullrich/DEV/carambus/carambus_master/.agents/skills/scenario-management/SKILL.md (Deployment-Workflow ueber Scenarios)
  </read_first>
  <action>
    Alle 4 Devise-Flows sind nun automated getestet auf 4 Layern (Model, Controller, Mailer, E2E).
    Aber CONTEXT.md Anti-Patterns warnt: "Don't ship without echtem Mail-Roundtrip-Verifikation. Mindestens einmal nach Deploy: echte Registrierung mit echter Inbox-Adresse. Tests reichen nicht — Gmail-SMTP-Auth ist environment-spezifisch."

    Dieser Checkpoint markiert den Deployment-Cut. Master ist tested; carambus_master kann
    via Scenario-Workflow (.agents/skills/scenario-management/SKILL.md) auf carambus_phat /
    carambus_api / carambus_bcw deployed werden. NACH Deploy: 1x manueller Roundtrip auf
    einer Production-Domain mit echter Gmail-Inbox.


    HUMAN-VERIFY-INSTRUKTIONEN (siehe how-to-verify unten):
  </action>
  <how-to-verify>
    Diese Verifikation findet NACH dem Production-Deploy statt (kann zeitlich verzoegert
    sein). Schritte:

    1. Deploy via Scenario-Workflow (separater Task, nicht Teil dieses Plans). Verifizieren:
       - production-Logs zeigen smtp_guard hat nicht gefeuert (kein "FATAL: SMTP-ENV").
       - production-Logs zeigen `[MAILER]`-tagged Lines fuer Test-Mails.

    2. Echter Production-Smoke-Test (Beispiel: https://www.carambus.de):
       a) /users/sign_up: Test-Account anlegen mit ECHTER Inbox-Adresse (deine Gmail/Mailbox)
       b) Inbox checken: confirmation_instructions Mail muss ankommen (binnen 2 Min)
          - Sender: ENV["SMTP_USERNAME"]-Wert (z.B. "carambus.app@gmail.com")
          - Spam-Folder kontrollieren
       c) Confirmation-Link klicken: User wird confirmed + redirected zu Login
       d) Einloggen, dann /users/password/new (Forgot-Form)
       e) Reset-Mail in Inbox? Klick auf Link, neues PW setzen
       f) Erneutes Login mit neuem PW funktioniert

    3. Falls Mail NICHT ankommt (Production-Roundtrip failed):
       - production.log: `grep '\[MAILER\]' log/production.log | tail -10`
       - SMTP-Auth-Errors? `grep -E "SMTPAuthenticationError|SMTPServerBusy" log/production.log`
       - SPF/DKIM-Konfiguration der Sender-Domain pruefen (Gmail-Workspace-Einstellungen)

    4. Verbleibende deferred Items dokumentieren:
       - DeviseMailJob-Retry-Job ist nun in Plan-04 Task-2 implementiert (D-41-B woertlich erfuellt) — falls Production-Logs transient SMTP-Errors zeigen, sollte das im Job sichtbar als Retry-Job auftauchen
       - Falls SPF/DKIM gebraucht: separate Ops-Task (gehoert nicht in Phase 41)

    Wenn 2a-2f alle erfolgreich: "approved — Production-Roundtrip verifiziert".
    Wenn 2c oder spaeter scheitert: konkrete Schritt-Nummer + production.log-Snippet.
  </how-to-verify>
  <resume-signal>"approved — Production-Roundtrip verifiziert" oder konkrete Fehlerausgabe</resume-signal>
  <verify>
    <automated>echo "human-verify checkpoint — production-deploy-roundtrip deferred"</automated>
  </verify>
  <acceptance_criteria>
    - User markiert "approved — Production-Roundtrip verifiziert" nach manuellem Smoke-Test mit echter Gmail-Inbox
    - ODER: Falls Roundtrip failed: production.log-Snippet + Schritt-Nummer dokumentiert; Folge-Plan repariert
  </acceptance_criteria>
  <done>Production-Mail-Roundtrip verifiziert (deferred bis nach Deploy); Phase 41 ist verlassbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Capybara Test-Browser → Rails-App (Test-Env) | Isolierte Session pro Test; rack_test/selenium reset zwischen Tests |
| ActionMailer.deliveries → Test-Code Token-Extraktion | Test-only; Helper-Regex limitiert auf bekannte Path-Strukturen (/confirmation, /password/edit) |
| Echte Inbox → Production-User (NACH Deploy) | Out-of-Scope-Phase-41; Human-Verify-Step verifiziert manuell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05-01 | Information Disclosure | Capybara-Session-Cookies persist zwischen Tests | mitigate | ApplicationSystemTestCase Default-Cleanup; jeder Test startet mit visit (frischer Browser-Tab); kein Cross-Test-Cookie-Leak |
| T-41-05-02 | Spoofing | Test-Mail-Extraktor liest fremde Mails | accept | ActionMailer::Base.deliveries ist Process-local in Tests; kein Cross-Process-Risiko |
| T-41-05-03 | Tampering | extract_*_url Regex-Bypass via Body-Injection | accept | Token wird von Devise generiert; kein User-Input flieht in Body; Test-Env nicht in Production |
| T-41-05-04 | Repudiation | Production-Mail-Receipt nicht nachvollziehbar | mitigate | Plan-04 Mail-Observer loggt jeden Versand; Human-Verify-Step prueft Inbox-Receipt |
</threat_model>

<verification>
- `bin/rails test test/system/devise_flows_test.rb` exit 0 — alle 4 Tests GREEN
- `bin/rails test test/system/user_authentication_test.rb` exit 0 — kein Regress in pre-existing System-Tests
- `bin/rails test test/system/devise_flows_test.rb test/system/user_authentication_test.rb` Gesamtlaufzeit < 3 Minuten
- `bundle exec standardrb test/system/devise_flows_test.rb` exit 0
- `bundle exec erblint --lint-all` exit 0 (keine ERB-Lint-Issues durch Plan-02 View-Aenderungen)
- Human-Verify-Checkpoint Task-2 erst NACH Deploy markierbar — kann als deferred Approval gefuehrt werden in 41-05-SUMMARY.md
</verification>

<success_criteria>
- D-41-A Layer 4 (System-Tests E2E) vollstaendig: 4 Tests pinnen alle 4 D-41-D Flows
- Cumulative Phase 41 Test-Coverage:
  - Layer 1 (Model): 5+ Tests (user_test.rb JTI-Section)
  - Layer 2 (Controller): 10+ Tests (passwords, confirmations, registrations)
  - Layer 3 (Mailer): 5+ Tests (devise_mailer_test.rb)
  - Layer 4 (System E2E): 4 Tests (devise_flows_test.rb)
  - Job (DeviseMailJob): 4 Tests (devise_mail_job_test.rb, Plan-04)
  - **TOTAL: 28+ neue Tests in Phase 41**
- Human-Verify-Checkpoint dokumentiert (Roundtrip-Test als deferred Approval falls Deploy spaeter)
- Phase 41 ist verlassbar — alle D-41-A bis D-41-D adressiert
</success_criteria>

<output>
After completion, create `.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-05-SUMMARY.md` mit:
- Final Test-Count Phase 41: pro Layer mit Test-Filenamen
- Liste der 4 E2E-Tests in devise_flows_test.rb mit Kern-Assertion jedes Tests
- Production-Roundtrip-Status: deferred bis nach Deploy ODER bereits durchgefuehrt
- Verbleibende Deferred-Items (SPF/DKIM-Setup falls noetig)
- Cross-Repo Sync-Hinweis (bcw/phat/api Scenario-Deploy nach Phase 41 abgeschlossen)
</output>
</content>
