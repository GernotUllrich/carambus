---
phase: 41
plan: 02
type: execute
wave: 1
depends_on: [41-01]
files_modified:
  - app/views/devise/registrations/new.html.erb
  - app/controllers/registrations_controller.rb
  - config/locales/devise.de.yml
  - config/locales/devise.en.yml
  - test/controllers/registrations_controller_test.rb
  - test/mailers/devise_mailer_test.rb
autonomous: true
requirements:
  - REQ-41-04   # Registrierungs-Flow funktional: POST /users -> confirmation_instructions versendet
  - REQ-41-05   # terms_of_service-Checkbox i18n-versorgt (devise.registrations.new.terms_html bereits in DE/EN)
  - REQ-41-06   # invisible_captcha server-seitig im RegistrationsController#create enforced
threat_refs:
  - T-41-02-01  # Honeypot-Bypass bei Registration
  - T-41-02-02  # Account-Enumeration via Resend-Mail
  - T-41-02-03  # Mail-Sender-Mismatch beim Versand (SPF/DKIM Risiko)
must_haves:
  truths:
    - "POST /users mit guelt. params + akzeptierten Terms erzeugt User + versendet exakt 1 confirmation_instructions Mail"
    - "POST /users mit gesetztem Honeypot-Field wird abgewiesen (Status 200, kein User-Insert, keine Mail)"
    - "Terms-of-Service-Checkbox ist via t() i18n-versorgt (DE+EN)"
    - "Confirmation-Mail-Sender matcht Devise.mailer_sender (kein Mismatch mit SMTP-Auth)"
  artifacts:
    - path: app/views/devise/registrations/new.html.erb
      provides: "i18n-Label fuer terms_of_service Checkbox via t('devise.registrations.new.terms_html')"
      contains: "terms_of_service"
    - path: app/controllers/registrations_controller.rb
      provides: "invisible_captcha-Macro auf #create"
      contains: "invisible_captcha"
  key_links:
    - from: app/controllers/registrations_controller.rb
      to: invisible_captcha gem
      via: "invisible_captcha only: [:create], honeypot: :subtitle"
      pattern: "invisible_captcha"
    - from: app/views/devise/registrations/new.html.erb
      to: config/locales/devise.de.yml
      via: "t('devise.registrations.new.terms_html')"
      pattern: "t\\(.devise\\.registrations\\.new\\.terms"
---

<objective>
Registrierungs-Flow auf produktiven Standard heben: POST /users versendet zuverlaessig die confirmation_instructions Mail, terms_of_service-Checkbox ist i18n-versorgt, und invisible_captcha wird server-seitig erzwungen (heute nur im View gerendert — siehe RESEARCH.md, Skip-Test in users/registrations_controller_test.rb:52). Mailer-Sender-Diskrepanz (ApplicationMailer.default_from vs. Devise.mailer_sender) wird in Plan-04 vereinheitlicht; in diesem Plan stellen wir nur sicher, dass die Mail GENERIERT und VERSENDET wird (E2E-Verifikation im Plan-05 mit echtem Mail-Roundtrip).

Purpose: D-41-D #1 Flow (Registrierung+Confirmation) — heute defekt laut User. Mail-Generierung ist im Code OK (siehe Plan 41-01 Mailer-Tests); die offenen Punkte sind Controller-Honeypot-Enforcement und View-i18n.

Output: Hardened Registrations-Controller mit Honeypot-Guard; i18n-versorgte Terms-Checkbox; erweitertes Layer-2-Test-Set; gruener Layer-3-Test fuer Registrierungs-getriggerte confirmation_instructions Mail.
</objective>

<execution_context>
@/Users/gullrich/DEV/carambus/carambus_master/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gullrich/DEV/carambus/carambus_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/CONTEXT.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-RESEARCH.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-01-PLAN.md

@app/controllers/registrations_controller.rb
@app/views/devise/registrations/new.html.erb
@app/models/user.rb
@config/locales/devise.de.yml
@config/locales/devise.en.yml
@test/controllers/registrations_controller_test.rb
@test/controllers/users/registrations_controller_test.rb
@test/system/user_authentication_test.rb

<interfaces>
<!-- invisible_captcha 2.0 API (aus Gemfile + Gem-Doku): -->
```ruby
# Im Controller:
class RegistrationsController < Devise::RegistrationsController
  invisible_captcha only: [:create], honeypot: :subtitle
  # honeypot: :subtitle = Name des verborgenen Felds (Default).
  # Bei gefuelltem Feld: redirect mit flash[:error] = I18n.t('invisible_captcha.sentence_for_humans').
end
```

<!-- Bestehende invisible_captcha-View-Integration (registrations/new.html.erb Zeile 44): -->
```erb
<%= invisible_captcha %>
```
Generiert ein <input type="text" name="subtitle" style="display:none"> Honeypot.

<!-- Existing User-Validation: -->
```ruby
validates :terms_of_service, acceptance: true, on: :create
attr_accessor :terms_of_service
# acceptance: true akzeptiert truthy-Werte ("1", true, "true").
# Bei NICHT-akzeptiert: validation error mit i18n-Schluessel :acceptance.
```

<!-- Devise mailer_sender (initializer): -->
```ruby
config.mailer_sender = ENV["SMTP_USERNAME"] || "no-reply@carambus.de"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Controller-Hardening — invisible_captcha + Mail-Versand-Assertion in Tests</name>
  <files>app/controllers/registrations_controller.rb, test/controllers/registrations_controller_test.rb, test/controllers/users/registrations_controller_test.rb, test/mailers/devise_mailer_test.rb</files>
  <behavior>
    RED -> GREEN:
    1. Im Layer-3-Test ein NEUER Test "POST /users versendet confirmation_instructions Mail" assertiert, dass nach POST /users (mit valid params) genau 1 Mail in deliveries landet. Initial RED falls Devise-Default `:registerable` heute aus irgendeinem Grund die Mail unterdrueckt (z.B. via @user.skip_confirmation!-Override) — was Research ausschliesst, aber CHARAKTERISIERUNG.
    2. Im Layer-2-Test der invisible_captcha-Honeypot-Skip in test/controllers/users/registrations_controller_test.rb:52 wird durch ECHTEN Test ersetzt: gefuelltes :subtitle-Feld -> kein User erzeugt, kein Mail-Versand.
  </behavior>
  <read_first>
    - /Users/gullrich/DEV/carambus/carambus_master/app/controllers/registrations_controller.rb (gesamtes File)
    - /Users/gullrich/DEV/carambus/carambus_master/test/controllers/registrations_controller_test.rb (gesamtes File — Pattern fuer existierende Tests)
    - /Users/gullrich/DEV/carambus/carambus_master/test/controllers/users/registrations_controller_test.rb (Zeilen 40-80 — der geskippte Honeypot-Test)
    - /Users/gullrich/DEV/carambus/carambus_master/Gemfile (Zeile mit "invisible_captcha" — Version verifizieren: ~> 2.0)
  </read_first>
  <action>
    1. app/controllers/registrations_controller.rb: NACH `before_action :configure_permitted_parameters` (Zeile 4) folgende Zeile einfuegen:
       ```ruby
       # D-41-D Registrierung: server-seitiger Honeypot-Guard. View rendert das verborgene
       # Feld via <%= invisible_captcha %> (registrations/new.html.erb). Bots, die das Feld
       # fuellen, werden hier auf #new redirected + flash[:error].
       invisible_captcha only: [:create], honeypot: :subtitle
       ```
       Falls die Methode `:create` nicht explizit ueberschrieben ist (heute via Devise-Default), bleibt das Macro wirksam (greift vor controller-action ein).

    2. test/controllers/users/registrations_controller_test.rb: Den geskippten Test umschreiben. Aktueller Skip (per RESEARCH.md Zeile 52) ersetzen durch echten Test. KONKRET — falls der Test heute so aussieht:
       ```ruby
       test "honeypot is filled and user creation fails" do
         skip "invisible_captcha controller-macro not yet active"
         # ...
       end
       ```
       Ersetzen durch:
       ```ruby
       test "POST /users mit gefuelltem honeypot wird abgewiesen (kein User, keine Mail)" do
         ActionMailer::Base.deliveries.clear
         assert_no_difference -> { User.count } do
           assert_no_difference -> { ActionMailer::Base.deliveries.size } do
             post user_registration_path, params: {
               user: {
                 email: "bot-#{SecureRandom.hex(4)}@example.test",
                 password: "BotPasswort123!",
                 password_confirmation: "BotPasswort123!",
                 first_name: "Bot",
                 last_name: "Spam",
                 terms_of_service: "1"
               },
               subtitle: "filled-by-bot"  # Honeypot getrigger
             }
           end
         end
         # invisible_captcha-Default-Verhalten: redirect (verschleiert Erfolg/Fehler vor Bots)
         assert_response :redirect
       end
       ```

    3. test/controllers/registrations_controller_test.rb: NEUER Test ans Ende vor letztem `end` einfuegen:
       ```ruby
       test "POST /users mit gueltigen Params versendet 1 confirmation_instructions Mail" do
         ActionMailer::Base.deliveries.clear
         email = "neu-#{SecureRandom.hex(4)}@example.test"
         assert_difference -> { User.count }, 1 do
           assert_difference -> { ActionMailer::Base.deliveries.size }, 1 do
             post user_registration_path, params: {
               user: {
                 email: email,
                 password: "GueltigesPasswort123!",
                 password_confirmation: "GueltigesPasswort123!",
                 first_name: "Neu",
                 last_name: "User",
                 terms_of_service: "1"
               }
             }
           end
         end
         mail = ActionMailer::Base.deliveries.last
         assert_equal [email], mail.to
         assert_match %r{/confirmation\?confirmation_token=}, mail.body.to_s
       end

       test "POST /users ohne terms_of_service-Akzeptanz wird abgewiesen" do
         ActionMailer::Base.deliveries.clear
         assert_no_difference -> { User.count } do
           assert_no_difference -> { ActionMailer::Base.deliveries.size } do
             post user_registration_path, params: {
               user: {
                 email: "no-terms-#{SecureRandom.hex(4)}@example.test",
                 password: "GueltigesPasswort123!",
                 password_confirmation: "GueltigesPasswort123!",
                 first_name: "No",
                 last_name: "Terms"
                 # terms_of_service ABSICHTLICH NICHT gesetzt
               }
             }
           end
         end
         # Devise rendert :new mit Validation-Error
         assert_response :unprocessable_entity
       end
       ```

    4. test/mailers/devise_mailer_test.rb: KEINE Mods in diesem Task (Mailer-Tests bleiben aus Plan-01); Controller-Test deckt den E2E-Pfad fuer POST /users -> Mail ab.
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_master && bin/rails test test/controllers/registrations_controller_test.rb test/controllers/users/registrations_controller_test.rb 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "invisible_captcha only: \[:create\]" app/controllers/registrations_controller.rb` == 1
    - `grep -c "skip " test/controllers/users/registrations_controller_test.rb` == 0 (KEINE skips mehr fuer Honeypot-Test)
    - Neue Tests "POST /users mit gueltigen Params versendet 1 confirmation_instructions Mail" und "POST /users ohne terms_of_service-Akzeptanz wird abgewiesen" sind GREEN
    - Honeypot-Test "POST /users mit gefuelltem honeypot wird abgewiesen" ist GREEN
    - `bin/rails test test/controllers/registrations_controller_test.rb test/controllers/users/registrations_controller_test.rb` exit-code 0
    - KEINE Regression in pre-existing Tests in diesen 2 Files (alle gruen)
  </acceptance_criteria>
  <done>Honeypot-Guard server-seitig aktiv; Registrierungs-Mail-Versand HTTP-getestet; terms_of_service-Validation getestet.</done>
</task>

<task type="auto">
  <name>Task 2: View-i18n — terms_of_service-Label via t() versorgt (DE+EN)</name>
  <files>app/views/devise/registrations/new.html.erb, config/locales/devise.de.yml, config/locales/devise.en.yml, test/system/user_authentication_test.rb</files>
  <read_first>
    - /Users/gullrich/DEV/carambus/carambus_master/app/views/devise/registrations/new.html.erb (gesamtes File — Zeile 44+ mit invisible_captcha und Zeile 48-49 mit terms_of_service-Checkbox)
    - /Users/gullrich/DEV/carambus/carambus_master/config/locales/devise.de.yml (Suchen nach "registrations:" — Section verifizieren)
    - /Users/gullrich/DEV/carambus/carambus_master/config/locales/devise.en.yml (gleiche Section)
    - /Users/gullrich/DEV/carambus/carambus_master/test/system/user_authentication_test.rb (Zeile 9 + Zeile 14 — check 'I accept the Terms of Service')
  </read_first>
  <action>
    1. config/locales/devise.de.yml: In der Section `de.devise.registrations.new` einen NEUEN Key einfuegen:
       ```yaml
       # Innerhalb de.devise.registrations.new (alphabetisch oder am Block-Ende):
       terms_acceptance: "Ich akzeptiere die Nutzungsbedingungen"
       ```
       FALLS bereits ein `terms_html`-Key existiert (Research erwaehnt das), dort orientieren UND ggf. einen separaten `terms_acceptance`-Key fuer die reine Checkbox-Beschriftung anlegen. Existierender Schluessel `terms_html` ist fuer Link/HTML-Markup; `terms_acceptance` ist plain text fuer das Label.

    2. config/locales/devise.en.yml: Korrespondierend in `en.devise.registrations.new`:
       ```yaml
       terms_acceptance: "I accept the Terms of Service"
       ```
       Exakter englischer Text MUSS mit aktuellem hartkodiertem View-String uebereinstimmen ("I accept the Terms of Service"), damit `user_authentication_test.rb:14` (`check 'I accept the Terms of Service'`) WEITERHIN funktioniert.

    3. app/views/devise/registrations/new.html.erb: Die Zeile mit der terms_of_service-Checkbox (laut Research ca. Zeile 48) umbauen. Aus:
       ```erb
       <%= f.check_box :terms_of_service %>
       <%= f.label :terms_of_service, "I accept the Terms of Service" %>
       ```
       Wird:
       ```erb
       <%= f.check_box :terms_of_service %>
       <%= f.label :terms_of_service, t("devise.registrations.new.terms_acceptance") %>
       ```
       FALLS die heutige Markup-Struktur abweicht (z.B. Label-Inline mit Tailwind-Klassen), die Klassen + Wrapper-Tags unveraendert lassen und NUR den String durch den t()-Call ersetzen.

    4. test/system/user_authentication_test.rb: KEINE Aenderung noetig — Test setzt `visit new_user_registration_path(locale: :en)` und der EN-i18n-Wert ist exakt "I accept the Terms of Service" -> identisches Capybara-Verhalten.

    5. KEIN Restart Rails noetig fuer i18n-yml-Files in Dev (in Test-Env wird I18n.load_path bei jedem Test-Run frisch geladen).
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_master && grep -q "terms_acceptance" config/locales/devise.de.yml && grep -q "terms_acceptance" config/locales/devise.en.yml && grep -q "terms_acceptance" app/views/devise/registrations/new.html.erb && bin/rails test test/controllers/registrations_controller_test.rb 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "terms_acceptance" config/locales/devise.de.yml` == 1
    - `grep -c "terms_acceptance" config/locales/devise.en.yml` == 1
    - `grep -c 't("devise.registrations.new.terms_acceptance")' app/views/devise/registrations/new.html.erb` == 1 (ODER mit single-quotes — `grep -c "terms_acceptance"` == 1 reicht)
    - `grep -c '"I accept the Terms of Service"' app/views/devise/registrations/new.html.erb` == 0 (hartkodierter String entfernt)
    - `bin/rails test test/system/user_authentication_test.rb` exit 0 (EN-Pfad funktioniert weiter, da i18n-Wert exakt identisch)
    - `bin/rails test test/controllers/registrations_controller_test.rb` exit 0 (Registrierungs-Tests aus Task 1 weiter gruen)
  </acceptance_criteria>
  <done>Terms-Label i18n-versorgt; System-Test (EN) bleibt gruen; DE-User sieht "Ich akzeptiere die Nutzungsbedingungen".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymer Browser → POST /users | Untrusted Input crosses here; Honeypot + Devise-Validation muessen vor User-Insert greifen |
| Devise-Mailer → SMTP-Gateway | Sender-Header muss mit SMTP-Auth-User uebereinstimmen (sonst Spoofing-Indikator fuer Gmail) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Spoofing | RegistrationsController#create | mitigate | invisible_captcha-Macro mit honeypot:`:subtitle` aktiv; Test "honeypot is filled" GREEN |
| T-41-02-02 | Information Disclosure | confirmation_instructions resend (POST /users/confirmation aus Plan-01) | accept | Devise-paranoid_messages liefert generische Antwort; Account-Enumeration via 2xx vs 4xx Difference minimiert. Wird in Plan-04 evaluiert ob `paranoid: true` zusaetzlich aktiviert werden soll — heute Default-akzeptiert |
| T-41-02-03 | Spoofing | confirmation_instructions Mail-From | accept (defer to Plan-04) | Sender-Diskrepanz ApplicationMailer.default_from vs. Devise.mailer_sender wird in Plan-04 vereinheitlicht; Plan-02 verifiziert nur dass Mail VERSENDET wird |
| T-41-02-04 | Tampering | terms_of_service-Validation | mitigate | `validates :terms_of_service, acceptance: true, on: :create` lockt die Pflicht; Test "POST /users ohne terms_of_service" GREEN |
| T-41-02-05 | Repudiation | User-Insert ohne Confirmation-Mail | mitigate | Test "POST /users mit gueltigen Params versendet 1 confirmation_instructions Mail" GREEN — keine stille User-Erstellung ohne Mail |
</threat_model>

<verification>
- `bin/rails test test/controllers/registrations_controller_test.rb test/controllers/users/registrations_controller_test.rb test/mailers/devise_mailer_test.rb` exit 0
- `bin/rails test test/system/user_authentication_test.rb` exit 0 (EN-Pfad bleibt)
- `bundle exec standardrb app/controllers/registrations_controller.rb` exit 0
- `bundle exec erblint app/views/devise/registrations/new.html.erb` exit 0
- KEINE Plan-04-Sender-Aenderungen hier — `mailer_sender` unveraendert in devise.rb
</verification>

<success_criteria>
- D-41-D Flow 1 (Registrierung + Confirmation) ist Layer-2 + Layer-3 GREEN getestet
- Server-seitiger Honeypot-Guard aktiv (Test "POST /users mit gefuelltem honeypot wird abgewiesen" GREEN)
- terms_of_service-Checkbox i18n-versorgt (DE+EN)
- EN-System-Test `user_authentication_test.rb` weiter gruen (kein Capybara-Selektor-Bruch)
- Bestehende JWT-Kompatibilitaet unveraendert (Plan-02 fasst keinen JWT-Code an — D-41-C-relevante Aenderungen liegen in Plan-03/04)
</success_criteria>

<output>
After completion, create `.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-02-SUMMARY.md` mit:
- Honeypot-Test Status (vorher skipped, jetzt GREEN)
- Test-Count nach Plan-02: 4 in registrations_controller_test.rb + die existierenden + Honeypot-Test in users/registrations_controller_test.rb
- i18n-Keys hinzugefuegt: `devise.registrations.new.terms_acceptance` (DE+EN)
- Offenes Item Plan-04: Sender-Diskrepanz ApplicationMailer.default_from vs. Devise.mailer_sender weiterhin offen
</output>
</content>
</invoke>