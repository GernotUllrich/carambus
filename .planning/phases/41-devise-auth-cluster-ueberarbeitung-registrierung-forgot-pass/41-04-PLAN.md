---
phase: 41
plan: 04
type: execute
wave: 2
depends_on: [41-01, 41-02, 41-03]
files_modified:
  - config/initializers/devise.rb
  - config/initializers/smtp_guard.rb
  - config/initializers/mail_observer.rb
  - app/jobs/devise_mail_job.rb
  - app/models/user.rb
  - app/mailers/application_mailer.rb
  - app/controllers/registrations_controller.rb
  - test/controllers/registrations_controller_test.rb
  - test/mailers/devise_mailer_test.rb
  - test/jobs/devise_mail_job_test.rb
autonomous: false
requirements:
  - REQ-41-10   # Change-Password-Flow: PATCH /users mit current_password+password aendert PW UND sendet password_change-Notification
  - REQ-41-11   # sign_in_after_change_password=false → User muss nach PW-Change neu einloggen (D-41-C semantisch durchsetzen)
  - REQ-41-12   # Mail-Sender vereinheitlicht: Devise.mailer_sender == ApplicationMailer.default_from = ENV["SMTP_USERNAME"]||Fallback
  - REQ-41-13   # SMTP-Haertung: Fail-Fast-Guard (Production) + Mail-Delivery-Observer (Logging) + Retry-Job (transient SMTP-Errors) + Bounce-Discard (permanent)
  - REQ-41-14   # Email-Change Reconfirmation: PATCH email → 1 confirmation_instructions an neue Adresse, kein Login-Bruch
threat_refs:
  - T-41-04-01  # Session-Hijacking nach PW-Change (sign_in_after_change_password=true blockiert Revoke-Effekt)
  - T-41-04-02  # SMTP-Credential-Leak via Logs
  - T-41-04-03  # Sender-Spoofing via SPF/DKIM-Mismatch
  - T-41-04-07  # Mail-Loss bei transienten SMTP-Errors (Net::SMTPAuthenticationError, SMTPServerBusy) — D-41-B Retry-Pflicht
  - T-41-04-08  # Mail-Loss bei permanenten Bounces (Net::SMTPFatalError) — Error-Trapping-Pflicht (D-41-B)
must_haves:
  truths:
    - "PATCH /users mit aktuellem PW + neuem PW versendet 1 password_change-Notification Mail UND rotiert User.jti"
    - "Nach erfolgreichem PW-Change ist User NICHT mehr eingeloggt (sign_in_after_change_password=false)"
    - "ApplicationMailer.default_from == Devise.mailer_sender (kein From-Header-Mismatch)"
    - "Production-Boot scheitert fail-fast wenn ENV['SMTP_USERNAME'] oder ENV['SMTP_PASSWORD'] fehlt"
    - "MailDeliveryObserver loggt Success/Fail jeder ActionMailer-Delivery (tagged 'MAILER')"
    - "Devise-Mails laufen via DeviseMailJob (deliver_later) — transient SMTP-Errors triggern Retry, permanent SMTPFatalError werden mit Logging verworfen (Bounce-Handling)"
    - "Email-Change: PATCH /users mit neuer email + current_password → confirmation_instructions an neue Adresse, alter Login bleibt funktional"
  artifacts:
    - path: config/initializers/smtp_guard.rb
      provides: "Production Fail-Fast wenn SMTP-ENV fehlt"
      contains: "raise"
    - path: config/initializers/mail_observer.rb
      provides: "MailDeliveryObserver + register_observer/register_interceptor fuer ActionMailer (success + failure)"
      contains: "register_observer"
    - path: app/jobs/devise_mail_job.rb
      provides: "ActiveJob mit retry_on (Net::SMTPAuthenticationError, Net::SMTPServerBusy) + discard_on (Net::SMTPFatalError) fuer Devise-Mails (D-41-B Retry + Bounce-Handling)"
      contains: "retry_on"
    - path: app/models/user.rb
      provides: "send_devise_notification-Override: queued ueber DeviseMailJob.perform_later statt synchronem deliver_now"
      contains: "DeviseMailJob"
    - path: app/mailers/application_mailer.rb
      provides: "default from: Devise.mailer_sender (kein OpenStruct.support_email-Diskrepanz)"
      contains: "Devise.mailer_sender"
    - path: config/initializers/devise.rb
      provides: "config.sign_in_after_change_password = false (D-41-C semantische Durchsetzung)"
      contains: "sign_in_after_change_password = false"
    - path: test/jobs/devise_mail_job_test.rb
      provides: "Retry- und Discard-Verhalten getestet via ActiveJob::TestHelper + perform_enqueued_jobs"
      min_lines: 40
  key_links:
    - from: app/mailers/application_mailer.rb
      to: config/initializers/devise.rb
      via: "default from: -> { Devise.mailer_sender } (Lambda fuer Lazy-Init-Order)"
      pattern: "Devise.mailer_sender"
    - from: app/controllers/registrations_controller.rb
      to: app/models/user.rb (after_update rotate_jti_on_password_change!)
      via: "PATCH /users => update_resource => user.save => Callback feuert"
      pattern: "saved_change_to_encrypted_password"
    - from: config/initializers/mail_observer.rb
      to: ActionMailer::Base
      via: "ActionMailer::Base.register_observer(MailDeliveryObserver)"
      pattern: "register_observer"
    - from: app/models/user.rb (#send_devise_notification)
      to: app/jobs/devise_mail_job.rb
      via: "DeviseMailJob.perform_later(...) statt devise_mailer.send(notification, ...).deliver_now"
      pattern: "DeviseMailJob"
    - from: app/jobs/devise_mail_job.rb
      to: ActiveJob queue
      via: "retry_on Net::SMTPAuthenticationError/SMTPServerBusy, discard_on Net::SMTPFatalError"
      pattern: "retry_on\\|discard_on"
---

<objective>
Drei verbleibende Themen abdecken (D-41-D Flows 3+4 + D-41-B Haertung):

1. **Change-Password-Flow + JTI-Rotation-Semantik**: `config.sign_in_after_change_password = false` setzen. Heute ist der Devise-Default `true` → `bypass_sign_in` setzt nach PW-Change eine neue Session, der User merkt nichts. Mit `false` muss der User nach PW-Change neu einloggen — was D-41-C-konsistent ist ("User muss sich auf allen Geraeten neu einloggen"). JTI-Rotation passiert AUTOMATISCH via Plan-03-Callback (`after_update :rotate_jti_on_password_change!`).

2. **Email-Change-Reconfirmation**: D-41-D #4. PATCH /users mit neuer email triggert (per reconfirmable=true) confirmation_instructions an die NEUE Adresse. Tests pinnen das Verhalten — kein neuer Code-Pfad, nur Test-Coverage.

3. **SMTP-Haertung (D-41-B vollstaendig)**:
   - **Fail-Fast in Production**: `config/initializers/smtp_guard.rb` raises wenn ENV fehlt — verhindert dass Production mit kaputtem Mailer startet.
   - **Mail-Delivery-Observer**: `config/initializers/mail_observer.rb` registriert MailDeliveryObserver fuer strukturiertes Logging (tagged 'MAILER') — Success UND Failure.
   - **Retry-Pattern via ActiveJob (D-41-B Wortlaut "Retry-Logik bei SMTP-Fehlern (transient vs. permanent)")**: `app/jobs/devise_mail_job.rb` mit `retry_on Net::SMTPAuthenticationError`/`Net::SMTPServerBusy` (transient, exponential backoff) und `discard_on Net::SMTPFatalError` (permanent → Bounce-Handling per Logging-Discard). User-Modell ueberschreibt `send_devise_notification` und queued ueber den Job statt synchron zu liefern.
   - **Sender-Angleichung**: `ApplicationMailer.default from:` matcht `Devise.mailer_sender` — verhindert SPF/DKIM-Mismatch und Gmail-Spam-Klassifizierung.

Purpose: Verbleibende User-Anforderungen aus CONTEXT.md schliessen — alle 4 D-41-* Decisions vollstaendig adressiert nach diesem Plan. D-41-B war im Vorgaenger-Plan auf Fail-Fast + Observer reduziert; Revision erweitert auf Retry + Bounce-Handling wie woertlich gefordert.

Output: Devise-Config angepasst (sign_in_after_change_password=false), 2 neue Initializers (smtp_guard + mail_observer), DeviseMailJob mit Retry/Discard-Verhalten + Job-Tests, User#send_devise_notification-Override, ApplicationMailer-Sender angeglichen, Layer-2-Tests fuer Change-Password + Email-Change-Reconfirmation gruen.
</objective>

<execution_context>
@/Users/gullrich/DEV/carambus/carambus_master/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gullrich/DEV/carambus/carambus_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/CONTEXT.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-RESEARCH.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-01-PLAN.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-03-PLAN.md

@config/initializers/devise.rb
@app/mailers/application_mailer.rb
@app/controllers/registrations_controller.rb
@app/models/user.rb
@config/environments/production-carambus-de.rb
@config/environments/development-carambus.rb
@test/controllers/registrations_controller_test.rb
@test/mailers/devise_mailer_test.rb

<interfaces>
<!-- Devise-Initializer relevante Zeilen (verifiziert): -->
```ruby
# config/initializers/devise.rb
config.mailer_sender = ENV["SMTP_USERNAME"] || "no-reply@carambus.de"
config.send_password_change_notification = true       # Zeile 153
config.reconfirmable = true                           # Zeile 178
# config.sign_in_after_change_password = true         # Zeile 331 (auskommentiert = Devise-Default true aktiv)
```

<!-- ApplicationMailer-IST (laut RESEARCH.md): -->
```ruby
# app/mailers/application_mailer.rb
class ApplicationMailer < ActionMailer::Base
  default from: Carambus.config.support_email  # = "gernot.ullrich@gmx.de" (DIVERGENT zu mailer_sender!)
end
```

<!-- Devise mailer dispatch: -->
```ruby
# devise/lib/devise/mailers/helpers.rb#devise_mail
headers[:from] = mailer_sender(devise_mapping)  # = config.mailer_sender
# Aber: ActionMailer's `default from:` wird VORHER gesetzt; devise_mail UEBERSCHREIBT.
# Bei Production wird also config.mailer_sender (ENV["SMTP_USERNAME"]) verwendet.
# Bei Custom-Mailer (z.B. UploadMailer) bleibt support_email als from.
# => Plan-04: ApplicationMailer.default sollte ebenfalls auf Devise.mailer_sender,
#    damit alle Mails konsistent denselben Sender haben.
```

<!-- Devise send_devise_notification (devise-4.9.4/lib/devise/models/authenticatable.rb): -->
```ruby
# Default-Implementation (synchrones deliver_now):
def send_devise_notification(notification, *args)
  message = devise_mailer.send(notification, self, *args)
  if message.respond_to?(:deliver_now)
    message.deliver_now
  else
    message.deliver
  end
end
# Override in User-Modell schaltet auf deliver_later via DeviseMailJob um.
```

<!-- ActiveJob Retry-API (Rails 7.2): -->
```ruby
class ApplicationJob < ActiveJob::Base
end

class DeviseMailJob < ApplicationJob
  # retry_on: transient errors → retry mit exponential backoff
  retry_on Net::SMTPAuthenticationError, wait: :exponentially_longer, attempts: 3
  retry_on Net::SMTPServerBusy,         wait: :exponentially_longer, attempts: 5
  # discard_on: permanente Fehler → Job aufgeben + log (Bounce-Handling)
  discard_on Net::SMTPFatalError
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Devise-Config + ApplicationMailer-Sender angleichen + Change-Password-Test</name>
  <files>config/initializers/devise.rb, app/mailers/application_mailer.rb, app/controllers/registrations_controller.rb, test/controllers/registrations_controller_test.rb, test/mailers/devise_mailer_test.rb</files>
  <behavior>
    RED-GREEN:
    1. Test "PATCH /users mit current_password + password rotiert jti UND versendet password_change-Mail": initial RED falls sign_in_after_change_password=true bypassed (Verhalten korrekt aber Test pinnt Vertrag).
    2. Test "Nach PATCH /users ist User nicht mehr eingeloggt": initial RED (heute bleibt User via bypass_sign_in eingeloggt) → GREEN nach config.sign_in_after_change_password=false.
    3. Test "Devise.mailer_sender == ApplicationMailer.default_from": initial RED (Diskrepanz heute) → GREEN nach Sender-Angleichung. Plan-01's "sender-lock"-Test wird mitziehen — kompatibel.
  </behavior>
  <read_first>
    - /Users/gullrich/DEV/carambus/carambus_master/config/initializers/devise.rb (Zeilen 1-50 + 150-340 — sender, reconfirmable, sign_in_after_*)
    - /Users/gullrich/DEV/carambus/carambus_master/app/mailers/application_mailer.rb (gesamtes File)
    - /Users/gullrich/DEV/carambus/carambus_master/app/controllers/registrations_controller.rb (gesamtes File — speziell #update + #update_resource)
    - /Users/gullrich/DEV/carambus/carambus_master/test/controllers/registrations_controller_test.rb (existing tests fuer update password preference)
    - /Users/gullrich/DEV/carambus/carambus_master/test/mailers/devise_mailer_test.rb (aus Plan-01 — Test "sender-lock" pruefen)
    - /Users/gullrich/DEV/carambus/carambus_master/test/fixtures/users.yml — Klartext-Password verifizieren:
      pruefen welcher Klartext-Password den fixture-Hash erzeugt. Falls Standard-Devise-Fixture vorhanden mit
      `password_digest: <%= User.new(password: 'password').password_digest %>` (test-env stretches=1) → Klartext = "password".
      Sonst expliziter Override im Test-setup: `@user.update!(password: 'TestPasswort123!')` und im Test mit
      `current_password: 'TestPasswort123!'` arbeiten.
  </read_first>
  <action>
    1. config/initializers/devise.rb — Zeile 331 (`# config.sign_in_after_change_password = true`) AKTIVIEREN und auf `false` setzen:
       ```ruby
       # D-41-C: Hard-Revoke bei PW-Change semantisch durchsetzen. User muss nach
       # Passwort-Aenderung neu einloggen — JTI-Rotation per Callback (siehe app/models/user.rb)
       # invalidiert alle JWTs, sign_in_after_change_password=false invalidiert auch die
       # aktuelle Browser-Session (bypass_sign_in wird nicht aktiviert in RegistrationsController#update).
       config.sign_in_after_change_password = false
       ```
       Die alte auskommentierte Zeile entfernen.

    2. app/mailers/application_mailer.rb — `default from:` angleichen. Aus:
       ```ruby
       class ApplicationMailer < ActionMailer::Base
         default from: Carambus.config.support_email
         # ...
       end
       ```
       Wird:
       ```ruby
       class ApplicationMailer < ActionMailer::Base
         # D-41-B: Sender ueber alle Mails identisch zu Devise.mailer_sender.
         # Lambda statt Direktwert, damit Devise-Initializer-Reihenfolge nicht kritisch ist
         # (Lambda evaluiert at-mail-time, nicht beim Class-Load).
         default from: -> { Devise.mailer_sender }
         # ...
       end
       ```
       Falls die Klasse weitere Konfiguration enthaelt (Layout, Helper-Methoden), nicht antasten — nur `default from:` ersetzen.

    3. app/controllers/registrations_controller.rb — In #update die Zeile `bypass_sign_in resource, scope: resource_name if sign_in_after_change_password?` UNVERAENDERT lassen. `sign_in_after_change_password?` liest jetzt `false` aus Devise-Config → bypass_sign_in feuert nicht mehr. KEINE Code-Aenderung am Controller noetig.

    4. test/controllers/registrations_controller_test.rb — NEUE Tests am Datei-Ende einfuegen (vor letztem `end`). Falls fixture-Klartext-Password NICHT "password" ist (siehe read_first), entweder lokalen Override `@user = users(:valid); @user.update!(password: "TestPasswort123!", password_confirmation: "TestPasswort123!")` im setup-Block setzen UND in den Tests den expliziten Wert nutzen, ODER (wenn "password" passt) "password" beibehalten:
       ```ruby
       # === D-41 Plan-04: Change-Password Flow ===

       # NOTE Klartext-Password: pruefen test/fixtures/users.yml. Wenn Devise-Default-
       # Pattern (password_digest: <%= User.new(password: 'password').password_digest %>)
       # vorliegt → Klartext = "password". Sonst @user.update!(password: 'TestPasswort123!')
       # im setup setzen und expliziten Wert in den 3 Tests unten verwenden.

       test "PATCH /users mit current_password+password rotiert jti und versendet password_change-Mail" do
         user = users(:valid)
         old_jti = user.jti
         old_encrypted = user.encrypted_password
         sign_in user
         ActionMailer::Base.deliveries.clear

         patch user_registration_path, params: {
           user: {
             current_password: "password",  # ggf. an fixture-Klartext anpassen, siehe NOTE oben
             password: "NeuesPasswort123!",
             password_confirmation: "NeuesPasswort123!"
           }
         }

         user.reload
         assert_not_equal old_encrypted, user.encrypted_password, "PW muss aendern"
         assert_not_equal old_jti, user.jti, "jti muss rotieren (Plan-03 Callback)"

         # send_password_change_notification = true (Devise-Config) → 1 Notification-Mail
         mails = ActionMailer::Base.deliveries
         assert_operator mails.size, :>=, 1, "Mind. 1 Mail (password_change Notification)"
         pw_change_mail = mails.find { |m| m.to == [user.email] && m.subject.match?(/Passwort|Password/) }
         refute_nil pw_change_mail, "password_change-Notification fehlt"
       end

       test "Nach PATCH /users mit PW-Change ist User NICHT mehr eingeloggt (sign_in_after_change_password=false)" do
         user = users(:valid)
         sign_in user

         patch user_registration_path, params: {
           user: {
             current_password: "password",  # ggf. anpassen, siehe NOTE oben
             password: "NeuesPasswort456!",
             password_confirmation: "NeuesPasswort456!"
           }
         }

         # Verifikation: Folge-Request auf geschuetzte Seite muss zu Login redirecten
         get edit_user_registration_path
         assert_redirected_to new_user_session_path,
           "Nach PW-Change muss User neu einloggen (D-41-C Semantik)"
       end

       test "PATCH /users mit neuer email triggert reconfirmation (Email-Change-Flow D-41-D #4)" do
         user = users(:valid)
         sign_in user
         ActionMailer::Base.deliveries.clear

         new_email = "neue-#{SecureRandom.hex(4)}@example.test"
         patch user_registration_path, params: {
           user: {
             current_password: "password",  # ggf. anpassen, siehe NOTE oben
             email: new_email
           }
         }

         user.reload
         # reconfirmable=true: neue email landet in unconfirmed_email, alte bleibt
         assert_equal new_email, user.unconfirmed_email,
           "Neue email muss in unconfirmed_email landen (reconfirmable=true)"
         refute_equal new_email, user.email,
           "Alte email muss bestehen bleiben bis Bestaetigung"

         # 1 confirmation_instructions-Mail an NEUE Adresse erwartet
         mails = ActionMailer::Base.deliveries
         confirm_mail = mails.find { |m| m.to == [new_email] }
         refute_nil confirm_mail, "confirmation_instructions muss an neue Adresse versendet werden"
         assert_match %r{/confirmation\?confirmation_token=}, confirm_mail.body.to_s
       end
       ```

    5. test/mailers/devise_mailer_test.rb — Plan-01's "sender-lock"-Test bleibt unveraendert wirksam: `assert_includes mail.from, Devise.mailer_sender`. Nach Plan-04 ist `ApplicationMailer.default_from == Devise.mailer_sender` UND `Devise.mailer_sender unveraendert` → Test bleibt GREEN. KEINE Aenderung im Test-File noetig in diesem Task.
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_master && bin/rails test test/controllers/registrations_controller_test.rb test/mailers/devise_mailer_test.rb 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "config.sign_in_after_change_password = false" config/initializers/devise.rb` == 1
    - `grep -c "Devise.mailer_sender" app/mailers/application_mailer.rb` == 1
    - 3 neue Tests in registrations_controller_test.rb GREEN
    - Test "sender-lock" aus Plan-01 mailer-test bleibt GREEN
    - `bin/rails test test/controllers/registrations_controller_test.rb test/mailers/devise_mailer_test.rb` exit 0
    - KEINE Regression in user_authentication_test.rb (System-Test) — `bin/rails test test/system/user_authentication_test.rb` weiter GREEN
  </acceptance_criteria>
  <done>D-41-C semantisch durchgesetzt (User-Re-Login Pflicht nach PW-Change); Sender-Diskrepanz geschlossen; Change-Password + Email-Change Reconfirmation Layer-2-getestet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SMTP-Haertung vollstaendig — Fail-Fast-Guard + Mail-Delivery-Observer + DeviseMailJob (Retry+Bounce) (D-41-B)</name>
  <files>config/initializers/smtp_guard.rb, config/initializers/mail_observer.rb, app/jobs/devise_mail_job.rb, app/models/user.rb, test/jobs/devise_mail_job_test.rb</files>
  <behavior>
    D-41-B fordert woertlich (CONTEXT.md):
    - "Retry-Logik bei SMTP-Fehlern (transient vs. permanent)"
    - "Bounce-Handling-Strategie (mindestens Error-Trapping)"
    - "Sender-Verifizierung (Fail-Fast in Production)"
    - "Strukturiertes Logging jedes Mail-Versuchs"

    TDD-Cycle:
    1. RED: `DeviseMailJobTest#test_retries_on_smtp_authentication_error` raised SMTPAuthenticationError im Mailer-Method-Stub, asserted dass Job exakt 3 mal executed wird (1 + 2 retries). Vor Implementation faellt das durch (Job existiert nicht).
    2. GREEN: `app/jobs/devise_mail_job.rb` mit retry_on/discard_on + `User#send_devise_notification`-Override.
    3. RED: `DeviseMailJobTest#test_discards_on_smtp_fatal_error` asserted dass nach 1 Execution mit SMTPFatalError der Job verworfen wird (Logging) — keine Retries.
    4. RED: `DeviseMailJobTest#test_user_send_devise_notification_enqueues_job` asserted dass `user.send_reset_password_instructions` keinen synchronen Mail-Send macht, sondern den Job enqueued.

    Test-Adapter: ActiveJob `:test` (Rails default in test-env) — assert_enqueued_with + perform_enqueued_jobs + Stub fuer Mailer-Method.
  </behavior>
  <read_first>
    - /Users/gullrich/DEV/carambus/carambus_master/config/initializers/devise.rb (Zeile 45 — config.mailer_sender)
    - /Users/gullrich/DEV/carambus/carambus_master/config/environments/production-carambus-de.rb (smtp_settings — verstehen welche ENV-Vars genutzt werden)
    - /Users/gullrich/DEV/carambus/carambus_master/.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-RESEARCH.md (Sektionen "Gmail-SMTP-Haertung", "Retry-Pattern via ActionMailer-Job", "Fail-Fast" — dort steht der konkrete Code fuer DeviseMailJob)
    - /Users/gullrich/DEV/carambus/carambus_master/app/models/user.rb (gesamtes File — bestehende Devise-Module + Callbacks aus Plan-03)
    - /Users/gullrich/DEV/carambus/carambus_master/app/jobs/application_job.rb (falls vorhanden — Parent-Klasse fuer Jobs)
    - /Users/gullrich/DEV/carambus/carambus_master/test/test_helper.rb (verifizieren: ActiveJob::TestHelper include? — fuer Job-Tests noetig)
  </read_first>
  <action>
    1. config/initializers/smtp_guard.rb NEU anlegen:
       ```ruby
       # frozen_string_literal: true

       # D-41-B: Fail-Fast bei fehlendem SMTP_USERNAME / SMTP_PASSWORD in Production.
       # Verhindert dass Production-App mit kaputtem Devise-Mailer startet — Devise-Mails
       # (confirmation, reset_password, password_change) wuerden sonst still scheitern
       # bzw. mit Fallback-Sender "no-reply@carambus.de" raus, was Gmail-SMTP-Auth
       # zurueckweist (SPF/DKIM-Mismatch).
       #
       # Wirkung: bei Production-Boot ohne ENV crashed der Rails-Process sofort mit
       # klarer Fehler-Message — viel besser als nachher silently kaputte Mails.
       if Rails.env.production?
         missing = []
         missing << "SMTP_USERNAME" if ENV["SMTP_USERNAME"].blank?
         missing << "SMTP_PASSWORD" if ENV["SMTP_PASSWORD"].blank?

         if missing.any?
           raise <<~MSG
             FATAL: SMTP-ENV nicht gesetzt — Devise-Mails wuerden Production-Boot brechen.
             Fehlend: #{missing.join(", ")}
             Devise.mailer_sender benoetigt SMTP_USERNAME, Gmail-SMTP-Auth benoetigt
             SMTP_USERNAME + SMTP_PASSWORD.
             Aborting startup. (config/initializers/smtp_guard.rb)
           MSG
         end
       end
       ```

    2. config/initializers/mail_observer.rb NEU anlegen — Success + Failure-Hook (Bounce-Trapping):
       ```ruby
       # frozen_string_literal: true

       # D-41-B: Strukturiertes Logging jedes ActionMailer-Delivery-Versuchs.
       # Standard-Rails-API (register_observer / register_interceptor) — kein Monkey-Patching.
       # Tagged-Logger 'MAILER' macht grep im production.log einfach:
       #   grep '\[MAILER\]' log/production.log | grep FAIL
       #
       # Bounce-Handling: Permanente SMTP-Fehler (Net::SMTPFatalError) werden im
       # DeviseMailJob mit discard_on verworfen + hier ueber delivery_failed-Hook
       # geloggt (Audit-Trail fuer Ops).
       class MailDeliveryObserver
         def self.delivered_email(message)
           Rails.logger.tagged("MAILER") do
             Rails.logger.info(
               "delivered to=#{Array(message.to).join(",")} " \
               "subject=#{message.subject&.gsub(/\s+/, " ")} " \
               "from=#{Array(message.from).join(",")}"
             )
           end
         rescue => e
           Rails.logger.error "[MAILER] Observer crashed: #{e.class}: #{e.message}"
         end

         # Wird von ActionMailer::DeliveryMethods (synchron-Path) bei Exception aufgerufen.
         # Bei deliver_later (DeviseMailJob) faengt retry_on/discard_on ab; trotzdem
         # registrieren wir hier den Hook fuer ggf. nicht-Devise-Mailer (UploadMailer etc.).
         def self.delivery_failed(message, error)
           Rails.logger.tagged("MAILER") do
             Rails.logger.error(
               "FAILED to=#{Array(message&.to).join(",")} " \
               "subject=#{message&.subject&.gsub(/\s+/, " ")} " \
               "error=#{error.class}: #{error.message}"
             )
           end
         rescue => e
           Rails.logger.error "[MAILER] Failure-Observer crashed: #{e.class}: #{e.message}"
         end
       end

       ActionMailer::Base.register_observer(MailDeliveryObserver)
       ```

    3. app/jobs/devise_mail_job.rb NEU anlegen — D-41-B Retry + Bounce-Handling:
       ```ruby
       # frozen_string_literal: true

       # D-41-B: Async-Versand aller Devise-Mails mit Retry-Logik.
       #
       # transient Errors (retry_on):
       #   - Net::SMTPAuthenticationError: Gmail-Auth temporaer fehlgeschlagen (z.B. App-Pw rotated,
       #     Gmail-Rate-Limit-Window) → bis zu 3 Versuche mit exponential backoff
       #   - Net::SMTPServerBusy: Gmail-MX temporaer ueberlastet → bis zu 5 Versuche
       #
       # permanente Errors (discard_on, Bounce-Handling):
       #   - Net::SMTPFatalError: Empfaenger-Adresse ungueltig / Permanent-Bounce →
       #     Job aufgeben, Logging via Observer / Rails.logger; keine Retries.
       #
       # User#send_devise_notification (Override unten) enqueued statt deliver_now.
       class DeviseMailJob < ApplicationJob
         queue_as :mailers

         retry_on Net::SMTPAuthenticationError, wait: :exponentially_longer, attempts: 3
         retry_on Net::SMTPServerBusy,          wait: :exponentially_longer, attempts: 5

         # Bounce-Handling: permanent fatal → log + verwerfen.
         discard_on Net::SMTPFatalError do |job, error|
           Rails.logger.tagged("MAILER") do
             Rails.logger.error(
               "DISCARD permanent SMTP-Fatal — Job=#{job.class} args=#{job.arguments.inspect} " \
               "error=#{error.class}: #{error.message}"
             )
           end
         end

         # perform(mailer_class_string, notification_method, record_id, *raw_args, opts)
         # Devise uebergibt User-Record + Token-Args; wir serialisieren record als id und
         # finden ihn wieder, damit ActiveJob's GlobalID-Serialisierung sauber laeuft.
         def perform(mailer_class, notification, record_class, record_id, *args)
           record = record_class.constantize.find_by(id: record_id)
           return if record.nil? # User wurde inzwischen geloescht — nichts zu tun

           mailer_class.constantize.send(notification, record, *args).deliver_now
         end
       end
       ```
       Falls `app/jobs/application_job.rb` nicht existiert: minimalen Parent-Job anlegen oder bestehende Job-Parent-Klasse pruefen (Rails-7-Default: `ApplicationJob < ActiveJob::Base`).

    4. app/models/user.rb erweitern — `send_devise_notification`-Override. NACH der `private`-Sektion aus Plan-03 (wo `rotate_jti_on_password_change!` liegt) ODER in eigener private-Sektion:
       ```ruby
       # D-41-B: Devise-Mails laufen async ueber DeviseMailJob (Retry+Bounce-Handling).
       # Ueberschreibt Devise's Default-`send_devise_notification` (deliver_now synchron).
       # Mailer-Klasse + Notification-Method + Record-Identitaet werden an Job uebergeben;
       # Job-perform laedt Record per ID neu und liefert deliver_now (in der Job-Worker-
       # Sandbox, mit retry_on/discard_on um SMTP-Errors).
       def send_devise_notification(notification, *args)
         devise_mailer_class = self.class.devise_mailer.name
         DeviseMailJob.perform_later(devise_mailer_class, notification.to_s, self.class.name, id, *args)
       end
       ```
       WICHTIG: `private`-Sichtbarkeit beibehalten falls schon `private` aktiv. Falls Methode oeffentlich werden muss (Devise-Reflection-API), oeffentlich lassen — Devise.send_devise_notification ist im Default public.

    5. test/jobs/devise_mail_job_test.rb NEU anlegen:
       ```ruby
       # frozen_string_literal: true

       require "test_helper"

       # D-41-B Retry+Bounce-Handling: Job-Verhalten unter SMTP-Error-Szenarien.
       class DeviseMailJobTest < ActiveJob::TestCase
         include ActiveJob::TestHelper

         setup do
           ActionMailer::Base.deliveries.clear
           @user = users(:valid)
         end

         test "User#send_devise_notification enqueued DeviseMailJob (kein synchrones deliver_now)" do
           assert_enqueued_with(job: DeviseMailJob) do
             @user.send_reset_password_instructions
           end
           # Im Test-Env mit ActiveJob :test-Adapter: deliveries.size bleibt 0 bis perform_enqueued_jobs.
           assert_equal 0, ActionMailer::Base.deliveries.size,
             "Mail darf erst beim Job-Perform versendet werden (deliver_later-Pfad)"
         end

         test "perform_enqueued_jobs liefert Mail im Erfolgs-Fall aus" do
           perform_enqueued_jobs do
             @user.send_reset_password_instructions
           end
           assert_equal 1, ActionMailer::Base.deliveries.size,
             "Nach perform_enqueued_jobs muss Mail versendet sein"
         end

         test "retry_on Net::SMTPAuthenticationError: bis zu 3 Attempts" do
           # Stub: erste 2 Aufrufe schmeissen SMTPAuthenticationError, 3. ist erfolgreich
           call_count = 0
           Devise::Mailer.stub(:reset_password_instructions, ->(*_args) {
             call_count += 1
             if call_count < 3
               raise Net::SMTPAuthenticationError, "transient auth-fail (simuliert)"
             else
               # Erfolgreich: returne Message-Mock mit deliver_now-Method
               OpenStruct.new(deliver_now: true)
             end
           }) do
             # ActiveJob retry mit :test-Adapter — perform_enqueued_jobs respektiert retry_on
             perform_enqueued_jobs do
               @user.send_reset_password_instructions
             end
           end
           assert_equal 3, call_count, "Job muss bis zu 3 Mal versuchen (1 + 2 retries)"
         end

         test "discard_on Net::SMTPFatalError: permanenter Fehler verwirft Job (kein Crash)" do
           call_count = 0
           Devise::Mailer.stub(:reset_password_instructions, ->(*_args) {
             call_count += 1
             raise Net::SMTPFatalError, "permanent bounce (simuliert)"
           }) do
             # KEIN raise — discard_on faengt; Job wird in Log verworfen
             assert_nothing_raised do
               perform_enqueued_jobs do
                 @user.send_reset_password_instructions
               end
             end
           end
           assert_equal 1, call_count, "Permanenter Fehler darf NUR 1 Mal versucht werden (discard_on)"
         end
       end
       ```
       FALLS `Devise::Mailer.stub(...)` Class-Method-Stub-Probleme macht (Minitest::Mock.expect oder Klassen-Method-Stubbing): alternative Variante mit `Devise::Mailer.define_singleton_method(...)` und `ensure` zum Restore nutzen.

    6. KEIN raise_delivery_errors-Tweak hier — production-carambus-de.rb hat das bereits auf `true` gesetzt; dev hat es per Plan-01 ebenfalls auf `true`. test-env behaelt Default (raise_delivery_errors fuer :test-Adapter ist nicht relevant).
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_master && test -f config/initializers/smtp_guard.rb && test -f config/initializers/mail_observer.rb && test -f app/jobs/devise_mail_job.rb && bin/rails test test/jobs/devise_mail_job_test.rb 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `config/initializers/smtp_guard.rb` existiert, enthaelt `Rails.env.production?` + `raise`
    - `config/initializers/mail_observer.rb` existiert, enthaelt `register_observer(MailDeliveryObserver)` + `delivery_failed`-Hook
    - `app/jobs/devise_mail_job.rb` existiert, enthaelt `retry_on Net::SMTPAuthenticationError` UND `retry_on Net::SMTPServerBusy` UND `discard_on Net::SMTPFatalError`
    - `grep -c "DeviseMailJob.perform_later" app/models/user.rb` >= 1 (Override aktiv)
    - `bin/rails test test/jobs/devise_mail_job_test.rb` exit 0 — alle 4 Job-Tests GREEN (enqueue-Pfad, perform-Pfad, retry-Pfad, discard-Pfad)
    - `bin/rails runner 'puts MailDeliveryObserver.name'` exit 0 + Output "MailDeliveryObserver"
    - `bin/rails runner 'puts DeviseMailJob.name'` exit 0 + Output "DeviseMailJob"
    - `bin/rails runner 'puts ENV["SMTP_USERNAME"]'` in Dev-Env funktioniert OHNE Crash (Guard ist `if Rails.env.production?`)
    - In Test-Env: `bin/rails test test/mailers/devise_mailer_test.rb` exit 0 — bestehende Mailer-Tests bleiben gruen (Stubben falls noetig: Mailer-Tests rufen Mailer-Klasse direkt auf, nicht via User-Notification → kein Job-Pfad → bleibt synchron)
    - `bundle exec standardrb config/initializers/smtp_guard.rb config/initializers/mail_observer.rb app/jobs/devise_mail_job.rb app/models/user.rb` exit 0
  </acceptance_criteria>
  <done>SMTP-Haertung vollstaendig im Sinne von D-41-B: Production-Boot fail-fast bei fehlender ENV; Mail-Observer loggt jeden Delivery-Versuch (success + failure) tagged 'MAILER'; DeviseMailJob versorgt alle Devise-Mails mit Retry-Logik fuer transiente SMTP-Errors + Discard mit Logging fuer permanente Bounces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-Verify — Mailbox in Dev (letter_opener_web) + lokaler Smoke-Test</name>
  <files>(human-verify checkpoint — no files modified)</files>
  <read_first>
    - /Users/gullrich/DEV/carambus/carambus_master/config/environments/development-carambus.rb (Plan-01 letter_opener-Aktivierung)
    - /Users/gullrich/DEV/carambus/carambus_master/config/initializers/smtp_guard.rb (Task-2 Output)
    - /Users/gullrich/DEV/carambus/carambus_master/app/jobs/devise_mail_job.rb (Task-2 Output)
  </read_first>
  <action>
    Plan 41-04 hat vier Dinge geliefert:
    1. config.sign_in_after_change_password = false (Devise-Config)
    2. ApplicationMailer.default_from = Devise.mailer_sender (Sender-Angleichung)
    3. SMTP-Guard + Mail-Observer-Initializers
    4. DeviseMailJob (Retry + Bounce-Handling) + User#send_devise_notification-Override

    Plan 41-01 hatte letter_opener_web in Dev aktiviert. Dieser Checkpoint verifiziert,
    dass der gesamte Flow lokal mit echtem Browser durchspielbar ist (vor dem System-Test
    in Plan 41-05).

    PAUSE: Warte auf User-Verifikation gemaess "how-to-verify"-Anleitung unten.
    Setze NICHT eigenmaechtig fort; User antwortet mit "approved" oder Issue-Beschreibung.
  </action>
  <how-to-verify>
    1. Rails-Server in Dev starten: `bin/rails server` (oder `foreman start -f Procfile.dev`)

    2. Browser oeffnen: http://localhost:3000/letter_opener — sollte leere Mailbox zeigen
       (HTML-Seite "No messages" o.ae., NICHT 404).

    3. In neuem Tab: http://localhost:3000/users/sign_up — Registrierungs-Formular ist da:
       - Hat einen sichtbaren "Ich akzeptiere die Nutzungsbedingungen"-Checkbox (DE-Locale)
         oder "I accept the Terms of Service" (EN-Locale).
       - Hat unsichtbaren Honeypot-Input (View-Source: <input name="subtitle" style="display:none">).

    4. Formular ausfuellen mit test-email-#{Date.today}@example.test, Passwort + Terms-Checkbox.
       Submit klicken. Sollte zu / redirecten mit Flash-Message "Eine Bestaetigungsmail
       wurde an ... gesendet" oder aequivalent.

    5. Zurueck zu http://localhost:3000/letter_opener — sollte JETZT 1 Mail anzeigen:
       Subject "Anleitung zur Bestaetigung" (DE) oder "Confirmation instructions" (EN).
       Mail oeffnen, der Confirmation-Link ist klickbar. Klicken → User wird confirmed.
       HINWEIS: Da DeviseMailJob jetzt deliver_later macht, kann es bis zu 1-2 Sekunden
       dauern, bis die Mail in der Mailbox auftaucht (Sidekiq/Async-Queue verarbeitet Job).

    6. http://localhost:3000/users/password/new → Forgot-Password-Form. Email eingeben,
       Submit. /letter_opener: 1 weitere Mail (Reset-Password). Link klicken, neues Passwort
       setzen → Flow funktioniert.

    7. Eingeloggt? Zu http://localhost:3000/users/edit gehen. Password aendern (current_password
       muss korrekt sein — Schritt 4 hat es gesetzt). Submit. SOLLTE redirecten zu Login-Seite
       (D-41-C: sign_in_after_change_password=false). /letter_opener: password_change-
       Notification-Mail erscheint.

    8. Production-Boot-Test (OHNE Server starten, nur Boot pruefen):
       `SMTP_USERNAME='' SMTP_PASSWORD='' RAILS_ENV=production bin/rails runner 'puts "boot OK"' 2>&1`
       MUSS mit Fehler abbrechen: "FATAL: SMTP-ENV nicht gesetzt".
       Dann: `SMTP_USERNAME=test SMTP_PASSWORD=test RAILS_ENV=production bin/rails runner 'puts "boot OK"' 2>&1`
       MUSS exit 0 mit "boot OK" (smtp_guard akzeptiert beide ENV).

    9. (Optional, Retry-Smoke) `bin/rails runner 'DeviseMailJob.new.tap { |j| puts j.class.retry_attempts }'`
       — sollte ohne Crash laufen; Job-Klasse korrekt loaded.

    Wenn alle 8(+1) Schritte funktionieren → "approved" antworten.
    Bei Problemen: konkrete Schritt-Nummer + Fehlerausgabe.
  </how-to-verify>
  <resume-signal>"approved" oder konkrete Issue-Beschreibung</resume-signal>
  <verify>
    <automated>echo "human-verify checkpoint — resume-signal awaited"</automated>
  </verify>
  <acceptance_criteria>
    - User antwortet mit "approved" nach 8(+1)-Schritte-Walkthrough
    - ODER: User dokumentiert konkretes Issue mit Schritt-Nummer; Plan wird repariert
  </acceptance_criteria>
  <done>Human-Verify-Checkpoint abgenickt; Dev-Mailbox + smtp_guard + DeviseMailJob funktional verifiziert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eingeloggter User → PATCH /users (Change-Password) | Trusted-but-verify; current_password als Re-Auth-Faktor; nach Change → Re-Login Pflicht |
| Eingeloggter User → PATCH /users (Email-Change) | Reconfirmable schuetzt vor Email-Hijacking via Session-Hijacking — neue Email muss bestaetigt werden |
| Rails-Boot → Production-ENV | smtp_guard.rb fail-fast verhindert silent-broken Production |
| Mail-Body → log/production.log | Observer loggt subject + to + from (KEIN Body, KEIN Token) — kein Credential-Leak |
| Devise-Notification → DeviseMailJob → Gmail-SMTP | Job-Sandbox: transient SMTP-Errors retry mit backoff; permanent bounces discard + log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04-01 | Elevation of Privilege | sign_in_after_change_password=true blockiert Revoke-Effekt | mitigate | config auf `false` gesetzt; Test "Nach PATCH /users nicht mehr eingeloggt" pinnt das Verhalten |
| T-41-04-02 | Information Disclosure | SMTP-Credential-Leak via Mail-Observer-Log | mitigate | Observer loggt NUR subject + to + from (gewollt fuer Audit); Body + Tokens werden NICHT geloggt; SMTP-PW nie in Logs |
| T-41-04-03 | Spoofing | From-Header-Mismatch (SPF/DKIM-Fehler) | mitigate | ApplicationMailer.default = Devise.mailer_sender = ENV["SMTP_USERNAME"] → konsistenter Sender ueber alle Mails |
| T-41-04-04 | Denial of Service | Production-Boot mit kaputter SMTP-Config | mitigate | smtp_guard.rb raises bei fehlender ENV → fail-fast, kein silent-broken-state |
| T-41-04-05 | Tampering | Email-Change ohne Reconfirmation | mitigate | reconfirmable=true (Devise-Config) → neue Email muss via Token-Click bestaetigt werden; alte Adresse bleibt bis dahin gueltig |
| T-41-04-06 | Repudiation | Mail-Delivery-Status nicht nachvollziehbar | mitigate | MailDeliveryObserver loggt jeden delivered_email-Aufruf tagged 'MAILER'; grep im log liefert Audit-Trail |
| T-41-04-07 | Denial of Service | Mail-Loss bei transienten SMTP-Errors (D-41-B Retry-Pflicht) | mitigate | DeviseMailJob `retry_on Net::SMTPAuthenticationError` (3 Versuche, exponential backoff) + `retry_on Net::SMTPServerBusy` (5 Versuche); Test "retry_on Net::SMTPAuthenticationError" beweist Verhalten |
| T-41-04-08 | Repudiation | Mail-Loss bei permanenten Bounces (D-41-B Bounce-Handling-Pflicht) | mitigate | DeviseMailJob `discard_on Net::SMTPFatalError` mit expliziter Logging-Block (Audit-Trail im production.log: "DISCARD permanent SMTP-Fatal"); Test "discard_on Net::SMTPFatalError" beweist kein Crash + 1 Versuch |
</threat_model>

<verification>
- `bin/rails test test/controllers/registrations_controller_test.rb test/mailers/devise_mailer_test.rb test/models/user_test.rb test/controllers/passwords_controller_test.rb test/controllers/confirmations_controller_test.rb test/jobs/devise_mail_job_test.rb` exit 0
- `bin/rails test test/system/user_authentication_test.rb` exit 0 (kein Regress)
- `bin/rails runner 'puts "OK"'` exit 0 in Dev (smtp_guard greift nicht)
- `SMTP_USERNAME='' RAILS_ENV=production bin/rails runner 'x' 2>&1` exit != 0 (smtp_guard greift)
- `bundle exec standardrb app/mailers/application_mailer.rb app/jobs/devise_mail_job.rb app/models/user.rb config/initializers/devise.rb config/initializers/smtp_guard.rb config/initializers/mail_observer.rb` exit 0
- Human-verify-Checkpoint Task-3 erfolgreich abgenickt
</verification>

<success_criteria>
- D-41-B vollstaendig (alle 4 Wortlaut-Anforderungen): Fail-Fast-Guard + Mail-Observer (Success+Failure) + Retry-Logik (transient SMTP-Errors via DeviseMailJob.retry_on) + Bounce-Handling (permanent Bounce via discard_on + Logging); Sender konsistent
- D-41-C vollstaendig: PW-Change rotiert jti (Plan-03 Callback) UND User wird abgemeldet (sign_in_after_change_password=false)
- D-41-D Flow 3 (Change-Password) Layer-2 + Layer-3 GREEN getestet
- D-41-D Flow 4 (Email-Change Reconfirmation) Layer-2 getestet
- Mailer-From-Mismatch behoben: ApplicationMailer + Devise.mailer_sender identisch
- DeviseMailJob mit 4 Tests deterministisch verifiziert (enqueue, perform, retry, discard)
- Human-Verify-Checkpoint bestaetigt Dev-Flow End-to-End
</success_criteria>

<output>
After completion, create `.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-04-SUMMARY.md` mit:
- Vier Bereiche abgearbeitet: Change-Password-Flow, Email-Change-Reconfirmation, SMTP-Haertung (Fail-Fast + Observer + Retry-Job + Bounce-Handling), Sender-Angleichung
- Test-Count Plan-04: 3 neue Tests in registrations_controller_test.rb + 4 Tests in devise_mail_job_test.rb
- Initializers-Diff: smtp_guard.rb + mail_observer.rb NEU, devise.rb angepasst (sign_in_after_change_password=false), application_mailer.rb angepasst (Sender-Lambda)
- Job-Diff: app/jobs/devise_mail_job.rb NEU, app/models/user.rb erweitert (send_devise_notification-Override)
- Human-Verify-Ergebnis (8+1-Schritte-Checkliste — was funktionierte, was nicht)
- Nach Plan-04 sind alle 4 D-41-* Decisions vollstaendig adressiert; Plan-05 ist nur noch E2E-System-Tests
</output>
</content>
