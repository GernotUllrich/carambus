---
phase: 41
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/test_helper.rb
  - test/support/mail_helpers.rb
  - test/support/letter_opener_helper.rb
  - test/support/devise_test_helpers.rb
  - test/mailers/devise_mailer_test.rb
  - test/controllers/passwords_controller_test.rb
  - test/controllers/confirmations_controller_test.rb
  - test/system/devise_flows_test.rb
  - config/environments/development-carambus.rb
  - config/routes.rb
autonomous: true
requirements:
  - REQ-41-01   # 4-Layer-Test-Infrastruktur etablieren
  - REQ-41-02   # letter_opener_web in Dev aktiv (Mailbox + Route)
  - REQ-41-03   # Charakterisierungstests aller 4 Flows dokumentieren IST-Zustand
threat_refs:
  - T-41-INFRA-01  # Sender-Diskrepanz-Erkennung (ApplicationMailer.from vs. mailer_sender)
must_haves:
  truths:
    - "test/mailers/ existiert und ist von bin/rails test gepickt"
    - "ActionMailer::Base.deliveries-basierte Mailer-Tests laufen lokal in <40s"
    - "letter_opener_web in development aktiv: Mailbox unter /letter_opener erreichbar"
    - "Charakterisierungstests dokumentieren IST-Zustand aller 4 Devise-Flows"
    - "Mail-Helper extrahiert Confirmation-/Reset-Token-URLs aus Mail-Body"
    - "Wave-0-Skelett-Files (devise_flows_test.rb + letter_opener_helper.rb + devise_test_helpers.rb) vorhanden, damit Plan-05/04 darauf bauen koennen"
    - "MailHelpers ist via test_helper.rb sauber in alle drei Test-Basisklassen included (ActionMailer::TestCase + ActionDispatch::IntegrationTest + ApplicationSystemTestCase) — kein extend-im-setup-Fragility"
  artifacts:
    - path: test/mailers/devise_mailer_test.rb
      provides: "Layer-3 Mailer-Charakterisierung (confirmation, reset, password_change, email_changed)"
      min_lines: 80
    - path: test/controllers/passwords_controller_test.rb
      provides: "Layer-2 Controller-Charakterisierung — Forgot+Reset HTTP-Flow"
      min_lines: 40
    - path: test/controllers/confirmations_controller_test.rb
      provides: "Layer-2 Controller-Charakterisierung — Resend+Confirm HTTP-Flow"
      min_lines: 30
    - path: test/support/mail_helpers.rb
      provides: "last_email + extract_confirmation_url + extract_reset_password_url Helper"
      exports: ["MailHelpers#last_email", "MailHelpers#extract_confirmation_url", "MailHelpers#extract_reset_password_url"]
    - path: test/support/letter_opener_helper.rb
      provides: "Skeleton-Helper-Module fuer ggf. Parsing von letter_opener-Mailbox in System-Tests (Dev-Env). Plan-05 nutzt aktuell ActionMailer::Base.deliveries direkt — File ist Wave-0-Pflichtlieferung aus VALIDATION.md."
      contains: "module LetterOpenerHelper"
    - path: test/support/devise_test_helpers.rb
      provides: "Skeleton-Helper-Module fuer Sign-in / Confirmation-Token-Generator. Plan-05 erweitert spaeter falls noetig — File ist Wave-0-Pflichtlieferung aus VALIDATION.md."
      contains: "module DeviseTestHelpers"
    - path: test/system/devise_flows_test.rb
      provides: "Skeleton-Klasse DeviseFlowsTest < ApplicationSystemTestCase — Plan-05 fuellt die Tests"
      contains: "class DeviseFlowsTest"
    - path: config/environments/development-carambus.rb
      provides: "delivery_method=:letter_opener_web + raise_delivery_errors=true (dev)"
      contains: "letter_opener_web"
  key_links:
    - from: test/test_helper.rb
      to: test/support/mail_helpers.rb
      via: "include MailHelpers in ActionMailer::TestCase + ActionDispatch::IntegrationTest + ApplicationSystemTestCase (saubere Include-Wiring, kein extend-im-setup)"
      pattern: "include MailHelpers"
    - from: config/routes.rb
      to: LetterOpenerWeb::Engine
      via: "mount LetterOpenerWeb::Engine, at: \"/letter_opener\" if Rails.env.development?"
      pattern: "LetterOpenerWeb::Engine"
---

<objective>
Wave-0-Phase: Test-Infrastruktur und letter_opener_web-Aktivierung schaffen die Voraussetzung für alle nachfolgenden Plan-02..05-Fixes. Außerdem werden Charakterisierungstests geschrieben, die den IST-Zustand aller vier Devise-Flows (Registrierung, Forgot-Password, Change-Password, Email-Change) dokumentieren. Diese Tests sind initial bewusst rot/grün gemischt — sie pinnen den Status quo und werden durch Plan-02..04 in den gewünschten Zustand überführt.

Purpose: Ohne Mailer-Test-Infrastruktur und ohne Dev-Mail-Sichtbarkeit sind die nachfolgenden Plans nicht verifizierbar. Wave-0 ist Voraussetzung gemäß D-41-A (4-Layer-Coverage Pflicht) und VALIDATION.md (Wave-0-Definition-of-Done).

Output: test/mailers/-Verzeichnis mit Charakterisierungs-Suite; Layer-2-Charakterisierungstests für passwords + confirmations; Mail-Helper für Token-Extraktion; letter_opener_web aktiv in Dev; alle 6 Wave-0-Pflichtlieferungen aus VALIDATION.md vorhanden (inkl. Skeleton-Files fuer Plan-05).
</objective>

<execution_context>
@/Users/gullrich/DEV/carambus/carambus_master/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gullrich/DEV/carambus/carambus_master/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/CONTEXT.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-RESEARCH.md
@.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-VALIDATION.md

# Existing files this plan extends or references
@test/test_helper.rb
@test/application_system_test_case.rb
@test/controllers/registrations_controller_test.rb
@test/controllers/users/registrations_controller_test.rb
@test/system/user_authentication_test.rb
@app/models/user.rb
@config/initializers/devise.rb
@config/environments/development-carambus.rb
@config/routes.rb
@test/fixtures/users.yml

<interfaces>
<!-- Aus Quellcode extrahiert — kein weiterer Codebase-Walk nötig. -->

User Model (app/models/user.rb):
```ruby
class User < ApplicationRecord
  devise :database_authenticatable, :registerable,
    :recoverable, :rememberable, :validatable, :confirmable,
    :jwt_authenticatable, jwt_revocation_strategy: self

  include Devise::JWT::RevocationStrategies::JTIMatcher

  validates :terms_of_service, acceptance: true, on: :create
  attr_accessor :terms_of_service

  def skip_confirmation!
    # leere Override — überschreibt Devise-Default (bewusst!)
  end
end
```

JTIMatcher (devise-jwt-0.13.0):
```ruby
def self.jwt_revoked?(payload, user)
  payload['jti'] != user.jti
end

def self.revoke_jwt(_payload, user)
  user.update_column(:jti, generate_jti)
end
```

Devise mailer convention (devise-4.9.4/app/mailers/devise/mailer.rb):
- confirmation_instructions(record, token, opts) — sends to record.email
- reset_password_instructions(record, token, opts) — sends to record.email
- password_change(record, opts) — sends to record.email
- email_changed(record, opts) — sends to record.unconfirmed_email if reconfirmable, sonst record.email
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: letter_opener_web in Dev aktivieren + Route mounten</name>
  <files>config/environments/development-carambus.rb, config/routes.rb</files>
  <read_first>
    - /Users/gullrich/DEV/carambus/carambus_master/config/environments/development-carambus.rb (gesamtes File, 84 Zeilen)
    - /Users/gullrich/DEV/carambus/carambus_master/config/routes.rb (Top 60 Zeilen — die devise_for-Section)
    - /Users/gullrich/DEV/carambus/carambus_master/Gemfile (Zeile mit "letter_opener_web" verifizieren — Group :development)
  </read_first>
  <action>
    1. In config/environments/development-carambus.rb folgende Zeilen ZWISCHEN Zeile 30 (`raise_delivery_errors = false`) und Zeile 31 (`perform_caching = false`) einfügen — config.action_mailer.* Block:
       ```ruby
       # D-41-B: letter_opener_web aktiviert für Dev-Mailbox unter /letter_opener.
       # raise_delivery_errors auf true gestellt, damit Mailer-Probleme früh sichtbar
       # sind (vorher: stille Drops in Dev). Production-SMTP-Config bleibt unberührt.
       config.action_mailer.delivery_method = :letter_opener_web
       config.action_mailer.perform_deliveries = true
       config.action_mailer.raise_delivery_errors = true
       config.action_mailer.default_url_options = { host: "localhost", port: 3000 }
       ```
       Die existierende Zeile 30 (`config.action_mailer.raise_delivery_errors = false`) IM file BELASSEN — der neue Block überschreibt sie. Begründung dokumentieren als Kommentar.

       Alternative (sauberer): Zeile 30 löschen und durch obigen Block ersetzen. Bevorzugt diese Variante.

    2. In config/routes.rb innerhalb von `Rails.application.routes.draw do` (nach den devise_for-Lines aber vor anderen Routen) folgenden Block hinzufügen — am Ende der Draw-Section, kurz vor `end`:
       ```ruby
       # D-41-B: Dev-Mailbox via letter_opener_web — Devise-Confirmation- und Reset-Mails
       # landen unter http://localhost:3000/letter_opener (development only).
       if Rails.env.development?
         mount LetterOpenerWeb::Engine, at: "/letter_opener"
       end
       ```

    3. KEIN Restart der Rails-App nötig — Routes werden in Dev hot-reloaded.
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_master && grep -q "letter_opener_web" config/environments/development-carambus.rb && grep -q "LetterOpenerWeb::Engine" config/routes.rb && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "config.action_mailer.delivery_method = :letter_opener_web" config/environments/development-carambus.rb` == 1
    - `grep -c "mount LetterOpenerWeb::Engine" config/routes.rb` == 1
    - `bin/rails routes 2>/dev/null | grep letter_opener` liefert ≥1 Route-Eintrag (manuell verifizieren — kann übersprungen werden falls Rails-Server nicht läuft)
    - Test-Env unverändert: `grep -c "delivery_method = :test" config/environments/test.rb` bleibt 1 (oder Default — keine Modifikation)
  </acceptance_criteria>
  <done>letter_opener_web aktiv in Dev (Mailbox-Route gemountet, delivery_method gesetzt, raise_delivery_errors=true für sichtbare Fehler).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mail-Helper + Layer-3 Charakterisierungstests (test/mailers/devise_mailer_test.rb)</name>
  <files>test/support/mail_helpers.rb, test/test_helper.rb, test/mailers/devise_mailer_test.rb</files>
  <behavior>
    Charakterisierungstests pinnen den IST-Zustand der vier Devise-Mailer-Methoden. Tests laufen INITIAL grün gegen den heutigen Code (kein Devise-Defekt im Mailer-Generierungs-Layer — die Mails werden GENERIERT korrekt, "kommen nur nicht an" laut User). Tests assertieren:
    - confirmation_instructions: 1 Mail in deliveries; to=[user.email]; Subject enthält DE-i18n-String aus devise.de.yml; Body enthält "/confirmation?confirmation_token="
    - reset_password_instructions: to=[user.email]; Body enthält "/password/edit?reset_password_token="; Body enthält raw token
    - password_change: send_password_change_notification triggert Mail; Subject enthält DE "Passwort"
    - email_changed: bei reconfirmable=true wird Mail an unconfirmed_email gesendet (NICHT alte email)

    Test-5 (Sender-Diskrepanz-Lock): assertet aktuell `mail.from == [ENV["SMTP_USERNAME"] || "no-reply@carambus.de"]` — wenn ApplicationMailer.default_from in Plan-04 angeglichen wird, lockt dieser Test die Korrektur.

    MailHelpers-Wiring (BLOCKER-Fix): MailHelpers wird via test_helper.rb in DREI Test-Basisklassen included:
    1. ActionMailer::TestCase   — fuer Mailer-Tests in test/mailers/
    2. ActionDispatch::IntegrationTest — fuer Controller-Request-Tests
    3. ApplicationSystemTestCase — fuer Capybara System-Tests (Plan-05)
    Damit kein `extend MailHelpers if !respond_to?(:last_email)`-Workaround in einzelnen Tests noetig ist.
  </behavior>
  <read_first>
    - /Users/gullrich/DEV/carambus/carambus_master/test/test_helper.rb (gesamte Datei)
    - /Users/gullrich/DEV/carambus/carambus_master/test/application_system_test_case.rb (verifizieren: ApplicationSystemTestCase erbt von ActionDispatch::SystemTestCase, NICHT von IntegrationTest → Auto-Include via Vererbung greift nicht)
    - /Users/gullrich/DEV/carambus/carambus_master/test/fixtures/users.yml (verifizieren: fixture-User haben `confirmed_at` und `jti`-Werte)
    - /Users/gullrich/DEV/carambus/carambus_master/config/locales/devise.de.yml (Suchen: devise.mailer.confirmation_instructions.subject und reset_password_instructions.subject — exakter String wird benötigt)
    - /Users/gullrich/DEV/carambus/carambus_master/app/models/user.rb (Zeilen 1-60 — Devise-Module + skip_confirmation!-Override-Pattern verstehen)
  </read_first>
  <action>
    1. test/support/mail_helpers.rb NEU anlegen:
       ```ruby
       # frozen_string_literal: true

       # D-41-A Layer-3+4-Helper: extrahiert Devise-Tokens aus generierten Mail-Bodies.
       # In Tests via `include MailHelpers` (siehe test_helper.rb — included in
       # ActionMailer::TestCase + ActionDispatch::IntegrationTest + ApplicationSystemTestCase).
       module MailHelpers
         def last_email
           ActionMailer::Base.deliveries.last
         end

         def clear_mail_queue
           ActionMailer::Base.deliveries.clear
         end

         # Extrahiert vollständige Confirmation-URL inkl. confirmation_token aus Mail-Body.
         # Nutzbar in System-Tests für Click-Through-Flow (Plan 41-05).
         def extract_confirmation_url(mail = last_email)
           body = mail.body.to_s
           match = body.match(%r{https?://[^\s"'<>]+/confirmation\?[^\s"'<>]+})
           match&.to_s
         end

         # Extrahiert vollständige Reset-Password-URL inkl. reset_password_token aus Mail-Body.
         def extract_reset_password_url(mail = last_email)
           body = mail.body.to_s
           match = body.match(%r{https?://[^\s"'<>]+/password/edit\?[^\s"'<>]+})
           match&.to_s
         end
       end
       ```

    2. test/test_helper.rb erweitern: NACH dem letzten `require`-Statement und VOR `class ActiveSupport::TestCase` folgende Zeile einfügen:
       ```ruby
       require_relative "support/mail_helpers"
       ```
       Dann am Ende der Datei (oder vor dem letzten `end`) folgende Include-Blocks einfügen. WICHTIG: Plan-Checker-Fix BLOCKER-3 — MailHelpers wird in alle drei Test-Basisklassen included, weil ApplicationSystemTestCase NICHT von ActionDispatch::IntegrationTest erbt (sondern von ActionDispatch::SystemTestCase). Auto-Include via Vererbung greift daher nicht; sauberes Include statt extend-im-setup verhindert Minitest-Parallelisierungs-Fragility:
       ```ruby
       class ActionMailer::TestCase
         include MailHelpers
       end

       class ActionDispatch::IntegrationTest
         include MailHelpers
       end

       # BLOCKER-3 Fix: ApplicationSystemTestCase erbt nicht von IntegrationTest;
       # daher hier explizit. Plan-05 setup kann ohne extend-Workaround arbeiten.
       # `if defined?(ApplicationSystemTestCase)` schuetzt frueheren require-Pfad,
       # falls test_helper geladen wird bevor application_system_test_case.rb da ist.
       require_relative "application_system_test_case" if File.exist?(File.expand_path("application_system_test_case.rb", __dir__))
       if defined?(ApplicationSystemTestCase)
         class ApplicationSystemTestCase
           include MailHelpers
         end
       end
       ```
       FALLS test_helper.rb bereits ActionMailer-/IntegrationTest-Erweiterungen hat, NUR den Include zu der bestehenden Klassendeklaration hinzufügen (NICHT duplizieren). FALLS der `require_relative "application_system_test_case"`-Pfad zu Boot-Order-Issues fuehrt: alternativer Pfad ist ein `ActiveSupport.on_load(:action_dispatch_system_test_case)`-Block — Executor entscheidet welcher Pfad fuer dieses Projekt sauber ist.

    3. test/mailers/devise_mailer_test.rb NEU anlegen:
       ```ruby
       # frozen_string_literal: true

       require "test_helper"

       # D-41-A Layer 3: Charakterisierungstests pinnen IST-Verhalten der Devise-Mailer.
       # Wenn Plan-02..04 Sender-Angleichung / View-Tweaks ändern, ziehen diese Tests mit.
       class DeviseMailerTest < ActionMailer::TestCase
         setup do
           clear_mail_queue
           @user = users(:valid)
         end

         test "confirmation_instructions: empfaenger, subject (DE), token-url im body" do
           I18n.with_locale(:de) do
             @user.update_columns(confirmed_at: nil, confirmation_token: nil, confirmation_sent_at: nil)
             @user.send_confirmation_instructions
           end

           assert_equal 1, ActionMailer::Base.deliveries.size, "exakt 1 Confirmation-Mail erwartet"
           mail = last_email
           assert_equal [@user.email], mail.to
           assert_includes mail.subject, I18n.t("devise.mailer.confirmation_instructions.subject", locale: :de)
           assert_match %r{/confirmation\?confirmation_token=}, mail.body.to_s
         end

         test "reset_password_instructions: token im body extrahierbar" do
           I18n.with_locale(:de) do
             @user.send_reset_password_instructions
           end

           mail = last_email
           refute_nil mail, "Reset-Mail muss versendet werden"
           assert_equal [@user.email], mail.to
           assert_match %r{/password/edit\?reset_password_token=}, mail.body.to_s
           # Plan 41-05 System-Test wird extract_reset_password_url nutzen — hier locken
           refute_nil extract_reset_password_url(mail), "Helper muss URL extrahieren koennen"
         end

         test "password_change: Notification-Mail wird versendet (DE-Subject enthaelt 'Passwort')" do
           I18n.with_locale(:de) do
             @user.send(:send_password_change_notification)
           end
           mail = last_email
           refute_nil mail
           assert_equal [@user.email], mail.to
           assert_match(/Passwort/, mail.subject, "DE-Subject sollte 'Passwort' enthalten (devise.de.yml)")
         end

         test "email_changed: bei reconfirmable=true Mail an unconfirmed_email" do
           # Reconfirmable-Flow: User aendert email, neue Adresse landet in unconfirmed_email,
           # email_changed wird an die NEUE Adresse versendet (Devise-Default).
           @user.update!(email: "alt-#{@user.id}@example.test", confirmed_at: 1.hour.ago)
           clear_mail_queue

           I18n.with_locale(:de) do
             @user.update!(email: "neu-#{@user.id}@example.test")
           end
           # In reconfirmable-Mode landet 1 confirmation_instructions an neue Adresse
           # und optional 1 email_changed an alte (Devise-Default config.send_email_changed_notification).
           assert_operator ActionMailer::Base.deliveries.size, :>=, 1
         end

         test "sender-lock: from-Adresse entspricht Devise.mailer_sender (IST-Zustand)" do
           # CHARAKTERISIERUNG: heute liefert Devise eine bestimmte from-Adresse.
           # Plan-04 angleicht ApplicationMailer.default from und Devise.mailer_sender.
           # Dieser Test pinnt den IST-Wert und wird in Plan-04 angepasst.
           I18n.with_locale(:de) do
             @user.send_reset_password_instructions
           end
           mail = last_email
           refute_nil mail.from, "from-Header muss gesetzt sein"
           # IST: Devise.mailer_sender wird genutzt — assertion lockt mailer_sender-Wert
           expected_sender = Devise.mailer_sender
           assert_includes mail.from, expected_sender, "from sollte Devise.mailer_sender enthalten — divergiert sonst SMTP-Auth"
         end
       end
       ```
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_master && SAFETY_ASSURED=true bin/rails db:test:prepare && bin/rails test test/mailers/devise_mailer_test.rb 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test/mailers/devise_mailer_test.rb` enthält exakt 5 `test "..."`-Blöcke
    - `bin/rails test test/mailers/devise_mailer_test.rb` exit-code 0 ODER (falls IST-Defekt) klar dokumentierte Failures mit aussagekräftiger Message — KEINE Errors (kein Crash)
    - `test/support/mail_helpers.rb` exportiert `MailHelpers` mit Methoden `last_email`, `clear_mail_queue`, `extract_confirmation_url`, `extract_reset_password_url`
    - `grep -c "include MailHelpers" test/test_helper.rb` >= 3 (ActionMailer::TestCase + ActionDispatch::IntegrationTest + ApplicationSystemTestCase)
    - Test-Runtime < 10s für die 5 Mailer-Tests
  </acceptance_criteria>
  <done>Layer-3-Mailer-Charakterisierung vorhanden; IST-Zustand der Devise-Mails (confirmation, reset, password_change, email_changed, sender) gepinnt; Helper für Token-Extraktion einsatzbereit für Plan-05 (saubere Include-Wiring in alle drei Test-Basisklassen).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Layer-2 Controller-Charakterisierung (passwords + confirmations)</name>
  <files>test/controllers/passwords_controller_test.rb, test/controllers/confirmations_controller_test.rb</files>
  <behavior>
    Layer-2-Tests pinnen Devise-Default-PasswordsController + ConfirmationsController HTTP-Verhalten:
    - POST /users/password mit gültiger email → 1 Mail in deliveries, redirect auf new_user_session_path, flash[:notice]
    - POST /users/password mit unbekannter email → KEINE Mail (Devise paranoid mode AUS = Default zeigt Fehler, Devise paranoid mode AN = identische Antwort). Test charakterisiert IST.
    - GET /users/password/edit?reset_password_token=<token> → 200, Form rendered
    - PUT /users/password mit korrektem Token + neuem PW → User.encrypted_password ändert sich; redirect to root or signed-in path
    - POST /users/confirmation mit email für unbestätigten User → 1 Mail (resend)
    - GET /users/confirmation?confirmation_token=<token> → User.confirmed_at gesetzt
  </behavior>
  <read_first>
    - /Users/gullrich/DEV/carambus/carambus_master/test/controllers/registrations_controller_test.rb (Pattern für ActionDispatch::IntegrationTest in diesem Projekt)
    - /Users/gullrich/DEV/carambus/carambus_master/test/controllers/users/registrations_controller_test.rb (zweites Test-Beispiel)
    - /Users/gullrich/DEV/carambus/carambus_master/config/routes.rb (devise_for users — Routenamen verifizieren: user_password_path, user_confirmation_path)
    - /Users/gullrich/DEV/carambus/carambus_master/test/fixtures/users.yml (fixture :valid und idealerweise :unconfirmed — falls fehlt, in Test auf-the-fly erzeugen via .new + .save(validate: false))
  </read_first>
  <action>
    1. test/controllers/passwords_controller_test.rb NEU anlegen:
       ```ruby
       # frozen_string_literal: true

       require "test_helper"

       # D-41-A Layer 2: Forgot- und Reset-Password HTTP-Flow charakterisieren.
       # Devise-Default-Controller (kein Custom-Override) — Tests pinnen, wie Devise
       # heute reagiert. Plan 41-03 erweitert dies um JTI-Rotation-Assertions.
       class PasswordsControllerTest < ActionDispatch::IntegrationTest
         setup do
           clear_mail_queue
           @user = users(:valid)
         end

         test "POST /users/password mit gueltiger email versendet Reset-Mail" do
           assert_difference -> { ActionMailer::Base.deliveries.size }, 1 do
             post user_password_path, params: { user: { email: @user.email } }
           end
           assert_redirected_to new_user_session_path
         end

         test "GET /users/password/edit mit gueltigem Token rendert Form" do
           raw_token = @user.send_reset_password_instructions
           get edit_user_password_path(reset_password_token: raw_token)
           assert_response :success
           assert_select "input[name='user[password]']"
         end

         test "PUT /users/password mit gueltigem Token aendert encrypted_password" do
           raw_token = @user.send_reset_password_instructions
           old_encrypted = @user.reload.encrypted_password

           put user_password_path, params: {
             user: {
               reset_password_token: raw_token,
               password: "NeuesPasswort123!",
               password_confirmation: "NeuesPasswort123!"
             }
           }
           # IST: redirect (root oder signed_in_root_path) — charakterisieren
           assert_response :redirect
           assert_not_equal old_encrypted, @user.reload.encrypted_password,
             "encrypted_password muss sich nach Reset aendern"
         end

         test "PUT /users/password mit invalidem Token gibt Form-Error zurueck" do
           put user_password_path, params: {
             user: {
               reset_password_token: "absolut-ungueltig-xyz",
               password: "NeuesPasswort123!",
               password_confirmation: "NeuesPasswort123!"
             }
           }
           # Devise rendert Form mit Error (kein redirect)
           assert_response :unprocessable_entity
         end
       end
       ```

    2. test/controllers/confirmations_controller_test.rb NEU anlegen:
       ```ruby
       # frozen_string_literal: true

       require "test_helper"

       # D-41-A Layer 2: Confirmation-Resend und Confirm-via-Token HTTP-Flow.
       class ConfirmationsControllerTest < ActionDispatch::IntegrationTest
         setup do
           clear_mail_queue
           @user = users(:valid)
           # Sicherstellen: User ist unconfirmed fuer diese Tests
           @user.update_columns(confirmed_at: nil, confirmation_token: nil, confirmation_sent_at: nil)
         end

         test "POST /users/confirmation versendet erneut Confirmation-Mail" do
           assert_difference -> { ActionMailer::Base.deliveries.size }, 1 do
             post user_confirmation_path, params: { user: { email: @user.email } }
           end
           assert_redirected_to new_user_session_path
         end

         test "GET /users/confirmation mit gueltigem Token bestaetigt User" do
           # Token generieren wie Devise es im Mailer macht
           raw_token, db_token = Devise.token_generator.generate(User, :confirmation_token)
           @user.update_columns(confirmation_token: db_token, confirmation_sent_at: Time.current)

           get user_confirmation_path(confirmation_token: raw_token)
           assert_response :redirect
           assert_not_nil @user.reload.confirmed_at, "User muss confirmed sein"
         end

         test "GET /users/confirmation mit invalidem Token gibt Form-Error" do
           get user_confirmation_path(confirmation_token: "absolut-ungueltig-xyz")
           # Devise rendert new mit Fehler (kein 500)
           assert_response :unprocessable_entity
         end
       end
       ```

    3. KEINE Mods an test_helper.rb (bereits in Task 2 erweitert).
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_master && bin/rails test test/controllers/passwords_controller_test.rb test/controllers/confirmations_controller_test.rb 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test/controllers/passwords_controller_test.rb` enthält 4 `test "..."`-Blöcke
    - `test/controllers/confirmations_controller_test.rb` enthält 3 `test "..."`-Blöcke
    - `bin/rails test test/controllers/passwords_controller_test.rb test/controllers/confirmations_controller_test.rb` exit 0 ODER dokumentierte Failures (Charakterisierung)
    - Tests laufen in <15s gesamt
    - KEINE Errors (kein Crash, kein 500, keine Missing-Route — alle Devise-Routen müssen existieren)
  </acceptance_criteria>
  <done>Layer-2-Charakterisierung für Forgot/Reset/Confirm-Flows liegt vor. Plan-03 wird hier JTI-Rotation-Tests anhängen.</done>
</task>

<task type="auto">
  <name>Task 4: Wave-0-Skeleton-Files (VALIDATION.md Pflichtlieferung)</name>
  <files>test/support/letter_opener_helper.rb, test/support/devise_test_helpers.rb, test/system/devise_flows_test.rb</files>
  <read_first>
    - /Users/gullrich/DEV/carambus/carambus_master/.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-VALIDATION.md (Section "Wave 0 Requirements" — listet alle 6 Pflichtlieferungen)
    - /Users/gullrich/DEV/carambus/carambus_master/test/application_system_test_case.rb (verstehen wie System-Test-Klasse strukturiert ist)
  </read_first>
  <action>
    Plan-Checker BLOCKER-2: VALIDATION.md listet 6 Wave-0-Pflichtlieferungen; Plan-01 Tasks 1-3 liefern 4 davon. Die fehlenden 2 Helper-Skeletons + 1 System-Test-Skeleton werden hier als minimaler Class-/Module-Skeleton angelegt. Plan-05 fuellt das System-Test-Skeleton spaeter mit Logik; die Helper-Skeletons koennen optional bleiben (Plan-05 nutzt aktuell ActionMailer::Base.deliveries direkt statt letter_opener-Parsing — die Skeletons sind aber als Wave-0-Vertrag erfuellt).

    1. test/support/letter_opener_helper.rb NEU anlegen — minimal-Skeleton:
       ```ruby
       # frozen_string_literal: true

       # D-41-A Wave-0 Skeleton (VALIDATION.md Pflichtlieferung).
       #
       # Aktueller Stand: Plan-05 System-Tests nutzen ActionMailer::Base.deliveries direkt
       # via MailHelpers (siehe test/support/mail_helpers.rb), KEIN letter_opener-Parsing.
       # Dieser Module ist Reserve: falls spaetere E2E-Tests in Dev (nicht test-env)
       # gegen die echte letter_opener-Mailbox laufen sollen, hier die Parsing-Logik
       # ergaenzen (tmp/letter_opener/*.html lesen + Token extrahieren).
       module LetterOpenerHelper
         # Placeholder: tmp/letter_opener-Ordner durchsuchen und letzte Mail liefern.
         # Wird befuellt sobald Bedarf entsteht — bis dahin nutzen Tests MailHelpers.
         def letter_opener_last_mail_path
           Dir.glob(Rails.root.join("tmp/letter_opener/**/*.html")).max_by { |f| File.mtime(f) }
         end
       end
       ```

    2. test/support/devise_test_helpers.rb NEU anlegen — minimal-Skeleton:
       ```ruby
       # frozen_string_literal: true

       # D-41-A Wave-0 Skeleton (VALIDATION.md Pflichtlieferung).
       #
       # Aktueller Stand: Plan-05 System-Tests definieren sign_in_via_form als private
       # Method inline. Dieser Module ist Reserve fuer spaetere DRY-Refactoring oder
       # zusaetzliche Helper (z.B. confirmed_user-Factory, raw-Confirmation-Token-
       # Generator). Plan-02..05 erweitern bei Bedarf.
       module DeviseTestHelpers
         # Placeholder: generiert ein Raw-Confirmation-Token + speichert digest am User.
         # Devise nutzt Devise.token_generator.generate(User, :confirmation_token).
         def generate_raw_confirmation_token(user)
           raw, db = Devise.token_generator.generate(User, :confirmation_token)
           user.update_columns(confirmation_token: db, confirmation_sent_at: Time.current)
           raw
         end
       end
       ```

    3. test/system/devise_flows_test.rb NEU anlegen — minimal-Skeleton (Plan-05 fuellt Tests):
       ```ruby
       # frozen_string_literal: true

       require "application_system_test_case"

       # D-41-A Wave-0 Skeleton (VALIDATION.md Pflichtlieferung).
       # Plan-05 (Wave-3) fuellt diese Klasse mit 4 E2E-System-Tests fuer die
       # Devise-Flows (Sign-up, Forgot, Change-Password, Email-Change).
       # MailHelpers ist via test_helper.rb in ApplicationSystemTestCase included,
       # daher kein extend-Workaround noetig.
       class DeviseFlowsTest < ApplicationSystemTestCase
         # Plan-05 fuellt: 4 Tests mit Capybara + Mail-Token-Click-Through
       end
       ```

    4. KEIN Test-Run noetig — alle 3 Files sind nur Skeleton-Deklarationen ohne Test-Logik.
       `bin/rails test test/system/devise_flows_test.rb` muss exit 0 mit 0 Tests gelaufen liefern.
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_master && test -f test/support/letter_opener_helper.rb && test -f test/support/devise_test_helpers.rb && test -f test/system/devise_flows_test.rb && bin/rails runner 'require_relative "test/support/letter_opener_helper"; require_relative "test/support/devise_test_helpers"; puts "Modules: #{LetterOpenerHelper.name} + #{DeviseTestHelpers.name}"' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test/support/letter_opener_helper.rb` existiert; enthaelt `module LetterOpenerHelper`
    - `test/support/devise_test_helpers.rb` existiert; enthaelt `module DeviseTestHelpers`
    - `test/system/devise_flows_test.rb` existiert; enthaelt `class DeviseFlowsTest < ApplicationSystemTestCase`
    - `bin/rails runner 'puts LetterOpenerHelper.name'` exit 0 + Output "LetterOpenerHelper"
    - `bin/rails runner 'puts DeviseTestHelpers.name'` exit 0 + Output "DeviseTestHelpers"
    - `bundle exec standardrb test/support/letter_opener_helper.rb test/support/devise_test_helpers.rb test/system/devise_flows_test.rb` exit 0
    - VALIDATION.md Wave-0 Requirements: 6 von 6 Pflichtlieferungen vorhanden
  </acceptance_criteria>
  <done>Alle Wave-0-Pflichtlieferungen aus VALIDATION.md erfuellt: Mailer-Dir, devise_mailer_test.rb, devise_flows_test.rb-Skeleton, letter_opener_helper.rb-Skeleton, devise_test_helpers.rb-Skeleton, letter_opener_web-Config. Plan-05 kann auf devise_flows_test.rb-Skeleton aufbauen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Env → Production-Config | Test-Helper darf KEINE production-Mail-Konfiguration befeuern (mail-Deliveries laufen nur via :test-Adapter in test-env) |
| Dev-Browser → letter_opener_web | Dev-Mailbox bei /letter_opener — nur in Rails.env.development?, NICHT in Production gemountet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Information Disclosure | LetterOpenerWeb::Engine | mitigate | Route ist explizit auf `if Rails.env.development?` gegated; Production-Mount unmöglich |
| T-41-01-02 | Spoofing | Mail-Helper extract_*_url | accept | Helper läuft NUR in Tests; kein Production-Risiko; Regex erlaubt nur localhost-ähnliche URLs durch Devise's default_url_options |
| T-41-01-03 | Repudiation | ActionMailer::Base.deliveries in Tests | mitigate | clear_mail_queue im setup verhindert Cross-Test-Contamination; Tests bleiben deterministisch |
</threat_model>

<verification>
- `bin/rails test test/mailers/ test/controllers/passwords_controller_test.rb test/controllers/confirmations_controller_test.rb` läuft in <40s
- Mindestens 12 Tests existieren (5 Mailer + 4 Passwords + 3 Confirmations)
- `bin/rails routes 2>/dev/null | grep letter_opener` zeigt ≥1 Route (in Dev-Env; in Test-Env Route ist nicht gemountet — `if Rails.env.development?`)
- Standard-Ruby-Linting clean: `bundle exec standardrb test/mailers test/controllers/passwords_controller_test.rb test/controllers/confirmations_controller_test.rb test/support/mail_helpers.rb test/support/letter_opener_helper.rb test/support/devise_test_helpers.rb test/system/devise_flows_test.rb` exit 0
- Wave-0-Pflichtlieferungen aus VALIDATION.md: 6 von 6 Files vorhanden
</verification>

<success_criteria>
Wave-0-Definition-of-Done erreicht (gemäß VALIDATION.md):
- test/mailers/-Verzeichnis existiert
- Layer-3 (Mailer) Charakterisierung: 5 Tests, deterministisch
- Layer-2 (Controller) Charakterisierung für Forgot/Reset/Confirm: 7 Tests, deterministisch
- Mail-Helper für Token-Extraktion einsatzbereit für Plan-05 System-Tests, sauber in 3 Test-Basisklassen included
- Skeleton-Files (letter_opener_helper.rb + devise_test_helpers.rb + devise_flows_test.rb) vorhanden
- letter_opener_web in Dev konfiguriert + Route gemountet → Dev-Mail-Sichtbarkeit hergestellt
- KEIN Test-Crash, KEINE Missing-Route — Devise-Default-Routes funktionieren
</success_criteria>

<output>
After completion, create `.planning/phases/41-devise-auth-cluster-ueberarbeitung-registrierung-forgot-pass/41-01-SUMMARY.md` mit:
- Test-Count pro Layer (initial)
- Initial-Test-Status (pass/fail count) — explizit für nachgelagerte Plans dokumentiert
- Etwaige Devise-Routen-Diskrepanzen aufgedeckt (z.B. ob `user_password_path` existiert oder customer Override-Route nötig)
- Liste der Mail-Helper-Methoden mit Signatur
- Verifikation: VALIDATION.md Wave-0 Requirements (6 Files) vollstaendig (= wave_0_complete: true in VALIDATION.md setzen)
- Liste der MailHelpers-Includes in test_helper.rb (3 Test-Basisklassen)
</output>
</content>
