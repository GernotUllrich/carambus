---
phase: 16-controller-job-channel-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/controllers/tournament_monitors_controller_test.rb
  - test/channels/tournament_channel_test.rb
  - test/channels/tournament_monitor_channel_test.rb
  - test/jobs/tournament_status_update_job_test.rb
  - test/jobs/tournament_monitor_update_results_job_test.rb
autonomous: true
requirements:
  - COV-02
  - COV-03
  - COV-04
  - COV-05
  - COV-06

must_haves:
  truths:
    - "TournamentMonitorsController actions (index, show, edit, update, destroy, switch_players, start_round_games, update_games) are tested"
    - "TournamentMonitorChannel subscription and rejection paths are tested"
    - "TournamentChannel subscription with and without tournament_id is tested"
    - "TournamentStatusUpdateJob guard paths (no monitor, not started) are tested"
    - "TournamentMonitorUpdateResultsJob skip path (API server) and execute path (local server) are tested"
  artifacts:
    - path: "test/controllers/tournament_monitors_controller_test.rb"
      provides: "TournamentMonitorsController test coverage"
      min_lines: 60
    - path: "test/channels/tournament_channel_test.rb"
      provides: "TournamentChannel test coverage"
      min_lines: 15
    - path: "test/channels/tournament_monitor_channel_test.rb"
      provides: "TournamentMonitorChannel test coverage"
      min_lines: 20
    - path: "test/jobs/tournament_status_update_job_test.rb"
      provides: "TournamentStatusUpdateJob test coverage"
      min_lines: 20
    - path: "test/jobs/tournament_monitor_update_results_job_test.rb"
      provides: "TournamentMonitorUpdateResultsJob test coverage"
      min_lines: 15
  key_links:
    - from: "test/controllers/tournament_monitors_controller_test.rb"
      to: "app/controllers/tournament_monitors_controller.rb"
      via: "HTTP integration test requests"
      pattern: "post\\|get\\|patch\\|delete.*tournament_monitor"
    - from: "test/channels/tournament_channel_test.rb"
      to: "app/channels/tournament_channel.rb"
      via: "ActionCable::Channel::TestCase"
      pattern: "subscribe\\|assert_has_stream"
    - from: "test/jobs/tournament_status_update_job_test.rb"
      to: "app/jobs/tournament_status_update_job.rb"
      via: "perform_now"
      pattern: "TournamentStatusUpdateJob.perform_now"
---

<objective>
Create test coverage for TournamentMonitorsController, both ActionCable channels, and both background jobs that touch Tournament/TournamentMonitor.

Purpose: COV-02 through COV-06 require test coverage for the remaining controllers, channels, and jobs. These are smaller targets (214 lines, 19 lines, 17 lines, 101 lines, 32 lines) that can be covered in one plan.
Output: 5 new test files covering all targets.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-controller-job-channel-coverage/16-RESEARCH.md

<interfaces>
<!-- Key types and contracts needed for these tests -->

From app/controllers/tournament_monitors_controller.rb:
- before_action :set_tournament_monitor — uses TournamentMonitor.find(params[:id])
- before_action :ensure_tournament_director — requires current_user.club_admin? || current_user.system_admin?
- before_action :ensure_local_server — same guard as TournamentsController
- 7 public actions: index, show, edit, update, destroy, switch_players, start_round_games, update_games

From app/channels/tournament_channel.rb:
- subscribed: streams from "tournament-stream-#{params[:tournament_id]}" or "tournament-stream"
- unsubscribed: logs only

From app/channels/tournament_monitor_channel.rb:
- subscribed: rejects if !ApplicationRecord.local_server?; else streams from "tournament-monitor-stream"
- unsubscribed: logs only

From app/jobs/tournament_status_update_job.rb:
- discard_on ActiveRecord::RecordNotFound
- Guards: returns early if no tournament_monitor or tournament not started
- Renders partial via ApplicationController.renderer

From app/jobs/tournament_monitor_update_results_job.rb:
- Guards: returns early on API server (ApplicationRecord.local_server? false)
- Renders two partials + broadcasts

Fixtures:
- tournaments(:local) — id 50_000_001, state: "registration"
- users(:club_admin) — role: club_admin (for ensure_tournament_director)
- No tournament_monitors fixture — must create in setup

Local server pattern:
```ruby
@original_api_url = Carambus.config.carambus_api_url
Carambus.config.carambus_api_url = "http://local.test"
# teardown: restore
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TournamentMonitorsController test coverage</name>
  <files>test/controllers/tournament_monitors_controller_test.rb</files>
  <read_first>
    - app/controllers/tournament_monitors_controller.rb
    - test/controllers/table_monitors_controller_test.rb
    - test/test_helper.rb
    - test/fixtures/tournaments.yml
    - test/fixtures/users.yml
    - test/support/t04_tournament_test_helper.rb (if exists, for TournamentMonitor creation pattern)
  </read_first>
  <action>
Create test/controllers/tournament_monitors_controller_test.rb using ActionDispatch::IntegrationTest.

**Setup:**
```ruby
setup do
  @original_api_url = Carambus.config.carambus_api_url
  Carambus.config.carambus_api_url = "http://local.test"
  @club_admin = users(:club_admin)
  @tournament = tournaments(:local)
  sign_in @club_admin
end

teardown do
  Carambus.config.carambus_api_url = @original_api_url
end
```

Note: TournamentMonitor records do not have fixtures. For actions that need a TournamentMonitor record (show, edit, update, destroy, switch_players, start_round_games, update_games), create one in setup. Check how Phase 11-15 characterization tests create TournamentMonitors — likely via `@tournament.initialize_tournament_monitor` or direct `TournamentMonitor.create!`. If creating a TournamentMonitor requires complex setup (tournament must be in specific AASM state), test the redirect/error behavior instead.

**Tests to write:**

1. **Auth guard tests:**
   - `ensure_tournament_director` redirect: sign in as `users(:one)` (basic user), attempt GET show for a tournament_monitor — assert redirect to root_path
   - `ensure_local_server` redirect: clear `Carambus.config.carambus_api_url`, attempt GET show — assert redirect

2. **Index (no auth guard beyond sign-in):**
   - GET tournament_monitors_url — assert_response :success

3. **CRUD actions (with local server + club_admin):**
   - `show`: GET tournament_monitor_url(@tournament_monitor) — assert_response (success or redirect)
   - `edit`: GET edit_tournament_monitor_url(@tournament_monitor) — assert_response
   - `update`: PATCH tournament_monitor_url(@tournament_monitor) — assert redirect
   - `destroy`: DELETE tournament_monitor_url(@tournament_monitor) — assert_difference + redirect

4. **Game pipeline actions:**
   - `switch_players`: POST switch_players_tournament_monitor_url(@tournament_monitor) — assert redirect (will likely redirect since no games exist)
   - `start_round_games`: POST start_round_games_tournament_monitor_url(@tournament_monitor) — assert redirect
   - `update_games`: POST update_games_tournament_monitor_url(@tournament_monitor) — assert redirect (empty params = no games processed)

For actions that raise due to missing game/table_monitor records, catch the expected behavior (redirect or handled error). The goal is to verify the guard chain works and the action responds without unhandled exceptions.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/tournament_monitors_controller_test.rb -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `# frozen_string_literal: true` as first line
    - Class inherits from ActionDispatch::IntegrationTest
    - At least 1 test for ensure_tournament_director guard (basic user redirected)
    - At least 1 test for ensure_local_server guard (API server redirected)
    - At least 1 test for index
    - Tests for show, edit, update, destroy (CRUD)
    - Tests for switch_players, start_round_games, update_games (game pipeline)
    - All tests pass (0 failures, 0 errors)
  </acceptance_criteria>
  <done>TournamentMonitorsController has test coverage for all 7+ actions including auth guards, local server guard, and game pipeline actions. COV-02 is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Channel and job test coverage</name>
  <files>
    test/channels/tournament_channel_test.rb
    test/channels/tournament_monitor_channel_test.rb
    test/jobs/tournament_status_update_job_test.rb
    test/jobs/tournament_monitor_update_results_job_test.rb
  </files>
  <read_first>
    - app/channels/tournament_channel.rb
    - app/channels/tournament_monitor_channel.rb
    - app/jobs/tournament_status_update_job.rb
    - app/jobs/tournament_monitor_update_results_job.rb
    - test/test_helper.rb
    - test/fixtures/tournaments.yml
  </read_first>
  <action>
Create 4 test files. Create `test/channels/` and `test/jobs/` directories first (they do not exist yet).

**1. test/channels/tournament_channel_test.rb:**
```ruby
# frozen_string_literal: true

require "test_helper"

class TournamentChannelTest < ActionCable::Channel::TestCase
  test "subscribes with tournament_id and streams from specific stream" do
    subscribe(tournament_id: 42)
    assert subscription.confirmed?
    assert_has_stream "tournament-stream-42"
  end

  test "subscribes without tournament_id and streams from generic stream" do
    subscribe
    assert subscription.confirmed?
    assert_has_stream "tournament-stream"
  end
end
```

**2. test/channels/tournament_monitor_channel_test.rb:**
```ruby
# frozen_string_literal: true

require "test_helper"

class TournamentMonitorChannelTest < ActionCable::Channel::TestCase
  test "rejects subscription on API server" do
    ApplicationRecord.stub(:local_server?, false) do
      subscribe
      assert subscription.rejected?
    end
  end

  test "confirms subscription on local server" do
    ApplicationRecord.stub(:local_server?, true) do
      subscribe
      assert subscription.confirmed?
      assert_has_stream "tournament-monitor-stream"
    end
  end
end
```

**3. test/jobs/tournament_status_update_job_test.rb:**

Use `ActiveSupport::TestCase` with `include ActiveJob::TestHelper`.

Tests:
- `returns early when tournament has no tournament_monitor`: Use `tournaments(:local)` which has no tournament_monitor. Call `TournamentStatusUpdateJob.perform_now(@tournament)`. Assert `assert_nothing_raised`.
- `returns early when tournament is not started`: Tournament in "registration" state. Call perform_now. Assert `assert_nothing_raised`.
- `discards on ActiveRecord::RecordNotFound`: The job has `discard_on ActiveRecord::RecordNotFound`. Create a mock tournament ID that doesn't exist. If calling `perform_now` with a non-existent record, verify it handles gracefully (may raise or discard depending on how the job receives the argument — check if it takes a Tournament object or an ID).

Read the job source carefully to understand the argument type. If it takes a Tournament object, the RecordNotFound guard only matters for internal finds. If it takes an ID, test with a non-existent ID.

**4. test/jobs/tournament_monitor_update_results_job_test.rb:**

Use `ActiveSupport::TestCase` with `include ActiveJob::TestHelper`.

Tests:
- `skips on API server (default test env)`: Default test env has blank carambus_api_url (API server context). Call `perform_now`. Assert `assert_nothing_raised`. The job should return early.
- `executes on local server`: Set `Carambus.config.carambus_api_url = "http://local.test"`. Call `perform_now` with a TournamentMonitor. Use `assert_nothing_raised` — the job renders partials which may or may not work in test context, but should not raise unhandled exceptions. Save/restore carambus_api_url in setup/teardown.

Read the job source to understand what argument it takes (TournamentMonitor object or ID) and whether the `local_server?` check is on `ApplicationRecord` or `Carambus.config`. Adjust the stub/config pattern accordingly.
  </action>
  <verify>
    <automated>bin/rails test test/channels/ test/jobs/ -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Directory test/channels/ exists with 2 test files
    - Directory test/jobs/ exists with 2 test files
    - All 4 files have `# frozen_string_literal: true` as first line
    - TournamentChannelTest has 2 tests (with/without tournament_id)
    - TournamentMonitorChannelTest has 2 tests (API reject / local confirm)
    - TournamentStatusUpdateJobTest has at least 2 tests (guard paths)
    - TournamentMonitorUpdateResultsJobTest has at least 2 tests (skip/execute paths)
    - All tests pass (0 failures, 0 errors)
  </acceptance_criteria>
  <done>Both channels and both jobs have test coverage. COV-03, COV-04, COV-05, COV-06 are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan only adds test files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | N/A | test files only | accept | No production code changes; no new attack surface |
</threat_model>

<verification>
- `bin/rails test test/controllers/tournament_monitors_controller_test.rb test/channels/ test/jobs/` passes with 0 failures, 0 errors
- 5 new test files exist covering all COV-02 through COV-06 targets
</verification>

<success_criteria>
- TournamentMonitorsController, both channels, and both jobs have test coverage
- All tests pass
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/16-controller-job-channel-coverage/16-02-SUMMARY.md`
</output>
