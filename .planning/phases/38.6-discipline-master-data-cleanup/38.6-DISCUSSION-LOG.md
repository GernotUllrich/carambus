# Phase 38.6: Discipline Master-Data Cleanup - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 38.6-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-29
**Phase:** 38.6-discipline-master-data-cleanup
**Areas discussed:** Phase Definition (User-vorgeschlagen), Merge-Map, PlayerRanking-Konflikt, Sync-Mechanik, Skript-Format, Verification, CC-Konflikt-Handling, super_discipline self-ref, PaperTrail Versions, Idempotenz, Stats-First-Design

---

## Phase Definition (Initial-Briefing)

User-Beschreibung als Ausgangspunkt:

> Discipline-Duplikate auf carambus_api Production:
> [[57, "BK 2kombi"], [61, "BK 50"], [59, "BK2-Kombi"], [60, "BK 2-100"], [62, "BK 100"], [95, "BK-2 Kombi"], [108, "BK50"], [109, "BK100"], [110, "BK-2"], [111, "BK-2plus"], [107, "BK2-Kombi"]]
> 4 Rows für BK-2kombi, 2 für BK50, 3 für BK100.
> Korrekte Namen: BK-2, BK-2plus, BK50, BK100, BK-2kombi.
> Wichtig: bei Merge müssen die Associations übertragen werden.
> Discipline.reflections.keys = 17 (versions, discipline_tournament_plans, table_kind, super_discipline, sub_disciplines, tournaments, player_classes, player_rankings, discipline_cc, leagues, game_plan_ccs, game_plan_row_ccs, seeding_plays, competition_cc, branch_cc, training_concept_disciplines, training_concepts)

Phase 38.6 musste neu in ROADMAP eingefügt werden (existierte vorher nicht).

---

## Merge-Map / Winner-IDs

**Frage:** Welche IDs werden Winner pro kanonischem Namen?

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Niedrigste ID | 57/61/62 als Winner | |
| Höchste ID (Phase-38.5-Anker) | 107/108/109/110/111 | ✓ |
| Manuelle Wahl pro Name | Per Discipline einzeln | |

**User's choice:** 107/108/109/110/111 (implizit über "roadmap ok")
**Rationale:** Phase 38.5-Seed (`script/seed_bk2_disciplines.rb`) hat diese IDs als Anker fixiert; abweichende Wahl würde 38.5-Re-Seed erzwingen.

---

## id 60 "BK 2-100" Klassifizierung

**Frage:** Was ist id 60 — Typo, eigene Discipline, oder Duplikat?

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Typo von BK100 → Loser für 109 | "BK 2-100" semantisch = BK100 | ✓ |
| Eigene Discipline (2-Set-Variante) | Behalten, ggf. umbenennen | |
| Garbage → Löschen ohne Merge | Keine Associations transferieren | |

**User's choice:** Loser für 109 (BK100)
**User-Quote:** "BK100 is a special case of BK-2 with balls_goal 100. BK2-100 is the same BK100"

---

## PlayerRanking-Konflikt-Strategie

**Frage:** Was passiert, wenn `(player_id, discipline_id)`-Tupel bei FK-Update kollidiert (Loser-Ranking + Winner-Ranking für selben Player existieren)?

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Highest-Wert behalten | Loser/Winner-Werte vergleichen, höchsten merge | |
| Neuesten Timestamp behalten | `updated_at`-Vergleich | |
| Loser destroyen + Recompute markieren | FK-Update, bei Konflikt: Loser löschen + Recompute-Liste | ✓ |
| Sum/Avg | Werte aggregieren | |

**User's choice:** Loser destroyen + Recompute markieren
**User-Quote:** "PlayerRankings can be recomputed later. but in the tupel (player_id, discipline_id) the discipline_id must be updated. If conflikt with existing tupel => recompute for Discipline is necessary"
**Rationale:** Rankings sind aus Game/Party-Daten ableitbar; kein Wert in Merge-Heuristik investieren, wenn Recompute autoritativ ist.

---

## Sync-Pfad an Local-Server

**Frage:** Wie erfahren Local-Server (carambus_bcw, _phat, etc.) von den Mergeaktionen?

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Manueller DB-Sync auf jedem Local-Server | Skript erneut auf jedem Local | |
| `Version#update_from_carambus_api` (PaperTrail) | Nur PaperTrail-aware Calls auf api → propagiert | ✓ |
| Broadcast via Redis/CableReady | Custom Sync-Mechanism | |

**User's choice:** PaperTrail-Versions
**User-Quote:** "Version#update_from_carambus_api - yes everything that causes PaperTrail to produce a Version record. so no update_colunm od delete etc."
**Konsequenz (D-04):** Skript-Verbot von `update_column`, `delete_all`, `update_all`, raw SQL.

---

## Skript-Format (Migration vs. One-shot)

**Frage:** Rails-Migration oder Ruby-Skript?

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Rails-Migration | `db:migrate`-tracked, irreversibel-by-default | |
| One-shot Ruby-Skript | `bin/rails runner script/merge_bk_disciplines.rb` | ✓ |
| Rake-Task | `bin/rails carambus:merge_disciplines` | |

**User's choice:** One-shot Skript
**User-Quote:** "I would prefer a script - can be tested in development and if ok to production"
**Rationale:** Matched bestehendes Pattern (`script/seed_bk2_disciplines.rb`); kann ohne Migration-Schema-Lock im dev iteriert werden.

---

## Verification / Dry-Run

**Frage:** Brauchen wir einen separaten Dry-Run-Modus oder reicht das Dev-Protokoll?

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| Separater `--dry-run` Flag | Skript simuliert ohne Mutation | |
| Before/After-Protokoll im Dev-Run | Stats vorher + Aktion + Stats nachher als Markdown | ✓ |
| Beides kombiniert | --dry-run schreibt Stats-Only-Output | |

**User's choice:** Dev-Protokoll
**User-Quote:** "not necessary - we can make a protocoll of the associations before and after in development"
**Konsequenz (D-06):** Markdown nach `tmp/merge-bk-disciplines-{timestamp}.md`.

---

## Stats-First-Design (User-Vorschlag)

**User-Quote:** "at first we should make statistics - most associations are not used with discipline_id at all"

**Diskussion:** Stats-Phase wird zu Step-0 des Skripts (D-07). Output informiert (a) ob CC-Konflikt-Handler überhaupt nötig (D-08 möglicherweise nie getriggert), (b) Größenordnung des PlayerRanking-FK-Updates, (c) Before-Snapshot für D-06 Protokoll.

**Selected:** Stats-Phase als integraler Skript-Schritt 0, kein separates Tool.

---

## CC-Konflikt-Handling (Q1 in Discussion)

**Frage:** `discipline_cc`/`competition_cc`/`branch_cc` sind `has_one dependent: :destroy`. Wenn Winner und Loser beide eine CC-Row haben, was passiert?

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| (a) Winner wins | Loser-CC cascade-deleten, Winner behalten | |
| (b) Loser wins | Winner-CC vorher destroyen, Loser-CC umhängen | |
| (c) Manual review | Skript hält an, druckt Konflikt, User entscheidet | ✓ |

**User's choice:** (c) Manual review
**Konsequenz (D-08):** Skript ist interaktiv für CC-Konflikte. Output: Winner-CC vs. Loser-CC mit allen Feldern; Eingabe: keep-winner / move-loser / merge-fields / abort.

---

## super_discipline self-ref (Q2 in Discussion)

**Frage:** Loser können `super_discipline` für andere Rows sein (oder via `super_discipline_id` auf andere Loser zeigen). Wie behandeln?

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| (a) Pre-Scan in dev | Vor Skript-Run prüfen, Befunde manuell handhaben | |
| (b) Defensiv im Skript | Pass 1: sub_disciplines reassignen; Pass 2: loser-zu-loser obsolet bei destroy | ✓ |

**User's choice:** (b) Defensiv im Skript
**Konsequenz (D-09):** Zwei-Pass-Strategie im Skript-Schritt 4 (Merge).

---

## PaperTrail `versions` der Loser (Q3 in Discussion)

**Frage:** Was passiert mit den PaperTrail-Audit-Versions der Loser nach Destroy?

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| (a) Leave behind | Versions bleiben mit ursprünglichem item_id, PaperTrail-Default | |
| (b) Reassign zu Winner | item_id rewriten (raw SQL → verletzt D-04) | |
| (c) Annahme: Local-Server sync | Versions-Reassign nicht nötig, alles ist bereits propagiert | ✓ |

**User's choice:** (c) Annahme synchroner Local-Server
**User-Quote:** "assume that all local servers are synchronized"
**Konsequenz (D-10):** Skript ignoriert `versions`-Reflection vollständig; Audit-History bleibt korrekt am ursprünglichen item_id (jetzt orphan, aber audit-mäßig sauber).

---

## Idempotenz (Q4 in Discussion)

**Frage:** Was passiert bei Re-Run des Skripts nach erfolgreichem ersten Run?

| Option | Beschreibung | Selected |
|--------|-------------|----------|
| (a) Sicherer No-Op | Loser-Existenz-Check, exit 0 wenn keine mehr | ✓ |
| (b) Error | Skript wirft Exception, wenn keine Loser mehr | |

**User's choice:** (a) Sicherer No-Op
**Konsequenz (D-11):** Pre-flight-Check: `Discipline.where(id: [57, 59, 60, 61, 62, 95]).exists?` → false → log "bereits durchgeführt", exit 0.

---

## Claude's Discretion

Folgende Punkte explizit nicht entschieden (downstream Plan-Phase frei):
- Genaue Logging-Verbosity (Rails.logger vs puts vs Markdown-Append-Reihenfolge)
- Exakte Spaltenformatierung der Markdown-Tabellen
- Exception-Handling für unerwartete Reflection-Konflikte (z.B. neue Reflection seit Schema-Änderung)
- Transaction-Wrapping (pro Loser oder gesamt) — Empfehlung: pro Loser eine Transaction
- CableReady-Broadcast-Pausierung während Skript-Run (zu klären in Plan-Phase, falls Broadcast-Storm)

## Deferred Ideas

- PlayerRanking-Recompute-Job (eigener Task nach 38.6)
- Test-Fixture-Cleanup `test/fixtures/disciplines.yml` (eigener Plan-Schritt innerhalb 38.6)
- Andere Discipline-Familien (Karambol/Snooker/Pool/Kegel/Biathlon) — Out-of-scope, ggf. eigene Phase 38.7
- Skript-Generalisierung zu `merge_records.rb` Library — Out-of-scope
