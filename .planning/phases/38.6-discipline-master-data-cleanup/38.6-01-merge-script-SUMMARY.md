---
phase: 38.6-discipline-master-data-cleanup
plan: "01"
subsystem: data-cleanup
tags:
  - discipline
  - merge
  - papertrail
  - bk-family
  - carambus_api
dependency_graph:
  requires:
    - "Phase 38.5 seed (seed_bk2_disciplines.rb) — Winner IDs 107/108/109/110/111 must exist"
  provides:
    - "script/merge_bk_disciplines.rb — one-shot Ruby script for BK-* Discipline merge"
  affects:
    - "carambus_api production DB (Discipline rows 57,59,60,61,62,95)"
    - "Local servers receive changes via PaperTrail Version sync (Version#update_from_carambus_api)"
tech_stack:
  added: []
  patterns:
    - "One-shot runner script via bin/rails runner (pattern from seed_bk2_disciplines.rb)"
    - "Per-loser transaction isolation (D-13 discretion)"
    - "Stats-first design (D-07) — emit matrix before any mutation"
    - "PaperTrail-aware API only — update!/destroy/save! (D-04)"
    - "Interactive CC-conflict halt with [k/m/a] prompt (D-08)"
    - "RECOMPUTE_LIST for PlayerRanking unique-constraint conflicts (D-03)"
    - "D-09 Pass 1: super_discipline self-ref children reassigned before loser.destroy"
key_files:
  created:
    - "script/merge_bk_disciplines.rb"
  modified: []
decisions:
  - "All 6 D-13 phases implemented in a single write — script is coherent and complete"
  - "Comment-only references to forbidden APIs in header allowed (plan mandated them verbatim); no code-level usage"
  - "WINNER_CANONICAL_NAMES covers all 5 winners including BK50/BK100/BK-2/BK-2plus for future-proof rename detection"
metrics:
  duration_minutes: 3
  completed_date: "2026-04-29"
  tasks_completed: 4
  files_created: 1
  files_modified: 0
---

# Phase 38.6 Plan 01: Merge Script — Summary

One-shot Ruby script `script/merge_bk_disciplines.rb` that merges 6 duplicate BK-* Discipline rows (losers 57/59/60/61/62/95) into 5 canonical winners (107/108/109/110/111) using exclusively PaperTrail-aware AR-API across 6 sequentially-ordered phases with per-loser transaction isolation and an interactive CC-conflict halt.

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | Header + constants + pre-flight phase | 87a8d010 |
| 2 | Stats phase (D-07) + CC-Conflict scan (D-08) | 87a8d010 |
| 3 | Merge phase (D-13 step 4 + D-03 PlayerRanking + D-09 super_discipline) | 87a8d010 |
| 4 | Rename phase (D-12) + Post-flight protocol + Winner deltas + Recompute list | 87a8d010 |

All 4 tasks were implemented in a single file write (the artifact is a single coherent Ruby script); committed atomically.

## Constants and Reflection Plan

### WINNER_TO_LOSERS (merge map — D-01/D-02)

```ruby
WINNER_TO_LOSERS = {
  107 => [57, 59, 95],   # BK-2kombi ← "BK 2kombi", "BK2-Kombi", "BK-2 Kombi"
  108 => [61],           # BK50 ← "BK 50"
  109 => [60, 62],       # BK100 ← "BK 2-100" (D-02 semantic), "BK 100"
  110 => [],             # BK-2 (no losers)
  111 => []              # BK-2plus (no losers)
}.freeze
```

### DISCIPLINE_ID_FK_REFLECTIONS (11 entries — all direct discipline_id FK reflections)

```
discipline_tournament_plans, tournaments, player_classes, player_rankings,
discipline_cc, leagues, game_plan_ccs, game_plan_row_ccs,
competition_cc, branch_cc, training_concept_disciplines
```

**Excluded with comments:**
- `:versions` — SKIP per D-10 (audit history stays at original item_id)
- `:training_concepts` — through-only reflection (no direct FK)
- `:sub_disciplines` — handled separately as D-09 Pass 1 (FK is super_discipline_id)
- `:seeding_plays` — handled separately (FK is playing_discipline_id, not discipline_id)
- `:table_kind`, `:super_discipline` — belongs_to on Discipline itself; destroyed with loser

## Forbidden-APIs Audit

Zero code-level matches for: `update_column|update_columns|update_all|delete_all|paper_trail.without_versioning|.unprotected = true`

The only match is on line 13 (header comment block that mandates "NEVER use these APIs") — expected and required by the plan's file header specification.

## PaperTrail-Aware API Call Count

12 occurrences of `update!|.destroy\b|save!` in the script (threshold requirement: >= 8). Verified via grep.

## File Line Count

460 lines (requirement: >= 250 lines).

## Six D-13 Phases Implemented

1. **Pre-flight** — Loser-Existenz-Check (D-11 idempotent exit) + Winner-Existenz-Check
2. **Stats** — Per-loser × all-reflections row count matrix to protocol (D-07)
3. **CC-Konflikt-Scan** — Interactive [k/m/a] halt on winner+loser CC-conflict (D-08)
4. **Merge** — Per-loser transaction: D-09 super_discipline children, all FK reassigns, D-03 PlayerRanking conflict handling, loser.destroy
5. **Rename** — WINNER_CANONICAL_NAMES.each with update!(name:) for D-12 rename of id=107
6. **Post-flight** — Winner reflection-count matrix + RECOMPUTE_LIST serialization to protocol

## What Is OUT of Scope

- **Running the script against any database** — this plan delivers the artifact only. Plan 03 (dev-dry-run, human checkpoint) gates the actual dev-DB execution. Production run is a manual deployment step entirely outside GSD execute-phase.
- **PlayerRanking recompute** — RECOMPUTE_LIST is collected and written to protocol; actual recompute is a separate deferred task.
- **Test fixture updates** — Plan 02 handles fixtures-canonical-rename.
- **Local server sync** — automatic via Version#update_from_carambus_api after production run.

## Deviations from Plan

None — plan executed exactly as written. All 4 tasks' acceptance criteria verified. All 10 plan-level verification commands pass.

## Known Stubs

None. The script is a one-shot data migration tool; there are no UI stubs or placeholder values.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. The script is a one-shot data migration tool that operates exclusively through the existing AR model layer.

## Self-Check: PASSED

- script/merge_bk_disciplines.rb: FOUND
- SUMMARY.md: FOUND
- commit 87a8d010: FOUND
