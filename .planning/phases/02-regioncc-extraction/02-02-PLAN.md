---
phase: 02-regioncc-extraction
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/services/region_cc/league_syncer.rb
  - app/services/region_cc/club_syncer.rb
  - app/services/region_cc/branch_syncer.rb
  - test/services/region_cc/league_syncer_test.rb
  - test/services/region_cc/club_syncer_test.rb
  - test/services/region_cc/branch_syncer_test.rb
autonomous: true
requirements: [RGCC-02]
must_haves:
  truths:
    - "LeagueSyncer produces identical database records to calling sync_leagues, sync_league_teams, sync_league_teams_new, sync_league_plan, sync_team_players, sync_team_players_structure directly on RegionCc"
    - "ClubSyncer produces identical database records to calling sync_clubs directly on RegionCc"
    - "BranchSyncer produces identical database records to calling sync_branches directly on RegionCc"
    - "Each syncer accepts an injected ClubCloudClient and uses its get/post methods instead of calling RegionCc HTTP methods"
  artifacts:
    - path: "app/services/region_cc/league_syncer.rb"
      provides: "League sync operations"
      contains: "class RegionCc::LeagueSyncer < ApplicationService"
    - path: "app/services/region_cc/club_syncer.rb"
      provides: "Club sync operations"
      contains: "class RegionCc::ClubSyncer < ApplicationService"
    - path: "app/services/region_cc/branch_syncer.rb"
      provides: "Branch sync operations"
      contains: "class RegionCc::BranchSyncer < ApplicationService"
  key_links:
    - from: "app/services/region_cc/league_syncer.rb"
      to: "app/services/region_cc/club_cloud_client.rb"
      via: "injected client.post / client.get calls"
      pattern: "@client\\.post\\|@client\\.get"
---

<objective>
Extract league, club, and branch sync methods from RegionCc into three standalone service classes.

Purpose: These are the core data structure syncers — leagues and clubs form the foundation that tournament and party syncing depends on. BranchSyncer is small but required by all other syncers. Per D-03, each syncer is its own class. Per D-04, each inherits ApplicationService with `.call(kwargs)`.

Output: Three syncer classes with unit tests using injected ClubCloudClient doubles.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-regioncc-extraction/02-CONTEXT.md
@.planning/phases/02-regioncc-extraction/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — ClubCloudClient interface -->
```ruby
class RegionCc::ClubCloudClient
  PATH_MAP = { ... }.freeze
  attr_reader :base_url, :username, :userpw
  
  def initialize(base_url:, username:, userpw:)
  def get(action, get_options = {}, opts = {})        # returns [response, nokogiri_doc]
  def get_with_url(action, url, get_options = {}, opts = {})
  def post(action, post_options = {}, opts = {})      # returns [response, nokogiri_doc]
  def post_with_formdata(action, post_options = {}, opts = {})
end
```

<!-- From app/services/application_service.rb -->
```ruby
class ApplicationService
  def self.call(kwargs = {})
    new(kwargs).call
  end
end
```

<!-- Key models referenced by syncers: -->
<!-- League, LeagueCc, LeagueTeam, LeagueTeamCc, Season, Region, BranchCc, CompetitionCc, SeasonCc -->
<!-- Club, ClubCc -->
<!-- Branch, BranchCc -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract LeagueSyncer, ClubSyncer, and BranchSyncer service classes</name>
  <files>app/services/region_cc/league_syncer.rb, app/services/region_cc/club_syncer.rb, app/services/region_cc/branch_syncer.rb</files>
  <read_first>
    - app/models/region_cc.rb (lines 1883-2500 for sync_leagues, sync_league_teams, sync_league_teams_new, sync_league_plan; lines 2498-2543 for sync_clubs; lines 1621-1647 for sync_branches; lines 795-870 for sync_team_players_structure and sync_team_players at 2676-2728)
    - app/services/region_cc/club_cloud_client.rb (ClubCloudClient interface from Plan 01)
    - app/services/application_service.rb (base class)
  </read_first>
  <action>
    Create three service classes in `app/services/region_cc/`:

    **1. RegionCc::BranchSyncer** (`branch_syncer.rb`)
    - Extracts: `sync_branches` (lines 1621-1647, ~28 lines)
    - Constructor: `def initialize(region_cc:, client:, **opts)` — stores @region_cc, @client, @opts
    - `def call` — moves the body of sync_branches
    - Replace `get_cc("showClubList", {}, opts)` with `@client.get("showClubList", {}, @opts)`
    - Replace `id` references with `@region_cc.id`
    - Replace `shortname` with `@region_cc.shortname`
    - Keep all BranchCc.find_by_cc_id / BranchCc.new / branch_cc.save logic
    - Keep RegionCc.logger calls (REPORT_LOGGER is a class-level logger)
    - Return value: array of branches (same as original)

    **2. RegionCc::ClubSyncer** (`club_syncer.rb`)
    - Extracts: `sync_clubs` (lines 2498-2543, ~47 lines)
    - Constructor: `def initialize(region_cc:, client:, **opts)`
    - `def call` — moves the body of sync_clubs
    - Replace `post_cc(...)` with `@client.post(...)`
    - Replace `cc_id` references with `@region_cc.cc_id`
    - Keep all Club.find_by_cc_id, Club.where logic
    - Note line 2499: `context ||= "nbv"` should use `@region_cc.shortname.downcase` instead of hardcoded "nbv"
    - Keep RegionCc.logger calls
    - Return value: array of done_clubs (same as original)

    **3. RegionCc::LeagueSyncer** (`league_syncer.rb`)
    - Extracts: `sync_leagues` (lines 1883-1988, ~107 lines), `sync_league_teams` (lines 2226-2361, ~136 lines), `sync_league_teams_new` (lines 2047-2225, ~179 lines), `sync_league_plan` (lines 2362-2497, ~136 lines), `sync_team_players` (lines 2676-2728, ~53 lines), `sync_team_players_structure` (lines 795-870, ~77 lines)
    - Constructor: `def initialize(region_cc:, client:, **opts)`
    - Public methods (not just `.call` — this syncer has multiple entry points called by different callers):
      - `def sync_leagues` — body of lines 1883-1988
      - `def sync_league_teams(league_cc)` — body of lines 2226-2361
      - `def sync_league_teams_new(league_cc)` — body of lines 2047-2225
      - `def sync_league_plan(league)` — body of lines 2362-2497
      - `def sync_team_players(league_team)` — body of lines 2676-2728
      - `def sync_team_players_structure` — body of lines 795-870
    - All `post_cc(...)` / `get_cc(...)` calls become `@client.post(...)` / `@client.get(...)`
    - All `cc_id` references become `@region_cc.cc_id`
    - `self` references to the RegionCc become `@region_cc`
    - Keep all League, LeagueCc, LeagueTeam, Season, Region model calls
    - Keep `RegionCc.logger` calls and `Rails.logger` calls
    - Keep all rescue StandardError blocks
    - Return values: preserve exactly (e.g., sync_leagues returns `[leagues, nil]` or `[[], error_string]`)

    CRITICAL: Since LeagueSyncer has multiple public methods, it does NOT use the standard `.call` pattern. Instead, callers will instantiate it and call specific methods. This is an intentional deviation from D-04 for this syncer only — the alternative (separate class per method) would create 6 tiny classes for tightly coupled league operations. The class still inherits ApplicationService for consistency but `call` is not the only entry point.

    For all three syncers:
    - Add `# frozen_string_literal: true` at top
    - Add `require "nokogiri"` if the syncer parses HTML directly
    - Preserve all existing error handling (rescue, raise patterns)
    - Preserve all logging exactly (RegionCc.logger, Rails.logger)
    - Do NOT modify the original region_cc.rb yet — delegation happens in Plan 05
  </action>
  <verify>
    <automated>ruby -c app/services/region_cc/league_syncer.rb && ruby -c app/services/region_cc/club_syncer.rb && ruby -c app/services/region_cc/branch_syncer.rb && echo "All syntax OK"</automated>
  </verify>
  <done>
    - `app/services/region_cc/league_syncer.rb` exists with `class RegionCc::LeagueSyncer < ApplicationService`
    - LeagueSyncer contains methods: sync_leagues, sync_league_teams, sync_league_teams_new, sync_league_plan, sync_team_players, sync_team_players_structure
    - `app/services/region_cc/club_syncer.rb` exists with `class RegionCc::ClubSyncer < ApplicationService`
    - `app/services/region_cc/branch_syncer.rb` exists with `class RegionCc::BranchSyncer < ApplicationService`
    - All three use `@client.get` / `@client.post` instead of direct HTTP calls
    - All three accept `region_cc:` and `client:` keyword args
    - All files pass Ruby syntax check
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for LeagueSyncer, ClubSyncer, and BranchSyncer</name>
  <files>test/services/region_cc/league_syncer_test.rb, test/services/region_cc/club_syncer_test.rb, test/services/region_cc/branch_syncer_test.rb</files>
  <read_first>
    - app/services/region_cc/league_syncer.rb (just created in Task 1)
    - app/services/region_cc/club_syncer.rb (just created in Task 1)
    - app/services/region_cc/branch_syncer.rb (just created in Task 1)
    - test/characterization/region_cc_char_test.rb (existing test patterns, VCR setup)
    - test/test_helper.rb (test infrastructure)
  </read_first>
  <behavior>
    BranchSyncer:
    - Test 1: Given a stubbed client.get("showClubList") returning HTML with branch options, creates BranchCc records
    - Test 2: Raises ArgumentError when branch name not found in Branch table

    ClubSyncer:
    - Test 1: Given stubbed client.post("showClubList") returning HTML with club options, finds and updates Club records
    - Test 2: Logs warning for clubs not found in database (does not raise)

    LeagueSyncer:
    - Test 1: sync_leagues with stubbed client.post("showLeagueList") returning HTML with league options, returns [leagues_array, nil]
    - Test 2: sync_leagues returns [[], error_string] on error
    - Test 3: sync_team_players with stubbed client returns player array
  </behavior>
  <action>
    Create test files in `test/services/region_cc/` directory.

    Each test file follows the pattern:
    ```ruby
    # frozen_string_literal: true
    require "test_helper"

    class RegionCc::BranchSyncerTest < ActiveSupport::TestCase
      setup do
        @region = regions(:nbv)
        @region_cc = RegionCc.create!(
          region: @region, name: "Test", shortname: "nbv",
          context: "nbv", cc_id: 20,
          base_url: "https://test.club-cloud.de",
          username: "test", userpw: "test"
        )
        @client = Minitest::Mock.new
      end

      # ... tests using @client mock expectations
    end
    ```

    For HTML stubs, create minimal Nokogiri-parseable HTML strings that match the CSS selectors each syncer uses. For example, BranchSyncer parses `doc.css('select[name="branchId"]')[0]`, so the stub HTML needs a select element with that name attribute containing option tags.

    Use `Minitest::Mock` for the client — set expectations for `get`/`post` calls and verify they receive correct action names and parameters. Return `[OpenStruct.new(message: "OK"), Nokogiri::HTML(stub_html)]` from mock.

    For model dependencies (Season, Region, Branch, etc.), use fixtures or create! with minimal attributes.

    Do NOT use VCR in these tests — these are unit tests with injected mocks, not integration tests.
  </action>
  <verify>
    <automated>bin/rails test test/services/region_cc/league_syncer_test.rb test/services/region_cc/club_syncer_test.rb test/services/region_cc/branch_syncer_test.rb</automated>
  </verify>
  <done>
    - All three test files exist and pass
    - Tests verify syncer uses injected client (not direct HTTP)
    - Tests verify correct action names passed to client.post/client.get
    - Tests verify database record creation/update behavior
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Syncer → ClubCloudClient | Syncers pass action names and form data to client — trusted internal boundary |
| Syncer → Database | Syncers create/update AR records based on HTML parsing — existing trust model unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | HTML parsing in syncers creates DB records | accept | Existing behavior — syncers parse ClubCloud HTML responses same as before extraction. No new attack surface. |
| T-02-06 | Information Disclosure | RegionCc.logger writes sync details to log/report.log | accept | Existing behavior preserved. No credentials logged (VCR config filters those). |
</threat_model>

<verification>
1. `bin/rails test test/services/region_cc/league_syncer_test.rb test/services/region_cc/club_syncer_test.rb test/services/region_cc/branch_syncer_test.rb` — all unit tests pass
2. `bin/rails test test/characterization/region_cc_char_test.rb` — characterization tests still pass (no interference)
3. `grep -c "def sync_\|def call" app/services/region_cc/league_syncer.rb` — confirms multiple sync methods extracted
</verification>

<success_criteria>
- Three syncer service classes exist with correct method extraction
- All syncers use injected ClubCloudClient (no direct Net::HTTP calls)
- Unit tests pass with mock client injection
- Characterization tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-regioncc-extraction/02-02-SUMMARY.md`
</output>
