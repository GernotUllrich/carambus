---
phase: 38.2-bk2-kombi-scoreboard-ux-realignment
plan: 04
type: execute
wave: 3
depends_on:
  - 38.2-03
files_modified:
  - app/views/table_monitors/_scoreboard_bk2_kombi.html.erb
  - app/javascript/controllers/bk2_kombi_shot_controller.js
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "The `bk2_bottom_bar_placeholder` div from Plan 03 is replaced by a full-width 3-row shot-entry bottom bar occupying the space below the Karambol 3-pane row (D-15)"
    - "Row 1 (amber accent): [ ] Volles Kegelbild vor Stoss checkbox + short hint text (D-16 Row 1)"
    - "Row 2: Gefallene Kegel select 0..5 | [ ] Mittelpin | [ ] Echter Karambol. | [ ] Unechter Karambol. (D-16 Row 2)"
    - "Row 3: Durchläufe number input | [ ] Fehler → reveals Fehlercode select + (conditional on foul_code) [ ] Bande getroffen | [STOSS ERFASSEN] submit button on the right (D-16 Row 3)"
    - "Stimulus controller `bk2_kombi_shot_controller.js` targets the new DOM (same target names: fullPinImage / fallenPins / middlePinOnly / trueCarom / falseCarom / passages / foul / foulCode / bandHit / submit), debounces submit 500ms, generates shot_sequence_number UUID, reflex endpoint `TableMonitorReflex#bk2_kombi_submit_shot` UNCHANGED"
    - "Bottom bar is NOT rendered when match_finished OR bk2_state_uninitialized? OR current_phase is blank"
  artifacts:
    - path: "app/views/table_monitors/_scoreboard_bk2_kombi.html.erb"
      provides: "full-width bottom-bar markup wired to bk2-kombi-shot Stimulus controller, 3 rows per D-16"
      contains: "data-controller=\"bk2-kombi-shot\""
    - path: "app/views/table_monitors/_scoreboard_bk2_kombi.html.erb"
      provides: "submit button with data-reflex to existing TableMonitor#bk2_kombi_submit_shot endpoint"
      contains: "TableMonitor#bk2_kombi_submit_shot"
    - path: "app/javascript/controllers/bk2_kombi_shot_controller.js"
      provides: "Stimulus controller targeting new bottom-bar DOM, preserves 500ms debounce + UUID seq number"
      contains: "bk2-kombi-shot-target"
  key_links:
    - from: "_scoreboard_bk2_kombi.html.erb bottom-bar"
      to: "app/javascript/controllers/bk2_kombi_shot_controller.js"
      via: "data-controller='bk2-kombi-shot' + matching data-bk2-kombi-shot-target='…' attributes"
      pattern: "data-bk2-kombi-shot-target"
    - from: "submit button"
      to: "app/reflexes/table_monitor_reflex.rb#bk2_kombi_submit_shot"
      via: "data-reflex='click->TableMonitor#bk2_kombi_submit_shot' reading btn.dataset.*"
      pattern: "TableMonitor#bk2_kombi_submit_shot"
---

<objective>
Replace the `bk2_bottom_bar_placeholder` div in `_scoreboard_bk2_kombi.html.erb` (created by Plan 03) with the full D-15 / D-16 shot-entry bottom bar, and rewrite the Stimulus controller to target the new DOM. The reflex endpoint is NOT touched — `TableMonitorReflex#bk2_kombi_submit_shot` still reads `btn.dataset.*` keys, so the wire protocol between JS and Ruby is unchanged.

Purpose: Volunteer scorers need a dedicated BK2 shot-entry surface because the Karambol `-10/-5/-1/+1/+5/+10` pattern doesn't map to BK2 rules (pins + carom + passages + foul codes). The bottom bar sits BELOW the Karambol 3-pane row (not inside the center pane) so player cards remain at Karambol size.

Output:
- Replacement of the `bk2_bottom_bar_placeholder` div in `_scoreboard_bk2_kombi.html.erb` with the full 3-row form.
- Rewrite of `bk2_kombi_shot_controller.js` with updated target references; preserved submit / debounce / seq-number logic.
- Stimulus target names unchanged (camelCase) → Reflex-side `btn.dataset.foo_bar` reads unchanged.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38.2-bk2-kombi-scoreboard-ux-realignment/38.2-CONTEXT.md
@.planning/phases/38.2-bk2-kombi-scoreboard-ux-realignment/38.2-03-PLAN.md
@.planning/phases/38.1-bk2-kombi-minimum-viable-support/38.1-BK2-KOMBI-RULES.md
@app/views/table_monitors/_scoreboard_bk2_kombi.html.erb
@app/javascript/controllers/bk2_kombi_shot_controller.js
@config/locales/de.yml
@config/locales/en.yml

<interfaces>
Reflex payload fields (keep these dataset key names exactly — `TableMonitorReflex#bk2_kombi_submit_shot` at reflex ~line 998 reads them):
  btn.dataset.fallen_pins          "0" .. "5"
  btn.dataset.middle_pin_only      "true" | "false"
  btn.dataset.full_pin_image       "true" | "false"
  btn.dataset.true_carom           "true" | "false"
  btn.dataset.false_carom          "true" | "false"
  btn.dataset.passages             "0" .. "3"
  btn.dataset.foul                 "true" | "false"
  btn.dataset.foul_code            "" | "wrong_ball" | "no_object_ball_hit" | "premature_shot" | "direct_play_on_pins" | "other"
  btn.dataset.band_hit             "true" | "false"
  btn.dataset.shot_sequence_number UUID string

Stimulus target names (MUST NOT change — Stimulus maps camelCase to underscored dataset keys):
  fallenPins, middlePinOnly, fullPinImage, trueCarom, falseCarom, passages, foul, foulCode, bandHit, submit

i18n keys (existing from 38.1 Plan 04):
  table_monitor.bk2_kombi.form.fallen_pins, .middle_pin_only, .full_pin_image, .full_pin_image_help,
  .true_carom, .false_carom, .passages, .foul, .foul_codes.{wrong_ball,no_object_ball_hit,premature_shot,direct_play_on_pins,other},
  .band_hit, .submit

New keys from Plan 01:
  table_monitor.bk2_kombi.bottom_bar.row1_hint, .row2_heading, .row3_heading
</interfaces>

<security_context>
No new threats. All input already validated server-side by `Bk2Kombi::ScoreShot` (pure evaluator with symbolised keys and type coercions via `.to_i` / `.to_s`). Dataset payload unchanged from 38.1. No new CSRF surface. No new PII.
</security_context>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace bottom-bar placeholder in _scoreboard_bk2_kombi.html.erb with full 3-row D-16 shot-entry form</name>
  <files>app/views/table_monitors/_scoreboard_bk2_kombi.html.erb</files>
  <read_first>
    - app/views/table_monitors/_scoreboard_bk2_kombi.html.erb (post-Plan-03 — locate the `bk2_bottom_bar_placeholder` id)
    - app/views/table_monitors/_scoreboard_bk2_kombi.html.erb pre-Plan-03 (git HEAD^) lines 94–227 — the old center-pane shot form, for reference on i18n label text + data-target attributes
    - .planning/phases/38.2-bk2-kombi-scoreboard-ux-realignment/38.2-CONTEXT.md (D-15 + D-16 — authoritative row-by-row layout spec)
    - config/locales/de.yml `table_monitor.bk2_kombi.form.*` tree + `table_monitor.bk2_kombi.bottom_bar.*` (Plan 01 additions)
    - app/reflexes/table_monitor_reflex.rb around line 998 — confirm reflex endpoint name `TableMonitor#bk2_kombi_submit_shot` unchanged
  </read_first>
  <action>
    **Find** the placeholder div in `app/views/table_monitors/_scoreboard_bk2_kombi.html.erb`:

    ```
    <div id="bk2_bottom_bar_placeholder" class="w-full dark:bg-black p-4 text-center text-gray-400 text-<%= vw2 %>">
      <em>Shot-entry bar — pending Plan 04</em>
    </div>
    ```

    **Replace** the entire placeholder div with the full 3-row bottom bar. Keep the comment delimiter line above it.

    Content to insert (full ERB block; preserve exact structure + class names + data-* attributes + i18n keys):

    ```
    <%# ================ BOTTOM BAR — D-15 / D-16 (Plan 04) ================ %>
    <%- show_bottom_bar = !match_finished && !table_monitor.bk2_state_uninitialized? && current_phase.present? %>
    <%- if show_bottom_bar %>
      <div id="bk2_bottom_bar_<%= table_monitor.id %>"
           class="w-full dark:bg-black text-gray-800 dark:text-gray-200 p-3 border-t border-gray-300 dark:border-gray-700"
           data-controller="bk2-kombi-shot"
           data-table-monitor-id="<%= table_monitor.id %>">

        <%# ---------- Row 1 (amber): full_pin_image + hint ---------- %>
        <div class="flex items-center gap-3 bg-amber-50 dark:bg-amber-900/20 border border-amber-300 dark:border-amber-700 rounded p-2 mb-2">
          <label class="flex items-center gap-2 font-semibold text-amber-800 dark:text-amber-300 cursor-pointer whitespace-nowrap">
            <input type="checkbox"
                   id="bk2_full_pin_image_<%= table_monitor.id %>"
                   class="w-5 h-5 accent-amber-500"
                   data-bk2-kombi-shot-target="fullPinImage">
            <span class="text-<%= vw2 %>"><%= t("table_monitor.bk2_kombi.form.full_pin_image") %></span>
          </label>
          <p class="text-<%= vw2 %> text-amber-700 dark:text-amber-400 flex-1">
            <%= t("table_monitor.bk2_kombi.bottom_bar.row1_hint") %>
          </p>
        </div>

        <%# ---------- Row 2: Gefallene Kegel + Mittelpin + Echter/Unechter Karambol ---------- %>
        <div class="flex flex-wrap items-center gap-4 mb-2">
          <div class="flex items-center gap-2">
            <label class="text-<%= vw2 %> font-medium" for="bk2_fallen_pins_<%= table_monitor.id %>">
              <%= t("table_monitor.bk2_kombi.form.fallen_pins") %>:
            </label>
            <select id="bk2_fallen_pins_<%= table_monitor.id %>"
                    class="border rounded px-2 py-1 dark:bg-gray-800 dark:border-gray-600 text-<%= vw2 %>"
                    data-bk2-kombi-shot-target="fallenPins">
              <%- (0..5).each do |n| %>
                <option value="<%= n %>"><%= n %></option>
              <%- end %>
            </select>
          </div>
          <label class="flex items-center gap-2 cursor-pointer">
            <input type="checkbox"
                   id="bk2_middle_pin_only_<%= table_monitor.id %>"
                   class="w-4 h-4"
                   data-bk2-kombi-shot-target="middlePinOnly">
            <span class="text-<%= vw2 %>"><%= t("table_monitor.bk2_kombi.form.middle_pin_only") %></span>
          </label>
          <label class="flex items-center gap-2 cursor-pointer">
            <input type="checkbox"
                   id="bk2_true_carom_<%= table_monitor.id %>"
                   class="w-4 h-4"
                   data-bk2-kombi-shot-target="trueCarom">
            <span class="text-<%= vw2 %>"><%= t("table_monitor.bk2_kombi.form.true_carom") %></span>
          </label>
          <label class="flex items-center gap-2 cursor-pointer">
            <input type="checkbox"
                   id="bk2_false_carom_<%= table_monitor.id %>"
                   class="w-4 h-4"
                   data-bk2-kombi-shot-target="falseCarom">
            <span class="text-<%= vw2 %>"><%= t("table_monitor.bk2_kombi.form.false_carom") %></span>
          </label>
        </div>

        <%# ---------- Row 3: Durchläufe + Fehler + Fehlercode + Bande + STOSS ERFASSEN ---------- %>
        <div class="flex flex-wrap items-center gap-4">
          <div class="flex items-center gap-2">
            <label class="text-<%= vw2 %> font-medium" for="bk2_passages_<%= table_monitor.id %>">
              <%= t("table_monitor.bk2_kombi.form.passages") %>:
            </label>
            <input type="number"
                   id="bk2_passages_<%= table_monitor.id %>"
                   min="0" max="3" value="0"
                   autocomplete="off"
                   class="border rounded px-2 py-1 w-20 dark:bg-gray-800 dark:border-gray-600 text-<%= vw2 %>"
                   data-bk2-kombi-shot-target="passages">
          </div>

          <label class="flex items-center gap-2 font-semibold text-red-600 dark:text-red-400 cursor-pointer">
            <input type="checkbox"
                   id="bk2_foul_<%= table_monitor.id %>"
                   class="w-4 h-4 accent-red-500"
                   data-bk2-kombi-shot-target="foul"
                   data-action="change->bk2-kombi-shot#toggleFoulFields">
            <span class="text-<%= vw2 %>"><%= t("table_monitor.bk2_kombi.form.foul") %></span>
          </label>

          <%# Foul sub-fields — hidden by default; toggled by toggleFoulFields(). %>
          <div id="bk2_foul_fields_<%= table_monitor.id %>"
               class="hidden flex items-center gap-4 pl-4 border-l-2 border-red-300 dark:border-red-700">
            <select id="bk2_foul_code_<%= table_monitor.id %>"
                    class="border rounded px-2 py-1 dark:bg-gray-800 dark:border-gray-600 text-<%= vw2 %>"
                    data-bk2-kombi-shot-target="foulCode"
                    data-action="change->bk2-kombi-shot#toggleBandHit">
              <option value="wrong_ball"><%= t("table_monitor.bk2_kombi.form.foul_codes.wrong_ball") %></option>
              <option value="no_object_ball_hit"><%= t("table_monitor.bk2_kombi.form.foul_codes.no_object_ball_hit") %></option>
              <option value="premature_shot"><%= t("table_monitor.bk2_kombi.form.foul_codes.premature_shot") %></option>
              <option value="direct_play_on_pins"><%= t("table_monitor.bk2_kombi.form.foul_codes.direct_play_on_pins") %></option>
              <option value="other"><%= t("table_monitor.bk2_kombi.form.foul_codes.other") %></option>
            </select>
            <label id="bk2_band_hit_row_<%= table_monitor.id %>"
                   class="hidden flex items-center gap-2 cursor-pointer">
              <input type="checkbox"
                     id="bk2_band_hit_<%= table_monitor.id %>"
                     class="w-4 h-4"
                     data-bk2-kombi-shot-target="bandHit">
              <span class="text-<%= vw2 %>"><%= t("table_monitor.bk2_kombi.form.band_hit") %></span>
            </label>
          </div>

          <%# STOSS ERFASSEN submit button — right-aligned via flex-grow spacer. %>
          <div class="flex-1"></div>
          <button id="bk2_submit_<%= table_monitor.id %>"
                  class="py-2 px-6 bg-blue-600 hover:bg-blue-700 text-white font-bold rounded text-<%= vw2 %> focus:outline-none focus:ring-4 focus:ring-blue-500"
                  data-bk2-kombi-shot-target="submit"
                  data-action="click->bk2-kombi-shot#submit"
                  data-reflex="click->TableMonitor#bk2_kombi_submit_shot"
                  data-id="<%= table_monitor.id %>">
            <%= t("table_monitor.bk2_kombi.form.submit") %>
          </button>
        </div>
      </div>
    <%- end %>
    ```

    Notes:
    - `data-reflex="click->TableMonitor#bk2_kombi_submit_shot"` text UNCHANGED from 38.1 Plan 04 (reflex endpoint lives at ~line 998) — no reflex-file modifications in this phase.
    - `data-id` attribute preserved — reflex reads it in the fallback lookup path.
    - `accent-amber-500` / `accent-red-500` match Plan 02's button color language (DZ=amber, SP=sky, red for destructive/foul).
    - The whole bar is `w-full` and lives BELOW the 3-pane row (D-15 layout symmetry).
    - Guard `show_bottom_bar` prevents rendering during match-finished, uninitialised-state, and blank-phase states.
  </action>
  <verify>
    <automated>bundle exec erblint app/views/table_monitors/_scoreboard_bk2_kombi.html.erb 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'bk2_bottom_bar_placeholder' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` (placeholder gone)
    - `grep -q 'bk2_bottom_bar_' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` (new bar id present)
    - `grep -q 'data-controller="bk2-kombi-shot"' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb`
    - `grep -q 'TableMonitor#bk2_kombi_submit_shot' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb`
    - All 10 Stimulus targets present — `grep -c 'data-bk2-kombi-shot-target=' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` returns 10
    - `grep -q 'bg-amber-50' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` (Row 1 amber accent)
    - `grep -q 'bottom_bar.row1_hint' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb`
    - `grep -q 'bk2_foul_fields_' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb && grep -q 'bk2_band_hit_row_' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb`
    - `grep -cE 'foul_codes\.(wrong_ball|no_object_ball_hit|premature_shot|direct_play_on_pins|other)' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` returns 5
    - `grep -q 'show_bottom_bar = !match_finished' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb`
    - `bundle exec erblint app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` exits 0 (or pre-existing warnings unchanged)
    - Reflex file NOT modified: `git diff app/reflexes/table_monitor_reflex.rb` prints nothing
  </acceptance_criteria>
  <done>Placeholder replaced by full 3-row D-16 bottom bar; all 10 Stimulus targets + data-reflex present; terminal-state guard prevents render during match-finished / uninitialised / blank-phase; reflex file untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite bk2_kombi_shot_controller.js for new DOM; preserve target names + debounce + UUID seq number + reflex wire protocol</name>
  <files>app/javascript/controllers/bk2_kombi_shot_controller.js</files>
  <read_first>
    - app/javascript/controllers/bk2_kombi_shot_controller.js (current 123 lines — existing logic to preserve)
    - app/views/table_monitors/_scoreboard_bk2_kombi.html.erb post-Task-1 (confirm target names match + data-action names match)
    - .planning/phases/38.2-bk2-kombi-scoreboard-ux-realignment/38.2-CONTEXT.md "Claude's Discretion" (Stimulus internals = discretion; target names + reflex fingerprint MUST stay stable)
  </read_first>
  <action>
    The current controller is largely correct — DOM id scheme (`bk2_foul_fields_*`, `bk2_band_hit_row_*`) is preserved in Task 1. The only DOM change is: the Stimulus controller's root element now carries `data-table-monitor-id="<%= table_monitor.id %>"` directly (previously it was inherited via `.closest()`). Add a primary lookup path; keep the `closest()` fallback.

    **Fully replace** `app/javascript/controllers/bk2_kombi_shot_controller.js` with the JS below.

    File content (JS; preserve exact import, class, target list, and method order):

    ```
    import { Controller } from "@hotwired/stimulus"

    // Phase 38.2 Plan 04 — BK2-Kombi shot input controller for the new full-width
    // bottom-bar layout (D-15 / D-16). Structural rewrite of the 38.1 Plan 04
    // controller; target names + dataset key names + reflex endpoint are UNCHANGED
    // so TableMonitorReflex#bk2_kombi_submit_shot reads the same keys.
    //
    // Responsibilities:
    //   - Pack all form field values into the submit button's dataset before the
    //     StimulusReflex fires (data-reflex reads btn.dataset in the reflex).
    //   - Generate a UUID shot_sequence_number per click for AdvanceMatchState
    //     idempotency guard (T-38.1-13).
    //   - 500ms debounce on submit (T-38.1-13 double-tap protection).
    //   - Toggle foul sub-fields visibility when foul checkbox changes.
    //   - Toggle band_hit row visibility when foul_code changes to/from
    //     "no_object_ball_hit".
    //
    // Target names MUST stay identical to 38.1 Plan 04 (fallenPins, middlePinOnly,
    // fullPinImage, trueCarom, falseCarom, passages, foul, foulCode, bandHit,
    // submit) — the reflex reads dataset keys (underscored) that Stimulus derives
    // from these camelCase names.

    export default class extends Controller {
      static targets = [
        "fallenPins",
        "middlePinOnly",
        "fullPinImage",
        "trueCarom",
        "falseCarom",
        "passages",
        "foul",
        "foulCode",
        "bandHit",
        "submit"
      ]

      connect() {
        this.lastSubmitAt = 0
      }

      submit(event) {
        const now = Date.now()
        if (now - this.lastSubmitAt < 500) {
          event.preventDefault()
          return
        }
        this.lastSubmitAt = now

        const btn = this.submitTarget

        btn.dataset.fallen_pins = this.fallenPinsTarget.value
        btn.dataset.middle_pin_only = this.middlePinOnlyTarget.checked

        btn.dataset.full_pin_image = this.hasFullPinImageTarget
          ? this.fullPinImageTarget.checked
          : false

        btn.dataset.true_carom = this.trueCaromTarget.checked
        btn.dataset.false_carom = this.falseCaromTarget.checked
        btn.dataset.passages = this.passagesTarget.value
        btn.dataset.foul = this.foulTarget.checked

        btn.dataset.foul_code = this.foulTarget.checked && this.hasFoulCodeTarget
          ? this.foulCodeTarget.value
          : ""

        btn.dataset.band_hit = this.hasBandHitTarget ? this.bandHitTarget.checked : false
        btn.dataset.shot_sequence_number = this.generateSequenceNumber()
      }

      // Phase 38.2: foul sub-fields container lives in the bottom bar (Row 3)
      // rather than a separate vertical block. ID scheme unchanged.
      toggleFoulFields() {
        const foulChecked = this.foulTarget.checked
        const tid = this.resolveTableMonitorId()
        const foulFields =
          (tid && document.getElementById(`bk2_foul_fields_${tid}`)) ||
          this.element.querySelector("[id^='bk2_foul_fields_']")
        if (foulFields) {
          foulFields.classList.toggle("hidden", !foulChecked)
        }
        if (!foulChecked) {
          this.hideBandHitRow()
        }
      }

      toggleBandHit() {
        if (!this.hasFoulCodeTarget) return
        const showBandHit = this.foulCodeTarget.value === "no_object_ball_hit"
        const tid = this.resolveTableMonitorId()
        const bandHitRow =
          (tid && document.getElementById(`bk2_band_hit_row_${tid}`)) ||
          this.element.querySelector("[id^='bk2_band_hit_row_']")
        if (bandHitRow) {
          bandHitRow.classList.toggle("hidden", !showBandHit)
        }
        if (!showBandHit && this.hasBandHitTarget) {
          this.bandHitTarget.checked = false
        }
      }

      hideBandHitRow() {
        const tid = this.resolveTableMonitorId()
        const bandHitRow =
          (tid && document.getElementById(`bk2_band_hit_row_${tid}`)) ||
          this.element.querySelector("[id^='bk2_band_hit_row_']")
        if (bandHitRow) {
          bandHitRow.classList.add("hidden")
        }
        if (this.hasBandHitTarget) {
          this.bandHitTarget.checked = false
        }
      }

      // Phase 38.2: primary lookup via this.element.dataset.tableMonitorId (Task 1
      // emits data-table-monitor-id on the bar root); fallback via closest() for
      // embedded-use robustness.
      resolveTableMonitorId() {
        return this.element.dataset.tableMonitorId
            || this.element.closest("[data-table-monitor-id]")?.dataset.tableMonitorId
            || ""
      }

      // Generate a unique shot sequence number for idempotency guard (T-38.1-13).
      generateSequenceNumber() {
        if (window.crypto && window.crypto.randomUUID) {
          return window.crypto.randomUUID()
        }
        return `${Date.now()}-${Math.random().toString(36).slice(2)}`
      }
    }
    ```

    Differences from 38.1 controller:
    - New private helper `resolveTableMonitorId()` — primary via `this.element.dataset.tableMonitorId`, fallback to `closest()`.
    - Three call sites (`toggleFoulFields`, `toggleBandHit`, `hideBandHitRow`) consolidated on this helper; repeated `const tid = …` blocks replaced.
    - Header comment updated to document Phase 38.2.
    - All Stimulus target names + dataset key names + submit/debounce logic byte-identical.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('app/javascript/controllers/bk2_kombi_shot_controller.js','utf8'); ['fallenPins','middlePinOnly','fullPinImage','trueCarom','falseCarom','passages','foul','foulCode','bandHit','submit','generateSequenceNumber','toggleFoulFields','toggleBandHit','resolveTableMonitorId','500'].forEach(t => { if (!c.includes(t)) throw new Error('missing: ' + t); }); console.log('controller-ok');" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'static targets' app/javascript/controllers/bk2_kombi_shot_controller.js`
    - All 10 target names listed: `for t in fallenPins middlePinOnly fullPinImage trueCarom falseCarom passages foul foulCode bandHit submit; do grep -q "\"$t\"" app/javascript/controllers/bk2_kombi_shot_controller.js || { echo "missing: $t"; exit 1; }; done`
    - Debounce preserved: `grep -q '500' app/javascript/controllers/bk2_kombi_shot_controller.js && grep -q 'lastSubmitAt' app/javascript/controllers/bk2_kombi_shot_controller.js`
    - UUID seq number preserved: `grep -q 'randomUUID' app/javascript/controllers/bk2_kombi_shot_controller.js && grep -q 'shot_sequence_number' app/javascript/controllers/bk2_kombi_shot_controller.js`
    - All 10 dataset key writes present: `for k in fallen_pins middle_pin_only full_pin_image true_carom false_carom passages foul foul_code band_hit shot_sequence_number; do grep -q "btn.dataset.$k" app/javascript/controllers/bk2_kombi_shot_controller.js || { echo "missing-dataset: $k"; exit 1; }; done`
    - New helper present: `grep -q 'resolveTableMonitorId' app/javascript/controllers/bk2_kombi_shot_controller.js`
    - No reference to deleted targets or endpoints — file must NOT reference any name outside the static targets list (visual audit)
    - Reflex file untouched: `git diff app/reflexes/table_monitor_reflex.rb` prints nothing
    - Services untouched: `git diff app/services/bk2_kombi/` prints nothing
    - Node sanity check (automated verify) prints `controller-ok`
  </acceptance_criteria>
  <done>Controller targets the new bottom-bar DOM; all 10 targets + 10 dataset key writes preserved; 500ms debounce + UUID seq number preserved; reflex fingerprint (data-reflex="click->TableMonitor#bk2_kombi_submit_shot") unchanged; reflex + services + Plan 03 partial structure untouched.</done>
</task>

</tasks>

<verification>
- ERB lint: `bundle exec erblint app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` exits 0.
- Node sanity check on the controller: `node -e "…"` (see Task 2 verify).
- Out-of-scope scope checks: `git diff app/reflexes/table_monitor_reflex.rb app/services/bk2_kombi/` prints nothing.
- Plan 03 scoreboard still compiles (placeholder replaced in-place; other regions untouched).
</verification>

<success_criteria>
- D-15 / D-16 implemented verbatim: 3-row bottom bar with Row 1 amber accent / Row 2 pins+carom / Row 3 passages+foul+submit.
- Stimulus controller preserves reflex fingerprint (target names, data-reflex string, dataset keys).
- Bar not rendered in match-finished / uninitialised / blank-phase states.
- Plan 05 can now assert on `data-controller="bk2-kombi-shot"` and the 10 target attributes.
</success_criteria>

<output>
After completion, create `.planning/phases/38.2-bk2-kombi-scoreboard-ux-realignment/38.2-04-SUMMARY.md` with:
- Before/after diff of the placeholder replacement region
- Before/after diff of the Stimulus controller (short — mostly comment updates + helper extraction)
- grep proofs of all target names + dataset keys
- Confirmation that reflex + services + scoreboard header untouched (scope guard)
</output>
