---
status: partial
phase: 38.2-bk2-kombi-scoreboard-ux-realignment
source:
  - 38.2-VERIFICATION.md
started: 2026-04-24T00:00:00Z
updated: 2026-04-24T00:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Visual parity — Karambol vs BK2-Kombi side-by-side
expected: Open both a Karambol scoreboard and a BK2-Kombi scoreboard in adjacent browser windows at the same table size. A volunteer scorer trained on the Karambol layout should recognize every slot (player names, current-set score, Ziel, match-score, timer controls, nav modal) in the same visual position. BK2 deltas (Satz/phase chip in center, phase-sensitive remaining badge, shot-entry bottom bar replacing the -10/-5/-1 row) should read as additive rather than disruptive. D-01 ("players not disoriented on tournament day 2026-05-02") is the load-bearing claim — this is a subjective UX quality check that selectors cannot assert.
result: [pending]

### 2. End-to-end start-game flow
expected: Navigate to `/locations/:md5?sb_state=free_game_detail&table_id=N`, select the BK2-Kombi radio option, and click each of the 4 new buttons in turn (Spieler A × DZ, Spieler A × SP, Spieler B × DZ, Spieler B × SP). Each click should submit the form and land on `_show_bk2_kombi.html.erb` with the correct first-set mode encoded in `bk2_state["current_phase"]` (DZ → "direkter_zweikampf" for Satz 1; SP → "serienspiel" for Satz 1). Verify the Alpine-timing assumptions from GAP-02 hold — hidden `:value` inputs (`free_game_form`, `set_target_points`, `bk2_first_set_mode`) submit with hydrated values (not blank).
result: [pending]

### 3. GAP-05 recovery affordance
expected: Manually construct a TableMonitor with `data["free_game_form"] = "bk2_kombi"` and empty `data["bk2_state"]` (simulating the pre-Plan-06 stuck state). Navigate to its scoreboard URL. Expected: fallback banner "Spiel nicht initialisiert — bitte neu starten" renders instead of zero-defaults + reflex crash. The Home/Cancel/Continue nav modal (D-10) must remain reachable from this fallback state so the user can exit cleanly. Confirm the banner does not trap the user.
result: [pending]

### 4. Rule interpretation sign-off (D-12)
expected: Confirm with BK2 tournament officials that `direkter_zweikampf_max_shots_per_turn = 2` and `serienspiel_max_innings_per_set = 5` match the current ruleset. User flagged this in CONTEXT.md as "muss klären mit Spezialisten". Values are config-driven (`bk2_options` in `TableMonitor#data`), so post-clarification tweaks are zero-code changes. Also note: code review WR-01 identified that `Bk2Kombi::ScoreShot#calculate_direct_duel_transitions` still hardcodes `2` at intra-set turn-end reset (pre-existing, scope-guarded out of 38.2 per D-02). If officials confirm `> 2` is valid, a follow-up phase must extend the configurability to that code path.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
