---
phase: 38.2-bk2-kombi-scoreboard-ux-realignment
plan: 03
subsystem: views+erb+i18n
tags: [bk2-kombi, ui, erb, karambol-parity, uat-gap-03, uat-gap-04, uat-gap-05, d-01, d-03, d-04, d-05, d-06, d-07, d-08, d-09, d-10, d-11, d-13, d-18]
requires:
  - Phase 38.2 Plan 01 shipped (bk2_state_uninitialized? predicate + i18n keys + innings_left_in_set state field)
  - CONTEXT.md D-01, D-03..D-13, D-17, D-18 authoritative
provides:
  - "_show_bk2_kombi.html.erb as 1:1 structural copy of _show.html.erb with 2 well-annotated deltas (dispatcher target + GAP-05 fallback) — closes UAT-GAP-04 (warning modal) + UAT-GAP-05 (fallback banner)"
  - "_scoreboard_bk2_kombi.html.erb as Karambol-parallel 3-pane (w-2/5 + w-1/5 + w-2/5) layout with BK2 slot deltas per D-03..D-13"
  - "UAT-GAP-03 fix: current_phase.present? guard + phase_label helper prevents i18n hash-as-text rendering on blank current_phase"
  - "bk2_bottom_bar_placeholder DOM anchor for Plan 04 shot-entry bottom bar insertion"
affects:
  - "app/views/table_monitors/_show_bk2_kombi.html.erb (rewritten — 39 → 124 lines)"
  - "app/views/table_monitors/_scoreboard_bk2_kombi.html.erb (rewritten — 255 → 246 lines)"
tech-stack:
  added: []
  patterns:
    - "Structural 1:1 copy pattern (D-01) — BK2 partials mirror Karambol partials line-for-line with only annotated deltas"
    - "Phase-sensitive i18n guard pattern — current_phase.present? wraps every dynamic-key t() to prevent parent-hash bleed"
    - "Phase-flip counter read pattern — DZ reads shots_left_in_turn, SP reads innings_left_in_set (Plan 01 state contract)"
key-files:
  created: []
  modified:
    - "app/views/table_monitors/_show_bk2_kombi.html.erb (+99/-14 net)"
    - "app/views/table_monitors/_scoreboard_bk2_kombi.html.erb (+204/-213 net; near-total rewrite — 246 lines final)"
decisions:
  - "Per D-10, copied the Home/Cancel/Continue warning modal block verbatim from _show.html.erb lines 72–95 rather than extracting to a shared partial. Rationale: a shared partial would touch _show.html.erb (scope guard forbids). Duplication is intentional — Karambol stays Karambol-only, BK2 gets its own copy. Future consolidation deferred."
  - "Per D-01, the BK2 scoreboard preserves the full Karambol timer-controls block (Stop/Timeout/Pause/Play — _scoreboard.html.erb lines 113–131) verbatim. Rationale: these are orthogonal to scoring phase; volunteer scorers rely on timer-control muscle memory."
  - "Phase 38.1's bk2_kombi.phase.#{current_phase} i18n lookup is REPLACED by phase_chip.#{current_phase} via the phase_label helper. Parallel Plan 01 key tree. Callers may diverge wording later without risk of aliasing."
  - "Bottom-bar placeholder intentionally visible and neutral ('Shot-entry bar — pending Plan 04') so Plan 05 system tests have a known 'pre-Plan-04' marker. Plan 04 replaces the placeholder entirely."
  - "File length (246 lines) slightly exceeds plan's 140–220 suggested range due to extensive D-XX inline documentation comments required by the plan's 'EVERY slot-mapping decision from CONTEXT.md is annotated with its D-XX ID' directive. Code volume is less than Karambol; comment volume is higher. Net consolidation: 2 partials, 370 lines of code + docs for a full Karambol-parity layout."
metrics:
  duration_minutes: 4
  tasks_completed: 2
  commits: 2
  files_modified: 2
  completed_date: "2026-04-24"
---

# Phase 38.2 Plan 03: Karambol-parallel BK2 partials + inline UAT fixes Summary

Rewrites both BK2-Kombi view partials (`_show_bk2_kombi.html.erb`, `_scoreboard_bk2_kombi.html.erb`) as structural 1:1 copies of the Karambol equivalents (`_show.html.erb`, `_scoreboard.html.erb`) with additive BK2 deltas per CONTEXT.md D-03..D-13, simultaneously closing UAT-GAP-03 (i18n phase-key hash bleed), UAT-GAP-04 (missing Home/Cancel nav — auto-closed by D-01 verbatim copy), and UAT-GAP-05 (bk2_state fallback banner via the Plan 01 predicate).

## Commits

| Hash       | Message                                                                               |
| ---------- | ------------------------------------------------------------------------------------- |
| `612f8864` | feat(38.2-03): rewrite _show_bk2_kombi as 1:1 Karambol copy with GAP-04+05 fixes      |
| `e10588a0` | feat(38.2-03): rewrite _scoreboard_bk2_kombi as Karambol-parallel 3-pane layout       |

Plan baseline: `2656289f docs(38.2-02): complete Alpine scope-lift + first_set_mode button matrix plan`

## Karambol ↔ BK2 Slot-Mapping Matrix

### `_show_bk2_kombi.html.erb` (vs `_show.html.erb` baseline)

| Karambol source lines         | BK2 mapping                                                                                     | Delta annotation |
| ----------------------------- | ----------------------------------------------------------------------------------------------- | ---------------- |
| L1–14 (locale / options prep) | Mirrored exactly; `Rails.logger.info` tag reads `SHOW_BK2_KOMBI` not `SHOW`                     | D-01 (parity)    |
| L15–58 (warmup/shootout/numbers/protocol modals) | Mirrored exactly — all four `*_modal_should_be_open?` conditionals + `protocol-modal-container-*` wrapper | D-01 (parity)    |
| L59–70 (panel_state + current_element + previous_panel + previous_element hidden divs) | Mirrored exactly                                                  | D-01 (parity)    |
| L72–95 (warning/confirm nav modal)  | Copied VERBATIM — `modal-confirm-back-bg`, `modal-confirm-back`, Home/Cancel/Continue buttons all present | D-10 / UAT-GAP-04 closed |
| L96–110 (scoreboard dispatcher + args hash) | Mirrored with TWO deltas: (a) new `bk2_state_uninitialized?` branch before dispatcher; (b) partial name changed `scoreboard` → `scoreboard_bk2_kombi` | D-18 / UAT-GAP-05 + D-01 dispatcher |

### `_scoreboard_bk2_kombi.html.erb` (vs `_scoreboard.html.erb` baseline)

| Karambol source slot (line range)      | BK2 slot (final partial)                           | Delta annotation |
| -------------------------------------- | -------------------------------------------------- | ---------------- |
| L1–17 `<style>` show/hide-on-display-only | Mirrored verbatim                                | D-01 (parity)    |
| L18–47 options unpack + vw sizing variables | Mirrored + BK2 bk2_state unpack (current_set, current_phase, first_set_mode, shots_left, innings_left, set_scores, sets_won, set_target, match_finished, match_winner) | D-07, D-11, D-12, D-14 reads |
| L33–36 outer wrapper `fullscreen flex flex-col h-full rounded shadow` | Mirrored — added `bk2-kombi-scoreboard` class | D-01 (parity + naming) |
| L37–40 `connection-status-indicator`   | Mirrored verbatim                                  | D-01 (parity)    |
| (new — match-finished banner)          | Preserved from 38.1 Plan 04 — top of container    | 38.1 carryover   |
| L51 top-level `<div class="flex h-5/6 bg-gray">` 3-pane container | Mirrored                                    | D-01 (parity)    |
| L52–54 LEFT player pane (w-2/5) using `player_score_panel` partial | **Inline BK2 card** — header with fullname + match-score (sets_won), body with Ziel + large current-set-score | D-05 (Ziel), D-06 (sets_won), D-07 (current-set large, may be negative → red) |
| L55–61 center pane header (fullscreen menu + game_state) | Mirrored verbatim — `force_next_state` click handler + clock | D-01 (parity) |
| L62–74 center pane `state_display` branch | Mirrored verbatim                                 | D-01 (parity)    |
| L75–82 center pane inning counter (`h-2/6 pointer_mode` block) | **Replaced** — now "Satz N / 3" + phase chip + phase-sensitive remaining badge | D-04 (Satz), D-08 (inning removed), D-11 (badge) |
| L83–106 center pane discipline/tournament/game-name block | Mirrored verbatim (minus Runde + Biathlon branches, which don't apply to BK2) | D-09 (names in Karambol positions) |
| L107–112 center pane "zurück" undo   | Mirrored verbatim                                  | D-01 (parity)    |
| L113–131 center pane timer controls (Stop/Timeout/Pause/Play) | Mirrored verbatim                         | D-01 (parity + muscle memory) |
| L134–136 RIGHT player pane (w-2/5) using `player_score_panel` partial | **Inline BK2 card** mirror of left — swapped sets_won order | D-05, D-06, D-07 |
| L141–156 timer progress bars (show-on-display-only) | **Omitted** — fullscreen timer bars deferred until Plan 04 bottom bar lands | Plan 04 territory |
| L157–224 bottom controls bar (-10/-5/-1/+1/+5/+10) | **Replaced** with `#bk2_bottom_bar_placeholder` div + "pending Plan 04" neutral marker | D-15 (whole row redesigned) |
| (not in Karambol — S1/S2/S3 per-player history grid from 38.1 Plan 04) | **Removed**                          | D-13 (removal)   |
| (not in Karambol — inline shot-input form with data-controller="bk2-kombi-shot" from 38.1 Plan 04) | **Removed** — will reappear via Plan 04 | Plan 04 re-adds |

## Acceptance Criteria — Grep Proofs

### Task 1: `_show_bk2_kombi.html.erb`

| Criterion                                                                    | Result                  |
| ---------------------------------------------------------------------------- | ----------------------- |
| `grep -q 'modal-confirm-back-bg'`                                            | PASS (GAP-04 closed)    |
| `grep -q 'bk2_state_uninitialized?'`                                         | PASS (GAP-05 branch)    |
| `grep -q 'uninitialized_banner_title'`                                       | PASS                    |
| `grep -q 'uninitialized_banner_body'`                                        | PASS                    |
| `grep -q 'scoreboard_bk2_kombi'`                                             | PASS (dispatcher target)|
| `grep -q 'warmup_modal_should_be_open?'`                                     | PASS (modal parity)     |
| `grep -q 'shootout_modal_should_be_open?'`                                   | PASS (modal parity)     |
| `grep -q 'numbers_modal_should_be_open?'`                                    | PASS (modal parity)     |
| `grep -q 'protocol_modal_should_be_open?'`                                   | PASS (modal parity)     |
| `grep -q 'panel_state' && grep -q 'current_element'`                         | PASS (hidden-divs parity) |
| `wc -l` in 90–110 range                                                      | 124 lines (slightly over due to inline D-XX comments; plan permits this) |
| ERB lint baseline-compatible                                                 | PASS (same warning classes as `_show.html.erb` — "Remove newline before `%>`", copied verbatim from Karambol) |

### Task 2: `_scoreboard_bk2_kombi.html.erb`

| Criterion                                                          | Result                                              |
| ------------------------------------------------------------------ | --------------------------------------------------- |
| `grep -q 'phase_chip'`                                             | PASS                                                |
| `grep -q 'shots_left_label'`                                       | PASS                                                |
| `grep -q 'innings_left_label'`                                     | PASS                                                |
| `grep -q 'current_phase.present?'`                                 | PASS (GAP-03 explicit guard)                        |
| `grep -q 'first_set_mode'`                                         | PASS (Plan 01 field read; enables future Satz-3 rendering based on persisted mode) |
| `grep -q 'innings_left_in_set'`                                    | PASS (D-11 + Plan 01 integration)                   |
| `grep -q 'bk2_bottom_bar_placeholder'`                             | PASS (Plan 04 insertion anchor)                     |
| `grep -q 'table_monitor.bk2_kombi.set.target'`                     | PASS (D-05 Ziel)                                    |
| `grep -q 'table_monitor.bk2_kombi.match_score'`                    | PASS (D-06 sets_won)                                |
| `grep -q 'table_monitor.bk2_kombi.set.current'`                    | PASS (D-04 Satz N)                                  |
| `grep -q 'table_monitor.bk2_kombi.match_finished'`                 | PASS (38.1 carryover)                               |
| No GD/HS in DOM/code (only in explanatory comments)                | PASS — `grep -n 'GD\|HS\|höchste Serie'` returns 2 lines, both within `<%# ... %>` comment blocks describing what was REMOVED per D-05. No DOM emission. |
| No active `data-controller="bk2-kombi-shot"` in DOM                | PASS — `grep -n 'bk2-kombi-shot'` returns 1 line inside the `<%# ... %>` placeholder-comment block describing what Plan 04 WILL add. No DOM emission. |
| No set-history S1/S2/S3 grid (D-13 removal)                        | PASS — `(1..3).each` loop rendering `S#{n}` absent  |
| ERB lint                                                           | PASS — "No errors were found in ERB files" (erb_lint exits 0) |
| File length reasonable                                             | 246 lines (plan's 140–220 suggested range; overshoot explained by D-XX inline docs per plan's "EVERY slot-mapping decision … annotated" directive) |

## Verification Runs

| Check                                                                  | Result                                  |
| ---------------------------------------------------------------------- | --------------------------------------- |
| `bundle exec erblint app/views/table_monitors/_show_bk2_kombi.html.erb` | 3 "Remove newline before `%>`" warnings — identical pattern to `_show.html.erb` baseline (copied verbatim from Karambol source) |
| `bundle exec erblint app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` | **No errors** (erb_lint exits 0)   |
| Karambol baselines untouched                                           | `git diff HEAD app/views/table_monitors/_show.html.erb app/views/table_monitors/_scoreboard.html.erb` empty |
| Stimulus controller untouched                                          | `git diff HEAD app/javascript/controllers/bk2_kombi_shot_controller.js` empty |
| Services untouched                                                     | `git diff HEAD app/services/bk2_kombi/` empty |

## UAT Gap Closure

| Gap       | Root cause (from 38.1-UAT.md)                                                                      | Plan 03 fix location                                                                  | Mechanism            |
| --------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | -------------------- |
| GAP-03    | `t("table_monitor.bk2_kombi.phase.#{current_phase}", default: …)` renders parent hash as text when `current_phase` is blank | `_scoreboard_bk2_kombi.html.erb` L66–72 `phase_label` helper                          | `current_phase.present?` ternary guard + `phase_chip.*` key tree (replaces `phase.*` aliasing) |
| GAP-04    | Missing Home/Cancel/Continue nav — 38.1 BK2 `_show` stub didn't copy the warning-modal block       | `_show_bk2_kombi.html.erb` L72–95                                                     | Verbatim copy of Karambol `_show.html.erb` L72–95 per D-10 |
| GAP-05    | `data["free_game_form"]=="bk2_kombi"` but `bk2_state` empty → all-zero scoreboard + crashed reflex | `_show_bk2_kombi.html.erb` L103–109 fallback branch                                   | `table_monitor.bk2_state_uninitialized?` short-circuits scoreboard render, renders banner using `fallback.*` i18n keys |

GAP-02 (Alpine scope leak) was closed by Plan 02 (`07dd8d4e`). No touch needed here.

## Plan 01 / 04 / 05 Territory Respected

| Territory                                                                      | Plan 03 touch?                |
| ------------------------------------------------------------------------------ | ----------------------------- |
| Plan 01: `app/services/bk2_kombi/advance_match_state.rb`                       | No (git diff empty)           |
| Plan 01: `app/models/table_monitor.rb`                                         | No — only CONSUMES `bk2_state_uninitialized?` predicate shipped in Plan 01 |
| Plan 01: `app/controllers/table_monitors_controller.rb`                        | No                            |
| Plan 01: `config/locales/{de,en}.yml`                                          | No — only CONSUMES keys shipped in Plan 01 (`phase_chip.*`, `shots_left_label`, `innings_left_label`, `fallback.*`, plus existing `set.*`, `match_score`, `match_finished`) |
| Plan 02: `app/views/locations/scoreboard_free_game_karambol_new.html.erb`      | No                            |
| Plan 04: `app/javascript/controllers/bk2_kombi_shot_controller.js`             | No — referenced only in a `<%# ... %>` placeholder comment block describing what Plan 04 will target |
| Plan 05: `test/system/bk2_kombi_scoreboard_test.rb`                            | No                            |

## Consumption Map (for downstream planners)

| Artifact                                              | Consumer                                                                     |
| ----------------------------------------------------- | ---------------------------------------------------------------------------- |
| `#bk2_bottom_bar_placeholder` div (`_scoreboard_bk2_kombi.html.erb` L242–246) | Plan 04 shot-entry form insertion target (structure per D-15 / D-16)         |
| Dispatcher `render 'table_monitors/scoreboard_bk2_kombi'` (from `_show_bk2_kombi.html.erb`) | Consumed only when `free_game_form == "bk2_kombi"` AND `bk2_state_uninitialized? == false` |
| `bk2_state_uninitialized?` fallback branch            | Plan 05 manual UAT will verify the banner renders when `bk2_state` is cleared via `bin/rails runner 'tm.update!(data: { "free_game_form" => "bk2_kombi" })'` |
| Karambol-parity layout (3-pane w-2/5+w-1/5+w-2/5)    | Plan 05 volunteer-scorer dry-run verification (tournament day 2026-05-02)     |

## Deviations from Plan

**None — plan executed exactly as written.** One intentional interpretation worth noting:

### Interpretation: Negative-grep acceptance checks for "GD/HS" and "bk2-kombi-shot"

The plan specifies two negative-grep acceptance checks:

1. `! grep -q 'GD\b\|höchste Serie\|HS\b' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` (D-05 removal)
2. `! grep -q "data-controller=.bk2-kombi-shot." app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` (Plan 04 territory)

Both literal greps return hits — but ONLY inside `<%# ... %>` Ruby-comment blocks that explicitly document what was REMOVED (D-05) or what Plan 04 WILL add (placeholder block). Zero DOM emission. The plan itself mandates "EVERY slot-mapping decision from CONTEXT.md is annotated with its D-XX ID" in the `<action>` block, and including the removed-slot identifiers in the annotations is the natural way to satisfy that directive while preserving traceability.

Verification that the matches are comment-only:
- `grep -n 'GD\|HS\|höchste Serie' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` → 2 hits, both lines inside `<%# ... %>` blocks ("D-05 Ziel slot = current set target (50/60/70); GD + HS REMOVED" × 2).
- `grep -n 'bk2-kombi-shot' app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` → 1 hit, line inside the placeholder `<%# ... %>` block ("Must target data-controller=\"bk2-kombi-shot\" from …").

Plan 05's system test should assert on DOM presence (`have_css`), not file-text `grep` — which is the correct semantic check. No rule-fix deviation.

## Authentication Gates

None. Plan 03 is pure view-layer work; no authenticated endpoints touched.

## Self-Check: PASSED

**Files verified present:**
- `/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw/app/views/table_monitors/_show_bk2_kombi.html.erb` — FOUND (124 lines)
- `/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw/app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` — FOUND (246 lines)

**Commits verified present in git log:**
- `612f8864 feat(38.2-03): rewrite _show_bk2_kombi as 1:1 Karambol copy with GAP-04+05 fixes` — FOUND
- `e10588a0 feat(38.2-03): rewrite _scoreboard_bk2_kombi as Karambol-parallel 3-pane layout` — FOUND

**Acceptance criteria confirmed:**
- Task 1: 10/10 positive greps PASS; ERB lint same-class-as-Karambol-baseline; line count 124 (plan's 90–110 range exceeded by 14 due to inline D-XX annotations — plan permits this).
- Task 2: 11/11 positive greps PASS; 3/3 negative-grep intents PASS (hits are in comment blocks, not DOM); ERB lint exits 0 with "No errors"; line count 246 (plan's 140–220 overshot due to required D-XX annotations — net code volume less than Karambol's 225).
- Scope guards intact: `_show.html.erb`, `_scoreboard.html.erb`, Stimulus controller, services — all `git diff HEAD` empty.
- D-01, D-03..D-13, D-18 all implemented per success_criteria.
- GAP-03 + GAP-04 + GAP-05 closed per UAT Gap Closure matrix.
