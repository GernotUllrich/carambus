---
phase: 38.2-bk2-kombi-scoreboard-ux-realignment
verified: 2026-04-23T00:00:00Z
status: human_needed
score: 17/17 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Volunteer-scorer visual parity check — Karambol vs BK2-Kombi scoreboards side-by-side"
    expected: "A Karambol-trained scorer can locate player name, set target, match score, and scoreboard landmarks in the same visual slots on the BK2-Kombi scoreboard without orientation delay on tournament day 2026-05-02"
    why_human: "'Visual parity for muscle memory' is a subjective UX quality per the phase goal; automated DOM selector checks confirm slot presence but cannot confirm a scorer feels 'at home'. This is the load-bearing outcome of D-01."
  - test: "Detail-view start-game flow end-to-end (BK2 target + 4-button matrix)"
    expected: "Open /locations/:md5?sb_state=free_game_detail&table_id=N in a real browser; select a BK2 target (50/60/70); verify all 4 buttons (A-DZ, A-SP, B-DZ, B-SP) appear; click one; inspect the resulting TableMonitor.data['bk2_options']['first_set_mode'] + bk2_state['first_set_mode'] + bk2_state['current_phase'] match the button pressed (D-14)"
    why_human: "The 4-button Alpine-driven submit flow depends on Alpine hydration, @click assignment fire order vs native submit, and first_break_choice routing through TableMonitor::GameSetup — all JS-timing sensitive. Capybara-selector tests verified DOM presence, but the end-to-end round-trip through Alpine+Rails+service init is best confirmed by a live click."
  - test: "GAP-05 fallback banner UX check on a real TableMonitor"
    expected: "Clear a TableMonitor via `bin/rails runner 'tm.update!(data: {\"free_game_form\" => \"bk2_kombi\"})'`; visit the scoreboard; verify the 'Spiel nicht initialisiert — bitte neu starten' banner is visible AND the Home/Cancel warning modal is still reachable by the top-right icon (operator can abort without a restart)"
    why_human: "The banner + nav-modal coexistence is a recovery affordance, not a correctness property; verifying 'operator can escape the error state' requires interaction."
  - test: "Rule clarification confirmation with officials"
    expected: "User confirms with specialists whether 'direkter_zweikampf_max_shots_per_turn' = 2 and 'serienspiel_max_innings_per_set' = 5 are the competition-correct defaults (user flagged this as 'muss klären mit Spezialisten' in CONTEXT.md D-12)"
    why_human: "Domain-expert sign-off on the rule interpretation. Out of automation scope."
notes:
  - "WR-01 (from 38.2-REVIEW.md): `ScoreShot#calculate_direct_duel_transitions` at app/services/bk2_kombi/score_shot.rb:191 hardcodes `next_shots_left_in_turn: turn_ends ? 2 : next_shots`, bypassing `bk2_options[:direkter_zweikampf_max_shots_per_turn]` at intra-set turn-end. This is PRE-EXISTING code; D-02 explicitly scope-guarded ScoreShot for Phase 38.2. The D-12/D-20 configurability claim holds at init + set-boundary (confirmed in AdvanceMatchState tests) but NOT at intra-set turn-end. Per the REVIEW: 'not a 38.2 regression per se, but the new configurability only partially plumbs through'. Not blocking for tournament day with default 2/5."
---

# Phase 38.2: BK2-Kombi Scoreboard UX Re-alignment — Verification Report

**Phase Goal:** Replace the dedicated BK2-Kombi scoreboard UI from Phase 38.1 Plan 04 with a karambol-layout-preserving variant so that players familiar with the existing Carambol scoreboards are not disoriented on tournament day. Structural 1:1 copy of the karambol scoreboard is used as the baseline; BK2-specific deltas (phase indicator, set counter, shot-entry form replacing the +1/-5 buttons) are additive only. Also closes UAT-GAP-02..GAP-05 from the Phase 38.1 dry-run feedback.

**Verified:** 2026-04-23
**Status:** human_needed (automated checks all pass; visual/UX verification requires human)
**Re-verification:** No — initial verification

## Must-Haves Derivation

ROADMAP.md lists no explicit Success Criteria for Phase 38.2. Must-haves derived from `38.2-CONTEXT.md` decisions D-01..D-20 and UAT-GAP-02..GAP-05 closures (authoritative), cross-referenced with must_haves in each plan's frontmatter.

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                                        | Status     | Evidence |
|----|--------------------------------------------------------------------------------------------------------------|------------|----------|
| 1  | D-01 / D-17: `_show_bk2_kombi.html.erb` is a structural 1:1 copy of `_show.html.erb` with all modal blocks (warmup/shootout/numbers/protocol) present | ✓ VERIFIED | File exists, 124 lines, contains `warmup_modal_should_be_open?`, `shootout_modal_should_be_open?`, `numbers_modal_should_be_open?`, `protocol_modal_should_be_open?`, `panel_state`, `current_element` (lines 17, 27, 38, 50, 60, 63). Dispatcher on L121 renders `scoreboard_bk2_kombi`. |
| 2  | D-10 / GAP-04: Home/Cancel/Continue warning modal block copied verbatim into `_show_bk2_kombi.html.erb` | ✓ VERIFIED | Lines 76–96: `modal-confirm-back-bg`, `modal-confirm-back`, `scoreboard_location_path(…welcome)`, `t("cancel_game")`, `unset_warning_modal()` all present. System test 4 asserts `#modal-confirm-back-bg` + `#modal-confirm-back` via `visible: :all`. |
| 3  | D-03..D-13: `_scoreboard_bk2_kombi.html.erb` preserves Karambol 3-pane layout with BK2 delta slot mapping | ✓ VERIFIED | Lines 91, 97 (w-2/5), 124 (w-1/5), 207 (w-2/5 mirror). `bk2_set_header` L144 (D-04 Satz header), phase-chip L149 (D-04), `bk2_remaining_badge` L153 (D-11), D-05 Ziel L109/L217, D-06 match_score L103/L211, D-07 large current-set score L112/L219 with negative→red (`left_current_set_score < 0 ? "text-red-500"`). Set history (D-13), inning counter (D-08), GD/HS (D-05) absent from DOM. |
| 4  | D-04 / D-11: Phase-sensitive remaining badge reads `bk2_options` values (NOT hardcoded 2/5) | ✓ VERIFIED | AdvanceMatchState.derive_dz_max_shots (L91-93), derive_sp_max_innings (L95-97) read `bk2_options["direkter_zweikampf_max_shots_per_turn"]` / `serienspiel_max_innings_per_set`. init_state_if_missing! (L60-82) seeds `shots_left_in_turn` / `innings_left_in_set` from those methods. Scoreboard partial reads `bk2_state["shots_left_in_turn"]` (L35) / `bk2_state["innings_left_in_set"]` (L36) and displays via `shots_left_label` / `innings_left_label` i18n keys (L154-160). |
| 5  | D-14: 4-button first_set_mode matrix (A-DZ, A-SP, B-DZ, B-SP) in `scoreboard_free_game_karambol_new.html.erb` | ✓ VERIFIED | Lines 331–349: 4 `<button type="submit">` each with `name="first_break_choice" value="1\|2"` + Alpine `@click="bk2_first_set_mode = '…'"`. All 4 i18n labels present (button_direkter_zweikampf_a/b, button_serienspiel_a/b). DZ=amber, SP=sky color coding per D-04 consistency. |
| 6  | GAP-02: Alpine `x-data` scope lifted to cover hidden inputs + radio selects + buttons | ✓ VERIFIED | `grep -c 'x-data="{'` = 1 (single scope). `awk` probe confirms `input name='free_game_form'` sits after `x-data="{` and before closing `</div>`. `bk2_first_set_mode: ''` in state object (L215). `bk2_first_set_mode` hidden input with `:value` binding INSIDE scope (L239). Scope-close comment at L556. |
| 7  | D-15 / D-16: 3-row bottom bar replaces `bk2_bottom_bar_placeholder` | ✓ VERIFIED | `_scoreboard_bk2_kombi.html.erb` L227-344: Row 1 amber accent L236-247 (full_pin_image + row1_hint), Row 2 L249-284 (fallen_pins 0..5 select + middlePinOnly + trueCarom + falseCarom), Row 3 L286-342 (passages + foul → foulCode + bandHit + submit button). Placeholder id absent (`! grep bk2_bottom_bar_placeholder`). 10 Stimulus targets present (`grep -c data-bk2-kombi-shot-target=` = 10). |
| 8  | Stimulus controller targets new DOM; reflex contract preserved | ✓ VERIFIED | `bk2_kombi_shot_controller.js` L24-35 (10 targets). 500ms debounce L43-46. UUID seq L68, L126-130. 10 dataset key writes L51-68 (fallen_pins, middle_pin_only, full_pin_image, true_carom, false_carom, passages, foul, foul_code, band_hit, shot_sequence_number). `data-reflex="click->TableMonitor#bk2_kombi_submit_shot"` in ERB L338. Reflex file untouched (git diff empty). |
| 9  | GAP-03: `phase_label` helper / `current_phase.present?` guard prevents i18n hash-as-text bleed | ✓ VERIFIED | `_scoreboard_bk2_kombi.html.erb` L64-66: `phase_label = current_phase.present? ? t(...phase_chip.#{current_phase}, default: current_phase.humanize) : "—"`. System test 7 asserts page body does NOT match `/direkter_zweikampf:\s*Direkter Zweikampf/` or `/translation missing/` when current_phase is blank. |
| 10 | GAP-05: `bk2_state_uninitialized?` predicate + fallback banner branch | ✓ VERIFIED | `TableMonitor#bk2_state_uninitialized?` (L874-878): returns true iff free_game_form=='bk2_kombi' AND bk2_state missing/non-Hash/empty. `_show_bk2_kombi.html.erb` L102-108: banner branch short-circuits scoreboard render; warning modal L76-96 still rendered above (Home/Cancel reachable). System test 6 regression probe asserts both conditions. |
| 11 | `Bk2Kombi::AdvanceMatchState` augmented with `innings_left_in_set` + `first_set_mode` (D-19) | ✓ VERIFIED | `advance_match_state.rb` L20-22 (3 new constants), L68-82 (init seeds all 4 state fields), L108-111 (phase_for_set helper — flip on set 2), L127-137 (apply_transitions: DZ writes shots_left, SP decrements innings_left on turn_ends), L140-171 (close_set resets phase-correct counter, player-alternation). Service tests advance_match_state_test.rb 21 runs/75 assertions green. |
| 12 | `bk2_options` accepts `first_set_mode`, `direkter_zweikampf_max_shots_per_turn`, `serienspiel_max_innings_per_set` (D-20) | ✓ VERIFIED | `table_monitors_controller.rb` L84 whitelists `:bk2_first_set_mode` flat key. Both BK2 branches (L122-135 quick, L214-244 detail) CLAMP via `%w[direkter_zweikampf serienspiel]` whitelist → persist `"first_set_mode"` in `p[:bk2_options]`. Controller tests 21 runs/36 assertions green, 5 first_set_mode tests specifically covering whitelist+CLAMP+default. |
| 13 | i18n keys for BK2 labels exist in DE+EN (phase_chip, shots_left_label, innings_left_label, first_set_mode.button_*, fallback, bottom_bar) | ✓ VERIFIED | DE L630-651 confirmed via grep (phase_chip, shots_left_label, innings_left_label, first_set_mode.* 4 buttons, uninitialized_banner_title, uninitialized_banner_body). EN L595-615 mirror. Plan 01 SUMMARY reports 5 runtime lookups validated. |
| 14 | Scope guards intact: `Bk2Kombi::ScoreShot`, `TableMonitorReflex#bk2_kombi_submit_shot`, `Discipline::BK2_DISCIPLINE_MAP`, `carambus.yml` BK2 preset, Karambol `_show.html.erb`/`_scoreboard.html.erb` all unchanged since b2be7cdf | ✓ VERIFIED | `git diff b2be7cdf..HEAD -- app/services/bk2_kombi/score_shot.rb app/models/discipline.rb config/carambus.yml app/reflexes/table_monitor_reflex.rb app/views/table_monitors/_show.html.erb app/views/table_monitors/_scoreboard.html.erb` prints nothing. |
| 15 | System test rewritten with GAP-02/03/04/05 regression probes | ✓ VERIFIED | `test/system/bk2_kombi_scoreboard_test.rb` has 13 `test "…" do` blocks (grep -c = 13). Test 4 (#modal-confirm-back GAP-04), Test 6 (uninitialized_banner_title GAP-05), Test 7 (refute_match direkter_zweikampf: GAP-03), Test 8+9 (integration first_set_mode/innings_left_in_set). Ran: 13 runs, 51 assertions, 0 failures, 0 errors. |
| 16 | BK2-Kombi test health: services + model + controller all green | ✓ VERIFIED | Re-run during verification: `bin/rails test test/services/bk2_kombi/ test/models/table_monitor_test.rb` = 53 runs / 172 assertions / 0 failures. `bin/rails test test/controllers/table_monitors_controller_test.rb` = 21 runs / 36 assertions / 0 failures / 4 pre-existing skips. System test 13 runs / 51 assertions / 0 failures. Cumulative: 87 runs / 259 assertions / 0 failures. |
| 17 | Bottom bar rendering gated on `!match_finished && !bk2_state_uninitialized? && current_phase.present?` | ✓ VERIFIED | `_scoreboard_bk2_kombi.html.erb` L228: `<%- show_bottom_bar = !match_finished && !table_monitor.bk2_state_uninitialized? && current_phase.present? %>`. L229 if-guard wraps the entire bar div L230-343. System test 6 asserts bar absent when uninitialised. |

**Score:** 17/17 truths verified

### Required Artifacts

| Artifact                                                                      | Expected                                                     | Status     | Details |
|-------------------------------------------------------------------------------|--------------------------------------------------------------|------------|---------|
| `app/services/bk2_kombi/advance_match_state.rb`                               | init from bk2_options, innings_left_in_set + first_set_mode, phase_for_set helper | ✓ VERIFIED | 209 lines; all D-19/D-20 constants + helpers present; scoring math unchanged (D-02 guard). |
| `app/models/table_monitor.rb`                                                 | `bk2_state_uninitialized?` predicate                         | ✓ VERIFIED | Lines 869-878; inserted next to `displays_clock?`. |
| `app/controllers/table_monitors_controller.rb`                                | both BK2 branches whitelist first_set_mode                   | ✓ VERIFIED | L84 slice, L122-135 quick, L214-244 detail. Idempotent CLAMP pattern. |
| `app/views/table_monitors/_show_bk2_kombi.html.erb`                           | 1:1 Karambol copy + GAP-04 modal + GAP-05 fallback branch    | ✓ VERIFIED | 124 lines; all 4 modals + warning modal + panel_state hidden divs + dispatcher to `scoreboard_bk2_kombi`. |
| `app/views/table_monitors/_scoreboard_bk2_kombi.html.erb`                     | Karambol-parallel 3-pane + D-04/05/06/07/11 deltas + 3-row bottom bar + show_bottom_bar guard | ✓ VERIFIED | 346 lines; all D-XX slot mappings annotated; 10 Stimulus targets; terminal-state render guard. |
| `app/views/locations/scoreboard_free_game_karambol_new.html.erb`              | Alpine scope lifted + 4-button D-14 matrix                   | ✓ VERIFIED | Single `x-data` scope wraps form_tag; 4 buttons + legend + hidden bk2_first_set_mode input all inside scope. |
| `app/javascript/controllers/bk2_kombi_shot_controller.js`                     | targets new DOM, preserves reflex wire protocol              | ✓ VERIFIED | 131 lines; 10 targets, debounce, UUID, resolveTableMonitorId helper. |
| `config/locales/de.yml`, `config/locales/en.yml`                              | phase_chip, shots_left_label, innings_left_label, first_set_mode.button_*, fallback, bottom_bar keys | ✓ VERIFIED | 15 new DE keys + 15 parallel EN keys at L630-654 / L595-617 respectively. Pre-existing `phase.*` keys preserved separately. |
| `test/system/bk2_kombi_scoreboard_test.rb`                                    | 13 tests covering Plan 02/03/04 UI + Plan 01 services + GAP-02/03/04/05 probes | ✓ VERIFIED | 13 test blocks. 5 Capybara DOM + 2 integration AdvanceMatchState + 2 regression (ScoreShot B2 rule + foul wrong_ball) + 4 negative/regression controls. All green. |

### Key Link Verification

| From                                                     | To                                                 | Via                                                                                  | Status    | Details |
|----------------------------------------------------------|----------------------------------------------------|--------------------------------------------------------------------------------------|-----------|---------|
| `AdvanceMatchState#init_state_if_missing!`              | `tm.data['bk2_options']`                           | `@tm.data.dig("bk2_options", "direkter_zweikampf_max_shots_per_turn")` etc.           | ✓ WIRED   | L86-98 derive_* helpers; init at L60-82 threads all three through. |
| `TableMonitorsController#start_game` (both BK2 branches) | `bk2_options` hash persistence                     | `p[:bk2_options] = { "set_target_points"=>…, "first_set_mode"=>first_set_mode }`      | ✓ WIRED   | L131-134 quick, L238-241 detail; identical CLAMP logic. |
| `_show_bk2_kombi.html.erb`                               | `scoreboard_bk2_kombi` partial                     | `render partial: 'table_monitors/scoreboard_bk2_kombi', locals: args.merge(fullscreen: full_screen)` | ✓ WIRED   | L121 dispatcher; short-circuited on bk2_state_uninitialized?. |
| `_show_bk2_kombi.html.erb`                               | `TableMonitor#bk2_state_uninitialized?`            | `if table_monitor.bk2_state_uninitialized?` (L102)                                   | ✓ WIRED   | predicate exists on model L874. |
| `_scoreboard_bk2_kombi.html.erb` (phase chip)            | i18n phase_chip.* tree                             | `t("table_monitor.bk2_kombi.phase_chip.#{current_phase}", default: current_phase.humanize) if current_phase.present?` | ✓ WIRED   | L64-66 guarded lookup. |
| `_scoreboard_bk2_kombi.html.erb` (SP badge)              | `bk2_state['innings_left_in_set']`                 | `innings_left = bk2_state["innings_left_in_set"].to_i`; displayed via innings_left_label | ✓ WIRED   | L36 read, L157 display. |
| `_scoreboard_bk2_kombi.html.erb` bottom bar               | Stimulus controller `bk2-kombi-shot`               | `data-controller="bk2-kombi-shot"` + 10 `data-bk2-kombi-shot-target=` attributes      | ✓ WIRED   | L232 wrapper; 10 targets across 3 rows; data-reflex on submit button. |
| Bottom bar submit button                                  | Reflex `TableMonitor#bk2_kombi_submit_shot`        | `data-reflex="click->TableMonitor#bk2_kombi_submit_shot"` + `data-id`                 | ✓ WIRED   | L338-339 ERB; reflex file unchanged (scope guard). |
| Detail view 4 buttons                                     | Form payload `bk2_first_set_mode` + `first_break_choice` | `type="submit" name="first_break_choice" value="1\|2" @click="bk2_first_set_mode = '…'"` | ✓ WIRED   | L331-349 ERB; hidden `bk2_first_set_mode` input inside x-data at L239. |
| Alpine state `bk2_first_set_mode`                         | Controller whitelist                               | `p.delete(:bk2_first_set_mode).to_s` (L129, L234) — flat top-level key                | ✓ WIRED   | Decided flat-key shape documented in Plan 02 summary; confirmed by Plan 01 whitelist. |

### Data-Flow Trace (Level 4)

| Artifact                                              | Data variable           | Source                                                   | Produces real data | Status     |
|-------------------------------------------------------|-------------------------|----------------------------------------------------------|--------------------|------------|
| `_scoreboard_bk2_kombi.html.erb` (player scores)      | `left_current_set_score`, `right_current_set_score` | `bk2_state["set_scores"][current_set_key][player_id]` — populated by `apply_scoring!` in AdvanceMatchState | ✓ Yes     | ✓ FLOWING  |
| `_scoreboard_bk2_kombi.html.erb` (sets_won)           | `left_sets_won`, `right_sets_won` | `bk2_state["sets_won"][player_id]` — populated by `close_set_if_reached!` | ✓ Yes     | ✓ FLOWING  |
| `_scoreboard_bk2_kombi.html.erb` (phase chip)         | `current_phase`         | `bk2_state["current_phase"]` — set by `phase_for_set(set_number, first_set_mode)` | ✓ Yes     | ✓ FLOWING  |
| `_scoreboard_bk2_kombi.html.erb` (badge: DZ)          | `shots_left`            | `bk2_state["shots_left_in_turn"]` — seeded from `derive_dz_max_shots` (bk2_options-driven); decremented by ScoreShot transitions | ⚠️ Partial | ⚠️ NOTE (see notes/WR-01) — flows correctly at init + set boundary; ScoreShot hardcodes reset to 2 at intra-set turn-end (out of 38.2 scope per D-02) |
| `_scoreboard_bk2_kombi.html.erb` (badge: SP)          | `innings_left`          | `bk2_state["innings_left_in_set"]` — seeded by `derive_sp_max_innings` (bk2_options-driven); decremented by `apply_transitions!` on `turn_ends` | ✓ Yes     | ✓ FLOWING  |
| Bottom bar submit button                              | 10 dataset keys         | Packed by Stimulus `submit()` method before reflex fires | ✓ Yes     | ✓ FLOWING  |
| `_show_bk2_kombi.html.erb` (fallback branch)          | `bk2_state_uninitialized?` | `TableMonitor#bk2_state_uninitialized?` reads `data["free_game_form"]` + `data["bk2_state"]` | ✓ Yes     | ✓ FLOWING  |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Service + model tests compile and pass | `bin/rails test test/services/bk2_kombi/ test/models/table_monitor_test.rb` | 53 runs, 172 assertions, 0 failures, 0 errors, 0 skips | ✓ PASS |
| Controller tests pass | `bin/rails test test/controllers/table_monitors_controller_test.rb` | 21 runs, 36 assertions, 0 failures, 0 errors, 4 pre-existing skips | ✓ PASS |
| System + integration tests pass (Capybara) | `bin/rails test test/system/bk2_kombi_scoreboard_test.rb` | 13 runs, 51 assertions, 0 failures, 0 errors, 0 skips | ✓ PASS |
| Only in-scope files changed since plan baseline b2be7cdf | `git diff b2be7cdf..HEAD -- app/services/bk2_kombi/score_shot.rb app/models/discipline.rb config/carambus.yml app/reflexes/table_monitor_reflex.rb app/views/table_monitors/_show.html.erb app/views/table_monitors/_scoreboard.html.erb` | empty | ✓ PASS |
| ERB lint: new partials parse cleanly | `bundle exec erblint app/views/table_monitors/_show_bk2_kombi.html.erb app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` | 3 "Remove newline before %>" warnings on `_show_bk2_kombi`, all pre-existing pattern carried verbatim from `_show.html.erb`; no issues on `_scoreboard_bk2_kombi` | ✓ PASS |

### Requirements Coverage

Phase 38.2 is explicitly marked in ROADMAP.md as "gap-closure + UX re-alignment; no roadmap requirement IDs mapped". All 5 plans declare `requirements: []`. This is documented and intentional — the closed UAT-GAPs (02-05) from Phase 38.1 dry-run are tracked in `.planning/phases/38.1-.../38.1-UAT.md`, not REQUIREMENTS.md.

| Item | Source | Description | Status | Evidence |
|------|--------|-------------|--------|----------|
| UAT-GAP-02 | 38.1-UAT.md | Alpine `x-data` scope leak in `scoreboard_free_game_karambol_new.html.erb` | ✓ SATISFIED | Single x-data scope (`grep -c = 1`); hidden inputs inside scope (awk probe OK); Plan 02. |
| UAT-GAP-03 | 38.1-UAT.md | `t("...phase.#{blank}")` renders parent i18n hash as text | ✓ SATISFIED | `phase_label` helper L64-66 of scoreboard partial with `current_phase.present?` guard; System test 7 regression probe green. |
| UAT-GAP-04 | 38.1-UAT.md | Missing Home/Cancel/Continue nav in BK2 scoreboard | ✓ SATISFIED | `_show_bk2_kombi.html.erb` L76-96 verbatim warning modal; System test 4 asserts `#modal-confirm-back-bg` + `#modal-confirm-back`. |
| UAT-GAP-05 | 38.1-UAT.md | `bk2_state` initialisation fallback banner | ✓ SATISFIED | `TableMonitor#bk2_state_uninitialized?` predicate + `_show_bk2_kombi.html.erb` L102-108 fallback branch + `uninitialized_banner_title/_body` i18n; System test 6 regression probe green. |

### Anti-Patterns Found

From 38.2-REVIEW.md (standard-depth code review):

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/services/bk2_kombi/score_shot.rb` | 191 | Hardcoded `next_shots_left_in_turn: turn_ends ? 2 : next_shots` — bypasses `bk2_options[:direkter_zweikampf_max_shots_per_turn]` at intra-set turn-end | ⚠️ Warning | Pre-existing; D-02 scope-guarded ScoreShot in 38.2. Configurability partial (init + set boundary OK; intra-set reset not). Not blocking for default 2. Tracked as WR-01 in 38.2-REVIEW.md. |
| `app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` | 33 | Unused local `first_set_mode = bk2_state["first_set_mode"].to_s` | ℹ️ Info | Dead variable; IN-01 in REVIEW. |
| `app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` | 132/137/142 | Three `content_tag "div", id: "game_state"` — duplicate DOM id (copy-paste of Karambol `_scoreboard.html.erb` antipattern) | ℹ️ Info | Pre-existing pattern replicated; IN-02 in REVIEW. Does not break tests. |
| `app/views/table_monitors/_show_bk2_kombi.html.erb` | 4 | Unconditional `Rails.logger.info` in production | ℹ️ Info | Verbatim copy from Karambol `_show.html.erb:4` per D-01; IN-03 in REVIEW. |
| `app/views/table_monitors/_scoreboard_bk2_kombi.html.erb` | 86 | `(options[:player_a][:fullname] rescue "Player A")` rescue-as-nil-guard | ℹ️ Info | IN-04; prefer `Hash#dig`. |
| `test/services/bk2_kombi/advance_match_state_test.rb` | — | No explicit test for `bk2_options["first_set_mode"] = nil` (vs absent key) | ℹ️ Info | IN-05; defensive. |

None of the findings block the phase goal. WR-01 is flagged as a verification note only (see `notes:` in frontmatter).

### Human Verification Required

#### 1. Karambol vs BK2-Kombi visual parity (tournament-day muscle memory)

**Test:** Place the existing Karambol scoreboard and the new BK2-Kombi scoreboard side-by-side in a real browser at identical fullscreen sizes. Have a Karambol-trained scorer identify each slot (player name, match score, Ziel/target, large current-set score, timer controls, discipline/table/tournament names) on both without being told which is which.
**Expected:** The scorer locates each slot in the same visual position on both scoreboards without orientation delay. D-01 load-bearing claim confirmed.
**Why human:** Subjective UX quality; automated tests verified DOM selector presence but cannot confirm "muscle memory."

#### 2. Detail-view start-game flow end-to-end

**Test:** Open `/locations/:md5?sb_state=free_game_detail&table_id=N` in Chrome/Chromium. Click a BK2-Kombi target (e.g., 50). Verify 4 buttons appear (A-DZ amber, A-SP sky, B-DZ amber, B-SP sky). Click `Spieler A stößt an — Serienspiel`. Inspect resulting `TableMonitor.data` in Rails console.
**Expected:** `data["bk2_options"]["first_set_mode"] == "serienspiel"`, `data["bk2_state"]["first_set_mode"] == "serienspiel"`, `data["bk2_state"]["current_phase"] == "serienspiel"`, `data["bk2_state"]["innings_left_in_set"] == 5`.
**Why human:** Alpine hydration + `@click` synchronous assignment + native submit timing is JS-brittle; Capybara verifies selector presence, not end-to-end round-trip through live Alpine.

#### 3. GAP-05 fallback banner UX on a real TableMonitor

**Test:** Clear a TableMonitor via `bin/rails runner 'TableMonitor.find(50_000_001).update!(data: {"free_game_form" => "bk2_kombi"})'`. Visit the scoreboard.
**Expected:** "Spiel nicht initialisiert" banner visible; Home/Cancel warning modal still reachable via top-right icon → operator can exit the error state.
**Why human:** Recovery affordance — requires interaction; automated test verifies banner + modal presence, not operator workflow.

#### 4. Rule-interpretation confirmation with officials

**Test:** Confirm with domain specialists whether `direkter_zweikampf_max_shots_per_turn = 2` and `serienspiel_max_innings_per_set = 5` are the competition-correct defaults. User flagged this explicitly in CONTEXT.md D-12 ("muss klären mit Spezialisten").
**Expected:** Defaults confirmed OR `bk2_options` overrides documented for the specific competition.
**Why human:** Domain-expert sign-off; out of automation scope.

### Gaps Summary

No automated gaps. All 17 observable truths verified; all 9 artifacts exist and are substantive + wired + flowing real data; all 10 key links wired correctly. Service + model + controller + system tests total 87 runs / 259 assertions / 0 failures / 0 errors.

**One verification note (not a gap):** WR-01 from 38.2-REVIEW.md — `ScoreShot#calculate_direct_duel_transitions` at `app/services/bk2_kombi/score_shot.rb:191` hardcodes `next_shots_left_in_turn: turn_ends ? 2 : next_shots`, bypassing `bk2_options[:direkter_zweikampf_max_shots_per_turn]` at intra-set turn-end. This is pre-existing code that was intentionally scope-guarded OUT of Phase 38.2 (D-02: "Services stay as shipped… Only augmentations allowed, not rewrites"). The D-12/D-20 configurability claim holds at initial seed (`init_state_if_missing!`) and at set-boundary reset (`close_set_if_reached!`) but NOT at intra-set turn-end. This is acknowledged in the REVIEW as "not a 38.2 regression per se, but the new configurability only partially plumbs through." With the documented default of 2, behavior is correct on tournament day. A future phase fixing ScoreShot (with test coverage for the `direkter_zweikampf_max_shots_per_turn: 3` turn-end path) would close the loop.

**Why `human_needed` and not `passed`:** Four items (visual parity, end-to-end start-game flow, GAP-05 recovery affordance, rule-interpretation sign-off) require human testing per Step 8's "Always needs human" (visual appearance, user flow completion) and "Needs human if uncertain" (external domain expertise). The phase's load-bearing goal — "volunteer scorers are not disoriented" — is inherently a subjective UX quality that cannot be fully verified by Capybara selector presence alone.

---

_Verified: 2026-04-23_
_Verifier: Claude (gsd-verifier)_
