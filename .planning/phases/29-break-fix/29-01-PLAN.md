---
phase: 29-break-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/players/pool_scoreboard_benutzerhandbuch.de.md
  - docs/players/pool_scoreboard_benutzerhandbuch.en.md
  - docs/players/scoreboard-guide.de.md
  - docs/players/scoreboard-guide.en.md
  - docs/reference/mkdocs_documentation.en.md
  - docs/reference/mkdocs_dokumentation.de.md
  - docs/reference/mkdocs_dokumentation.en.md
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "Automated fixer has been run in --live mode against all active docs"
    - "All 32 screenshot broken links are resolved by removing image link markup and keeping alt text"
    - "All 12 template example links in mkdocs documentation guides are converted to non-link code examples"
    - "bin/check-docs-links.rb reports zero broken links for the 7 files modified in this plan"
  artifacts:
    - path: "docs/players/scoreboard-guide.de.md"
      provides: "Screenshot references converted to text descriptions"
    - path: "docs/reference/mkdocs_dokumentation.de.md"
      provides: "Example links converted to inline code"
  key_links:
    - from: "bin/fix-docs-links.rb"
      to: "docs/**/*.md"
      via: "pattern-based regex replacement"
      pattern: "FIX_PATTERNS"
---

<objective>
Run the automated link fixer and resolve all 44 pattern-based broken links (32 screenshot image references + 12 template example links) so that nearly 60% of Phase 29 broken links are cleared in one pass.

Purpose: Per D-01, automation runs first to handle pattern-based fixes before any manual work. This clears the bulk of findings and establishes a clean intermediate baseline.
Output: 7 documentation files with broken links resolved; intermediate verification showing reduced broken link count.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-audit-triage/28-02-SUMMARY.md
@docs/audit.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated fixer and resolve screenshot + template broken links</name>
  <files>
    docs/players/pool_scoreboard_benutzerhandbuch.de.md,
    docs/players/pool_scoreboard_benutzerhandbuch.en.md,
    docs/players/scoreboard-guide.de.md,
    docs/players/scoreboard-guide.en.md,
    docs/reference/mkdocs_documentation.en.md,
    docs/reference/mkdocs_dokumentation.de.md,
    docs/reference/mkdocs_dokumentation.en.md
  </files>
  <read_first>
    bin/fix-docs-links.rb,
    bin/check-docs-links.rb,
    docs/audit.json,
    docs/players/pool_scoreboard_benutzerhandbuch.de.md,
    docs/players/pool_scoreboard_benutzerhandbuch.en.md,
    docs/players/scoreboard-guide.de.md,
    docs/players/scoreboard-guide.en.md,
    docs/reference/mkdocs_documentation.en.md,
    docs/reference/mkdocs_dokumentation.de.md,
    docs/reference/mkdocs_dokumentation.en.md
  </read_first>
  <action>
**Step 1 — Run automated fixer (per D-01):**
Run `ruby bin/fix-docs-links.rb --live` to apply all pattern-based fixes across all active docs. This handles language suffix removal (.de.md/.en.md -> .md) and path prefix corrections. Commit the result.

**Step 2 — Fix 32 screenshot broken links (per D-02):**
These are FIND-027 through FIND-058 in docs/audit.json. The target images (e.g., `players/screenshots/scoreboard_welcome.png`) do not exist. Per D-02: remove the image link markup but keep the alt text as a text description.

For each broken screenshot image reference in these 4 files:
- `docs/players/pool_scoreboard_benutzerhandbuch.de.md` (FIND-027 to FIND-031: 5 images)
- `docs/players/pool_scoreboard_benutzerhandbuch.en.md` (FIND-032 to FIND-036: 5 images)
- `docs/players/scoreboard-guide.de.md` (FIND-037 to FIND-047: 11 images)
- `docs/players/scoreboard-guide.en.md` (FIND-048 to FIND-058: 11 images)

Transform: `![Alt text](players/screenshots/some_image.png)` -> `*[Alt text — Screenshot ausstehend]*` (DE files) or `*[Alt text — screenshot pending]*` (EN files).

If the image is inline within other content (not on its own line), just replace `![alt](path)` with the italicized text placeholder.

**Step 3 — Fix 12 template example links (per D-02):**
These are FIND-063 to FIND-074 in docs/audit.json. The links point to placeholder names (`file.md`, `datei.md`, `assets/image.png`, `assets/bild.png`) used as EXAMPLES in mkdocs documentation guides. They are not real links — they are showing users how to write markdown links.

In these 3 files:
- `docs/reference/mkdocs_documentation.en.md` (FIND-063 to FIND-066, lines ~264-272)
- `docs/reference/mkdocs_dokumentation.de.md` (FIND-067 to FIND-070, lines ~264-272)
- `docs/reference/mkdocs_dokumentation.en.md` (FIND-071 to FIND-074, lines ~264-272)

These example links like `[link text](file.md)` should be wrapped in backtick code spans so they are not parsed as actual links: `` `[link text](file.md)` ``. Similarly for image examples: `` `![alt](assets/image.png)` ``.

If the examples are already inside a fenced code block, they should not be flagged — check whether the link checker is reading inside code blocks. If they are NOT in code blocks, wrap each example link line in a fenced code block or use inline code backticks.
  </action>
  <verify>
    <automated>ruby bin/check-docs-links.rb --exclude-archives 2>&1 | grep -c "players/pool_scoreboard\|players/scoreboard-guide\|mkdocs_documentation\|mkdocs_dokumentation" | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - `docs/players/scoreboard-guide.de.md` contains no string matching `](players/screenshots/`
    - `docs/players/scoreboard-guide.en.md` contains no string matching `](players/screenshots/`
    - `docs/players/pool_scoreboard_benutzerhandbuch.de.md` contains no string matching `](players/screenshots/`
    - `docs/players/pool_scoreboard_benutzerhandbuch.en.md` contains no string matching `](players/screenshots/`
    - `docs/reference/mkdocs_dokumentation.de.md` lines 260-275 have no bare `](datei.md)` or `](assets/bild.png)` outside code formatting
    - `docs/reference/mkdocs_dokumentation.en.md` lines 260-275 have no bare `](file.md)` or `](assets/image.png)` outside code formatting
    - `docs/reference/mkdocs_documentation.en.md` lines 260-275 have no bare `](file.md)` or `](assets/image.png)` outside code formatting
  </acceptance_criteria>
  <done>All 32 screenshot image references converted to text placeholders. All 12 template example links wrapped in code formatting. Automated fixer has been run. Zero broken links reported by check-docs-links.rb for the 7 modified files.</done>
</task>

<task type="auto">
  <name>Task 2: Verify intermediate state and run check-docs-links.rb (per D-04)</name>
  <files>docs/audit.json</files>
  <read_first>
    bin/check-docs-links.rb,
    bin/check-docs-coderef.rb
  </read_first>
  <action>
**Per D-04:** Run both verification tools after the automation pass to establish intermediate baseline:

1. Run `ruby bin/check-docs-links.rb --exclude-archives` and capture the output. The remaining broken links should be only the 31 non-screenshot, non-template links (FIND-001 to FIND-026, FIND-059 to FIND-062, FIND-075) plus any the automated fixer resolved. Record the count.

2. Run `ruby bin/check-docs-coderef.rb --exclude-archives` and capture the output. All 3 stale refs (FIND-076 to FIND-078) should still be present since they are handled in Plan 02.

3. Log the intermediate state: "After Plan 01: X broken links remaining (down from 75), 3 stale refs remaining (unchanged)."

Do NOT update audit.json — that is discretionary and will be done at the end if needed.
  </action>
  <verify>
    <automated>ruby bin/check-docs-links.rb --exclude-archives 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ruby bin/check-docs-links.rb --exclude-archives` output does NOT contain any line referencing `scoreboard-guide` or `pool_scoreboard_benutzerhandbuch` or `mkdocs_dokumentation` or `mkdocs_documentation`
    - `ruby bin/check-docs-coderef.rb --exclude-archives` output still references `UmbScraperV2` and `tournament_monitor_support` (3 findings — these are Plan 02 scope)
  </acceptance_criteria>
  <done>Intermediate verification confirms 44 broken links resolved by Plan 01. Remaining broken link count and stale ref count logged for Plan 02 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bin/ scripts -> docs/ files | Scripts read and modify documentation files on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | T (Tampering) | bin/fix-docs-links.rb | accept | Script is local tooling run by developer; no untrusted input. Regex patterns are hardcoded. |
| T-29-02 | I (Info Disclosure) | docs/ files | accept | Documentation is public; no secrets in doc files. |
</threat_model>

<verification>
- `ruby bin/check-docs-links.rb --exclude-archives` shows reduced broken link count (75 -> ~31)
- No broken links reference screenshot paths or template example paths
- All 7 modified files still render valid markdown
</verification>

<success_criteria>
- Automated fixer has run in --live mode
- 32 screenshot broken links resolved (FIND-027 to FIND-058)
- 12 template example links resolved (FIND-063 to FIND-074)
- Intermediate verification baseline established for Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/29-break-fix/29-01-SUMMARY.md`
</output>
