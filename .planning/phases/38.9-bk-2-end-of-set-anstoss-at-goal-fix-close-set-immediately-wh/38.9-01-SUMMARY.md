---
phase: 38.9-bk-2-end-of-set-anstoss-at-goal-fix-close-set-immediately-wh
plan: 01
subsystem: scoring-engine
tags: [bk-2, bk-2kombi, end-of-set, erste-aufnahme-gate, table-monitor, predicate, extend-before-build]

# Dependency graph
requires:
  - phase: 38.7
    provides: Plan 38.7-02's D-02 BK-2 / BK-2kombi-SP Nachstoss-Aufnahme-close branch + bk_with_nachstoss/anstoss_role/anstoss_innings/anstoss_at_goal locals (Phase 38.9 4th branch reuses these locals verbatim — zero recomputation)
  - phase: 38.7
    provides: follow_up? Erste-Aufnahme-Gate at table_monitor.rb:1205-1210 (Phase 38.9 mirrors this gate symmetrically on the close-side)
  - phase: 38.8
    provides: AASM operator-gate (final_match_score :start_rematch + :close_match) — Phase 38.9 piggybacks on this end-state architecture without modifying it
provides:
  - 4th BK-2 / BK-2kombi-SP branch in TableMonitor#end_of_set? — closes the set IMMEDIATELY when Anstoss reached balls_goal in inning >= 2 (Erste-Aufnahme-Gate fails on the close-side)
  - 2 new tests in test/models/table_monitor_test.rb pinning RED-then-GREEN semantics (SC-1 + SC-2 boundary condition guard)
  - Symmetric closure of the three-way semantic mismatch between follow_up?, end_of_set?, and ScoreEngine#terminate_inning_data — without touching the latter two
affects: [Phase 38.10+ if any future BK-2 scoring rule changes; UAT scenario for BK-2 quick-game where Anstoss reaches goal in inning 2]

# Tech tracking
tech-stack:
  added: []  # No new dependencies — additive 25-LOC predicate branch
  patterns:
    - "Erste-Aufnahme-Gate close-side mirror — symmetric guard pattern for BK-2 / BK-2kombi-SP discipline-specific set-closure rules"
    - "extend-before-build SKILL applied (validated 2026-04-29 with -1463 LOC rollback on this exact surface): additive sub-branch on the existing Plan 38.7-02 D-02 block, reusing already-computed locals — NO parallel state machine, NO new helper class, NO new predicate"

key-files:
  created: []
  modified:
    - app/models/table_monitor.rb (added Phase 38.9 4th branch inside the existing bk_with_nachstoss block, after the Plan 38.7-02 D-02 nachstoss_finished_followup sub-block)
    - test/models/table_monitor_test.rb (appended Phase 38.9 Plan 01 test block with header comment + 2 tests: SC-1 RED + SC-2 regression-guard)

key-decisions:
  - "Insertion location chosen INSIDE the existing Plan 38.7-02 D-02 block rather than between branches — reuses the already-computed locals (anstoss_role, nachstoss_role, anstoss_innings, nachstoss_innings, anstoss_at_goal) verbatim, zero recomputation, minimum diff surface."
  - "Sub-check order: existing D-02 nachstoss_finished_followup check FIRST, new Phase 38.9 anstoss_innings >= 2 check SECOND. Cases are mutually exclusive (D-02 requires nachstoss has played the +1 inning; Phase 38.9 fires when nachstoss has not played in the goal-reaching inning). Order does not matter for correctness, but listing the existing case first preserves diff-locality."
  - "Phase 38.9 fix is ENTIRELY in end_of_set? — follow_up?, ScoreEngine#terminate_inning_data, and the banner conditional in _player_score_panel.html.erb are UNCHANGED. The three-way semantic mismatch is closed by making end_of_set? agree with follow_up?'s Erste-Aufnahme-Gate semantics on the close-side."
  - "Pre-existing failures in test/system/bk2_scoreboard_test.rb (6 failures + 13 errors) are NOT auto-fixed — verified pre-existing at commit cfee5962 (parent of Phase 38.9). Logged to deferred-items.md for a future cleanup quick-task. Per scope-boundary rule."

patterns-established:
  - "Mirror-the-gate: when a discipline-specific gate exists at one site (banner-side), check its mirror at all related decision sites (close-side, switch-side). The original 79328663 commit only added the gate at follow_up?; the symmetric end_of_set? mirror was forgotten — this is the latent defect Phase 38.9 closes."
  - "Reuse locals from Plan 38.7-02 D-02 block — proves the SKILL extend-before-build is honored at the variable-naming level, not just at the structural level."

requirements-completed: [PHASE-38.9-SC-1, PHASE-38.9-SC-2, PHASE-38.9-SC-3, PHASE-38.9-SC-4]

# Metrics
duration: ~6 min
completed: 2026-05-01
---

# Phase 38.9 Plan 01: BK-2 End-of-Set Fourth-Branch Fix Summary

**Fixed the latent BK-2 / BK-2kombi-SP "Anstoss-at-goal-in-inning-2" defect (introduced together with the Erste-Aufnahme-Gate in commit 79328663, 2026-04-28) by adding a symmetric 4th sub-branch to `TableMonitor#end_of_set?` that mirrors the gate's logic on the close-side — additive 25-LOC patch reusing all locals from the existing Plan 38.7-02 D-02 block, no parallel state machine, no new helper class.**

## Performance

- **Duration:** ~6 min
- **Started:** 2026-05-01T14:48:08Z
- **Completed:** 2026-05-01T14:53:00Z
- **Tasks:** 4 (Tasks 1-4 of the 4-task plan)
- **Files modified:** 2 (app/models/table_monitor.rb, test/models/table_monitor_test.rb)

## Accomplishments

- **SC-1 sealed (RED → GREEN):** Task 1 RED test pinned the latent defect (BK-2 with Anstoss=playera at balls_goal=50, innings=2, Nachstoss innings=0 returned `end_of_set? == false` today). Task 3 fix turned it GREEN via the new 4th sub-branch.
- **SC-2 sealed (regression):** Task 2 regression-guard test pinned the boundary condition (BK-2 with Anstoss at balls_goal in inning 1 still returns `end_of_set? == false` so the Nachstoss-Aufnahme + red banner happy path stays open). Predicate `anstoss_innings >= 2` provably excludes inning 1 by construction.
- **SC-3 sealed (no regression):** All 21 tests in `test/models/table_monitor_test.rb` GREEN (5 Plan 38.7-02 D-02 tests + 2 Phase 38.9 tests + 14 baseline). 19 pre-existing `bk2_scoreboard_test.rb` failures verified pre-existing at parent commit `cfee5962` and deferred (see `deferred-items.md`).
- **SC-4 sealed (tiebreak preserved):** `test/system/tiebreak_test.rb` 4/4 GREEN — tied-at-equal-innings BK-2 sets still flow through the legacy karambol parity branch into Plan 38.7-04's tiebreak detection, unchanged.
- **Phase 38.8 regression seal:** `test/system/final_match_score_operator_gate_test.rb` 4/4 GREEN — Phase 38.8 operator-gate architecture untouched.

## Task Commits

Each task committed atomically (RED → regression → GREEN → docs):

1. **Task 1: RED — characterization test for inning-2 Anstoss-at-goal latent defect** — `78b0dfbc` (test)
2. **Task 2: regression — Erste-Aufnahme-Gate happy path stays open at inning 1** — `4d275e69` (test)
3. **Task 3: GREEN — close BK-2 / BK-2kombi-SP set immediately when Erste-Aufnahme-Gate fails (inning >= 2)** — `d231f9d7` (fix)
4. **Task 4: SUMMARY + STATE update + ROADMAP progress + REQUIREMENTS check-off** — pending final commit (this commit)

## Files Created/Modified

- `app/models/table_monitor.rb` — Added 25-LOC sub-branch inside the existing `bk_with_nachstoss && data["playera"]["balls_goal"].to_i.positive?` block (line 1510), AFTER the Plan 38.7-02 D-02 `nachstoss_finished_followup` sub-block (lines 1521-1527), BEFORE the outer `end` (line 1554). New predicate: `if anstoss_at_goal && anstoss_innings >= 2 ... return true`. Mirrors the `follow_up?` Erste-Aufnahme-Gate at lines 1205-1210 on the close-side. Logs `[TableMonitor#end_of_set?] Phase 38.9 BK-2-immediate-close: ...` matching the style of branches 1-3.
- `test/models/table_monitor_test.rb` — Appended Phase 38.9 Plan 01 test block (header comment + 2 tests) AFTER line 174 (the last existing Plan 38.7-02 D-02 regression-guard test). Both tests use the existing `build_bk_data` helper (canonical fixture shape — DRY with the 5 Plan 38.7-02 D-02 tests). Total file growth: +60 LOC.
- `.planning/phases/38.9-…/deferred-items.md` (NEW) — Documents the 19 pre-existing `bk2_scoreboard_test.rb` failures verified at parent commit `cfee5962`, with cluster diagnosis (Cluster A: stale `Bk2::CommitInning` references from Phase 38.5; Cluster B: stale `BK2-Kombi` regexes from Phase 38.6) and recommended cleanup follow-up.

## Decisions Made

- **Local-variable reuse, NOT recomputation:** The new sub-branch sits INSIDE the existing Plan 38.7-02 D-02 outer-`if` block, after the `nachstoss_finished_followup` sub-check. This means it reads `anstoss_at_goal` and `anstoss_innings` directly from locals already computed at lines 1518-1520. NO new helper, NO redundant data lookups. (Plan-prescribed local names `bk_with_nachstoss`, `anstoss_role`, `anstoss_innings`, `anstoss_at_goal` ALL match the actual source — no substitutions needed.)
- **Sub-check order documented:** Existing D-02 `nachstoss_finished_followup` first, new Phase 38.9 `anstoss_innings >= 2` second. The cases are mutually exclusive (the D-02 case requires Nachstoss has played his +1 inning; the Phase 38.9 case requires Anstoss reached goal in inning >= 2 with Nachstoss not having played in that inning). Order is not load-bearing, but listing the existing case first preserves diff-locality.
- **NOT-to-modify list strictly honored:** `follow_up?` (lines 1188-1219) UNCHANGED, `score_engine.rb#terminate_inning_data` UNCHANGED, `_player_score_panel.html.erb` UNCHANGED. The three-way semantic mismatch is resolved entirely by making `end_of_set?` agree with `follow_up?`'s Erste-Aufnahme-Gate semantics on the close-side.

## Deviations from Plan

None - plan executed exactly as written.

The plan-prescribed local variable names (`bk_with_nachstoss`, `anstoss_role`, `anstoss_innings`, `anstoss_at_goal`) all matched the actual source verbatim — no substitutions needed. Insertion location, sub-check order, and predicate shape all matched the plan's specification.

## Issues Encountered

- **Pre-existing `bk2_scoreboard_test.rb` failures:** During Task 4 regression sweep, the BK-2 family system suite reported 6 failures + 13 errors. **Verified pre-existing** by checking out parent commit `cfee5962` and re-running the suite — identical 6 failures + 13 errors at the parent. Root cause: stale references to `Bk2::CommitInning` (deleted in Phase 38.5 D-13) and stale regexes for `BK2-Kombi` (renamed to `BK-2kombi` in Phase 38.6 D-12). Per scope-boundary rule, NOT auto-fixed by Phase 38.9. Logged to `.planning/phases/38.9-…/deferred-items.md` with full cluster analysis and recommended follow-up.

## Regression Sweep — Tail Logs

### `bin/rails test test/models/table_monitor_test.rb` (unit-level — full file)

```
.....................

Finished in 0.293939s, 71.4434 runs/s, 146.2889 assertions/s.
21 runs, 43 assertions, 0 failures, 0 errors, 0 skips
```

### `bin/rails test test/system/tiebreak_test.rb` (Phase 38.7 SC-4 seal)

```
* Listening on http://127.0.0.1:59544
.********* Test Client is live *******
.********* Test Client is live *******
.********* Test Client is live *******
.

Finished in 2.023760s, 1.9765 runs/s, 15.8122 assertions/s.
4 runs, 32 assertions, 0 failures, 0 errors, 0 skips
```

### `bin/rails test test/system/final_match_score_operator_gate_test.rb` (Phase 38.8 regression seal)

```
Run options: --seed 24925

# Running:

....

Finished in 0.345077s, 11.5916 runs/s, 55.0602 assertions/s.
4 runs, 19 assertions, 0 failures, 0 errors, 0 skips
```

### `bin/rails test test/system/bk2_scoreboard_test.rb` (BK-2 family system suite — pre-existing failures)

```
Finished in 3.674554s, 15.7842 runs/s, 72.9340 assertions/s.
58 runs, 268 assertions, 6 failures, 13 errors, 0 skips
```

**These 19 failures are pre-existing** — confirmed by checking out parent commit `cfee5962` and re-running the suite (identical 6 failures + 13 errors at parent). NOT regressions from Phase 38.9. See `deferred-items.md` for cluster analysis and recommended cleanup. SC-3 verification path is intact via the unit-level + tiebreak system + final-match-score system suites.

### Lint final pass

```bash
$ bundle exec standardrb app/models/table_monitor.rb test/models/table_monitor_test.rb
```

The new lines I added in `app/models/table_monitor.rb` (1529-1553) introduced **zero new lint violations** (verified by greppin for line numbers in the inserted range — no hits). The new lines in `test/models/table_monitor_test.rb` (206-207, 227-228) follow the **identical Layout/ArgumentAlignment style** as the existing baseline tests at lines 122-124, 132-134, 161-162, 170-171 — pre-existing project-style consistent. Per scope-boundary rule, no new violations introduced.

## SC ↔ Test Mapping

| Success Criterion | Test name | Status | Commit |
|---|---|---|---|
| SC-1: BK-2 Anstoss-at-goal in inning >= 2 closes set IMMEDIATELY | `end_of_set? closes BK-2 set immediately when Anstoss reaches balls_goal in inning 2 (Erste-Aufnahme-Gate fails — SC-1)` | RED `78b0dfbc` → GREEN `d231f9d7` | Lock |
| SC-2: BK-2 Anstoss-at-goal in inning 1 still triggers Nachstoss-Aufnahme + red banner | `end_of_set? does NOT close BK-2 set when Anstoss reaches balls_goal in inning 1 (Erste-Aufnahme-Gate happy path — SC-2)` | PASS today AND after Task 3 (`4d275e69`) | Boundary guard |
| SC-3: All existing BK-2 tests still green | 21/21 `test/models/table_monitor_test.rb` (5 Plan 38.7-02 D-02 + 2 Phase 38.9 + 14 baseline) | All GREEN | Sweep above |
| SC-4: Phase 38.7 tiebreak path preserved | 4/4 `test/system/tiebreak_test.rb` | All GREEN | Sweep above |

## Self-Check

### Created files exist

```
$ ls .planning/phases/38.9-bk-2-end-of-set-anstoss-at-goal-fix-close-set-immediately-wh/
38.9-01-end-of-set-fourth-branch-PLAN.md
38.9-CONTEXT.md
deferred-items.md
38.9-01-SUMMARY.md   (this file)
```

FOUND: `.planning/phases/38.9-…/38.9-01-SUMMARY.md`
FOUND: `.planning/phases/38.9-…/deferred-items.md`

### Commits exist

```
$ git log --oneline -5
d231f9d7 fix(38.9-01): close BK-2 / BK-2kombi-SP set immediately when Erste-Aufnahme-Gate fails (inning >= 2)
4d275e69 test(38.9-01): regression — Erste-Aufnahme-Gate happy path stays open at inning 1
78b0dfbc test(38.9-01): RED — characterization test for inning-2 Anstoss-at-goal latent defect
cfee5962 docs(phase-38.9): plan BK-2 end-of-set fourth-branch fix
63d3fad4 docs(phase-38.8): evolve PROJECT.md after phase completion
```

FOUND: `78b0dfbc` (Task 1 RED test)
FOUND: `4d275e69` (Task 2 regression test)
FOUND: `d231f9d7` (Task 3 GREEN fix)

### Production code carries the load-bearing artifacts

```
$ grep -n "Phase 38.9 Plan 01\|anstoss_at_goal && anstoss_innings >= 2\|Erste-Aufnahme-Gate close-side mirror\|BK-2-immediate-close" app/models/table_monitor.rb
1529:      # Phase 38.9 Plan 01 — BK-2 / BK-2kombi-SP Erste-Aufnahme-Gate close-side mirror.
1546:      if anstoss_at_goal && anstoss_innings >= 2
1547:        Rails.logger.info "[TableMonitor#end_of_set?] Phase 38.9 BK-2-immediate-close: " \
```

FOUND: load-bearing predicate `anstoss_at_goal && anstoss_innings >= 2` at line 1546.
FOUND: comment block `Phase 38.9 Plan 01 — ... Erste-Aufnahme-Gate close-side mirror`.
FOUND: log line tag `Phase 38.9 BK-2-immediate-close`.

```
$ grep -c "bk_with_nachstoss" app/models/table_monitor.rb
2
```

CONFIRMED: `bk_with_nachstoss` declared once + reused once. NO new helper, NO new predicate. SKILL extend-before-build honored at the variable-naming level.

## Self-Check: PASSED

All claims verified. Production code carries the documented changes; both new tests landed; all three task commits exist; SC ↔ test mapping is intact.

## Next Phase Readiness

- **Phase 38.9 ready for `/gsd-verify-work`** — human UAT scenario:
  1. Start BK-2 quick-game (TR-Ctl preset).
  2. Player A: inning 1 → some non-goal score; switch.
  3. Player A: inning 2 → reaches balls_goal exactly.
  4. **Expect:** game ends IMMEDIATELY (transitions to `set_over` / `final_match_score`), NO Player B inning, NO red Nachstoß banner. The "Nächstes Spiel" button (Phase 38.8 operator-gate) appears.
- **Phase 38.9 ready for `/gsd-code-review`** — additive 25-LOC change, predicate-only, no new dependencies, threat model trivially N/A (T-38.9-01..T-38.9-05 all `accept` except T-38.9-03 which is `mitigate` via the existing `bk_with_nachstoss` outer gate).
- **Deferred for future cleanup:** 19 pre-existing failures in `test/system/bk2_scoreboard_test.rb` (Cluster A: stale `Bk2::CommitInning` from Phase 38.5; Cluster B: stale `BK2-Kombi` regexes from Phase 38.6). Estimated scope: ~1 hour single quick-task or small Phase 38.10 cleanup plan. Documented in `deferred-items.md`.

---
*Phase: 38.9-bk-2-end-of-set-anstoss-at-goal-fix-close-set-immediately-wh*
*Plan: 01*
*Completed: 2026-05-01*
