---
phase: 38.9-bk-2-end-of-set-anstoss-at-goal-fix-close-set-immediately-wh
verified: 2026-05-01T17:30:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
re_verification: false
human_verification:
  - test: "Start a BK-2 quick-game (TR-Ctl preset). Player A inning 1: enter a non-goal score and switch. Player A inning 2: reach balls_goal exactly."
    expected: "Game ends IMMEDIATELY (transitions to set_over / final_match_score). NO Player B inning is opened. NO red Nachstoß banner appears. The 'Nächstes Spiel' button (Phase 38.8 operator-gate) appears."
    why_human: "End-to-end UAT against the live scoreboard view + AASM state machine + WebSocket broadcast — automated unit/system tests cover the predicate and the seal suites, but the visual confirmation that the red banner does NOT render and that the operator-gate button DOES render in this exact scenario requires human eyes."
  - test: "Start a BK-2 quick-game. Player A reaches balls_goal in his FIRST inning."
    expected: "Red 'Nachstoß' banner appears. Set stays open. Player B gets exactly one Nachstoss-Aufnahme. After Player B's inning completes (with or without reaching the goal), the set closes via the Phase 38.7-02 D-02 branch."
    why_human: "Boundary-condition UAT for the Erste-Aufnahme-Gate happy path. SC-2 unit test pins end_of_set? == false at inning 1, but the human-visible banner rendering and operator flow through the Nachstoss-Aufnahme cannot be programmatically observed."
  - test: "Start a BK-2 quick-game where both players end up at balls_goal in equal innings (tied set)."
    expected: "Tiebreak modal opens via the legacy karambol parity branch (Phase 38.7 path). Phase 38.9 4th branch must NOT short-circuit this — verified at unit level, but the modal opening + operator workflow needs a visual check."
    why_human: "Phase 38.7 tiebreak UI flow — system test covers backend, but UAT confirms the modal still opens and operator can complete the tiebreak."
---

# Phase 38.9: BK-2 End-of-Set Anstoss-at-Goal Fix Verification Report

**Phase Goal:** Close the latent BK-2 / BK-2kombi-SP defect by adding a 4th branch to `TableMonitor#end_of_set?` that mirrors the Erste-Aufnahme-Gate from `follow_up?` on the close-side. When Anstoss reaches `balls_goal` in inning >= 2 (Nachstoss not yet played), the set must close IMMEDIATELY — preventing an unauthorized Player B inning + the silent active_player switch. Phase 38.7 tiebreak path and Phase 38.8 final_match_score operator-gate must be preserved unchanged.
**Verified:** 2026-05-01T17:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 1   | SC-1: BK-2 set with Anstoss reaching balls_goal in inning >= 2 (Nachstoss not yet played) closes IMMEDIATELY via `end_of_set? == true` — no unauthorized Player B inning, no silent active_player switch | VERIFIED | RED test `end_of_set? closes BK-2 set immediately when Anstoss reaches balls_goal in inning 2 (Erste-Aufnahme-Gate fails — SC-1)` PASSES at HEAD (test/models/table_monitor_test.rb:189-214). Production predicate `if anstoss_at_goal && anstoss_innings >= 2` present at app/models/table_monitor.rb:1546. |
| 2   | SC-2: BK-2 set with Anstoss reaching balls_goal in inning 1 still leaves `end_of_set? == false` (Erste-Aufnahme-Gate happy path preserved — Nachstoss-Aufnahme still triggers, red banner still renders) | VERIFIED | Regression test `end_of_set? does NOT close BK-2 set when Anstoss reaches balls_goal in inning 1 (Erste-Aufnahme-Gate happy path — SC-2)` PASSES (test/models/table_monitor_test.rb:216-235). Predicate `anstoss_innings >= 2` excludes inning 1 by construction. |
| 3   | SC-3: All existing BK-2 + general TableMonitor tests stay green — Plan 38.7-02 D-02 tests, karambol regression guards, bk2_state_uninitialized? tests are unchanged in outcome | VERIFIED | `bin/rails test test/models/table_monitor_test.rb` reports `21 runs, 43 assertions, 0 failures, 0 errors, 0 skips` (5 D-02 + 2 Phase 38.9 + 14 baseline). 19 pre-existing failures in `test/system/bk2_scoreboard_test.rb` documented as pre-existing in deferred-items.md (verified at parent commit cfee5962 — identical 6 failures + 13 errors before any 38.9 work). |
| 4   | SC-4: Phase 38.7 tiebreak path preserved — tied BK-2 set (both at goal in equal innings) still opens the tiebreak modal via the existing legacy karambol parity branch | VERIFIED | `bin/rails test test/system/tiebreak_test.rb` reports `4 runs, 32 assertions, 0 failures, 0 errors, 0 skips` (matches Phase 38.7 baseline contract). |
| 5   | RED test BEFORE the fix lands demonstrates the latent defect (returns false today); turns GREEN after the 4th branch lands | VERIFIED | Commit `78b0dfbc test(38.9-01): RED — characterization test for inning-2 Anstoss-at-goal latent defect` precedes commit `d231f9d7 fix(38.9-01): close BK-2 / BK-2kombi-SP set immediately ...` in git log. SUMMARY documents the RED state at `78b0dfbc` and GREEN state at `d231f9d7`. |
| 6   | Regression test (inning-1 happy path) PASSES on master and continues to PASS after the fix — proving the new branch does NOT collide with the Erste-Aufnahme-Gate in `follow_up?` | VERIFIED | Commit `4d275e69 test(38.9-01): regression — Erste-Aufnahme-Gate happy path stays open at inning 1` lands BEFORE Task 3 fix. Test currently passes inside the 21-run suite at HEAD. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected    | Status | Details |
| -------- | ----------- | ------ | ------- |
| `app/models/table_monitor.rb` | `end_of_set?` extended with 4th BK-2/BK-2kombi-SP branch (close-side mirror of `follow_up?` Erste-Aufnahme-Gate at lines 1205-1210); contains "Phase 38.9" | VERIFIED | grep finds `Phase 38.9 Plan 01 — BK-2 / BK-2kombi-SP Erste-Aufnahme-Gate close-side mirror` at line 1529; load-bearing predicate `if anstoss_at_goal && anstoss_innings >= 2` at line 1546; log tag `Phase 38.9 BK-2-immediate-close` at line 1547. Total addition = 25 lines (per `git diff cfee5962..HEAD --stat`). |
| `test/models/table_monitor_test.rb` | RED-then-GREEN test pair (1 RED for inning >= 2 close + 1 regression for inning 1 happy path) using canonical `build_bk_data` helper; contains "test_end_of_set?" pattern | VERIFIED | Both tests present at lines 189-214 and 216-235; `build_bk_data` used 8 times (5 baseline + 1 BK-2kombi + 2 Phase 38.9 — meets >= 6 requirement); Phase 38.9 header comment block present at lines 176-187. |

### Key Link Verification

| From | To  | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `app/models/table_monitor.rb#end_of_set?` (Phase 38.9 branch) | `app/models/table_monitor.rb#follow_up?` (lines 1205-1210) | Symmetric Erste-Aufnahme-Gate logic — `follow_up?` suppresses banner when Anstoss past inning 1; new `end_of_set?` branch closes the set in the same condition | WIRED | Both gates are present in the same file. `follow_up?` lines 1205-1210 verified UNCHANGED vs `cfee5962` (parent commit) — no diff in that block. New `end_of_set?` branch at lines 1529-1552 reuses the local `anstoss_innings` already computed inside the existing Plan 38.7-02 D-02 outer-`if` block at line 1518. Symmetry preserved at predicate level (`innings == 1` open, `innings >= 2` close). |
| `app/models/table_monitor.rb#end_of_set?` | `app/models/table_monitor/score_engine.rb#terminate_inning_data` | When `end_of_set?` returns true, `ResultRecorder` closes the set BEFORE `terminate_inning_data` runs the unconditional `active_player` swap | WIRED | `score_engine.rb` UNCHANGED vs parent commit (`git diff cfee5962..HEAD -- app/models/table_monitor/score_engine.rb` returns empty output). The 4th branch's `return true` short-circuits `end_of_set?`, satisfying the precondition that `ResultRecorder` reaches the close path before the active_player swap. |
| `app/models/table_monitor.rb#end_of_set?` | `test/system/tiebreak_test.rb` | Plan 38.7's tied-at-equal-innings tiebreak detection runs AFTER the new 4th branch in source order; tied case still flows through legacy karambol parity branch at lines 1555-1564 | WIRED | tiebreak_test.rb run reports 4/4 PASS. The new branch is gated on `bk_with_nachstoss && anstoss_at_goal` only — it cannot fire on the tied case where both players are at goal in equal innings (SUMMARY's edge-case walk-through and 38.9-REVIEW.md IN-03 confirm path coverage). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Phase 38.9 unit tests pass | `bin/rails test test/models/table_monitor_test.rb` | `21 runs, 43 assertions, 0 failures, 0 errors, 0 skips` | PASS |
| Phase 38.7 tiebreak seal preserved | `bin/rails test test/system/tiebreak_test.rb` | `4 runs, 32 assertions, 0 failures, 0 errors, 0 skips` | PASS |
| Phase 38.8 operator-gate seal preserved | `bin/rails test test/system/final_match_score_operator_gate_test.rb` | `4 runs, 19 assertions, 0 failures, 0 errors, 0 skips` | PASS |
| Load-bearing predicate present verbatim | `grep -n "anstoss_at_goal && anstoss_innings >= 2" app/models/table_monitor.rb` | hits line 1546 | PASS |
| Phase 38.9 inline comment block present | `grep -nE "Phase 38.9 Plan 01\|Erste-Aufnahme-Gate close-side mirror\|BK-2-immediate-close" app/models/table_monitor.rb` | hits lines 1529, 1547 | PASS |
| `bk_with_nachstoss` reused (no new helper) | `grep -c "bk_with_nachstoss" app/models/table_monitor.rb` | 2 (one declaration + one reuse) | PASS |
| Lint clean on inserted lines | `bundle exec standardrb app/models/table_monitor.rb` | no offenses on lines 1529-1552 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| PHASE-38.9-SC-1 | 38.9-01-end-of-set-fourth-branch-PLAN.md | BK-2 set with Anstoss-at-goal in inning >= 2 closes set IMMEDIATELY | SATISFIED | RED→GREEN test SC-1 passes at HEAD; production predicate `anstoss_at_goal && anstoss_innings >= 2` at line 1546 |
| PHASE-38.9-SC-2 | 38.9-01-end-of-set-fourth-branch-PLAN.md | BK-2 set with Anstoss-at-goal in inning 1 still triggers Nachstoss-Aufnahme + red banner (Erste-Aufnahme-Gate happy path preserved) | SATISFIED | Regression test SC-2 passes at HEAD; predicate excludes inning 1 by `>= 2` construction; `follow_up?` Erste-Aufnahme-Gate at lines 1205-1210 UNCHANGED |
| PHASE-38.9-SC-3 | 38.9-01-end-of-set-fourth-branch-PLAN.md | All existing BK-2 + general TableMonitor tests stay green | SATISFIED (with documented caveat) | 21/21 unit tests GREEN. 19 pre-existing system-test failures in bk2_scoreboard_test.rb confirmed pre-existing at parent `cfee5962` (Cluster A: stale `Bk2::CommitInning` refs from Phase 38.5; Cluster B: stale `BK2-Kombi` regexes from Phase 38.6) — documented in deferred-items.md, NOT regressions from Phase 38.9. |
| PHASE-38.9-SC-4 | 38.9-01-end-of-set-fourth-branch-PLAN.md | Phase 38.7 tiebreak path preserved — tied BK-2 set still opens the tiebreak modal via the existing legacy karambol parity branch | SATISFIED | tiebreak_test.rb 4/4 GREEN; new branch's `bk_with_nachstoss` outer-gate cannot fire on the tied-equal-innings tiebreak path; SUMMARY's edge-case walk-through traces all adjacent BK-2 scenarios |

**Note on REQUIREMENTS.md:** The PHASE-38.9-SC-* IDs are declared in the PLAN frontmatter `requirements` array and traced back to `38.9-CONTEXT.md` Success Criteria section. They are NOT registered in `.planning/REQUIREMENTS.md` because that document tracks the v7.1 milestone requirements (UX-POL-*, I18N-*, DATA-01) — Phase 38.9 is an INSERTED late-phase fix not part of the v7.1 milestone scope. No orphaned IDs detected.

### NOT-to-modify List Verification (CONTEXT.md preservation)

| Path | Lines | Status | Evidence |
| ---- | ----- | ------ | -------- |
| `app/models/table_monitor.rb` | 1188-1219 (`follow_up?` Erste-Aufnahme-Gate) | UNCHANGED | `git diff cfee5962..HEAD -- app/models/table_monitor.rb` shows ONLY additions in the 1529-1552 range; no edits to lines 1188-1219. The full `follow_up?` body (including the Erste-Aufnahme-Gate at 1205-1210) is byte-identical with the parent commit. |
| `app/models/table_monitor/score_engine.rb` | 1306 (`terminate_inning_data` active_player swap) | UNCHANGED | `git diff cfee5962..HEAD -- app/models/table_monitor/score_engine.rb` produces empty output. |
| `app/views/table_monitors/_player_score_panel.html.erb` | 125-127, 185-187 (Nachstoß banner conditional) | UNCHANGED | `git diff cfee5962..HEAD -- app/views/table_monitors/_player_score_panel.html.erb` produces empty output. |

All three NOT-to-modify constraints from `38.9-CONTEXT.md` are honored.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |

None. The change is a 25-LOC additive predicate branch with thorough comments, reuses already-computed locals, and follows the surrounding style (lint clean, no TODO/FIXME, no stub markers, no empty handlers, no console.log/debugger residue).

### Code Review Cross-Reference

The standard-depth `38.9-REVIEW.md` (2026-05-01T17:00:00Z) reports `0 critical, 0 warning, 3 info` findings. The three Info-level observations (IN-01 minor `anstoss_role` formula asymmetry inherited from D-02; IN-02 optional bk2_kombi multiset test; IN-03 TR-B baseline log-message-shift but contract preserved) are documented as future-consideration items. None block phase completion. Edge-case walk-through traces 9 BK-2 scenarios — all behavioral contracts preserved.

### Human Verification Required

3 items need human testing:

#### 1. BK-2 inning-2 anstoss-at-goal end-to-end flow

**Test:** Start a BK-2 quick-game (TR-Ctl preset). Player A inning 1: enter a non-goal score and switch. Player A inning 2: reach `balls_goal` exactly.
**Expected:** Game ends IMMEDIATELY (transitions to `set_over` / `final_match_score`). NO Player B inning is opened. NO red Nachstoß banner appears. The "Nächstes Spiel" button (Phase 38.8 operator-gate) appears.
**Why human:** End-to-end UAT against the live scoreboard view + AASM state machine + WebSocket broadcast — automated unit/system tests cover the predicate and the seal suites, but the visual confirmation that the red banner does NOT render and that the operator-gate button DOES render in this exact scenario requires human eyes.

#### 2. BK-2 inning-1 Nachstoss happy path

**Test:** Start a BK-2 quick-game. Player A reaches `balls_goal` in his FIRST inning.
**Expected:** Red "Nachstoß" banner appears. Set stays open. Player B gets exactly one Nachstoss-Aufnahme. After Player B's inning completes (with or without reaching the goal), the set closes via the Phase 38.7-02 D-02 branch.
**Why human:** Boundary-condition UAT for the Erste-Aufnahme-Gate happy path. SC-2 unit test pins `end_of_set? == false` at inning 1, but the human-visible banner rendering and operator flow through the Nachstoss-Aufnahme cannot be programmatically observed.

#### 3. BK-2 tied-at-goal tiebreak modal

**Test:** Start a BK-2 quick-game where both players end up at `balls_goal` in equal innings (tied set).
**Expected:** Tiebreak modal opens via the legacy karambol parity branch (Phase 38.7 path). Phase 38.9 4th branch must NOT short-circuit this — verified at unit level, but the modal opening + operator workflow needs a visual check.
**Why human:** Phase 38.7 tiebreak UI flow — system test covers backend, but UAT confirms the modal still opens and operator can complete the tiebreak.

### Gaps Summary

No gaps. All 6 must-have truths verified, all artifacts present and substantive, all key links wired, all NOT-to-modify constraints honored, all 4 success criteria (PHASE-38.9-SC-1..SC-4) satisfied, and all three regression-seal test suites pass with the documented counts (21/21 unit + 4/4 tiebreak + 4/4 operator-gate). The 19 pre-existing `bk2_scoreboard_test.rb` failures are NOT regressions from Phase 38.9 (verified at parent commit `cfee5962`) and are correctly documented in `deferred-items.md` for a future cleanup quick-task.

Status is **human_needed** because the BK-2 end-to-end UAT scenarios documented above require visual confirmation of the AASM state transition, banner suppression, and operator-gate button rendering against the live scoreboard view — none of which can be observed programmatically. The unit-level RED→GREEN trace and the system-level seal suites form a complete automated verification sandwich; UAT closes the goal-achievement loop.

---

_Verified: 2026-05-01T17:30:00Z_
_Verifier: Claude (gsd-verifier)_
