---
phase: 38.8-endergebnis-erfasst-state-restore-operator-gate-the-post-mat
plan: 04
subsystem: tournament-state-machine

tags: [aasm, tournament-monitor, table-monitor, result-processor, operator-gate, deferred-cascade, extend-before-build]

# Dependency graph
requires:
  - phase: 38.8
    provides: 38.8-01 RED characterization test (training-mode contract — locked GREEN by Plan 03); 38.8-02 :start_rematch AASM event (training operator-gated path); 38.8-03 deleted training auto-rematch block (training operator-gate restored)
  - phase: 38.7
    provides: tiebreak_not_pending? AASM guard on :acknowledge_result event — must remain verbatim; this plan only adds an after-callback to a sibling event (:close_match), never touches the upstream tiebreak guard
provides:
  - TournamentMonitor::ResultProcessor#advance_round_after_match_close — public deferred-cascade method (idempotent, gated by all_table_monitors_finished? + AASM may_? predicates)
  - TableMonitor#advance_tournament_round_if_present — bridge from AASM close_match event to ResultProcessor (training-mode no-op)
  - AASM event :close_match wired with `after: :advance_tournament_round_if_present` callback
  - report_result body free of REAL cascade call sites (populate_tables / incr_current_round! / finalize_round / TournamentMonitorUpdateResultsJob)
  - 5 new lock-in tests in result_processor_test.rb (training-mode no-op, public method, AASM event presence, source-static contract assertions)
affects: [38.8-05 view-and-reflex-wiring, 38.8-06 integration-system-test]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Extend-before-build SKILL applied: extracted existing cascade into a new public method + added an AASM after-callback (no parallel state machine, no new branching). Mirrors the training-mode pattern landed by Plans 02/03."
    - "Symmetric operator-gate pattern: training mode (admin_ack_result + finish_match! + Plan 02 :start_rematch) and tournament mode (admin_ack_result -> report_result writes data + finish_match! / cascade DEFERRED until operator-triggered close_match!) now share the same operator-visible state — :final_match_score (Endergebnis erfasst) — before any state advance"
    - "Idempotent deferred cascade: each branch internally gated by AASM may_? predicates + all_table_monitors_finished?, so re-running on an already-progressed round is a no-op (defense against double-click on close_match)"

key-files:
  created: []
  modified:
    - "app/services/tournament_monitor/result_processor.rb (+64 / -33 = +31 LOC net; cascade extracted from report_result lines 84-116 into new public method `advance_round_after_match_close(table_monitor)` immediately after report_result; explanatory comment block in report_result documents the deferral and the wiring path)"
    - "app/models/table_monitor.rb (+25 / -1 = +24 LOC net; AASM event :close_match gains `after: :advance_tournament_round_if_present`; new instance method `advance_tournament_round_if_present` inserted between `admin_ack_result` and `force_next_state` — no-ops when tournament_monitor.blank?, delegates to ResultProcessor.new(tournament_monitor).advance_round_after_match_close(self) otherwise)"
    - "test/services/tournament_monitor/result_processor_test.rb (+69 LOC; 5 new tests appended inside existing class — training-mode no-op assertion, public-method assertion, AASM event/method presence assertion, two source-static contract assertions)"

key-decisions:
  - "AASM event placement: existing event :close_match (table_monitor.rb:353-355 before this plan, now 358-365) had no callback before — surgical injection point per plan's <interfaces> guidance. transitions list (from %i[playing set_over final_match_score ready_for_new_match], to: :ready_for_new_match) UNCHANGED — only the after-callback was added"
  - "advance_tournament_round_if_present placed between admin_ack_result and force_next_state in table_monitor.rb (per plan's read_first guidance) — keeps the operator-control surface methods clustered together"
  - "Test 4 (source-static contract assertion) tightened from plan-prescribed bare-word regex `refute_match(/populate_tables/, ...)` to call-site-aware regex `refute_match(/@tournament_monitor\\.populate_tables/, ...)` — Rule 1 deviation (auto-fix plan-prescribed test bug). Reason: the new explanatory comment block in report_result intentionally mentions the deferred method names (\"populate_tables / incr_current_round! / finalize_round / etc.\") to document the deferral. The substantive contract (no REAL CALL SITE in report_result) is what matters; the documentary comment is intentional. Mirrors Plan 38.8-03's documented `comment-text grep noise accepted as DOCUMENTARY` key-decision pattern"
  - "Test 4 expanded from plan's 3 refute_match assertions to 4 — added refute_match(/TournamentMonitorUpdateResultsJob.perform_later/, body, ...) since the cascade also enqueues that job and report_result must NOT do so under the new contract. Strengthening the contract, not weakening it"
  - "Phase 38.7 tiebreak guard preserved verbatim — `tiebreak_not_pending?` grep count UNCHANGED (3 before, 3 after). This plan only adds an after-callback to a SIBLING event (:close_match), never touches the upstream :acknowledge_result event or its guard"
  - "Plan 02 :start_rematch event preserved verbatim — grep count UNCHANGED (1 before, 1 after). The two operator-paths (training :start_rematch from :final_match_score → :playing vs tournament :close_match from :final_match_score → :ready_for_new_match) are sibling AASM events, both branching from :final_match_score for different operator outcomes (training rematch vs tournament close+next-round)"

patterns-established:
  - "Pattern: defer-via-callback — when a side-effect cascade currently fires inline at moment X but should fire only at later operator-confirmed moment Y, extract the cascade into a new public method named for moment Y (`advance_round_after_match_close`) and wire it as an AASM after-callback on the operator-triggered event (`close_match`). Idempotency comes from the AASM may_? predicates inside each branch (which also protect the original inline call site, so this is a safe refactor). Bridge method on the model (`advance_tournament_round_if_present`) handles the training-mode no-op case via early-return on `tournament_monitor.blank?`"
  - "Pattern: source-static contract assertion — when locking a source-code-shape contract (e.g. \"method X must NOT mention pattern Y\") and the file legitimately contains pattern Y in explanatory comments, tighten the regex to assert the REAL call shape (e.g. `@tournament_monitor.populate_tables` not bare `populate_tables`) so the contract test doesn't false-fire on documentary mentions. Document the tightening in test comments + plan summary"

requirements-completed: [SC-2, SC-4, SC-5]

# Metrics
duration: ~5min
completed: 2026-05-01
---

# Phase 38.8 Plan 04: Defer Tournament Round Progression Summary

**Tournament-mode round-progression cascade extracted from `TournamentMonitor::ResultProcessor#report_result` into a new public method `advance_round_after_match_close(table_monitor)`, wired to `TableMonitor` AASM `:close_match` event via `after:` callback through new bridge method `advance_tournament_round_if_present`. After this plan: tournament TM enters `:final_match_score` ("Endergebnis erfasst"), STAYS there until operator triggers `close_match!` from Plan 05 reflex, then the cascade fires (populate_tables / incr_current_round! / finalize_round / start_playing_groups! / TournamentMonitorUpdateResultsJob / etc.). Symmetric to the training-mode operator-gate landed by Plans 02 + 03. Phase 38.7 tiebreak guard preserved verbatim; SC-2 + SC-4 + SC-5 met.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-01T08:40:04Z
- **Completed:** 2026-05-01T08:45:03Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments

- **Task 1:** Extracted cascade from `report_result` (lines 84-116 of pre-plan file) into new public method `advance_round_after_match_close(table_monitor)` immediately after `report_result`. Cascade extracted verbatim — populate_tables, incr_current_round!, finalize_round, group_phase_finished?/finals_finished? branches, end_of_tournament! / finish_tournament! / have_results_published!, start_playing_finals!, start_playing_groups!, TournamentMonitorUpdateResultsJob.perform_later, TournamentStatusUpdateJob.perform_later. report_result body now contains a 13-line explanatory comment block documenting the deferral and naming the wiring path. Each cascade method appears EXACTLY ONCE in the file (verified by grep counts: populate_tables 1, incr_current_round! 1, finalize_round 1, start_playing_groups! 1, start_playing_finals! 1, end_of_tournament! 1, TournamentMonitorUpdateResultsJob 1).
- **Task 2:** Modified AASM event `:close_match` to add `after: :advance_tournament_round_if_present` (transitions list `from: %i[playing set_over final_match_score ready_for_new_match], to: :ready_for_new_match` UNCHANGED). Added new instance method `advance_tournament_round_if_present` between `admin_ack_result` and `force_next_state` — early-returns when `tournament_monitor.blank?` (training-mode safety), delegates to `TournamentMonitor::ResultProcessor.new(tournament_monitor).advance_round_after_match_close(self)` in tournament mode. Errors logged + re-raised so AASM transition fails visibly.
- **Task 3:** Appended 5 new tests to existing `test/services/tournament_monitor/result_processor_test.rb` (was 19 tests, now 24): training-mode no-op assertion, public-method assertion on ResultProcessor, AASM event + method presence assertion on TableMonitor, source-static contract assertion that report_result body contains no REAL call sites for the cascade (4 refute_match), source-static contract assertion that advance_round_after_match_close body contains all 6 cascade calls.
- **Phase 38.7 tiebreak guard preserved verbatim** — grep count for `tiebreak_not_pending?` UNCHANGED at 3 (before and after).
- **Plan 02 :start_rematch event preserved verbatim** — grep count UNCHANGED at 1 occurrence.
- **All training-mode tests still GREEN** (24/24 result_recorder + 16/16 table_monitor) — Plans 02/03 deliverables fully preserved.
- **All Phase 38.7 tiebreak system tests still GREEN** (4/4, 32 assertions) — SC-5 preservation verified.

## Task Commits

Each task was committed atomically:

1. **Task 1: Extract round-progression cascade from report_result** — `6c3fc990` (refactor)
2. **Task 2: Wire AASM close_match -> advance_tournament_round_if_present** — `9e3203d9` (feat)
3. **Task 3: Lock deferred-cascade contract in result_processor_test** — `0e2f8ba3` (test)

## Files Created/Modified

- `app/services/tournament_monitor/result_processor.rb` (+64 / -33 = +31 LOC net) — `report_result` body trimmed (cascade replaced with 13-line explanatory comment block); new public method `advance_round_after_match_close(table_monitor)` added immediately after `report_result` with the cascade extracted verbatim. Source order: `report_result` → `advance_round_after_match_close` → `accumulate_results` (preserved). No changes to `accumulate_results`, `update_ranking`, `update_game_participations`, `write_game_result_data`, `finalize_game_result`, `update_game_participations_for_game`, `add_result_to`, `write_finale_csv_for_upload`. No visibility changes — new method is public, callable from TableMonitor.
- `app/models/table_monitor.rb` (+25 / -1 = +24 LOC net) — AASM event `:close_match` gains `after: :advance_tournament_round_if_present` callback (transitions list UNCHANGED); new instance method `advance_tournament_round_if_present` inserted between `admin_ack_result` and `force_next_state`. No changes to `admin_ack_result`, `force_next_state`, `set_game_over`, `tiebreak_pending_block?`, the `:acknowledge_result` AASM event (Phase 38.7 guard preserved), or the `:start_rematch` AASM event (Plan 02 preserved).
- `test/services/tournament_monitor/result_processor_test.rb` (+69 LOC) — 5 new tests appended inside existing class block. No changes to existing setup, teardown, or 19 baseline tests.

## Decisions Made

- **AASM event injection point: existing :close_match event was the surgical fit.** The plan's `<interfaces>` section explicitly identified this event as having no callback before, making it the cleanest injection point. The from-states list `[playing set_over final_match_score ready_for_new_match]` was preserved verbatim — only the `after:` callback was added. This is the extend-before-build SKILL applied: 1 new callback + 1 new bridge method instead of a new event or a new state.
- **Bridge method placement: between admin_ack_result and force_next_state.** The plan's `<read_first>` section pointed to admin_ack_result (line 1655) and force_next_state (line 1654-now-1685) as the operator-control surface area. Inserting `advance_tournament_round_if_present` between them keeps the operator-action methods clustered, matching Carambus's organizational convention (related methods adjacent in source).
- **Test 4 regex tightening — Rule 1 auto-fix (plan-prescribed test bug).** The plan literally specified `refute_match(/populate_tables/, body, ...)` which would FAIL because the new explanatory comment block in report_result intentionally mentions `populate_tables / incr_current_round! / finalize_round` to document the deferral. Tightened the regex to assert the REAL call shape `@tournament_monitor.populate_tables` (etc.). The substantive contract — that report_result must not actually CALL the cascade methods — is preserved and even strengthened (added a 4th assertion for `TournamentMonitorUpdateResultsJob.perform_later`). This mirrors Plan 38.8-03's documented `comment-text grep noise accepted as DOCUMENTARY` key-decision pattern. Without this fix Test 4 would have false-fired on every run, masking the real contract.
- **No new fixture chain for Test 1 (training-mode no-op).** The plan suggested creating a Game + Player + GameParticipation triple to test the no-op. Since the no-op is a simple early-return on `tournament_monitor.blank?`, a non-persisted `TableMonitor.new(state: "final_match_score", data: { "ba_results" => {} })` is sufficient. Tested at runtime that `TableMonitor.new(...).advance_tournament_round_if_present` returns `nil` without raising. Cheaper test, same coverage.
- **Test class extended in-place rather than as a new test file.** The plan said "If file already exists, append the same 5 test methods inside the existing class block (do NOT duplicate setup/teardown — adapt to existing fixture pattern)." The existing class uses `KoTournamentTestHelper` with full DB fixture setup (`@tm = @tournament.tournament_monitor`); the new tests don't need that fixture (Test 1 uses in-memory TM, Tests 2-5 are pure source-static or AASM-introspection assertions). They were appended to the existing class without using `@tm` or `@processor` — they don't interfere with the existing setup or teardown.
- **Phase 38.7 tiebreak guard preservation explicitly verified.** Ran `grep -c "tiebreak_not_pending?" app/models/table_monitor.rb` BEFORE the edits (returned 3) and AFTER the edits (returned 3). Defense against accidental search-and-replace damage to the upstream :acknowledge_result event.
- **Plan 02 :start_rematch event preservation explicitly verified.** Ran `grep -c "event :start_rematch" app/models/table_monitor.rb` BEFORE (1) and AFTER (1). The two operator-paths from `:final_match_score` (training :start_rematch → :playing for rematch; tournament :close_match → :ready_for_new_match for next round) are sibling events, both correctly preserved.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Plan-prescribed test bug] Test 4 regex tightened from bare-word to call-site-aware**

- **Found during:** Task 3 implementation (writing the source-static contract assertion)
- **Issue:** Plan's prescribed Test 4 used `refute_match(/populate_tables/, body, ...)` against the body of `report_result`. After Task 1's intentional 13-line explanatory comment block was added to report_result (mentioning "populate_tables / incr_current_round! / finalize_round" to document the deferral), this regex would false-fire. The contract that matters is "no REAL CALL SITE for the cascade in report_result", not "the literal word `populate_tables` does not appear".
- **Fix:** Tightened the regex to `refute_match(/@tournament_monitor\.populate_tables/, body, ...)` (and `@tournament_monitor.incr_current_round!`, `@tournament_monitor.finalize_round`). This asserts the substantive contract — no real call site — without false-firing on the documentary comment. Also expanded from 3 assertions to 4 by adding `refute_match(/TournamentMonitorUpdateResultsJob\.perform_later/, body, ...)` to also lock that the job is not enqueued from report_result.
- **Files modified:** test/services/tournament_monitor/result_processor_test.rb (in-test only, no source code changes from this fix)
- **Verification:** Test 4 GREEN; 24/24 result_processor_test.rb runs total; mirrors documented Plan 38.8-03 pattern.
- **Committed in:** `0e2f8ba3` (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — direct cascade of an intentional documentary comment added by this plan's own Task 1). The fix STRENGTHENED the test contract (4 assertions instead of 3) while making it accurate. No scope creep, no architectural change, no behavior change.

## Issues Encountered

- **Read-Before-Edit hook noise:** The pre-tool-use hook fired three times during edit operations on `result_processor.rb`, `table_monitor.rb`, and `result_processor_test.rb` despite all three files having been read in full earlier in the same session. Same hook noise pattern observed in Plans 38.8-02 and 38.8-03. Hook reminders did not block execution; all edits succeeded. Tracked as ongoing UX nit, not a defect in this plan's deliverables.
- **awk-based slicing variation in acceptance criterion:** The plan's AC `awk '/def report_result/,/^  end$/' ... | grep -c "populate_tables"` returns 1 (not 0) because the `populate_tables` mention appears inside the new explanatory comment block. The substantive AC (no REAL @tournament_monitor.populate_tables call site) was verified with `grep -c "@tournament_monitor.populate_tables"` against the same awk slice — returns 0. Same DOCUMENTARY pattern as Plan 03; documented in Decisions Made above.

## Verification Evidence

```
$ ruby -c app/services/tournament_monitor/result_processor.rb
Syntax OK

$ ruby -c app/models/table_monitor.rb
Syntax OK

$ grep -c "def advance_round_after_match_close" app/services/tournament_monitor/result_processor.rb
1

$ grep -c "Phase 38.8 — round-progression cascade DEFERRED" app/services/tournament_monitor/result_processor.rb
1

$ grep -c "Phase 38.8 — Deferred round-progression cascade" app/services/tournament_monitor/result_processor.rb
1

$ awk '/def report_result/,/^  end$/' app/services/tournament_monitor/result_processor.rb | grep -c "@tournament_monitor.populate_tables"
0    # zero REAL call sites in report_result body

$ awk '/def advance_round_after_match_close/,/^  end$/' app/services/tournament_monitor/result_processor.rb | grep -c "@tournament_monitor.populate_tables"
1    # exactly one REAL call site in advance_round_after_match_close

$ grep -c "@tournament_monitor.populate_tables" app/services/tournament_monitor/result_processor.rb
1    # cascade extracted (not duplicated) — exactly one occurrence file-wide

$ grep -c "@tournament_monitor.incr_current_round!" app/services/tournament_monitor/result_processor.rb
1

$ grep -c "@tournament_monitor.finalize_round" app/services/tournament_monitor/result_processor.rb
1

$ grep -c "@tournament_monitor.start_playing_groups!" app/services/tournament_monitor/result_processor.rb
1

$ grep -c "@tournament_monitor.start_playing_finals!" app/services/tournament_monitor/result_processor.rb
1

$ grep -c "@tournament_monitor.end_of_tournament!" app/services/tournament_monitor/result_processor.rb
1

$ grep -c "TournamentMonitorUpdateResultsJob.perform_later" app/services/tournament_monitor/result_processor.rb
1

$ bin/rails runner 'puts TournamentMonitor::ResultProcessor.instance_methods(false).include?(:advance_round_after_match_close)'
true

$ grep -c "event :close_match, after: :advance_tournament_round_if_present" app/models/table_monitor.rb
1

$ grep -c "def advance_tournament_round_if_present" app/models/table_monitor.rb
1

$ grep -A3 "def advance_tournament_round_if_present" app/models/table_monitor.rb | grep -c "return if tournament_monitor.blank?"
1

$ grep -A6 "def advance_tournament_round_if_present" app/models/table_monitor.rb | grep -c "TournamentMonitor::ResultProcessor.new(tournament_monitor).advance_round_after_match_close(self)"
1

$ grep -c "transitions from: %i\[playing set_over final_match_score ready_for_new_match\], to: :ready_for_new_match" app/models/table_monitor.rb
1   # transitions list preserved

$ grep -c "event :start_rematch" app/models/table_monitor.rb
1   # Plan 02 event preserved

$ grep -c "tiebreak_not_pending?" app/models/table_monitor.rb
3   # Phase 38.7 guard preserved (UNCHANGED from before this plan — was 3, still 3)

$ bin/rails runner 'tm = TableMonitor.new(state: "final_match_score"); puts tm.may_close_match?'
true

$ bin/rails runner 'puts TableMonitor.instance_methods(false).include?(:advance_tournament_round_if_present)'
true

$ bin/rails test test/services/tournament_monitor/result_processor_test.rb 2>&1 | tail -3
Finished in 1.820316s, 13.1845 runs/s, 41.7510 assertions/s.
24 runs, 76 assertions, 0 failures, 0 errors, 2 skips
# was 19 runs / 48 assertions baseline — +5 new tests, +28 new assertions, 0 regressions

$ bin/rails test test/services/table_monitor/result_recorder_test.rb test/models/table_monitor_test.rb 2>&1 | tail -3
Finished in 0.550644s, 72.6422 runs/s, 154.3647 assertions/s.
40 runs, 85 assertions, 0 failures, 0 errors, 0 skips
# 24 result_recorder + 16 table_monitor — Plans 02/03 + baseline preserved

$ bin/rails test test/system/tiebreak_test.rb 2>&1 | tail -3
Finished in 2.301775s, 1.7378 runs/s, 13.9023 assertions/s.
4 runs, 32 assertions, 0 failures, 0 errors, 0 skips
# Phase 38.7 SC-5 preserved
```

All Task 1 acceptance criteria met:

- [x] `grep -c "def advance_round_after_match_close" app/services/tournament_monitor/result_processor.rb` → 1
- [x] `grep -c "Phase 38.8 — round-progression cascade DEFERRED" app/services/tournament_monitor/result_processor.rb` → 1
- [x] `grep -c "Phase 38.8 — Deferred round-progression cascade" app/services/tournament_monitor/result_processor.rb` → 1
- [x] Cascade methods appear EXACTLY ONCE each in result_processor.rb (populate_tables, incr_current_round!, finalize_round, start_playing_groups!, start_playing_finals!, end_of_tournament!, TournamentMonitorUpdateResultsJob)
- [x] `awk '/def report_result/,/^  end$/' ... | grep -c "@tournament_monitor.populate_tables"` → 0 (zero REAL call sites; the 1 awk-grep `populate_tables` match is in the documentary comment block, accepted per Plan 38.8-03 pattern)
- [x] `ruby -c app/services/tournament_monitor/result_processor.rb` → Syntax OK
- [x] `bin/rails runner 'puts TournamentMonitor::ResultProcessor.instance_methods(false).include?(:advance_round_after_match_close)'` → true

All Task 2 acceptance criteria met:

- [x] `grep -c "event :close_match, after: :advance_tournament_round_if_present" app/models/table_monitor.rb` → 1
- [x] `grep -c "def advance_tournament_round_if_present" app/models/table_monitor.rb` → 1
- [x] training-mode no-op preserved (`return if tournament_monitor.blank?` present)
- [x] ResultProcessor delegation present
- [x] transitions list preserved verbatim
- [x] :start_rematch event preserved (count: 1)
- [x] `tiebreak_not_pending?` count UNCHANGED at 3 (Phase 38.7 guard preserved)
- [x] `ruby -c app/models/table_monitor.rb` → Syntax OK
- [x] `tm.may_close_match?` → true at runtime
- [x] `TableMonitor.instance_methods(false).include?(:advance_tournament_round_if_present)` → true at runtime

All Task 3 acceptance criteria met (with documented Test 4 regex tightening):

- [x] `test/services/tournament_monitor/result_processor_test.rb` exists
- [x] `grep -c "advance_tournament_round_if_present is a no-op" ...` → 1
- [x] `grep -c "report_result no longer mentions populate_tables" ...` → 1
- [x] `grep -c "advance_round_after_match_close method body contains all 6 cascade calls" ...` → 1
- [x] result_processor_test.rb: 24 runs, 0 failures, 0 errors, 2 skips (baseline pre-existing skips)
- [x] result_recorder_test.rb: 24 runs, 0 failures, 0 errors (Plans 02/03 GREEN preserved)
- [x] tiebreak_test.rb (system): 4 runs, 0 failures, 0 errors (Phase 38.7 SC-5 preserved)

## User Setup Required

None — code-only and test-only changes; no external service configuration, no DB schema change, no production-data migration.

## Next Phase Readiness

- **Plan 38.8-05 (view-and-reflex-wiring) is unblocked.** That plan adds the operator-facing "Weiter" / "Continue" button on the scoreboard view that fires `close_match!` via reflex. After Plan 05 lands, the operator-gated tournament round-progression contract becomes end-to-end testable from the UI: TM enters :final_match_score after report_result → operator sees "Endergebnis erfasst" → operator clicks "Weiter" → reflex fires `tm.close_match!` → AASM transitions to :ready_for_new_match → after-callback fires `advance_tournament_round_if_present` → ResultProcessor cascade runs → next round set up.
- **Plan 38.8-06 (integration-system-test) gets its end-to-end validation surface.** The unit-test contract locked by Task 3 is the structural lock-in; Plan 06 will provide the multi-TM tournament E2E test asserting the operator-gate visibly held under realistic conditions.
- **Phase 38.7 tiebreak suite GREEN.** Verified explicitly — 4/4 system tests + grep count for `tiebreak_not_pending?` UNCHANGED at 3.
- **Training-mode operator-gate (Plans 02 + 03) preserved.** Verified explicitly — 24/24 result_recorder_test.rb + 16/16 table_monitor_test.rb GREEN. The training-mode :start_rematch event is untouched; the new tournament-mode :close_match callback is a sibling event.
- **No blockers introduced.** Both training-mode (training :start_rematch flow from Plans 02/03) and tournament-mode (tournament :close_match flow from this plan) operator-gates are now in place at the model + service layer. Plans 05 (view) and 06 (integration test) can proceed in parallel.

## Self-Check: PASSED

- File modified at expected path: FOUND `app/services/tournament_monitor/result_processor.rb` (+31 LOC net)
- File modified at expected path: FOUND `app/models/table_monitor.rb` (+24 LOC net)
- File modified at expected path: FOUND `test/services/tournament_monitor/result_processor_test.rb` (+69 LOC)
- Commit hash: FOUND `6c3fc990` (refactor(38.8-04): extract round-progression cascade from report_result)
- Commit hash: FOUND `9e3203d9` (feat(38.8-04): wire AASM close_match -> advance_tournament_round_if_present)
- Commit hash: FOUND `0e2f8ba3` (test(38.8-04): lock deferred-cascade contract in result_processor_test)
- ResultProcessor#advance_round_after_match_close is a public method: VERIFIED via runner
- TableMonitor#advance_tournament_round_if_present is a public instance method: VERIFIED via runner
- AASM event :close_match with after-callback: VERIFIED (grep count 1)
- All Plan 38.8-03 tests still GREEN: VERIFIED (24/24 result_recorder_test.rb)
- All baseline TableMonitor model tests still GREEN: VERIFIED (16/16)
- All Phase 38.7 tiebreak system tests still GREEN: VERIFIED (4/4, 32 assertions)
- `tiebreak_not_pending?` grep count UNCHANGED at 3 (Phase 38.7 guard preserved): VERIFIED
- Plan 02 :start_rematch event preserved (grep count 1): VERIFIED
- 5 new tests GREEN: VERIFIED (24 runs total — was 19; +5)

---
*Phase: 38.8-endergebnis-erfasst-state-restore-operator-gate-the-post-mat*
*Completed: 2026-05-01*
