---
phase: 38.8
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - app/models/table_monitor.rb
  - config/locales/de.yml
  - config/locales/en.yml
autonomous: true
requirements: [SC-1, SC-3]
must_haves:
  truths:
    - "TableMonitor AASM has a `start_rematch` event with transition `final_match_score → playing`"
    - "The transition runs `revert_players` and `do_play` as `after` callbacks (mirroring the deleted auto-rematch sequence)"
    - "Calling `start_rematch!` on a TM in any state other than `:final_match_score` raises AASM::InvalidTransition"
    - "i18n keys `table_monitor.next_game` exist in DE (`Nächstes Spiel`) and EN (`Next Game`)"
    - "Existing AASM events (acknowledge_result, finish_match, close_match, next_set) remain UNCHANGED"
    - "Phase 38.7 tiebreak guard `tiebreak_not_pending?` on `acknowledge_result` is preserved verbatim"
  artifacts:
    - path: "app/models/table_monitor.rb"
      provides: "AASM event :start_rematch with transition final_match_score → playing"
      contains: "event :start_rematch"
    - path: "config/locales/de.yml"
      provides: "DE i18n key for the rematch button"
      contains: "next_game: Nächstes Spiel"
    - path: "config/locales/en.yml"
      provides: "EN i18n key for the rematch button"
      contains: "next_game: Next Game"
  key_links:
    - from: "app/models/table_monitor.rb"
      to: "revert_players, do_play"
      via: "AASM after callbacks"
      pattern: "after: \\[:revert_players, :do_play\\]|after: %i\\[revert_players do_play\\]"
threat_model_summary: |
  Adds a new AASM event with strict from-state guard (only `:final_match_score`).
  Reduces attack surface vs auto-progression — the transition is operator-gated,
  cannot be silently triggered by background jobs, and AASM enforces invalid
  transitions.
out_of_scope: |
  DOES NOT modify BK-2 Nachstoß flow (handled in phase 38.9).
  DOES NOT modify tiebreak modal flow (handled by phase 38.7 — must remain green).
  DOES NOT delete the auto-rematch block in ResultRecorder (Plan 03 owns that).
  DOES NOT defer the tournament round-progression cascade (Plan 04 owns that).
  DOES NOT wire view buttons or reflex handlers (Plan 05 owns that).
---

<objective>
Add the AASM event `start_rematch` to `TableMonitor` so that — once Plan 03 deletes
the auto-rematch block — the operator has an explicit, testable, AASM-enforced way
to leave `final_match_score` and start a new training game with swapped players.

Also add the i18n button-label keys (`table_monitor.next_game` DE+EN) that Plan 05
will consume when wiring the scoreboard button. Adding the keys here keeps the
locale-file edits in a single plan.

Purpose: extend the existing AASM state machine with one new event (per
`extend-before-build` SKILL — small guard, not a parallel state machine).
Output: 1 new AASM event block in `table_monitor.rb`, 1 new key in `de.yml`,
1 new key in `en.yml`.

After this plan lands, the Plan 01 RED test STILL fails (because the auto-rematch
block in ResultRecorder is still firing — only Plan 03 removes it). That is correct:
this plan only ships the new event; Plan 03 ships the deletion that turns the test GREEN.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38.8-endergebnis-erfasst-state-restore-operator-gate-the-post-mat/38.8-CONTEXT.md
@.planning/debug/endergebnis-erfasst-state-skip.md
@.agents/skills/extend-before-build/SKILL.md

@app/models/table_monitor.rb
@config/locales/de.yml
@config/locales/en.yml
</context>

<interfaces>
<!-- Existing AASM block in app/models/table_monitor.rb (lines 333-401):
     States: new (initial), ready, warmup, warmup_a, warmup_b, match_shootout,
             playing, set_over, final_set_score, final_match_score, ready_for_new_match
     Events:  start_new_match, close_match, warmup_a, warmup_b, finish_warmup,
              finish_shootout, end_of_set, undo, acknowledge_result (with tiebreak guard),
              finish_match (final_set_score → final_match_score), next_set, ready, force_ready

     Existing methods to be reused as `after:` callbacks:
       - revert_players (line 1389) — swaps player A ↔ B in @tm.data
       - do_play (line 574) — re-arms the play timer and sets state for active player
     These are the SAME two methods that the buggy auto-rematch block called inline.

     i18n labels in de.yml:587-603 — `table_monitor.status.*` keys exist for every
     AASM state including `final_match_score: "Endergebnis erfasst"` (line 589).
     Sibling structure in en.yml:553-570.

     The button label belongs at the same nesting depth as `status:` — directly
     under `table_monitor:` — using a fresh key `next_game:` so it's discoverable
     by Plan 05 as `t("table_monitor.next_game")`.
-->

AASM block currently ends at table_monitor.rb:401 with:
```ruby
    event :force_ready do
      transitions to: :ready
    end
  end
```

The new `start_rematch` event must be added INSIDE this `aasm column: "state"` block,
positioned AFTER `event :finish_match` (line 389-391) and BEFORE `event :next_set`
(line 392-394), so the read order matches the runtime flow:
acknowledge_result → finish_match → start_rematch.

i18n insertion points:
- de.yml: under top-level `table_monitor:` map (~line 580). Add `next_game: Nächstes Spiel` as a sibling of the existing `status:` map (NOT inside `status:` — that map is reserved for AASM state-display labels).
- en.yml: parallel insertion under `table_monitor:` map (~line 540). Add `next_game: Next Game`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add :start_rematch AASM event to TableMonitor</name>
  <files>app/models/table_monitor.rb</files>
  <read_first>
    - app/models/table_monitor.rb (lines 333-401 — full AASM block; understand existing event syntax for finish_match and acknowledge_result-with-guard)
    - app/models/table_monitor.rb (lines 574-600 — `do_play` method signature)
    - app/models/table_monitor.rb (lines 1389-1430 — `revert_players` method signature)
    - .agents/skills/extend-before-build/SKILL.md (heuristic — small guard on existing path, NOT a parallel state machine)
  </read_first>
  <action>
In `app/models/table_monitor.rb`, inside the `aasm column: "state", whiny_transitions: true do ... end` block (lines 333-401), insert the following new event AFTER the existing `event :finish_match` block (currently lines 389-391, ends with `end`) and BEFORE `event :next_set` (line 392).

Insert verbatim:

```ruby
    # Phase 38.8 — operator-gated rematch event. Replaces the auto-rematch
    # bypass that ResultRecorder#perform_evaluate_result used to perform via
    # `update(state: "playing")` (regression introduced in c3dedb69 2026-03-24,
    # deleted in Plan 38.8-03). The operator now sees the `final_match_score`
    # state ("Endergebnis erfasst" — de.yml:589) until they explicitly trigger
    # `start_rematch!` via the "Nächstes Spiel" button (Plan 38.8-05).
    #
    # Callback chain mirrors the deleted inline sequence:
    #   revert_players (swaps player A ↔ B)
    #   do_play         (re-arms the play timer / panel state)
    # Both methods are defined further down in this file (lines 1389 and 574).
    event :start_rematch do
      transitions from: :final_match_score, to: :playing,
                  after: [:revert_players, :do_play]
    end
```

Do NOT modify ANY other event. Do NOT modify the existing `acknowledge_result` event (line 377-388) — its `tiebreak_not_pending?` guard from phase 38.7 must remain verbatim. Do NOT modify the `finish_match` event (lines 389-391). Do NOT modify the `close_match` event (lines 353-355).
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw && bin/rails runner 'tm = TableMonitor.new(state: "final_match_score"); puts "may_start_rematch?=#{tm.may_start_rematch?}"; tm2 = TableMonitor.new(state: "playing"); puts "may_start_rematch_from_playing?=#{tm2.may_start_rematch?}"' 2>&amp;1 | tee /tmp/aasm-38.8-02.log; grep -q "may_start_rematch?=true" /tmp/aasm-38.8-02.log &amp;&amp; grep -q "may_start_rematch_from_playing?=false" /tmp/aasm-38.8-02.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "event :start_rematch" app/models/table_monitor.rb` returns `1`
    - `grep -A2 "event :start_rematch" app/models/table_monitor.rb | grep -E "transitions from: :final_match_score, to: :playing"` returns 1 match
    - `grep -A2 "event :start_rematch" app/models/table_monitor.rb | grep -E "after: \[:revert_players, :do_play\]"` returns 1 match
    - `bin/rails runner 'puts TableMonitor.aasm.events.map(&:name).sort.inspect'` includes `:start_rematch` in the output
    - `bin/rails runner 'tm = TableMonitor.new(state: "final_match_score"); puts tm.may_start_rematch?'` prints `true`
    - `bin/rails runner 'tm = TableMonitor.new(state: "playing"); puts tm.may_start_rematch?'` prints `false` (state guard works — invalid transition blocked)
    - `grep -c "tiebreak_not_pending?" app/models/table_monitor.rb` is unchanged (Plan 38.7 guard preserved — verify before/after with the same grep count)
    - `grep -E "event :acknowledge_result|event :finish_match|event :close_match" app/models/table_monitor.rb | wc -l` returns `3` (existing events untouched)
  </acceptance_criteria>
  <done>
    `TableMonitor.aasm.events.map(&:name)` includes `:start_rematch`. Calling `tm.start_rematch!` on a TM in `:final_match_score` transitions to `:playing` and runs `revert_players` then `do_play`. Calling on any other state raises AASM::InvalidTransition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add DE + EN i18n keys for the "Nächstes Spiel" button</name>
  <files>config/locales/de.yml, config/locales/en.yml</files>
  <read_first>
    - config/locales/de.yml (lines 580-605 — see the `table_monitor:` map with `name`, `state`, `status:` sub-map, etc.)
    - config/locales/en.yml (lines 540-570 — parallel `table_monitor:` map structure)
  </read_first>
  <action>
**Edit 1 — config/locales/de.yml.** Locate the `table_monitor:` top-level map (around line 580). Inside this map, add a new sibling key `next_game:` immediately after the `name:` key (around line 581). The key must be a direct child of `table_monitor:`, NOT nested under `status:` (the `status:` sub-map is reserved for AASM state-display labels).

Insert (matching the file's existing 4-space indent under `table_monitor:`):

```yaml
    next_game: Nächstes Spiel
```

**Edit 2 — config/locales/en.yml.** Locate the `table_monitor:` top-level map (around line 540). Add the parallel sibling key `next_game:` at the same indent level as `name:` and `status:`:

```yaml
    next_game: Next Game
```

Do NOT modify the existing `status:` sub-map. Do NOT modify the existing `final_match_score: Endergebnis erfasst` (de.yml:589) or `final_match_score: Results reported` (en.yml:556) entries — those are the state-display labels and must remain verbatim. Do NOT change YAML indentation of surrounding lines.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw &amp;&amp; ruby -e "require 'yaml'; de = YAML.load_file('config/locales/de.yml'); en = YAML.load_file('config/locales/en.yml'); puts 'de.next_game=' + de['de']['table_monitor']['next_game'].to_s; puts 'en.next_game=' + en['en']['table_monitor']['next_game'].to_s" 2>&amp;1 | tee /tmp/i18n-38.8-02.log; grep -q "de.next_game=Nächstes Spiel" /tmp/i18n-38.8-02.log &amp;&amp; grep -q "en.next_game=Next Game" /tmp/i18n-38.8-02.log</automated>
  </verify>
  <acceptance_criteria>
    - `ruby -e "require 'yaml'; puts YAML.load_file('config/locales/de.yml').dig('de','table_monitor','next_game')"` prints `Nächstes Spiel`
    - `ruby -e "require 'yaml'; puts YAML.load_file('config/locales/en.yml').dig('en','table_monitor','next_game')"` prints `Next Game`
    - `grep -c "Nächstes Spiel" config/locales/de.yml` returns at least `1`
    - `grep -c "next_game: Next Game" config/locales/en.yml` returns `1`
    - `grep -c "final_match_score: Endergebnis erfasst" config/locales/de.yml` returns `1` (unchanged — state-display label preserved)
    - `grep -c "final_match_score: Results reported" config/locales/en.yml` returns `1` (unchanged)
    - YAML still parses: both `ruby -ryaml -e "YAML.load_file('config/locales/de.yml')"` and `ruby -ryaml -e "YAML.load_file('config/locales/en.yml')"` exit 0
  </acceptance_criteria>
  <done>
    Both locale files have `table_monitor.next_game` keys. DE = "Nächstes Spiel". EN = "Next Game". YAML structure intact. State-display labels unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → AASM event | Operator click triggers reflex which fires `start_rematch!`. AASM enforces from-state guard. |
| Background job → AASM | No background job calls `start_rematch!`. Event is reachable only from operator-triggered code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.8-02-01 | Tampering | start_rematch event | mitigate | AASM `transitions from: :final_match_score` guard rejects calls from any other state with AASM::InvalidTransition. Eliminates the auto-rematch silent bypass that Plan 03 deletes. |
| T-38.8-02-02 | Elevation of Privilege | reflex caller | mitigate | Plan 05 wires the button via existing TableMonitorReflex which inherits ApplicationReflex CSRF + connection_token authorization (existing pattern, e.g. admin_ack_result line 803). No new authorization surface. |
| T-38.8-02-03 | Denial of Service | revert_players + do_play callbacks | accept | These methods already run today inline via the buggy auto-rematch. Same compute cost, same DB writes. No new DoS vector. |
| T-38.8-02-04 | Information Disclosure | i18n keys | accept | Static UI strings. No PII, no secrets. |
</threat_model>

<verification>
- Run: `bin/rails runner 'puts TableMonitor.aasm.events.map(&:name).sort.include?(:start_rematch)'` → `true`
- Run: `bin/rails runner 'tm = TableMonitor.new(state: "final_match_score"); puts tm.may_start_rematch?'` → `true`
- Run: `bin/rails runner 'tm = TableMonitor.new(state: "playing"); puts tm.may_start_rematch?'` → `false`
- Run: `bin/rails test test/services/table_monitor/result_recorder_test.rb` — Plan 01 RED test still fails (expected, Plan 03 will turn it GREEN); the other 8 existing tests still pass.
- Run: `bin/rails test test/models/table_monitor_test.rb` — existing 6 tests still pass (no AASM regression).
</verification>

<success_criteria>
- AASM `:start_rematch` event present and from-state-guarded to `:final_match_score`
- DE locale has `table_monitor.next_game = "Nächstes Spiel"`
- EN locale has `table_monitor.next_game = "Next Game"`
- All existing AASM events unchanged (acknowledge_result tiebreak guard preserved)
- All existing locale state-display labels unchanged
- YAML files still parse
</success_criteria>

<output>
After completion, create `.planning/phases/38.8-endergebnis-erfasst-state-restore-operator-gate-the-post-mat/38.8-02-SUMMARY.md`
</output>
