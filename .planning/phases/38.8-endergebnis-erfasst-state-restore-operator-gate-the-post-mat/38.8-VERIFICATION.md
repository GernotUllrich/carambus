---
phase: 38.8-endergebnis-erfasst-state-restore-operator-gate-the-post-mat
verified: 2026-05-01T12:30:00Z
re_verified: 2026-05-01T15:00:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
post_review_corrections:
  - finding: CR-02
    action: reverted
    note: "User manually validated BK-2 round_play 2026-05-01 — cascade fires exactly once per click; sentinel was dead code, reverted in 0dbe45c4 + 61e9e8c8."
human_verification:
  - test: "Verify operator-visible UX end-to-end via a Capybara/browser session on the BCW dev server: complete a training match across all targeted disciplines (karambol, BK-2 family, and any Pool/Snooker fixture available) and confirm the green Nächstes Spiel button appears, click triggers a clean rematch."
    expected: "After the final inning is recorded, the scoreboard renders the green button labeled Nächstes Spiel (DE) / Next Game (EN). The state_display label reads Endergebnis erfasst. Clicking the button transitions the TM to :playing with players reverted; the next match starts cleanly. Same flow holds across the tested disciplines. No console errors, no broadcast errors, no cable subscription drops."
    why_human: "Visual verification of UI affordance, label correctness, button styling, and reflex round-trip cannot be asserted by service-level dispatch tests (Phase 38.5 D-06 / Phase 38.7 Plan 08 D-08 pattern). The system test in test/system/final_match_score_operator_gate_test.rb dispatches the reflex via .allocate stubs — it never renders the partial in a browser. WR-04 explicitly narrowed SC-2 scope to 'training-mode no-op cascade' so no automated test exercises the full cascade in a real DOM session. Plan 05 SUMMARY also lists this as a Next Phase Readiness item."
human_verification_resolved:
  - test: "Drive a full tournament round-progression cycle on a real fixture and confirm the cascade fires exactly once per operator click."
    resolved_at: 2026-05-01
    resolution: "User manually exercised BK-2 round_play scenario end-to-end. Cascade observed firing exactly once per operator click. CR-02 reclassified as false positive; sentinel reverted (0dbe45c4 + 61e9e8c8)."
---

# Phase 38.8: Endergebnis-erfasst State Restore Verification Report

**Phase Goal:** Restore the AASM `final_match_score` state ("Endergebnis erfasst") as the operator-gated end-state across ALL disciplines and BOTH modes. Training mode lands in `:final_match_score` and waits for "Nächstes Spiel"; tournament mode lands in `:final_match_score`, defers round-progression until operator triggers `close_match!`. Cross-discipline regression test locks the contract. Phase 38.7 tiebreak modal flow remains green.
**Verified:** 2026-05-01T12:30:00Z
**Re-verified:** 2026-05-01T15:00:00Z (after CR-02 false-positive revert)
**Status:** human_needed

## Goal Achievement

### Observable Truths

| # | Truth (= Success Criterion) | Status | Evidence |
|---|----|----|----|
| SC-1 | Training operator-gate: training-mode single-set games land in `:final_match_score` after evaluate_result and stay there until operator clicks Nächstes Spiel | VERIFIED | `app/services/table_monitor/result_recorder.rb:473, 487` show `@tm.finish_match! if @tm.may_finish_match?` replacing the deleted auto-rematch blocks. `update(state: "playing")` AASM bypass only appears in a documentary comment (line 463). Plan 01 RED test now GREEN; test/services/table_monitor/result_recorder_test.rb 24 tests / 0 failures. test/system/final_match_score_operator_gate_test.rb SC-1 test ("training match completion lands in final_match_score and waits for operator click") passes. |
| SC-2 | Tournament operator-gate: round-progression cascade deferred from `report_result` to `advance_round_after_match_close` and fires only when operator-triggered `close_match!` runs the AASM after-callback | VERIFIED | `app/services/tournament_monitor/result_processor.rb:122` defines `advance_round_after_match_close(table_monitor)` containing the cascade (populate_tables, incr_current_round!, finalize_round, start_playing_groups!, TournamentMonitorUpdateResultsJob). `app/models/table_monitor.rb:358` wires `event :close_match, after: :advance_tournament_round_if_present`. Bridge method at line 1691 has the CR-01 PartyMonitor polymorphism guard. result_processor_test.rb has 24 tests / 0 failures locking the contract via static-source assertion. **Empirical:** user manually validated BK-2 round_play 2026-05-01 — cascade fires exactly once per click. |
| SC-3 | final_match_score view + button: scoreboard renders Nächstes Spiel / Next Game button visible only when `table_monitor.final_match_score?`; click fires correct AASM event for training vs tournament mode | VERIFIED | `app/views/table_monitors/_scoreboard.html.erb:159-179` has `<%- elsif table_monitor.final_match_score? %>` branch with single content_tag (post WR-03 dedupe) using `reflex_action = table_monitor.tournament_monitor.blank? ? "start_rematch" : "close_match"`. i18n keys present: `de.yml:582 next_game: Nächstes Spiel`, `en.yml:549 next_game: Next Game`. Reflex methods present: `app/reflexes/table_monitor_reflex.rb:833 def start_rematch`, `:854 def close_match`. Both reflex methods follow the locked_scoreboard + suppress_broadcast + save! pattern. Runtime spot-check: I18n.t resolves "Nächstes Spiel" and "Endergebnis erfasst" correctly. |
| SC-4 | Cross-discipline regression test locks the contract: at minimum karambol and BK-2 single-set training games all land in `:final_match_score` | VERIFIED | test/system/final_match_score_operator_gate_test.rb test "cross-discipline single-set training games all land in final_match_score" loops over `[karambol/Freie Partie klein/standard, bk_2/BK-2/bk_2]` and asserts `:final_match_score` landing for each. build_training_tm helper coerces TM to `:set_over` via update_columns + reload (mirrors result_recorder_test.rb:433/:468) so each discipline reaches Branch C deterministically. 4/4 tests in this file GREEN. Plan 06 SUMMARY documents Pool/Snooker scoped out due to fixture absence (acceptable per plan's permission for `skip` annotation). |
| SC-5 | Phase 38.7 tiebreak preserved: 4 system tests in test/system/tiebreak_test.rb still GREEN; tiebreak_not_pending? AASM guard preserved verbatim | VERIFIED | `bin/rails test test/system/tiebreak_test.rb` returns 4 runs, 32 assertions, 0 failures, 0 errors, 0 skips. `grep -c "tiebreak_not_pending?" app/models/table_monitor.rb` returns 3 (matching pre-phase baseline per all 6 plan summaries). Seal test in final_match_score_operator_gate_test.rb verifies the file still exists and contains ≥4 tests (Plan 06 SC-5). |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `app/models/table_monitor.rb` | `event :start_rematch` (final_match_score → playing, after: revert_players + do_play); `event :close_match` with `after: :advance_tournament_round_if_present`; `def advance_tournament_round_if_present` with PartyMonitor polymorphism guard | VERIFIED | Line 408: `event :start_rematch do` block. Line 358: `event :close_match, after: :advance_tournament_round_if_present do`. Line 1691: bridge method with `is_a?(TournamentMonitor)` guard (CR-01 fix). Phase 38.7 `tiebreak_not_pending?` count = 3 (preserved). |
| `app/services/table_monitor/result_recorder.rb` | Both auto-rematch blocks deleted (Branch C 462-476, final_set_score 485-497); replaced with `@tm.finish_match! if @tm.may_finish_match?` | VERIFIED | Lines 473 + 487 contain the replacement. Lines 462 + 482 contain explanatory "Phase 38.8 — operator-gate restored" comments. Line 463 retains documentary mention of `update(state: "playing")` (regression-naming comment, accepted as documentary per Plan 03 SUMMARY). No remaining real call sites of the AASM bypass. |
| `app/services/tournament_monitor/result_processor.rb` | `def advance_round_after_match_close(table_monitor)` containing the cascade extracted verbatim from report_result; report_result body free of real cascade call sites | VERIFIED | Line 122 method signature; lines 128 (incr_current_round!), 129 (populate_tables) inside the new method. Static-source contract assertion test in result_processor_test.rb passes (refute_match against `@tournament_monitor.populate_tables` etc. in report_result body). |
| `app/reflexes/table_monitor_reflex.rb` | `def start_rematch` and `def close_match` reflex methods following admin_ack_result + force_next_state pattern | VERIFIED | Lines 833 (start_rematch) and 854 (close_match). Each method 11 LOC body: morph :nothing → find TM → locked_scoreboard guard → suppress_broadcast wrap → save!. AASM `may_?` predicate gates the event call. |
| `app/views/table_monitors/_scoreboard.html.erb` | `<%- elsif table_monitor.final_match_score? %>` branch rendering Nächstes Spiel button with mode-conditional reflex routing | VERIFIED | Lines 159-179. Post WR-03 dedupe: single content_tag with `reflex_action` local computed from `tournament_monitor.blank?`. Includes touchstart pairing, tabindex 1, green-text styling, t("table_monitor.next_game") label. |
| `config/locales/de.yml` | `table_monitor.next_game: Nächstes Spiel` | VERIFIED | Line 582. State label `final_match_score: Endergebnis erfasst` preserved at line 590. |
| `config/locales/en.yml` | `table_monitor.next_game: Next Game` | VERIFIED | Line 549. State label `final_match_score: Results reported` preserved at line 557. |
| `test/services/table_monitor/result_recorder_test.rb` | RED characterization test (Plan 01) now GREEN; T6 contract realigned to operator-gated semantics | VERIFIED | 24 tests / 0 failures. Plan 01 test at line 628 ("evaluate_result for training single-set no-tiebreak game lands in final_match_score (NOT playing)") passes. T6 inversion landed in commit cd6bcf7c. |
| `test/services/tournament_monitor/result_processor_test.rb` | Static-source contract assertions for `advance_round_after_match_close` extraction | VERIFIED | 24 tests / 0 failures. Tests assert advance_round_after_match_close exists publicly, report_result body free of real cascade call sites, advance method body contains all 6 cascade calls. (Note: 2 synthetic CR-02 sentinel tests reverted with the false-positive sentinel.) |
| `test/services/party_monitor/result_processor_test.rb` | CR-01 regression tests asserting close_match! does not crash with PartyMonitor polymorphic association | VERIFIED | 14 tests / 0 failures. Two tests added by CR-fix commit f2e438d2: "TableMonitor#advance_tournament_round_if_present is a no-op when polymorphic tournament_monitor is a PartyMonitor (CR-01)" and "PartyMonitor close_match! cascade does not crash via Phase 38.8 after-callback (CR-01)". |
| `test/models/table_monitor_test.rb` | 3 new AASM unit tests for :start_rematch event | VERIFIED | 19 tests / 0 failures (baseline 16 + 3 new). Tests cover positive transition + after-callback execution, from-state guard rejection, may_start_rematch? state-by-state guard. |
| `test/system/final_match_score_operator_gate_test.rb` | E2E system test covering SC-1 (training), SC-2 (training-mode AASM transition post WR-04 narrowing), SC-4 (cross-discipline), SC-5 (tiebreak file seal) | VERIFIED | 4 tests / 0 failures, 18 assertions. Service-level dispatch pattern (ActiveSupport::TestCase + reflex via .allocate). build_training_tm helper coerces to :set_over. SC-2 test renamed by WR-04 fix to clarify training-mode-only scope. SC-5 seal test asserts tiebreak_test.rb file integrity + ≥4 test definitions. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `_scoreboard.html.erb` | `TableMonitorReflex#start_rematch` / `#close_match` | `data: { reflex: "click->TableMonitorReflex#${reflex_action} touchstart->..." }` | WIRED | Line 173. reflex_action local computed at line 171 from `tournament_monitor.blank?`. Both reflex methods exist in target class. |
| `TableMonitorReflex#start_rematch` | `TableMonitor` AASM `:start_rematch` event | `@table_monitor.start_rematch! if @table_monitor.may_start_rematch?` | WIRED | reflex line ~840. AASM event registered. |
| `TableMonitorReflex#close_match` | `TableMonitor` AASM `:close_match` event | `@table_monitor.close_match! if @table_monitor.may_close_match?` | WIRED | reflex line ~860. AASM event has after-callback wired (line 358). |
| AASM `:close_match` event | `TableMonitor#advance_tournament_round_if_present` | `after: :advance_tournament_round_if_present` | WIRED | Line 358 of table_monitor.rb. Bridge method at line 1691. |
| `TableMonitor#advance_tournament_round_if_present` | `TournamentMonitor::ResultProcessor#advance_round_after_match_close` | `TournamentMonitor::ResultProcessor.new(tournament_monitor).advance_round_after_match_close(self)` | WIRED (with CR-01 polymorphism guard) | Line 1695. Guarded by `is_a?(TournamentMonitor)` check (line 1693). |
| AASM `:start_rematch` event | `revert_players` + `do_play` callbacks | `after: [:revert_players, :do_play]` | WIRED | Line 408-411 of table_monitor.rb. Both methods exist. Order verified by Plan 06 Test B: state transitions to :playing AND both spy variables set. |
| `ResultRecorder#perform_evaluate_result` (Branch C / final_set_score) | `TableMonitor` AASM `:finish_match` event | `@tm.finish_match! if @tm.may_finish_match?` | WIRED | Lines 473 and 487 of result_recorder.rb. Mirrors admin_ack_result tournament path (table_monitor.rb:1669). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| start_rematch AASM event registered | `bin/rails runner 'puts TableMonitor.aasm.events.map(&:name).include?(:start_rematch)'` | true | PASS |
| advance_tournament_round_if_present is a public instance method | `bin/rails runner 'puts TableMonitor.instance_methods(false).include?(:advance_tournament_round_if_present)'` | true | PASS |
| advance_round_after_match_close is a public instance method on ResultProcessor | `bin/rails runner 'puts TournamentMonitor::ResultProcessor.instance_methods(false).include?(:advance_round_after_match_close)'` | true | PASS |
| Reflex method start_rematch present | `bin/rails runner 'puts TableMonitorReflex.instance_methods(false).include?(:start_rematch)'` | true | PASS |
| Reflex method close_match present | `bin/rails runner 'puts TableMonitorReflex.instance_methods(false).include?(:close_match)'` | true | PASS |
| DE i18n key resolves | `I18n.with_locale(:de) { I18n.t("table_monitor.next_game") }` | "Nächstes Spiel" | PASS |
| DE state-display label resolves | `I18n.with_locale(:de) { I18n.t("table_monitor.status.final_match_score") }` | "Endergebnis erfasst" | PASS |
| AASM state guard from final_match_score | `TableMonitor.new(state: "final_match_score").may_start_rematch?` | true | PASS |
| AASM state guard from playing (rejection) | `TableMonitor.new(state: "playing").may_start_rematch?` | false | PASS |
| AASM may_close_match? from final_match_score | `TableMonitor.new(state: "final_match_score").may_close_match?` | true | PASS |
| Combined Phase 38.8 regression sweep (post CR-02 revert) | `bin/rails test test/models/table_monitor_test.rb test/services/table_monitor/result_recorder_test.rb test/services/tournament_monitor/result_processor_test.rb test/services/party_monitor/result_processor_test.rb test/system/final_match_score_operator_gate_test.rb test/system/tiebreak_test.rb` | 89 runs, 254 assertions, 0 failures, 0 errors, 0 skips | PASS |

### Requirements Coverage

The phase declares its own internal Success Criteria (SC-1..SC-5) in ROADMAP.md and propagates them as `requirements: [SC-1, ...]` in each plan's frontmatter. These are NOT entries in `.planning/REQUIREMENTS.md` (which scopes the v7.1 milestone of Phase 38; this phase 38.8 is an inserted quality-gate phase). The SC-N IDs are phase-local and verified via the truths table above.

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| SC-1 (training operator-gate) | 38.8-01, 38.8-03, 38.8-06 | Training-mode single-set games land in :final_match_score after evaluate_result and stay until operator click | SATISFIED | Plan 01 RED→GREEN flip; result_recorder.rb deletions verified; test/system/final_match_score_operator_gate_test.rb SC-1 GREEN. |
| SC-2 (tournament operator-gate) | 38.8-04, 38.8-06 | Tournament-mode round-progression cascade deferred until operator close_match! | SATISFIED | Static-source assertions verify the wiring contract. CR-01 polymorphism guard prevents PartyMonitor crashes. **Empirical:** user manually validated BK-2 round_play 2026-05-01 — cascade fires exactly once per click. |
| SC-3 (final_match_score view + button) | 38.8-02, 38.8-05 | Scoreboard renders Nächstes Spiel button; click fires correct AASM event for training vs tournament mode | SATISFIED | i18n keys present (de + en), view branch wired, reflex methods present, runtime spot-checks all PASS. |
| SC-4 (cross-discipline regression test) | 38.8-01, 38.8-03, 38.8-06 | Karambol + BK-2 single-set training games all land in :final_match_score | SATISFIED | test/system/final_match_score_operator_gate_test.rb cross-discipline test loops over both disciplines and asserts landing. Pool/Snooker fixture absence accepted per plan permission. |
| SC-5 (Phase 38.7 tiebreak preserved) | 38.8-02, 38.8-03, 38.8-04, 38.8-06 | 4 system tests in tiebreak_test.rb still GREEN; tiebreak_not_pending? guard preserved verbatim | SATISFIED | bin/rails test test/system/tiebreak_test.rb → 4/4 GREEN. tiebreak_not_pending? grep count = 3 (preserved). Seal test in final_match_score_operator_gate_test.rb locks file integrity. |

No orphaned requirements: every SC-N ID declared in plan frontmatter is satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `app/services/table_monitor/result_recorder.rb` | 463 | `update(state: "playing")` literal phrase in documentary comment | Info | Comment names the c3dedb69 regression for future readers. Substantive AC (zero real call sites) verified — this is the only match and it is documentary. |
| `app/services/tournament_monitor/result_processor.rb` | 98, 151 | `Rails.logger.info` for StandardError logging | Info (IN-01 from REVIEW, intentionally not fixed in CR-fix iteration since IN findings out of scope) | Errors logged at info level rather than error. Pre-existing pattern in report_result; new advance_round_after_match_close inherited it. Does not block goal. |
| `test/system/final_match_score_operator_gate_test.rb` | 161-178 | SC-2 test renamed by WR-04 fix to "training-mode no-op cascade" | Info | Plan 06 SC-2 test does NOT exercise the real tournament cascade. WR-04 fix narrowed the test scope; the cascade behavioral contract is covered by static-source assertions + CR-01 PartyMonitor regression tests + manual UAT. |
| `test/models/table_monitor_test.rb` | (pre-existing) | 16 baseline standardrb offenses | Info | Pre-existing; Plan 06 explicitly forbade modifying baseline tests. Plan 06's new code is standardrb-clean. |

### Human Verification Required

The status is `human_needed` because one behavioral contract — operator-visible UX end-to-end across disciplines — cannot be verified programmatically with the test infrastructure adopted by this phase (service-level dispatch via .allocate stubs, no Capybara browser session).

**Note:** A second human-verification item (tournament round-progression exactly-once contract, originally raised by REVIEW.md CR-02) was **resolved on 2026-05-01** when the user manually exercised the BK-2 round_play scenario end-to-end and confirmed the cascade fires exactly once per click. CR-02 was reclassified as a false positive and the sentinel-based fix was reverted (commits 0dbe45c4 + 61e9e8c8). See `.planning/phases/38.8-endergebnis-erfasst-state-restore-operator-gate-the-post-mat/38.8-REVIEW.md` (CR-02 RECLASSIFIED block) and `38.8-REVIEW-FIX.md` (post_review_corrections frontmatter).

#### Operator-visible UX end-to-end across disciplines

**Test:** On the BCW dev server, complete one training match end-to-end for at least one karambol discipline AND one BK-2 family discipline. After the final inning is recorded, observe the scoreboard.

**Expected:**
- Green "Nächstes Spiel" button appears in the slot previously occupied by "zurück" (state_display slot, lines 154-179 of `_scoreboard.html.erb`).
- DE locale: button label reads "Nächstes Spiel"; state_display shows "Endergebnis erfasst".
- EN locale: button label reads "Next Game"; state_display shows "Results reported".
- Click triggers a clean rematch: TM transitions to `:playing`, players reverted (A↔B swap), play timer rearmed, no console errors, no broadcast errors, no cable subscription drops.
- Touchscreen: a touchstart event also triggers the rematch (touchstart pairing in data-reflex string).

**Why human:** Visual verification (button styling, color, position, affordance), I18n locale switching, browser-side reflex round-trip (StimulusReflex frame transport, CableReady morph), and touch-screen behavior cannot be asserted by service-level dispatch tests. The Plan 06 system test uses `ActiveSupport::TestCase` + `.allocate` stubs and never renders the partial in a DOM. Plan 05 SUMMARY explicitly references this as a Next Phase Readiness item.

### Gaps Summary

No gaps blocking goal achievement. All 5 must-haves are VERIFIED and all 89 tests across the combined regression sweep pass. The remaining human_verification item is about *coverage of the verification surface*, not a gap in the implementation itself: the implementation is complete and correct under the asserted contract, but the visual UX across disciplines is intentionally out-of-scope for this phase's automated test infrastructure and requires human confirmation before production deployment.

---

*Initial verification: 2026-05-01T12:30:00Z*
*Re-verified after CR-02 false-positive revert: 2026-05-01T15:00:00Z*
*Verifier: Claude (gsd-verifier)*
