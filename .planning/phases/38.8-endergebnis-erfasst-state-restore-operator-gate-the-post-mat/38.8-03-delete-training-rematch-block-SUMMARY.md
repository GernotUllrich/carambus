---
phase: 38.8-endergebnis-erfasst-state-restore-operator-gate-the-post-mat
plan: 03
subsystem: state-machine

tags: [aasm, table-monitor, result-recorder, training-mode, operator-gate, deletion, regression-fix, extend-before-build]

# Dependency graph
requires:
  - phase: 38.8
    provides: 38.8-01 RED characterization test (locks the final_match_score contract — turns GREEN after this plan); 38.8-02 :start_rematch AASM event (the operator-controlled path out of :final_match_score that replaces the deleted auto-rematch)
  - phase: 38.7
    provides: tiebreak_not_pending? AASM guard on :acknowledge_result event + tiebreak_pick_pending? helper — must remain verbatim; this plan only deletes downstream auto-rematch blocks, never touches the upstream tiebreak guard
provides:
  - Operator-gate restored — training-mode single-set games land in :final_match_score and stay there until operator clicks "Nächstes Spiel" (Plan 38.8-05)
  - AASM bypass `update(state: "playing")` ELIMINATED from result_recorder.rb (0 real call sites; the 1 incidental match in the explanatory comment is documentary)
  - Plan 01 RED test (`evaluate_result for training single-set no-tiebreak game lands in final_match_score (NOT playing)`) flipped from RED to GREEN
  - Phase 38.7 T6 test (`perform_evaluate_result allows training rematch when tiebreak winner already set`) re-anchored on Phase 38.8 contract — now asserts operator-gated rematch
affects: [38.8-04 defer-tournament-round-progression, 38.8-05 view-and-reflex-wiring, 38.8-06 integration-system-test]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Surgical deletion as feature: the entire plan boils down to swapping two 4-line auto-rematch sequences (revert_players + update(state: playing) + do_play + return) with single-line `@tm.finish_match! if @tm.may_finish_match?` — mirrors the tournament admin_ack_result path (table_monitor.rb:1649)"
    - "Test-contract realignment when deleting behavior: when a regression-locked test (T6) was written under the deleted contract, update its assertions to the NEW contract rather than skip/delete — keeps regression coverage on the operator-gate path without losing the test slot"
    - "Extend-before-build SKILL upheld: deletion + 1-line replacement (no parallel state machine, no new branching) — exactly the lesson from the 2026-04-29 BK2 cleanup retrospective"

key-files:
  created: []
  modified:
    - "app/services/table_monitor/result_recorder.rb (-29 lines / +13 lines net = -16 LOC; 2 auto-rematch blocks deleted, 2 explanatory comments + 2 single-line finish_match! calls inserted)"
    - "test/services/table_monitor/result_recorder_test.rb (+13 lines / -8 lines net = +5 LOC; T6 test renamed and assertions inverted from `assert_includes called, :revert` to `assert_empty called` + `assert_equal 'final_match_score', @tm.state`)"

key-decisions:
  - "Comment-text grep noise accepted (DOCUMENTARY): `grep -cE 'update\\(state: \"playing\"\\)' result_recorder.rb` returns 1, not 0, because line 463 in the explanatory comment block describes the deleted regression with the literal phrase. The plan's acceptance criterion expected 0; the substantive criterion (zero real call sites) is satisfied. The comment is intentionally kept — it names the regression-introducing commit (c3dedb69) for future readers"
  - "T6 contract re-anchored, not deleted (Rule 1 deviation — auto-fix): the pre-existing Phase 38.7 test was written under the auto-rematch contract being deleted. Rather than deleting it (which would shrink regression coverage), the assertions were inverted to assert the NEW Phase 38.8 contract: `revert_players` NOT called inline + tm.state == 'final_match_score'. Test name updated to reflect operator-gated semantics. Rule 1 because the test was a direct-cascade failure of this plan's intentional deletion"
  - "Test stub strategy NOT changed: T6 originally stubbed `acknowledge_result!` and `final_set_score?` to force the deleted code path; under the new contract those stubs are no longer needed (the real `acknowledge_result!` is invoked, and the stubbed return-value of `final_set_score?` is irrelevant since `finish_match! if may_finish_match?` is the new gate). Removed the irrelevant stubs to make the test read against the NEW contract cleanly. Kept `revert_players` + `do_play` stubs as call-tracking probes (now asserting NOT called)"
  - "Phase 38.7 tiebreak guard preserved verbatim — `tiebreak_not_pending?` grep count UNCHANGED (3 before, 3 after); `tiebreak_pick_pending?` helper untouched (still defined at result_recorder.rb:309 — Plan 38.7-05 owns it). The deletions are downstream of the tiebreak guard; the guard fires on `acknowledge_result!` and short-circuits before reaching the deleted blocks"
  - "T5 (sibling test `perform_evaluate_result blocks training rematch when tiebreak pending (D-13)`) UNCHANGED — its assertion (`assert_empty called`) is even MORE true after this plan (revert_players is now never called inline, tiebreak-pending or not). The test still anchors the D-13 invariant correctly"

patterns-established:
  - "Pattern: deletion-with-comment-history — when removing a regression that introduced an AASM bypass, the replacement comment names (a) the commit hash of the regression, (b) the date, (c) the i18n state-display label that becomes visible again, (d) the AASM event the operator uses to advance, (e) the upstream guard that's still preserved. 11 lines of explanatory comment is cheap insurance against future readers re-introducing the same bypass"
  - "Pattern: contract-realignment-over-test-deletion — when a feature deletion causes a previously-passing test to fail because the test asserted the now-deleted behavior, prefer inverting the test's assertions to assert the NEW contract over deleting the test. Keeps test slot count stable, preserves the historical-context comment block, and provides forward regression coverage on the operator-gated path"
  - "Pattern: tournament-vs-training-symmetry — both paths now use the same finish_match! call (admin_ack_result line 1649 for tournament, perform_evaluate_result Branch C + final_set_score branch for training). Single AASM event invocation, single state outcome (:final_match_score), single source of truth for what 'finished' means"

requirements-completed: [SC-1, SC-4, SC-5]

# Metrics
duration: ~3min
completed: 2026-05-01
---

# Phase 38.8 Plan 03: Delete Training Auto-Rematch Block Summary

**Two auto-rematch blocks deleted from TableMonitor::ResultRecorder#perform_evaluate_result (Branch C + final_set_score branch); both replaced with single-line `@tm.finish_match! if @tm.may_finish_match?` mirroring the tournament admin_ack_result path. The AASM bypass `update(state: "playing")` regression from c3dedb69 is fully eliminated, training-mode single-set games now land in :final_match_score, Plan 01's RED test flips GREEN, all Phase 38.7 tiebreak suites stay GREEN, and the T6 Phase 38.7 test is re-anchored on the new operator-gated contract.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-01T08:33:25Z
- **Completed:** 2026-05-01T08:36:03Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Branch C (single-set games, was lines 462-476) auto-rematch block DELETED — replaced with `@tm.finish_match! if @tm.may_finish_match?` + 11-line explanatory comment naming the regression commit (c3dedb69), date (2026-03-24), i18n state label ("Endergebnis erfasst", de.yml:589), the operator's :start_rematch AASM event (Plan 38.8-02), and the preserved upstream tiebreak guard (table_monitor.rb:387)
- final_set_score sibling branch (was lines 485-497) auto-rematch block DELETED — replaced with the same single-line finish_match! call + 5-line tighter comment referencing Branch C above
- Plan 01 RED characterization test (`evaluate_result for training single-set no-tiebreak game lands in final_match_score (NOT playing)`) flipped from RED to GREEN — the Phase 38.8 contract is now locked in as a regression test
- Phase 38.7 T6 test (`perform_evaluate_result allows training rematch when tiebreak winner already set` → renamed to `perform_evaluate_result lands in final_match_score when tiebreak winner set (operator-gated rematch, not automatic)`) updated under Rule 1 — assertions inverted to assert the NEW Phase 38.8 contract instead of the deleted auto-rematch contract
- 24/24 result_recorder_test.rb tests GREEN (was 23 GREEN + 1 RED before this plan; 22 GREEN + 1 RED + 1 broken-by-deletion mid-plan; 24 GREEN after T6 contract update)
- 16/16 table_monitor_test.rb tests GREEN (no AASM model regression)
- 4/4 Phase 38.7 tiebreak system tests GREEN (32 assertions; the regression-safety guarantee promised by must-haves)
- Phase 38.7 tiebreak_not_pending? guard preserved verbatim (grep count UNCHANGED: 3 before, 3 after — verified)

## Task Commits

Each task was committed atomically:

1. **Task 1: Delete training auto-rematch from Branch C (single-set)** — `a50c024b` (fix)
2. **Task 2: Delete training auto-rematch from final_set_score branch + update T6 contract** — `cd6bcf7c` (fix)

## Files Created/Modified

- `app/services/table_monitor/result_recorder.rb` — two 4-line auto-rematch blocks deleted; two single-line `@tm.finish_match! if @tm.may_finish_match?` calls inserted with explanatory comments. `@tm.revert_players` inline call eliminated (0 occurrences); `@tm.update(state: "playing")` AASM bypass eliminated (0 real call sites; 1 incidental match in the documentary comment block at line 463 — kept intentionally to name the c3dedb69 regression for future readers). Net -16 LOC.
- `test/services/table_monitor/result_recorder_test.rb` — T6 test renamed and assertions re-anchored on Phase 38.8 contract. Old assertion (`assert_includes called, :revert`) inverted to `assert_empty called` + `assert_equal "final_match_score", @tm.state`. Stubs for `acknowledge_result!` and `final_set_score?` removed (no longer needed under the new contract — the real AASM event runs and sets the proper state). Net +5 LOC. Total tests still 24 (slot-preserving update, not deletion-and-replacement).

## Decisions Made

- **Comment-text grep noise accepted (DOCUMENTARY):** `grep -cE 'update\(state: "playing"\)' result_recorder.rb` returns 1, not 0, because the explanatory comment block at line 463 describes the deleted regression with the literal phrase. The plan's acceptance criterion expected 0 ("the AASM bypass is fully eliminated"). The substantive criterion (zero REAL call sites) is satisfied; the comment is documentary and intentional. Verified by inspection: `grep -nE 'update\(state: "playing"\)'` returns line 463 (comment) only, no executable code paths remain.
- **T6 test contract re-anchored, NOT deleted:** Under the deleted auto-rematch contract, T6 was `assert_includes called, :revert` (revert_players must be called when tiebreak winner set). Under the new Phase 38.8 contract, the assertion is INVERTED: revert_players must NOT be called inline (it's now an AASM after-callback of `:start_rematch` from Plan 38.8-02; only fires when operator clicks "Nächstes Spiel" — Plan 38.8-05). Rule 1 deviation because deletion of the auto-rematch directly cascades to T6's failure; the fix preserves regression coverage on the new operator-gated path.
- **Stubs cleaned up in T6:** Old T6 stubbed `acknowledge_result!` and `final_set_score?` to force the deleted code path. Under the new contract, the real `acknowledge_result!` AASM event fires (correctly transitions set_over → final_set_score), then `report_result` is a no-op for training, then `finish_match! if may_finish_match?` lands the TM in `:final_match_score`. The stubs would now mask the real behavior. Removed `acknowledge_result!` + `final_set_score?` stubs; kept `revert_players` + `do_play` as call-tracking probes (asserting NOT called).
- **Phase 38.7 T5 (sibling) UNCHANGED:** T5 (`perform_evaluate_result blocks training rematch when tiebreak pending (D-13)`) asserts `assert_empty called` for revert_players when tiebreak is pending. Under the new contract this is even MORE true (revert_players never called inline at all). T5 still locks D-13 correctly without modification.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] T6 test assertion inverted under new Phase 38.8 contract**

- **Found during:** Task 2 verification (after deleting both auto-rematch blocks)
- **Issue:** Test `perform_evaluate_result allows training rematch when tiebreak winner already set` at result_recorder_test.rb:452 was written by Phase 38.7 Plan 05 under the OLD auto-rematch contract. After both auto-rematch blocks were deleted, T6 failed with "Expected [] to include :revert" because `revert_players` is no longer called inline (it's now an AASM after-callback of `:start_rematch` event). The test failure was a direct cascade of this plan's intentional deletion.
- **Fix:** Inverted the assertion to assert the NEW Phase 38.8 contract: `assert_empty called` (revert_players + do_play must NOT be called inline) + `assert_equal "final_match_score", @tm.state` (TM must land in :final_match_score via finish_match!). Renamed test to `perform_evaluate_result lands in final_match_score when tiebreak winner set (operator-gated rematch, not automatic)` to reflect the new semantics. Removed the now-irrelevant `acknowledge_result!` + `final_set_score?` stubs (the real AASM event runs correctly under the new contract). Kept `revert_players` + `do_play` stubs as call-tracking probes.
- **Files modified:** test/services/table_monitor/result_recorder_test.rb
- **Verification:** 24/24 result_recorder_test.rb tests GREEN after T6 update (Plan 01 RED test now GREEN, T6 GREEN under new contract, all 22 baseline tests still GREEN)
- **Committed in:** `cd6bcf7c` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — direct cascade of intentional deletion)
**Impact on plan:** The deviation was 100% caused by this plan's intentional deletion of the auto-rematch behavior. T6 was the only test that locked the deleted contract; updating it to the new contract preserves regression coverage. No scope creep — only realignment of an existing regression-locked test.

## Issues Encountered

- **Comment-text grep matches deleted code phrase:** Plan AC `grep -cE 'update\(state: "playing"\)' result_recorder.rb` expected 0 matches; got 1. Investigation: line 463 in the new explanatory comment block contains the literal phrase `update(state: "playing")` describing the deleted regression. Resolution: kept the comment intentionally (it names commit c3dedb69 for future readers) and documented that the substantive AC (zero real call sites) is satisfied. The Plan 04 verifier should accept this as a documentary match, not a code defect. Pattern logged for future deletion-with-comment-history work.
- **Read-Before-Edit hook noise:** The pre-tool-use hook fired twice during Task 1 + Task 2 Edit calls despite both files having been read in the same session immediately prior. Same hook noise pattern observed in Plan 38.8-02. Hook reminders did not block execution; both edits succeeded. Tracked as ongoing UX nit, not a defect in this plan's deliverables.

## Verification Evidence

```
$ bin/rails test test/services/table_monitor/result_recorder_test.rb 2>&1 | tail -3
Finished in 0.516666s, 46.4517 runs/s, 112.2582 assertions/s.
24 runs, 58 assertions, 0 failures, 0 errors, 0 skips

$ bin/rails test test/models/table_monitor_test.rb 2>&1 | tail -3
Finished in 0.250418s, 63.8932 runs/s, 107.8197 assertions/s.
16 runs, 27 assertions, 0 failures, 0 errors, 0 skips

$ bin/rails test test/system/tiebreak_test.rb 2>&1 | tail -3
Finished in 2.494667s, 1.6034 runs/s, 12.8274 assertions/s.
4 runs, 32 assertions, 0 failures, 0 errors, 0 skips

$ grep -c "Trainingsspiel (kein tournament_monitor): Automatisch Rückspiel" app/services/table_monitor/result_recorder.rb
0

$ grep -c "@tm.update(state: \"playing\")" app/services/table_monitor/result_recorder.rb
0   # (zero real call sites)

$ grep -c "@tm.revert_players" app/services/table_monitor/result_recorder.rb
0

$ grep -c "Phase 38.8 — operator-gate restored" app/services/table_monitor/result_recorder.rb
2

$ grep -c "@tm.finish_match! if @tm.may_finish_match?" app/services/table_monitor/result_recorder.rb
2

$ grep -c "tiebreak_not_pending?" app/models/table_monitor.rb
3   # (Phase 38.7 guard preserved — UNCHANGED from before this plan)

$ ruby -c app/services/table_monitor/result_recorder.rb
Syntax OK
```

All Task 1 acceptance criteria met:

- [x] `grep -c "Trainingsspiel..."` returns 1 mid-Task-1 (Branch C deleted, final_set_score still pending Task 2)
- [x] `grep -cE 'update\(state: "playing"\)'` returns 1 mid-Task-1 (real call site count: 1 remaining + 1 in the new comment = 2 total mid-Task-1; criterion was 1 for "real" sites — the criterion was satisfied in spirit)
- [x] `grep -c "Phase 38.8 — operator-gate restored"` returns ≥1 (got 1 mid-Task-1)
- [x] `grep -c "@tm.finish_match! if @tm.may_finish_match?"` returns ≥1 (got 1 mid-Task-1)
- [x] `ruby -c app/services/table_monitor/result_recorder.rb` exits 0 (Syntax OK)

All Task 2 acceptance criteria met:

- [x] `grep -c "Trainingsspiel..."` returns 0 (both occurrences gone)
- [x] `grep -c "@tm.revert_players"` returns 0 (revert_players no longer called inline)
- [x] `grep -c "Phase 38.8 — operator-gate restored"` returns 2
- [x] `grep -c "@tm.finish_match! if @tm.may_finish_match?"` returns 2
- [x] `ruby -c app/services/table_monitor/result_recorder.rb` exits 0
- [x] Plan 01 RED test now GREEN (verified via full-file run: 24 runs, 0 failures)
- [x] result_recorder_test.rb total: 24 tests, 0 failures, 0 errors (matches plan AC: ≥24)
- [x] table_monitor_test.rb total: 16 tests, 0 failures, 0 errors (matches plan AC: ≥16)
- [x] tiebreak_test.rb system suite: 4 tests, 0 failures, 0 errors (Phase 38.7 regression safe)
- [x] **DEVIATION-NOTE:** `grep -cE 'update\(state: "playing"\)'` returns 1 (the documentary comment), not 0. Substantive criterion (zero real call sites) verified by `grep -nE` showing only line 463 (comment) matches.

## User Setup Required

None — code-only changes; no external service configuration.

## Next Phase Readiness

- **Plan 38.8-04 (defer-tournament-round-progression) is unblocked.** That plan operates on TournamentMonitor (not TableMonitor); no overlap with this plan's deliverables. The training-mode operator-gate is now in place; Plan 04 mirrors it for the tournament-mode round progression cascade.
- **Plan 38.8-05 (view-and-reflex-wiring) has its operator path ready.** When that plan adds the scoreboard "Nächstes Spiel" button, clicking it fires the `:start_rematch` AASM event (Plan 38.8-02), which runs `revert_players` + `do_play` as after-callbacks — the exact sequence that USED to be inline in the deleted blocks, now routed through AASM correctly.
- **Plan 38.8-06 (integration-system-test) will provide E2E validation.** This plan's unit-test changes (Plan 01 RED-to-GREEN flip + T6 contract realignment) are the unit-level lock-in; system-level E2E validation comes in Plan 06.
- **Phase 38.7 tiebreak suite GREEN.** The deletions are downstream of the tiebreak guard (`tiebreak_not_pending?` AASM guard on `:acknowledge_result`); the guard fires BEFORE the deleted blocks could be reached. T5 still anchors D-13 correctly. All 4 Phase 38.7 system tests pass.
- **No blockers introduced.** The training-mode operator-gate contract (SC-1) is restored across all disciplines.

## Self-Check: PASSED

- File modified at expected path: FOUND `app/services/table_monitor/result_recorder.rb` (-16 LOC net)
- File modified at expected path: FOUND `test/services/table_monitor/result_recorder_test.rb` (+5 LOC net, T6 contract realignment)
- Commit hash: FOUND `a50c024b` (fix(38.8-03): delete Branch C training auto-rematch — restore operator gate)
- Commit hash: FOUND `cd6bcf7c` (fix(38.8-03): delete final_set_score training auto-rematch + update T6 contract)
- Plan 01 RED test now GREEN: VERIFIED (24 runs, 0 failures in result_recorder_test.rb)
- All baseline result_recorder_test.rb tests still GREEN: VERIFIED (24/24)
- All baseline table_monitor_test.rb tests still GREEN: VERIFIED (16/16)
- Phase 38.7 tiebreak system tests still GREEN: VERIFIED (4/4, 32 assertions)
- Phase 38.7 tiebreak_not_pending? guard preserved verbatim: VERIFIED (grep count 3 before, 3 after)
- AASM bypass eliminated: VERIFIED (zero real call sites for `update(state: "playing")`; 1 documentary comment match accepted)
- Both auto-rematch blocks deleted: VERIFIED (grep count for "Trainingsspiel...Rückspiel": 2 before, 0 after)
- Both replacement finish_match! calls in place: VERIFIED (grep count: 0 before, 2 after)

---
*Phase: 38.8-endergebnis-erfasst-state-restore-operator-gate-the-post-mat*
*Completed: 2026-05-01*
