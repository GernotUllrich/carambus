---
phase: 04-tablemonitor-gamesetup-optionspresenter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/table_monitor/game_setup.rb
  - test/services/table_monitor/game_setup_test.rb
autonomous: true
requirements: [TMON-02]

must_haves:
  truths:
    - "TableMonitor::GameSetup exists as an ApplicationService subclass with .call(table_monitor:, options:) interface"
    - "GameSetup handles Game and GameParticipation record creation (both existing-party-game and new-game branches)"
    - "GameSetup calls initialize_game on the model, deep_merge_data!, and save! — the full start_game sequence"
    - "GameSetup sets suppress_broadcast=true before batch saves and ensures cleanup in ensure block"
    - "GameSetup enqueues exactly one TableMonitorJob at the end, not during intermediate saves"
    - "Unit tests verify Game creation, GameParticipation creation, data hash initialization, and shootout handling"
  artifacts:
    - path: "app/services/table_monitor/game_setup.rb"
      provides: "GameSetup ApplicationService for start_game extraction"
      contains: "class TableMonitor::GameSetup < ApplicationService"
    - path: "test/services/table_monitor/game_setup_test.rb"
      provides: "Unit tests for GameSetup"
      contains: "class TableMonitor::GameSetupTest"
  key_links:
    - from: "app/services/table_monitor/game_setup.rb"
      to: "app/models/table_monitor.rb"
      via: "@tm parameter — calls public methods on model"
      pattern: "@tm\\.save!|@tm\\.deep_merge_data!|@tm\\.initialize_game"
---

<objective>
Create the TableMonitor::GameSetup ApplicationService that encapsulates the full start_game logic: Game/GameParticipation record creation, data hash initialization via initialize_game, and single-job-enqueue at the end. This is the extraction target — wiring into TableMonitor happens in Plan 02.

Purpose: Extract the most entangled method cluster from TableMonitor into an independently testable service (per D-01, D-02, D-03).
Output: app/services/table_monitor/game_setup.rb + unit tests
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tablemonitor-gamesetup-optionspresenter/04-CONTEXT.md
@.planning/phases/04-tablemonitor-gamesetup-optionspresenter/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/services/application_service.rb:
```ruby
class ApplicationService
  def self.call(kwargs = {})
    new(kwargs).call
  end
end
```

Note: ApplicationService.call passes a single hash to new(). GameSetup must override
self.call to accept keyword args, OR define initialize to destructure the hash:
```ruby
def initialize(kwargs = {})
  @tm = kwargs[:table_monitor]
  @options = HashWithIndifferentAccess.new(kwargs[:options] || {})
end
```

From app/models/table_monitor.rb (public methods GameSetup will call on @tm):
- `deep_merge_data!(hash)` — line 2230, public (before private at 2737)
- `initialize_game` — line 775, public
- `seeding_from(role)` — line 978, public (called from initialize_game)
- `save!` — ActiveRecord
- `reload` — ActiveRecord (overridden to reset @score_engine)
- `assign_attributes(hash)` — ActiveRecord
- `may_finish_warmup?` / `finish_warmup!` — AASM events
- `start_new_match!` — AASM event
- `data` — attribute accessor (JSON hash)
- `game` / `game=` / `game_id=` — association
- `tournament_monitor` — association
- `copy_from=` — attribute writer
- `skip_update_callbacks=` — attr_accessor (line 71)

From app/models/table_monitor.rb line 2001-2130 (current start_game logic to extract):
- Two branches: existing_party_game vs new game creation
- Sets skip_update_callbacks = true at start
- Creates Game + GameParticipation records
- Builds result hash with player data
- Calls initialize_game, deep_merge_data!(result), save!
- Sets skip_update_callbacks = false
- Enqueues TableMonitorJob.perform_later(id, "table_scores")
- Calls finish_warmup! for shootout discipline

From app/models/table_monitor.rb line 736-773 (assign_game logic to extract):
- Receives game_p parameter
- Sets allow_change_tables from tournament_monitor
- Two branches: tmp_results state present vs ready state
- In ready branch: calls initialize_game, save!, then AASM start_new_match!
- Calls finish_warmup! for shootout discipline

From app/models/table_monitor.rb line 2168-2178 (set_player_sequence):
- Iterates (a..d) with index, sets data["player_map"]["player#{ab_seqno}"]
- Only called from GameSetup context
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create GameSetup ApplicationService with unit tests</name>
  <files>app/services/table_monitor/game_setup.rb, test/services/table_monitor/game_setup_test.rb</files>
  <read_first>
    app/services/application_service.rb
    app/models/table_monitor.rb (lines 2001-2130 for start_game, lines 736-773 for assign_game, lines 775-967 for initialize_game, lines 2168-2178 for set_player_sequence, lines 978-989 for seeding_from)
    app/services/region_cc/league_syncer.rb (for ApplicationService pattern reference)
    test/models/table_monitor/score_engine_test.rb (for test pattern reference)
  </read_first>
  <behavior>
    - Test 1: GameSetup.call creates a new Game linked to the TableMonitor when no existing party game
    - Test 2: GameSetup.call creates GameParticipation records for playera and playerb roles
    - Test 3: GameSetup.call preserves existing party/tournament game when game.tournament_type present
    - Test 4: GameSetup.call unlinks previous game (sets table_monitor: nil) before creating new one
    - Test 5: GameSetup.call builds correct result hash with player balls_goal, discipline, sets_to_win, etc.
    - Test 6: GameSetup.call sets suppress_broadcast=true before saves, resets after (verify via mock)
    - Test 7: GameSetup.call enqueues exactly 1 TableMonitorJob with (id, "table_scores") at the end
    - Test 8: GameSetup.call triggers finish_warmup! when discipline matches /shootout/i
    - Test 9: GameSetup.call resets suppress_broadcast=false in ensure block even on exception
    - Test 10: assign_game branch — assigns game_id to model, calls initialize_game and start_new_match!
  </behavior>
  <action>
Create `app/services/table_monitor/game_setup.rb` as an ApplicationService subclass (per D-01, D-03).

The class structure:
```ruby
# frozen_string_literal: true

class TableMonitor::GameSetup < ApplicationService
  def initialize(kwargs = {})
    @tm = kwargs[:table_monitor]
    @options = HashWithIndifferentAccess.new(kwargs[:options] || {})
  end

  def call
    @tm.suppress_broadcast = true
    perform_start_game
    true
  ensure
    @tm.suppress_broadcast = false
  end

  private
  # ... extract start_game body (lines 2001-2130)
  # ... extract assign_game body (lines 736-773)
  # ... extract set_player_sequence body (lines 2168-2178)
end
```

Key implementation details:
1. Extract the TWO branches from start_game (existing_party_game vs new game) into private methods `setup_existing_party_game` and `create_new_game`
2. Extract the result hash builder (lines 2055-2112) into a private method `build_result_hash`
3. Move `assign_game` logic — receives a game_participation object, assigns game_id, calls initialize_game on @tm, triggers AASM events. Make this a class method or separate public method: `self.assign(table_monitor:, game_participation:)`
4. Move `set_player_sequence` as private method — it only mutates @tm.data["player_map"]
5. The `perform_start_game` method: creates Game/GameParticipation, builds result hash, calls @tm.initialize_game, @tm.deep_merge_data!(result), @tm.copy_from = nil, @tm.save!, then enqueues TableMonitorJob
6. The ensure block guarantees @tm.suppress_broadcast = false (per Pitfall 5 in RESEARCH.md)
7. For assign_game: expose as `TableMonitor::GameSetup.assign(table_monitor:, game_participation:)` — a second class-level entry point, since assign_game has different parameters than start_game
8. Use `@tm.suppress_broadcast=` (the new accessor name) rather than `skip_update_callbacks` — Plan 02 will add this accessor to the model. For now, the service sets both: `@tm.skip_update_callbacks = true` as the accessor still exists. Plan 02 will rename.

Write unit tests in `test/services/table_monitor/game_setup_test.rb` using Minitest. Tests should:
- Use fixtures or FactoryBot to create a TableMonitor with associated Table, TournamentMonitor
- Stub `TableMonitorJob.perform_later` to verify enqueue count
- Verify Game and GameParticipation record creation
- Verify the result hash structure
- Verify suppress_broadcast cleanup in ensure
- Test both start_game and assign_game paths

Note: For this task, GameSetup still uses `skip_update_callbacks=` (the existing accessor). Plan 02 will rename it to `suppress_broadcast`.
  </action>
  <verify>
    <automated>ruby -c app/services/table_monitor/game_setup.rb && bin/rails test test/services/table_monitor/game_setup_test.rb -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class TableMonitor::GameSetup < ApplicationService" app/services/table_monitor/game_setup.rb
    - grep -q "def call" app/services/table_monitor/game_setup.rb
    - grep -q "def initialize" app/services/table_monitor/game_setup.rb
    - grep -q "@tm" app/services/table_monitor/game_setup.rb
    - grep -q "Game.new\|Game.create\|GameParticipation.create" app/services/table_monitor/game_setup.rb
    - grep -q "deep_merge_data!" app/services/table_monitor/game_setup.rb
    - grep -q "initialize_game" app/services/table_monitor/game_setup.rb
    - grep -q "TableMonitorJob.perform_later" app/services/table_monitor/game_setup.rb
    - grep -q "ensure" app/services/table_monitor/game_setup.rb
    - grep -q "skip_update_callbacks" app/services/table_monitor/game_setup.rb
    - grep -q "class TableMonitor::GameSetupTest" test/services/table_monitor/game_setup_test.rb
  </acceptance_criteria>
  <done>
    - GameSetup service file exists with ApplicationService inheritance, .call(table_monitor:, options:) interface
    - All start_game logic (Game creation, GameParticipation creation, result hash, initialize_game call, single job enqueue) is in GameSetup
    - assign_game logic is in GameSetup as a second entry point
    - set_player_sequence is a private method of GameSetup
    - Unit tests pass covering both new-game and existing-party-game branches, job enqueue count, and ensure cleanup
    - Ruby syntax check passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller/Reflex -> GameSetup | Options hash from user input crosses into service |
| GameSetup -> ActiveRecord | Service creates Game/GameParticipation records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | GameSetup options hash | mitigate | Coerce options to HashWithIndifferentAccess (same as current start_game); do not trust raw ActionController::Parameters |
| T-04-02 | Denial of Service | suppress_broadcast not reset | mitigate | Use ensure block to guarantee @tm.suppress_broadcast = false even on exception (per Pitfall 5) |
| T-04-03 | Information Disclosure | Game record integrity | accept | GameSetup creates records same way as existing start_game; no new data paths; existing authorization (Pundit) unchanged |
</threat_model>

<verification>
- `ruby -c app/services/table_monitor/game_setup.rb` — syntax OK
- `bin/rails test test/services/table_monitor/game_setup_test.rb` — all tests pass
- `bin/rails test test/characterization/table_monitor_char_test.rb` — existing characterization tests still pass (GameSetup is new code; no changes to model yet)
</verification>

<success_criteria>
- GameSetup service exists with correct class hierarchy and interface
- Unit tests cover both game-creation branches, job enqueue assertion, ensure cleanup
- No changes to table_monitor.rb in this plan (wiring is Plan 02)
- Characterization tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/04-tablemonitor-gamesetup-optionspresenter/04-01-SUMMARY.md`
</output>
