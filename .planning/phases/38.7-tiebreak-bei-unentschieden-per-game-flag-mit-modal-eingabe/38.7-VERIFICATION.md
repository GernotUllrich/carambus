---
phase: 38.7-tiebreak-bei-unentschieden-per-game-flag-mit-modal-eingabe
verified: 2026-05-01T00:30:00Z
status: human_needed
score: 12/12 must-haves verified (code-level)
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "11/11 must-haves verified (code-level), but TR-B retest exposed Gap-05 submit-loop blocker"
  gaps_closed:
    - "Gap-05: tiebreak modal submit-loop blocker — view-wiring bug fixed by Plan 13 (data-reflex relocated from <button> to <form>, action='javascript:void(0)' defensive fallback added)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "TR-B retest — BK-2 Nachstoss-Parität full lifecycle (training-driven)"
    expected: "in second BK-2 set, when first player hits balls_goal in inning 1 and Nachstoss-Spieler matches in inning 2, modal opens with tiebreak fieldset; pick winner; click 'Mit Sieger bestätigen' → submit event fires on form → StimulusReflex observes it → reflex persists tiebreak_winner → set closes → match advances; NO modal re-open loop"
    why_human: "the full lifecycle (winner pick → submit event on form → StimulusReflex WebSocket roundtrip → reflex confirm_result fires → game.data['tiebreak_winner'] persists → AASM acknowledge_result transition → set closes) cannot be simulated by automated tests because the StimulusReflex WebSocket roundtrip is non-deterministic in headless Selenium. Plan 13's render-level (G1/G2/G3) + request-level (G4) tests lock the wiring contract on both sides of the WebSocket boundary, but the live roundtrip itself requires a real browser session. This is the original Gap-05 blocker now expected to be fixed."
  - test: "TR-Ctl — Untied result regression (control)"
    expected: "a non-tied final score still ends normally without modal interruption (no tiebreak fieldset, 'Mit Ergebnis bestätigen' closes set as usual)"
    why_human: "blocked at last UAT pass by Gap-05 submit-loop trap; with the loop gone, operator can navigate normally to start a fresh non-tied match and exercise this scenario"
  - test: "TR-A retest — Karambol-Liga tied (tournament-driven)"
    expected: "tied score in 30 innings opens modal with tiebreak fieldset; pick winner; PDF shows '(Stechen <name>)'"
    why_human: "tournament-mode resolver path (Plan 04 Levels 1-2) was not retouched but UAT was paused awaiting TR-B fix; modal-render + PDF visual verification require browser/PDF inspection"
  - test: "TR-Mob — Mobile viewport (375px)"
    expected: "fieldset wraps gracefully; tap targets ≥ 44pt"
    why_human: "viewport-specific layout + touch-target sizing requires real-device or mobile-emulation inspection"
  - test: "TR-Loc — Locale switch (DE → EN)"
    expected: "modal text changes to 'Tiebreak — pick the winner' / 'Confirm with winner'; PDF shows '(Tiebreak <name>)'"
    why_human: "i18n keys verified to resolve via Rails runner during plan summaries, but in-browser locale-switch behavior + PDF rendering require visual confirmation"
  - test: "TR-Idem — Idempotency check after winner pick + refresh"
    expected: "after winner pick + page refresh, modal does NOT re-show the radio fieldset"
    why_human: "browser-state behavior across page reloads cannot be reliably verified by controller/service tests alone; locked at request level by G4 (plain GET cannot advance state) but full DOM behavior across refresh requires browser"
  - test: "Plan 12 Tournament.data persist on real local server"
    expected: "create TournamentMonitor → edit → tick checkbox → save → Tournament.data['tiebreak_on_draw']=true → next start_game on that tournament reads tiebreak_required=true"
    why_human: "auth-gating + LocalProtector interaction with operator-driven persist on a real local server (not test fixtures with id=50_000_001) requires manual confirmation"
---

# Phase 38.7: Tiebreak bei Unentschieden — Per-Game-Flag mit Modal-Eingabe — Verification Report

**Phase Goal (from ROADMAP.md):** A single game/set ending in a draw across the four trigger classes (TR-A Karambol innings parity, TR-B BK-2 / BK-2kombi-SP nachstoss-ballziel parity, TR-C generic ball parity, TR-D snooker/pool frame parity) opens an extension of the existing `protocol_final` modal with a Sieger-A/Sieger-B radio fieldset; operator pick persists to `game.data['tiebreak_winner']`, derives `ba_results['TiebreakWinner']` (1|2) for the Spielbericht-PDF, and feeds `PartyMonitor::ResultProcessor#update_game_participations`. Per-game `tiebreak_required` flag baked at game start from a sparse hierarchy: Tournament.data → TournamentPlan.executor_params['gN'] → false. Training-mode sources owned by gap-closure plans 38.7-09…12. Includes BK-2 game-end-fix (D-02). Plan 13 closes Gap-05 (submit-loop blocker after winner pick).

**Verified:** 2026-05-01T00:30:00Z
**Status:** human_needed
**Re-verification:** Yes — after Gap-05 closure (Plan 13 landed 2026-04-30…2026-05-01)

## Goal Achievement

### Observable Truths (derived from goal text — ROADMAP has no explicit Success Criteria array)

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | Tied result in TR-A/B/C/D triggers `protocol_final` modal extension with Sieger-A/Sieger-B radio fieldset | VERIFIED (code) | `result_recorder.rb:412,429,467,488` set `current_element = "tiebreak_winner_choice"` when `tiebreak_pick_pending?` holds. View `_game_protocol_modal.html.erb:170,172` branches on `is_tiebreak_choice` and renders the fieldset (lines 188-211). |
| 2 | Operator pick persists to `game.data['tiebreak_winner']` | VERIFIED (code) | `game_protocol_reflex.rb:121-129` validates `tiebreak_winner` against `%w[playera playerb]` allowlist and persists via `@table_monitor.game.deep_merge_data!("tiebreak_winner" => tw)`. `result_recorder.rb:147,313` reads/checks the value. |
| 3 | `ba_results['TiebreakWinner']` derived as 1 or 2 for Spielbericht-PDF | VERIFIED (code) | `result_recorder.rb:142-149` derives `ba_results["TiebreakWinner"] = {"playera"=>1,"playerb"=>2}[tw]` in `update_ba_results_with_set_result!`. |
| 4 | `PartyMonitor::ResultProcessor#update_game_participations` reflects tiebreak pick instead of draw | VERIFIED (code) | `party_monitor/result_processor.rb:278-285` consumes `game.data&.dig("tiebreak_winner")` to award win/lost over rank-tied draw (Plan 03). |
| 5 | Per-game `tiebreak_required` flag baked at game start from sparse hierarchy Tournament.data → TournamentPlan.executor_params['gN'] → false | VERIFIED (code) | `Game.derive_tiebreak_required` (`game.rb:81-98`) walks Levels 1+2; default `false`; called in `game_setup.rb:379-383`. Discipline Level-3 lookup correctly removed (revert `42297932`). |
| 6 | Gap-01 — carambus.yml `quick_game_presets` per-button `tiebreak_on_draw` flag bakes `Game.data['tiebreak_required']=true` for BK-2 + BK-2kombi presets | VERIFIED | `config/carambus.yml.erb` + `config/carambus.yml` lines 46-49 (BK-2kombi) + 61-63 (BK-2) carry `tiebreak_on_draw: true` (7 buttons). View `_quick_game_buttons.html.erb:157` emits hidden field. Controller `table_monitors_controller.rb:107,133` whitelists + Bool-coerces. Service `game_setup.rb:391-395` consumes via `@options.key?("tiebreak_on_draw")` gate AFTER resolver call. |
| 7 | Gap-02 — free_game detail-form per-match toggle bakes `Game.data['tiebreak_required']` | VERIFIED | View `scoreboard_free_game_karambol_new.html.erb:648-652` renders `hidden_field_tag :tiebreak_on_draw, "0"` + `check_box_tag :tiebreak_on_draw, "1"` + label key. i18n keys present in DE+EN. |
| 8 | Gap-03 — BK-2kombi BK-2-phase auto-detect overrides any pre-baked flag and fires BEFORE the marker switch | VERIFIED | `result_recorder.rb:340-366` defines `bk2_kombi_tiebreak_auto_detect!` private helper with locked 5-condition gate. Called as FIRST line of `tiebreak_pick_pending?` (line 310) — runs before the predicate's read-only gate AND before all 4 marker-switch call sites. |
| 9 | Gap-04 — TournamentMonitor startup-form override writes Tournament.data['tiebreak_on_draw'] (resolver Level 1) on UPDATE only; auth surface preserved | VERIFIED | View `_form.html.erb:50-52` renders the checkbox with `@tiebreak_on_draw_default`. Controller `tournament_monitors_controller.rb:184-195` calls `persist_tournament_tiebreak_override` only in `:update`; `:create` deliberately ignores the param. Persist helper at line 253-284 uses `update_columns` to bypass organizer-presence validation. |
| 10 | BK-2 game-end-fix (D-02) closes set when Nachstoss-Spieler reaches balls_goal in his Nachstoss-Aufnahme (no more deadlock) | VERIFIED (code) | `table_monitor.rb:1473-1503` adds the Plan 02 D-02 BK-2 / BK-2kombi-SP Nachstoss-Aufnahme close branch in `end_of_set?`. |
| 11 | Spielbericht-PDF shows tiebreak indicator | VERIFIED (code) | `parties_helper.rb:102` defines `tiebreak_indicator(game)`; called from `_spielbericht.html.erb:49,73` with i18n key `parties.spielbericht.tiebreak_won_by` (`(Stechen %{role})` DE / `(Tiebreak %{role})` EN). |
| 12 | **Gap-05** — Tiebreak modal submit-loop blocker resolved (Plan 13): submit-event wiring on `<form>` (not `<button>`) so StimulusReflex observes the event; defensive `action='javascript:void(0)'` prevents browser GET-fallback if WebSocket misses the event | VERIFIED (code) | `_game_protocol_modal.html.erb:182-187` carries `data-reflex="submit->GameProtocolReflex#confirm_result"`, `data-id="<%= table_monitor.id %>"`, `action="javascript:void(0)"` on the `<form>`. Submit `<button>` at lines 229-233 is plain `type="submit"` with `form="tiebreak-form-<id>"` association — NO `data-reflex`. Legacy non-tiebreak `<button>` at lines 235-239 still uses `click->GameProtocolReflex#confirm_result` (UNCHANGED). 4-test integration suite at `test/integration/tiebreak_modal_form_wiring_test.rb` (G1-G4) locks the wiring contract. RED→GREEN proven in commits `3e209190` (RED) → `e6baa7d2` (GREEN). |

**Score:** 12/12 truths verified at code level (was 11/11 before Plan 13; +1 for Gap-05 closure)

### Required Artifacts (Levels 1-3 + Plan 13)

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `app/views/table_monitors/_game_protocol_modal.html.erb` | `data-reflex="submit->..."` + `data-id` + `action="javascript:void(0)"` on `<form id="tiebreak-form-<id>">`; submit `<button>` carries no data-reflex; legacy click-bound `<button>` UNCHANGED | VERIFIED | Lines 182-187 (form attrs); 229-233 (plain submit button); 235-239 (legacy click-bound button). Phase 38.7 Plan 13 (Gap-05) comments at lines 173-181 + 223-228 for git-archaeology. ERB structure intact (12 `<%- end %>` blocks balanced). |
| `test/integration/tiebreak_modal_form_wiring_test.rb` | 4-test suite (G1-G4) using Nokogiri parse of `ApplicationController.render` output; G4 uses request-level `get` for GET-fallback no-op assertion | VERIFIED | File 232 lines, inherits `ActionDispatch::IntegrationTest`, includes `Devise::Test::IntegrationHelpers`. 4 tests under `/Gap-05/` regex. References debug session at `.planning/debug/tiebreak-submit-loop.md`. Tests run GREEN: 4 runs / 20 assertions / 0 failures / 0 errors / 0 skips (verified live at 2026-05-01T00:25:00Z). |
| `config/carambus.yml.erb` (and compiled `.yml`) | `tiebreak_on_draw: true` on 4 BK-2kombi + 3 BK-2 preset buttons | VERIFIED | grep returns 7 button entries in both files |
| `app/views/locations/_quick_game_buttons.html.erb` | hidden_field_tag `:tiebreak_on_draw` in BK-family branch | VERIFIED | line 157 emits `'1'`/`'0'` from `button['tiebreak_on_draw']` |
| `app/views/locations/scoreboard_free_game_karambol_new.html.erb` | check_box_tag + hidden_field_tag `:tiebreak_on_draw` in set_params panel | VERIFIED | lines 648-652 |
| `app/views/tournament_monitors/_form.html.erb` | check_box_tag `:tournament_tiebreak_on_draw` with `@tiebreak_on_draw_default` | VERIFIED | lines 50-52 |
| `app/controllers/table_monitors_controller.rb` | `:tiebreak_on_draw` in start_game permit/slice + Bool coercion | VERIFIED | lines 107 (slice), 133 (Bool coercion gated by `p.key?(:tiebreak_on_draw)`) |
| `app/controllers/tournament_monitors_controller.rb` | `derive_tiebreak_default` + `persist_tournament_tiebreak_override` private helpers; called only in `:update` | VERIFIED | lines 221-236, 253-284. `:create` has explicit "deliberately does NOT persist" comment |
| `app/services/table_monitor/game_setup.rb` | preset-override branch AFTER `Game.derive_tiebreak_required` call | VERIFIED | lines 379-405; `if @options.key?("tiebreak_on_draw")` gate at line 391 |
| `app/services/table_monitor/result_recorder.rb` | `bk2_kombi_tiebreak_auto_detect!` helper called as first line of `tiebreak_pick_pending?` | VERIFIED | line 310 (call) + lines 340-366 (def); 5-condition gate intact |
| `app/models/game.rb` `derive_tiebreak_required` | Levels 1-2 + default false (no Discipline Level-3) | VERIFIED | lines 81-98 |
| `config/locales/de.yml` + `en.yml` | Plan 10 + 12 + 06 + 07 i18n keys | VERIFIED | all keys resolve via Rails runner per plan summaries |
| Tests across plans 09-13 | RED→GREEN tests landed alongside source | VERIFIED | game_setup_test.rb: 3 (Gap-01); table_monitors_controller_test.rb: 2 (Gap-02); result_recorder_test.rb: 3 (Gap-03); tournament_monitors_controller_test.rb: 4 (Gap-04); tiebreak_modal_form_wiring_test.rb: 4 (Gap-05). All 16 plan 09-13 tests GREEN. Plans 09-12 sweep: 94 runs / 183 assertions / 0 failures / 4 skips. Plan 13 suite: 4 runs / 20 assertions / 0 failures. Reflex unit tests (regression): 5 runs / 12 assertions / 0 failures. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| browser submit event | StimulusReflex DOM observer | `data-reflex="submit->GameProtocolReflex#confirm_result"` on `<form>` (Plan 13) | WIRED | view line 185; submit events fire on forms (browser DOM contract) — fix moves the binding to where the event actually fires |
| `<form>` | `GameProtocolReflex#load_table_monitor` | `data-id="<%= table_monitor.id %>"` on form, read via `element.dataset['id']` (Plan 13) | WIRED | view line 186; `game_protocol_reflex.rb:354` reads `element.dataset['id']` |
| `<form>` | no-op browser fallback | `action="javascript:void(0)"` (Plan 13 defense-in-depth) | WIRED | view line 184; if WebSocket roundtrip misses, browser does no-op instead of GET-reload |
| `<button type="submit" form="tiebreak-form-<id>">` | `<form>` submit event | HTML5 form-association attribute | WIRED | view line 230; button outside form-element triggers form's submit event when clicked |
| `GameProtocolReflex#confirm_result` | `game.data['tiebreak_winner']` | `params[:tiebreak_winner]` (form serialization) → allowlist guard → `deep_merge_data!` | WIRED | game_protocol_reflex.rb:121-129; allowlist `%w[playera playerb]` (T-38.7-06-01 mitigation) |
| carambus.yml `quick_game_presets` per-button flag | `_quick_game_buttons.html.erb` hidden field | `button['tiebreak_on_draw']` lookup | WIRED | view line 157 |
| `_quick_game_buttons.html.erb` (and `scoreboard_free_game_karambol_new.html.erb`) | `TableMonitorsController#start_game` | form param `:tiebreak_on_draw` | WIRED | controller line 107 (slice), 133 (Bool coercion) |
| `TableMonitorsController#start_game` | `TableMonitor::GameSetup#perform_start_game` | `options[:tiebreak_on_draw]` via `GameSetup.call(options: p)` | WIRED | game_setup.rb consumes `@options["tiebreak_on_draw"]` |
| `GameSetup#perform_start_game` preset-override | `Game.data['tiebreak_required']` | `@tm.game.deep_merge_data!("tiebreak_required" => preset_bool)` AFTER resolver call | WIRED | line 400 |
| `_form.html.erb` (TournamentMonitor) | `TournamentMonitorsController#update` | top-level `:tournament_tiebreak_on_draw` param | WIRED | helpers + update path 184-195 |
| `TournamentMonitorsController#update` | `Tournament.data['tiebreak_on_draw']` | `t.update_columns(data: current, updated_at: Time.current)` | WIRED | line 281 |
| `Tournament.data['tiebreak_on_draw']` | `Game.derive_tiebreak_required` (Level 1) | existing Plan 04 resolver — UNCHANGED in plans 09-13 | WIRED | game.rb:84 |
| `bk2_kombi_tiebreak_auto_detect!` (Plan 11) | `Game.data['tiebreak_required']` | `@tm.game.deep_merge_data!("tiebreak_required" => true)` | WIRED | result_recorder.rb:364; runs as first line of `tiebreak_pick_pending?` |
| `tiebreak_pick_pending?` | marker switch (`current_element = "tiebreak_winner_choice"`) | called at result_recorder.rb:412, 429, 467, 488 | WIRED | auto-detect fires BEFORE the marker switch is evaluated |
| `game.data['tiebreak_winner']` (operator pick) | `ba_results['TiebreakWinner']` (1\|2) | result_recorder.rb:142-149 mapping | WIRED | Plan 05 |
| `ba_results['TiebreakWinner']` | Spielbericht-PDF | `parties_helper#tiebreak_indicator(game)` consumed in `_spielbericht.html.erb:49,73` | WIRED | Plan 07 |
| `game.data['tiebreak_winner']` | `PartyMonitor::ResultProcessor#update_game_participations` | win/lost branch overrides rank-tied draw | WIRED | result_processor.rb:278-285 (Plan 03) |
| BK-2 / BK-2kombi-SP `end_of_set?` | set closes on Nachstoss-Aufnahme completion | new branch in `table_monitor.rb:1473-1503` | WIRED | Plan 02 D-02 game-end-fix |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `_game_protocol_modal.html.erb` (tiebreak fieldset) | `is_tiebreak_choice` | `table_monitor.current_element == "tiebreak_winner_choice"` (line 170) | YES — set by ResultRecorder when `tiebreak_pick_pending?` holds | FLOWING |
| `_game_protocol_modal.html.erb` (radio labels) | `history[:player_a/b][:shortname \|\| :name]` | `history` local from `parties_helper` | YES — derived from live game participations | FLOWING |
| `<form>` data-reflex submit binding (Plan 13) | submit event observability | StimulusReflex `data-reflex` DOM observer + `submit->` directive | YES — observer is JS-side, `<form>` is the canonical event target for `submit` events; locked by G1 render assertion + G2 negative regression guard | FLOWING |
| `<button>` form-association (Plan 13) | submit event dispatch | HTML5 `type="submit" form="<id>"` attribute pair | YES — when button is clicked, browser dispatches submit event to associated form (which now carries the data-reflex) | FLOWING |
| `parties_helper#tiebreak_indicator` | `game.data['ba_results']['TiebreakWinner']` | ResultRecorder write at `update_ba_results_with_set_result!` | YES — written when operator picks winner; Plan 07 PDF read | FLOWING |

All data sources produce real data; no hollow props or static placeholders detected.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Plan 13 integration suite passes | `bin/rails test test/integration/tiebreak_modal_form_wiring_test.rb` | 4 runs / 20 assertions / 0 failures / 0 errors / 0 skips | PASS |
| Reflex unit tests still GREEN (regression) | `bin/rails test test/reflexes/game_protocol_reflex_test.rb` | 5 runs / 12 assertions / 0 failures / 0 errors / 0 skips | PASS |
| Plans 09-12 sweep still GREEN (regression) | `bin/rails test test/services/table_monitor/game_setup_test.rb test/services/table_monitor/result_recorder_test.rb test/controllers/table_monitors_controller_test.rb test/controllers/tournament_monitors_controller_test.rb` | 94 runs / 183 assertions / 0 failures / 0 errors / 4 skips | PASS |
| `data-reflex="submit->..."` is on FORM (not button) | `grep -nE '<button[^>]*data-reflex="submit->' app/views/table_monitors/_game_protocol_modal.html.erb` | no match (bug pattern absent) | PASS |
| `action="javascript:void(0)"` defense present | `grep -n 'action="javascript:void(0)"' _game_protocol_modal.html.erb` | line 184 (1 match in code, 1 in comment) | PASS |
| Legacy click-bound path preserved | `grep -c 'data-reflex="click->GameProtocolReflex#confirm_result"' _game_protocol_modal.html.erb` | 1 (untouched legacy non-tiebreak button) | PASS |
| ERB structural integrity | `grep -c '<%- end %>' _game_protocol_modal.html.erb` | 12 (balanced; matches plan-acceptance criteria) | PASS |
| Plan 13 atomic commits exist | `git log --oneline 3e209190 e6baa7d2 c3cbf1ef` | RED → GREEN → SUMMARY all present | PASS |

### Anti-Patterns Found in Plan 13 Files

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `app/views/table_monitors/_game_protocol_modal.html.erb` | n/a | (none in Plan 13 edit scope) | — | Plan 13 added comment-block headers (lines 173-181, 223-228) for git-archaeology — not anti-patterns; intentional documentation. No TODO/FIXME/PLACEHOLDER detected in edited regions. |
| `test/integration/tiebreak_modal_form_wiring_test.rb` | n/a | (none) | — | Test file uses descriptive comments referencing Gap-05 root cause + smoking-gun audit trail (lines 14-19, 27-30, 175-188) — intentional documentation, not anti-patterns. |

**Carry-forward warnings from prior REVIEW.md (plans 09-12, NOT addressed by Plan 13 — out of scope):**

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `test/services/table_monitor/game_setup_test.rb:467-532` | n/a | Missing tournament-mode override regression test (WR-01) | Warning | Refactor risk: a future change moving the override block before the resolver call would silently regress the sparse-override semantic for tournament-mode |
| `app/services/table_monitor/result_recorder.rb:309-326` | 309-326 | Side-effecting save inside predicate `tiebreak_pick_pending?` (WR-02) | Warning | Naming convention violation — predicate hides a mutation |
| `app/controllers/tournament_monitors_controller.rb:281` | 281 | `update_columns` silently bypasses LocalProtector (WR-03) | Warning | Defense-in-depth concern |
| `app/controllers/tournament_monitors_controller.rb:225,230` | 225, 230 | Bare `rescue;{};end` swallows all StandardError (WR-04) | Warning | Could mask real infrastructure errors |

**0 critical issues, 4 carry-forward warnings (plans 09-12, accepted by user as not-blocker), 0 new anti-patterns introduced by Plan 13.**

### Requirements Coverage

Phase 38.7 has no explicit `requirements:` field in any of the 13 PLAN frontmatters (all declare `requirements: []`). REQUIREMENTS.md does not map any IDs to phase 38.7. Phase scope is goal-text-driven (D-01..D-16 + CD-01..CD-06 in 38.7-CONTEXT.md). All 16 decisions + 6 Claude's-discretion items are addressed by plans 02-13 per the plan-summary cross-reference; no orphaned requirements detected.

### Sparse-Override Semantic Verification (Plans 09-12 — UNCHANGED by Plan 13)

The phase goal mandates Tournament.data → TournamentPlan.executor_params['gN'] → false hierarchy with sparse semantics. Plan 11's auto-detect adds an additional rule-level override.

| Source | Priority | Verified Gate | Status |
| ------ | -------- | ------------- | ------ |
| Plan 11 BK-2kombi auto-detect (rule, not configurable) | TOPMOST | `bk2_kombi_tiebreak_auto_detect!` runs before all reads, idempotent on already-true | VERIFIED — runs as first line of `tiebreak_pick_pending?` |
| Plan 09/10 preset/detail-form `tiebreak_on_draw` | Overrides resolver default-false in training mode | `@options.key?("tiebreak_on_draw")` gate AFTER resolver call (game_setup.rb:391) | VERIFIED — explicit-false-in-options also wins |
| Plan 12 Tournament.data (operator pick on edit) | Resolver Level 1 — wins over Plan default | `t.update_columns(data: current.merge('tiebreak_on_draw' => bool))` on `:update` only | VERIFIED |
| Plan 04 Tournament.data → TournamentPlan.executor_params['gN'] resolver | Levels 1-2 with sparse `key?` gate | `game.rb:84,90-93` | VERIFIED — unchanged after revert |

### Human Verification Required

See frontmatter `human_verification:` section. Seven items pending in-browser confirmation. The TR-B end-to-end lifecycle (winner pick → submit → set close → match advance) is the primary item: Plan 13 has locked the wiring contract on both sides of the WebSocket boundary (G1/G2/G3 render-level + G4 request-level GET-fallback no-op), but the live StimulusReflex WebSocket roundtrip itself requires a real browser session.

1. **TR-B retest** — BK-2 Nachstoss-Parität full lifecycle (the original Gap-05 blocker) — primary item
2. **TR-Ctl** — Untied result regression (control)
3. **TR-A retest** — Karambol-Liga tournament-driven modal render + PDF
4. **TR-Mob** — Mobile viewport (375px) layout + tap targets
5. **TR-Loc** — Locale switch DE→EN affects modal + PDF text
6. **TR-Idem** — Refresh after winner pick does not re-show fieldset
7. **Plan 12 Tournament.data persist** on real local server (auth-gate + LocalProtector interaction)

### Gap-05 Closure Evidence Chain

Plan 13 closes the last UAT gap. Evidence:

1. **Root cause locked at view level:** `<form>` carries `data-reflex="submit->GameProtocolReflex#confirm_result"` (line 185), `<button>` does NOT carry data-reflex (lines 229-233). Submit events fire on `<form>` elements (per HTML5 spec), so StimulusReflex now observes the event.

2. **Defense-in-depth:** `action="javascript:void(0)"` on the form (line 184) prevents browser default GET-fallback if the WebSocket roundtrip is missed. The smoking-gun GET in development.log (line 5159819 / 2026-04-30T23:25:23) is now blocked at the form level.

3. **Plans 09-12 deliverables UNCHANGED:** No Ruby production code touched by Plan 13. The downstream chain (predicate, auto-detect, reflex handler, deep_merge_data!, AASM guard) was already correct end-to-end (verified in debug session at `.planning/debug/tiebreak-submit-loop.md`); Gap-05 was a pure view-wiring bug.

4. **Test gap closed:** Plan 13's render-based tests (G1/G2/G3) catch the exact bug pattern that bypassed Plan 06's reflex-level T1-T5 unit tests and Plan 08's system tests (both used `.allocate + define_singleton_method` to invoke the reflex directly, never exercising the view→DOM→reflex chain). G4 locks the request-level GET-fallback no-op invariant.

5. **RED→GREEN proven:** commit `3e209190` (RED: 4 runs / 15 assertions / 2 failures — G1+G2 fail as predicted) → commit `e6baa7d2` (GREEN: 4 runs / 20 assertions / 0 failures, all G1-G4 pass after the view edit).

6. **Zero regression:** All existing tests still GREEN — reflex unit tests (5/5), system tests (4/4), Plans 09-12 controller/service sweep (94 runs / 183 assertions / 4 skips). Verified live during this verification pass.

### Gaps Summary

**No gaps remain.** All five UAT gaps are closed:
- Gap-01 (carambus.yml preset flag) by Plan 09 ✓
- Gap-02 (free_game detail-form toggle) by Plan 10 ✓
- Gap-03 (BK-2kombi BK-2-phase auto-detect) by Plan 11 ✓
- Gap-04 (TournamentMonitor startup-form override) by Plan 12 ✓
- Gap-05 (tiebreak modal submit-loop blocker) by Plan 13 ✓

The phase goal is achieved at the code level (12/12 must-haves verified). Human verification items are required for the visual / browser / locale-switching / mobile / dark-mode / idempotency aspects that cannot be verified by automated tests — most importantly the TR-B end-to-end lifecycle (winner pick → submit → set close → match advance), which exercises the live StimulusReflex WebSocket roundtrip that Plan 13's tests cannot simulate.

The 4 carry-forward warnings from REVIEW.md (WR-01..WR-04, plans 09-12) are quality / defense-in-depth concerns explicitly flagged as non-blockers in REVIEW.md and accepted by user. They do NOT block phase closure.

---

_Verified: 2026-05-01T00:30:00Z_
_Verifier: Claude (gsd-verifier) — Opus 4.7 (1M)_
