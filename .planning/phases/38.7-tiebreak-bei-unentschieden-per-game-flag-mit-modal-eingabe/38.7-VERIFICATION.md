---
phase: 38.7-tiebreak-bei-unentschieden-per-game-flag-mit-modal-eingabe
verified: 2026-04-30T17:58:04Z
status: human_needed
score: 11/11 must-haves verified (code-level)
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "0/8 UAT scenarios passing — TR-B failed in browser UAT"
  gaps_closed:
    - "Gap-01: carambus.yml quick_game_presets per-button tiebreak source (Plan 09)"
    - "Gap-02: free_game detail-form per-match toggle (Plan 10)"
    - "Gap-03: BK-2kombi BK-2-phase auto-detect (Plan 11)"
    - "Gap-04: TournamentMonitor startup-form override (Plan 12)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "TR-B retest — BK-2 Nachstoss-Parität (training-driven)"
    expected: "in second BK-2 set, when first player hits balls_goal in inning 1 and Nachstoss-Spieler matches in inning 2, modal opens with tiebreak fieldset; pick winner; match continues correctly"
    why_human: "in-browser modal-render + visual contrast cannot be verified by automated tests; the original TR-B failure was the trigger for plans 09-12 and code-level evidence shows the fix should now render the modal — but visual confirmation is required"
  - test: "TR-A retest — Karambol-Liga tied (tournament-driven)"
    expected: "tied score in 30 innings opens modal with tiebreak fieldset; pick winner; PDF shows '(Stechen <name>)'"
    why_human: "tournament-mode resolver path (Plan 04 Levels 1-2) was not retouched but UAT was paused awaiting TR-B fix; modal-render + PDF visual verification require browser/PDF inspection"
  - test: "TR-DM — dark-mode contrast on tiebreak fieldset"
    expected: "yellow-bordered fieldset readable in dark scheme; player-name contrast OK; submit button distinguishable"
    why_human: "visual contrast assessment under user-selected color scheme is inherently human-only"
  - test: "TR-Mob — mobile viewport (375px)"
    expected: "fieldset wraps gracefully; tap targets ≥ 44pt"
    why_human: "viewport-specific layout + touch-target sizing requires real-device or mobile-emulation inspection"
  - test: "TR-Loc — locale switch (DE → EN)"
    expected: "modal text changes to 'Tiebreak — pick the winner' / 'Confirm with winner'; PDF shows '(Tiebreak <name>)'"
    why_human: "i18n keys verified to resolve via Rails runner during plan summaries, but in-browser locale-switch behavior + PDF rendering require visual confirmation"
  - test: "TR-Idem — idempotency check after winner pick + refresh"
    expected: "after winner pick + page refresh, modal does NOT re-show the radio fieldset"
    why_human: "browser-state behavior across page reloads cannot be reliably verified by controller/service tests alone"
  - test: "Plan 12 Tournament.data persist on real local server"
    expected: "create TournamentMonitor → edit → tick checkbox → save → Tournament.data['tiebreak_on_draw']=true → next start_game on that tournament reads tiebreak_required=true"
    why_human: "auth-gating + LocalProtector interaction with operator-driven persist on a real local server (not test fixtures with id=50_000_001) requires manual confirmation"
---

# Phase 38.7: Tiebreak bei Unentschieden — Per-Game-Flag mit Modal-Eingabe — Verification Report

**Phase Goal (from ROADMAP.md):** A single game/set ending in a draw across the four trigger classes opens an extension of the existing `protocol_final` modal with a Sieger-A/Sieger-B radio fieldset; operator pick persists to `game.data['tiebreak_winner']`, derives `ba_results['TiebreakWinner']` (1|2) for the Spielbericht-PDF, and feeds `PartyMonitor::ResultProcessor#update_game_participations`. Per-game `tiebreak_required` flag baked at game start from a sparse hierarchy: Tournament.data → TournamentPlan.executor_params['gN'] → false. Training-mode sources owned by gap-closure plans 38.7-09…12. Includes BK-2 game-end-fix (D-02).

**Verified:** 2026-04-30T17:58:04Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (plans 38.7-09 / 10 / 11 / 12 landed 2026-04-30)

## Goal Achievement

### Observable Truths (derived from goal text — ROADMAP has no explicit Success Criteria array)

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | Tied result in TR-A/B/C/D triggers `protocol_final` modal extension with Sieger-A/Sieger-B radio fieldset | VERIFIED (code) | `result_recorder.rb:412,429` set `current_element = tiebreak_pick_pending? ? "tiebreak_winner_choice" : "confirm_result"`. View `_game_protocol_modal.html.erb:170` branches on `is_tiebreak_choice`. |
| 2 | Operator pick persists to `game.data['tiebreak_winner']` | VERIFIED (code) | `result_recorder.rb:147,313` reads/checks `game.data['tiebreak_winner']`; Plan 06 reflex writes the value (per Plan 06 SUMMARY). |
| 3 | `ba_results['TiebreakWinner']` derived as 1 or 2 for Spielbericht-PDF | VERIFIED (code) | `result_recorder.rb:142-149` derives `ba_results["TiebreakWinner"] = {"playera"=>1,"playerb"=>2}[tw]` in `update_ba_results_with_set_result!`. |
| 4 | `PartyMonitor::ResultProcessor#update_game_participations` reflects tiebreak pick instead of draw | VERIFIED (code) | `party_monitor/result_processor.rb:278-285` consumes `game.data&.dig("tiebreak_winner")` to award win/lost over rank-tied draw (Plan 03). |
| 5 | Per-game `tiebreak_required` flag baked at game start from sparse hierarchy Tournament.data → TournamentPlan.executor_params['gN'] → false | VERIFIED (code) | `Game.derive_tiebreak_required` (`game.rb:81-98`) walks Levels 1+2; default `false`; called in `game_setup.rb:379-383`. Discipline Level-3 lookup correctly removed (revert `42297932`). |
| 6 | Gap-01: carambus.yml quick_game_presets per-button `tiebreak_on_draw` flag bakes `Game.data['tiebreak_required']=true` for BK-2 + BK-2kombi presets | VERIFIED | `config/carambus.yml.erb` + `config/carambus.yml` lines 46-49 (BK-2kombi) + 61-63 (BK-2) carry `tiebreak_on_draw: true` (7 buttons). View `_quick_game_buttons.html.erb:157` emits hidden field. Controller `table_monitors_controller.rb:107,133` whitelists + Bool-coerces. Service `game_setup.rb:391-395` consumes via `@options.key?("tiebreak_on_draw")` gate; AFTER resolver call. Tests G1-G3 (Gap-01) GREEN. |
| 7 | Gap-02: free_game detail-form per-match toggle bakes `Game.data['tiebreak_required']` | VERIFIED | View `scoreboard_free_game_karambol_new.html.erb:648-652` renders `hidden_field_tag :tiebreak_on_draw, "0"` + `check_box_tag :tiebreak_on_draw, "1"` + `t("locations.scoreboard_free_game.tiebreak_on_draw")`. i18n keys present in DE (`Stechen bei Unentschieden`) + EN (`Tiebreak on draw`). Tests G1-G2 (Gap-02) GREEN; rides Plan 09 controller wiring (untouched by Plan 10). |
| 8 | Gap-03: BK-2kombi BK-2-phase auto-detect overrides any pre-baked flag and fires BEFORE the marker switch | VERIFIED | `result_recorder.rb:340-366` defines `bk2_kombi_tiebreak_auto_detect!` private helper with locked 5-condition gate. Called as FIRST line of `tiebreak_pick_pending?` (line 310) — runs before the predicate's read-only gate AND before all 4 marker-switch call sites (lines 412, 429, 467, 488). Tests G1-G3 (Gap-03) GREEN. |
| 9 | Gap-04: TournamentMonitor startup-form override writes Tournament.data['tiebreak_on_draw'] (resolver Level 1) on UPDATE only; auth surface preserved | VERIFIED | View `_form.html.erb:50-52` renders the checkbox with `@tiebreak_on_draw_default`. Controller `tournament_monitors_controller.rb:184-195` calls `persist_tournament_tiebreak_override` only in `:update`; `:create` deliberately ignores the param (lines 168-176 explain auth rationale). Persist helper at line 253-284 uses `update_columns` to bypass organizer-presence validation. Tests G1-G4 (Gap-04) GREEN — G4 specifically locks the auth-gate contract. |
| 10 | BK-2 game-end-fix (D-02) closes set when Nachstoss-Spieler reaches balls_goal in his Nachstoss-Aufnahme (no more deadlock) | VERIFIED (code) | `table_monitor.rb:1473-1503` adds the Plan 02 D-02 BK-2 / BK-2kombi-SP Nachstoss-Aufnahme close branch in `end_of_set?`. |
| 11 | Spielbericht-PDF shows tiebreak indicator | VERIFIED (code) | `parties_helper.rb:102` defines `tiebreak_indicator(game)`; called from `_spielbericht.html.erb:49,73` with i18n key `parties.spielbericht.tiebreak_won_by` (`(Stechen %{role})` DE / `(Tiebreak %{role})` EN). |

**Score:** 11/11 truths verified at code level

### Required Artifacts (Levels 1-3)

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `config/carambus.yml.erb` (and compiled `.yml`) | `tiebreak_on_draw: true` on 4 BK-2kombi + 3 BK-2 preset buttons | VERIFIED | grep returns 7 button entries in both files; sibling-edit pattern documented in STATE memory |
| `app/views/locations/_quick_game_buttons.html.erb` | hidden_field_tag `:tiebreak_on_draw` in BK-family branch | VERIFIED | line 157 emits `'1'`/`'0'` from `button['tiebreak_on_draw']` |
| `app/views/locations/scoreboard_free_game_karambol_new.html.erb` | check_box_tag + hidden_field_tag `:tiebreak_on_draw` in set_params panel | VERIFIED | lines 648-652 |
| `app/views/tournament_monitors/_form.html.erb` | check_box_tag `:tournament_tiebreak_on_draw` with `@tiebreak_on_draw_default` | VERIFIED | lines 50-52 |
| `app/controllers/table_monitors_controller.rb` | `:tiebreak_on_draw` in start_game permit/slice + Bool coercion | VERIFIED | lines 107 (slice), 133 (Bool coercion gated by `p.key?(:tiebreak_on_draw)`) |
| `app/controllers/tournament_monitors_controller.rb` | `derive_tiebreak_default` + `persist_tournament_tiebreak_override` private helpers; called only in `:update` | VERIFIED | lines 221-236, 253-284. `persist_tournament_tiebreak_override` referenced 2× (1 def + 1 call in `update`); `:create` has explicit "deliberately does NOT persist" comment |
| `app/services/table_monitor/game_setup.rb` | preset-override branch AFTER `Game.derive_tiebreak_required` call | VERIFIED | lines 379-405; `if @options.key?("tiebreak_on_draw")` gate at line 391 |
| `app/services/table_monitor/result_recorder.rb` | `bk2_kombi_tiebreak_auto_detect!` helper called as first line of `tiebreak_pick_pending?` | VERIFIED | line 310 (call) + lines 340-366 (def); 5-condition gate intact |
| `app/models/game.rb` `derive_tiebreak_required` | Levels 1-2 + default false (no Discipline Level-3) | VERIFIED | lines 81-98; Discipline lookup correctly removed in revert `42297932` |
| `config/locales/de.yml` + `en.yml` | `tournament_monitors.form.tiebreak_on_draw` + `locations.scoreboard_free_game.tiebreak_on_draw` keys (DE+EN) | VERIFIED | de.yml lines 361, 757; en.yml lines 344, 717. Disjoint namespaces; both resolve via Rails runner per Plan 10 + 12 self-checks |
| Tests across plans 09-12 | RED→GREEN tests landed alongside source | VERIFIED | game_setup_test.rb: 3 G1-G3 (Gap-01); table_monitors_controller_test.rb: 2 G1-G2 (Gap-02); result_recorder_test.rb: 3 G1-G3 (Gap-03); tournament_monitors_controller_test.rb: 4 G1-G4 (Gap-04). All 12 tests GREEN; 94 runs / 183 assertions / 0 failures across 4 suites combined |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| carambus.yml `quick_game_presets` per-button flag | `_quick_game_buttons.html.erb` hidden field | `button['tiebreak_on_draw']` lookup | WIRED | view line 157 |
| `_quick_game_buttons.html.erb` (and `scoreboard_free_game_karambol_new.html.erb`) | `TableMonitorsController#start_game` | form param `:tiebreak_on_draw` | WIRED | controller line 107 (slice), 133 (Bool coercion) |
| `TableMonitorsController#start_game` | `TableMonitor::GameSetup#perform_start_game` | `options[:tiebreak_on_draw]` via `GameSetup.call(options: p)` | WIRED | game_setup.rb consumes `@options["tiebreak_on_draw"]` |
| `GameSetup#perform_start_game` preset-override | `Game.data['tiebreak_required']` | `@tm.game.deep_merge_data!("tiebreak_required" => preset_bool)` AFTER resolver call | WIRED | line 400 |
| `_form.html.erb` (TournamentMonitor) | `TournamentMonitorsController#update` | top-level `:tournament_tiebreak_on_draw` param | WIRED | helpers + update path 184-195 |
| `TournamentMonitorsController#update` | `Tournament.data['tiebreak_on_draw']` | `t.update_columns(data: current, updated_at: Time.current)` | WIRED | line 281 |
| `Tournament.data['tiebreak_on_draw']` | `Game.derive_tiebreak_required` (Level 1) | existing Plan 04 resolver — UNCHANGED in plans 09-12 | WIRED | game.rb:84 |
| `bk2_kombi_tiebreak_auto_detect!` (Plan 11) | `Game.data['tiebreak_required']` | `@tm.game.deep_merge_data!("tiebreak_required" => true)` | WIRED | result_recorder.rb:364; runs as first line of `tiebreak_pick_pending?` |
| `tiebreak_pick_pending?` | marker switch (`current_element = "tiebreak_winner_choice"`) | called at result_recorder.rb:412, 429, 467, 488 | WIRED | auto-detect fires BEFORE the marker switch is evaluated (extend-before-build SKILL applied) |
| `game.data['tiebreak_winner']` (operator pick) | `ba_results['TiebreakWinner']` (1\|2) | result_recorder.rb:142-149 mapping | WIRED | Plan 05 |
| `ba_results['TiebreakWinner']` | Spielbericht-PDF | `parties_helper#tiebreak_indicator(game)` consumed in `_spielbericht.html.erb:49,73` | WIRED | Plan 07 |
| `game.data['tiebreak_winner']` | `PartyMonitor::ResultProcessor#update_game_participations` | win/lost branch overrides rank-tied draw | WIRED | result_processor.rb:278-285 (Plan 03) |
| BK-2 / BK-2kombi-SP `end_of_set?` | set closes on Nachstoss-Aufnahme completion | new branch in `table_monitor.rb:1473-1503` | WIRED | Plan 02 D-02 game-end-fix |

### Sparse-Override Semantic Verification

The phase goal mandates Tournament.data → TournamentPlan.executor_params['gN'] → false hierarchy with sparse semantics. After Plan 11 the auto-detect adds an additional rule-level override.

| Source | Priority | Verified Gate | Status |
| ------ | -------- | ------------- | ------ |
| Plan 11 BK-2kombi auto-detect (rule, not configurable) | TOPMOST (overrides all) | `bk2_kombi_tiebreak_auto_detect!` runs before all reads, idempotent on already-true | VERIFIED — runs as first line of `tiebreak_pick_pending?` |
| Plan 09/10 preset/detail-form `tiebreak_on_draw` | Overrides resolver default-false in training mode | `@options.key?("tiebreak_on_draw")` gate AFTER resolver call (game_setup.rb:391) | VERIFIED — explicit-false-in-options also wins (sparse override consistent with 38.5 D-06) |
| Plan 12 Tournament.data (operator pick on edit) | Resolver Level 1 — wins over Plan default | `t.update_columns(data: current.merge('tiebreak_on_draw' => bool))` on `:update` only | VERIFIED |
| Plan 04 Tournament.data → TournamentPlan.executor_params['gN'] resolver | Levels 1-2 with sparse `key?` gate | `game.rb:84,90-93` | VERIFIED — unchanged after revert |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| All 4 plan test suites pass | `bin/rails test test/services/table_monitor/game_setup_test.rb test/services/table_monitor/result_recorder_test.rb test/controllers/table_monitors_controller_test.rb test/controllers/tournament_monitors_controller_test.rb` | 94 runs / 183 assertions / 0 failures / 4 skips | PASS |
| YAML parses cleanly | `ruby -ryaml -e 'YAML.load_file("config/carambus.yml")'` | exit 0 | PASS (per plan 09 self-check) |
| i18n keys resolve | `bin/rails runner 'puts I18n.t("tournament_monitors.form.tiebreak_on_draw", locale: :de)'` and `locations.scoreboard_free_game.tiebreak_on_draw` | both resolve in DE+EN | PASS (per plan 10 + 12 self-checks) |
| Auto-detect sequence verified | `grep -A1 "def tiebreak_pick_pending?" result_recorder.rb` shows `bk2_kombi_tiebreak_auto_detect!` as first line | matches spec | PASS |

### Anti-Patterns Found (from REVIEW)

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `test/services/table_monitor/game_setup_test.rb:467-532` | n/a | Missing tournament-mode override regression test (WR-01) | Warning | Refactor risk: a future change moving the override block before the resolver call would silently regress the sparse-override semantic for tournament-mode |
| `app/services/table_monitor/result_recorder.rb:309-326` | 309-326 | Side-effecting save inside predicate `tiebreak_pick_pending?` (WR-02) | Warning | Naming convention violation — predicate hides a mutation; preferred fix is to lift auto-detect to `perform_evaluate_result` or rename the predicate |
| `app/controllers/tournament_monitors_controller.rb:281` | 281 | `update_columns` silently bypasses LocalProtector (WR-03) | Warning | Defense-in-depth: future contributors reusing the helper from a non-director-gated action would silently bypass LocalProtector |
| `app/controllers/tournament_monitors_controller.rb:225,230` | 225, 230 | Bare `rescue;{};end` swallows all StandardError (WR-04) | Warning | Could mask real infrastructure errors as default-false return; should narrow to `JSON::ParserError, TypeError` |
| `app/controllers/tournament_monitors_controller.rb:225-232` | 225-232 | `derive_tiebreak_default` parsing duplicates `Game.parse_data_hash` (IN-06) | Info | Triplication risk — extract to public utility |
| `config/carambus.yml.erb` | n/a | Discipline name "BK2-Kombi" used in views/yml vs canonical "BK-2kombi" (IN-01) | Info | Pre-existing legacy naming; transitional `BK_NAME_TO_FORM` mapping handles it |
| `app/views/tournament_monitors/_form.html.erb:49-53` | 49-53 | Checkbox renders unconditionally on both new and edit (IN-03) | Info | Operator UX trap — ticking on `:create` silently has no effect |
| `app/services/table_monitor/game_setup.rb:402-404` | 402-404 | `Rails.logger.info` instead of `.debug{}` block-form for hot path (IN-05) | Info | Production log-noise concern; phase 38.1 IN-02 documented downgrade-info-to-debug pattern |
| `test/services/table_monitor/game_setup_test.rb` | n/a | No integration test exercises full 4-level resolver chain end-to-end (IN-04) | Info | Each plan tested in isolation; priority semantic across all four levels not locked by a single integration test |

**0 critical issues, 4 warnings, 5 info findings** — all surfaced in the code review (REVIEW.md) and accepted by user as not-blocker for gap-closure shipping.

### Requirements Coverage

Phase 38.7 has no explicit `requirements:` field in PLAN frontmatters (all four gap-closure plans declare `requirements: []`). Phase scope is goal-text-driven (D-01..D-16 + CD-01..CD-06 in 38.7-CONTEXT.md). All 16 decisions + 6 Claude's-discretion items are addressed by plans 02-12 per the plan-summary cross-reference; no orphaned requirements detected.

### Human Verification Required

See frontmatter `human_verification:` section. Seven items pending in-browser confirmation:

1. **TR-B retest** — BK-2 Nachstoss-Parität training-driven modal render (the original UAT failure)
2. **TR-A retest** — Karambol-Liga tournament-driven modal render + PDF
3. **TR-DM** — Dark-mode contrast on tiebreak fieldset
4. **TR-Mob** — Mobile viewport (375px) layout + tap targets
5. **TR-Loc** — Locale switch DE→EN affects modal + PDF text
6. **TR-Idem** — Refresh after winner pick does not re-show fieldset
7. **Plan 12 Tournament.data persist** on real local server (auth-gate + LocalProtector interaction)

The original TR-B failure that triggered the gap-closure cycle has code-level evidence of resolution: Plan 09 carambus.yml flags `tiebreak_on_draw: true` on BK-2 + BK-2kombi presets → the form emits the hidden field → controller whitelists + Bool-coerces → GameSetup's preset-override block fires AFTER the resolver call → `Game.data['tiebreak_required']=true` is baked at start_game → `tiebreak_pick_pending?` sees the flag + ties → marker switch sets `current_element = "tiebreak_winner_choice"` → modal renders the fieldset. Additionally, Plan 11's BK-2kombi BK-2-phase auto-detect fires as a runtime backstop even if the preset path is not taken.

### Gaps Summary

**No gaps remain.** All four UAT gaps from the prior verification (Gap-01 through Gap-04) are closed by plans 09-12; the four code review warnings (WR-01..WR-04) are quality / defense-in-depth concerns explicitly flagged as non-blockers in REVIEW.md and accepted by user (commit `cfdedd4e` "revise plans 09-12 per checker (BLOCKER fix + warnings + infos)").

The phase goal is achieved at the code level. Human verification items are required for the visual / browser / locale-switching / mobile / dark-mode / idempotency aspects that cannot be verified by automated tests — these were the items blocked by TR-B's failure during the prior UAT run, and they need a fresh in-browser pass now that plans 09-12 have shipped.

---

_Verified: 2026-04-30T17:58:04Z_
_Verifier: Claude (gsd-verifier) — Opus 4.7 (1M)_
