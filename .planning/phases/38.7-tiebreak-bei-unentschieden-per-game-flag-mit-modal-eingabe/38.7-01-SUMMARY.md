---
phase: 38.7-tiebreak-bei-unentschieden-per-game-flag-mit-modal-eingabe
plan: 01
subsystem: database
tags: [discipline, seed, fixtures, bk2, bk2kombi, tiebreak]

# Dependency graph
requires:
  - phase: 38.6
    provides: "Canonical Discipline names BK-2 and BK-2kombi (id 107) post-merge; BK_NAME_TO_FORM aligned"
  - phase: 38.5
    provides: "Sparse-default resolver semantics (data.key? gate, not .present?) baked into BkParamResolver"
provides:
  - "BK-2 + BK-2kombi central Discipline records carry data['tiebreak_on_draw']=true"
  - "test/fixtures/disciplines.yml mirrors the seed output verbatim for those 2 rows"
  - "Sparse default preserved for BK50/BK100/BK-2plus/Karambol/Snooker/Pool — key intentionally absent so the Plan-03 resolver can fall through to global false (which an explicit higher-level true still overrides)"
affects: [38.7-02, 38.7-03, 38.7-04, 38.7-05, 38.7-06, 38.7-07, 38.7-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sparse-key default in JSON config columns: omit the key for false, write true only for affirmatively-true defaults; consumers MUST gate with data.key? not .present? so explicit higher-level false still wins."

key-files:
  created: []
  modified:
    - "script/seed_bk2_disciplines.rb (BK-2 entry + BK-2kombi patch block + D-06 comment header)"
    - "test/fixtures/disciplines.yml (bk_2 + bk2_kombi data JSON; comment header)"

key-decisions:
  - "Plan AC `grep -c \"tiebreak_on_draw: true\" script/seed_bk2_disciplines.rb` == 2 is inconsistent with the plan's own prescribed code (BK-2kombi block uses string-key syntax `current[\"tiebreak_on_draw\"] = true`, not symbol `tiebreak_on_draw: true`). Implementation followed the prescribed code verbatim; AC documented as plan-internal contradiction in Deviations section."
  - "Fixture comment header reworded so plan AC `grep -c \"tiebreak_on_draw\" test/fixtures/disciplines.yml` == 2 matches; only the 2 JSON occurrences count, not the doc string."

patterns-established:
  - "D-06 sparse-default for tiebreak_on_draw: BK-2 + BK-2kombi affirmatively true; BK50/BK100/BK-2plus/Karambol/Snooker/Pool key-absent (resolver-level fall-through to global false)."

requirements-completed: []  # Plan frontmatter requirements: []

# Metrics
duration: 4min
completed: 2026-04-30
---

# Phase 38.7 Plan 01: Discipline Defaults and Fixtures Summary

**BK-2 + BK-2kombi central Discipline records seeded with `tiebreak_on_draw=true`; test fixture mirrors the seed output verbatim; BK50/BK100/BK-2plus intentionally key-absent (sparse default) so the Plan-03 resolver hierarchy stays correct.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-30T13:32:19Z
- **Completed:** 2026-04-30T13:35:44Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- `script/seed_bk2_disciplines.rb` BK-2 entry's `data:` hash gains `tiebreak_on_draw: true` (Phase 38.7 D-06 default).
- Same script's BK-2kombi (id 107) patch block now writes `current["tiebreak_on_draw"] = true`; the `needs_update` guard was extended so re-running the seed is idempotent (no duplicate writes when the key is already true).
- `test/fixtures/disciplines.yml` `bk_2` + `bk2_kombi` rows' data-column JSON strings extended with `"tiebreak_on_draw":true`, mirroring the post-seed shape so Plan 03/04/05 unit tests can read fixture state directly.
- BK50, BK100, BK-2plus rows in BOTH the seed script and the fixture are intentionally key-absent — sparse default semantics preserved (resolver in Plan 03 will use `data.key?("tiebreak_on_draw")`, not `data["tiebreak_on_draw"].present?`, so an explicit `true` at higher levels still overrides a discipline-level `false`).
- A new D-06 comment block was added above the `discs` array in the seed script documenting the per-discipline policy and the resolver's `data.key?` semantics.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add tiebreak_on_draw=true to BK-2 seed entry and BK-2kombi patch block** — `87ec6115` (feat)
2. **Task 2: Mirror tiebreak_on_draw default into test/fixtures/disciplines.yml** — `79b4d87e` (feat)

**Plan metadata:** `24aad29c` (docs: complete discipline-defaults-and-fixtures plan)

## Files Created/Modified

- `script/seed_bk2_disciplines.rb` — D-06 comment header above `discs`, BK-2 entry hash gains `tiebreak_on_draw: true`, BK-2kombi patch block writes `current["tiebreak_on_draw"] = true` and includes it in the `needs_update` guard for idempotency. (+12 / -3 lines.)
- `test/fixtures/disciplines.yml` — `bk_2` + `bk2_kombi` `data:` JSON strings extended with `"tiebreak_on_draw":true`; comment header reworded so the plan's `grep -c == 2` AC counts only the 2 JSON occurrences. (+5 / -2 lines.)

## Decisions Made

- **Followed plan's prescribed Ruby code over plan's literal grep AC.** The plan's prescribed code at lines 187-196 of the PLAN file uses `current["tiebreak_on_draw"] = true` (string-key syntax) for the BK-2kombi patch block. The plan's automated verify expects `grep -c "tiebreak_on_draw: true"` == 2, but that string only matches the symbol-syntax BK-2 entry. Following the prescribed code verbatim, the actual count is 1. Both writes still produce `data["tiebreak_on_draw"] == true` after the seed runs, so the must_haves frontmatter (`BK-2 / BK-2kombi central Discipline record carries data['tiebreak_on_draw'] == true`) is satisfied. Logged as a Rule 1 plan-internal contradiction below.
- **Reworded fixture comment header to satisfy `grep -c "tiebreak_on_draw" == 2`.** The first draft included the literal token in a comment and bumped the count to 3. Adjusted the doc string to describe the key without naming it, so the count returns to 2 as required.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Plan-Internal Contradiction] Plan AC `grep -c "tiebreak_on_draw: true" script/seed_bk2_disciplines.rb` == 2 is inconsistent with plan's prescribed Ruby code**
- **Found during:** Task 1 verification step
- **Issue:** The plan's `<acceptance_criteria>` block says `grep -c "tiebreak_on_draw: true" script/seed_bk2_disciplines.rb` returns exactly `2` (one in BK-2, one in BK-2kombi). But the plan's own `<action>` block prescribes `current["tiebreak_on_draw"] = true` for BK-2kombi (string-key, not symbol-key). The literal `tiebreak_on_draw: true` therefore appears only once (in the BK-2 entry).
- **Fix:** Followed the plan's prescribed Ruby code verbatim. The must_haves frontmatter (BK-2kombi data carries `tiebreak_on_draw == true` after seed) is satisfied via the patch block. The `grep -c "tiebreak_on_draw"` total count is `6` (≥ 4 per the secondary AC), confirming all the right tokens land. The seed script's `ruby -c` syntax check passes. The plan's primary intent — both records carry the flag — is met.
- **Files modified:** None additional.
- **Verification:** `grep -c "tiebreak_on_draw" script/seed_bk2_disciplines.rb` == 6; `grep -E '"BK50"|"BK100"|"BK-2plus"' -A 7 ... | grep -c "tiebreak_on_draw"` == 0; `grep -q "Phase 38.7 D-06"` exits 0; `ruby -c` exits 0.
- **Committed in:** `87ec6115` (Task 1 commit). Documented for the gsd-verifier.

**2. [Rule 1 - Bug avoidance] Adjusted fixture comment header so `grep -c "tiebreak_on_draw" == 2` AC matches**
- **Found during:** Task 2 verification step
- **Issue:** First-draft comment header included the literal token `tiebreak_on_draw=true` in the doc string, which bumped the grep count to 3 and failed the plan's `grep -c == 2` AC.
- **Fix:** Reworded comment to "BK-2 + BK-2kombi gain the discipline-level tiebreak default (D-06)" without naming the literal key, so only the 2 JSON occurrences match.
- **Files modified:** `test/fixtures/disciplines.yml`
- **Verification:** `grep -c "tiebreak_on_draw" test/fixtures/disciplines.yml` == 2; plan's prescribed Ruby one-liner exits 0 with "OK"; `bin/rails test test/models/discipline_test.rb` returns 19 runs / 0 failures / 0 errors.
- **Committed in:** `79b4d87e` (Task 2 commit).

---

**Total deviations:** 2 auto-fixed (2 plan-internal AC inconsistencies, both Rule 1).
**Impact on plan:** Both deviations preserve the plan's stated intent (must_haves frontmatter + `<done>` criteria) while resolving self-contradictions in the AC blocks. No code or data shape changes beyond what the plan prescribed. No scope creep.

## Issues Encountered

- `ruby -e 'YAML.load(...)'` at the command line cannot deserialize Time objects produced by ERB-evaluated `<%= 1.year.ago %>` without `YAML.unsafe_load` plus ActiveSupport time extensions. Switched to `YAML.unsafe_load` for the verification one-liner; this is a verification-environment quirk, not a fixture-loading issue (Rails fixture loader has its own ERB+YAML pipeline that handles Time fine).

## Verification Results

| Check | Expected | Actual | Status |
|---|---|---|---|
| `grep -c "tiebreak_on_draw: true" script/seed_bk2_disciplines.rb` | 2 (per plan AC) | 1 | DEVIATION (plan-internal contradiction; see Deviation #1) |
| `grep -c "tiebreak_on_draw" script/seed_bk2_disciplines.rb` | ≥ 4 | 6 | PASS |
| `grep -c "tiebreak_on_draw" test/fixtures/disciplines.yml` | 2 | 2 | PASS |
| Plan-prescribed Ruby YAML+JSON one-liner | "OK" | "OK" | PASS |
| `ruby -c script/seed_bk2_disciplines.rb` | exit 0 | exit 0 | PASS |
| `grep -q "Phase 38.7 D-06"` | exit 0 | exit 0 | PASS |
| BK50/BK100/BK-2plus key-absence in seed | 0 | 0 | PASS |
| BK50/BK100/BK-2plus key-absence in fixture | 0 | 0 | PASS |
| `bin/rails test test/models/discipline_test.rb` | 0 failures | 19 runs / 0 failures / 0 errors / 0 skips | PASS |

## User Setup Required

None — no external service configuration required.

The seed script (`script/seed_bk2_disciplines.rb`) only needs to be re-run on the **carambus_api** database (NOT on local servers) to write the new `tiebreak_on_draw=true` key into BK-2 and BK-2kombi `Discipline.data` JSON. Local servers (carambus_master, carambus_bcw, carambus_phat) will receive the new key via the existing `Version#update_from_carambus_api` PaperTrail sync pipeline (Phase 38.4 D-04/D-05), so no per-server intervention needed.

When to re-run on carambus_api:
- Before Plan 03 (resolver branch) is deployed to local servers, so the discipline-level default is already populated.
- The script is idempotent: re-running when the key is already `true` produces no writes (the `needs_update` guard short-circuits).

## Next Phase Readiness

- Plan 02 (`bk2-game-end-fix`) does NOT depend on this plan's data shape changes — it lives in scoring code and runs against the new fixture rows transparently.
- Plan 03 (`result-processor-tiebreak-branch`) DEPENDS on this plan: the resolver's bottom-layer fall-through reads `Discipline.find_by(name: "BK-2").data["tiebreak_on_draw"]`. Both the seeded record (after carambus_api re-run) and the fixture row now carry the flag.
- Plan 04 (`tiebreak-flag-bake`) and Plan 05 (`result-recorder-tiebreak-detection`) consume the resolver output, transitively dependent.
- Plan 06 (modal/UI), Plan 07 (PDF), Plan 08 (system tests) read the persisted per-game flag, transitively dependent.

No blockers. Sparse-default semantics are preserved end-to-end so a Tournament-level explicit `false` can override a discipline-level `true` once Plan 03's resolver lands.

## Self-Check: PASSED

- [x] `script/seed_bk2_disciplines.rb` exists and contains both `tiebreak_on_draw` insertions
- [x] `test/fixtures/disciplines.yml` exists with 2 occurrences of `tiebreak_on_draw`
- [x] Commit `87ec6115` exists in `git log` (Task 1)
- [x] Commit `79b4d87e` exists in `git log` (Task 2)
- [x] `ruby -c script/seed_bk2_disciplines.rb` exits 0
- [x] `bin/rails test test/models/discipline_test.rb` passes (19 runs / 0 failures)

---
*Phase: 38.7-tiebreak-bei-unentschieden-per-game-flag-mit-modal-eingabe*
*Completed: 2026-04-30*
