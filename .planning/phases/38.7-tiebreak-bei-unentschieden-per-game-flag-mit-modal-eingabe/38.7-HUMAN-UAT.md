---
status: diagnosed
phase: 38.7-tiebreak-bei-unentschieden-per-game-flag-mit-modal-eingabe
source: [38.7-VERIFICATION.md]
started: 2026-05-01T00:10:00Z
updated: 2026-05-01T01:10:00Z
note: |
  All 12 code-level must-haves verified. The TR-B end-to-end lifecycle (winner pick → submit
  event on form → StimulusReflex WebSocket roundtrip → reflex confirm_result → game.data
  persist → AASM advance → set close → match advance) cannot be auto-verified — the live
  WebSocket roundtrip is non-deterministic in headless Selenium. Plan 13 locks the wiring
  contract on both sides of the WebSocket boundary (G1/G2/G3 render-level + G4 request-level
  GET-fallback no-op), but the live roundtrip itself requires a real browser session.
---

## Current Test

[testing complete]

## Tests

### 1. TR-B retest — full Gap-05 lifecycle (primary)
expected: BK-2 quick-game with `tiebreak_on_draw: true`; both players reach balls_goal in 1 inning each → modal opens with tiebreak fieldset → operator picks Sieger A or B → clicks "Mit Sieger bestätigen" → set is closed (NOT a modal re-open loop) → match advances to next set or final result; PDF Spielbericht shows `(Stechen <name>)` indicator on the closed set
result: pass
reported: "Nach Abschluss mit ausgewähltem Spieler endet das Spiel und neues wird sofort gestartet"

### 2. TR-Ctl — Untied result regression (control)
expected: a non-tied final score still ends normally without modal interruption (no tiebreak fieldset); "Mit Ergebnis bestätigen" closes set as usual; was blocked by Gap-05 trap, now unblocked after Plan 13
result: issue
reported: "Spiel BK-2; Spieler macht in der zweiten Aufnahme aus. Jetzt sollte das Spiel eigentlich beendet sein. Springt aber zu Spieler B ohne die rote Message Nachstoß. Nach wechsel beendet das Spiel"
severity: major

### 3. TR-A retest — Karambol-Liga tournament-driven modal + PDF
expected: tied score in 30 innings opens modal with tiebreak fieldset; pick winner; PDF shows `(Stechen <name>)`; tournament path (resolver Levels 1-2 active)
result: skipped
reason: "Tournament setup is too time-consuming for this UAT session; user requested training-mode-only verification today (training-mode tests TR-B + TR-Ctl + TR-Mob + TR-Loc + TR-Idem cover the same code path; the tournament resolver Level 1-2 path is unit-tested in plans 04 + 12)"

### 4. TR-Mob — Mobile viewport (375px)
expected: tiebreak fieldset renders correctly at 375px viewport; radios + submit button reachable; layout not broken
result: skipped
reason: "Real-phone test would require a production deploy; user prefers to defer deployment until the Test-2 BK-2 Nachstoß issue is fixed (avoid shipping a regression-adjacent change). Will retest after the gap-closure follow-up phase lands"

### 5. TR-Loc — Locale switch DE→EN
expected: tiebreak fieldset labels render correctly in both DE ("Stechen — Sieger festlegen", "Mit Sieger bestätigen") and EN; switch DE→EN mid-flow still works
result: skipped
reason: "Scoreboard auto-switches to DE on entering the Quick Start Page — locale cannot be held on EN through training-mode setup. Pre-existing locale-switcher behavior, unrelated to phase 38.7. EN labels for the tiebreak fieldset are unit-tested via the i18n keys added in plans 06/07/10/12 (en.yml entries verified by code review)"

### 6. TR-Idem — Refresh after winner pick
expected: after picking tiebreak winner + submit + page refresh, modal does NOT re-show the tiebreak fieldset; `current_element` advanced past `tiebreak_winner_choice`; no infinite-loop regression
result: pass

### 7. Plan 12 Tournament.data persist on real local server
expected: TournamentMonitor startup-form checkbox persists `Tournament.data['tiebreak_on_draw']` correctly on a real DB write; resolver Level 1 picks it up at every game's start_game; verified via Administrate or Rails console after form submit
result: skipped
reason: "Tournament setup is too time-consuming for this UAT session; user requested training-mode-only verification today. Plan 12 has 3 controller tests covering the persist path (`update_columns` + organizer-presence bypass) — automated coverage stands in for manual DB verification this session"

## Summary

total: 7
passed: 2
issues: 1
pending: 0
skipped: 4
blocked: 0

## Gaps

- truth: "BK-2 untied final inning: when Anstoss reaches balls_goal in inning ≥ 2, the set must end immediately (no further Nachstoss-Aufnahme since Erste-Aufnahme-Gate failed); user-visible: no red 'Nachstoß' banner appears, no Player-B turn occurs"
  status: failed
  reason: "User reported: 'Spiel BK-2; Spieler macht in der zweiten Aufnahme aus. Jetzt sollte das Spiel eigentlich beendet sein. Springt aber zu Spieler B ohne die rote Message Nachstoß. Nach wechsel beendet das Spiel'"
  severity: major
  test: 2
  root_cause: "Three-way semantic mismatch in BK-2 / BK-2kombi-SP path: TableMonitor#follow_up? correctly enforces the Erste-Aufnahme-Gate (Nachstoß only when Anstoss reaches goal in inning 1, suppressing the banner in inning ≥ 2), BUT TableMonitor#end_of_set? is missing the matching 'Anstoss-at-goal in inning ≥ 2 → close immediately' branch. Plan 02's D-02 branch requires +1 innings lag; legacy karambol branch needs innings parity or allow_follow_up=false (BK-2 forces it true). Set stays open silently; ScoreEngine#terminate_inning_data switches active_player unconditionally → Player B plays an unauthorized inning before legacy parity gate finally closes. Pre-existing latent defect (commit 79328663, 2026-04-28, before phase 38.7). NOT a regression from plans 02/05/13."
  artifacts:
    - path: "app/models/table_monitor.rb"
      lines: "1456-1521"
      issue: "end_of_set? missing 4th BK-2 branch: 'bk_with_nachstoss && anstoss_at_goal && anstoss_innings >= 2 → return true'"
    - path: "app/models/table_monitor.rb"
      lines: "1188-1190"
      issue: "(reference, do not modify) follow_up? Erste-Aufnahme-Gate is correct"
    - path: "app/models/table_monitor/score_engine.rb"
      lines: "1306"
      issue: "(reference) terminate_inning_data switches active_player unconditionally — works fine when end_of_set? closes the set first; current bug is that end_of_set? returns false"
    - path: "app/views/table_monitors/_player_score_panel.html.erb"
      lines: "125-127, 185-187"
      issue: "(reference, do not modify) banner conditional is correct — it relies on follow_up?, which correctly suppresses the banner; the bug is upstream in end_of_set?"
  missing:
    - "Add a BK-2 / BK-2kombi-SP branch to TableMonitor#end_of_set? mirroring follow_up?'s Erste-Aufnahme-Gate: when bk_with_nachstoss && anstoss_at_goal && anstoss_innings >= 2 → return true (set closes immediately, ScoreEngine never reaches terminate_inning_data with the wrong state)"
    - "RED-then-GREEN test pair: 2 new BK-2 set-end tests in test/models/table_monitor_test.rb (one for inning-2 anstoss-at-goal closing immediately, one for inning-1 anstoss-at-goal correctly NOT closing — Erste-Aufnahme-Gate happy path regression guard)"
  debug_session: ".planning/debug/bk2-nachstoss-banner-missing.md"
