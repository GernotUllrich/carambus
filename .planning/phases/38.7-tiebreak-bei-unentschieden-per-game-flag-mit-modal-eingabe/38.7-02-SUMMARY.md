---
phase: 38.7-tiebreak-bei-unentschieden-per-game-flag-mit-modal-eingabe
plan: 02
subsystem: scoring
tags: [bk2, bk2kombi, end_of_set, nachstoss, tiebreak, table_monitor]

# Dependency graph
requires:
  - phase: 38.4
    provides: "bk2_kombi_current_phase predicate (helper for BK-2kombi DZ↔SP per-set switching) and BK-Familie no_followup_phase override in end_of_set?"
  - phase: 38.5
    provides: "BkParamResolver hierarchy + per-set effective_discipline; not directly consumed in this plan but establishes the substrate Plan 03 will read."
provides:
  - "TableMonitor#end_of_set? extended with BK-2 / BK-2kombi-SP Nachstoss-completion close (no_followup_phase carve-out)"
  - "5 characterization tests in test/models/table_monitor_test.rb pinning the legacy + BK-2 + BK-2kombi-SP + regression-guard surface (RED-then-GREEN protocol preserved across 2 commits)"
affects: [38.7-03, 38.7-04, 38.7-05, 38.7-06, 38.7-07, 38.7-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Extend-before-build (SKILL): small guard inside legacy predicate over a parallel state machine. Validated 2026-04-29 BK2 cleanup (-1463 LOC); reapplied here as +37 LOC end_of_set? branch instead of a new lifecycle hook."

key-files:
  created: []
  modified:
    - "app/models/table_monitor.rb (+37 lines: new bk_with_nachstoss branch in end_of_set?, between no_followup_phase block and legacy karambol balls_goal branch)"
    - "test/models/table_monitor_test.rb (+76 lines: build_bk_data helper + 5 new test methods covering BK-2, BK-2kombi-SP, equal-innings legacy path, Nachstoss-not-at-goal, and no-goal regression guards)"

key-decisions:
  - "Plan author predicted 2 RED failures before Task 2; actual RED state had 3 failures. Test 4 (Nachstoss not at goal, innings 5 vs 6) was predicted to pass via legacy karambol gate but actually fails today because the legacy gate requires innings parity OR allow_follow_up=false — the 5-vs-6 mismatch with allow_follow_up=true breaks both. Task 2's new branch covers this case correctly (anstoss_at_goal && nachstoss_innings == anstoss+1), so all 3 RED failures turn GREEN under the same fix. Documented as plan-internal RED-prediction error, not a code-level deviation."
  - "Anstoss role identification: use data['current_kickoff_player'].presence with 'playera' fallback. The build_bk_data helper does NOT set current_kickoff_player, so all 5 tests treat playera as Anstoss; this matches the test fixtures' setup (playera at higher result + lower innings, playerb +1 inning as Nachstoss-Aufnahme)."
  - "no_followup_phase block kept untouched at the top of the BK-Familie ladder (BK-2plus/BK-2kombi DZ/BK50/BK100 immediate-close); new branch inserted strictly BELOW it so the existing immediate-close path stays the dominant rule for those disciplines."

patterns-established:
  - "BK-2 / BK-2kombi-SP set close: Anstoss at balls_goal AND Nachstoss innings == Anstoss innings + 1 → end_of_set? returns true regardless of Nachstoss score. Plan 04 layers tiebreak detection on top of this now-firing predicate for the score-equal subcase."

requirements-completed: []  # Plan frontmatter requirements: []

# Metrics
duration: 3min
completed: 2026-04-30
---

# Phase 38.7 Plan 02: BK-2 Game-End Fix Summary

**TableMonitor#end_of_set? extended with a 37-line BK-2 / BK-2kombi-SP Nachstoss-completion close branch — closes the D-02 deadlock where the legacy karambol innings-parity gate failed when the Nachstoss-Spieler reached balls_goal in his single follow-up inning. RED-then-GREEN sequence preserved in 2 atomic commits; 5 new characterization tests pin the surface.**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-04-30T13:41:04Z
- **Completed:** 2026-04-30T13:43:39Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- **Task 1 (RED):** Appended 5 new tests + a `build_bk_data` helper to `test/models/table_monitor_test.rb` (lines 100-176). Tests cover (1) BK-2 equal-innings legacy gate, (2) BK-2 D-02 fix, (3) BK-2kombi-SP D-02 fix, (4) Nachstoss-not-at-goal regression guard, (5) no-goal regression guard.
- **Task 2 (GREEN):** Inserted a new `bk_with_nachstoss` branch in `TableMonitor#end_of_set?` between the existing `no_followup_phase` block (lines 1452-1462) and the legacy karambol balls_goal branch (line 1501+). The branch fires when `data["free_game_form"]` is `"bk_2"` OR (`"bk2_kombi"` AND `bk2_kombi_current_phase == "serienspiel"`), Anstoss reached `balls_goal`, and Nachstoss innings counter is exactly Anstoss + 1.
- **All 5 Plan 02 tests GREEN** after Task 2 (`bin/rails test test/models/table_monitor_test.rb` reports 14 runs / 0 failures / 0 errors).
- **No regressions** in the 41-test TableMonitor characterization suite or the 30-test combined run of `result_recorder_test.rb` + `game_setup_test.rb` + `bk_param_latent_bugs_test.rb`.
- **Standardrb clean** on the changed lines (pre-existing violations on lines 1507+ untouched).

## Task Commits

Each task committed atomically:

1. **Task 1: RED — characterization tests for D-02 BK-2 deadlock** — `2262fd36` (test)
2. **Task 2: GREEN — extend end_of_set? to close BK-2 / BK-2kombi-SP after Nachstoss-Aufnahme** — `1d2d236f` (fix)

## Files Created/Modified

- `app/models/table_monitor.rb` — `+37 / -0` lines. New `bk_with_nachstoss` branch in `end_of_set?` between the `no_followup_phase` block (line 1462) and the legacy karambol balls_goal branch (now line 1501). Branch resolves the D-02 deadlock; comment block above it documents the bug semantics, the SKILL extend-before-build rationale, and why the fix is a guard (not a parallel state machine).
- `test/models/table_monitor_test.rb` — `+76 / -0` lines. `build_bk_data` helper near the bottom of the class + 5 new `test "..."` methods covering the BK-2 / BK-2kombi-SP Nachstoss surface and 2 regression guards.

## Decisions Made

- **RED-prediction error documented, not flagged as code-level bug.** Plan said 2 RED failures (Tests 2, 3); actual RED state had 3 failures (Tests 2, 3, 4). Test 4's setup (playera.innings=5, playerb.innings=6, allow_follow_up=true, no equal innings) breaks both halves of the legacy gate `(innings_a == innings_b OR !allow_follow_up)`, which the plan author overlooked. Task 2's new branch correctly handles all 3 cases under one rule, so the plan's primary intent (5 tests GREEN after Task 2) is satisfied. Logged as plan-internal RED-prediction error (no impact on shipped code).
- **Anstoss role identification.** Used `data['current_kickoff_player'].presence` with `'playera'` fallback. Matches the existing `follow_up?` helper at line 1164 which uses the same pattern. The `build_bk_data` helper omits `current_kickoff_player`, so playera is treated as Anstoss in all 5 tests. Test fixtures align with this assumption (playera at +1 result, playerb at +1 inning Nachstoss-Aufnahme).
- **Branch placement.** Inserted strictly BELOW the existing `no_followup_phase` block. This keeps the BK-2plus / BK-2kombi DZ / BK50 / BK100 immediate-close path dominant for those disciplines and ensures the new branch only fires for BK-2 and BK-2kombi-SP — exactly the surface where the deadlock exists.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Plan-Internal Red-Prediction Error] Plan predicted 2 RED failures; actual RED state had 3**
- **Found during:** Task 1 verification (running tests in RED phase)
- **Issue:** The plan's `<acceptance_criteria>` for Task 1 says `bin/rails test ... shows 2 failures (the two D-02 tests, NOT regression guards)`. Actual run showed 3 failures: Test 2 (BK-2 D-02 fix), Test 3 (BK-2kombi-SP D-02 fix), AND Test 4 (Nachstoss-not-at-goal regression guard). Test 4 was predicted to PASS via legacy karambol gate, but the gate `(innings_a == innings_b OR !allow_follow_up)` requires either innings parity (5 != 6 in this fixture) or `allow_follow_up=false` (we set true in build_bk_data) — so both halves of the OR are false today, gate fails, end_of_set? returns false in RED phase.
- **Fix:** None at code level. The plan's `<action>` block for Task 2 explicitly anticipates Test 4 turning GREEN via the new branch (text: "NEW branch must fire because anstoss_at_goal=true AND nachstoss_innings == anstoss_innings + 1. PASSES via new branch."). So the plan's RED prediction is internally inconsistent with its own GREEN prediction, but the code Task 2 prescribes correctly handles all 3 cases under one rule. Followed the prescribed code; documented the RED prediction error here for the gsd-verifier.
- **Files modified:** None additional.
- **Verification:** Post-Task-2 run shows 14 runs / 0 failures / 0 errors on `bin/rails test test/models/table_monitor_test.rb`.
- **Committed in:** `2262fd36` (Task 1 RED commit message documents the 3-vs-2 discrepancy explicitly).

---

**Total deviations:** 1 auto-fixed (1 plan-internal acceptance-criterion inconsistency, Rule 1 plan-internal contradiction).
**Impact on plan:** Zero. The plan's stated intent (5 tests GREEN after Task 2 + extend-before-build SKILL applied + no regressions) is fully satisfied. The RED-vs-GREEN test count discrepancy is a documentation issue inside the plan's own action+acceptance text, not a code defect.

## Issues Encountered

None blocking. The fixture name in the plan (`table_monitors(:tm_one)`) does not exist in `test/fixtures/table_monitors.yml` (which has `:one`, `:two`, `:free`). The existing test class already uses `@tm = TableMonitor.create!(state: "new", data: {})` in its `setup` block; the new tests reuse `@tm` via `@tm.data = build_bk_data(...)`. This matches the project's existing pattern and avoids a fixture-naming deviation.

## Verification Results

| Check | Expected | Actual | Status |
|---|---|---|---|
| `bin/rails test test/models/table_monitor_test.rb` | 0 failures | 14 runs / 15 assertions / 0 failures / 0 errors / 0 skips | PASS |
| `bin/rails test test/characterization/table_monitor_char_test.rb` | 0 failures | 41 runs / 75 assertions / 0 failures / 0 errors / 0 skips | PASS |
| `bin/rails test test/services/table_monitor/result_recorder_test.rb test/services/table_monitor/game_setup_test.rb test/integration/bk_param_latent_bugs_test.rb` | 0 failures | 30 runs / 75 assertions / 0 failures / 0 errors / 0 skips | PASS |
| `grep -q "Phase 38.7 Plan 02 — D-02 BK-2" app/models/table_monitor.rb` | exit 0 | exit 0 | PASS |
| `grep -q "bk_with_nachstoss" app/models/table_monitor.rb` | exit 0 | exit 0 | PASS |
| `bundle exec standardrb app/models/table_monitor.rb` (changed lines only) | 0 new violations | 0 new violations on lines 1464-1499; 28 pre-existing violations on untouched lines (1507+) | PASS |
| Git history shows 2 distinct commits (RED + GREEN) | yes | `2262fd36` (test, RED) + `1d2d236f` (fix, GREEN) | PASS |

## User Setup Required

None — no external service configuration required.

The fix is pure scoring-engine logic in `app/models/table_monitor.rb`; deployment to the carambus_api production database happens via the existing `Version#update_from_carambus_api` PaperTrail sync pipeline once the master branch is deployed. Local servers (carambus_master, carambus_bcw, carambus_phat) pick up the new code on next `cap deploy`.

No data migration needed: the new branch reads `data["free_game_form"]`, `data["playera"|"playerb"]["result"|"innings"|"balls_goal"]`, and `data["current_kickoff_player"]` — all keys that already exist in the live data shape produced by `start_game`.

## Known Stubs

None. The branch is wired end-to-end: it reads from `data` (already populated), uses the existing `bk2_kombi_current_phase` helper, and returns a final-decision boolean. No placeholder/TODO/empty-default exits.

## Next Phase Readiness

- **Plan 03 (`result-processor-tiebreak-branch`)** can now layer the tiebreak-detection branch on top of the now-firing `end_of_set?`. The contract is: when `end_of_set?` returns true AND scores are equal AND the Discipline-resolved `tiebreak_on_draw` flag is true, route to the tiebreak modal instead of finalizing the set normally.
- **Plans 04-08** (resolver bake, modal UI, PDF rendering, system tests) all depend transitively on `end_of_set?` being firing for the Nachstoss-Aufnahme deadlock case. That dependency is now satisfied.
- **No blockers.** The extend-before-build SKILL was applied as prescribed: +37 lines guard, no parallel state machine, no scope creep. Pre-existing TableMonitor characterization tests confirm zero regression.

## Self-Check: PASSED

- [x] `app/models/table_monitor.rb` exists and contains `Phase 38.7 Plan 02 — D-02 BK-2` comment block (lines 1464-1478)
- [x] `app/models/table_monitor.rb` contains `bk_with_nachstoss` variable (line 1479)
- [x] `test/models/table_monitor_test.rb` exists with 5 new test methods (lines 105-176) and `build_bk_data` helper (lines 110-122)
- [x] Commit `2262fd36` (Task 1 RED) exists in git log: `git log --oneline | grep 2262fd36` returns the expected line
- [x] Commit `1d2d236f` (Task 2 GREEN) exists in git log: `git log --oneline | grep 1d2d236f` returns the expected line
- [x] `bin/rails test test/models/table_monitor_test.rb` reports 14 runs / 0 failures / 0 errors
- [x] `bin/rails test test/characterization/table_monitor_char_test.rb` reports 41 runs / 0 failures / 0 errors
- [x] No new standardrb violations on the changed lines (1464-1499)

---
*Phase: 38.7-tiebreak-bei-unentschieden-per-game-flag-mit-modal-eingabe*
*Completed: 2026-04-30*
