---
phase: 15-high-risk-extractions
plan: "02"
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/services/tournament_monitor/table_populator.rb
  - test/services/tournament_monitor/table_populator_test.rb
  - app/models/tournament_monitor.rb
  - lib/tournament_monitor_support.rb
  - lib/tournament_monitor_state.rb
autonomous: true
requirements: [TMEX-04]
must_haves:
  truths:
    - "TournamentMonitor::TablePopulator exists with populate_tables, do_placement, initialize_table_monitors, and do_reset_tournament_monitor"
    - "do_reset_tournament_monitor save count is preserved exactly — no fewer and no more saves than original"
    - "cattr_accessor allow_change_tables is set via TournamentMonitor.allow_change_tables (not self.) in the service"
    - "Instance variables (@placements, @placement_candidates, etc.) are service-local, initialized from @tournament_monitor.data"
    - "populate_tables called from do_reset_tournament_monitor stays intra-service (not round-trip through model)"
    - "AASM after_enter on new_tournament_monitor calls model wrapper which delegates to service"
    - "All Phase 11-12 characterization tests pass unchanged after extraction"
    - "lib/tournament_monitor_support.rb is empty or removed (D-11)"
  artifacts:
    - path: "app/services/tournament_monitor/table_populator.rb"
      provides: "Table population algorithm service"
      contains: "class TournamentMonitor::TablePopulator"
    - path: "test/services/tournament_monitor/table_populator_test.rb"
      provides: "Unit tests for TablePopulator public methods"
      contains: "class TournamentMonitor::TablePopulatorTest"
  key_links:
    - from: "app/models/tournament_monitor.rb"
      to: "app/services/tournament_monitor/table_populator.rb"
      via: "delegation wrappers for do_reset_tournament_monitor, populate_tables, initialize_table_monitors"
      pattern: "TablePopulator\\.new\\(self\\)"
    - from: "app/services/tournament_monitor/table_populator.rb"
      to: "app/models/tournament_monitor.rb"
      via: "@tournament_monitor for data access, AASM events, and accumulate_results delegation"
      pattern: "@tournament_monitor\\."
---

<objective>
Extract TablePopulator from TournamentMonitor lib modules — the table population algorithm including populate_tables, do_placement, initialize_table_monitors, and do_reset_tournament_monitor. Clean up lib modules after extraction.

Purpose: Remove the most complex algorithm (500+ lines) from lib modules into a testable service. After this extraction, lib/tournament_monitor_support.rb should be empty and removable per D-11.
Output: TablePopulator service + unit tests + delegation wrappers + lib module cleanup
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-high-risk-extractions/15-CONTEXT.md
@.planning/phases/15-high-risk-extractions/15-RESEARCH.md
@.planning/phases/15-high-risk-extractions/15-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/models/tournament_monitor.rb (after Plan 01):
- Delegation wrappers for report_result, accumulate_results, update_ranking, update_game_participations (Plan 01)
- `def deep_merge_data!(hash)` — model method, called by service as @tournament_monitor.deep_merge_data!
- `def current_round` / `current_round!` / `incr_current_round!` / `decr_current_round!` — model methods
- `def next_seqno` — model method
- `def player_id_from_ranking(rule_str, opts)` — delegates to RankingResolver
- `cattr_accessor :allow_change_tables` — class-level, service must use `TournamentMonitor.allow_change_tables =`
- AASM: `start_playing_finals!`, `start_playing_groups!` — called on @tournament_monitor from service

From app/services/tournament_monitor/result_processor.rb (Plan 01 output):
- `accumulate_results` — public method, called via @tournament_monitor.accumulate_results delegation

From lib/tournament_monitor_support.rb — methods to extract (D-06):
- `populate_tables` line 407 — complex algorithm with @placements, @placement_candidates ivars
- `do_placement(game, r_no, t_no, sets, balls, innings)` line 855
- `initialize_table_monitors` line 370

From lib/tournament_monitor_state.rb — methods to extract (D-07):
- `do_reset_tournament_monitor` line 201 — AASM after_enter entry point, longest method

State query methods staying on model/lib (D-08):
- `group_phase_finished?` (state.rb:171) — called from PartyMonitor
- `finals_finished?` (state.rb:183) — called from report_result
- `all_table_monitors_finished?` (state.rb:115) — called from PartyMonitor
- `table_monitors_ready?` (state.rb:190) — no external callers confirmed
- `finalize_round` (state.rb:120) — called from PartyMonitor
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TablePopulator service with all extracted methods + unit tests</name>
  <files>
    app/services/tournament_monitor/table_populator.rb
    test/services/tournament_monitor/table_populator_test.rb
  </files>
  <read_first>
    lib/tournament_monitor_support.rb (lines 370-1078 for initialize_table_monitors, populate_tables, do_placement)
    lib/tournament_monitor_state.rb (lines 201-521 for do_reset_tournament_monitor)
    app/services/tournament_monitor/result_processor.rb (sibling service pattern from Plan 01)
    app/services/tournament_monitor/ranking_resolver.rb (PORO pattern reference)
    app/models/tournament_monitor.rb (model interface after Plan 01)
    test/services/tournament_monitor/ranking_resolver_test.rb (test pattern reference)
  </read_first>
  <behavior>
    - Test 1: TablePopulator.new(tournament_monitor) creates instance with @tournament_monitor accessor
    - Test 2: initialize_table_monitors creates/updates table_monitor records for tournament tables
    - Test 3: populate_tables initializes @placements from @tournament_monitor.data and calls do_placement
    - Test 4: do_reset_tournament_monitor orchestrates the full reset sequence including populate_tables
    - Test 5: cattr_accessor allow_change_tables is set via TournamentMonitor.allow_change_tables (not self.)
  </behavior>
  <action>
    Create `app/services/tournament_monitor/table_populator.rb` as a plain class (NOT ApplicationService — follows RankingResolver pattern, multiple public entry points).

    Per D-06: Receives TournamentMonitor instance in constructor. Store as `@tournament_monitor`.

    Extract from `lib/tournament_monitor_support.rb` (D-06):
    - `populate_tables` — PUBLIC. Contains the core game assignment algorithm.
    - `do_placement(game, r_no, t_no, sets, balls, innings)` — private
    - `initialize_table_monitors` — PUBLIC

    Extract from `lib/tournament_monitor_state.rb` (D-07):
    - `do_reset_tournament_monitor` — PUBLIC. AASM after_enter entry point. Calls `populate_tables` INTERNALLY (not via round-trip through model — Pitfall C for intra-service calls). Calls `initialize_table_monitors` internally too.

    **self-to-@tournament_monitor conversion** (per D-09):
    - Every bare `tournament` -> `@tournament_monitor.tournament`
    - Every bare `reload` -> `@tournament_monitor.reload`
    - Every bare `save!` -> `@tournament_monitor.save!`
    - Every bare `update(...)` -> `@tournament_monitor.update(...)`
    - Every bare `deep_merge_data!(h)` -> `@tournament_monitor.deep_merge_data!(h)`
    - Every bare `data` -> `@tournament_monitor.data`
    - Every bare `data_will_change!` -> `@tournament_monitor.data_will_change!`
    - Every bare `current_round` -> `@tournament_monitor.current_round`
    - Every bare `current_round!(n)` -> `@tournament_monitor.current_round!(n)`
    - Every bare `table_monitors` -> `@tournament_monitor.table_monitors`
    - Every bare `next_seqno` -> `@tournament_monitor.next_seqno`
    - Every bare `new_record?` -> `@tournament_monitor.new_record?`
    - Every bare `id` (TM id) -> `@tournament_monitor.id`
    - `player_id_from_ranking(...)` -> `@tournament_monitor.player_id_from_ranking(...)` (delegates to RankingResolver)
    - `accumulate_results` -> `@tournament_monitor.accumulate_results` (delegates to ResultProcessor via Plan 01 wrapper)
    - AASM events per D-02: `start_playing_finals!` -> `@tournament_monitor.start_playing_finals!`, `start_playing_groups!` -> `@tournament_monitor.start_playing_groups!`
    - `tournament.signal_tournament_monitors_ready!` -> `@tournament_monitor.tournament.signal_tournament_monitors_ready!`

    **Pitfall D — cattr_accessor:** Replace `self.allow_change_tables = false` with `TournamentMonitor.allow_change_tables = false` and `self.allow_change_tables = true` with `TournamentMonitor.allow_change_tables = true`. Also replace reads: `self.allow_change_tables` -> `TournamentMonitor.allow_change_tables`.

    **Pitfall E — self references in initialize_table_monitors:** Replace `tournament_monitor: self` with `tournament_monitor: @tournament_monitor`.

    **Instance variables** (per research Pattern 3): `@placements`, `@placement_candidates`, `@placements_done`, `@groups`, `@tournament_plan`, `@table`, `@table_monitor` become service-local ivars. `@tournament_plan` is set in `do_reset_tournament_monitor` and used in the same service instance — no round-trip needed (Pitfall B is non-issue per research).

    **`try do` blocks** must be preserved exactly (Pitfall G). Do NOT convert to begin/rescue.

    **Save count** in do_reset_tournament_monitor: preserve exactly the ~6 distinct `deep_merge_data! + save!` / `update(...)` calls. Do not collapse or add saves (research: Save Count Baseline section).

    German docstring at top of file following RankingResolver pattern.
    `frozen_string_literal: true` as first line.

    Create `test/services/tournament_monitor/table_populator_test.rb` with unit tests verifying:
    - Instance creation and @tournament_monitor reference
    - initialize_table_monitors calls table_monitors association methods
    - populate_tables initializes @placements from data hash
    - do_reset_tournament_monitor calls populate_tables internally (not via model delegation)
    - TournamentMonitor.allow_change_tables is used (not self.allow_change_tables)
    Use stubs/mocks for TournamentMonitor to avoid full DB setup in unit tests.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/tournament_monitor/table_populator_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - File `app/services/tournament_monitor/table_populator.rb` exists and contains `class TournamentMonitor::TablePopulator`
    - `do_reset_tournament_monitor` is a public method
    - `populate_tables` is a public method
    - `initialize_table_monitors` is a public method
    - `do_placement` is a private method
    - `populate_tables` is called DIRECTLY from `do_reset_tournament_monitor` (not via `@tournament_monitor.populate_tables`)
    - All `self.allow_change_tables` converted to `TournamentMonitor.allow_change_tables`
    - All `tournament_monitor: self` converted to `tournament_monitor: @tournament_monitor`
    - `try do` blocks preserved (not converted to begin/rescue)
    - No `ApplicationService` inheritance
    - `frozen_string_literal: true` as first line
    - Test file exists and all unit tests pass
  </acceptance_criteria>
  <done>TablePopulator service exists with populate_tables, do_placement, initialize_table_monitors, do_reset_tournament_monitor. All conversions applied correctly. Unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TablePopulator delegation + remove lib module methods + cleanup</name>
  <files>
    app/models/tournament_monitor.rb
    lib/tournament_monitor_support.rb
    lib/tournament_monitor_state.rb
  </files>
  <read_first>
    app/services/tournament_monitor/table_populator.rb (just created in Task 1)
    app/models/tournament_monitor.rb (current model — add delegation wrappers)
    lib/tournament_monitor_support.rb (identify ALL remaining methods after Plan 01 removals)
    lib/tournament_monitor_state.rb (identify methods to remove: do_reset_tournament_monitor; identify methods staying: finalize_round, group_phase_finished?, finals_finished?, all_table_monitors_finished?, table_monitors_ready?)
    test/models/tournament_monitor_t04_test.rb (characterization — must pass unchanged)
    test/models/tournament_monitor_t06_test.rb (characterization — must pass unchanged)
    test/models/tournament_monitor_ko_test.rb (characterization — must pass unchanged)
  </read_first>
  <action>
    **Step 1: Add delegation wrappers to `app/models/tournament_monitor.rb`** (BEFORE removing lib methods — Pitfall F):

    Add these public delegation methods:

    ```ruby
    # Delegation wrappers — table population lives in TournamentMonitor::TablePopulator
    def do_reset_tournament_monitor
      TournamentMonitor::TablePopulator.new(self).do_reset_tournament_monitor
    end

    def populate_tables
      TournamentMonitor::TablePopulator.new(self).populate_tables
    end

    def initialize_table_monitors
      TournamentMonitor::TablePopulator.new(self).initialize_table_monitors
    end
    ```

    NOTE: The AASM `after_enter: [:do_reset_tournament_monitor]` on `new_tournament_monitor` state calls `do_reset_tournament_monitor` as a symbol — this resolves to the model method above, which delegates to the service. No AASM block changes needed (Pitfall F).

    **Step 2: Run characterization tests WITH both lib methods AND delegation wrappers present:**
    ```bash
    bin/rails test test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb test/models/tournament_monitor_ko_test.rb
    ```

    **Step 3: Remove extracted methods from lib modules:**

    From `lib/tournament_monitor_support.rb` remove:
    - `initialize_table_monitors` (line 370)
    - `populate_tables` (line 407)
    - `do_placement(game, r_no, t_no, sets, balls, innings)` (line 855)

    After Plan 01 + this removal, `lib/tournament_monitor_support.rb` should contain NO methods. Per D-11: if the file is empty (only module declaration + end), REMOVE THE FILE ENTIRELY and remove the `include TournamentMonitorSupport` from `app/models/tournament_monitor.rb`. Also remove the `require` if there is one.

    From `lib/tournament_monitor_state.rb` remove:
    - `do_reset_tournament_monitor` (line 201)

    The file should retain: `finalize_round`, `group_phase_finished?`, `finals_finished?`, `all_table_monitors_finished?`, `table_monitors_ready?` (D-08). Verify these methods are still present.

    **Step 4: Run characterization tests again to verify removal did not break anything.**

    **Step 5: Run ALL service tests (result_processor + table_populator + ranking_resolver + player_group_distributor) to confirm everything works together.**

    **Step 6: Count lines to measure extraction impact:**
    ```bash
    wc -l app/models/tournament_monitor.rb lib/tournament_monitor_state.rb
    ```
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb test/models/tournament_monitor_ko_test.rb test/services/tournament_monitor/table_populator_test.rb test/services/tournament_monitor/result_processor_test.rb test/services/tournament_monitor/ranking_resolver_test.rb test/services/tournament_monitor/player_group_distributor_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/tournament_monitor.rb` contains delegation wrappers for `do_reset_tournament_monitor`, `populate_tables`, `initialize_table_monitors` — each calling `TournamentMonitor::TablePopulator.new(self).method_name`
    - `lib/tournament_monitor_support.rb` either does not exist (removed per D-11) or contains only the empty module declaration
    - If `tournament_monitor_support.rb` was removed, `include TournamentMonitorSupport` is also removed from `app/models/tournament_monitor.rb`
    - `lib/tournament_monitor_state.rb` retains exactly 5 query methods: `finalize_round`, `group_phase_finished?`, `finals_finished?`, `all_table_monitors_finished?`, `table_monitors_ready?`
    - `lib/tournament_monitor_state.rb` no longer contains `do_reset_tournament_monitor`
    - All 3 characterization tests pass unchanged (0 failures, 0 errors)
    - All 4 service test files pass (result_processor, table_populator, ranking_resolver, player_group_distributor)
  </acceptance_criteria>
  <done>Delegation wrappers on TournamentMonitor delegate to TablePopulator. Lib methods removed. tournament_monitor_support.rb removed if empty. tournament_monitor_state.rb reduced to ~5 query methods. All characterization + service tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service-to-model | TablePopulator calls AASM events, save!, update on @tournament_monitor |
| cattr_accessor | Service sets class-level allow_change_tables — must use TournamentMonitor.allow_change_tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | do_reset_tournament_monitor save count | mitigate | Preserve exact save count (~6 saves) — no collapse, no additions |
| T-15-04 | Information Disclosure | @tournament_plan ivar | accept | Service-local ivar, garbage collected after call — no persistence risk |
| T-15-05 | Elevation of Privilege | cattr_accessor self. confusion | mitigate | Replace all self.allow_change_tables with TournamentMonitor.allow_change_tables in service code |
</threat_model>

<verification>
1. `bin/rails test test/services/tournament_monitor/table_populator_test.rb` — all unit tests pass
2. `bin/rails test test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb test/models/tournament_monitor_ko_test.rb` — all characterization tests pass unchanged
3. `ls lib/tournament_monitor_support.rb 2>/dev/null` — file should not exist (or be empty module only)
4. `grep -c "def do_reset_tournament_monitor" lib/tournament_monitor_state.rb` returns 0
5. `grep -c "def finalize_round" lib/tournament_monitor_state.rb` returns 1 (method stays)
6. `grep -c "TablePopulator.new(self)" app/models/tournament_monitor.rb` returns 3
</verification>

<success_criteria>
- TablePopulator service exists with populate_tables, do_placement, initialize_table_monitors, do_reset_tournament_monitor
- do_reset_tournament_monitor save count preserved exactly
- cattr_accessor handled correctly (TournamentMonitor.allow_change_tables, not self.)
- Intra-service populate_tables call (not round-trip through model)
- lib/tournament_monitor_support.rb empty/removed (D-11)
- lib/tournament_monitor_state.rb reduced to query methods only
- All characterization tests pass unchanged (D-10)
- All 4 service test files pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-high-risk-extractions/15-02-SUMMARY.md`
</output>
