---
phase: 08-service-tests-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/services/table_monitor/game_setup_test.rb
  - test/services/table_monitor/result_recorder_test.rb
autonomous: true
requirements: [SRVC-01]

must_haves:
  truths:
    - "game_setup_test.rb line 97 asserts the actual game ID value, not just presence"
    - "result_recorder_test.rb lines 120 and 236 have post-condition assertions after assert_nothing_raised"
    - "Both TableMonitor service test files pass after changes"
  artifacts:
    - path: "test/services/table_monitor/game_setup_test.rb"
      provides: "Strengthened game_id assertion"
    - path: "test/services/table_monitor/result_recorder_test.rb"
      provides: "Post-condition assertions on 2 tests"
  key_links:
    - from: "test/services/table_monitor/game_setup_test.rb"
      to: "app/services/table_monitor/game_setup.rb"
      via: "Assertion verifies game_id matches created game"
      pattern: "assert_equal.*game_id"
    - from: "test/services/table_monitor/result_recorder_test.rb"
      to: "app/services/table_monitor/result_recorder.rb"
      via: "Post-condition assertions verify game state after recording"
      pattern: "assert_equal|assert_predicate"
---

<objective>
Fix the 3 sole-assertion cases in the 2 TableMonitor service test files — strengthen game_setup_test.rb game_id check and add post-condition assertions to result_recorder_test.rb.

Purpose: Per D-06/D-07/D-08, these v1.0-era tests are mostly solid but have 3 specific weak spots identified in the audit.
Output: 2 modified test files with stronger assertions, all passing.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audit-baseline-standards/STANDARDS.md
@.planning/phases/06-audit-baseline-standards/AUDIT-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen 3 weak assertions in TableMonitor service tests</name>
  <files>
    test/services/table_monitor/game_setup_test.rb
    test/services/table_monitor/result_recorder_test.rb
  </files>
  <read_first>
    test/services/table_monitor/game_setup_test.rb
    test/services/table_monitor/result_recorder_test.rb
    app/services/table_monitor/game_setup.rb
    app/services/table_monitor/result_recorder.rb
  </read_first>
  <action>
Per D-06/D-07/D-08, fix only the 3 identified sole-assertion cases. Do NOT refactor other parts of these tests — they are mostly solid from v1.0.

1. **game_setup_test.rb line 97** (D-07): `assert_not_nil @tm.game_id` is a presence-only check. Read the test setup and game_setup.rb source to understand what game is created. Strengthen to verify the actual game ID value — e.g., `assert_equal game.id, @tm.game_id` where `game` is the Game record created by GameSetup. If the game record is accessible in the test (captured from the setup or findable via Game.last), use `assert_equal`. If not directly accessible, at minimum verify `@tm.game_id` is a positive integer and that `Game.exists?(@tm.game_id)` is true.

2. **result_recorder_test.rb line 120** (D-08): `assert_nothing_raised` as sole assertion. Read the test to understand what result is being recorded. Add post-condition assertions checking the game state after recording — e.g., game.reload and check score attributes, state, or result fields. Keep the assert_nothing_raised block.

3. **result_recorder_test.rb line 236** (D-08): Same pattern as line 120. Add post-condition assertions verifying the outcome of the result recording operation. Read what the test does and what save_result / evaluate_result produces, then assert on the resulting state.

IMPORTANT: These tests may use complex setup with TableMonitor fixtures and AASM states. Read the full test file to understand the setup before modifying.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/table_monitor/game_setup_test.rb test/services/table_monitor/result_recorder_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - grep "assert_not_nil @tm.game_id" game_setup_test.rb returns 0 matches OR is followed by a stronger assertion on the next line
    - grep -c "assert_equal\|assert_predicate\|assert_includes\|Game.exists" game_setup_test.rb shows at least 1 new assertion near line 97
    - grep -c "assert_nothing_raised" result_recorder_test.rb still shows the original count (none removed)
    - result_recorder_test.rb lines near 120 and 236 each have at least 1 new post-condition assertion (assert_equal, assert_predicate, or similar)
    - Both files pass: bin/rails test test/services/table_monitor/
  </acceptance_criteria>
  <done>All 3 weak assertion cases fixed with meaningful checks. game_setup_test verifies actual game_id value. result_recorder_test verifies game state after recording. Tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. This plan modifies test files only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | T (Tampering) | Test fixtures | accept | Tests only add assertions; fixture data and setup unchanged |
</threat_model>

<verification>
After task completes:
```bash
bin/rails test test/services/table_monitor/
```
Both TableMonitor service test files pass.
</verification>

<success_criteria>
- game_setup_test.rb game_id assertion verifies actual value, not just presence
- result_recorder_test.rb both assert_nothing_raised blocks have post-condition assertions
- Both test files pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-service-tests-review/08-02-SUMMARY.md`
</output>
