---
phase: 13-low-risk-extractions
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - app/services/tournament/ranking_calculator.rb
  - app/models/tournament.rb
  - test/services/tournament/ranking_calculator_test.rb
autonomous: true
requirements: [TEXT-01]

must_haves:
  truths:
    - "Tournament::RankingCalculator class exists with calculate_and_cache_rankings and reorder_seedings instance methods"
    - "Tournament#calculate_and_cache_rankings delegates to RankingCalculator and produces identical ranking output"
    - "AASM after_enter callback on tournament_seeding_finished works identically through delegation"
    - "TournamentsController calls at lines 112 and 961 work transparently via delegation"
    - "All Phase 11-12 characterization tests pass without modification"
  artifacts:
    - path: "app/services/tournament/ranking_calculator.rb"
      provides: "PORO with calculate_and_cache_rankings and reorder_seedings"
      contains: "class Tournament::RankingCalculator"
    - path: "test/services/tournament/ranking_calculator_test.rb"
      provides: "Unit tests for ranking calculation"
      contains: "class Tournament::RankingCalculatorTest"
  key_links:
    - from: "app/models/tournament.rb"
      to: "app/services/tournament/ranking_calculator.rb"
      via: "delegation wrapper instance methods"
      pattern: "RankingCalculator\\.new\\(self\\)"
---

<objective>
Extract Tournament::RankingCalculator — the ranking calculation and seeding reorder logic from Tournament model.

Purpose: Second extraction proving the PORO pattern with DB writes (save! on tournament instance). Slightly more complex than PlayerGroupDistributor because it mutates the tournament's data hash and calls save!.
Output: Working RankingCalculator PORO in app/services/tournament/, delegation wrappers on Tournament, unit tests passing.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-low-risk-extractions/13-CONTEXT.md
@.planning/phases/13-low-risk-extractions/13-RESEARCH.md
@.planning/phases/13-low-risk-extractions/13-01-SUMMARY.md

<interfaces>
<!-- Source methods being extracted — from app/models/tournament.rb lines 886-940 -->

```ruby
# line 886 — instance method, no parameters
def calculate_and_cache_rankings
  return unless organizer.is_a?(Region) && discipline.present?
  return unless id.present? && id >= Tournament::MIN_ID
  # Uses: organizer, discipline, discipline_id, id, Season, PlayerRanking, seedings
  # Calls: data_will_change!, save!, Tournament.logger
  # Writes: self.data['player_rankings'] = player_rank

# line 934 — instance method, no parameters
def reorder_seedings
  # Uses: seeding_ids
  # Calls: Seeding.find_by_id(...).update_columns, reload
```

AASM callback on Tournament (stays on model):
```ruby
# line ~274
after_enter :tournament_seeding_finished, after: [:calculate_and_cache_rankings]
```

TournamentsController callers:
```ruby
# tournaments_controller.rb:112 — @tournament.calculate_and_cache_rankings
# tournaments_controller.rb:961 — @tournament.calculate_and_cache_rankings
```

Self-references that must become @tournament references:
- organizer, discipline, discipline_id, id, data, data_will_change!, save!, seedings, seeding_ids, reload
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RankingCalculator PORO with unit tests</name>
  <files>
    app/services/tournament/ranking_calculator.rb
    test/services/tournament/ranking_calculator_test.rb
  </files>
  <read_first>
    app/models/tournament.rb (lines 886-940 — calculate_and_cache_rankings, reorder_seedings)
    app/models/tournament.rb (line 274 area — AASM after_enter callback referencing calculate_and_cache_rankings)
    app/services/table_monitor/game_setup.rb (reference for service structure)
    test/services/table_monitor/game_setup_test.rb (reference for test structure)
    test/models/tournament_aasm_test.rb (characterization tests that exercise calculate_and_cache_rankings)
  </read_first>
  <behavior>
    - Test 1: calculate_and_cache_rankings returns early (nil) when tournament has no discipline
    - Test 2: calculate_and_cache_rankings returns early (nil) when tournament id < MIN_ID (global record)
    - Test 3: calculate_and_cache_rankings returns early (nil) when organizer is not a Region
    - Test 4: calculate_and_cache_rankings computes and caches player_rankings in data hash when valid tournament with PlayerRanking records exist
    - Test 5: reorder_seedings updates Seeding positions sequentially starting from 1
  </behavior>
  <action>
    Create `app/services/tournament/` directory. Per D-02 and D-04/D-05, create a PORO class:

    1. Class: `Tournament::RankingCalculator` — no ApplicationService inheritance (per D-02)
    2. Constructor: `def initialize(tournament)` — receives tournament instance (per D-05)
    3. Instance method `calculate_and_cache_rankings`:
       - Copy body verbatim from tournament.rb lines 886-931
       - Replace ALL bare method calls with `@tournament.` prefix:
         `organizer` -> `@tournament.organizer`
         `discipline` -> `@tournament.discipline`
         `discipline_id` -> `@tournament.discipline_id`
         `id` -> `@tournament.id`
         `data_will_change!` -> `@tournament.data_will_change!`
         `data` -> `@tournament.data`
         `save!` -> `@tournament.save!`
       - Keep `Tournament.logger`, `Season`, `PlayerRanking`, `Tournament::MIN_ID` references unchanged (class-level)
    4. Instance method `reorder_seedings`:
       - Copy body verbatim from tournament.rb lines 934-940
       - Replace: `seeding_ids` -> `@tournament.seeding_ids`, `reload` -> `@tournament.reload`
    5. `frozen_string_literal: true` as first line

    Write tests FIRST (RED), then create the service (GREEN). Use fixtures or Model.create! with id >= 50_000_000 for local tournament records.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/tournament/ranking_calculator_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - File `app/services/tournament/ranking_calculator.rb` exists with `class Tournament::RankingCalculator`
    - Constructor accepts a tournament parameter: `def initialize(tournament)`
    - Contains `def calculate_and_cache_rankings` instance method
    - Contains `def reorder_seedings` instance method
    - All `self` references converted to `@tournament` (no bare `organizer`, `discipline`, `data`, etc.)
    - File starts with `# frozen_string_literal: true`
    - Class does NOT inherit from ApplicationService (PORO per D-02)
    - Test file exists and all tests pass
  </acceptance_criteria>
  <done>RankingCalculator PORO exists with both methods, all self-references converted to @tournament, unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire delegation in Tournament and verify characterization tests</name>
  <files>
    app/models/tournament.rb
  </files>
  <read_first>
    app/models/tournament.rb (lines 886-940 — methods to replace with delegation wrappers)
    app/models/tournament.rb (line 274 area — AASM after_enter callback must NOT change)
    app/services/tournament/ranking_calculator.rb (just created in Task 1)
    test/models/tournament_aasm_test.rb (must pass unchanged — exercises calculate_and_cache_rankings via AASM)
    test/models/tournament_calendar_test.rb (must pass unchanged)
  </read_first>
  <action>
    Per D-04, replace the original method bodies in tournament.rb with delegation wrappers:

    1. REPLACE `calculate_and_cache_rankings` method body (lines 886-932) with:
       ```ruby
       def calculate_and_cache_rankings
         Tournament::RankingCalculator.new(self).calculate_and_cache_rankings
       end
       ```

    2. REPLACE `reorder_seedings` method body (lines 934-940) with:
       ```ruby
       def reorder_seedings
         Tournament::RankingCalculator.new(self).reorder_seedings
       end
       ```

    3. The AASM `after_enter :tournament_seeding_finished, after: [:calculate_and_cache_rankings]` stays UNCHANGED — it calls the model method which now delegates.

    4. Run ALL characterization tests to verify nothing broke (D-11).
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/models/tournament_aasm_test.rb test/models/tournament_calendar_test.rb test/services/tournament/ranking_calculator_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - tournament.rb `calculate_and_cache_rankings` is a 1-line delegation to `Tournament::RankingCalculator.new(self).calculate_and_cache_rankings`
    - tournament.rb `reorder_seedings` is a 1-line delegation to `Tournament::RankingCalculator.new(self).reorder_seedings`
    - AASM after_enter callback for tournament_seeding_finished is unchanged
    - tournament_aasm_test.rb passes without modification
    - tournament_calendar_test.rb passes without modification
    - RankingCalculator unit tests still pass
    - tournament.rb reduced by ~47 lines (method bodies removed, delegation wrappers added)
  </acceptance_criteria>
  <done>Tournament delegates to RankingCalculator, all characterization and unit tests pass, ~47 lines removed from tournament.rb</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. RankingCalculator accesses the same data through the tournament instance reference.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | T (Tampering) | RankingCalculator data_will_change! + save! | accept | Same write path as before — goes through ActiveRecord validations and callbacks |
</threat_model>

<verification>
1. `bin/rails test test/services/tournament/ranking_calculator_test.rb` — unit tests pass
2. `bin/rails test test/models/tournament_aasm_test.rb` — AASM characterization passes unchanged
3. `bin/rails test test/models/tournament_calendar_test.rb` — calendar tests unaffected
4. `grep -c "RankingCalculator" app/models/tournament.rb` — returns 2 (two delegation wrappers)
5. Method bodies in tournament.rb are 1-line delegations, not full implementations
</verification>

<success_criteria>
- RankingCalculator PORO exists at app/services/tournament/ranking_calculator.rb
- Unit tests exist and pass at test/services/tournament/ranking_calculator_test.rb
- Tournament delegates calculate_and_cache_rankings and reorder_seedings
- AASM callback works through delegation
- All Phase 11-12 characterization tests pass without modification
- tournament.rb reduced by ~47 lines
</success_criteria>

<output>
After completion, create `.planning/phases/13-low-risk-extractions/13-02-SUMMARY.md`
</output>
