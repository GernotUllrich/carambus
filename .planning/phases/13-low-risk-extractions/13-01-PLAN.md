---
phase: 13-low-risk-extractions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/tournament_monitor/player_group_distributor.rb
  - app/models/tournament_monitor.rb
  - app/models/tournament_plan.rb
  - test/services/tournament_monitor/player_group_distributor_test.rb
autonomous: true
requirements: [TMEX-01]

must_haves:
  truths:
    - "TournamentMonitor::PlayerGroupDistributor class exists with distribute_to_group and distribute_with_sizes class methods"
    - "TournamentMonitor.distribute_to_group delegates to PlayerGroupDistributor and returns identical results"
    - "All 7+ callers (controllers, helpers, views) work transparently via delegation wrapper"
    - "All Phase 11-12 characterization tests pass without modification"
  artifacts:
    - path: "app/services/tournament_monitor/player_group_distributor.rb"
      provides: "Pure PORO with distribute_to_group, distribute_with_sizes, DIST_RULES, GROUP_RULES, GROUP_SIZES"
      contains: "class TournamentMonitor::PlayerGroupDistributor"
    - path: "test/services/tournament_monitor/player_group_distributor_test.rb"
      provides: "Unit tests for distribution algorithm"
      contains: "class TournamentMonitor::PlayerGroupDistributorTest"
  key_links:
    - from: "app/models/tournament_monitor.rb"
      to: "app/services/tournament_monitor/player_group_distributor.rb"
      via: "delegation wrapper class method"
      pattern: "PlayerGroupDistributor\\.distribute_to_group"
---

<objective>
Extract TournamentMonitor::PlayerGroupDistributor — the pure distribution algorithm with DIST_RULES, GROUP_RULES, GROUP_SIZES constants and distribute_to_group/distribute_with_sizes class methods.

Purpose: Prove the PORO extraction + delegation pattern on the simplest target (pure algorithm, no DB writes, no side effects) before tackling RankingCalculator and TableReservationService.
Output: Working PlayerGroupDistributor PORO in app/services/tournament_monitor/, delegation wrappers on TournamentMonitor, unit tests passing.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-low-risk-extractions/13-CONTEXT.md
@.planning/phases/13-low-risk-extractions/13-RESEARCH.md

<interfaces>
<!-- Source methods being extracted — from app/models/tournament_monitor.rb lines 170-327 -->

Constants to move:
```ruby
DIST_RULES = { 6 => [[1, 2], [4, 3], [6, 5]], ... 16 => [...] }.freeze  # lines 170-182
GROUP_RULES = { 6 => [[1, 4, 6], [2, 3, 5]], ... 16 => [...] }.freeze   # lines 184-196
GROUP_SIZES = { 6 => [3, 3], ... 16 => [4, 4, 4, 4] }.freeze            # lines 197-209
```

Class methods to move:
```ruby
def self.distribute_to_group(players, ngroups, group_sizes = nil)  # line 211
  # Returns Hash like {"group1" => [player_ids], "group2" => [player_ids]}
  # Rescue block uses Tournament.logger — keep as-is for behavior preservation

def self.distribute_with_sizes(players, ngroups, group_sizes)       # line 269
  # Returns Hash like {"group1" => [player_ids], ...}
```

Existing delegation pattern from app/services/table_monitor/game_setup.rb:
```ruby
class TableMonitor::GameSetup < ApplicationService
  def initialize(kwargs = {})
    @tm = kwargs[:table_monitor]
  end
end
```

ApplicationService base class (app/services/application_service.rb):
```ruby
class ApplicationService
  def self.call(kwargs = {})
    new(kwargs).call
  end
end
```

TournamentPlan caller at line 393:
```ruby
# app/models/tournament_plan.rb
TournamentMonitor.distribute_to_group(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PlayerGroupDistributor PORO with unit tests</name>
  <files>
    app/services/tournament_monitor/player_group_distributor.rb
    test/services/tournament_monitor/player_group_distributor_test.rb
  </files>
  <read_first>
    app/models/tournament_monitor.rb (lines 170-327 — DIST_RULES, GROUP_RULES, GROUP_SIZES, distribute_to_group, distribute_with_sizes)
    app/services/table_monitor/game_setup.rb (reference for service file structure)
    test/services/table_monitor/game_setup_test.rb (reference for test file structure)
    test/models/tournament_monitor_t04_test.rb (characterization tests that exercise distribute_to_group)
  </read_first>
  <behavior>
    - Test 1: distribute_to_group with 8 players, 2 groups returns correct zig-zag assignment per GROUP_RULES[8]
    - Test 2: distribute_to_group with 12 players, 3 groups returns correct GROUP_RULES[12] assignment
    - Test 3: distribute_to_group with 16 players, 4 groups returns correct GROUP_RULES[16] assignment
    - Test 4: distribute_to_group with 9 players, 3 groups returns 3 groups of 3
    - Test 5: distribute_with_sizes with custom group_sizes parameter distributes correctly
    - Test 6: distribute_to_group with 0 ngroups does not raise (edge case from rescue block)
    - Test 7: Constants DIST_RULES, GROUP_RULES, GROUP_SIZES are frozen hashes accessible on the class
  </behavior>
  <action>
    Create `app/services/tournament_monitor/` directory. Per D-02 and D-08, create a PORO class (no ApplicationService inheritance) with:

    1. Move DIST_RULES, GROUP_RULES, GROUP_SIZES constants verbatim from tournament_monitor.rb lines 170-209
    2. Move `self.distribute_to_group` method body verbatim from lines 211-265
    3. Move `self.distribute_with_sizes` method body verbatim from lines 269-327
    4. Keep the `Tournament.logger` reference in the rescue block unchanged (behavior preservation per research pitfall 2)
    5. Add `frozen_string_literal: true` as first line (project convention)
    6. Class name: `TournamentMonitor::PlayerGroupDistributor`

    Write tests FIRST (RED), then create the service (GREEN). Test file goes in `test/services/tournament_monitor/player_group_distributor_test.rb`. Use integer arrays as player inputs (the methods handle both Player objects and integers via `player.is_a?(Integer)` check).
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/tournament_monitor/player_group_distributor_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - File `app/services/tournament_monitor/player_group_distributor.rb` exists with `class TournamentMonitor::PlayerGroupDistributor`
    - File contains `DIST_RULES`, `GROUP_RULES`, `GROUP_SIZES` frozen hash constants
    - File contains `def self.distribute_to_group(players, ngroups, group_sizes = nil)` class method
    - File contains `def self.distribute_with_sizes(players, ngroups, group_sizes)` class method
    - File starts with `# frozen_string_literal: true`
    - Class does NOT inherit from ApplicationService (PORO per D-02)
    - Test file exists at `test/services/tournament_monitor/player_group_distributor_test.rb`
    - All unit tests pass with `bin/rails test test/services/tournament_monitor/player_group_distributor_test.rb`
  </acceptance_criteria>
  <done>PlayerGroupDistributor PORO exists with all constants and class methods, unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire delegation in TournamentMonitor and verify characterization tests</name>
  <files>
    app/models/tournament_monitor.rb
    app/models/tournament_plan.rb
  </files>
  <read_first>
    app/models/tournament_monitor.rb (lines 170-327 — methods and constants to replace with delegation)
    app/models/tournament_plan.rb (line 393 — group_sizes_from calls TournamentMonitor.distribute_to_group)
    app/services/tournament_monitor/player_group_distributor.rb (just created in Task 1)
    test/models/tournament_monitor_t04_test.rb (must pass unchanged)
    test/models/tournament_monitor_t06_test.rb (must pass unchanged)
    test/models/tournament_monitor_ko_test.rb (must pass unchanged)
  </read_first>
  <action>
    Per D-09, replace the original class methods and constants in tournament_monitor.rb with delegation wrappers:

    1. REMOVE the following from tournament_monitor.rb:
       - `DIST_RULES` constant (lines 170-182)
       - `GROUP_RULES` constant (lines 184-196)
       - `GROUP_SIZES` constant (lines 197-209)
       - `self.distribute_to_group` method body (lines 211-265)
       - `self.distribute_with_sizes` method body (lines 269-327)

    2. ADD delegation wrappers in the same location:
       ```ruby
       def self.distribute_to_group(players, ngroups, group_sizes = nil)
         TournamentMonitor::PlayerGroupDistributor.distribute_to_group(players, ngroups, group_sizes)
       end

       def self.distribute_with_sizes(players, ngroups, group_sizes)
         TournamentMonitor::PlayerGroupDistributor.distribute_with_sizes(players, ngroups, group_sizes)
       end
       ```

    3. Per D-09, optionally update `TournamentPlan.group_sizes_from` (line 393) to call `TournamentMonitor::PlayerGroupDistributor.distribute_to_group` directly. This is a cleaner reference but optional since the delegation wrapper handles it.

    4. Run ALL characterization tests to verify nothing broke (D-11).
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb test/models/tournament_monitor_ko_test.rb test/services/tournament_monitor/player_group_distributor_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - tournament_monitor.rb no longer contains DIST_RULES, GROUP_RULES, or GROUP_SIZES constants
    - tournament_monitor.rb no longer contains the full body of distribute_to_group or distribute_with_sizes
    - tournament_monitor.rb contains `def self.distribute_to_group` delegation wrapper calling `PlayerGroupDistributor.distribute_to_group`
    - tournament_monitor.rb contains `def self.distribute_with_sizes` delegation wrapper calling `PlayerGroupDistributor.distribute_with_sizes`
    - All characterization tests pass: tournament_monitor_t04_test.rb, tournament_monitor_t06_test.rb, tournament_monitor_ko_test.rb
    - PlayerGroupDistributor unit tests still pass
    - `self.ranking` class method remains on TournamentMonitor (per D-08)
  </acceptance_criteria>
  <done>TournamentMonitor delegates to PlayerGroupDistributor, all characterization and unit tests pass, ~160 lines removed from tournament_monitor.rb</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. All extracted code handles the same data with the same access patterns. No external inputs, no new endpoints.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | T (Tampering) | PlayerGroupDistributor constants | accept | Constants are frozen hashes; same protection as before extraction |
</threat_model>

<verification>
1. `bin/rails test test/services/tournament_monitor/player_group_distributor_test.rb` — unit tests pass
2. `bin/rails test test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb test/models/tournament_monitor_ko_test.rb` — characterization tests pass unchanged
3. `grep -c "DIST_RULES\|GROUP_RULES\|GROUP_SIZES" app/models/tournament_monitor.rb` — returns 0 (constants moved)
4. `grep -c "PlayerGroupDistributor" app/models/tournament_monitor.rb` — returns 2+ (delegation wrappers present)
</verification>

<success_criteria>
- PlayerGroupDistributor PORO exists at app/services/tournament_monitor/player_group_distributor.rb
- Unit tests exist and pass at test/services/tournament_monitor/player_group_distributor_test.rb
- TournamentMonitor delegates distribute_to_group and distribute_with_sizes to the service
- All Phase 11-12 characterization tests pass without modification
- tournament_monitor.rb reduced by ~160 lines (constants + method bodies)
</success_criteria>

<output>
After completion, create `.planning/phases/13-low-risk-extractions/13-01-SUMMARY.md`
</output>
