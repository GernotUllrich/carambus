---
phase: 13-low-risk-extractions
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - app/services/tournament/table_reservation_service.rb
  - app/models/tournament.rb
  - test/services/tournament/table_reservation_service_test.rb
autonomous: true
requirements: [TEXT-02]

must_haves:
  truths:
    - "Tournament::TableReservationService exists as ApplicationService subclass with .call entry point"
    - "Tournament#create_table_reservation delegates to TableReservationService and produces identical calendar events"
    - "Google Calendar API calls are properly stubbed in tests using WebMock"
    - "Private helper methods (format_table_list, build_event_summary, calculate_start_time, calculate_end_time, fallback_table_count, create_google_calendar_event) are extracted to the service"
    - "required_tables_count and available_tables_with_heaters remain on Tournament model"
    - "All Phase 11-12 characterization tests pass without modification"
  artifacts:
    - path: "app/services/tournament/table_reservation_service.rb"
      provides: "ApplicationService with Google Calendar reservation logic"
      contains: "class Tournament::TableReservationService < ApplicationService"
    - path: "test/services/tournament/table_reservation_service_test.rb"
      provides: "Unit tests for table reservation with stubbed Google API"
      contains: "class Tournament::TableReservationServiceTest"
  key_links:
    - from: "app/models/tournament.rb"
      to: "app/services/tournament/table_reservation_service.rb"
      via: "delegation wrapper calling .call"
      pattern: "TableReservationService\\.call"
    - from: "app/services/tournament/table_reservation_service.rb"
      to: "app/models/tournament.rb"
      via: "@tournament.required_tables_count, @tournament.available_tables_with_heaters"
      pattern: "@tournament\\.required_tables_count"
---

<objective>
Extract Tournament::TableReservationService — the Google Calendar reservation flow with all private helper methods from Tournament.

Purpose: Third and most complex extraction in this phase. Uses ApplicationService pattern (per D-02) because it orchestrates external Google Calendar API calls with side effects.
Output: Working TableReservationService in app/services/tournament/, delegation wrapper on Tournament, unit tests with stubbed Google API passing.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-low-risk-extractions/13-CONTEXT.md
@.planning/phases/13-low-risk-extractions/13-RESEARCH.md
@.planning/phases/13-low-risk-extractions/13-01-SUMMARY.md
@.planning/phases/13-low-risk-extractions/13-02-SUMMARY.md

<interfaces>
<!-- Source methods being extracted — from app/models/tournament.rb -->

Public method (line 1035):
```ruby
def create_table_reservation
  return nil unless location.present? && discipline.present? && date.present?
  tables_needed = required_tables_count                          # STAYS on model (D-07)
  return nil if tables_needed.zero?
  available_tables = available_tables_with_heaters(limit: ...)   # STAYS on model (D-07)
  return nil if available_tables.empty?
  # ... calls format_table_list, build_event_summary, calculate_start_time, calculate_end_time, create_google_calendar_event
```

Private helpers to extract (lines 1659-1774):
```ruby
def fallback_table_count(participant_count)  # line 1659 — pure math
def format_table_list(table_names)           # line 1665 — pure string formatting
def build_event_summary(table_string)        # line 1684 — reads shortname, title, discipline, player_class
def calculate_start_time                     # line 1709 — reads date, tournament_cc
def calculate_end_time                       # line 1732 — reads date
def create_google_calendar_event(summary, start_time, end_time)  # line 1746 — Google Calendar API
```

Self-references that must become @tournament references:
- location, discipline, date, required_tables_count, available_tables_with_heaters
- shortname, title, player_class, tournament_cc, id

ApplicationService base (app/services/application_service.rb):
```ruby
class ApplicationService
  def self.call(kwargs = {})
    new(kwargs).call
  end
end
```

Existing stub pattern from test/models/tournament_calendar_test.rb:
- Uses WebMock to stub Google API calls
- Stubs Rails.application.credentials.dig for google_service private_key
- Stubs GoogleCalendarService.calendar_service and .calendar_id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TableReservationService ApplicationService with unit tests</name>
  <files>
    app/services/tournament/table_reservation_service.rb
    test/services/tournament/table_reservation_service_test.rb
  </files>
  <read_first>
    app/models/tournament.rb (lines 1035-1057 — create_table_reservation public method)
    app/models/tournament.rb (lines 1659-1774 — all 6 private helper methods)
    app/services/application_service.rb (base class)
    app/services/table_monitor/game_setup.rb (reference ApplicationService pattern)
    app/services/google_calendar_service.rb (calendar_service and calendar_id class methods used by create_google_calendar_event)
    test/models/tournament_calendar_test.rb (existing stubs for Google Calendar API — copy the exact stub pattern)
    test/services/table_monitor/game_setup_test.rb (reference for test structure)
  </read_first>
  <behavior>
    - Test 1: call returns nil when tournament has no location
    - Test 2: call returns nil when tournament has no discipline
    - Test 3: call returns nil when tournament has no date
    - Test 4: call returns nil when required_tables_count is zero
    - Test 5: call returns nil when no available tables with heaters
    - Test 6: format_table_list with consecutive table names returns range format "T1-T3"
    - Test 7: format_table_list with non-consecutive table names returns comma format "T1, T3, T5"
    - Test 8: build_event_summary includes shortname, discipline name, and player class
    - Test 9: calculate_start_time uses tournament_cc.starting_at when present, defaults to 11:00 otherwise
    - Test 10: create_google_calendar_event returns nil when no google_service credentials
  </behavior>
  <action>
    Per D-02 and D-06, create an ApplicationService subclass:

    1. Class: `Tournament::TableReservationService < ApplicationService`
    2. Constructor: `def initialize(kwargs = {})` with `@tournament = kwargs[:tournament]`
    3. Public `def call` method — body from `create_table_reservation` (lines 1035-1057), converting all bare method calls to `@tournament.` prefix:
       - `location` -> `@tournament.location`
       - `discipline` -> `@tournament.discipline`
       - `date` -> `@tournament.date`
       - `required_tables_count` -> `@tournament.required_tables_count` (stays on model per D-07)
       - `available_tables_with_heaters` -> `@tournament.available_tables_with_heaters`
    4. Private methods — copy verbatim from tournament.rb, converting self-references:
       - `fallback_table_count(participant_count)` — pure math, no self-references
       - `format_table_list(table_names)` — pure string, no self-references
       - `build_event_summary(table_string)` — convert `shortname` -> `@tournament.shortname`, `title` -> `@tournament.title`, `discipline` -> `@tournament.discipline`, `player_class` -> `@tournament.player_class`
       - `calculate_start_time` — convert `date` -> `@tournament.date`, `tournament_cc` -> `@tournament.tournament_cc`
       - `calculate_end_time` — convert `date` -> `@tournament.date`
       - `create_google_calendar_event(summary, start_time, end_time)` — convert `id` -> `@tournament.id`
    5. `frozen_string_literal: true` as first line

    Write tests FIRST (RED), then create the service (GREEN). For Google API tests, copy the EXACT stub pattern from `test/models/tournament_calendar_test.rb`. Use WebMock stubs — do NOT make real HTTP calls. The test needs to stub `GoogleCalendarService.calendar_service`, `GoogleCalendarService.calendar_id`, and `Rails.application.credentials.dig`.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/tournament/table_reservation_service_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - File `app/services/tournament/table_reservation_service.rb` exists
    - Class inherits from ApplicationService: `class Tournament::TableReservationService < ApplicationService`
    - Constructor accepts kwargs with `:tournament` key
    - Public `call` method contains the reservation flow logic
    - Private methods: fallback_table_count, format_table_list, build_event_summary, calculate_start_time, calculate_end_time, create_google_calendar_event
    - All self-references converted to `@tournament.` (no bare `location`, `discipline`, `shortname`, etc.)
    - File starts with `# frozen_string_literal: true`
    - Test file exists with Google API stubs matching tournament_calendar_test.rb pattern
    - All unit tests pass
  </acceptance_criteria>
  <done>TableReservationService exists with all 7 methods extracted, unit tests pass with stubbed Google API</done>
</task>

<task type="auto">
  <name>Task 2: Wire delegation in Tournament, remove private helpers, verify all tests</name>
  <files>
    app/models/tournament.rb
  </files>
  <read_first>
    app/models/tournament.rb (lines 1035-1057 — create_table_reservation to replace)
    app/models/tournament.rb (lines 1659-1774 — 6 private methods to remove)
    app/services/tournament/table_reservation_service.rb (just created in Task 1)
    test/models/tournament_calendar_test.rb (must pass unchanged)
    test/models/tournament_aasm_test.rb (must pass unchanged — from Plan 02)
    test/models/tournament_monitor_t04_test.rb (must pass unchanged — from Plan 01)
  </read_first>
  <action>
    Per D-06 and D-07:

    1. REPLACE `create_table_reservation` method body (lines 1035-1057) with:
       ```ruby
       def create_table_reservation
         Tournament::TableReservationService.call(tournament: self)
       end
       ```

    2. REMOVE these private methods from tournament.rb (they now live in the service):
       - `fallback_table_count` (lines 1659-1663)
       - `format_table_list` (lines 1665-1682)
       - `build_event_summary` (lines 1684-1707)
       - `calculate_start_time` (lines 1709-1730)
       - `calculate_end_time` (lines 1732-1744)
       - `create_google_calendar_event` (lines 1746-1774)

    3. KEEP on Tournament model (per D-07):
       - `required_tables_count` method
       - `available_tables_with_heaters` method

    4. Run the FULL test suite to verify nothing is broken across all three extractions (D-11):
       - All characterization tests from Phases 11-12
       - All new service unit tests from Plans 01, 02, 03
       - Full `bin/rails test` for comprehensive verification
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/models/tournament_calendar_test.rb test/models/tournament_aasm_test.rb test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb test/models/tournament_monitor_ko_test.rb test/services/tournament/ranking_calculator_test.rb test/services/tournament/table_reservation_service_test.rb test/services/tournament_monitor/player_group_distributor_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - tournament.rb `create_table_reservation` is a 1-line delegation to `Tournament::TableReservationService.call(tournament: self)`
    - tournament.rb no longer contains private methods: fallback_table_count, format_table_list, build_event_summary, calculate_start_time, calculate_end_time, create_google_calendar_event
    - tournament.rb STILL contains `required_tables_count` and `available_tables_with_heaters` (per D-07)
    - tournament_calendar_test.rb passes without modification
    - All Phase 11-12 characterization tests pass without modification
    - All 3 new service unit test files pass
    - tournament.rb reduced by ~100 lines (create_table_reservation body + 6 private methods removed)
    - `grep -c "TableReservationService" app/models/tournament.rb` returns 1 (delegation wrapper)
  </acceptance_criteria>
  <done>Tournament delegates to TableReservationService, all private helpers removed, all characterization + unit tests pass, tournament.rb reduced by ~100 lines. Phase 13 complete — all three extractions done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tournament.rb -> Google Calendar API | External API call via GoogleCalendarService — unchanged from pre-extraction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | I (Info Disclosure) | create_google_calendar_event credentials | accept | Same credential access pattern as before; credentials stored in encrypted Rails credentials |
| T-13-04 | D (Denial of Service) | Google Calendar API timeout | accept | Same rescue StandardError pattern as before; returns nil on failure |
</threat_model>

<verification>
1. `bin/rails test test/services/tournament/table_reservation_service_test.rb` — unit tests pass
2. `bin/rails test test/models/tournament_calendar_test.rb` — calendar characterization passes unchanged
3. `bin/rails test test/models/tournament_aasm_test.rb test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb test/models/tournament_monitor_ko_test.rb` — all characterization tests pass
4. `grep -c "fallback_table_count\|format_table_list\|build_event_summary\|calculate_start_time\|calculate_end_time\|create_google_calendar_event" app/models/tournament.rb` — returns 0 (all private helpers removed)
5. `grep "required_tables_count\|available_tables_with_heaters" app/models/tournament.rb | grep "def "` — both query methods still present on model
6. Final `bin/rails test` — full suite green
</verification>

<success_criteria>
- TableReservationService exists at app/services/tournament/table_reservation_service.rb inheriting ApplicationService
- Unit tests exist and pass at test/services/tournament/table_reservation_service_test.rb
- Tournament delegates create_table_reservation to the service
- 6 private helper methods removed from tournament.rb
- required_tables_count and available_tables_with_heaters remain on Tournament
- All Phase 11-12 characterization tests pass without modification
- tournament.rb reduced by ~100 lines
- Combined Phase 13 reduction: ~307 lines removed (160 from TM + 47 from ranking + 100 from reservation)
</success_criteria>

<output>
After completion, create `.planning/phases/13-low-risk-extractions/13-03-SUMMARY.md`
</output>
