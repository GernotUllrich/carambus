---
status: partial
phase: 38.3-bk2-kombi-dry-run-corrections
source:
  - 38.3-VERIFICATION.md
started: 2026-04-24T18:00:00Z
updated: 2026-04-24T22:00:00Z
---

## Current Test

[user reported 2 issues during manual UAT 2026-04-24 — see Gaps]

## Tests

### 1. Full Reflex Round-Trip (add_n accumulation → next_step commit)
expected: Open a BK2 match in playing state in the browser. Click +5 then +10 on the active player card. Small red inning-score shows 15. Click the swap icon. Inning total commits to the set score (set_scores.1.playera increases by 15 in SP, or opponent's score increases by 15 in DZ). Inning-score resets to 0 for the new player. Aufnahmewechsel via swap icon commits accumulated total; player_at_table flips; bk2_state persisted correctly.
result: partial — Serienspiel commit flow OK per user ("ansonsten im Serienspiel ok"); DZ flow not explicitly reported

### 2. Full Start-Game Flow (detail-view → warmup → shootout → playing)
expected: Open the BK2 detail-view, select BK2 50, verify only Ballziel + DZ-max + SP-max inputs appear (not the old 4-button first_set_mode matrix). Change DZ-max to 3, SP-max to 7, click Start Game. Navigate through warmup, shootout (confirm 4 buttons). Click "Spieler A – Serienspiel". The scoreboard shows the Serienspiel phase chip + "Aufnahmen übrig: 7". Full AASM chain: detail config → warmup → 4-button shootout → playing scoreboard with phase chip + correct SP remaining badge matching the configured max innings.
result: partial — "Start nach shootout landet jetzt im scoreboard" (I6 fix confirmed — shootout → playing transition renders scoreboard). Detail-view changes "unverändert" per user observation (noted as out-of-scope by user).

### 3. Visual Layout D-20 at 1024×768
expected: On the playing BK2 scoreboard, GD and HS stat rows are absent from both player cards. Ziel slot shows `set_target_points` (e.g., "50"). Large center score renders negative values (e.g., set a negative `bk2_state` score and reload — "-3" displays without layout issues). BK2 scoreboard fits without scroll on tablet resolution: phase chip + remaining badge visible.
result: [pending]

### 4. Build artifact cleanup verification
expected: Run `foreman start -f Procfile.dev` to regenerate `app/assets/builds/application.js`. Confirm the deleted `bk2_kombi_shot_controller` no longer appears in the rebuilt bundle (advisory — Stimulus will not instantiate a deleted controller regardless, but a clean bundle eliminates confusion). The JS source file is deleted and `index.js` no longer registers it.
result: [pending]

## Summary

total: 4
passed: 0
issues: 2
pending: 2
skipped: 0
blocked: 0
partial: 2

## Gaps

### G1. table_monitor 50000001 cannot be deleted from the webapp
severity: blocker
reported_by: user (manual UAT 2026-04-24)
source: "table_monitors/50000001 bekomme ich nicht gelöscht aus der webapp"
area: table_monitor destroy path
summary: User reports they cannot delete TableMonitor id=50000001 via the web UI. ID ≥ MIN_ID (50_000_000) is a local record so LocalProtector should not block it. Needs investigation — possibly a destroy route/controller/policy or button-handler regression, or a Pundit/CanCanCan authorization issue.
next_step: Reproduce in dev, check TableMonitorsController#destroy behavior, policy, and UI delete-button wiring.

### G2. Ballziel (set_target_points) is not honored in Serienspiel
severity: warning
reported_by: user (manual UAT 2026-04-24)
source: "Ballziel wird nicht berücksichtigt - ansonsten im Serienspiel ok"
area: Bk2Kombi::CommitInning / AdvanceMatchState / bk2_options.set_target_points flow
summary: Detail-view (Plan 38.3-06) exposed a Ballziel picker (50/60/70) intended to populate `bk2_options[:set_target_points]`. In Serienspiel mode the set should close when the cumulative inning total reaches this target, but user reports the value is ignored (set continues past target, or target isn't compared). Needs end-to-end trace: form → controller permit → persistence → AdvanceMatchState#close_set_if_needed (or equivalent).
next_step: Trace bk2_options[:set_target_points] from form submission through to set-close logic; confirm either the field is not being persisted, or CommitInning is not reading it.

### Out-of-scope notes (acknowledged by user, not gaps)
- Detail-view appearance "unverändert" per user — user explicitly said "war aber wohl auch nicht aufgabe dieser phase" (noted but not scoped to fix here)
