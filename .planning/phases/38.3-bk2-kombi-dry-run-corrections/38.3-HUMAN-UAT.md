---
status: partial
phase: 38.3-bk2-kombi-dry-run-corrections
source:
  - 38.3-VERIFICATION.md
started: 2026-04-24T18:00:00Z
updated: 2026-04-24T18:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Full Reflex Round-Trip (add_n accumulation → next_step commit)
expected: Open a BK2 match in playing state in the browser. Click +5 then +10 on the active player card. Small red inning-score shows 15. Click the swap icon. Inning total commits to the set score (set_scores.1.playera increases by 15 in SP, or opponent's score increases by 15 in DZ). Inning-score resets to 0 for the new player. Aufnahmewechsel via swap icon commits accumulated total; player_at_table flips; bk2_state persisted correctly.
result: [pending]

### 2. Full Start-Game Flow (detail-view → warmup → shootout → playing)
expected: Open the BK2 detail-view, select BK2 50, verify only Ballziel + DZ-max + SP-max inputs appear (not the old 4-button first_set_mode matrix). Change DZ-max to 3, SP-max to 7, click Start Game. Navigate through warmup, shootout (confirm 4 buttons). Click "Spieler A – Serienspiel". The scoreboard shows the Serienspiel phase chip + "Aufnahmen übrig: 7". Full AASM chain: detail config → warmup → 4-button shootout → playing scoreboard with phase chip + correct SP remaining badge matching the configured max innings.
result: [pending]

### 3. Visual Layout D-20 at 1024×768
expected: On the playing BK2 scoreboard, GD and HS stat rows are absent from both player cards. Ziel slot shows `set_target_points` (e.g., "50"). Large center score renders negative values (e.g., set a negative `bk2_state` score and reload — "-3" displays without layout issues). BK2 scoreboard fits without scroll on tablet resolution: phase chip + remaining badge visible.
result: [pending]

### 4. Build artifact cleanup verification
expected: Run `foreman start -f Procfile.dev` to regenerate `app/assets/builds/application.js`. Confirm the deleted `bk2_kombi_shot_controller` no longer appears in the rebuilt bundle (advisory — Stimulus will not instantiate a deleted controller regardless, but a clean bundle eliminates confusion). The JS source file is deleted and `index.js` no longer registers it.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
