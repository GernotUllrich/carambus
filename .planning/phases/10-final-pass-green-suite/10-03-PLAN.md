---
phase: 10-final-pass-green-suite
plan: 03
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - test/characterization/region_cc_char_test.rb
  - test/snapshots/vcr/region_cc_get_cc.yml
  - test/snapshots/vcr/region_cc_post_cc.yml
  - test/snapshots/vcr/region_cc_sync_tournaments.yml
  - test/snapshots/vcr/region_cc_sync_parties.yml
  - test/snapshots/vcr/region_cc_sync_game_details.yml
  - test/snapshots/vcr/region_cc_fix_tournament_structure.yml
  - test/snapshots/vcr/region_cc_ensure_admin_base_url.yml
autonomous: false
requirements: [QUAL-04]

must_haves:
  truths:
    - "VCR cassettes are recorded for 7 RegionCcCharTest tests if external service is accessible"
    - "If cassettes are recorded, tests run in replay mode without skips"
    - "If recording fails, each skip has documented justification in the test file"
  artifacts:
    - path: "test/snapshots/vcr/"
      provides: "VCR cassette YAML files for RegionCcCharTest"
  key_links:
    - from: "test/characterization/region_cc_char_test.rb"
      to: "test/snapshots/vcr/"
      via: "VCR.use_cassette"
      pattern: "with_vcr_cassette"
---

<objective>
Attempt VCR cassette recording for 7 skipped RegionCcCharTest characterization tests.

Purpose: Per D-03, attempt to record VCR cassettes using NBV Region[1] dev credentials. If the external ClubCloud service is accessible and credentials work, record cassettes so tests run in replay mode without skips. If not, document each skip with an accepted justification. This resolves QUAL-04 (all skipped tests resolved).

Output: Either recorded VCR cassettes (tests pass without skips) or documented skip justifications.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-final-pass-green-suite/10-CONTEXT.md
@.planning/phases/10-final-pass-green-suite/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attempt VCR cassette recording for RegionCcCharTest</name>
  <files>test/characterization/region_cc_char_test.rb</files>
  <read_first>
    - test/characterization/region_cc_char_test.rb (full file — understand VCR setup, cassette names, test structure)
    - test/support/vcr_setup.rb (VCR configuration, sensitive data filtering)
    - config/credentials/development.yml.enc (check if ClubCloud credentials exist — use `RAILS_ENV=test bin/rails runner "puts RegionCc.first&.base_url"` to verify)
  </read_first>
  <action>
    **Step 1: Check credentials availability.**

    Run: `RAILS_ENV=test bin/rails runner "puts RegionCc.first&.base_url"`

    If this returns nil or errors, credentials are not available. Skip to Step 3.

    **Step 2: If credentials are available, attempt recording.**

    Run: `RECORD_VCR=true bin/rails test test/characterization/region_cc_char_test.rb`

    Check if cassette files were created: `ls test/snapshots/vcr/region_cc_*.yml`

    If cassettes were created, verify replay mode works:
    `bin/rails test test/characterization/region_cc_char_test.rb`

    This should show 0 skips for RegionCcCharTest.

    **Step 3: If credentials are unavailable or recording fails.**

    The current skip mechanism is already well-documented in the test file (line 41):
    ```
    skip "VCR-Kassette '#{name}.yml' fehlt. Aufnehmen mit: RECORD_VCR=true bin/rails test test/characterization/region_cc_char_test.rb"
    ```

    This is an ACCEPTABLE skip per STANDARDS.md E03 exception #2 (requires live external service). Add a comment block at the top of the test file documenting the skip acceptance:

    ```ruby
    # SKIP JUSTIFICATION (QUAL-04, Phase 10):
    # These 7 tests require VCR cassettes recorded against live ClubCloud API.
    # Cassettes not yet recorded — external service credentials required.
    # Skips are accepted per STANDARDS.md E03 exception #2 (live external service).
    # To record: RECORD_VCR=true bin/rails test test/characterization/region_cc_char_test.rb
    ```
  </action>
  <verify>
    <automated>bin/rails test test/characterization/region_cc_char_test.rb 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Either: ls test/snapshots/vcr/region_cc_*.yml shows 7 cassette files AND bin/rails test test/characterization/region_cc_char_test.rb shows 0 skips
    - Or: grep "SKIP JUSTIFICATION" test/characterization/region_cc_char_test.rb returns a match AND skips are documented as accepted
  </acceptance_criteria>
  <done>VCR cassettes either recorded (tests pass without skips) or skip justification documented in test file. QUAL-04 resolved for these 7 tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify VCR cassette recording outcome</name>
  <files>test/characterization/region_cc_char_test.rb, test/snapshots/vcr/</files>
  <action>
    VCR cassette recording was attempted for 7 RegionCcCharTest tests. User verifies the outcome and decides whether to accept skip justifications or provide credentials for manual recording.
  </action>
  <what-built>VCR cassette recording was attempted for 7 RegionCcCharTest tests. If credentials were available, cassettes should be in test/snapshots/vcr/. If not, skip justifications were added to the test file.</what-built>
  <how-to-verify>
    1. Check if VCR cassettes were recorded: `ls test/snapshots/vcr/region_cc_*.yml`
    2. If cassettes exist, run: `bin/rails test test/characterization/region_cc_char_test.rb --verbose` — should show 0 skips
    3. If no cassettes, verify skip justification: `head -15 test/characterization/region_cc_char_test.rb` — should show SKIP JUSTIFICATION block
    4. If you have ClubCloud credentials and want to record cassettes manually, run: `RECORD_VCR=true bin/rails test test/characterization/region_cc_char_test.rb`
  </how-to-verify>
  <verify>
    <automated>bin/rails test test/characterization/region_cc_char_test.rb 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - User has verified VCR outcome (either cassettes recorded or skips accepted)
  </acceptance_criteria>
  <done>User approved VCR cassette outcome.</done>
  <resume-signal>Type "approved" if cassettes are recorded or skip justification is acceptable. Type "record" if you want to provide credentials and attempt recording. Type "skip accepted" if you accept the skips without recording.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test env -> ClubCloud API | VCR recording sends real HTTP requests to external service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | I (Info Disclosure) | VCR cassettes | mitigate | VCR setup filters sensitive data (CC_USERNAME, CC_PASSWORD) from cassettes per vcr_setup.rb configuration |
| T-10-04 | S (Spoofing) | ClubCloud credentials | accept | Dev credentials only; test environment; no production access |
</threat_model>

<verification>
Run `bin/rails test test/characterization/region_cc_char_test.rb` — either 0 skips (cassettes recorded) or 7 skips with documented justification.
Check VCR sensitive data filtering: if cassettes exist, `grep -c "CC_USERNAME\|CC_PASSWORD" test/snapshots/vcr/region_cc_*.yml` should return 0 (filtered).
</verification>

<success_criteria>
- All 7 RegionCcCharTest skips are resolved: either cassettes recorded and tests pass, or skips are documented with accepted justification per STANDARDS.md E03 exception #2
- No sensitive credentials leaked into cassette files
</success_criteria>

<output>
After completion, create `.planning/phases/10-final-pass-green-suite/10-03-SUMMARY.md`
</output>
