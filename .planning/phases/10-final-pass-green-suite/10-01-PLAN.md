---
phase: 10-final-pass-green-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/test_helper.rb
  - test/fixtures/clubs.yml
  - test/fixtures/seasons.yml
  - test/fixtures/table_monitors.yml
  - test/fixtures/game_plans.yml
  - test/fixtures/party_monitors.yml
autonomous: true
requirements: [QUAL-04, PASS-01]

must_haves:
  truths:
    - "ApiProtectorTestOverride exists in test_helper.rb and prevents silent rollbacks for ApiProtector models"
    - "club_bochum fixture exists and all PlayerSearchTest/LocationSearchTest tests pass"
    - "season_2024 fixture exists and tests referencing it pass"
    - "table_monitors fixture file exists with a valid :one entry"
    - "game_plans and party_monitors fixtures have valid JSON in data fields"
    - "Remaining failure/error count is significantly reduced from 106 baseline"
  artifacts:
    - path: "test/test_helper.rb"
      provides: "ApiProtectorTestOverride module prepended to ApiProtector"
      contains: "ApiProtectorTestOverride"
    - path: "test/fixtures/clubs.yml"
      provides: "club_bochum fixture entry"
      contains: "club_bochum"
    - path: "test/fixtures/seasons.yml"
      provides: "season_2024 fixture entry"
      contains: "season_2024"
    - path: "test/fixtures/table_monitors.yml"
      provides: "table_monitors fixture for controller tests"
      contains: "one:"
    - path: "test/fixtures/game_plans.yml"
      provides: "Valid JSON in data field"
    - path: "test/fixtures/party_monitors.yml"
      provides: "Valid JSON in data field"
  key_links:
    - from: "test/test_helper.rb"
      to: "app/models/api_protector.rb"
      via: "prepend ApiProtectorTestOverride"
      pattern: "ApiProtector.prepend"
    - from: "test/models/player_search_test.rb"
      to: "test/fixtures/clubs.yml"
      via: "clubs(:club_bochum)"
      pattern: "club_bochum"
---

<objective>
Fix test infrastructure: ApiProtector test override, missing fixtures, and invalid fixture data.

Purpose: Resolve the bulk of the 106 test failures/errors (75 errors + 31 failures) by fixing shared infrastructure — missing fixtures account for ~36 errors, invalid JSON fixtures cause ~4 errors, and table_monitors fixture causes ~10 errors. ApiProtectorTestOverride (per D-02) prevents silent test rollbacks for 7 model classes. After these fixes, re-run the suite to measure the actual remaining failures.

Output: Updated test_helper.rb, 5 fixture files fixed/created, re-run results showing reduced failure count.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-final-pass-green-suite/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ApiProtectorTestOverride and fix missing/invalid fixtures</name>
  <files>test/test_helper.rb, test/fixtures/clubs.yml, test/fixtures/seasons.yml, test/fixtures/table_monitors.yml, test/fixtures/game_plans.yml, test/fixtures/party_monitors.yml</files>
  <read_first>
    - test/test_helper.rb (understand existing LocalProtectorTestOverride pattern)
    - app/models/api_protector.rb (understand disallow_saving_local_records method signature)
    - app/models/application_record.rb (understand local_server? method)
    - test/fixtures/clubs.yml (existing club fixtures)
    - test/fixtures/seasons.yml (existing season fixtures)
    - test/fixtures/regions.yml (existing region fixtures for foreign key references)
    - test/fixtures/game_plans.yml (see MyText placeholder in data field)
    - test/fixtures/party_monitors.yml (see MyText placeholder in data field)
    - test/models/player_search_test.rb (understand what club_bochum and season_2024 need)
    - test/models/location_search_test.rb (also references club_bochum)
    - test/controllers/table_monitors_controller_test.rb (references table_monitors(:one))
    - app/models/table_monitor.rb (understand schema for table_monitors fixture)
    - db/schema.rb (check table_monitors, game_plans, party_monitors column definitions)
  </read_first>
  <action>
    **Per D-02: Add ApiProtectorTestOverride to test_helper.rb.**

    Add a module mirroring the existing LocalProtectorTestOverride pattern. Place it directly after LocalProtectorTestOverride:

    ```ruby
    # Disable ApiProtector for all test records
    module ApiProtectorTestOverride
      def disallow_saving_local_records
        # Skip protection in test environment
        true
      end
    end

    ApiProtector.prepend(ApiProtectorTestOverride)
    ```

    Note: ApiProtector's method is `disallow_saving_local_records` (checks `id > MIN_ID` and `!local_server?`). The override must return `true` (not raise Rollback). Read the actual method in api_protector.rb to confirm the exact method name and return value semantics.

    **Fix missing fixtures:**

    1. **test/fixtures/clubs.yml** — Add `club_bochum` entry with id >= 50_000_000 (local record range), a name like "VfL Bochum Billard", shortname "BOC", and timestamps. Look at existing entries for the pattern.

    2. **test/fixtures/seasons.yml** — Add `season_2024` entry with id >= 50_000_000, name "2023/2024" (the season that covers 2024), and timestamps. CRITICAL: the season name must NOT conflict with existing fixtures "2025/2026" and "2024/2025" because there is a unique index on seasons.name. Use a different name like "2023/2024".

    3. **test/fixtures/table_monitors.yml** — This file does NOT exist. Create it with a `:one` entry. Read the table_monitors schema from db/schema.rb to determine required columns. The fixture needs at minimum: id (>= 50_000_000), state (a valid AASM state — read the model to find initial state), and a valid data field (JSON string like "{}"). Check if tournament_monitor_id is required — if so, you may need to also check/create a tournament_monitors fixture.

    **Fix invalid fixture data:**

    4. **test/fixtures/game_plans.yml** — Replace `data: MyText` with `data: '{}'` (valid empty JSON string) for both entries.

    5. **test/fixtures/party_monitors.yml** — Replace `data: MyText` with `data: '{}'` (valid empty JSON string) for both entries.

    For the table_monitors fixture, also check whether the controller test expects specific attributes (tournament_monitor_id, game_id, etc.) — read the test file to see what attributes it accesses on `@table_monitor`.
  </action>
  <verify>
    <automated>bin/rails test test/models/player_search_test.rb test/models/location_search_test.rb test/controllers/table_monitors_controller_test.rb test/controllers/game_plans_controller_test.rb test/controllers/party_monitors_controller_test.rb 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ApiProtectorTestOverride" test/test_helper.rb returns 2 (module def + prepend)
    - grep -c "ApiProtector.prepend" test/test_helper.rb returns 1
    - grep -c "club_bochum" test/fixtures/clubs.yml returns at least 1
    - grep -c "season_2024" test/fixtures/seasons.yml returns at least 1
    - test -f test/fixtures/table_monitors.yml (file exists)
    - grep "MyText" test/fixtures/game_plans.yml returns no matches
    - grep "MyText" test/fixtures/party_monitors.yml returns no matches
  </acceptance_criteria>
  <done>ApiProtectorTestOverride prepended in test_helper.rb. club_bochum fixture in clubs.yml. season_2024 fixture in seasons.yml. table_monitors.yml created with :one entry. game_plans.yml and party_monitors.yml have valid JSON data fields. The 12 PlayerSearchTest errors, 7 LocationSearchTest errors, 10 TableMonitorsControllerTest errors, and 4 JSON::ParserError errors are resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run full suite and document remaining failures</name>
  <files>none (diagnostic task)</files>
  <read_first>
    - test/test_helper.rb (verify ApiProtectorTestOverride is in place)
  </read_first>
  <action>
    Run the full test suite: `bin/rails test 2>&1`

    Capture the output and categorize remaining failures/errors. Record:
    1. Total runs, assertions, failures, errors, skips
    2. Each remaining failure/error grouped by test file and root cause
    3. Which failures are likely caused by ApiProtector (TableLocal, TournamentLocal, etc.)
    4. Which failures need fixture or test code fixes

    This diagnostic output is CRITICAL for Plan 10-02 — it tells the executor exactly what remains after infrastructure fixes.

    Write a brief summary in the SUMMARY for this plan documenting the before/after counts and remaining failure categories.
  </action>
  <verify>
    <automated>bin/rails test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - bin/rails test completes without crash
    - Error count is lower than 75 (baseline)
    - Failure count may change (some may resolve, some new ones may appear)
  </acceptance_criteria>
  <done>Full suite re-run after infrastructure fixes. Remaining failure/error count documented. Categories of remaining issues identified for Plan 10-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — test infrastructure only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T (Tampering) | ApiProtectorTestOverride | accept | Test-only code; disabled in production by environment check. Same pattern as existing LocalProtectorTestOverride. |
</threat_model>

<verification>
Run `bin/rails test` and confirm error count dropped significantly from 75 errors baseline.
Run `grep -c "ApiProtectorTestOverride" test/test_helper.rb` and confirm output is 2.
Run `bin/rails test test/models/player_search_test.rb` and confirm 0 errors, 0 failures.
</verification>

<success_criteria>
- ApiProtectorTestOverride is active in test environment
- Missing fixture errors (club_bochum, season_2024, table_monitors) are eliminated
- JSON::ParserError from MyText fixtures is eliminated
- Total errors reduced from 75 to a measurably lower number
- Remaining failures are documented for Plan 10-02
</success_criteria>

<output>
After completion, create `.planning/phases/10-final-pass-green-suite/10-01-SUMMARY.md`
</output>
