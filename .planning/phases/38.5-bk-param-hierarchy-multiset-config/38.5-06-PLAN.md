---
phase: 38.5-bk-param-hierarchy-multiset-config
plan: 06
type: execute
wave: 3
depends_on: [01, 04, 05]
files_modified:
  - test/system/bk2_scoreboard_test.rb
autonomous: true
decisions_addressed: [D-11, D-12, D-13, D-14, D-15, D-16]

must_haves:
  truths:
    - "All 4 RED tests from Plan 01 (test/integration/bk_param_latent_bugs_test.rb) turn GREEN without any edits to that file"
    - "End-to-end system test verifies BK-2kombi DZ-Phase: -3 from Spieler A scores +3 for Spieler B (full UI to data flow, not just unit-level)"
    - "End-to-end system test verifies BK-2 / BK50 / BK100: negative score input stays signed on the shooter (not clamped to 0)"
    - "No lazy-bake fallback is added to ScoreEngine — D-15 forbids migration code; the resolver fires only at the two hooks (start_game + set-boundary)"
    - "In-flight pre-Phase-38.5 BK-* TableMonitors that lack the new data keys correctly fall back to false / false per D-04 (preserves zero-bake-needed safety net)"
    - "BCW deployment runbook documents the seed-on-carambus_api-first sequence (Phase 38.5 D-08 sync path)"
  artifacts:
    - path: "test/system/bk2_scoreboard_test.rb"
      provides: "End-to-end DZ-credit-opponent + BK-2 negative-input regression tests (system level)"
      contains: "Phase 38.5"
  key_links:
    - from: "test/system/bk2_scoreboard_test.rb"
      to: "app/models/table_monitor/score_engine.rb (Plan 04) + app/services/bk_param_resolver.rb (Plan 02) + app/services/table_monitor/game_setup.rb + app/services/table_monitor/result_recorder.rb (Plan 05)"
      via: "Capybara visit + DOM interaction triggering full controller -> bake -> ScoreEngine flow"
      pattern: "visit table_monitor_path"
---

<objective>
Phase 38.5 final wave: verify the integration is real. Plan 04 fixed the predicates,
Plan 05 wired the bake hooks, Plan 03 seeded the Discipline data — but until an
end-to-end test exercises the full path (controller -> resolver -> ScoreEngine ->
DOM), the integration is unverified.

This plan adds:
1. Two E2E system tests that drive the full BK-2kombi DZ-credit-opponent flow
   and the BK-2 negative-stays-signed flow through the actual UI.
2. A verification step that re-runs Plan 01's 4 RED tests and confirms they all
   turn GREEN (with NO edits to test/integration/bk_param_latent_bugs_test.rb —
   that file is the GSD verification contract).
3. An explicit decision RECORD against Open Question 2 (research §Open Questions):
   no lazy bake, no migration code. D-15 forbids migration code; in-flight pre-38.5
   BK-* matches fall back to false/false (Karambol-safe behaviour) until they
   restart, at which point Hook 1 bakes the correct values. This is acceptable
   because:
   - Phase 38.5 ships in carambus_master first; carambus_api seeds the new
     Discipline data; BCW pulls + deploys; sync propagates new keys.
   - During the sync window, in-flight matches behave as today (Karambol-style
     clamp / signed-on-self for BK-*). This is no WORSE than today — it's exactly
     today's behaviour, which is what the user has been running with successfully
     until now (with the documented latent bugs D-11 / D-12).
   - The next start_game on each BK-* TableMonitor bakes correctly. No data is
     lost; no manual migration is required.

extend-before-build: this plan does not modify ANY production code. It only adds
tests + documentation. The principle here is that the simpler choice (no lazy
bake) wins because lazy-bake would couple ScoreEngine to BkParamResolver, and
ScoreEngine has no reference to the TableMonitor AR model — it only has a Hash.
Adding the reference would require a refactor that's out of scope.

Output: 1 file modified (test/system/bk2_scoreboard_test.rb) + Plan 01 verification
documented in SUMMARY + decision against lazy-bake recorded for future planners.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-CONTEXT.md
@.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-RESEARCH.md
@.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-01-SUMMARY.md
@.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-02-SUMMARY.md
@.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-03-SUMMARY.md
@.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-04-SUMMARY.md
@.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-05-SUMMARY.md
@test/system/bk2_scoreboard_test.rb

<interfaces>
The existing bk2_scoreboard_test.rb after Phase 38.4 has 35 test methods covering
17 preserved + 18 new regression probes for I1-I9 (excluding I6). Plan 06 adds
2 new tests at the end of the file (additive, no edits to existing tests).

Existing test pattern (verbatim, from STATE.md Phase 38.4 conventions):
- Capybara visit table_monitor_path(tm)
- DOM interaction via fill_in / click_on / find().click
- Assertion via assert_selector / page.has_text? / data attribute reads

For the BK-2kombi DZ-credit-opponent test:
- Set up a TableMonitor with free_game_form=bk2_kombi, in playing state, set 1
  active (DZ phase). Bake hook 1 has run via start_game (real path).
- Spieler A makes a -3 input via the existing point-entry bottom bar (the
  Karambol-parallel bar from Phase 38.3).
- Assert: Spieler B's set-score increases by 3, Spieler A's set-score unchanged.

For the BK-2 negative-stays-signed test:
- Set up a TableMonitor with free_game_form=bk_2 (single-set match, no kombi).
- Spieler A starts at 5 points. Inputs -2 via the point-entry bar.
- Assert: Spieler A's score is 3 (signed-add: 5 + (-2) = 3, NOT clamped to 0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Plan 01 RED tests turn GREEN end-to-end</name>
  <files>test/system/bk2_scoreboard_test.rb</files>
  <read_first>
    - test/integration/bk_param_latent_bugs_test.rb (Plan 01 — verify it is unmodified by this plan)
    - test/system/bk2_scoreboard_test.rb (full file — Phase 38.4 35-method baseline)
    - .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-CONTEXT.md (D-11, D-12, D-13)
    - .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-RESEARCH.md (Open Question 2 — lazy bake decision: SKIP per D-15)
    - .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-04-SUMMARY.md (Plan 04 confirms Tests 2/3/4 from Plan 01 turn GREEN; Test 1 BK-2kombi DZ awaits Plan 05)
    - .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-05-SUMMARY.md (Plan 05 confirms Hook 1 + Hook 2 wired)
  </read_first>
  <behavior>
    Two new system tests at the end of bk2_scoreboard_test.rb:

    Test A — BK-2kombi DZ-credit-opponent end-to-end (D-11 closure):
    1. Build / fixture a User, Tournament, TournamentMonitor, TableMonitor for
       BK-2kombi. (Reuse existing helper from bk2_scoreboard_test.rb — likely
       a setup_bk2_kombi_match method or fixture combo.)
    2. Visit table_monitor_path(tm) as admin.
    3. Click "Spielen starten" / start_game (the real start_game path triggers
       Hook 1 -> BkParamResolver.bake! writes effective_discipline=bk_2plus +
       allow_negative_score_input=true + negative_credits_opponent=true).
    4. Confirm via DOM data attribute or rendered score panel that the match is
       in DZ phase, set 1, both scores 0.
    5. Click the -3 button (Karambol-parallel point-entry bottom bar). Player A
       is the active shooter.
    6. Assert: Spieler B's current-set score is 3. Spieler A's current-set score
       is 0. (This is the D-11 fix end-to-end.)
    7. Trigger set-close (e.g., Spieler B reaches balls_goal=70 — or simulate
       advance via UI). Bake hook 2 fires.
    8. Assert: data["effective_discipline"] now == "bk_2" (set 2 = SP phase).
       data["negative_credits_opponent"] == false (the BK-2 default — NOT credit
       opponent in SP).
    9. (Optional, if feasible without too much fixture overhead): make a -2 input
       in the new SP set; assert Spieler A's score becomes -2 (signed) NOT 0
       (clamped) and NOT credited to opponent.

    Test B — BK-2 single-set negative-stays-signed end-to-end (D-12 closure):
    1. Build a Tournament + TableMonitor for free_game_form=bk_2 (single match,
       not kombi).
    2. Visit table_monitor_path. Start game. Hook 1 bakes
       effective_discipline=bk_2 + allow_negative_score_input=true +
       negative_credits_opponent=false.
    3. Player A scores +5 (via point-entry buttons).
    4. Player A inputs -2.
    5. Assert: Spieler A's score is 3 (5 + (-2) = 3, signed-add, NOT clamped to 0).
       Spieler B's score is unchanged at 0 (not credited the opponent — D-12 split
       from D-11).

    These are END-TO-END tests. They go through the controller -> GameSetup ->
    ResultRecorder/CommitInning -> ScoreEngine flow. If anything in the integration
    is broken, these tests fail.

    The tests are NEW additions, not edits to existing tests in bk2_scoreboard_test.rb.

    Test names MUST reference D-11 and D-12 explicitly.
  </behavior>
  <action>
    Open test/system/bk2_scoreboard_test.rb. Read the existing setup blocks at
    file top to understand the fixture/factory conventions. Read 2-3 existing
    tests to understand the Capybara DOM-interaction pattern.

    Add 2 new tests at the end of the test class (before the final `end`):

      # Phase 38.5 D-11 / D-13 — end-to-end DZ-credit-opponent regression.
      # Verifies the resolver-bake -> ScoreEngine integration: BK-2kombi DZ-Phase
      # negative input credits the OPPONENT, not the shooter. Pre-Phase-38.5 this
      # was a latent bug (bk_credit_negative_to_opponent? matched only "bk_2plus",
      # not the DZ phase of "bk2_kombi"). After Plans 04+05 land, this test passes.
      test "Phase 38.5 D-11: BK-2kombi DZ -3 credits Spieler B end-to-end" do
        # Use the existing BK-2kombi setup helper (look at file top for naming).
        tm = create_bk2_kombi_table_monitor_via_existing_helper
        sign_in_admin
        visit table_monitor_path(tm)
        click_on_start_game_button
        # Resolver hook 1 should have written effective_discipline=bk_2plus + both flags
        tm.reload
        assert_equal "bk_2plus", tm.data["effective_discipline"], "Hook 1 sets DZ effective_discipline"
        assert_equal true, tm.data["allow_negative_score_input"], "BK-2plus default for negative input"
        assert_equal true, tm.data["negative_credits_opponent"], "BK-2plus default for opponent credit"

        # Spieler A as active shooter, click -3 button
        click_negative_three_button_for_player_a
        # Assert via DOM (rendered score panel)
        assert_selector "[data-player='b'] [data-current-set-score]", text: "3"
        assert_selector "[data-player='a'] [data-current-set-score]", text: "0"
      end

      # Phase 38.5 D-12 / D-13 — end-to-end BK-2 negative-stays-signed regression.
      # Verifies that BK-2 (and by extension BK50/BK100) accept negative input
      # signed on the shooter's own score, not clamped to 0. Pre-Phase-38.5 the
      # ScoreEngine#allow_negative_scores? matched only "bk2_kombi" — BK-2/BK50/
      # BK100 negative inputs were silently clamped (D-12 latent bug).
      test "Phase 38.5 D-12: BK-2 -2 stays signed on Spieler A end-to-end" do
        tm = create_bk_2_table_monitor_via_existing_helper
        sign_in_admin
        visit table_monitor_path(tm)
        click_on_start_game_button
        tm.reload
        assert_equal "bk_2", tm.data["effective_discipline"], "Hook 1 identity for non-kombi BK"
        assert_equal true, tm.data["allow_negative_score_input"], "BK-2 default true"
        assert_equal false, tm.data["negative_credits_opponent"], "BK-2 default false (signed-add)"

        click_plus_five_button_for_player_a   # A: 0 -> 5
        click_negative_two_button_for_player_a # A: 5 -> 3 (signed, NOT 0 clamped)
        assert_selector "[data-player='a'] [data-current-set-score]", text: "3"
        assert_selector "[data-player='b'] [data-current-set-score]", text: "0"
      end

    Helper methods (create_bk2_kombi_table_monitor_via_existing_helper,
    create_bk_2_table_monitor_via_existing_helper, sign_in_admin,
    click_on_start_game_button, click_negative_three_button_for_player_a,
    click_plus_five_button_for_player_a, click_negative_two_button_for_player_a):
    these are placeholders. Replace with whatever pattern the existing 35 tests
    in bk2_scoreboard_test.rb use. If the file has top-of-file factories /
    Capybara matchers, use those. If it uses `find("button[data-input='-3']").click`
    inline, copy that idiom.

    DO NOT introduce a new test helper file. DO NOT change the existing test
    infrastructure. Just match the file's existing style.

    If a particular DOM interaction (e.g., -3 button) does not exist in the
    Karambol-parallel bottom bar after Phase 38.3, the test executor MUST stop
    and verify the actual DOM structure before fabricating a click target.
    Reference: Phase 38.3-04 added the Karambol-parallel point-entry bottom bar
    "(-10 -5 -1 | reset | +1 +5 +10) + number popup" — the test should use the
    -1 button 3x or the popup for arbitrary -3, depending on which is realistic.

    Naming MUST reference Phase 38.5 + D-11 / D-12 in the test description.
  </action>
  <verify>
    <automated>bin/rails test test/system/bk2_scoreboard_test.rb -n "/Phase 38.5/"</automated>
  </verify>
  <acceptance_criteria>
    - test/system/bk2_scoreboard_test.rb contains 2 new tests with names referencing "Phase 38.5 D-11" and "Phase 38.5 D-12"
    - The 2 new tests visit table_monitor_path and click through the real UI (not direct service-level calls)
    - The 2 new tests assert tm.data["effective_discipline"] is correctly set after start_game
    - The 2 new tests assert tm.data["allow_negative_score_input"] and tm.data["negative_credits_opponent"] are correctly set after start_game
    - The 2 new tests pass when run after Plans 04+05 land
    - Existing 35 tests in bk2_scoreboard_test.rb are UNCHANGED (file diff shows additions only at end-of-class, no modifications mid-file)
    - test/integration/bk_param_latent_bugs_test.rb (Plan 01) is UNCHANGED — no edits in this Plan
    - Running `bin/rails test test/integration/bk_param_latent_bugs_test.rb` produces 4 PASSES (Plan 01 RED tests turn GREEN)
  </acceptance_criteria>
  <done>
    Two E2E system tests prove the full bake-flow works through the UI. Plan 01's
    4 RED tests now pass without any edits to that file. The phase-level
    integration is verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document the no-lazy-bake decision + BCW deployment runbook</name>
  <files>.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-DEPLOYMENT-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-RESEARCH.md (Open Question 2 + Scenario Impact)
    - .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-CONTEXT.md (D-15, D-16)
    - .agents/skills/scenario-management/SKILL.md (carambus_api -> Version sync -> BCW)
    - .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-03-SUMMARY.md (seed sync path)
  </read_first>
  <action>
    Create file .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-DEPLOYMENT-RUNBOOK.md.

    Required sections:

    # Phase 38.5 Deployment Runbook

    ## Decision Record: No Lazy Bake (Open Question 2 closed)

    Researcher Open Question 2 raised: "Lazy bake vs. migration for in-flight
    TableMonitors". Decision: NEITHER. Rationale:
    - D-15 forbids migration code.
    - Lazy bake in ScoreEngine would couple ScoreEngine to BkParamResolver +
      TableMonitor AR; ScoreEngine currently only holds a Hash.
    - Pre-38.5 in-flight BK-* matches lack the new data keys -> ScoreEngine
      fallback false (D-04) -> behaves exactly as today (Karambol-style clamp /
      signed-on-self). This is the SAME as the latent-bug behaviour users have
      been running with successfully — no regression introduced.
    - Next start_game on each BK-* TableMonitor (a fresh game start) bakes
      correctly. No data lost; no manual migration step.

    ## Sync Path (carambus_api -> BCW + phat)

    1. Merge Phase 38.5 to master.
    2. Deploy carambus_api: `cd carambus_api && git pull && cap api deploy`.
    3. Run seed on carambus_api console:
       `cd /var/www/carambus_api/current && bin/rails runner script/seed_bk2_disciplines.rb`
    4. Wait for next Version sync cycle (or trigger manually) so BCW + phat
       receive new Discipline.data values.
    5. Deploy carambus_bcw: `cd carambus_bcw && git pull && cap bcw deploy`.
    6. Verify on BCW console:
       `Discipline.find_by(name: "BK-2plus").data` parses to a Hash containing
       `"allow_negative_score_input" => true` AND `"negative_credits_opponent" => true`.
    7. (Optional) Repeat for carambus_phat.

    ## Verification Checklist

    On BCW after deploy:
    - [ ] `bin/rails runner "puts JSON.parse(Discipline.find_by(name: 'BK-2plus').data)"` shows the two new keys with correct values.
    - [ ] Same for BK-2, BK50, BK100 (with negative_credits_opponent: false).
    - [ ] BK2-Kombi (id 107) shows `multiset_components: ["bk_2plus", "bk_2"]` and does NOT show the two BK-param keys (resolver looks up effective_discipline).
    - [ ] Start a fresh BK-2kombi training match. After "Spielen starten":
      `tm.reload.data["effective_discipline"]` == "bk_2plus".
      `tm.data["allow_negative_score_input"]` == true.
      `tm.data["negative_credits_opponent"]` == true.
    - [ ] Make a -3 input as Player A in DZ phase. Player B's set-score becomes 3.
      Player A's set-score stays 0. (D-11 closed end-to-end.)
    - [ ] Force a set-close. New set is SP phase. `tm.data["effective_discipline"]`
      flips to "bk_2". `tm.data["negative_credits_opponent"]` flips to false.
    - [ ] Start a fresh BK-2 training match. -2 input stays signed at -2 on shooter,
      not clamped to 0. (D-12 closed end-to-end.)

    ## Rollback Plan

    If a critical bug surfaces in production:
    1. Revert Phase 38.5 commits on master.
    2. Re-run seed on carambus_api: the seed script's `current.delete(...)` lines
       are idempotent guards — re-running an OLDER seed without the new keys
       does NOT remove them automatically. Manual rollback would require an
       explicit removal script. (Recommendation: forward-fix rather than rollback.)
    3. ScoreEngine's old free_game_form-string-equality bodies are git-revertible.
    4. The two new bake-hook calls in game_setup.rb + result_recorder.rb +
       advance_match_state.rb's rebake_at_set_open! are all revertible without
       touching data.

    Do NOT include any actual deployment commands beyond what's documented above.
    The runbook is a checklist for the human operator (Gernot), not an automated
    script.
  </action>
  <verify>
    <automated>test -f .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-DEPLOYMENT-RUNBOOK.md &amp;&amp; grep -q "Lazy Bake" .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-DEPLOYMENT-RUNBOOK.md &amp;&amp; grep -q "Sync Path" .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-DEPLOYMENT-RUNBOOK.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-DEPLOYMENT-RUNBOOK.md
    - File contains a "Decision Record: No Lazy Bake" section closing Open Question 2 from RESEARCH.md
    - File contains a "Sync Path" section with the carambus_api -> Version sync -> BCW + phat sequence
    - File contains a "Verification Checklist" with at least 6 checkboxes
    - File contains a "Rollback Plan" section
    - File explicitly references D-15 (no migration code) as the rationale for no lazy bake
    - File references script/seed_bk2_disciplines.rb in the runbook step
  </acceptance_criteria>
  <done>
    Operator runbook is in place. Future Gernot (or anyone deploying Phase 38.5)
    has a clear sequence + verification checklist + rollback plan.
  </done>
</task>

</tasks>

<verification>
- bin/rails test test/integration/bk_param_latent_bugs_test.rb produces 4 passes (Plan 01 RED -> GREEN).
- bin/rails test test/system/bk2_scoreboard_test.rb passes including the 2 new Phase 38.5 tests.
- bin/rails test test/services/bk_param_resolver_test.rb passes (Plan 02 baseline).
- bin/rails test test/services/bk2/advance_match_state_test.rb passes (Plan 05 hook tests).
- bin/rails test test/services/table_monitor/result_recorder_test.rb passes.
- bin/rails test test/models/table_monitor/score_engine_test.rb passes (Plan 04 truth-table tests).
- File .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-DEPLOYMENT-RUNBOOK.md exists with required sections.
</verification>

<success_criteria>
- All Plan 01 RED tests turn GREEN.
- 2 new E2E system tests prove the integration end-to-end (UI -> data -> ScoreEngine).
- Decision against lazy-bake is documented for future planners.
- BCW deployment runbook exists with sync sequence + verification checklist + rollback.
- No production code is modified in Plan 06 (test-only + docs-only).
</success_criteria>

<output>
After completion, create `.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-06-SUMMARY.md`
documenting: (a) Plan 01 RED -> GREEN verification (with test pass output if useful),
(b) the 2 new E2E tests added, (c) confirmation that test/integration/bk_param_latent_bugs_test.rb
was NOT edited in Plan 06, (d) the no-lazy-bake decision summary referencing D-15,
(e) the BCW deployment runbook location.
</output>
