---
phase: 38.5-bk-param-hierarchy-multiset-config
plan: 01
subsystem: testing
tags: [minitest, score-engine, bk2-kombi, regression, tdd, latent-bug]

# Dependency graph
requires:
  - phase: 38.4-bk2-kombi-post-dry-run-gaps
    provides: ScoreEngine#allow_negative_scores? + bk_credit_negative_to_opponent? predicate scaffolding (current free_game_form-equality bodies)
  - phase: 38.1-bk2-kombi-minimum-viable-support
    provides: BK-2kombi free_game_form constant, current scoring contract
provides:
  - test/integration/bk_param_latent_bugs_test.rb — RED-test characterization suite for D-11 (BK-2kombi DZ-credit-opponent) and D-12 (BK-2/BK50/BK100 negative-clamp) latent bugs
  - GSD verification contract — file is locked against edits in Plan 06; turning all 4 tests GREEN (without editing this file) is how Plan 06 proves Plans 04+05 land correctly
affects:
  - 38.5-04 (predicate rewrite — fix turns these RED tests GREEN)
  - 38.5-05 (bake-hook integration — end-to-end path also turns these GREEN)
  - 38.5-06 (verification — this file is the contract)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pre-fix RED-test seeded with post-resolver state: tests can be written before the fix lands AND remain decoupled from the resolver implementation by writing the post-bake state directly into the data hash."
    - "GSD verification contract: a test file shipped failing today, locked against edits, that turns GREEN automatically when the dependent plans land."

key-files:
  created:
    - test/integration/bk_param_latent_bugs_test.rb
  modified: []

key-decisions:
  - "Seed post-resolver state directly in data hash (not via BkParamResolver.bake!) so Plan 01 ships independently in Wave 1, decoupled from Plan 02."
  - "Use 4 separate tests (1 per BK-* form) instead of a single parameterized test, so each failure points at a specific discipline and the RED→GREEN transition per discipline is visible in test output."
  - "File header docstring explicitly names this the GSD verification contract for Plan 06; no edits permitted in Plan 06 even if the suite remains RED at that point."

patterns-established:
  - "Latent-bug regression contract: write the RED test BEFORE the fix, with the post-fix data shape, so it ships failing and passes automatically."

requirements-completed: []

# Metrics
duration: ~2min
completed: 2026-04-29
---

# Phase 38.5 Plan 01: RED-tests for latent BK-* score engine bugs (D-11, D-12) Summary

**4 RED Minitest integration tests document and lock in two latent ScoreEngine bugs (BK-2kombi DZ-credit-opponent, BK-2/BK50/BK100 negative-clamp); the file is the GSD verification contract that turns GREEN when Plans 04+05 land — without further edits.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-29T13:41:36Z
- **Completed:** 2026-04-29T13:43:41Z
- **Tasks:** 1 / 1
- **Files modified:** 1 created, 0 modified

## Accomplishments

- Created `test/integration/bk_param_latent_bugs_test.rb` with 4 RED tests:
  1. **BK-2kombi DZ-Phase: -3 credits opponent (D-11 RED→GREEN)** — locks in D-11 fix.
  2. **BK-2 single-set: -2 stays signed at player (D-12 RED→GREEN)** — locks in D-12 fix for BK-2.
  3. **BK50 single-set: -2 stays signed at player (D-12 RED→GREEN)** — locks in D-12 fix for BK50.
  4. **BK100 single-set: -2 stays signed at player (D-12 RED→GREEN)** — locks in D-12 fix for BK100.
- Confirmed RED state today: `bin/rails test test/integration/bk_param_latent_bugs_test.rb` → **4 runs, 4 failures, 0 errors, 0 skips**.
- Each failure message references the specific decision (D-11 / D-12) and the bug class (DZ-credit-opponent vs negative-clamp).
- File header docstring explicitly names this the GSD verification contract for Plan 06 — DO NOT edit during Plan 06.
- `build_data_hash` helper centralizes the post-resolver shape (`effective_discipline`, `allow_negative_score_input`, `negative_credits_opponent`) so tests pass deterministically once Plan 04 rewrites the predicate bodies — Plan 02's resolver does not need to run.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED-tests for D-11 and D-12 latent bugs** — `3cbe731e` (test)

## Files Created/Modified

- `test/integration/bk_param_latent_bugs_test.rb` (created, 151 lines) — 4 Minitest integration tests + `build_data_hash` helper, inheriting from `ActiveSupport::TestCase`.

## Decisions Made

- **Seed post-resolver state directly:** The tests write `effective_discipline`, `allow_negative_score_input`, and `negative_credits_opponent` into the `data` hash explicitly, so they pass after the predicate rewrite (Plan 04) without needing the resolver (Plan 02) to run. This decouples Plan 01 from Plan 02 and lets Plan 01 ship independently in Wave 1.
- **One test per discipline (4 tests, not parametrized):** Each failure message points at a specific BK-* form, and the RED→GREEN transition per form is auditable in test output.
- **Integration directory, not models/table_monitor/:** Avoids file-conflict with Plan 04 which rewrites `score_engine_test.rb`. Per plan `<objective>`.
- **No edits to existing files:** Verified via `git diff HEAD~1 --stat -- 'app/' 'config/' 'db/' 'test/models/'` → empty.

## Deviations from Plan

None — plan executed exactly as written. The `<action>` block instructed creating one new test file with 4 tests + `build_data_hash` helper, and that is exactly what shipped.

## Verification Confirmation

Per the plan's `<verification>` block:

- ✓ `bin/rails test test/integration/bk_param_latent_bugs_test.rb` produces **4 failures** (D-11 ×1, D-12 ×3).
- ✓ Each failure message references its specific decision (D-11 or D-12) and discipline.
- ✓ Tests are NOT skipped — 0 skips in the test run; only `skip` substring in the file is a comment word at line 31.
- ✓ No edits to existing files outside the new test file.

Per the plan's `<acceptance_criteria>`:

- ✓ File exists at `test/integration/bk_param_latent_bugs_test.rb`
- ✓ File contains `class BkParamLatentBugsTest`
- ✓ File contains 4 `test "..."` blocks
- ✓ File contains `build_data_hash` helper definition
- ✓ File header comment contains "Phase 38.5 D-13" and "RED→GREEN"
- ✓ All 4 tests reference `TableMonitor::ScoreEngine.new`
- ✓ Test 1 asserts `data["playerb"]["innings_redo_list"][-1] == 3` after `add_n_balls(-3, "playera")`
- ✓ Tests 2/3/4 assert `data["playera"]["innings_redo_list"][-1] == -2`
- ✓ Running the test against unmodified score_engine.rb produces 4 failures (confirmed)
- ◯ After Plans 04+05 land: tests turn GREEN — *not yet runnable; verified in Plan 06*

## Today's Bug Behaviour (RED Failures)

| # | Test | Expected | Actual today | Reason |
|---|------|----------|--------------|--------|
| 1 | BK-2kombi DZ −3 → opponent +3 | `playerb` redo[-1] == 3 | 0 | `bk_credit_negative_to_opponent?` returns false for `bk2_kombi`; falls into signed-add branch on A. |
| 2 | BK-2 −2 → signed | `playera` redo[-1] == −2 | 0 | `allow_negative_scores?` returns false for `bk_2`; line 84 gate rejects → `should_process_input = false` → no write. |
| 3 | BK50 −2 → signed | `playera` redo[-1] == −2 | 0 | Same as Test 2 with `bk50`. |
| 4 | BK100 −2 → signed | `playera` redo[-1] == −2 | 0 | Same as Test 2 with `bk100`. |

## Self-Check

Verified claims:

- ✓ FOUND: `test/integration/bk_param_latent_bugs_test.rb` (file exists, 151 lines)
- ✓ FOUND commit `3cbe731e` in `git log --oneline -5`

## Self-Check: PASSED

## Issues Encountered

None.

## User Setup Required

None — pure code/test addition, no external services or env vars.

## Note on Working Tree

Plan 01 executed in `carambus_bcw/` (the active GSD working tree where Phase 38.5 planning artifacts live and where prior phase commits 3338ebd1 / 2b3085a5 / 20e9069c also landed). Per scenario-management skill, normal-mode development is expected in `carambus_master/`; the user may sync via `git pull` in `carambus_master/` and any deployment checkouts as needed. This is not a deviation — the GSD orchestrator dispatched the plan here, and the plan modifies test code only (no production code, no scenario-specific config).

## Next Plan Readiness

- Wave 1 work continues with **Plan 02** (`BkParamResolver` service) and **Plan 03** (Discipline seed update + fixtures), both `autonomous: true` and runnable in parallel.
- These four RED tests will be re-run by **Plan 06** to verify the end-to-end fix lands. Per GSD verification contract: Plan 06 MUST NOT edit this file.

---
*Phase: 38.5-bk-param-hierarchy-multiset-config*
*Completed: 2026-04-29*
