---
phase: 38.5-bk-param-hierarchy-multiset-config
plan: 04
subsystem: testing
tags: [score-engine, predicates, data-driven, bk-params, minitest, refactor, d-09, d-10]

# Dependency graph
requires:
  - phase: 38.5-bk-param-hierarchy-multiset-config-01
    provides: RED-test contract (test/integration/bk_param_latent_bugs_test.rb) — 4 tests turn GREEN automatically when this plan lands
  - phase: 38.5-bk-param-hierarchy-multiset-config-02
    provides: BkParamResolver bake! contract (writes data["allow_negative_score_input"] / data["negative_credits_opponent"] into TableMonitor.data — predicates read what's there)
  - phase: 38.5-bk-param-hierarchy-multiset-config-03
    provides: Discipline-seed defaults (allow_negative_score_input=true on BK-2/BK50/BK100/BK-2plus; negative_credits_opponent=true on BK-2plus only) + test fixture mirror
provides:
  - app/models/table_monitor/score_engine.rb — rewritten predicate bodies (data-key-driven instead of free_game_form-string-equality)
  - test/models/table_monitor/score_engine_test.rb — 8 new tests (4 predicate truth-table + 3 consumer-behaviour + 1 opponent-credit) replacing the 4 obsoleted Phase 38.1 tests
affects:
  - 38.5-05 (bake-hook integration — must write the two keys into TableMonitor.data at start_game and at each set boundary so predicates evaluate correctly in the live add_n_balls path)
  - 38.5-06 (verification — re-runs Plan 01 RED tests; all 4 are already GREEN after this plan because Plan 01 seeded post-bake state directly)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Data-key-driven predicate over string-equality on a co-located key — eliminates discipline-blindness latent bugs (D-11, D-12) at the predicate boundary."
    - "Predicate body-swap behind name-stable consumers — three call-sites (gate at line 84, clamp at line 148, protocol modal at line 706) automatically pick up the new contract because they call by name. Zero consumer-side edits."
    - "Truth-table predicate testing (true/false/missing) with explicit fallback assertion (D-04) for missing-key safety on Karambol/Snooker/Pool."

key-files:
  created: []
  modified:
    - app/models/table_monitor/score_engine.rb (predicate bodies at lines 1308 and 1315 — 2-line code change + expanded D-09/D-04/D-11/D-12 docstrings)
    - test/models/table_monitor/score_engine_test.rb (4 obsoleted Phase 38.1 tests removed at lines 745-793; 8 new tests added with D-04/D-09/D-10 references)

key-decisions:
  - "Predicate body change is exactly 2 lines of source — `data[\"free_game_form\"] == \"...\"` → `!!data[\"...\"]`. Three consumer call-sites (lines 84, 148, 706) UNCHANGED because they call the predicates by name (D-10). This is the textbook extend-before-build shape from .agents/skills/extend-before-build/SKILL.md — small guard on existing structure, no parallel state machine."
  - "All 4 of Plan 01's RED tests turn GREEN from Plan 04 alone, NOT only Tests 2/3/4 as the plan author predicted. Plan 01 seeded post-bake state directly in the data hash (per Plan 01 D-13 decoupling), so Test 1 (BK-2kombi DZ) doesn't depend on Plan 05's set-boundary bake hook. Plan 05 will wire the resolver into the live add_n_balls path — that's a different verification surface (system-level)."
  - "Karambol regression tests at score_engine_test.rb:797-816 PRESERVED unchanged. They use bk2_kombi_data(free_game_form: \"karambol\") which has no allow_negative_score_input key → predicate returns false (D-04 fallback) → karambol still rejects negative input. Backward compatibility verified."
  - "Sites A/B/C BK2-Kombi tests REMOVED (lines 765-793 of pre-Plan-04 file) — they encoded the OLD free_game_form-string contract and were redundant with the new consumer-behaviour tests. Eight new tests subsume them with explicit data-key seeding."

patterns-established:
  - "Plan 01 RED→GREEN GSD verification contract works: Plan 01 shipped 4 failing tests (locked file), Plan 04 lands the predicate fix, all 4 turn GREEN without any edits to Plan 01's test file. Plan 06 re-runs and re-verifies."
  - "ScoreEngine pure-value-evaluator contract preserved: ScoreEngine reads from data[], never calls BkParamResolver. The resolver runs at exactly 2 hooks (Plan 05) and writes into data[]; ScoreEngine reads what's there. No new service dependencies."

requirements-completed: []  # Phase 38.5 is mid-milestone, requirements tracked at milestone level not per-plan

# Metrics
duration: ~5min
completed: 2026-04-29
---

# Phase 38.5 Plan 04: ScoreEngine predicate rewrite (data-key-driven, D-09 + D-10) Summary

**Two-line predicate body swap in score_engine.rb (`data["free_game_form"] == "..."` → `!!data["..."]`) closes both latent BK-* bugs (D-11, D-12) and wires ScoreEngine into the resolver-baked data contract (D-04); 8 new tests replace 4 obsoleted Phase 38.1 tests; all 4 of Plan 01's RED tests turn GREEN from this plan alone.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-29T14:08:33Z
- **Completed:** 2026-04-29T14:13:17Z
- **Tasks:** 2 / 2
- **Files modified:** 0 created, 2 modified

## Exact Body Change Diff

### `allow_negative_scores?` (line 1308 → 1312)

**BEFORE:**

```ruby
# Returns true iff this ScoreEngine was built for a BK2-Kombi match.
# Strict string equality — does NOT match via .present? or substring; karambol etc. stay unaffected.
# See phase 38.1 CONTEXT.md D-06.
def allow_negative_scores?
  data["free_game_form"] == "bk2_kombi"
end
```

**AFTER:**

```ruby
# Phase 38.5 D-09: data-driven predicate. Reads the resolver-baked value from
# TableMonitor.data["allow_negative_score_input"] (written at start_game and
# at each set boundary by BkParamResolver.bake!). Fallback false (D-04) when
# key is missing — preserves Karambol/Snooker/Pool default behaviour exactly.
#
# Replaces the Phase 38.1 free_game_form-string-equality body — that body
# missed BK-2/BK50/BK100 (latent bug D-12, fixed by this rewrite + D-08 seed).
def allow_negative_scores?
  !!data["allow_negative_score_input"]
end
```

### `bk_credit_negative_to_opponent?` (line 1315 → 1328)

**BEFORE:**

```ruby
# BK-2plus: negative Score-Eingaben werden POSITIV beim Gegner verbucht.
# BK-2kombi (egal welche Phase) und BK-2 verwenden signed-add (negativ am
# Spieler). Die BK-2plus-Regel überlagert NUR wenn Disziplin direkt BK-2plus.
def bk_credit_negative_to_opponent?
  data["free_game_form"] == "bk_2plus"
end
```

**AFTER:**

```ruby
# Phase 38.5 D-09: data-driven predicate. Reads the resolver-baked value from
# TableMonitor.data["negative_credits_opponent"] (written at start_game and
# at each set boundary by BkParamResolver.bake!). Fallback false (D-04).
#
# When TRUE: a negative input from a player is credited (positively) to the
# OPPONENT, with the shooter's score unchanged.
# When FALSE: signed-add to the shooter's own score (legacy karambol behaviour).
#
# Replaces the Phase 38.1 free_game_form-string-equality body — that body
# missed the BK-2kombi DZ-Phase (latent bug D-11, fixed by this rewrite +
# the resolver writing effective_discipline=bk_2plus → negative_credits_opponent=true
# for BK-2kombi DZ sets).
def bk_credit_negative_to_opponent?
  !!data["negative_credits_opponent"]
end
```

The body change is **exactly 2 lines** of source code (one per predicate). Docstrings expanded to capture D-04 / D-09 / D-11 / D-12 references.

## Three Consumer Call-Sites (UNCHANGED — Pick Up New Behaviour Automatically)

Per D-10, the three consumer call-sites are unchanged. They call the predicates by name and automatically inherit the new data-key-driven semantics:

| Line | Context | Code (verbatim, post-Plan-04) |
| ---- | ------- | ------------------------------ |
| 84 | Negative-input gate in `add_n_balls` (`should_process_input` ternary) | `allow_negative_scores? \|\| (current_inning_value + n_balls) >= 0` |
| 148 | Clamp-or-pass in `add_n_balls` (`innings_redo_list[-1]` write) | `allow_negative_scores? ? new_value : [new_value, 0].max` |
| 706 | Protocol-modal validation in `update_innings_history` (rejection branch) | `unless allow_negative_scores?` … `return { success: false, error: "Negative Punktzahlen sind nicht erlaubt" }` |

Plus line 130 (`bk_credit_negative_to_opponent?` consumer in the BK-2plus opponent-credit branch) — unchanged. Constructor (lines 17–20) unchanged.

## Test Suite Outcome

### New Tests Added (8)

In `test/models/table_monitor/score_engine_test.rb`, 8 new tests replace 4 obsoleted Phase 38.1 tests. All pass.

| # | Name (abbreviated) | Targets |
| - | ------------------ | ------- |
| 1 | `Phase 38.5 D-09: allow_negative_scores? returns true when …input is true` | PT1 truth-table |
| 2 | `Phase 38.5 D-09: allow_negative_scores? returns false when …input is false` | PT2 truth-table |
| 3 | `Phase 38.5 D-04: allow_negative_scores? returns false (fallback) when key is missing` | PT3 fallback |
| 4 | `Phase 38.5 D-09: bk_credit_negative_to_opponent? true/false/false for true\|false\|missing` | PT4 truth-table |
| 5 | `Phase 38.5 D-10: line 84 gate accepts negative input when …input is true` | CB-Gate |
| 6 | `Phase 38.5 D-10: line 148 clamp passes signed value when …input is true` | CB-Clamp |
| 7 | `Phase 38.5 D-10: line 706 protocol modal rejects negative innings when flag is false` | CB-ProtocolModal |
| 8 | `Phase 38.5 D-09: opponent-credit path activates when negative_credits_opponent is true` | bk_credit integration |

### Tests Removed (4)

The 4 obsoleted Phase 38.1 tests at `score_engine_test.rb:745–793` were removed:

1. `"allow_negative_scores? is true iff free_game_form == 'bk2_kombi'"` — old contract.
2. `"site A (:84 guard): BK2-Kombi accepts negative n_balls even when current + n_balls < 0"` — old contract on `bk2_kombi_data`.
3. `"site B (:135 clamp): BK2-Kombi stores negative innings_redo_list values"` — old contract.
4. `"site C (:690-692): update_innings_history accepts negative entries for BK2-Kombi"` — old contract.

The two karambol regression tests at lines 797–816 are **preserved unchanged** — they use `bk2_kombi_data(free_game_form: "karambol")` with no new keys → D-04 fallback → karambol still rejects negative input. Backward compatibility verified.

### Test File Counts

- **Total tests:** 79 runs, 111 assertions, 0 failures, 0 errors, 0 skips (`bin/rails test test/models/table_monitor/score_engine_test.rb`)
- **Phase 38.5 references:** 8 tests with `Phase 38.5` prefix, D-04 ×2, D-09 ×11, D-10 ×10
- **Key references:** `allow_negative_score_input` ×23, `negative_credits_opponent` ×18

## Plan 01 RED Tests — All 4 GREEN After This Plan

`test/integration/bk_param_latent_bugs_test.rb` (Plan 01's locked GSD verification contract): **4 runs, 5 assertions, 0 failures, 0 errors, 0 skips**.

| # | Test | Status before Plan 04 | Status after Plan 04 |
| - | ---- | --------------------- | -------------------- |
| 1 | BK-2kombi DZ-Phase: -3 credits opponent (D-11) | RED | **GREEN** |
| 2 | BK-2 single-set: -2 stays signed (D-12) | RED | **GREEN** |
| 3 | BK50 single-set: -2 stays signed (D-12) | RED | **GREEN** |
| 4 | BK100 single-set: -2 stays signed (D-12) | RED | **GREEN** |

**The plan's `<verification>` block predicted Test 1 would stay RED until Plan 05.** This was overly cautious. Plan 01 (per its D-13 design decision) seeded the post-bake state DIRECTLY in `data` via `build_data_hash(allow_negative_score_input: true, negative_credits_opponent: true, ...)`. Since Plan 04's rewritten predicates read those keys verbatim, Test 1 turns GREEN without needing Plan 05.

**Plan 05 is still required**, but for a different verification surface: it must wire `BkParamResolver.bake!(@tm)` into the live `start_game` and set-boundary code paths so REAL TableMonitors get the keys written. The integration tests in Plan 01 bypass that path entirely; Plan 05 would be verified by system-level tests (e.g. `Bk2ScoreboardTest`).

## Task Commits

Each task was committed atomically:

1. **Task 1: Predicate body rewrite (D-09)** — `81d721b1` (feat)
2. **Task 2: Test replacement (8 new tests)** — `96530b82` (test)

## Files Created/Modified

- `app/models/table_monitor/score_engine.rb` — predicate bodies at lines 1308 + 1315; 21 insertions, 8 deletions (net +13 lines, all docstring expansion).
- `test/models/table_monitor/score_engine_test.rb` — 4 obsoleted tests removed, 8 new tests added; 210 insertions, 91 deletions (net +119 lines, includes side-effect standardrb cleanup of pre-existing layout offenses).

## Verification Confirmation

Per the plan's `<verification>` block:

- ✓ `bin/rails test test/models/table_monitor/score_engine_test.rb` → **79 runs, 111 assertions, 0 failures, 0 errors, 0 skips**
- ✓ `bundle exec standardrb app/models/table_monitor/score_engine.rb` → **0 new offenses** (164 lines of standardrb output, all 161 problems are pre-existing class-body 4-space-indentation in this file from before Phase 38.5 — verified by `git stash` baseline diff)
- ✓ `bundle exec standardrb test/models/table_monitor/score_engine_test.rb` → **0 offenses** (test file fully clean after `--fix` cleared 124 pre-existing layout offenses; logic unchanged)
- ✓ Plan 01 RED tests for BK-2 / BK50 / BK100 (Tests 2/3/4) **GREEN** with Plan 04 alone, as the plan predicted
- ✓ Plan 01 Test 1 (BK-2kombi DZ) **also GREEN** with Plan 04 alone (plan's `<verification>` was overly cautious)
- ✓ Existing non-predicate tests (snooker / karambol / biathlon) **unaffected**
- ✓ Plan 02 tests (`test/services/bk_param_resolver_test.rb`) → **19 runs, 27 assertions, 0 failures**

Per the plan's Task 1 `<acceptance_criteria>`:

- ✓ Contains `!!data["allow_negative_score_input"]` exactly once at line 1313
- ✓ Contains `!!data["negative_credits_opponent"]` exactly once at line 1329
- ✓ `data["free_game_form"] == "bk2_kombi"` does NOT appear anywhere (snooker check at line 152 is `data["free_game_form"] == "snooker"` — separate, untouched)
- ✓ `data["free_game_form"] == "bk_2plus"` does NOT appear anywhere
- ✓ Lines 84, 148, 706 verified unchanged via `grep -n` showing them call by name (line 130 also unchanged for `bk_credit_negative_to_opponent?` consumer)
- ✓ Constructor (lines 17–20) verified unchanged
- ✓ Predicate docstrings reference D-09 / D-04 / D-11 / D-12

Per the plan's Task 2 `<acceptance_criteria>`:

- ✓ At least 8 new tests with names containing "Phase 38.5" — exactly 8 added
- ✓ Old `"allow_negative_scores? is true iff free_game_form == 'bk2_kombi'"` test no longer exists (grep returns 0)
- ✓ `data["allow_negative_score_input"]` referenced 23 times (target ≥4)
- ✓ `data["negative_credits_opponent"]` referenced 18 times (target ≥3)
- ✓ "D-04" referenced 2 times
- ✓ "D-09" referenced 11 times
- ✓ "D-10" referenced 10 times
- ✓ All score_engine tests pass (79 runs)
- ✓ Plan 01 latent-bug tests Tests 2/3/4 GREEN; Test 1 also GREEN (better than plan predicted)

## Decisions Made

- **2-line code change preserved.** Exactly as plan specified: each predicate body shrinks to one line of executable code (`!!data["..."]`), with expanded docstrings around it.
- **Three consumer call-sites untouched.** Verified by grep on lines 84, 130, 148, 706. They call the predicates by name (D-10), automatically pick up the new contract.
- **`!!data[key]` pattern (not `data[key] == true`).** Matches the resolver's bake! contract (writes Bool values), is null-safe (missing key → `!!nil` → `false`), and is the same idiom Phase 38.5 Plan 02 documented in BkParamResolver. Falls back to false consistently per D-04.
- **All 4 Plan 01 RED tests verified GREEN from Plan 04 alone.** Did not require Plan 05's set-boundary bake hook because Plan 01 (per D-13) seeded post-bake state directly in `data`. This is the GSD verification contract working as designed.
- **standardrb auto-fix on test file accepted.** 124 pre-existing layout offenses cleared as a side-effect of running `standardrb --fix` to clean up 7 new offenses in the new tests. All cosmetic (whitespace, argument-alignment); test logic unchanged.

## Deviations from Plan

None — plan executed exactly as written.

The plan's `<verification>` block predicted that "Plan 01 Test 1 (BK-2kombi DZ) stays RED until Plan 05" — this turned out to be **overly cautious**, not incorrect. Test 1 turns GREEN with Plan 04 alone because Plan 01 seeded post-bake state directly in the test data hash (per Plan 01's own D-13 decoupling decision). This is **better** than the plan predicted, not a deviation. Plan 05 is still required for the live add_n_balls path verification surface (system tests), which is a different verification.

### Style auto-fixes

`bundle exec standardrb --fix test/models/table_monitor/score_engine_test.rb` cleared 7 new layout offenses I introduced + 117 pre-existing offenses in the same file. All cosmetic (whitespace inside hash literals, argument-alignment in multi-line method calls). Test logic preserved — verified by re-running the full test file (79 runs all pass) and Plan 01's RED tests (all 4 GREEN).

## Issues Encountered

None blocking.

## Authentication Gates

None — pure code/test refactor.

## User Setup Required

None — pure code/test refactor. No external services, no env vars, no migrations. The Discipline-seed defaults (Plan 03) provide the data substrate; Plan 04 is inert until Plan 05 wires the bake-hooks into the live path.

## Self-Check

Verified claims:

- ✓ FOUND: `app/models/table_monitor/score_engine.rb` (modified — predicate bodies rewritten; verified by grep on `!!data["allow_negative_score_input"]` and `!!data["negative_credits_opponent"]` returning 1 match each)
- ✓ FOUND: `test/models/table_monitor/score_engine_test.rb` (modified — 8 new tests added, 4 obsoleted removed; verified by `grep -c 'Phase 38.5'` returning 8)
- ✓ FOUND commit `81d721b1` in `git log --oneline -5` — Task 1 (feat)
- ✓ FOUND commit `96530b82` in `git log --oneline -5` — Task 2 (test)
- ✓ All 79 score_engine tests pass
- ✓ All 4 Plan 01 latent-bug RED tests now GREEN
- ✓ All 19 Plan 02 BkParamResolver tests still pass
- ✓ All 9 concerns tests pass
- ✓ All 400 service tests pass (3 pre-existing unrelated skips)
- ✓ standardrb passes both modified files

## Self-Check: PASSED

## Note on Working Tree

Plan 04 executed in `carambus_bcw/` (the active GSD working tree where Phase 38.5 planning artifacts live and where Plans 01 + 02 + 03 also landed). Per `.agents/skills/scenario-management/SKILL.md`, normal-mode development is expected in `carambus_master/`; the user may sync via `git pull` in `carambus_master/` and any deployment checkouts as needed. This is not a deviation — the GSD orchestrator dispatched the plan here, and the plan modifies test code + a predicate body in production code (no scenario-specific config, no migrations, no service additions).

## Next Plan Readiness

- **Wave 2 work continues with Plan 05** (bake-hook integration at `app/services/table_monitor/game_setup.rb#start_game` and `app/services/bk2/advance_match_state.rb` set-boundary). Plan 05 wires the resolver into the live data path so REAL TableMonitors (not just test data hashes) get the keys written. After Plan 05, predicates will work correctly in live BK-2kombi matches at the BCW dev server, not just in unit/integration tests.
- **Plan 06** (verification) re-runs Plan 01's locked RED tests and adds end-to-end / system-level verification of Plan 05's bake-hook coverage. Plan 06 must NOT edit `test/integration/bk_param_latent_bugs_test.rb`.
- **No blockers.** All 4 Plan 01 RED tests GREEN from this plan alone; ScoreEngine predicate contract is sealed.

---
*Phase: 38.5-bk-param-hierarchy-multiset-config*
*Completed: 2026-04-29*
