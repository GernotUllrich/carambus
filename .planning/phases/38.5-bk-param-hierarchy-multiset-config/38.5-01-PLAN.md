---
phase: 38.5-bk-param-hierarchy-multiset-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/bk_param_latent_bugs_test.rb
autonomous: true
decisions_addressed: [D-13]

must_haves:
  truths:
    - "Two RED tests exist that document the current latent BK-2kombi DZ-credit-opponent bug (D-11)"
    - "Two RED tests exist that document the current BK-2/BK50/BK100 negative-clamp bug (D-12)"
    - "All four RED tests fail today against the unmodified score_engine.rb (verifies bug is real)"
    - "After Plans 04+05 land, these tests turn GREEN without modification (verifies fix is correct)"
  artifacts:
    - path: "test/integration/bk_param_latent_bugs_test.rb"
      provides: "RED-test characterization suite for the two latent BK-* scoring bugs (D-11, D-12)"
      contains: "class BkParamLatentBugsTest"
  key_links:
    - from: "test/integration/bk_param_latent_bugs_test.rb"
      to: "app/models/table_monitor/score_engine.rb"
      via: "instantiates ScoreEngine.new(data, discipline:) and calls add_n_balls(-N, role)"
      pattern: "ScoreEngine.new\\("
---

<objective>
Phase 38.5 D-13: write RED-tests BEFORE the fix. The RED tests document two latent
bugs that have lurked since Phase 38.1 / 38.4:

1. BK-2kombi DZ-Phase: a player shooting -3 today writes -3 onto their OWN score
   (because `bk_credit_negative_to_opponent?` matches only `bk_2plus`, not the
   DZ phase of `bk2_kombi`). Correct behaviour: the absolute value (+3) is credited
   to the OPPONENT and the shooter's score stays unchanged.
2. BK-2 / BK50 / BK100: a player shooting -2 today gets clamped to 0 (because
   `allow_negative_scores?` matches only `bk2_kombi`). Correct behaviour: the -2
   stays signed on the shooter's own score.

These tests live in a NEW file (`test/integration/bk_param_latent_bugs_test.rb`)
to avoid file-conflict with Plan 04, which rewrites the predicates inside
`score_engine.rb` and updates `score_engine_test.rb` itself.

Purpose: Lock in the bug behaviour as a regression suite. After Plans 04+05 land
(predicate rewrite + seed update + bake-hook), these tests turn GREEN automatically
— no test edits needed in Plan 06. That is the GSD definition of a regression
contract.

Output: One Minitest file with 4 tests (2 RED for D-11, 2 RED for D-12).
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-CONTEXT.md
@.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-RESEARCH.md
@.planning/phases/38.1-bk2-kombi-minimum-viable-support/38.1-BK2-KOMBI-RULES.md
@app/models/table_monitor/score_engine.rb
@test/models/table_monitor/score_engine_test.rb

<interfaces>
<!-- Key contracts the executor needs. Extracted from current codebase. -->

From app/models/table_monitor/score_engine.rb (lines 1308–1317, CURRENT bodies):

```ruby
def allow_negative_scores?
  data["free_game_form"] == "bk2_kombi"   # MATCHES ONLY bk2_kombi today
end

def bk_credit_negative_to_opponent?
  data["free_game_form"] == "bk_2plus"    # MATCHES ONLY bk_2plus today
end
```

From app/models/table_monitor/score_engine.rb (lines 84, 148, 706 — the THREE consumers):
- Line 84: `allow_negative_scores? || (current_inning_value + n_balls) >= 0` — gate
- Line 148: `allow_negative_scores? ? new_value : [new_value, 0].max` — clamp
- Line 706: `unless allow_negative_scores?` — protocol-modal validation

From app/models/table_monitor/score_engine.rb (constructor, lines 17–20):

```ruby
def initialize(data, discipline:)
  @data = data
  @discipline = discipline
end
```

ScoreEngine receives the `data` Hash by reference. Mutations to `data[player]["innings_redo_list"]`
are visible to the caller. Tests assert against the mutated `data` Hash.

From test/models/table_monitor/score_engine_test.rb (existing pattern at lines 747–762):

```ruby
test "allow_negative_scores? is true iff free_game_form == 'bk2_kombi'" do
  e_bk2 = ScoreEngine.new({"free_game_form" => "bk2_kombi"}, discipline: nil)
  assert_equal true, e_bk2.allow_negative_scores?
  ...
end
```

Use this as the construction template. The minimal valid `data` Hash for `add_n_balls`
must include `playera`/`playerb` sub-hashes with `innings_redo_list`, the active player,
`current_inning`, and `free_game_form`. Reference fixture builders in
test/models/table_monitor/score_engine_test.rb top-of-file `setup` blocks for shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-tests for D-11 and D-12 latent bugs</name>
  <files>test/integration/bk_param_latent_bugs_test.rb</files>
  <read_first>
    - .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-CONTEXT.md (D-11, D-12, D-13)
    - .planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-RESEARCH.md (Code Examples → Latent Bug Regression Test)
    - app/models/table_monitor/score_engine.rb lines 1–60 (constructor + add_n_balls signature)
    - app/models/table_monitor/score_engine.rb lines 75–155 (negative-input gate + clamp)
    - app/models/table_monitor/score_engine.rb lines 1280–1322 (predicate bodies + bk_credit_negative_to_opponent body that does the opponent-credit logic — required to mirror the data shape correctly)
    - test/models/table_monitor/score_engine_test.rb lines 1–80 (existing setup helpers + minimal data Hash shape)
    - test/models/table_monitor/score_engine_test.rb lines 740–800 (existing predicate tests for the construction pattern)
  </read_first>
  <behavior>
    The new file declares 4 tests, all currently RED:

    Test 1 (D-11 BUG, RED today):
      Build a ScoreEngine for a BK-2kombi match in the DZ phase with the data hash
      WITHOUT any of the new Phase 38.5 keys (allow_negative_score_input,
      negative_credits_opponent, effective_discipline). Player A shoots -3.
      Today's behaviour: data["playera"]["innings_redo_list"][-1] becomes -3 (signed
      onto own score because bk_credit_negative_to_opponent? returns false for
      bk2_kombi). The test asserts the CORRECT post-fix behaviour:
        - data["playerb"]["innings_redo_list"][-1] == 3
        - data["playera"]["innings_redo_list"][-1] == 0
      → RED today, GREEN after Plans 04+05 land (because the resolver writes
        effective_discipline=bk_2plus + negative_credits_opponent=true at start_game,
        and the rewritten predicate body reads !!data["negative_credits_opponent"]).

    Test 2 (D-12 BUG, RED today, BK-2 case):
      Build a ScoreEngine for a free-game form "bk_2" match. Player A shoots -2
      with current inning value 0. Today's behaviour: clamped to 0 (because
      allow_negative_scores? returns false for bk_2). The test asserts:
        - data["playera"]["innings_redo_list"][-1] == -2
      → RED today, GREEN after Plans 03+04 land.

    Test 3 (D-12 BUG, RED today, BK50 case):
      Same as Test 2 but free_game_form="bk50". Asserts -2 stays signed.

    Test 4 (D-12 BUG, RED today, BK100 case):
      Same as Test 2 but free_game_form="bk100". Asserts -2 stays signed.

    For all 4 tests, in addition to the score assertions, the test seeds the data
    hash with the post-resolver state (allow_negative_score_input=true,
    negative_credits_opponent=true|false, effective_discipline=...) so that the
    tests pass deterministically AFTER the predicate rewrite (Plan 04) WITHOUT
    requiring the resolver itself to run. This decouples Plan 01 from Plan 02
    completion — Plan 01 ships independently in Wave 1. Plan 06 verifies that the
    end-to-end path (where the resolver bakes these keys) also produces GREEN.
  </behavior>
  <action>
    Create file test/integration/bk_param_latent_bugs_test.rb. Use frozen_string_literal: true.
    Inherit from ActiveSupport::TestCase. Require "test_helper".

    File header docstring must reference: "Phase 38.5 D-13 — RED-tests for latent BK-* bugs.
    Today: ALL FOUR tests FAIL (bug behaviour). After Plans 04+05 land: ALL FOUR turn GREEN
    automatically. Do NOT edit this file in Plan 06 — that file is the GSD verification
    contract."

    Define a private helper method `build_data_hash(free_game_form:, effective_discipline:,
    allow_negative_score_input:, negative_credits_opponent:, kickoff: "playera")` that returns
    the minimal Hash needed by ScoreEngine#add_n_balls. Required keys (from score_engine.rb
    add_n_balls path):
      - "free_game_form" => free_game_form
      - "effective_discipline" => effective_discipline
      - "allow_negative_score_input" => allow_negative_score_input
      - "negative_credits_opponent" => negative_credits_opponent
      - "current_inning" => { "active_player" => kickoff, "balls" => 0 }
      - "current_kickoff_player" => kickoff
      - "playera" => { "result" => 0, "innings" => 0, "innings_list" => [],
                       "innings_redo_list" => [0], "hs" => 0, "gd" => "0.00" }
      - "playerb" => { "result" => 0, "innings" => 0, "innings_list" => [],
                       "innings_redo_list" => [0], "hs" => 0, "gd" => "0.00" }
      - "ba_results" => { "Sets1" => 0, "Sets2" => 0, "Innings1" => 0, "Innings2" => 0 }

    Read score_engine_test.rb existing setup for any extra keys add_n_balls touches that
    aren't in the list above; if so, add them with neutral defaults.

    Test 1 — "BK-2kombi DZ-Phase: -3 credits opponent (D-11 RED→GREEN)":
      data = build_data_hash(free_game_form: "bk2_kombi", effective_discipline: "bk_2plus",
                             allow_negative_score_input: true, negative_credits_opponent: true)
      engine = TableMonitor::ScoreEngine.new(data, discipline: nil)
      engine.add_n_balls(-3, "playera")
      assert_equal 3, data["playerb"]["innings_redo_list"][-1],
        "BK-2kombi DZ: -3 from A must credit B with +3 (D-11)"
      assert_equal 0, data["playera"]["innings_redo_list"][-1],
        "BK-2kombi DZ: A's score must stay 0 when -3 routes to opponent (D-11)"

    Test 2 — "BK-2 single-set: -2 stays signed at player (D-12 RED→GREEN)":
      data = build_data_hash(free_game_form: "bk_2", effective_discipline: "bk_2",
                             allow_negative_score_input: true, negative_credits_opponent: false)
      engine = TableMonitor::ScoreEngine.new(data, discipline: nil)
      engine.add_n_balls(-2, "playera")
      assert_equal(-2, data["playera"]["innings_redo_list"][-1],
        "BK-2: -2 must stay signed on A's own score, NOT clamp to 0 (D-12)")

    Test 3 — "BK50: -2 stays signed at player (D-12 RED→GREEN)":
      Same shape as Test 2, free_game_form="bk50", effective_discipline="bk50".

    Test 4 — "BK100: -2 stays signed at player (D-12 RED→GREEN)":
      Same shape as Test 2, free_game_form="bk100", effective_discipline="bk100".

    All four tests must:
    - Pre-fix: FAIL when run against unmodified score_engine.rb (allow_negative_scores?
      returns false for bk_2/bk50/bk100; bk_credit_negative_to_opponent? returns false
      for bk2_kombi).
    - Post-fix: PASS when score_engine.rb predicates have been rewritten to read from
      data["allow_negative_score_input"] / data["negative_credits_opponent"] (Plan 04
      D-09 rewrite).

    Naming: class BkParamLatentBugsTest < ActiveSupport::TestCase.

    Do NOT use TableMonitor::ScoreEngine.new fixtures from existing tests if those fixtures
    smuggle in any of the new D-08 keys or pre-baked effective_discipline. Build a fresh
    data Hash in this file's helper to keep the bug demonstration auditable.
  </action>
  <verify>
    <automated>bin/rails test test/integration/bk_param_latent_bugs_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - File exists at test/integration/bk_param_latent_bugs_test.rb
    - File contains `class BkParamLatentBugsTest`
    - File contains 4 `test "..."` blocks
    - File contains `build_data_hash` helper method definition
    - File header comment contains "Phase 38.5 D-13" and "RED→GREEN"
    - All 4 tests reference TableMonitor::ScoreEngine.new
    - Test 1 asserts `data["playerb"]["innings_redo_list"][-1] == 3` after `add_n_balls(-3, "playera")`
    - Tests 2/3/4 assert `data["playera"]["innings_redo_list"][-1] == -2`
    - Running `bin/rails test test/integration/bk_param_latent_bugs_test.rb` against UNMODIFIED score_engine.rb produces 4 failures (this is expected RED state — Plan 01 ships with the failing tests, Plan 06 verifies them GREEN after Plans 04+05)
    - After Plans 04+05 land, running the same command produces 4 passes WITHOUT editing this file
  </acceptance_criteria>
  <done>
    File exists, 4 RED tests are wired up. Tests fail today. The plan ships the
    failing tests intentionally — that is the regression contract.
  </done>
</task>

</tasks>

<verification>
- Run `bin/rails test test/integration/bk_param_latent_bugs_test.rb` and confirm 4 failures.
- Each failure message references the bug it documents (D-11 or D-12).
- Verify file is committed to the branch but tests are NOT skipped or wrapped in
  `skip` calls — they must visibly fail in the test run.
- Verify no edits to existing files (score_engine.rb, score_engine_test.rb, etc.).
</verification>

<success_criteria>
- test/integration/bk_param_latent_bugs_test.rb is committed
- The 4 tests fail RED today against unmodified score_engine.rb
- The 4 tests will turn GREEN after Plans 04+05 land, without further edits to this file
- No other files are modified
</success_criteria>

<output>
After completion, create `.planning/phases/38.5-bk-param-hierarchy-multiset-config/38.5-01-SUMMARY.md`
documenting: (a) the 4 tests added, (b) confirmation of RED state today, (c) explicit
note that the file is the GSD verification contract for Plan 06 — DO NOT edit during Plan 06.
</output>
