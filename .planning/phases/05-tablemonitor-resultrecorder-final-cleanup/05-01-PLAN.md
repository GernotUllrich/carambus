---
phase: 05-tablemonitor-resultrecorder-final-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/table_monitor/result_recorder.rb
  - app/models/table_monitor.rb
  - test/services/table_monitor/result_recorder_test.rb
autonomous: true
requirements:
  - TMON-03
must_haves:
  truths:
    - "ResultRecorder ApplicationService exists with 5 public entry points matching D-02"
    - "AASM events (finish_match!, end_of_set!) fire correctly from ResultRecorder context"
    - "TableMonitor delegates save_result, save_current_set, evaluate_result, switch_to_next_set, get_max_number_of_wins to ResultRecorder"
    - "No CableReady calls exist in ResultRecorder (broadcasts via after_update_commit)"
    - "Characterization tests pass with zero regressions"
  artifacts:
    - path: "app/services/table_monitor/result_recorder.rb"
      provides: "ResultRecorder ApplicationService"
      contains: "class TableMonitor::ResultRecorder < ApplicationService"
    - path: "test/services/table_monitor/result_recorder_test.rb"
      provides: "ResultRecorder unit tests"
      contains: "class TableMonitor::ResultRecorderTest < ActiveSupport::TestCase"
  key_links:
    - from: "app/models/table_monitor.rb"
      to: "app/services/table_monitor/result_recorder.rb"
      via: "delegation wrappers"
      pattern: "TableMonitor::ResultRecorder"
    - from: "app/services/table_monitor/result_recorder.rb"
      to: "AASM state machine"
      via: "direct event calls"
      pattern: "@tm\\.finish_match!|@tm\\.end_of_set!"
---

<objective>
Extract ResultRecorder ApplicationService from TableMonitor and wire delegation.

Purpose: Move the highest-risk AASM-coupled result persistence logic (save_result, save_current_set, evaluate_result, switch_to_next_set, get_max_number_of_wins) out of TableMonitor into a standalone, testable service. This is the primary extraction for Phase 5 per D-01 through D-04.

Output: ResultRecorder service, unit tests, and thin delegation wrappers in TableMonitor.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tablemonitor-resultrecorder-final-cleanup/05-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs -->

From app/services/application_service.rb:
```ruby
class ApplicationService
  def self.call(kwargs = {})
    new(kwargs).call
  end
end
```

From app/services/table_monitor/game_setup.rb (reference pattern):
```ruby
class TableMonitor::GameSetup < ApplicationService
  def initialize(kwargs = {})
    @tm = kwargs[:table_monitor]
    @options = HashWithIndifferentAccess.new(kwargs[:options] || {})
  end
  def call
    @tm.suppress_broadcast = true
    perform_start_game
    true
  ensure
    @tm.suppress_broadcast = false
  end
  # Class-level entry point for alternate operation:
  def self.assign(table_monitor:, game_participation:)
    new(table_monitor: table_monitor).tap { |s| s.instance_variable_set(:@game_participation, game_participation) }.perform_assign
  end
end
```

Target methods in TableMonitor (line numbers):
- save_result: line 1482 (~72 lines)
- save_current_set: line 1554 (~45 lines)
- get_max_number_of_wins: line 1599 (~8 lines)
- switch_to_next_set: line 1611 (~70 lines)
- evaluate_result: line 1681 (~121 lines)

AASM events used by evaluate_result:
- @tm.end_of_set! -> after_enter :set_game_over (line 455)
- @tm.finish_match! -> after: :set_end_time (line 723)
Both use soft save (no exception conflict with ResultRecorder's save!).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ResultRecorder ApplicationService + unit tests</name>
  <files>app/services/table_monitor/result_recorder.rb, test/services/table_monitor/result_recorder_test.rb</files>
  <read_first>
    - app/services/table_monitor/game_setup.rb (reference pattern for ApplicationService subclass)
    - app/services/application_service.rb (base class)
    - app/models/table_monitor.rb lines 1482-1800 (5 methods to extract: save_result, save_current_set, get_max_number_of_wins, switch_to_next_set, evaluate_result)
    - test/services/table_monitor/game_setup_test.rb (reference test pattern: setup block, Player.create!, TableMonitor.create!)
    - app/models/table_monitor.rb lines 307-500 (AASM block: end_of_set!, finish_match!, after_enter callbacks)
  </read_first>
  <behavior>
    - Test 1: ResultRecorder.call(table_monitor: tm) triggers evaluate_result logic
    - Test 2: ResultRecorder.save_result(table_monitor: tm) builds and persists set result hash into data["sets"]
    - Test 3: ResultRecorder.save_current_set(table_monitor: tm) finalizes current set data
    - Test 4: ResultRecorder.get_max_number_of_wins(table_monitor: tm) returns correct max wins count
    - Test 5: ResultRecorder.switch_to_next_set(table_monitor: tm) initializes next set
    - Test 6: evaluate_result fires end_of_set! when set is decided (player reaches target)
    - Test 7: evaluate_result fires finish_match! when match is decided (all sets played)
    - Test 8: No CableReady references exist in result_recorder.rb
  </behavior>
  <action>
    Create `app/services/table_monitor/result_recorder.rb` following the GameSetup pattern exactly (per D-01):
    - Inherit from ApplicationService
    - `initialize(kwargs = {})` stores `@tm = kwargs[:table_monitor]`
    - `call` dispatches to `perform_evaluate_result` (main entry point)
    - Class-level entry points for the other 4 operations: `self.save_result(table_monitor:)`, `self.save_current_set(table_monitor:)`, `self.get_max_number_of_wins(table_monitor:)`, `self.switch_to_next_set(table_monitor:)` — each creates an instance and calls the corresponding perform_ method
    - Move the FULL method bodies from TableMonitor lines 1482-1800 into the corresponding private `perform_*` methods
    - Replace all `self.` references with `@tm.` inside the service
    - AASM events called directly on `@tm` per D-03: `@tm.end_of_set!`, `@tm.finish_match!`
    - No CableReady calls per D-04 — broadcasts happen through after_update_commit on @tm
    - Preserve ALL `return` statements in evaluate_result to prevent recursion (Pitfall 2 from research)
    - German docstring at class top matching GameSetup style
    - `frozen_string_literal: true` header

    Create `test/services/table_monitor/result_recorder_test.rb`:
    - Follow game_setup_test.rb setup pattern: `TableMonitor.options = nil`, direct Player.create!/Game.new/GameParticipation.create!
    - Create @tm with `state: "playing"` and realistic data hash containing player results, innings, sets_to_win, ba_results
    - Test each of the 5 operations: save_result persists to data["sets"], save_current_set, get_max_number_of_wins returns integer, switch_to_next_set resets current set, evaluate_result triggers AASM transitions
    - Test AASM integration: when player reaches target, end_of_set! fires and state changes
    - Assert no CableReady references: `refute File.read(result_recorder_path).include?("CableReady")`
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/table_monitor/result_recorder_test.rb -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class TableMonitor::ResultRecorder < ApplicationService" app/services/table_monitor/result_recorder.rb
    - grep -q "def self.save_result" app/services/table_monitor/result_recorder.rb
    - grep -q "def self.save_current_set" app/services/table_monitor/result_recorder.rb
    - grep -q "def self.get_max_number_of_wins" app/services/table_monitor/result_recorder.rb
    - grep -q "def self.switch_to_next_set" app/services/table_monitor/result_recorder.rb
    - grep -q "@tm.finish_match!" app/services/table_monitor/result_recorder.rb
    - grep -q "@tm.end_of_set!" app/services/table_monitor/result_recorder.rb
    - ! grep -q "CableReady" app/services/table_monitor/result_recorder.rb
    - grep -q "class TableMonitor::ResultRecorderTest" test/services/table_monitor/result_recorder_test.rb
  </acceptance_criteria>
  <done>ResultRecorder service exists with all 5 methods, unit tests pass, no CableReady references</done>
</task>

<task type="auto">
  <name>Task 2: Wire ResultRecorder delegation in TableMonitor + verify</name>
  <files>app/models/table_monitor.rb</files>
  <read_first>
    - app/models/table_monitor.rb lines 1482-1800 (methods to replace with delegation wrappers)
    - app/services/table_monitor/result_recorder.rb (just created — verify entry points)
    - app/reflexes/table_monitor_reflex.rb (calls evaluate_result — public API must be preserved)
    - app/reflexes/game_protocol_reflex.rb (calls evaluate_result — 1 call site)
  </read_first>
  <action>
    Replace the 5 full method bodies in TableMonitor (lines 1482-1800) with thin delegation wrappers:

    ```ruby
    def save_result
      TableMonitor::ResultRecorder.save_result(table_monitor: self)
    end

    def save_current_set
      TableMonitor::ResultRecorder.save_current_set(table_monitor: self)
    end

    def get_max_number_of_wins
      TableMonitor::ResultRecorder.get_max_number_of_wins(table_monitor: self)
    end

    def switch_to_next_set
      TableMonitor::ResultRecorder.switch_to_next_set(table_monitor: self)
    end

    def evaluate_result
      TableMonitor::ResultRecorder.call(table_monitor: self)
    end
    ```

    Keep `sets_played` method (line 1607, ~4 lines) in TableMonitor — it is a simple accessor, not result logic.

    After replacing, verify the public method names are unchanged (reflexes call `@table_monitor.evaluate_result` etc.).

    Run characterization tests to verify zero regressions.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/characterization/table_monitor_char_test.rb test/services/table_monitor/result_recorder_test.rb -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "TableMonitor::ResultRecorder" app/models/table_monitor.rb returns at least 5
    - grep -q "def evaluate_result" app/models/table_monitor.rb (public API preserved)
    - grep -q "def save_result" app/models/table_monitor.rb (public API preserved)
    - The method body of evaluate_result in table_monitor.rb is a single delegation line (not the full 121-line body)
  </acceptance_criteria>
  <done>TableMonitor delegates all 5 result methods to ResultRecorder; characterization tests pass with zero failures; ~300 net lines removed from TableMonitor</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reflex -> ResultRecorder | Reflex actions trigger result persistence via model delegation |
| ResultRecorder -> AASM | Service fires state transition events on model instance |
| ResultRecorder -> Database | save! calls persist game outcome data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | ResultRecorder#perform_save_result | mitigate | Result data hash structure validated by existing save_result guard logic (moved intact from TM); @tm.save! fires AR validations |
| T-05-02 | Elevation | ResultRecorder AASM events | mitigate | finish_match! and end_of_set! have AASM guards (playing? state required); guards remain on model — ResultRecorder cannot bypass them |
| T-05-03 | Denial of Service | evaluate_result recursion | mitigate | All return statements preserved from original code; characterization tests verify no infinite recursion (Pitfall 2) |
| T-05-04 | Information Disclosure | Result data in logs | accept | Existing logging patterns preserved; no new PII exposure — game scores are public data |
</threat_model>

<verification>
1. `bin/rails test test/characterization/table_monitor_char_test.rb` — 41 tests, 0 failures
2. `bin/rails test test/services/table_monitor/result_recorder_test.rb` — all tests pass
3. `grep -c "TableMonitor::ResultRecorder" app/models/table_monitor.rb` — at least 5 delegation calls
4. `! grep -q "CableReady" app/services/table_monitor/result_recorder.rb` — no direct broadcasts
</verification>

<success_criteria>
- ResultRecorder ApplicationService exists with 5 entry points
- TableMonitor delegates result methods via thin wrappers
- AASM events fire correctly from service context
- Characterization tests pass with zero regressions
- ~300 net lines removed from TableMonitor
</success_criteria>

<output>
After completion, create `.planning/phases/05-tablemonitor-resultrecorder-final-cleanup/05-01-SUMMARY.md`
</output>
