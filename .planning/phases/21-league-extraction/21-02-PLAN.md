---
phase: 21-league-extraction
plan: "02"
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - app/services/league/club_cloud_scraper.rb
  - app/services/league/bbv_scraper.rb
  - app/models/league.rb
  - test/services/league/club_cloud_scraper_test.rb
  - test/services/league/bbv_scraper_test.rb
autonomous: true
requirements: [EXTR-01, EXTR-03, EXTR-04]
must_haves:
  truths:
    - "League#scrape_single_league_from_cc delegates to League::ClubCloudScraper and behaves identically"
    - "League#scrape_single_bbv_league delegates to League::BbvScraper and behaves identically"
    - "League.scrape_bbv_leagues delegates to League::BbvScraper.scrape_all and returns records_to_tag"
    - "All Phase 20 characterization tests pass without modification"
    - "All 831+ existing tests pass with 0 failures and 0 errors"
    - "League model line count is reduced by at least 1100 lines from original 2221"
  artifacts:
    - path: "app/services/league/club_cloud_scraper.rb"
      provides: "ApplicationService extracting the 821-line scrape_single_league_from_cc method"
      contains: "class League::ClubCloudScraper < ApplicationService"
    - path: "app/services/league/bbv_scraper.rb"
      provides: "ApplicationService extracting BBV scraping methods (~135 lines)"
      contains: "class League::BbvScraper < ApplicationService"
    - path: "test/services/league/club_cloud_scraper_test.rb"
      provides: "Tests for ClubCloudScraper service"
      contains: "class League::ClubCloudScraperTest"
    - path: "test/services/league/bbv_scraper_test.rb"
      provides: "Tests for BbvScraper service"
      contains: "class League::BbvScraperTest"
  key_links:
    - from: "app/models/league.rb"
      to: "app/services/league/club_cloud_scraper.rb"
      via: "delegation wrapper"
      pattern: "League::ClubCloudScraper\\.call"
    - from: "app/models/league.rb"
      to: "app/services/league/bbv_scraper.rb"
      via: "delegation wrappers"
      pattern: "League::BbvScraper"
---

<objective>
Extract ClubCloudScraper (ApplicationService, ~821 lines) and BbvScraper (ApplicationService, ~135 lines) from League model, completing the extraction phase. League model line count target: under ~1100 lines (from 2221).

Purpose: Final two extractions per D-02 ordering. ClubCloudScraper is the hardest extraction (821-line method with early returns, cross-method instance variables). BbvScraper is small and similar in pattern.
Output: Two new service classes with tests, League model fully reduced, all 831+ tests green.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-league-extraction/21-CONTEXT.md
@.planning/phases/21-league-extraction/21-RESEARCH.md
@.planning/phases/21-league-extraction/21-01-SUMMARY.md

<interfaces>
<!-- ApplicationService base class -->
From app/services/application_service.rb:
```ruby
class ApplicationService
  def self.call(kwargs = {})
    new(kwargs).call
  end
end
```

<!-- Existing service pattern from Plan 01 -->
From app/services/league/standings_calculator.rb:
```ruby
class League::StandingsCalculator
  def initialize(league)
    @league = league
  end
  # public instance methods
end
```

From app/services/league/game_plan_reconstructor.rb:
```ruby
class League::GamePlanReconstructor < ApplicationService
  def initialize(kwargs = {})
    @league = kwargs[:league]
    @season = kwargs[:season]
    @operation = kwargs[:operation] || :reconstruct
    @opts = kwargs.except(:league, :season, :operation)
  end
  def call
    # dispatcher
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract League::ClubCloudScraper ApplicationService</name>
  <files>
    app/services/league/club_cloud_scraper.rb,
    test/services/league/club_cloud_scraper_test.rb,
    app/models/league.rb
  </files>
  <read_first>
    app/models/league.rb,
    app/services/tournament_monitor/result_processor.rb,
    test/models/league_scraping_test.rb,
    .planning/phases/21-league-extraction/21-RESEARCH.md
  </read_first>
  <action>
1. Create `app/services/league/club_cloud_scraper.rb` (per D-07, D-09):
   - `# frozen_string_literal: true`
   - Class `League::ClubCloudScraper < ApplicationService`
   - `def initialize(kwargs = {})` extracting `@league = kwargs[:league]`, `@opts = kwargs.except(:league)`
   - `def call` — the top-level orchestration that was `scrape_single_league_from_cc`
   - **CRITICAL (Pitfall 2):** Map all 5 `return` statements in the original method:
     - First `return unless opts[:league_details]` becomes a guard clause at the top of `call`
     - Other returns within sub-sections become returns from their respective private methods, with the orchestrating `call` method checking return values to decide whether to continue
   - **CRITICAL (Pitfall 3):** Shared local variables (`clubs_cache`, `league_teams_cache`, `league_team_players`, `global_context`, `region_id`, etc.) become instance variables (`@clubs_cache`, `@league_teams_cache`, etc.) initialized in `call` before dispatching to private methods
   - Break the 821-line body into well-named private methods (per D-09):
     - `def scrape_league` — main orchestration (guard checks, fetch HTML, dispatch to parsers)
     - `def parse_teams(league_doc, url)` — team table scraping (~lines 624-760 of league.rb)
     - `def parse_parties(league_doc, url)` — party/game scraping (~lines 761-1200)
     - `def build_game_plan(disciplines)` — game plan creation (~lines 1200-1393)
     - Additional small private helpers as needed for readability
   - All moved code: replace `self` with `@league` where it refers to the League instance
   - The broad `rescue StandardError => e` at the end of the original method (~line 1391) must be preserved in the service — wrap `call` body in the same rescue pattern

2. In `league.rb`, replace `scrape_single_league_from_cc` body with delegation wrapper (per D-11):
   ```ruby
   def scrape_single_league_from_cc(opts = {})
     League::ClubCloudScraper.call(league: self, **opts)
   end
   ```
   - Remove ALL private helper methods that were exclusively used by `scrape_single_league_from_cc` from league.rb
   - Keep `scrape_leagues_from_cc` and `scrape_leagues_optimized` class methods in league.rb (they are coordination methods, per RESEARCH.md assumption A1)
   - Keep `scrape_league_optimized`, `scrape_league_teams_optimized`, `scrape_party_games_optimized` in league.rb (these are separate optimized scraping methods, not part of the 821-line extraction)

3. Create `test/services/league/club_cloud_scraper_test.rb`:
   - `# frozen_string_literal: true`
   - `require "test_helper"`
   - Class `League::ClubCloudScraperTest < ActiveSupport::TestCase`
   - Use `TEST_ID_BASE = 50_000_000` and `ID_OFFSET = 90_000` (unique range)
   - Test that `League::ClubCloudScraper.call(league: league)` (without league_details) returns without error (early guard)
   - Test that `League::ClubCloudScraper.call(league: league, league_details: true)` handles 200 response with empty HTML table (returns without error) — use WebMock `stub_request` with regex matching for ClubCloud URLs
   - Test that `League::ClubCloudScraper.call(league: league, league_details: true)` handles timeout (returns without error due to broad rescue) — use WebMock `.to_timeout`
   - Create League with local ID, `regions(:nbv)` organizer, `seasons(:current)`, appropriate discipline fixture

4. Run `bin/rails test test/services/league/club_cloud_scraper_test.rb` — must pass.
5. Run `bin/rails test test/models/league_scraping_test.rb` — must pass (characterization tests unchanged).
6. Run `bundle exec standardrb app/services/league/club_cloud_scraper.rb app/models/league.rb` — must pass.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/league/club_cloud_scraper_test.rb test/models/league_scraping_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - app/services/league/club_cloud_scraper.rb exists and contains `class League::ClubCloudScraper < ApplicationService`
    - app/services/league/club_cloud_scraper.rb contains `def initialize(kwargs = {})`
    - app/services/league/club_cloud_scraper.rb contains `def call`
    - app/services/league/club_cloud_scraper.rb contains `frozen_string_literal: true`
    - app/services/league/club_cloud_scraper.rb contains at least 3 private methods (parse_teams, parse_parties, build_game_plan or similar)
    - app/models/league.rb contains `League::ClubCloudScraper.call(league: self`
    - app/models/league.rb does NOT contain the original 821-line method body (the body of scrape_single_league_from_cc should be 1-2 lines)
    - test/services/league/club_cloud_scraper_test.rb exists and contains `class League::ClubCloudScraperTest`
    - `bin/rails test test/services/league/club_cloud_scraper_test.rb` exits 0
    - `bin/rails test test/models/league_scraping_test.rb` exits 0
  </acceptance_criteria>
  <done>ClubCloudScraper ApplicationService extracted with 821-line method decomposed into private methods, delegation wrapper in place, broad rescue preserved, new service tests + characterization tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Extract League::BbvScraper ApplicationService + full suite verification</name>
  <files>
    app/services/league/bbv_scraper.rb,
    test/services/league/bbv_scraper_test.rb,
    app/models/league.rb
  </files>
  <read_first>
    app/models/league.rb,
    app/services/league/club_cloud_scraper.rb,
    test/models/league_scraping_test.rb,
    .planning/phases/21-league-extraction/21-RESEARCH.md
  </read_first>
  <action>
1. Create `app/services/league/bbv_scraper.rb` (per D-08, D-10):
   - `# frozen_string_literal: true`
   - Class `League::BbvScraper < ApplicationService`
   - `def initialize(kwargs = {})` extracting `@league = kwargs[:league]`, `@region = kwargs[:region]`, `@opts = kwargs.except(:league, :region)`
   - `def call` — body of `scrape_single_bbv_league` (instance method)
   - Class method `def self.scrape_all(region:, season:, opts: {})` — body of `League.scrape_bbv_leagues` class method
   - **CRITICAL (Pitfall 4):** `scrape_all` must return `records_to_tag` array. The original `scrape_bbv_leagues` returns this array and it is used by callers.
   - Private method `def scrape_bbv_league_teams` — body of `League#scrape_bbv_league_teams`
   - Replace `self` with `@league` in moved instance methods
   - In `scrape_all`, the method iterates leagues — keep it as a class method that creates instances or calls `.call` per league

2. In `league.rb`, replace extracted methods with delegation wrappers (per D-11):
   ```ruby
   def scrape_single_bbv_league(region, opts = {})
     League::BbvScraper.call(league: self, region: region, **opts)
   end

   def self.scrape_bbv_leagues(region, season, opts = {})
     League::BbvScraper.scrape_all(region: region, season: season, opts: opts)
   end
   ```
   - Remove `scrape_bbv_league_teams` from league.rb entirely (no external callers, moved to service as private method)

3. Create `test/services/league/bbv_scraper_test.rb`:
   - `# frozen_string_literal: true`
   - `require "test_helper"`
   - Class `League::BbvScraperTest < ActiveSupport::TestCase`
   - Use `TEST_ID_BASE = 50_000_000` and `ID_OFFSET = 100_000` (unique range)
   - Test `League::BbvScraper.call(league: league, region: region)` does not raise on stubbed empty HTML response
   - Test `League::BbvScraper.scrape_all(region: region, season: season)` returns an Array (records_to_tag)
   - Use WebMock stubs for BBV URLs

4. Run `bin/rails test test/services/league/bbv_scraper_test.rb` — must pass.
5. Run `bin/rails test test/models/league_scraping_test.rb` — must pass.

6. **Full suite verification (EXTR-04):**
   - Run `bin/rails test` — all 831+ tests must pass with 0 failures, 0 errors.
   - Run `wc -l app/models/league.rb` — must show significant reduction from 2221 (target: under ~1100 lines, per D-01 ~50% reduction).
   - Run `bundle exec standardrb app/services/league/ app/models/league.rb` — must pass.

7. Report final line counts:
   - `wc -l app/models/league.rb` (after all extractions)
   - `wc -l app/services/league/*.rb` (new service files)
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/league/bbv_scraper_test.rb test/models/league_scraping_test.rb && bin/rails test</automated>
  </verify>
  <acceptance_criteria>
    - app/services/league/bbv_scraper.rb exists and contains `class League::BbvScraper < ApplicationService`
    - app/services/league/bbv_scraper.rb contains `def self.scrape_all`
    - app/services/league/bbv_scraper.rb contains `frozen_string_literal: true`
    - app/models/league.rb contains `League::BbvScraper.call(league: self`
    - app/models/league.rb contains `League::BbvScraper.scrape_all`
    - app/models/league.rb does NOT contain `def scrape_bbv_league_teams` (moved to service)
    - test/services/league/bbv_scraper_test.rb exists and contains `class League::BbvScraperTest`
    - `bin/rails test test/services/league/bbv_scraper_test.rb` exits 0
    - `bin/rails test test/models/league_scraping_test.rb` exits 0
    - `bin/rails test` exits 0 with 0 failures, 0 errors (full suite green)
    - `wc -l app/models/league.rb` shows value under 1200 (significant reduction from 2221)
    - `bundle exec standardrb app/services/league/ app/models/league.rb` exits 0
  </acceptance_criteria>
  <done>BbvScraper extracted, all 4 service classes complete, League model reduced by 50%+ from 2221 lines, all 831+ tests green, StandardRB clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure internal refactoring — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | T (Tampering) | ClubCloudScraper instance vars | accept | Instance variables in service class have same scope as locals in original method — no escalation |
| T-21-04 | R (Repudiation) | Scraper error handling | accept | Broad rescue preserved from original — same logging/swallowing behavior |
</threat_model>

<verification>
- `bin/rails test` — full suite green (831+ runs, 0 failures, 0 errors)
- `bin/rails test test/services/league/` — all 4 service test files pass
- `bin/rails test test/models/league_test.rb test/models/league_standings_test.rb test/models/league_scraping_test.rb` — all characterization tests pass unmodified
- `wc -l app/models/league.rb` shows under 1200 lines (from 2221)
- `bundle exec standardrb app/services/league/ app/models/league.rb` — clean
</verification>

<success_criteria>
- League::ClubCloudScraper exists with decomposed private methods
- League::BbvScraper exists with scrape_all class method preserving records_to_tag return
- League model has thin delegation wrappers for all scrapers
- All Phase 20 characterization tests pass without modification
- Full test suite (831+ tests) green with 0 failures, 0 errors
- League model reduced by at least 50% from 2221 lines
- StandardRB clean on all new and modified files
</success_criteria>

<output>
After completion, create `.planning/phases/21-league-extraction/21-02-SUMMARY.md`
</output>
