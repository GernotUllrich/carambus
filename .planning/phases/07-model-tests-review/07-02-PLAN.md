---
phase: 07-model-tests-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/models/league_test.rb
  - test/models/tournament_test.rb
  - test/models/table_monitor/options_presenter_test.rb
  - test/models/tournament_monitor_ko_test.rb
  - test/models/table_heater_management_test.rb
autonomous: true
requirements:
  - MODL-01
  - MODL-02

must_haves:
  truths:
    - "league_test.rb conditional skip is removed; test runs unconditionally"
    - "league_test.rb line 15 disjunction assertion replaced with specific value check"
    - "league_test.rb and tournament_test.rb have frozen_string_literal: true as first line"
    - "tournament_test.rb line 16 assert_nothing_raised replaced with value verification"
    - "options_presenter_test.rb 3 sole assert_not_nil tests have meaningful post-condition assertions"
    - "tournament_monitor_ko_test.rb 2 sole assert_nothing_raised tests have meaningful post-condition assertions"
    - "table_heater_management_test.rb lines 541 and 575 sole assert_not_nil tests have meaningful post-condition assertions"
    - "All modified test files pass"
  artifacts:
    - path: "test/models/league_test.rb"
      provides: "Fixed skip, weak assertion, and frozen_string_literal"
    - path: "test/models/tournament_test.rb"
      provides: "Strengthened assertion and frozen_string_literal"
    - path: "test/models/table_monitor/options_presenter_test.rb"
      provides: "3 strengthened assertions"
    - path: "test/models/tournament_monitor_ko_test.rb"
      provides: "2 strengthened assertions"
    - path: "test/models/table_heater_management_test.rb"
      provides: "2 strengthened assertions"
  key_links:
    - from: "test/models/league_test.rb"
      to: "app/models/league.rb"
      via: "model under test"
      pattern: "League"
    - from: "test/models/tournament_test.rb"
      to: "app/models/tournament.rb"
      via: "model under test"
      pattern: "Tournament"
---

<objective>
Fix weak assertions, the skipped test, and add missing frozen_string_literal in 5 model test files identified by AUDIT-REPORT.md.

Purpose: Per D-02, strengthen sole-assertion cases where assert_nothing_raised or assert_not_nil is the ONLY assertion in a test. Per D-05, add frozen_string_literal to league_test.rb and tournament_test.rb (the other 2 missing files are handled in Plan 01). Per D-06, fix the league_test.rb conditional skip. Per D-03, leave precondition checks (assert_not_nil followed by stronger assertions) alone. Per D-04, clean up large files in place without splitting.
Output: 5 test files with meaningful post-condition assertions replacing weak sole-assertion patterns. 10 individual test methods improved total. 2 files gain frozen_string_literal.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-model-tests-review/07-CONTEXT.md
@.planning/phases/06-audit-baseline-standards/STANDARDS.md
@.planning/phases/06-audit-baseline-standards/AUDIT-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix league_test.rb skip and weak assertion, strengthen tournament_test.rb, add frozen_string_literal to both</name>
  <files>
    test/models/league_test.rb
    test/models/tournament_test.rb
  </files>
  <read_first>
    test/models/league_test.rb
    app/models/league.rb
    test/models/tournament_test.rb
    app/models/tournament.rb
    test/fixtures/leagues.yml
    test/fixtures/disciplines.yml
    test/fixtures/parties.yml
  </read_first>
  <action>
**frozen_string_literal (D-05) for both files:**
Both league_test.rb and tournament_test.rb currently start with `require "test_helper"`. Add `# frozen_string_literal: true` as the very first line, followed by a blank line, then the existing content. Per STANDARDS.md File Structure: (1) frozen_string_literal, (2) require "test_helper", (3) class declaration.

**league_test.rb -- D-06 (E03, line 10):**
Read the fixture data to understand what `@league` has. The test at line 10 does `skip unless @league.discipline.present? && @league.parties.any?`. Either:
- (a) Ensure the fixture `leagues(:one)` (or whichever is used) has a discipline and parties associated, then remove the skip entirely, OR
- (b) If the fixture cannot easily have those associations, restructure the test to create the needed associations in setup using `Model.create!` with explicit attributes.

The goal: the test RUNS unconditionally. No `skip` in this test.

**league_test.rb -- D-02 (E02, line 15):**
The assertion `assert result.nil? || result.is_a?(GamePlan)` is a disjunction that allows nil to pass silently. Read `league.rb` to understand what the method being tested actually returns. Replace with a specific assertion:
- If the method should return a GamePlan, use `assert_instance_of GamePlan, result` or `assert_equal expected_plan, result`
- If the method legitimately returns nil in some cases, split into two tests: one where it returns nil (with specific setup) and one where it returns a GamePlan

**tournament_test.rb -- D-02 (E02, line 16):**
The test "allows local modifications to data field" uses `assert_nothing_raised` as sole assertion around `local.update!(data: ...)`. After the update, add a verification that the data was actually persisted:
```ruby
assert_nothing_raised do
  local.update!(data: { "test_key" => "test_value" })
end
local.reload
assert_equal({ "test_key" => "test_value" }, local.data)
```
Read `tournament.rb` and the test to see what data field and update are being tested, then add the appropriate reload + assert_equal.
  </action>
  <verify>
    <automated>bin/rails test test/models/league_test.rb test/models/tournament_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 test/models/league_test.rb` outputs `# frozen_string_literal: true`
    - `head -1 test/models/tournament_test.rb` outputs `# frozen_string_literal: true`
    - `grep -c "skip" test/models/league_test.rb` returns 0
    - `grep -c "result.nil? || result.is_a?" test/models/league_test.rb` returns 0
    - `grep -c "assert_equal\|assert_instance_of" test/models/league_test.rb` is at least 1 more than before
    - `grep -c "reload" test/models/tournament_test.rb` returns at least 1
    - `bin/rails test test/models/league_test.rb test/models/tournament_test.rb` passes with 0 failures
  </acceptance_criteria>
  <done>league_test.rb skip is removed and runs unconditionally. league_test.rb disjunction replaced with specific assertion. tournament_test.rb assert_nothing_raised supplemented with value verification. Both files have frozen_string_literal: true. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen sole weak assertions in options_presenter, tournament_monitor_ko, and table_heater_management tests</name>
  <files>
    test/models/table_monitor/options_presenter_test.rb
    test/models/tournament_monitor_ko_test.rb
    test/models/table_heater_management_test.rb
  </files>
  <read_first>
    test/models/table_monitor/options_presenter_test.rb
    app/models/table_monitor.rb
    app/models/table_monitor/options_presenter.rb
    test/models/tournament_monitor_ko_test.rb
    test/models/table_heater_management_test.rb
    test/support/ko_tournament_test_helper.rb
  </read_first>
  <action>
**Per D-02:** Only fix tests where the weak assertion is the SOLE assertion. Per D-03, leave precondition assert_not_nil followed by stronger assertions untouched.

**options_presenter_test.rb (3 fixes, lines 206, 234, 247):**
Read the OptionsPresenter class to understand what `gps`, `location`, and `my_table` return. Each of these tests does only `assert_not_nil presenter.X`. For each:
- Determine the expected return type and meaningful attributes
- Replace or augment with specific assertions like `assert_instance_of Gps, presenter.gps` and `assert_equal expected_id, presenter.gps.id` (using actual values from test setup)

**tournament_monitor_ko_test.rb (2 fixes, lines 155, 177):**
Read the test to understand what operations are performed inside the `assert_nothing_raised` blocks. These are KO tournament operations. After the block:
- Verify expected post-conditions: game count, game state, player assignments, bracket structure
- Read the test setup and the model methods being called to determine what meaningful state changes to verify

**table_heater_management_test.rb (2 fixes, lines 541, 575):**
Read the tests around lines 541 and 575. These are sole `assert_not_nil @table.heater_switched_on_at` or similar timestamp checks. Strengthen by:
- Checking the timestamp is within an expected range: `assert_in_delta Time.current.to_i, @table.heater_switched_on_at.to_i, 5`
- Or checking specific state: `assert @table.heater_switched_on_at >= time_before_action`
- Read the surrounding test context to understand what action was performed and what the meaningful post-condition is

**IMPORTANT:** Per D-04, do NOT split or restructure these large files. Only modify the specific test methods identified. Per D-03, do NOT touch assert_not_nil instances that serve as preconditions before stronger assertions (e.g., lines 156, 193, 218, 257 in table_heater_management_test.rb are NOT targets).
  </action>
  <verify>
    <automated>bin/rails test test/models/table_monitor/options_presenter_test.rb test/models/tournament_monitor_ko_test.rb test/models/table_heater_management_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - In options_presenter_test.rb, the tests around lines 206, 234, 247 each contain at least one assertion beyond assert_not_nil (verify with `grep -A5` around those line ranges for assert_equal, assert_instance_of, or assert_kind_of)
    - In tournament_monitor_ko_test.rb, the tests around lines 155 and 177 each contain at least one assertion beyond assert_nothing_raised (verify post-condition checks exist)
    - In table_heater_management_test.rb, the tests around lines 541 and 575 each contain at least one assertion beyond assert_not_nil (verify time comparison or state check exists)
    - `bin/rails test test/models/table_monitor/options_presenter_test.rb test/models/tournament_monitor_ko_test.rb test/models/table_heater_management_test.rb` passes with 0 failures
  </acceptance_criteria>
  <done>7 sole-assertion test methods across 3 files now have meaningful post-condition assertions. All tests pass. No structural changes to large files (per D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed -- this plan modifies test assertions only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | T (Tampering) | test assertions | accept | Strengthening assertions only increases test rigor; no production code changed |
</threat_model>

<verification>
```bash
# All 5 files modified have stronger assertions
bin/rails test test/models/league_test.rb test/models/tournament_test.rb test/models/table_monitor/options_presenter_test.rb test/models/tournament_monitor_ko_test.rb test/models/table_heater_management_test.rb

# frozen_string_literal in both files
head -1 test/models/league_test.rb test/models/tournament_test.rb

# No skip in league_test
grep -c "skip" test/models/league_test.rb
# Should be 0

# No disjunction in league_test
grep -c "nil? || result" test/models/league_test.rb
# Should be 0
```
</verification>

<success_criteria>
- league_test.rb and tournament_test.rb have frozen_string_literal: true
- league_test.rb has no skip and no disjunction assertion
- tournament_test.rb verifies actual persisted value after update
- options_presenter_test.rb has 3 strengthened test methods
- tournament_monitor_ko_test.rb has 2 strengthened test methods
- table_heater_management_test.rb has 2 strengthened test methods
- All 5 files pass with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/07-model-tests-review/07-02-SUMMARY.md`
</output>
