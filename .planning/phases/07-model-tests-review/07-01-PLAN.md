---
phase: 07-model-tests-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/models/club_location_test.rb
  - test/models/discipline_phase_test.rb
  - test/models/game_plan_test.rb
  - test/models/party_monitor_test.rb
  - test/models/slot_test.rb
  - test/models/source_attribution_test.rb
  - test/models/sync_hash_test.rb
  - test/models/table_local_test.rb
  - test/models/training_source_test.rb
  - test/models/upload_test.rb
  - test/models/tournament_auto_reserve_test.rb
  - test/models/user_test.rb
autonomous: true
requirements:
  - MODL-01

must_haves:
  truths:
    - "10 empty scaffold test stubs are deleted from test/models/"
    - "tournament_auto_reserve_test.rb and user_test.rb have frozen_string_literal: true as first line"
    - "bin/rails test test/models/ passes with no regressions"
  artifacts:
    - path: "test/models/club_location_test.rb"
      provides: "Deleted (must NOT exist)"
    - path: "test/models/discipline_phase_test.rb"
      provides: "Deleted (must NOT exist)"
    - path: "test/models/game_plan_test.rb"
      provides: "Deleted (must NOT exist)"
    - path: "test/models/party_monitor_test.rb"
      provides: "Deleted (must NOT exist)"
    - path: "test/models/slot_test.rb"
      provides: "Deleted (must NOT exist)"
    - path: "test/models/source_attribution_test.rb"
      provides: "Deleted (must NOT exist)"
    - path: "test/models/sync_hash_test.rb"
      provides: "Deleted (must NOT exist)"
    - path: "test/models/table_local_test.rb"
      provides: "Deleted (must NOT exist)"
    - path: "test/models/training_source_test.rb"
      provides: "Deleted (must NOT exist)"
    - path: "test/models/upload_test.rb"
      provides: "Deleted (must NOT exist)"
  key_links: []
---

<objective>
Delete 10 empty scaffold test stubs and add frozen_string_literal to the 2 clean model test files missing it.

Purpose: Remove false-confidence empty stubs (per D-01) and enforce the frozen_string_literal convention (per D-05) on the clean files that are not being edited by Plan 02.
Output: 10 fewer test files, 2 files updated with frozen_string_literal header.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-model-tests-review/07-CONTEXT.md
@.planning/phases/06-audit-baseline-standards/STANDARDS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 10 empty scaffold test stubs</name>
  <files>
    test/models/club_location_test.rb
    test/models/discipline_phase_test.rb
    test/models/game_plan_test.rb
    test/models/party_monitor_test.rb
    test/models/slot_test.rb
    test/models/source_attribution_test.rb
    test/models/sync_hash_test.rb
    test/models/table_local_test.rb
    test/models/training_source_test.rb
    test/models/upload_test.rb
  </files>
  <read_first>
    test/models/club_location_test.rb
    test/models/discipline_phase_test.rb
    test/models/game_plan_test.rb
    test/models/party_monitor_test.rb
    test/models/slot_test.rb
    test/models/source_attribution_test.rb
    test/models/sync_hash_test.rb
    test/models/table_local_test.rb
    test/models/training_source_test.rb
    test/models/upload_test.rb
  </read_first>
  <action>
Per D-01: Delete all 10 files. These are Rails scaffold stubs with zero test methods (E01 in AUDIT-REPORT.md). They provide false confidence. Before deleting each file, confirm it has 0 test methods (no `test "` or `def test_` blocks). Use `git rm` for each file so the deletion is tracked.

Commands:
```bash
git rm test/models/club_location_test.rb
git rm test/models/discipline_phase_test.rb
git rm test/models/game_plan_test.rb
git rm test/models/party_monitor_test.rb
git rm test/models/slot_test.rb
git rm test/models/source_attribution_test.rb
git rm test/models/sync_hash_test.rb
git rm test/models/table_local_test.rb
git rm test/models/training_source_test.rb
git rm test/models/upload_test.rb
```
  </action>
  <verify>
    <automated>test ! -f test/models/club_location_test.rb && test ! -f test/models/discipline_phase_test.rb && test ! -f test/models/game_plan_test.rb && test ! -f test/models/party_monitor_test.rb && test ! -f test/models/slot_test.rb && test ! -f test/models/source_attribution_test.rb && test ! -f test/models/sync_hash_test.rb && test ! -f test/models/table_local_test.rb && test ! -f test/models/training_source_test.rb && test ! -f test/models/upload_test.rb && echo "ALL DELETED"</automated>
  </verify>
  <acceptance_criteria>
    - None of the 10 files exist in test/models/
    - `git status` shows them as deleted
  </acceptance_criteria>
  <done>All 10 empty scaffold test stubs are deleted from test/models/.</done>
</task>

<task type="auto">
  <name>Task 2: Add frozen_string_literal to clean model test files missing it</name>
  <files>
    test/models/tournament_auto_reserve_test.rb
    test/models/user_test.rb
  </files>
  <read_first>
    test/models/tournament_auto_reserve_test.rb
    test/models/user_test.rb
  </read_first>
  <action>
Per D-05: Add `# frozen_string_literal: true` as the very first line of each file, followed by a blank line before the existing content. These 2 clean model test files (no other changes needed) are missing the header:

1. `test/models/tournament_auto_reserve_test.rb` — currently starts with `require "test_helper"`
2. `test/models/user_test.rb` — currently starts with `# == Schema Information`

For each file, prepend `# frozen_string_literal: true\n\n` before existing first line. If the file already has a blank line after the first line, do not add a double blank.

Note: league_test.rb and tournament_test.rb also need frozen_string_literal but are handled in Plan 02 since that plan edits them for assertion fixes.

Per STANDARDS.md File Structure: Required file elements in order: (1) frozen_string_literal, (2) require "test_helper", (3) class declaration.

After editing, run `bin/rails test test/models/` to confirm no regressions from both the deletions and the frozen_string_literal additions.
  </action>
  <verify>
    <automated>head -1 test/models/tournament_auto_reserve_test.rb test/models/user_test.rb | grep -c "frozen_string_literal: true"</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 test/models/tournament_auto_reserve_test.rb` outputs `# frozen_string_literal: true`
    - `head -1 test/models/user_test.rb` outputs `# frozen_string_literal: true`
    - `bin/rails test test/models/` passes with 0 failures
  </acceptance_criteria>
  <done>Both clean model test files have frozen_string_literal: true. Full model test suite passes after stub deletions and pragma additions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed — this plan only deletes empty test files and adds pragmas.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T (Tampering) | test file deletion | accept | Files contain zero tests; deletion verified by reading each file first |
</threat_model>

<verification>
```bash
# All 10 stubs deleted
ls test/models/club_location_test.rb test/models/discipline_phase_test.rb test/models/game_plan_test.rb test/models/party_monitor_test.rb test/models/slot_test.rb test/models/source_attribution_test.rb test/models/sync_hash_test.rb test/models/table_local_test.rb test/models/training_source_test.rb test/models/upload_test.rb 2>&1 | grep -c "No such file"
# Should be 10

# frozen_string_literal present in both clean files
head -1 test/models/tournament_auto_reserve_test.rb test/models/user_test.rb

# Full model test suite passes
bin/rails test test/models/
```
</verification>

<success_criteria>
- 10 empty scaffold test stubs no longer exist
- tournament_auto_reserve_test.rb and user_test.rb have frozen_string_literal: true
- bin/rails test test/models/ passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/07-model-tests-review/07-01-SUMMARY.md`
</output>
