---
phase: 38.1-bk2-kombi-minimum-viable-support
plan: "01"
subsystem: table_monitor/score_engine
tags:
  - table_monitor
  - score_engine
  - bk2_kombi
  - negative_scores
  - characterization_tests
dependency_graph:
  requires: []
  provides:
    - allow_negative_scores? helper in TableMonitor::ScoreEngine
    - negative-score gate bypass at sites A, B, C for bk2_kombi
  affects:
    - app/models/table_monitor/score_engine.rb
    - test/models/table_monitor/score_engine_test.rb
tech_stack:
  added: []
  patterns:
    - discipline-scoped gate bypass via strict string equality helper
    - TDD red-green cycle (characterization tests first)
key_files:
  created: []
  modified:
    - app/models/table_monitor/score_engine.rb
    - test/models/table_monitor/score_engine_test.rb
decisions:
  - "allow_negative_scores? placed as public method (above private) so Plan 02/03 and tests can call it directly"
  - "Site C uses unless allow_negative_scores? / if block to preserve the error message path for all other disciplines"
  - "bk2_kombi literal appears exactly once (inside the helper) — no string duplication at gate sites"
metrics:
  duration: "~30 minutes"
  completed: "2026-04-23"
  tasks_completed: 3
  files_changed: 2
---

# Phase 38.1 Plan 01: Engine Negative-Score Gate Summary

One-liner: BK2-Kombi negative-score bypass via strict-equality `allow_negative_scores?` helper gating three ScoreEngine sites, characterization-tested with karambol regression baseline.

## What Was Built

Added `allow_negative_scores?` public predicate to `TableMonitor::ScoreEngine` and applied it at the three known negative-score gate sites:

| Site | Line | Before | After |
|------|------|--------|-------|
| A — `:84` guard | 84 | `(current_inning_value + n_balls) >= 0` | `allow_negative_scores? \|\| (current + n_balls) >= 0` |
| B — `:135` clamp | 135–136 | `[value, 0].max` | `allow_negative_scores? ? new_value : [new_value, 0].max` |
| C — `:690-692` rejection | 691–695 | direct guard | `unless allow_negative_scores?` wrapper |

Helper definition at line 1293:
```ruby
def allow_negative_scores?
  data["free_game_form"] == "bk2_kombi"
end
```

## Test Counts

- **New BK2 bypass tests (RED → GREEN):** 4 tests (1 strict-equality test with 4 assertions, 2 site-A/B tests, 1 site-C test)
- **Karambol regression tests (GREEN throughout):** 2 tests
- **Total score_engine_test.rb runs:** 75 runs, 99 assertions, 0 failures, 0 errors, 0 skips
- **Critical test suite:** 29 runs, 55 assertions, 0 failures, 0 errors, 0 skips
- **Full table_monitor model suite:** 86 runs, 151 assertions, 0 failures, 0 errors, 0 skips

## Git SHAs

| Commit | Message |
|--------|---------|
| `4f005bfe` | `test(38.1-01): add characterization tests for BK2-Kombi negative-score gates` |
| `2abb270e` | `feat(38.1-01): add BK2-Kombi negative-score gate bypass at three score_engine sites` |

## Deviations from Plan

### Auto-fixed Issues

None. Plan executed exactly as written with one structural note:

**Helper visibility:** The plan placed `allow_negative_scores?` under `private` in the initial draft. During GREEN phase, moving it above `private` was required because the test calls `e.allow_negative_scores?` directly (public API). This matches the plan's intent — the helper is documented as callable from Plan 02/03 and tests.

## Pre-existing Conditions

### standardrb non-zero exit

`bundle exec standardrb app/models/table_monitor/score_engine.rb` exits 1. This was true before this plan (158 pre-existing violations, 4-space indentation throughout the file). After this plan: 159 violations — 1 new `Layout/IndentationWidth` at line 1293 (the new helper method, matching the existing 4-space convention). The file's global indentation style is out of scope for Plan 01 per CLAUDE.md scope boundary rules.

Out-of-scope violations logged to deferred-items: standardrb full-file fix for score_engine.rb.

### No test/services/table_monitor/ directory

The plan's Task 3 regression command `bin/rails test test/models/table_monitor/ test/services/table_monitor/` was adapted to `bin/rails test test/models/table_monitor/` only, as the services directory does not exist in this worktree. All 4 model test files (options_presenter_test, score_engine_test, game_setup_test, result_recorder_test) ran green.

## Known Stubs

None. This plan adds a helper and gate bypasses only — no UI rendering, no data stubs.

## Threat Flags

No new network endpoints, auth paths, file access patterns, or schema changes introduced.

Threat mitigations as designed:
- T-38.1-01: `allow_negative_scores?` uses strict `==` equality — tested with `"BK2_KOMBI"` → false, `nil` → false, `"karambol"` → false.
- T-38.1-02: Karambol/snooker/pool/biathlon rejection path preserved — regression-tested at Site C.

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| `app/models/table_monitor/score_engine.rb` exists | FOUND |
| `test/models/table_monitor/score_engine_test.rb` exists | FOUND |
| `38.1-01-engine-negative-score-gate-SUMMARY.md` exists | FOUND |
| Commit `4f005bfe` exists | FOUND |
| Commit `2abb270e` exists | FOUND |
