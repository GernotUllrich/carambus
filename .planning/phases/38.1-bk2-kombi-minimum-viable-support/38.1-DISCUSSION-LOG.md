# Phase 38.1: BK2-Kombi Minimum Viable Support - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 38.1-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-23
**Phase:** 38.1-bk2-kombi-minimum-viable-support
**Areas discussed:** MVP scope cut, Engine routing + negative-score gating, BK2-Kombi Discipline record, Scoreboard template routing

**Context:** Phase inserted urgently after Phase 38, during a `/gsd-discuss-phase 39` session. User flagged an unplanned real-world deadline: BK2-Kombi tournament at BCW club on 2026-05-02 (9 days out from 2026-04-23). Routing discussion (before this log starts) chose `insert-phase 38.1` over pivoting Phase 39, quick-task, or unplanned build.

---

## Pre-discussion routing

**Q: What level of scoreboard support does BK2-Kombi need for the Saturday tournament?**

| Option | Description | Selected |
|--------|-------------|----------|
| Full live scoring (Recommended) | TableMonitor drives set-by-set scoring with two game modes, tracks pins + caroms per shot, auto-advances sets, handles bonus-shot rule | ✓ (tentative) |
| Manual-input scorekeeper | Set score entry, no automatic logic | |
| Display + results entry only | Discipline wiring + final result, no live scoring | |
| Something else | | |

**Q: What's the fallback if we don't get scoreboard support shipped in time?**

User deferred answering; then provided new info: *"The normal karambol scoreboard would be ok as fallback, but in BK2-Kombi negative entries and negative scores are possible. At least this must be implemented. But of course full live scoring would be preferred."*

Reframed fallback: karambol scoreboard with negative-score gate disabled is acceptable fallback, but full BK2-Kombi live scoring is the target.

**Q: How should we structure this into the GSD workflow?**

| Option | Description | Selected |
|--------|-------------|----------|
| Insert Phase 38.5 urgent, keep 39 (Recommended) | New phase 38.5 scoped to urgent BK2-Kombi work; Phase 39 (DTP-backed parameter ranges) unchanged | ✓ |
| Pivot Phase 39 entirely to BK2-Kombi | Drop DTP work, replace with BK2-Kombi scope | |
| Quick task for Tier 1 + new milestone for Tier 2 | Split across /gsd-quick and a later milestone | |
| Pause GSD, just build it | Skip workflow entirely | |

Inserted phase was assigned **38.1** by the CLI auto-numbering (first decimal after Phase 38).

---

## Area 1: MVP scope cut

**Q: Given full-time availability through 2026-05-02, what scope should Phase 38.1 target as 'ship by Saturday'?**

| Option | Description | Selected |
|--------|-------------|----------|
| Full BK2-Kombi live scoring (Recommended) | Negative-score + discipline wiring + two game modes + sets + pin+carom hybrid + mode-switch UI + parameter_ranges + tests + dry run | ✓ |
| Negative scores + mode-switch UI, no set structure | | |
| Negative scores only (karambol fallback) | | |
| Let me narrow it | | |

**User's choice:** Full BK2-Kombi live scoring.
**Notes:** User is working full-time on this phase (declared during area selection). ~72 hours of focused work available through 2026-05-02 makes the ambitious target reachable. If a stretch item slips, karambol-with-negatives remains a functional fallback.

**Q: What's the UAT / real-table test plan?**

| Option | Description | Selected |
|--------|-------------|----------|
| Pre-tournament dry run on real table (Recommended) | Full match end-to-end on actual club table with real player before 2026-05-02 | ✓ |
| Characterization tests + developer smoke test | Minitest for 3 engine gate sites + manual dev smoke | |
| Tests only, no smoke test | | |

**Q: Who should capture the BK2-Kombi rule details for the planner?**

| Option | Description | Selected |
|--------|-------------|----------|
| Phase-researcher agent reads billardregel.de (Recommended) | Researcher extracts rules into RESEARCH.md | ~ |
| You walk me through it now | Conversational capture in-session | |
| Both — researcher first, then you review | | |

**User's choice:** Option 1 shortened — user provided a Perplexity session transcript with rules + architectural sketch in-line. Researcher round trip replaced by the transcript, which was saved as `38.1-BK2-KOMBI-RULES.md` in the phase directory.
**Notes:** User's caveat: *"of course that has to be adapted to carambus MVC structure"* — Perplexity's proposed `Match` / `RuleSet` AR models are NOT adopted; the target remains the existing TableMonitor / `data`-hash state model.

---

## Area 2: Engine routing + negative-score flag

**Q: How should the scoreboard route between karambol-style scoring and BK2-Kombi-style scoring?**

| Option | Description | Selected |
|--------|-------------|----------|
| New Discipline#game_form value 'bk2_kombi' (Recommended) | Add game_form dispatch | ✓ (refined below) |
| New data['game_form'] override on TableMonitor | Per-scoreboard override | |
| Hardcoded discipline.name match | ScoreEngine checks name == 'BK2-Kombi' | |

**Q: Where does the negative-score gating live?**

| Option | Description | Selected |
|--------|-------------|----------|
| Derived from game_form (Recommended) | allow_negative_scores? { game_form == 'bk2_kombi' } | ✓ |
| Separate Discipline#allow_negative_scores boolean | New DB column | |
| Per-tournament data flag | data['allow_negative_scores']=true | |

**Follow-up: existing codebase doesn't have Discipline#game_form — the pattern is `data["free_game_form"]` on TableMonitor.**

**Q: How should 'bk2_kombi' get into data['free_game_form']? (Refining Area 2 pick)**

| Option | Description | Selected |
|--------|-------------|----------|
| Extend existing pattern, derive from discipline name (Recommended) | New value "bk2_kombi" in existing string; derived in GameSetup/OptionsPresenter | ✓ |
| Add new Discipline#game_form column + derive | Migration + seed + derive | |
| Rename free_game_form to game_form now, consolidate | Cleanup debt, ~25 callsites | |

**User's choice:** Extend existing pattern. Zero migration, matches snooker/pool dispatch.
**Notes:** Derivation: `discipline.data["free_game_form"]` first; falls back to `discipline.name == "BK2-Kombi"` so the Saturday scenario works even if the authoritative `discipline.data` write hasn't synced from carambus_api yet.

---

## Area 3: Which BK2-Kombi Discipline record to use

**Q: Which discipline record do we wire the scoreboard to?**

| Option | Description | Selected |
|--------|-------------|----------|
| Use id 107 as canonical, don't merge (Recommended) | Tournament routes through id 107 (the only one with table_kind='Small Billard'); duplicates 57/59/95 left alone | ✓ |
| Merge 57/59/95 into 107 now | LocalProtector / re-sync risk | |
| Create a new local BK2-Kombi record | Divergence risk | |

**User's note:** *"But make a notice that we have to fix these duplicates later in carambus_api scenario context"* — captured in CONTEXT.md `<deferred>` section.

**Q: Should Phase 38.1 set data on id 107 (e.g., a 'bk2_kombi' game_form marker)?**

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — set discipline.data = { 'free_game_form' => 'bk2_kombi' } (Recommended) | JSON in existing data:text column | ✓ |
| No — hardcode mapping by name in GameSetup | Cleaner but less extensible | |
| Hybrid — hardcoded now, data-driven later | | |

**User's choice:** Set `discipline.data` on id 107.
**Notes:** Authoritative path is carambus_api scenario (global record protection). Fallback: `unprotected=true` local write from carambus_bcw if sync timing is tight; reconcile in carambus_api post-tournament.

---

## Area 4: Scoreboard template + scoring service

**Q: Scoreboard template for BK2-Kombi?**

| Option | Description | Selected |
|--------|-------------|----------|
| New dedicated ERB partial (Recommended) | Own shot-input section, phase indicator, shots-left badge, 3-set display | ✓ |
| Extend karambol partial with branches | Single file but mixed concerns | |
| Karambol fallback only — skip BK2-specific UI | Tier-2 fallback only | |

**Q: Where does the BK2-Kombi scoring service live in the codebase?**

| Option | Description | Selected |
|--------|-------------|----------|
| New app/services/bk2_kombi/ namespace (Recommended) | score_shot.rb + advance_match_state.rb, matches existing table_monitor/ pattern | ✓ |
| Extend TableMonitor::ScoreEngine with a Bk2Kombi module | Mixin | |
| New ScoreEngine subclass or sibling class | Clean polymorphism, biggest refactor | |

---

## Closing checkpoint

**Q: Any remaining gray areas to explore before I write CONTEXT.md?**

| Option | Description | Selected |
|--------|-------------|----------|
| I'm ready for context | Write CONTEXT.md, proceed to /gsd-plan-phase 38.1 | ✓ |
| Explore more gray areas | Undo/redo, protocol modal editing, rule config schema, etc. | |

---

## Claude's Discretion

Areas explicitly left to planner/implementer judgment (captured in CONTEXT.md D-17 and "Claude's Discretion" subsection):

- Exact Ruby method signatures for `Bk2Kombi::ScoreShot.call` and `Bk2Kombi::AdvanceMatchState.call`
- ERB layout details inside `_scoreboard_bk2_kombi.html.erb` (spacing, iconography, hotkey bindings)
- Undo/redo mechanics at per-shot granularity
- Stimulus vs plain form-submit for shot input
- Exact characterization-test scope for the 3 engine gate sites
- Warning-logging policy in GameSetup for name-match-without-data-value case

## Deferred Ideas

Captured in CONTEXT.md `<deferred>` section; not repeated here.

Primary deferrals:
- Discipline duplicate merge (57/59/95 → 107) — carambus_api scenario
- Authoritative `discipline.data` write on id 107 — carambus_api scenario
- Config-table `rule_sets` migration — post-deadline
- Generalisation to other Kegel disciplines — out of scope
- Shot-level protocol audit/replay — post-deadline
