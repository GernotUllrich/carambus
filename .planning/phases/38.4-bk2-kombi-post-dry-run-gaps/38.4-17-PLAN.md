---
plan_id: 38.4-17
phase: 38.4-bk2-kombi-post-dry-run-gaps
plan: "17"
type: execute
gap_closure: true
gaps_addressed:
  - { test: advisory, sub_id: CR-01, source: 38.4-VERIFICATION.md }
  - { test: advisory, sub_id: INFO-01, source: 38.4-VERIFICATION.md }
  - { test: advisory, sub_id: INFO-02, source: 38.4-VERIFICATION.md }
wave: 1
depends_on: []
autonomous: true
files_modified:
  - app/models/version.rb
  - .planning/ROADMAP.md
  - .planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md
  - .planning/todos/done/2026-04-23-sync-version-yaml-load-json-collision.md
  - test/models/version_test.rb
requirements:
  - CR-01
  - INFO-01
  - INFO-02
must_haves:
  truths:
    - "CR-01 closed: `Version.local_from_api` calls `local_server?` (with the question mark) at app/models/version.rb:434. The pre-existing NameError on console invocation no longer reproduces — `Version.local_from_api` returns without raising."
    - "INFO-01 closed: the sync-version-yaml-load-json-collision todo file is moved from `.planning/todos/pending/` to `.planning/todos/done/`. The `todos/done/` directory is created if it doesn't exist."
    - "INFO-02 closed: ROADMAP.md plans 38.4-10, 38.4-11, 38.4-12 checkboxes flipped from `[ ]` to `[x]` (consistent with the SUMMARY.md files existing for all three plans)."
    - "1 NEW model test (T-CR-01-local-from-api-no-raises) covers the CR-01 fix — calls Version.local_from_api on a test where local_server? returns true and asserts no NameError raised."
    - "Existing 9 version_test.rb tests stay green."
    - "Per I-17-01: the inline comment added by Task 1 above the local_server? predicate fix is in English. CLAUDE.md line 23 explicitly allows English for technical terms; the comment is a technical reference (NameError, predicate naming convention) rather than business logic. Plan 17 SUMMARY documents this as the convention exception so the choice is auditable."
  artifacts:
    - path: app/models/version.rb
      provides: "local_from_api method calls local_server? (with ?) instead of local_server"
      contains: "local_server?"
    - path: .planning/ROADMAP.md
      provides: "plans 38.4-10, 38.4-11, 38.4-12 marked complete with [x]"
      contains: "[x] `38.4-10-PLAN.md`"
    - path: .planning/todos/done/2026-04-23-sync-version-yaml-load-json-collision.md
      provides: "Sync bug todo moved from pending to done"
      contains: "sync-version-yaml-load-json-collision"
    - path: test/models/version_test.rb
      provides: "T-CR-01-local-from-api-no-raises regression test"
      contains: "T-CR-01-local-from-api-no-raises"
  key_links:
    - from: "Version.local_from_api class-method invocation"
      to: "ApplicationRecord.local_server? (the predicate method)"
      via: "Version.local_server? class method delegating to ApplicationRecord.local_server? (line 429-430)"
      pattern: "local_server?"
---

<objective>
Close 3 advisory items deferred from the verification report (`38.4-VERIFICATION.md`):

- **CR-01 (CRITICAL):** `Version.local_from_api` at `app/models/version.rb:434` calls
  `local_server` (no question mark) instead of `local_server?`. This is a pre-existing
  NameError when invoked from console/scripts (`if local_server` resolves as a local
  variable that's nil → returns nil silently in Ruby, but actually `local_server` is
  NOT a defined method — it raises `NameError: undefined local variable or method
  'local_server'`). One-character fix.

- **INFO-01:** The sync-version-yaml-load-json-collision todo file at
  `.planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md` was never
  moved to `todos/done/` after Plan 38.4-01 closed the underlying bug. The `todos/done/`
  directory does not exist. Bookkeeping — no functional impact.

- **INFO-02:** ROADMAP.md plans 38.4-10, 38.4-11, 38.4-12 still have unchecked checkboxes
  (`[ ]`) despite all 3 having SUMMARY.md files committed. Bookkeeping — no functional
  impact.

**Why combine these into one plan:** All three are tiny, isolated, and unrelated to the
P1-P5 round-4 gap closures (Plans 13-16). Bundling them keeps round-4 commit hygiene tight
and ensures the verification report's deferred items don't bit-rot into round 5+.

**WR-01..06 NOT included:** Those are warnings, not blockers. They survive into a future
phase or quick task. WR-05 (`score_shot.rb` rule gate) was identified as adjacent to P4
in the planning prompt, but on inspection score_shot.rb is on the dormant Variante A path
(per Plan 38.4-CONTEXT.md "deferred"); no live-game write traffic touches it. Closing
WR-05 is therefore not load-bearing for round 4.

**Per I-17-01 — comment language convention:** The inline comment added by Task 1 above
the fix uses English. CLAUDE.md line 23 (`German comments for business logic, English
for technical terms`) explicitly permits this — the comment references technical concepts
(NameError, predicate naming convention, line numbers) rather than business logic. The
choice is documented in the SUMMARY for auditability. NO action change needed; this is
a planner-attestation that the existing comment style is correct.

**Output:**
- 1 model file modified (`app/models/version.rb` — single character fix on line 434).
- 1 test file extended (`test/models/version_test.rb` — 1 new T-CR-01 regression test).
- 1 file moved (`.planning/todos/pending/...md` → `.planning/todos/done/...md`).
- 1 documentation file modified (`.planning/ROADMAP.md` — 3 checkbox flips).

**Wave gating:** Wave 1 — independent of Plans 13, 14, 15, 16. Pure bookkeeping; touches
files no other round-4 plan touches.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-VERIFICATION.md
@.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-REVIEW.md
@.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-01-SUMMARY.md

# Files under modification
@app/models/version.rb
@test/models/version_test.rb
@.planning/ROADMAP.md
@.planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md

# Project skills + conventions
@.agents/skills/scenario-management/SKILL.md
@./CLAUDE.md

<interfaces>
<!-- CR-01 fix surface (verbatim from app/models/version.rb:428-436): -->

```ruby
# Phase 38.4-CR-01 fix target — current state:
def self.local_server?
  ApplicationRecord.local_server?
end

def self.local_from_api
  Version.sequence_reset if local_server  # ← BUG: should be local_server?
end
```

The `local_server` reference (without ?) is a NameError. Ruby will look up `local_server`
as a local variable (nil), then as a method on `Version` (defined as `local_server?`,
NOT `local_server`). Since neither matches, NameError raises at call time.

Fix:
```ruby
def self.local_from_api
  Version.sequence_reset if local_server?  # ← fixed
end
```

Same pattern as line 429-430 (`local_server?` predicate is the canonical method name).

INFO-01 file path:
- Source: `.planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md`
- Destination: `.planning/todos/done/2026-04-23-sync-version-yaml-load-json-collision.md`
- The `done/` directory does NOT exist; must be created.

INFO-02 ROADMAP.md target lines (verified by grep above):
```
Line 131: - [ ] `38.4-10-PLAN.md` — UAT round 3 layout/form integrity (closes O1, O3, O6, O7, O7a, O8): ...
Line 132: - [ ] `38.4-11-PLAN.md` — UAT round 3 Nachstoß rule + off-by-one fix (closes O2, O4): ...
Line 133: - [ ] `38.4-12-PLAN.md` — UAT round 3 BK-2kombi quick-game shortcut (closes O5): ...
```

All three should flip to `[x]`. (Plans 38.4-13..17 — the round-4 plans — STAY at `[ ]`
because they haven't shipped yet at the time Plan 17 executes.)

Existing test patterns in test/models/version_test.rb (read full file before adding the new test):
```ruby
# Plan 38.4-01 closed the safe_parse / safe_parse_for_text_column bug.
# T-CR-01 follows the same test scaffold.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix Version.local_from_api NameError + add regression test (CR-01)</name>
  <files>
    app/models/version.rb,
    test/models/version_test.rb
  </files>
  <read_first>
    - app/models/version.rb (lines 425-436 — the local_server? predicate + local_from_api method)
    - test/models/version_test.rb (full file — pattern reference for the new test)
    - .planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-VERIFICATION.md (CR-01 entry verbatim)
  </read_first>
  <behavior>
    - Test 1 (RED): `Version.local_from_api` raises NameError (pre-fix). Test asserts NO NameError raised.
    - Test 2 (GREEN): `Version.local_from_api` runs without error. When `local_server?` returns true, sequence_reset is called; when false, it's not called (no-op).
  </behavior>
  <action>
    --- STEP 1: Add RED test ---

    Find the existing test class in `test/models/version_test.rb`. After the last existing
    test, append:

    ```ruby
    test "T-CR-01-local-from-api-no-raises 38.4-17: Version.local_from_api uses local_server? (predicate)" do
      # Phase 38.4-CR-01: pre-fix, Version.local_from_api at version.rb:434 called
      # `local_server` (no question mark) — a NameError. This test asserts the method
      # invocation completes without raising NameError. Behaviour-wise, it's a no-op
      # when local_server? returns false (typical test-DB state) and triggers
      # sequence_reset when local_server? returns true.
      assert_nothing_raised do
        Version.local_from_api
      end
    end

    test "T-CR-01-local-from-api-stub-true 38.4-17: when local_server? stubbed true, sequence_reset is called" do
      called = false
      Version.stub :local_server?, true do
        Version.stub :sequence_reset, ->() { called = true } do
          Version.local_from_api
        end
      end
      assert_equal true, called,
        "T-CR-01: Version.sequence_reset must be called when local_server? returns true"
    end

    test "T-CR-01-local-from-api-stub-false 38.4-17: when local_server? stubbed false, sequence_reset is NOT called" do
      called = false
      Version.stub :local_server?, false do
        Version.stub :sequence_reset, ->() { called = true } do
          Version.local_from_api
        end
      end
      assert_equal false, called,
        "T-CR-01: Version.sequence_reset must NOT be called when local_server? returns false"
    end
    ```

    Run RED tests:
    ```bash
    bin/rails test test/models/version_test.rb -n /T-CR-01/
    ```
    Expected: T-CR-01-local-from-api-no-raises FAILS with NameError. The two stub tests
    may or may not reach the assertion depending on whether stub-then-call still raises.

    Atomic commit (RED): `test(38.4-17): RED — Version.local_from_api NameError regression test (CR-01)`

    --- STEP 2: Apply the one-character fix ---

    Edit `app/models/version.rb` line 434.

    NOTE on comment language (per I-17-01): The inline comment below uses English. This
    is the project-convention exception for technical terms (CLAUDE.md line 23 — "German
    comments for business logic, English for technical terms"). NameError + predicate
    naming convention + line-number references are technical, not business logic.

    BEFORE:
    ```ruby
    def self.local_from_api
      Version.sequence_reset if local_server
    end
    ```

    AFTER:
    ```ruby
    def self.local_from_api
      # Phase 38.4-17 CR-01: was `local_server` (no ?) — caused NameError on console
      # invocation. The canonical predicate is `local_server?` per line 429-430.
      # English per CLAUDE.md line 23 (technical terms convention).
      Version.sequence_reset if local_server?
    end
    ```

    Re-run RED tests — expect 3 PASS:
    ```bash
    bin/rails test test/models/version_test.rb -n /T-CR-01/
    ```

    Run the full version_test.rb suite (Plan 01 added 9 tests) for regression check:
    ```bash
    bin/rails test test/models/version_test.rb
    ```
    Expected: 9 + 3 = 12 tests PASS, 0 failures, 0 errors.

    Atomic commit (GREEN): `fix(38.4-17): Version.local_from_api uses local_server? (closes CR-01)`
  </action>
  <verify>
    <automated>bin/rails test test/models/version_test.rb -n /T-CR-01/ 2>&amp;1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Version.sequence_reset if local_server?' app/models/version.rb` == 1 (with question mark)
    - `grep -c 'Version.sequence_reset if local_server$' app/models/version.rb` == 0 (without question mark — no remaining bug)
    - `grep -c 'test "T-CR-01-local-from-api-no-raises 38\.4-17' test/models/version_test.rb` == 1
    - `grep -c 'test "T-CR-01-local-from-api-stub-true 38\.4-17' test/models/version_test.rb` == 1
    - `grep -c 'test "T-CR-01-local-from-api-stub-false 38\.4-17' test/models/version_test.rb` == 1
    - `bin/rails test test/models/version_test.rb -n /T-CR-01/` exits 0 (3 tests PASS)
    - `bin/rails test test/models/version_test.rb` exits 0 (12 total tests, 0 failures, 0 errors)
    - `bundle exec standardrb app/models/version.rb` exits 0
  </acceptance_criteria>
  <done>
    Version.local_from_api uses local_server? (with ?). Inline comment in English per CLAUDE.md line 23 technical-terms exception (per I-17-01 attestation). 3 RED→GREEN regression tests PASS. Full version_test.rb suite green. Atomic commits landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move sync-version-yaml-load-json-collision todo from pending → done (INFO-01)</name>
  <files>
    .planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md,
    .planning/todos/done/2026-04-23-sync-version-yaml-load-json-collision.md
  </files>
  <read_first>
    - .planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md (current state — verify it exists; capture file size for diff check)
    - .planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-01-SUMMARY.md (Plan 01 closed the underlying bug — confirm closure)
    - .planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-VERIFICATION.md (INFO-01 entry verbatim)
  </read_first>
  <action>
    --- STEP 1: Create todos/done directory if needed ---

    ```bash
    test -d .planning/todos/done || mkdir -p .planning/todos/done
    ```

    --- STEP 2: Move the todo file ---

    ```bash
    git mv .planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md \
           .planning/todos/done/2026-04-23-sync-version-yaml-load-json-collision.md
    ```

    Use `git mv` (not raw `mv`) so git tracks the rename. If the file was untracked,
    fall back to:
    ```bash
    mv .planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md \
       .planning/todos/done/2026-04-23-sync-version-yaml-load-json-collision.md
    ```

    --- STEP 3: Append a closure marker to the moved file ---

    Append to the bottom of the moved file (now at `.planning/todos/done/...md`):

    ```markdown

    ---

    ## Closure (Phase 38.4-17, 2026-04-25)

    Closed by Phase 38.4 Plan 01 (commit references in 38.4-01-SUMMARY.md). The
    `Version.safe_parse` / `safe_parse_for_text_column` helpers replaced all 4
    `YAML.load(args["data/remarks"])` callsites. 9 regression tests in
    `test/models/version_test.rb` lock the fix in place. This file is moved to
    `done/` for bookkeeping — the underlying code fix landed in Plan 38.4-01.
    ```

    Verify the move:
    ```bash
    test -f .planning/todos/done/2026-04-23-sync-version-yaml-load-json-collision.md
    test ! -f .planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md
    ```
    Both checks must pass.

    Atomic commit: `chore(38.4-17): move sync-version-yaml todo to done (closes INFO-01)`
  </action>
  <verify>
    <automated>test -f .planning/todos/done/2026-04-23-sync-version-yaml-load-json-collision.md &amp;&amp; test ! -f .planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md &amp;&amp; echo "INFO-01 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/todos/done/2026-04-23-sync-version-yaml-load-json-collision.md` exits 0 (file exists in done/)
    - `test ! -f .planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md` exits 0 (file no longer in pending/)
    - `grep -c 'Closure (Phase 38.4-17' .planning/todos/done/2026-04-23-sync-version-yaml-load-json-collision.md` == 1 (closure marker appended)
    - `git status` shows the file was renamed (not deleted + new) for clean history
  </acceptance_criteria>
  <done>
    sync-version-yaml-load-json-collision.md moved from pending/ to done/. Closure marker appended. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tick ROADMAP.md plans 38.4-10/11/12 checkboxes (INFO-02)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 130-135 — the BK-* phase 38.4 round-3 plans)
    - .planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-10-SUMMARY.md (proof Plan 10 complete)
    - .planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-11-SUMMARY.md (proof Plan 11 complete)
    - .planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-12-SUMMARY.md (proof Plan 12 complete)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`. Locate the 3 lines for plans 38.4-10/11/12 (around lines
    131-133 — verify exact line numbers via grep before editing).

    BEFORE:
    ```markdown
    - [ ] `38.4-10-PLAN.md` — UAT round 3 layout/form integrity (closes O1, O3, O6, O7, O7a, O8): ...
    - [ ] `38.4-11-PLAN.md` — UAT round 3 Nachstoß rule + off-by-one fix (closes O2, O4): ...
    - [ ] `38.4-12-PLAN.md` — UAT round 3 BK-2kombi quick-game shortcut (closes O5): ...
    ```

    AFTER:
    ```markdown
    - [x] `38.4-10-PLAN.md` — UAT round 3 layout/form integrity (closes O1, O3, O6, O7, O7a, O8): ...
    - [x] `38.4-11-PLAN.md` — UAT round 3 Nachstoß rule + off-by-one fix (closes O2, O4): ...
    - [x] `38.4-12-PLAN.md` — UAT round 3 BK-2kombi quick-game shortcut (closes O5): ...
    ```

    DO NOT modify any other lines in ROADMAP.md. Specifically, plans 38.4-13..17 (this
    round) STAY at `[ ]` because they haven't fully shipped yet at the time Plan 17
    executes (Plans 13/14/15/16 may already be complete; Plan 14 is wave 2).

    Verify the change:
    ```bash
    grep -E '^- \[x\] `38\.4-(10|11|12)-PLAN\.md`' .planning/ROADMAP.md
    grep -cE '^- \[ \] `38\.4-(10|11|12)-PLAN\.md`' .planning/ROADMAP.md
    ```
    Expected: first command shows 3 lines all marked `[x]`; second command outputs 0.

    Atomic commit: `docs(38.4-17): tick ROADMAP plans 38.4-10/11/12 (closes INFO-02)`
  </action>
  <verify>
    <automated>grep -cE '^- \[x\] `38\.4-(10|11|12)-PLAN\.md`' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^- \[x\] `38\.4-(10|11|12)-PLAN\.md`' .planning/ROADMAP.md` == 3 (all 3 plans now marked complete)
    - `grep -cE '^- \[ \] `38\.4-(10|11|12)-PLAN\.md`' .planning/ROADMAP.md` == 0 (no remaining unchecked entries for those plan IDs)
    - `grep -c '38\.4-12-PLAN\.md' .planning/ROADMAP.md` == 1 (line not duplicated)
    - `grep -c '38\.4-11-PLAN\.md' .planning/ROADMAP.md` == 1 (line not duplicated)
    - `grep -c '38\.4-10-PLAN\.md' .planning/ROADMAP.md` == 1 (line not duplicated)
  </acceptance_criteria>
  <done>
    ROADMAP.md plans 38.4-10/11/12 marked complete with [x]. No other lines modified. Atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — local-only bookkeeping) | This plan touches no public surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.4-17-01 | Information Disclosure | Version.local_from_api error message | accept | The CR-01 fix removes a NameError; the method is now well-defined. No new sensitive surface introduced. |
| T-38.4-17-02 | Tampering | local_server? predicate | accept | local_server? delegates to ApplicationRecord.local_server? — pre-existing trusted predicate. No new attack vector. |
</threat_model>

<verification>
- `bin/rails test test/models/version_test.rb -n /T-CR-01/` — 3 NEW PASS.
- `bin/rails test test/models/version_test.rb` — 12 tests PASS, 0 failures, 0 errors.
- `test -f .planning/todos/done/2026-04-23-sync-version-yaml-load-json-collision.md` — file exists.
- `test ! -f .planning/todos/pending/2026-04-23-sync-version-yaml-load-json-collision.md` — file gone from pending/.
- `grep -cE '^- \[x\] `38\.4-(10|11|12)-PLAN\.md`' .planning/ROADMAP.md` == 3.
- `bundle exec standardrb app/models/version.rb` — clean.
- Manual smoke (gating): on a local server, `bin/rails console` then `Version.local_from_api`
  → no NameError. (Pre-fix: NameError raised. Post-fix: returns nil silently when
  local_server? is false, OR triggers sequence_reset when true.)
</verification>

<success_criteria>
1. **CR-01 closure** — Version.local_from_api uses local_server? (with ?). NameError no longer reproduces. Verified by T-CR-01-local-from-api-no-raises + 2 stub-based tests.
2. **INFO-01 closure** — sync-version-yaml-load-json-collision todo file moved from pending/ to done/. Closure marker appended.
3. **INFO-02 closure** — ROADMAP.md plans 38.4-10/11/12 marked complete with [x].
4. **I-17-01 attestation** — inline English comment above the local_server? fix is per CLAUDE.md line 23 (technical-terms exception). Documented in SUMMARY.
5. **No regression** — full version_test.rb suite green (12 tests).
6. ERBlint / standardrb clean.
7. Round-4 plans 38.4-13..17 stay at [ ] in ROADMAP — they're closed in a separate update at the end of round 4.
</success_criteria>

<output>
After completion, create `.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-17-SUMMARY.md`
following the standard template. Note explicitly:
- Whether `Version.stub` worked for the local_server? mocking. If not, note the alternative
  (`Version.singleton_class.alias_method` + `define_method`).
- Confirmation that all 3 ROADMAP.md plan rows were ticked correctly (paste a `grep` output).
- The exact files moved by INFO-01 — confirming git tracked the rename rather than
  delete+new.
- **I-17-01 attestation:** record that the inline comment on the local_server? fix is
  English per CLAUDE.md line 23's technical-terms exception. NameError + predicate naming
  + line numbers are technical concepts, not business logic — English is the canonical
  language for these references.
- Cross-Plan reference: WR-01..06 NOT closed in this plan; they remain advisory and may
  be addressed in a future quick task or quality phase.
- Whether the ROADMAP.md edit also needs to update any milestone-progress percentages
  (likely NOT — STATE.md tracks progress; ROADMAP.md just tracks individual plans).
</output>
</content>
