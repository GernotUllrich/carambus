---
phase: 38.4-bk2-kombi-post-dry-run-gaps
plan: 07
plan_number: 07
type: execute
wave: 5
depends_on: ["38.4-01", "38.4-02", "38.4-03", "38.4-04", "38.4-05", "38.4-06"]
files_modified:
  - test/system/bk2_scoreboard_test.rb
  - test/services/bk2/commit_inning_test.rb
  - test/services/bk2/advance_match_state_test.rb
autonomous: true
requirements: []
decisions: [D-03]
issues: [I1, I2, I3, I4, I5, I7, I8, I9]

threats:
  - "T-38.4-07-01 (Tampering — T): test file rename misses references in other test files causing cascade breakage. Severity: LOW. Disposition: MITIGATE. Plan 05 already migrated Bk2Kombi:: references in services + tests; this plan's file rename (bk2_kombi_scoreboard_test.rb → bk2_scoreboard_test.rb) is additive."

must_haves:
  truths:
    - "test/system/bk2_kombi_scoreboard_test.rb is renamed to test/system/bk2_scoreboard_test.rb"
    - "New system tests cover I8 regression (delete button present in fallback banner)"
    - "New system tests cover I9 regression (set closes at balls_goal, not at 0)"
    - "New system tests cover all 5 BK-* disciplines for scoring dispatch (BK-2plus opponent-credit, BK-2 additive, BK50 additive, BK100 additive, BK-2kombi phase-dependent)"
    - "New system tests cover D-16 conditional visibility (DZ-max hidden for BK-2/BK50/BK100; SP-max hidden for BK-2plus/BK50/BK100)"
    - "New system tests cover D-17 shootout 4-button BK-2kombi-only"
    - "New system tests cover D-18 player-name labels on shootout buttons"
    - "New system tests cover D-19 grid orientation (cols=player, rows=mode)"
    - "Existing 38.3 T1-T13 tests updated for new strings (BK-2plus / BK-2) and new state key (balls_goal vs set_target_points)"
    - "Service-level tests for Bk2::AdvanceMatchState cover balls_goal-based set-close plus legacy set_target_points fallback"
    - "All tests pass on fresh DB after running bin/rails db:migrate (including BackfillBk2BallsGoal)"
  artifacts:
    - path: "test/system/bk2_scoreboard_test.rb"
      provides: "End-to-end system coverage for all 5 BK-* disciplines + I8/I9 regression"
      contains: "class Bk2ScoreboardTest < ApplicationSystemTestCase"
    - path: "test/services/bk2/advance_match_state_test.rb"
      provides: "Service coverage for balls_goal-based set-close + legacy fallback"
      contains: "balls_goal"
    - path: "test/services/bk2/commit_inning_test.rb"
      provides: "5-branch dispatcher test matrix"
      contains: "bk_2plus"
  key_links:
    - from: "test/system/bk2_scoreboard_test.rb"
      to: "All Phase 38.4 issues (I1-I9 except I6 already closed)"
      via: "end-to-end browser automation"
      pattern: "test \"I[0-9]"
---

<objective>
Close the Wave 5 test-coverage gate: rewrite `test/system/bk2_kombi_scoreboard_test.rb` (→ `test/system/bk2_scoreboard_test.rb`) to cover all 5 BK-* disciplines plus explicit regression probes for I8 (delete button) and I9 (balls_goal-based set-close). Extend service-level tests for `Bk2::AdvanceMatchState` and `Bk2::CommitInning` to lock in D-06 balls_goal semantics. Keep all Phase 38.3 T1-T13 assertions alive (updated for new strings) so nothing regresses.

Purpose: Proof of closure for all 8 deferred issues (I1-I9 except I6). Tournament-day safety net: if this test suite passes green, the BK-2 tournament in <4 weeks can go live with confidence.

Output: 1 renamed system test + ~13 new system tests + service test extensions.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-CONTEXT.md
@.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-RESEARCH.md
@.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-04-SUMMARY.md
@.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-05-SUMMARY.md
@.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-06-SUMMARY.md
@test/system/bk2_kombi_scoreboard_test.rb

<interfaces>
<!-- Existing Phase 38.3 coverage — 13 tests T1-T13 (preserved, updated for new strings): -->
<!-- T1: detail-view 4-button mode selector presence (now shootout, not detail-view — moved per Plan 06/38.3-05/06) -->
<!-- T2: i18n phase key guard -->
<!-- T3: shootout 4-button for BK-2kombi -->
<!-- T4-T11: various scoring + state + close-set flows -->
<!-- T12: reflex deletion guard (Bk2Kombi reflex endpoints removed) -->
<!-- T13: Alpine scope guard (GAP-02) -->

<!-- New Phase 38.4 tests per RESEARCH Focus Area 11 -->
<!-- T-I8, T-I9, T-BK50, T-BK100, T-BK2, T-BK2plus, T-BK2kombi-DZ, T-BK2kombi-SP, -->
<!-- T-DZ-max-visibility, T-SP-max-visibility, T-shootout-4btn-bk2kombi-only, -->
<!-- T-shootout-player-names, T-shootout-grid -->

<!-- Plan 04 deliverables (must be landed for tests to pass): -->
<!-- - 5 BK-* discipline fixtures (disciplines.yml) -->
<!-- - Discipline#bk_family? + ballziel_choices helpers -->
<!-- - TableMonitorsController CLAMP for 5 BK-* + balls_goal intersection with ballziel_choices -->
<!-- - GameSetup.BK_NAME_TO_FORM constant -->

<!-- Plan 05 deliverables: -->
<!-- - Bk2::CommitInning 5-way dispatcher -->
<!-- - Bk2::AdvanceMatchState balls_goal semantics -->
<!-- - All Bk2Kombi:: → Bk2:: rename complete -->

<!-- Plan 06 deliverables: -->
<!-- - _shootout.html.erb BK-2kombi 4-button in new grid layout -->
<!-- - scoreboard_free_game_karambol_new.html.erb conditional DZ/SP inputs -->
<!-- - is_bk2 5-family predicate in _scoreboard + _player_score_panel -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename system test file; preserve + update T1-T13; add 13 new Phase 38.4 tests</name>
  <files>
    - test/system/bk2_scoreboard_test.rb
  </files>
  <read_first>
    - test/system/bk2_kombi_scoreboard_test.rb (FULL FILE — understand all 13 existing tests T1-T13; many assertions will need string updates for Plan 03 rename; some may need state-key updates for Plan 04 balls_goal migration)
    - test/application_system_test_case.rb (understand base class + setup/teardown conventions)
    - test/fixtures/disciplines.yml (confirm Plan 04's 5 BK-* fixtures exist; tests use them)
    - app/views/table_monitors/_shootout.html.erb (Plan 06 structure — 4 buttons with data-bk2-first-set-mode + data-first-break-choice)
    - app/views/table_monitors/_show.html.erb (Plan 02 fallback banner with delete button)
    - .planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-RESEARCH.md (Focus Area 11: test rewrite scope)
  </read_first>
  <action>
Step 1 — Rename the test file:
```bash
git mv test/system/bk2_kombi_scoreboard_test.rb test/system/bk2_scoreboard_test.rb
```

Step 2 — Update the class declaration and module reference:
```ruby
# BEFORE:
class Bk2KombiScoreboardTest < ApplicationSystemTestCase

# AFTER:
class Bk2ScoreboardTest < ApplicationSystemTestCase
```

Step 3 — Update all existing T1-T13 tests (see RESEARCH Focus Area 11 — existing T3 shootout 4-button stays valid for BK-2kombi; T11 set-close at set_target_points changes to balls_goal). For each test:

- Find any hardcoded strings `"Direkter Zweikampf"` / `"Serienspiel"` / `"Direct duel"` / `"Series play"` → replace with `"BK-2plus"` / `"BK-2"`.
- Find any reference to `bk2_state["set_target_points"]` / `bk2_options["set_target_points"]` → replace with `bk2_state["balls_goal"]` / `tournament_monitor.balls_goal`.
- Find any `Bk2Kombi::` class reference → replace with `Bk2::`.
- Find any reflex endpoint reference for `bk2_kombi_submit_shot` (deleted in Plan 38.3-04) — those tests should be deletion-regression guards and stay.

Grep to find all sites needing updates:
```bash
grep -n 'Direkter Zweikampf\|Serienspiel\|Direct duel\|Series play\|set_target_points\|Bk2Kombi' test/system/bk2_scoreboard_test.rb
```

Step 4 — Add 13 new Phase 38.4 tests. Template structure:

```ruby
class Bk2ScoreboardTest < ApplicationSystemTestCase
  # Setup: create TableMonitor per discipline, populate bk2_state where needed
  setup do
    @admin = users(:admin)  # or appropriate fixture
    sign_in @admin
  end

  # ==========================================================================
  # Phase 38.4 I8 — delete button on fallback banner
  # ==========================================================================

  test "I8: fallback banner renders a delete button" do
    tm = create_bk2_kombi_table_monitor(with_bk2_state: false)  # bk2_state_uninitialized? == true
    visit table_monitor_path(tm)
    assert_selector 'form[action="/table_monitors/' + tm.id.to_s + '"][method="post"]'
    assert_selector 'input[type="hidden"][name="_method"][value="delete"]'
    assert_selector 'button', text: I18n.t("table_monitor.bk2_kombi.fallback.delete_button")
  end

  test "I8: delete button click destroys TableMonitor after confirm" do
    tm = create_bk2_kombi_table_monitor(with_bk2_state: false)
    visit table_monitor_path(tm)
    accept_confirm do  # Capybara accepts the Turbo confirm
      find('button', text: I18n.t("table_monitor.bk2_kombi.fallback.delete_button")).click
    end
    assert_nil TableMonitor.find_by(id: tm.id)
    assert_current_path table_monitors_path
  end

  # ==========================================================================
  # Phase 38.4 I9 — set closes at balls_goal (not set_target_points=0)
  # ==========================================================================

  test "I9: set closes when player reaches balls_goal for BK-2" do
    tm = create_bk_family_table_monitor(discipline: "BK-2", balls_goal: 50)
    # Commit an inning of 50 balls → set closes
    result = Bk2::CommitInning.call(table_monitor: tm, player: "playera", inning_total: 50)
    assert_equal 1, result[:state]["sets_won"]["playera"], "Set should have closed at 50"
  end

  test "I9: set does NOT close below balls_goal for BK-2" do
    tm = create_bk_family_table_monitor(discipline: "BK-2", balls_goal: 50)
    result = Bk2::CommitInning.call(table_monitor: tm, player: "playera", inning_total: 49)
    assert_equal 0, result[:state]["sets_won"]["playera"], "Set should NOT have closed at 49"
  end

  # ==========================================================================
  # Phase 38.4 I7 — 5-way scoring dispatch (browser-level sanity + service unit tests cover the matrix)
  # ==========================================================================

  test "T-BK50: BK50 additive, single-set, closes at 50" do
    tm = create_bk_family_table_monitor(discipline: "BK50", balls_goal: 50)
    # BK50 ballziel_choices = [50] (fixed)
    result = Bk2::CommitInning.call(table_monitor: tm, player: "playera", inning_total: 50)
    assert_equal 1, result[:state]["sets_won"]["playera"]
  end

  test "T-BK100: BK100 additive, single-set, closes at 100" do
    tm = create_bk_family_table_monitor(discipline: "BK100", balls_goal: 100)
    result = Bk2::CommitInning.call(table_monitor: tm, player: "playera", inning_total: 100)
    assert_equal 1, result[:state]["sets_won"]["playera"]
  end

  test "T-BK2plus: opponent-credit rule (negative inning → opponent)" do
    tm = create_bk_family_table_monitor(discipline: "BK-2plus", balls_goal: 50)
    result = Bk2::CommitInning.call(table_monitor: tm, player: "playera", inning_total: -3)
    assert_equal 0, result[:state]["set_scores"]["1"]["playera"]
    assert_equal 3, result[:state]["set_scores"]["1"]["playerb"]
  end

  test "T-BK2plus: positive inning credits to current player" do
    tm = create_bk_family_table_monitor(discipline: "BK-2plus", balls_goal: 50)
    result = Bk2::CommitInning.call(table_monitor: tm, player: "playera", inning_total: 5)
    assert_equal 5, result[:state]["set_scores"]["1"]["playera"]
    assert_equal 0, result[:state]["set_scores"]["1"]["playerb"]
  end

  test "T-BK2: additive (negative stays on player)" do
    tm = create_bk_family_table_monitor(discipline: "BK-2", balls_goal: 50)
    result = Bk2::CommitInning.call(table_monitor: tm, player: "playera", inning_total: -3)
    assert_equal(-3, result[:state]["set_scores"]["1"]["playera"])
    assert_equal 0, result[:state]["set_scores"]["1"]["playerb"]
  end

  test "T-BK2kombi-DZ: BK-2kombi in DZ phase uses opponent-credit" do
    tm = create_bk_family_table_monitor(discipline: "BK2-Kombi", balls_goal: 50, first_phase: "direkter_zweikampf")
    result = Bk2::CommitInning.call(table_monitor: tm, player: "playera", inning_total: -5)
    assert_equal 0, result[:state]["set_scores"]["1"]["playera"]
    assert_equal 5, result[:state]["set_scores"]["1"]["playerb"]
  end

  test "T-BK2kombi-SP: BK-2kombi in SP phase uses additive" do
    tm = create_bk_family_table_monitor(discipline: "BK2-Kombi", balls_goal: 50, first_phase: "serienspiel")
    result = Bk2::CommitInning.call(table_monitor: tm, player: "playera", inning_total: -5)
    assert_equal(-5, result[:state]["set_scores"]["1"]["playera"])
    assert_equal 0, result[:state]["set_scores"]["1"]["playerb"]
  end

  # ==========================================================================
  # Phase 38.4 I3/D-16 — detail-view conditional visibility
  # ==========================================================================

  test "T-DZ-max-visibility: DZ-max input hidden for BK-2/BK50/BK100" do
    visit scoreboard_free_game_karambol_new_location_path(locations(:bcw_small))
    # Click BK50 radio
    find('input[name="bk_form_choice"][value="bk50"]').click
    assert_no_selector 'input[name="bk2_options[direkter_zweikampf_max_shots_per_turn]"]:visible'
    # Click BK-2 radio
    find('input[name="bk_form_choice"][value="bk_2"]').click
    assert_no_selector 'input[name="bk2_options[direkter_zweikampf_max_shots_per_turn]"]:visible'
    # Click BK-2plus radio → DZ-max should appear
    find('input[name="bk_form_choice"][value="bk_2plus"]').click
    assert_selector 'input[name="bk2_options[direkter_zweikampf_max_shots_per_turn]"]:visible'
  end

  test "T-SP-max-visibility: SP-max input hidden for BK-2plus/BK50/BK100" do
    visit scoreboard_free_game_karambol_new_location_path(locations(:bcw_small))
    find('input[name="bk_form_choice"][value="bk_2plus"]').click
    assert_no_selector 'input[name="bk2_options[serienspiel_max_innings_per_set]"]:visible'
    find('input[name="bk_form_choice"][value="bk_2"]').click
    assert_selector 'input[name="bk2_options[serienspiel_max_innings_per_set]"]:visible'
    find('input[name="bk_form_choice"][value="bk2_kombi"]').click
    assert_selector 'input[name="bk2_options[serienspiel_max_innings_per_set]"]:visible'
  end

  test "T-Ballziel-readonly-bk50: BK50 shows fixed 50 (no dropdown)" do
    visit scoreboard_free_game_karambol_new_location_path(locations(:bcw_small))
    find('input[name="bk_form_choice"][value="bk50"]').click
    assert_no_selector 'select[x-model="bk_balls_goal"]:visible'
    assert_selector 'span[x-text="bk_balls_goal"]', text: "50"
  end

  # ==========================================================================
  # Phase 38.4 I4/D-17 — shootout 4-button BK-2kombi only
  # ==========================================================================

  test "T-shootout-4btn-bk2kombi-only: 4 buttons for BK-2kombi" do
    tm = create_bk_family_table_monitor(discipline: "BK2-Kombi", aasm_state: "match_shootout")
    visit table_monitor_path(tm)
    assert_equal 4, page.all('button[data-bk2-first-set-mode]').count
  end

  test "T-shootout-2btn-bk2: 2 buttons for BK-2 (generic Karambol)" do
    tm = create_bk_family_table_monitor(discipline: "BK-2", aasm_state: "match_shootout")
    visit table_monitor_path(tm)
    assert_equal 0, page.all('button[data-bk2-first-set-mode]').count
    # Generic 2-button path — assert presence of Karambol-style buttons instead
    assert_selector 'button', text: /stößt an/i, count: 2
  end

  # ==========================================================================
  # Phase 38.4 I5/D-18+D-19 — shootout player names + grid layout
  # ==========================================================================

  test "T-shootout-player-names: button labels contain player names" do
    tm = create_bk_family_table_monitor(discipline: "BK2-Kombi", aasm_state: "match_shootout",
                                         player_a_name: "Alice Anderson", player_b_name: "Bob Brown")
    visit table_monitor_path(tm)
    assert_selector 'button', text: /Alice Anderson.*BK-2plus/
    assert_selector 'button', text: /Bob Brown.*BK-2plus/
    assert_selector 'button', text: /Alice Anderson.*BK-2/
    assert_selector 'button', text: /Bob Brown.*BK-2/
  end

  test "T-shootout-grid: D-19 cols=player, rows=mode" do
    tm = create_bk_family_table_monitor(discipline: "BK2-Kombi", aasm_state: "match_shootout",
                                         player_a_name: "Alice", player_b_name: "Bob")
    visit table_monitor_path(tm)
    buttons = page.all('button[data-bk2-first-set-mode]').to_a
    # Row 1 (BK-2plus): Alice left, Bob right; both direkter_zweikampf
    assert_equal "direkter_zweikampf", buttons[0]["data-bk2-first-set-mode"]
    assert_match(/Alice/, buttons[0].text)
    assert_equal "direkter_zweikampf", buttons[1]["data-bk2-first-set-mode"]
    assert_match(/Bob/, buttons[1].text)
    # Row 2 (BK-2): Alice left, Bob right; both serienspiel
    assert_equal "serienspiel", buttons[2]["data-bk2-first-set-mode"]
    assert_match(/Alice/, buttons[2].text)
    assert_equal "serienspiel", buttons[3]["data-bk2-first-set-mode"]
    assert_match(/Bob/, buttons[3].text)
  end

  # ==========================================================================
  # Phase 38.4 I2 — i18n rename (BK-2plus / BK-2 replace old strings)
  # ==========================================================================

  test "T-i18n: phase chip renders 'BK-2plus' for direkter_zweikampf" do
    assert_equal "BK-2plus", I18n.t("table_monitor.bk2_kombi.phase.direkter_zweikampf.label")
    assert_equal "BK-2", I18n.t("table_monitor.bk2_kombi.phase.serienspiel.label")
  end

  # ==========================================================================
  # Plan 38.3 regression guards (Alpine scope + deleted reflex endpoints)
  # ==========================================================================

  test "T-alpine-scope: scoreboard_free_game_karambol_new has single x-data wrapper" do
    visit scoreboard_free_game_karambol_new_location_path(locations(:bcw_small))
    # Use JS to count x-data attributes
    count = page.evaluate_script('document.querySelectorAll("[x-data]").length')
    assert_equal 1, count, "GAP-02 regression: more than one x-data wrapper detected"
  end

  test "T-deleted-reflex-bk2_kombi_submit_shot: endpoint no longer exists" do
    refute TableMonitorReflex.instance_methods.include?(:bk2_kombi_submit_shot),
           "bk2_kombi_submit_shot should have been deleted in Plan 38.3-04"
  end

  private

  # ==========================================================================
  # Test helpers
  # ==========================================================================

  def create_bk2_kombi_table_monitor(with_bk2_state: true)
    # Returns a TableMonitor fixture with or without bk2_state populated
    # — used for I8 fallback-banner tests.
    tournament_monitor = tournament_monitors(:tm_bk2_fixture)
    discipline = disciplines(:bk2_kombi)
    tournament_monitor.update!(balls_goal: 50, sets_to_win: 2, sets_to_play: 3)
    tm = TableMonitor.create!(
      tournament_monitor: tournament_monitor,
      discipline: discipline,
      data: { "free_game_form" => "bk2_kombi" }
    )
    Bk2::AdvanceMatchState.new(tm).init_state_if_missing! if with_bk2_state
    tm.update!(data: tm.data.merge("bk2_state" => {})) unless with_bk2_state
    tm.reload
    tm
  end

  # Concrete implementation per checker issue 38.4-07 (info) resolution:
  # provides the executor the exact setup pattern rather than a comment-only stub.
  # Fixture names follow Plan 04's test/fixtures/disciplines.yml (bk2_kombi, bk50,
  # bk100, bk_2, bk_2plus).
  def create_bk_family_table_monitor(discipline:, balls_goal: 50, first_phase: "direkter_zweikampf",
                                      aasm_state: "playing", sets_to_win: 1, sets_to_play: 1,
                                      player_a_name: "Player A", player_b_name: "Player B")
    tournament_monitor = tournament_monitors(:tm_bk2_fixture)
    # Map the discipline display name (e.g., "BK-2plus") to its fixture symbol
    # (e.g., :bk_2plus). Plan 04 fixtures: bk2_kombi, bk50, bk100, bk_2, bk_2plus.
    fixture_symbol = case discipline
                     when "BK2-Kombi" then :bk2_kombi
                     when "BK50"      then :bk50
                     when "BK100"     then :bk100
                     when "BK-2"      then :bk_2
                     when "BK-2plus"  then :bk_2plus
                     else raise ArgumentError, "Unknown BK-family discipline: #{discipline.inspect}"
                     end
    discipline_record = disciplines(fixture_symbol)

    # Plan 04 wired balls_goal into tournament_monitor (D-06 migration).
    tournament_monitor.update!(
      balls_goal: balls_goal,
      sets_to_win: sets_to_win,
      sets_to_play: sets_to_play
    )

    # Free-game form string matches discipline.data[:free_game_form] per Plan 04 fixtures.
    free_game_form = case discipline
                     when "BK2-Kombi" then "bk2_kombi"
                     when "BK50"      then "bk50"
                     when "BK100"     then "bk100"
                     when "BK-2"      then "bk_2"
                     when "BK-2plus"  then "bk_2plus"
                     end

    tm = TableMonitor.create!(
      tournament_monitor: tournament_monitor,
      discipline: discipline_record,
      data: { "free_game_form" => free_game_form },
      aasm_state: aasm_state
    )

    # Initialize bk2_state via Plan 05's extended AdvanceMatchState.
    # For BK-2kombi, first_phase controls DZ vs SP semantics; ignored for others.
    tm.update!(data: tm.data.merge(
      "bk2_options" => { "first_set_mode" => first_phase }
    )) if discipline == "BK2-Kombi"

    Bk2::AdvanceMatchState.new(tm).init_state_if_missing!
    tm.reload
    tm
  end
end
```

Implement the `create_bk_family_table_monitor` helper and `create_bk2_kombi_table_monitor` helper using fixtures from `test/fixtures/disciplines.yml` (Plan 04) + dynamically constructed `TableMonitor` instances.

Step 5 — Run the full system test:
```bash
bin/rails test test/system/bk2_scoreboard_test.rb
```
All tests should pass. If any fail, triage:
- Fixture issue → verify Plan 04 fixtures landed.
- Controller issue → verify Plan 04 CLAMP.
- View issue → verify Plan 06 rendering.
- Service issue → verify Plan 05 dispatcher.

Step 6 — Extend `test/services/bk2/advance_match_state_test.rb` with balls_goal tests (per Plan 05 behavior):

```ruby
test "init_state_if_missing! reads balls_goal from tournament_monitor" do
  tm = table_monitors(:bk2_kombi_tm)
  tm.tournament_monitor.update!(balls_goal: 60)
  service = Bk2::AdvanceMatchState.new(tm)
  service.init_state_if_missing!
  assert_equal 60, tm.reload.data["bk2_state"]["balls_goal"]
end

test "init_state_if_missing! falls back to bk2_options[:set_target_points] if tournament_monitor.balls_goal is 0" do
  tm = table_monitors(:bk2_kombi_tm)
  tm.tournament_monitor.update!(balls_goal: 0)
  tm.update!(data: tm.data.merge("bk2_options" => { "set_target_points" => 70 }))
  service = Bk2::AdvanceMatchState.new(tm)
  service.init_state_if_missing!
  assert_equal 70, tm.reload.data["bk2_state"]["balls_goal"]
end

test "close_set_if_reached! reads state['balls_goal'] (new key)" do
  tm = table_monitors(:bk2_kombi_tm)
  tm.update!(data: tm.data.merge("bk2_state" => {
    "current_set_number" => 1, "balls_goal" => 50,
    "set_scores" => { "1" => {"playera" => 50, "playerb" => 30} },
    "sets_won" => {"playera" => 0, "playerb" => 0}
  }))
  Bk2::AdvanceMatchState.new(tm).close_set_if_reached!
  assert_equal 1, tm.reload.data["bk2_state"]["sets_won"]["playera"]
end

test "close_set_if_reached! reads state['set_target_points'] as transitional fallback" do
  tm = table_monitors(:bk2_kombi_tm)
  tm.update!(data: tm.data.merge("bk2_state" => {
    "current_set_number" => 1, "set_target_points" => 50,  # legacy key only
    "set_scores" => { "1" => {"playera" => 50, "playerb" => 30} },
    "sets_won" => {"playera" => 0, "playerb" => 0}
  }))
  Bk2::AdvanceMatchState.new(tm).close_set_if_reached!
  assert_equal 1, tm.reload.data["bk2_state"]["sets_won"]["playera"]
end
```

Step 7 — Run full test suite to confirm no regressions:
```bash
bin/rails test
# Or focus:
bin/rails test test/system/ test/services/bk2/ test/models/version_test.rb test/models/discipline_test.rb
```
  </action>
  <verify>
    <automated>[ -f test/system/bk2_scoreboard_test.rb ]</automated>
    <automated>[ ! -f test/system/bk2_kombi_scoreboard_test.rb ]</automated>
    <automated>grep -c 'class Bk2ScoreboardTest' test/system/bk2_scoreboard_test.rb  # expect 1</automated>
    <automated>grep -c 'test "I[89]:\|test "T-BK\|test "T-shootout\|test "T-DZ\|test "T-SP\|test "T-i18n\|test "T-alpine\|test "T-Ballziel\|test "T-deleted' test/system/bk2_scoreboard_test.rb  # expect >=13</automated>
    <automated>bin/rails test test/system/bk2_scoreboard_test.rb</automated>
    <automated>bin/rails test test/services/bk2/</automated>
    <automated>bin/rails test test/models/version_test.rb test/models/discipline_test.rb</automated>
    <automated>bundle exec standardrb test/system/bk2_scoreboard_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - File `test/system/bk2_kombi_scoreboard_test.rb` does NOT exist.
    - File `test/system/bk2_scoreboard_test.rb` EXISTS with `class Bk2ScoreboardTest < ApplicationSystemTestCase`.
    - New tests include: T-I8 (2 tests), T-I9 (2 tests), T-BK50, T-BK100, T-BK2plus (2 variants), T-BK2, T-BK2kombi-DZ, T-BK2kombi-SP, T-DZ-max-visibility, T-SP-max-visibility, T-Ballziel-readonly-bk50, T-shootout-4btn-bk2kombi-only, T-shootout-2btn-bk2, T-shootout-player-names, T-shootout-grid, T-i18n, T-alpine-scope, T-deleted-reflex.
    - Minimum 13 new tests; total (including preserved T1-T13) should be 20+ tests.
    - ZERO occurrences of `"Direkter Zweikampf"` / `"Serienspiel"` / `"Direct duel"` / `"Series play"` / `Bk2Kombi::` in `test/system/bk2_scoreboard_test.rb`.
    - `bin/rails test test/system/bk2_scoreboard_test.rb` exits 0.
    - `bin/rails test test/services/bk2/` exits 0.
    - `bundle exec standardrb test/system/bk2_scoreboard_test.rb` clean.
  </acceptance_criteria>
  <done>
    Phase 38.4 closure proof: all 8 deferred issues covered by explicit regression tests. Tournament-day BK-* test matrix is locked in. Any future regression on this suite blocks merge.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → service contract | Fixture DB provides 5 BK-* disciplines; tests rely on Plan 04's fixture landing correctly |
| browser automation (Capybara) → live view rendering | System tests probe the same DOM a user would see |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.4-07-01 | T (missing test references) | Other test files referencing Bk2Kombi:: | mitigate | Plan 05 already renamed service tests. This plan renames the ONE remaining system test file. Cross-file grep after this plan: `grep -rn 'Bk2Kombi' test/` should be empty. |
| T-38.4-07-02 | I (test flakiness from async Turbo/Alpine) | browser tests on reactive inputs | accept | Use Capybara's default wait mechanisms; assert on `x-show`-controlled elements via `:visible` modifier which waits for Alpine to settle. If flakiness surfaces post-merge, Plan 07 SUMMARY flags for a future stabilization pass. |
</threat_model>

<verification>
- All tests pass: `bin/rails test`.
- Test file rename committed via git mv (history preserved).
- No remaining `Bk2Kombi` / `bk2_kombi_scoreboard_test` references in test/.
- Full issue closure confirmed: I1 (discipline model), I2 (i18n), I3 (conditional detail-view), I4 (shootout 4-btn), I5 (shootout names + grid), I7 (namespace rename + dispatch), I8 (delete button), I9 (balls_goal).
</verification>

<success_criteria>
- Tournament-day test suite is green on a fresh clone + db:migrate.
- 13+ new tests explicitly tag the Phase 38.4 issue they close.
- Prior Phase 38.3 tests T1-T13 are either preserved-with-string-updates OR superseded-with-new-tests.
- Service-layer tests cover the balls_goal migration transitional fallback.
- Plan 07 = the final Phase 38.4 closure artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-07-SUMMARY.md` documenting:
- System test rename (bk2_kombi_scoreboard_test.rb → bk2_scoreboard_test.rb)
- List of all 13+ new tests with issue mapping (I1-I9 except I6)
- Service test extensions for balls_goal
- Final test counts (total passing)
- Any flakiness / timing issues to monitor post-merge
- CONFIRMATION: all 8 deferred issues (I1-I9 except I6) have explicit regression coverage
- CONFIRMATION: all 19 D-decisions from CONTEXT.md are implemented across Plans 01-07 at full fidelity
</output>
