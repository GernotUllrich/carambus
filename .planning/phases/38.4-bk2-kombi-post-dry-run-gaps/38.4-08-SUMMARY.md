---
phase: 38.4-bk2-kombi-post-dry-run-gaps
plan: "08"
subsystem: api
tags: [rails, action_controller, parameters, hash_with_indifferent_access, bk2, table_monitor, regression]

# Dependency graph
requires:
  - phase: 38.4-04
    provides: clamp_bk_family_params! helper + bk2_options nested hash structure (D-06/D-07)
  - phase: 38.3-06
    provides: existing free-game persists/CLAMPs tests (lines 328-426) that are RED gates
provides:
  - "TableMonitorsController#start_game converts params to plain Hash (.to_h) before slicing/clamping"
  - "TableMonitor::GameSetup#initialize defensively coerces ActionController::Parameters via .to_unsafe_h"
  - "Regression test I9 (integration) + I9b (unit) covering nested bk2_options crash"
affects: [38.4-09 (detail-view UI gaps), 38.4-HUMAN-UAT test 2 + test 4 (shootout)]

# Tech tracking
tech-stack:
  added: []
  patterns: ["params.permit(...).to_h coercion before nested-hash mutation", "Defensive .to_unsafe_h guard at service-layer ingress"]

key-files:
  created: []
  modified:
    - app/controllers/table_monitors_controller.rb
    - app/services/table_monitor/game_setup.rb
    - test/controllers/table_monitors_controller_test.rb

key-decisions:
  - "Two-layer fix (controller .to_h + service .to_unsafe_h) is intentionally belt-and-braces"
  - "Plan deviation Rule 1: dropped 'state == playing' assertion from I9 (fixture starts in state=new and start_game does not auto-transition through ready→warmup→playing)"

patterns-established:
  - "Controller params coercion: when controller helpers mutate nested hashes (clamp_bk_family_params!), call .to_h on permit() result first to ensure writes target a Hash not Parameters"
  - "Service-layer Parameters tolerance: services callable from controllers AND console/scripts should defensively .to_unsafe_h Parameters input"

requirements-completed: [I9-bk2-start-game-bk2_options-permit]

# Metrics
duration: 6min
completed: 2026-04-25
---

# Phase 38.4 Plan 08: BK2 start_game Nested-Hash Crash Fix Summary

**Two-layer fix (controller `.to_h` + service `.to_unsafe_h`) eliminates the `ActionController::UnfilteredParameters` crash on BK-* detail-form submission with nested `bk2_options` hash, also un-RED-ing 4 pre-existing Phase 38.3-06 free-game tests that were silently dropping `bk2_options`.**

## Performance

- **Duration:** 6 min 5 sec
- **Started:** 2026-04-25T12:41:15Z
- **Completed:** 2026-04-25T12:47:20Z
- **Tasks:** 4 (Task 0 pre-check + Tasks 1-3)
- **Files modified:** 3

## Accomplishments

- **I9 closed:** UAT test 2 verbatim payload (`free_game_form=bk2_kombi` + nested `bk2_options` hash) now starts a BK-2kombi game cleanly with `bk2_options.balls_goal=50`, `dz_max=2`, `sp_max=5` all persisted.
- **I9b closed:** `TableMonitor::GameSetup#initialize` now tolerates raw `ActionController::Parameters` with unpermitted nested keys (production crash path bypass), making the service safe to call from console/scripts/future tests.
- **4 bonus regressions closed:** Phase 38.3-06 free-game persists/CLAMPs tests at lines 329-426 of `table_monitors_controller_test.rb` were silently FAILING today (`bk2_options` ended up `nil` because of the same root-cause Parameters→Hash conversion gap). Task 2's `.to_h` fix flipped them GREEN.
- **Test 4 of UAT (shootout BK-2kombi 4-btn) is now unblocked** — re-run gating happens at phase UAT pass after Plan 09 lands.

## Task Commits

Each task was committed atomically:

1. **Task 0: PRE-CHECK — verify whether existing 38.3-06 tests pass today** — no commit (read-only diagnostic step). Result: **CASE A** (existing tests FAIL today; the IntegrationTest path reproduces the bug as silent `nil` rather than an explicit `UnfilteredParameters` raise — same root cause).
2. **Task 1: Add regression tests reproducing UnfilteredParameters crash** — `e04d2c39` (test) — RED: I9 fails with HTTP 500, I9b raises `ActionController::UnfilteredParameters: unable to convert unpermitted parameters to hash`.
3. **Task 2: Fix controller — convert params to plain Hash at line 67** — `5a604f3b` (fix) — Single trailing `.to_h` appended to `params.permit(params.keys)`. Closes I9 + the 4 pre-existing 38.3-06 free-game tests in one stroke.
4. **Task 3: Defensive belt-and-braces guard in GameSetup#initialize** — `91c37456` (fix) — `.to_unsafe_h` coercion if `kwargs[:options]` responds to it. Closes I9b unit test (the actual safety net for unpermitted nested Parameters values).

**Plan metadata:** (final commit pending — covers SUMMARY.md, STATE.md, ROADMAP.md)

## Files Created/Modified

- `app/controllers/table_monitors_controller.rb` (line 67 + 5-line comment) — `params.permit(params.keys)` → `params.permit(params.keys).to_h` so subsequent `clamp_bk_family_params!` writes target a Hash and the value forwarded to `GameSetup.call(options: p)` is convertible.
- `app/services/table_monitor/game_setup.rb` (lines 17-29) — `initialize(kwargs)` now extracts `raw = kwargs[:options] || {}` and defensively calls `raw.to_unsafe_h if raw.respond_to?(:to_unsafe_h)` before `HashWithIndifferentAccess.new(raw)`. Belt-and-braces with the controller fix.
- `test/controllers/table_monitors_controller_test.rb` (lines 428-501, +73 lines) — new I9 (integration) + I9b (unit) regression tests with full Task-0 pre-check rationale documented in a comment block.
- `.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/deferred-items.md` — new file documenting 3 pre-existing `set_target_points` test failures (out of scope for this plan).

## Decisions Made

- **Task 0 outcome — CASE A confirmed:** Existing 38.3-06 free-game tests at lines 329-426 FAIL today, but with a different failure mode than predicted. The plan predicted explicit `ActionController::UnfilteredParameters` raises; actual failures are silent `nil` data drops in `bk2_options`. **Same root cause** (Parameters not Hash, nested writes silently dropped), **same fix**, but the failure-mode mismatch was useful: it confirmed the bug reproduces in IntegrationTest path, which made I9 a valid RED→GREEN gate without needing the I9b unit-test fallback as primary.
- **Load-bearing fix is Task 2 (controller .to_h)**, not Task 3 (.to_unsafe_h guard). Task 2 alone closed I9 + the 4 existing tests. Task 3 closes I9b (the synthetic crash path that bypasses Rack-form-parsing). Both are belt-and-braces — but Task 2 is the production-path fix and Task 3 is the console/scripts/future-tests safety net. Documented this in commit messages and SUMMARY for future reference.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed incorrect `state == "playing"` assertion from I9 test**
- **Found during:** Task 2 (controller fix verification).
- **Issue:** The plan's `<behavior>` block at line 244 specified `assert_equal "playing", @table_monitor.state` after start_game. This is incorrect: the `table_monitors(:one)` fixture starts in `state="new"`, and `TableMonitor` AASM (`app/models/table_monitor.rb:333`) does NOT auto-transition from `:new` during start_game — that requires explicit `ready!` then `start_new_match!` events first. Existing 38.3-06 tests at lines 329-426 follow the correct pattern: they assert data persistence only, not state transition.
- **Fix:** Changed assertion to `assert_equal "new", @table_monitor.state` with a comment explaining why (cross-references the AASM definition + the existing test pattern + bk2_scoreboard_test.rb where state transitions ARE exercised with a properly-initialized fixture chain).
- **Files modified:** `test/controllers/table_monitors_controller_test.rb`
- **Verification:** I9 now passes; the 7 other assertions in I9 (data persistence) all pass. State assertion is a check that the test is correctly scoped, not silently failing.
- **Committed in:** `5a604f3b` (Task 2 commit, alongside the controller fix).

---

**Total deviations:** 1 auto-fixed (Rule 1 - test assertion bug)
**Impact on plan:** Minimal. The corrected assertion still proves what the plan wanted to prove (the controller flow completes without crash and bk2_options round-trips through GameSetup). The "state == playing" was a planning-time misunderstanding of AASM semantics, not a contract requirement.

## Issues Encountered

**3 pre-existing controller test failures discovered during Task 3 verification — out of scope, deferred:**

After Task 3 landed, the broader test sweep (`bin/rails test test/controllers/table_monitors_controller_test.rb`) revealed 3 failing tests:
- Line 110: `start_game with free_game_form=bk2_kombi seeds BK2 TableMonitor from detail form`
- Line 153: `start_game with quick_game_form=bk2_kombi bypasses the unless-block but still seeds bk2_kombi form`
- Line 221: `start_game BK2 detail-form persists first_set_mode=serienspiel when whitelisted value is submitted`

All 3 assert `bk2_options.set_target_points == N` with submitted `set_target_points: N`. **Verified pre-existing** by stashing my Task 2 + Task 3 changes and re-running against pre-fix code: same failures reproduce, confirming they are NOT regressions introduced by this plan.

**Root cause:** Per Phase 38.4-04 D-06, `set_target_points` was deprecated in favor of `balls_goal` as the canonical per-set Ballziel target. The controller no longer reads `set_target_points` as a top-level param. These 3 tests were written under the old contract and never updated.

**Status:** Logged to `.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/deferred-items.md`. Recommend a small chore commit in a follow-up to update the 3 tests under the D-06 contract (`set_target_points: N` → `balls_goal: N`, assertions `set_target_points` → `balls_goal`).

## User Setup Required

None — no external service configuration required. Pure code-side fix.

## Next Phase Readiness

- **Plan 09 unblocked:** detail-view UI gap closure (the other I9-cluster gap from UAT) can proceed without coordination.
- **UAT Test 2 ready for re-run:** the user's verbatim crashing payload now starts a BK-2kombi game cleanly. Manual UI smoke (browser → BK-* detail view → submit) is recommended at the next dev-server session, not gating.
- **UAT Test 4 (shootout BK-2kombi 4-btn) unblocked:** was blocked by I9 crash; re-run gating happens at phase UAT pass after Plan 09 lands.

## Self-Check: PASSED

Verified:
- File `app/controllers/table_monitors_controller.rb` modified — confirmed via `git show 5a604f3b --stat`
- File `app/services/table_monitor/game_setup.rb` modified — confirmed via `git show 91c37456 --stat`
- File `test/controllers/table_monitors_controller_test.rb` modified — confirmed via `git show e04d2c39 --stat`
- Commit `e04d2c39` exists in git log (Task 1 RED tests)
- Commit `5a604f3b` exists in git log (Task 2 controller .to_h fix)
- Commit `91c37456` exists in git log (Task 3 GameSetup defensive guard)
- Test runs: I9 + I9b + 4 existing 38.3-06 tests = 6 runs, 14 assertions, 0 failures, 0 errors, 0 skips
- BK2 service tests: 79 runs, 260 assertions, 0 failures, 0 errors, 0 skips
- BK2 dispatch integration test: 7 runs, 25 assertions, 0 failures, 0 errors, 0 skips
- Standardrb on changed files: clean (no output)

---
*Phase: 38.4-bk2-kombi-post-dry-run-gaps*
*Completed: 2026-04-25*
