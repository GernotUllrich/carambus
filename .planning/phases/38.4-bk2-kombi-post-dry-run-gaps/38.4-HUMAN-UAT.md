---
status: partial
phase: 38.4-bk2-kombi-post-dry-run-gaps
source: [38.4-VERIFICATION.md]
started: 2026-04-25T12:05:00Z
updated: 2026-04-25T20:15:00Z
gap_closure_plans: [38.4-08, 38.4-09, 38.4-10, 38.4-11, 38.4-12, 38.4-13, 38.4-14, 38.4-15, 38.4-16, 38.4-17]
round_4_closure:
  plans: [38.4-13, 38.4-14, 38.4-15, 38.4-16, 38.4-17]
  resolves_in_code:
    "38.4-13": [P1, P3]
    "38.4-14": [P4]
    "38.4-15": [P2]
    "38.4-16": [P5]
    "38.4-17": [CR-01, INFO-01, INFO-02]
  status: pending_browser_confirmation
  pending_human_uat: [R4-1, R4-2, R4-3, R4-4, R4-5]
  open_question: "Provisional Nachstoß resolution rule (trailing-equals-target → trailing wins) still pending Landessportwart sign-off — orthogonal to round-4 closure."
---

## Current Test

[testing complete — round 2 (post-08+09) — 4 pass, 1 issue with 8 sub-gaps O1-O8]

## Tests

### 1. I8 — Delete escape-hatch end-to-end on BK2 fallback banner
expected: Navigate to a BK2 fallback banner page (TableMonitor with bk2_state_uninitialized? == true). The red "Spiel löschen" button is visible inside the yellow banner. Clicking it triggers a Turbo "Sicher?" confirmation modal. Confirming destroys the record and redirects to /table_monitors. Record is gone from DB.
result: pass

### 2. I9 — balls_goal end-to-end via detail-view → set close
expected: Start a BK-2 game via the detail-view form. Select balls_goal=70. Start game. Commit innings until a player reaches 70. Set closes exactly at 70 (not 0, not 50). bk2_state shows balls_goal=70 (not set_target_points).
result: issue
severity: blocker
prior_round:
  reported: |
    1. Layout is not conform with the rest of the page - looks ugly
    2. on start: ActionController::UnfilteredParameters in TableMonitorsController#start_game — "unable to convert unpermitted parameters to hash" raised at app/services/table_monitor/game_setup.rb:19.
  closed_by: [38.4-08, 38.4-09]
  closure_evidence: |
    Plan 38.4-08 (commits e04d2c39, 5a604f3b, 91c37456): controller `params.permit(params.keys).to_h` + GameSetup defensive `.to_unsafe_h` guard. UnfilteredParameters crash no longer reproduces.
reported: |
  Re-test on 2026-04-25 surfaced 8 NEW issues (start_game crash IS fixed, but multiple downstream defects):

  O1: Spiel wird konstant mit Ballziel 50 gestartet — egal, was vorher ausgewählt wurde.
  O2: Bei Auswahl BK50 oder BK100 im shortcut wird kein Nachstoß erlaubt und Spiel endet bei Spieler A bei 50 Punkten.
  O3: In der Detailansicht gibt es für Punktziel zwei Reihen mit leicht unterschiedlicher Schreibweise ("Punkt-Ziel" vs "Punktziel") — zusammenführen. (Screenshot: /Users/gullrich/Desktop/Bildschirmfoto 2026-04-25 um 15.44.04.png)
  O4: Wenn das BK100 Spiel aus der Detailseite gestartet wird, ist Ballziel später 50 (s.o.) aber bei Erreichen von 50 wird zum Nachstoß gewechselt. Allerdings kommt der Nachstoßende nur bis 49 — danach wird jedes Increment abgelehnt und das Spiel kommt nicht zum Ende. Auch nicht, wenn ich über den Protokoll-Editor 50 eingebe.
  O5: BK-2kombi mit 2 Stoß im BK-2plus und 5 Aufnahmen im BK-2, mit Nachstoß und 70 Ballziel sollte es als Shortcut an erster Stelle geben.
  O6: Die Boxen in der Reihe BK-Variante enger machen, sodass die letzte rechtsbündig mit den anderen Reihen ist; ganz rechts stehen die +/- Korrekturen.
  O7: Die Zeile "BK-2 — max. Aufnahmen / Satz" sollte mit der Reihe Aufnahmebegrenzung zusammenfallen, dort dann 5 und 7 zur Auswahl stellen (bei BK-2kombi, BK-2 und BK-2plus). 5 und 7 sind Buttons — rechts das +/- Konstrukt zur Auswahl beliebiger Werte.
  O7a: Bei BK50 und BK100 gibt es fest nur eine Aufnahme.
  O8: Die Eingabe "BK-2plus — max. Stöße / Aufnahme" kann weg — fest per Konstante auf 2 verdrahtet. Es ist keine andere Variante bekannt.

### 3. I3 — Detail-view Alpine conditional input visibility per discipline
expected: For BK-2 — SP-max input visible, DZ-max input hidden. For BK-2plus — DZ-max visible, SP-max hidden. For BK-2kombi — both visible. (BK50 / BK100 hide both.)
result: pass
note: |
  Visibility logic confirmed working in re-test 2026-04-25.
  Layout/rows themselves still scheduled for restructuring per O3/O7/O8 from test 2.
prior_round:
  reported: |
    The conditional input works correctly — but the layout has to be like for the other disciplines in style: NO SELECTORS - only touch buttons. Punkt-Ziel has a separate row, where no row exists, it has to be introduced.
  closed_by: [38.4-09]

### 4. I4 + I5 — Shootout 4-btn BK-2kombi-only with real player names
expected: For BK-2kombi — 4 buttons rendered with real player names: "{PlayerName} stößt an — BK-2plus" and "{PlayerName} stößt an — BK-2". For BK-2 (and other non-BK-2kombi disciplines) — only the 2-button generic form: "{PlayerName} stößt an!".
result: pass
note: |
  Confirmed working in re-test 2026-04-25. Shootout reachable
  (Plan 08 unblocked the start_game path) and 4-button rendering
  matches expected for BK-2kombi.
prior_round:
  prior_result: blocked
  blocked_by: other
  reason: "Test 2 start_game crash prevented reaching the shootout screen."
  unblocked_by: [38.4-08]

### 5. ROADMAP.md — add Phase 38.4 row to milestone table
expected: Phase 38.4 row added to .planning/ROADMAP.md under v7.1 UX Polish & i18n Debt milestone, with status Complete, date 2026-04-25, and short goal description (close 8 BK-* dry-run gaps + sync collision fix). Bookkeeping prerequisite for marking phase complete.
result: pass

## Summary

total: 5
passed: 4
issues: 1
pending: 0
skipped: 0
blocked: 0
note: "Round 2 UAT (post 08+09 gap-closure): Tests 1, 3, 4, 5 pass. Test 2 surfaced 8 new sub-issues (O1-O8). Plans 08 + 09 confirmed working (start_game crash fixed, layout converted, shootout reachable). Round 3 gap-closure required for the 8 sub-issues."

## Gaps

- truth: "BK-2 / BK-* detail-view → start_game completes without error and the TableMonitor begins play with bk2_state.balls_goal honoring the form selection"
  status: resolved
  reason: |
    User reported: TableMonitorsController#start_game raises ActionController::UnfilteredParameters in app/services/table_monitor/game_setup.rb:19 — `HashWithIndifferentAccess.new(kwargs[:options] || {})` cannot convert ActionController::Parameters to a hash without explicit permission. Stack: controller#start_game (line 270) → table_monitor#start_game (line 1307) → GameSetup.initialize (line 19).
    Submitted params include free_game_form="bk2_kombi", balls_goal="50", bk2_options={direkter_zweikampf_max_shots_per_turn: "2", serienspiel_max_innings_per_set: "5"} plus the full set of *_choice form fields.
    Additional cosmetic: "Layout is not conform with the rest of the page - looks ugly"
  severity: blocker
  test: 2
  root_cause: |
    `clamp_bk_family_params!` writes a plain Ruby Hash into `p[:bk2_options]` where `p = params.permit(params.keys)` (line 67 of table_monitors_controller.rb). `params.permit(params.keys)` does shallow permit — it whitelists top-level scalar keys but the result is still an ActionController::Parameters object, and the nested `bk2_options` sub-object is never converted to a plain hash. When `start_game(p)` forwards `p` to `GameSetup.call(options: p)` and GameSetup does `HashWithIndifferentAccess.new(kwargs[:options])`, Rails 7.x rejects the conversion because the outer p was never explicitly permitted-to-h. Phase 38.4 didn't introduce the bug per se (the shallow permit pattern pre-existed), but Phase 38.4-04's `clamp_bk_family_params!` made `bk2_options` a *nested* parameter — previously bk2_options was either flat or absent for non-BK2 disciplines, so the issue did not surface.
  artifacts:
    - path: app/controllers/table_monitors_controller.rb:67
      issue: "`params.permit(params.keys)` produces an ActionController::Parameters object, not a plain Hash; nested bk2_options is never permitted/converted."
    - path: app/controllers/table_monitors_controller.rb:270
      issue: "p (still ActionController::Parameters) handed to start_game without `.to_h`/`.to_unsafe_h`."
    - path: app/services/table_monitor/game_setup.rb:19
      issue: "`HashWithIndifferentAccess.new(kwargs[:options] || {})` — fix belongs in caller, but defensive `.to_unsafe_h if .respond_to?(...)` here would also work."
  missing:
    - "Convert `p` to a plain hash before passing to start_game. Minimal fix: `params.permit(params.keys).to_h` at line 67 (so subsequent `p[:bk2_options] = {...}` writes target a plain Hash), OR `@table_monitor.start_game(p.to_h)` at line 270."
    - "Defensive guard in GameSetup (optional belt-and-braces): `kwargs[:options].respond_to?(:to_unsafe_h) ? kwargs[:options].to_unsafe_h : kwargs[:options]` before HashWithIndifferentAccess.new."
  debug_session: ""

- truth: "Detail-view BK-* configuration uses the same touch-button affordance as the rest of the karambol scoreboard family — no radios, no selectors; values selected by tapping numeric buttons; each parameter on its own row (introduce a Punkt-Ziel row where one is missing)"
  status: resolved
  reason: |
    User reported: "The conditional input works correctly — but the layout has to be like for the other disciplines in style: NO SELECTORS - only touch buttons. E.g. Punkt-Ziel has a separate row, where no row exists, it has to be introduced."
    Functional I3 visibility logic works (DZ-max / SP-max show/hide per discipline). The defect is the input *style*: Phase 38.4-06 introduced a 5-radio BK-family selector + Ballziel dropdown; the rest of the karambol detail-view uses touch-button rows (single row per parameter, big buttons, no dropdown/radio chrome). BK-* configuration must match.
  severity: minor
  test: 3
  root_cause: |
    Phase 38.4-06 added lines 317-390 of scoreboard_free_game_karambol_new.html.erb using bare HTML elements: `<input type="radio">` + styled `<span>` for BK-variant (lines 319-358); read-only `<span>` or `<select>` dropdown for Ballziel (lines 361-374); bare `<input type="number" class="w-24">` for DZ-max (line 380) and SP-max (line 387). The established pattern on the rest of the screen is the `_radio_select` partial — large `<label>` touch-buttons with hidden radios inside, plus optional ± counter rows. Existing Punktziel row at lines 451-462 of the same file is the canonical reference. Conditional visibility logic via Alpine x-show getters (is_bk_family, is_bk_dz_configurable, is_bk_sp_configurable, is_bk_fixed_goal) and state slots (bk_selected_form, bk_balls_goal, bk2_dz_max_shots, bk2_sp_max_innings) are CORRECT and need no change — only the DOM affordance changes.
  artifacts:
    - path: app/views/locations/scoreboard_free_game_karambol_new.html.erb:317-390
      issue: "Replace bare radio + select + number-input block with 4 _radio_select-style rows (BK-variant, Ballziel/Punkt-Ziel, DZ-max, SP-max). Preserve existing Alpine state slots and getters."
    - path: app/views/locations/_radio_select.html.erb
      issue: "Pattern reference — hidden radios inside large `<label>` touch-buttons with `bg-blue-600` active state + optional ± counter row."
  missing:
    - "Row 1 (BK-variant): Convert raw radio+span (lines 319-358) to _radio_select pattern — 5 large touch-buttons for BK50/BK100/BK-2/BK-2plus/BK-2kombi, hidden radios, x-show='is_bk_family' or always-shown."
    - "Row 2 (Ballziel/Punkt-Ziel): Introduce a dedicated row using _radio_select. For BK50/BK100 a single non-changeable button (50 or 100); for BK-2/BK-2plus a row of 6 buttons [50 60 70 80 90 100]; for BK-2kombi [50 60 70]. Drive values from `bk_ballziel_choices` getter. x-show='is_bk_family'. THIS IS THE 'MISSING ROW' THE USER EXPLICITLY MENTIONED."
    - "Row 3 (DZ-max): Convert bare `<input type=number>` (line 380) to ± button row pattern (dec-button / number-display / inc-button) like Spielzeit/Warnzeit at lines 544-555. x-show='is_bk_dz_configurable' guard preserved."
    - "Row 4 (SP-max): Same ± button row conversion as DZ-max. x-show='is_bk_sp_configurable' guard preserved."
  debug_session: ""

# === NEW GAPS from 2026-04-25 re-test (post 08+09 gap-closure) ===

- truth: "Started game honors the balls_goal selected in the form (BK50→50, BK100→100, BK-2/plus→user choice, BK-2kombi→user choice). bk2_state.balls_goal matches form selection — never silently coerced to 50."
  status: resolved
  reason: "User reported (O1): Spiel wird konstant mit Ballziel 50 gestartet — egal, was vorher ausgewählt wurde. Even when BK100 is started from the detail view, the resulting bk2_state.balls_goal is 50."
  severity: blocker
  test: 2
  sub_id: O1
  hypothesis: |
    Detail-view form has TWO Ballziel-shaped controls visible simultaneously (see O3): the new "Punkt-Ziel" row added by Plan 38.4-09 (lines 317-390) AND the pre-existing "Punktziel" row at lines 451+. Both bind to the same hidden input or fight over `balls_goal`/`set_target_points`. The OLD row likely wins (or the new row's discipline-aware default of 50 for BK50 leaks through to all variants). Investigate which input is actually submitted with the form and why it always equals 50.
  artifacts:
    - path: app/views/locations/scoreboard_free_game_karambol_new.html.erb:317-390
      issue: "New BK-* Punkt-Ziel touch-button row from Plan 09 — discipline-aware values via bk_ballziel_choices."
    - path: app/views/locations/scoreboard_free_game_karambol_new.html.erb
      issue: "Pre-existing Punktziel row (KEINS / 10 / 20 / 40 / 80 / 100 / 200 / 400 + counter) — likely overrides the new row's value when both are visible."
    - path: app/controllers/table_monitors_controller.rb:67
      issue: "clamp_bk_family_params! must reconcile both potential balls_goal sources and pick the new BK-* one for BK-* disciplines."
  missing:
    - "Hide or remove the pre-existing Punktziel row when a BK-* discipline is selected (x-show='!is_bk_family' guard), so only the new BK-aware row contributes to the form payload."
    - "Verify hidden-input wiring: which name= attribute carries the form-selected balls_goal into the params hash? Trace through start_game → GameSetup → bk2_state seeding."
  debug_session: ""

- truth: "BK50 and BK100 quick-game shortcuts allow Nachstoß (equalizing shot) when the second player has not yet had their turn at the closing point — set should NOT close immediately when player A reaches 50/100 if player B is still owed innings."
  status: resolved
  reason: "User reported (O2): Bei Auswahl BK50 oder BK100 im shortcut wird kein Nachstoß erlaubt und Spiel endet bei Spieler A bei 50 Punkten."
  severity: blocker
  test: 2
  sub_id: O2
  hypothesis: |
    `Bk2::AdvanceMatchState` set-close logic for BK50/BK100 (added in Phase 38.4-04 / 38.4-05) is missing the equalizing-shot branch. The renamed `Bk2::CommitInning` dispatcher likely treats BK50/BK100 as "additive scoring with hard cap" (D-12 style) but skips the Nachstoß rule that BK-2 / BK-2kombi use. Verify the per-discipline branch in commit_inning.rb / advance_match_state.rb honors `discipline.data[:nachstoss_allowed]` (or equivalent) for BK50/BK100.
  artifacts:
    - path: app/services/bk2/advance_match_state.rb
      issue: "Set-close logic for BK50/BK100 — missing equalizing-shot branch."
    - path: app/services/bk2/commit_inning.rb
      issue: "Dispatcher: should it route BK50/BK100 through the same Nachstoß path as BK-2?"
    - path: app/models/discipline.rb
      issue: "Discipline.data may need a `nachstoss_allowed: true` flag for BK50/BK100; if absent, scoreboard cannot decide."
  missing:
    - "Add Nachstoß branch for BK50 and BK100 in advance_match_state.rb — mirror the BK-2 / BK-2kombi logic."
    - "Seed-update Discipline records (script/seed_bk2_disciplines.rb) so BK50/BK100 carry the same nachstoss_allowed flag."
  debug_session: ""

- truth: "Detail-view shows exactly ONE Ballziel-shaped row for BK-* disciplines — the new BK-aware Punkt-Ziel row from Plan 09 — and hides the pre-existing generic Punktziel row to avoid duplication."
  status: resolved
  reason: "User reported (O3): In der Detailansicht gibt es für Punktziel zwei Reihen mit leicht unterschiedlicher Schreibweise ('Punkt-Ziel' vs 'Punktziel') — zusammenführen. Screenshot at /Users/gullrich/Desktop/Bildschirmfoto 2026-04-25 um 15.44.04.png shows both rows simultaneously."
  severity: minor
  test: 2
  sub_id: O3
  related: [O1]
  hypothesis: |
    Plan 38.4-09 added a NEW Punkt-Ziel row (lines 317-390 of scoreboard_free_game_karambol_new.html.erb) with x-show='is_bk_family' guard. The pre-existing Punktziel row (lines 451+, KEINS / 10 / 20 / 40 / 80 / 100 / 200 / 400 + counter) has no opposite x-show='!is_bk_family' guard, so for BK-* disciplines BOTH rows render. This is the same root cause as O1 (both fight for balls_goal).
  artifacts:
    - path: app/views/locations/scoreboard_free_game_karambol_new.html.erb:451
      issue: "Pre-existing Punktziel row needs x-show='!is_bk_family' guard so it hides when a BK-* discipline is selected."
  missing:
    - "Add x-show='!is_bk_family' (or equivalent) wrapper to the pre-existing Punktziel row block."
    - "OR: Merge the two rows into one — extend the existing Punktziel row to drive its values from bk_ballziel_choices when is_bk_family is true."
  debug_session: ""

- truth: "When a BK100 (or any BK-*) game is in Nachstoß state, the Nachstossende player can score up to and including the closing point — increments at the boundary value (50, 100, etc.) are accepted; the set closes when the Nachstossende either reaches the closing point or commits an inning that does not equalize."
  status: resolved
  reason: "User reported (O4): Bei Erreichen von 50 (BK100, but starts as Ballziel 50 per O1) wird zum Nachstoß gewechselt. Der Nachstoßende kommt nur bis 49 — danach wird jedes Increment abgelehnt und das Spiel kommt nicht zum Ende. Auch nicht, wenn ich über den Protokoll-Editor 50 eingebe."
  severity: blocker
  test: 2
  sub_id: O4
  related: [O1, O2]
  hypothesis: |
    Off-by-one in the Nachstoß increment guard. Once Player A has closed at 50, Player B starts Nachstoß and the score guard is incorrectly clamping at `balls_goal - 1` instead of `balls_goal`. Likely in score_shot.rb or the increment validator in commit_inning.rb. Also affects ProtokollEditor write path which appears to use the same validator. Investigate: `score < balls_goal` vs `score <= balls_goal` predicate in the increment guard.
  artifacts:
    - path: app/services/bk2/score_shot.rb
      issue: "Increment guard during Nachstoß — likely `score < balls_goal` should be `score <= balls_goal` for the Nachstossende."
    - path: app/services/bk2/commit_inning.rb
      issue: "Set-close detection after Nachstoß inning commit — must trigger when Nachstossende reaches OR fails to equal balls_goal."
    - path: app/reflexes/table_monitor_reflex.rb
      issue: "ProtokollEditor write path also blocked at 49 — shares the same validator."
  missing:
    - "Fix the off-by-one: Nachstoß player must be allowed to reach balls_goal exactly (currently capped at balls_goal-1)."
    - "Add set-close trigger: when Nachstossende's inning ends without equalizing, set closes with player A as winner; when they equal balls_goal, set goes to overtime per discipline rules (or closes per BK50/BK100 rules)."
    - "System test: T-bk100-nachstoss-equal-closes-set and T-bk100-nachstoss-below-loses."
  debug_session: ""

- truth: "Quick-game shortcut row (location#scoreboard) includes a BK-2kombi shortcut at first position with the most common defaults: BK-2plus max 2 strokes, BK-2 max 5 innings, Nachstoß enabled, balls_goal=70."
  status: resolved
  reason: "User reported (O5): BK-2kombi mit 2 Stoß im BK-2plus und 5 Aufnahmen im BK-2, mit Nachstoß und 70 Ballziel sollte es als Shortcut an erster Stelle geben."
  severity: minor
  test: 2
  sub_id: O5
  hypothesis: |
    Quick-game button list at app/views/locations/_quick_game_buttons.html.erb (last touched in Plan 38.4-04) currently has BK-* shortcuts for individual disciplines but no canonical BK-2kombi config. Add one button at the front: discipline=bk2_kombi, dz_max=2, sp_max=5, balls_goal=70, nachstoss=true.
  artifacts:
    - path: app/views/locations/_quick_game_buttons.html.erb
      issue: "Add new BK-2kombi shortcut button at index 0 of the BK-* group."
    - path: config/carambus.yml.erb
      issue: "If quick-game shortcuts are config-driven (not hardcoded in the view), add the BK-2kombi entry to the relevant section."
  missing:
    - "Insert BK-2kombi quick-start button: label='BK-2kombi 2/5/70+NS', params={free_game_form: 'bk2_kombi', balls_goal: 70, dz_max: 2, sp_max: 5, nachstoss: true}, position: first in BK-* group."
  debug_session: ""

- truth: "BK-Variante row in detail-view has 5 narrower buttons (BK50 / BK100 / BK-2 / BK-2plus / BK-2KOMBI) so the row width matches other rows — the rightmost BK-2KOMBI button aligns flush-right with the other rows' right edge; +/- correction controls (where present) sit at the far right."
  status: resolved
  reason: "User reported (O6): Die Boxen in der Reihe BK-Variante enger machen, sodass die letzte rechtsbündig mit den anderen Reihen ist; ganz rechts stehen die +/- Korrekturen."
  severity: cosmetic
  test: 2
  sub_id: O6
  artifacts:
    - path: app/views/locations/scoreboard_free_game_karambol_new.html.erb:317-358
      issue: "BK-Variante row buttons currently span full row width with equal flex; should be narrower (e.g., flex-none w-24 or grid-cols-12 with col-span-2 each) so the row reserves space for +/- controls on the right edge."
  missing:
    - "Constrain BK-Variante button widths to match other rows' button widths (e.g., grid layout with 5 fixed-width buttons + spacer + +/- column on right)."
    - "Verify visual alignment with screenshot baseline: BK-2KOMBI right edge flush with Punktziel '400'/+ right edge."
  debug_session: ""

- truth: |
    'BK-2 — max. Aufnahmen / Satz' row is merged into the existing 'Aufnahmebegrenzung' row.
    For BK-* disciplines that allow inning limits (BK-2kombi, BK-2, BK-2plus): touch buttons [5, 7] plus the existing +/- counter for arbitrary values.
    For BK50 and BK100: the row is fixed at 1 inning (no buttons, no counter — display-only or hidden).
  status: resolved
  reason: "User reported (O7): Die Zeile 'BK-2 — max. Aufnahmen / Satz' sollte mit der Reihe Aufnahmebegrenzung zusammenfallen, dort dann 5 und 7 zur Auswahl stellen (bei BK-2kombi, BK-2 und BK-2plus). 5 und 7 sind Buttons — rechts das +/- Konstrukt zur Auswahl beliebiger Werte. (O7a) Bei BK50 und BK100 gibt es fest nur eine Aufnahme."
  severity: minor
  test: 2
  sub_id: O7
  artifacts:
    - path: app/views/locations/scoreboard_free_game_karambol_new.html.erb
      issue: "Two separate rows currently: new SP-max row from Plan 09 (lines 317-390 area) AND existing Aufnahmebegrenzung row (KEINE / 20 / 25 / 30 + counter). Merge them — one row, discipline-aware."
  missing:
    - "Remove the new standalone SP-max row from Plan 09 lines 317-390."
    - "Extend the existing Aufnahmebegrenzung row: Alpine x-bind to swap button-set based on is_bk_family — for BK-* show [5, 7] + counter, for non-BK keep [20, 25, 30] + counter, for BK50/BK100 hide buttons (fixed 1 inning)."
    - "Update bk2_options.serienspiel_max_innings_per_set seeding to honor the merged row's value."
  debug_session: ""

- truth: "BK-2plus DZ-max (max strokes per turn) is hardcoded to 2 — no UI input visible in the detail-view because there is no other valid variant. The pre-existing 'BK-2plus — max. Stöße / Aufnahme' row from Plan 38.4-09 is removed from the form."
  status: resolved
  reason: "User reported (O8): Die Eingabe 'BK-2plus — max. Stöße / Aufnahme' kann weg — fest per Konstante auf 2 verdrahtet. Es ist keine andere Variante bekannt."
  severity: minor
  test: 2
  sub_id: O8
  artifacts:
    - path: app/views/locations/scoreboard_free_game_karambol_new.html.erb:317-390
      issue: "DZ-max input row added by Plan 09 — remove entirely; submit dz_max=2 as a hidden constant."
    - path: app/controllers/table_monitors_controller.rb
      issue: "clamp_bk_family_params! should hardcode bk2_options.direkter_zweikampf_max_shots_per_turn=2 for BK-2plus and BK-2kombi."
    - path: app/services/bk2/commit_inning.rb
      issue: "DZ-max enforcement at runtime: ensure the constant is honored regardless of submitted form value."
  missing:
    - "Delete DZ-max input row from detail-view (lines ~380 in scoreboard_free_game_karambol_new.html.erb)."
    - "Hardcode dz_max=2 in clamp_bk_family_params! for BK-2plus and BK-2kombi disciplines."
    - "Update Alpine state slot: bk2_dz_max_shots can be removed from x-data initialization, OR set to 2 and never mutated."
  debug_session: ""

# === Round 3 verification — pending browser/dev-server confirmation (Plans 10/11/12 shipped) ===

- truth: "BK-100 / BK-50 Nachstoß end-to-end — Player A reaches balls_goal → set does NOT close immediately; Player B receives equalizing inning; trailing player can score up to and including balls_goal (off-by-one removed). Provisional resolution rule (trailing equals balls_goal → trailing wins) needs Landessportwart sign-off."
  status: pending
  reason: "Plan 38.4-11 implementation verified via 11 service-level tests (T-O2 + T-O4) and 1 system-level file-grep guard. End-to-end browser confirmation pending."
  severity: high
  test: round_3
  sub_id: R3-1
  closure_evidence: |
    Plan 38.4-11 (commits 5a4c6874 → de64d303): Discipline#nachstoss_allowed? helper, seed flag set on all 5 BK-* disciplines, Bk2::AdvanceMatchState#close_set_if_reached! gains nachstoss_pending state machine, advance_to_next_set! extracted as private helper. 11 RED→GREEN service tests pass.
  open_question: "Provisional rule trailing-equals-target → trailing wins is isolated in close_set_if_reached!; can be changed without state migration if Landessportwart rules differently."

- truth: "Detail-view layout — single Punkt-Ziel row (no duplicate); BK-Variante row right-flush in 8-col grid; standalone DZ-max + SP-max rows removed; Aufnahmebegrenzung discipline-aware ([5,7] for BK-2/BK-2plus/BK-2kombi, [20,25,30] for BK-2plus phase, hidden for BK50/BK100); BK2-Kombi DZ-max hardcoded server-side to 2."
  status: pending
  reason: "Plan 38.4-10 implementation verified via 7 system tests (T-O1, T-O3, T-O6, T-O7, T-O8 + 2 layout guards). Visual confirmation in browser pending."
  severity: medium
  test: round_3
  sub_id: R3-2
  closure_evidence: |
    Plan 38.4-10 (commits f88c28d0 → 37261447): generic Punktziel row gated on !is_bk_family; BK-Variante row to grid-cols-8; standalone DZ-max + SP-max rows deleted; Aufnahmebegrenzung merged with discipline-aware values; clamp_bk_family_params! hardcodes dz_max=2 for BK-2plus and BK2-Kombi.

- truth: "BK-2kombi quick-game shortcut at index 0 — visible label 'BK-2kombi 2/5/70+NS' with canonical defaults (dz_max=2, sp_max=5, balls_goal=70, nachstoss_allowed=true). Renders before the existing 70/Best-of-3 entry."
  status: pending
  reason: "Plan 38.4-12 implementation verified via 2 system tests (T-O5-quick-game-shortcut-renders + T-O5-button-id-uniqueness). Browser tap confirmation pending."
  severity: low
  test: round_3
  sub_id: R3-3
  closure_evidence: |
    Plan 38.4-12 (commits d9e68a34 → 7019277c): config/carambus.yml.erb line 44 has 'BK-2kombi 2/5/70+NS' as first BK2-Kombi button; _quick_game_buttons.html.erb appends label_suffix to button_id for collision-free DOM ids.

- truth: "Existing BK-2kombi games — backwards-compatible. Adding nachstoss_allowed:true to all 5 BK-* disciplines (Plan 11 seed) does not regress existing BK-2kombi behavior; tournaments in flight continue without state migration."
  status: pending
  reason: "Plan 38.4-11 design preserves existing behavior — when nachstoss_allowed?true and close-set logic engages, the deferred-close branch is reached only on first leader-reaches-target event. Pre-Plan-11 games never set nachstoss_pending=true, so post-deploy they take the legacy path on existing in-flight sets. Confirmation via real-tournament smoke test pending."
  severity: medium
  test: round_3
  sub_id: R3-4
  closure_evidence: |
    Plan 11 SUMMARY: state-machine entry only on `target_reached_now? && discipline_nachstoss_allowed? && !nachstoss_pending`. Existing in-flight tm.data["bk2_state"] hashes lack nachstoss_pending key — safe.

# === Round 4 gaps — round 3 browser test 2026-04-25 18:50 surfaced 5 NEW issues ===
# Round 3 entries above (R3-1..R3-4) marked failed below; status flipped from pending.

- truth: "BK100 quick-start launches a game with balls_goal=100 and Nachstoß behavior consistent with the BK-2kombi quick-game contract."
  status: resolved
  reason: |
    User reported (P1): "Game BK100 still starts with Ball Goal 50, still no follow shot, when started from the quick start."
    The detail-page path closes O1 (balls_goal selection round-trips), but the QUICK-START path still launches with balls_goal=50 instead of 100, and Nachstoß is not engaged on reaching 50.
  severity: blocker
  test: round_3
  sub_id: P1
  flips: R3-1
  hypothesis: |
    1. clamp_bk_family_params! reads `p[:balls_goal]` (top-level), but the BK100 quick-start button in carambus.yml emits `balls_goal_a` / `balls_goal_b` only — no top-level `balls_goal`. The clamp falls through to `allowed.first` which for BK100 is 100, but for BK-2kombi (the discipline that the quick-button form actually carries) it is 50. Investigate which discipline the BK100 quick-button binds to in `_quick_game_buttons.html.erb` — if it is "BK2-Kombi" instead of "BK100", clamp picks 50 from BK2-Kombi's [50,60,70].
    2. Per P5, nachstoss_allowed should be scoped to BK-2kombi only — so "no Nachstoß for BK100" may actually be the intended new contract. P1 is then ONLY about the balls_goal=50 bug.
  artifacts:
    - path: app/views/locations/_quick_game_buttons.html.erb
      issue: "Verify which discipline_a/discipline_b values the BK100 quick-start button emits — if 'BK2-Kombi' instead of 'BK100', clamp picks wrong allowed list."
    - path: config/carambus.yml.erb
      issue: "Inspect the BK100 entry in quick_game_presets — is discipline=BK100 with balls_goal=100 declared, or does the partial inherit the parent category's discipline?"
    - path: app/controllers/table_monitors_controller.rb:111-123
      issue: "Quick-start branch sets discipline_a from BK2_DISCIPLINE_MAP.first if not already in the map. Verify discipline_a actually says 'BK100' before clamp_bk_family_params! runs."
  missing:
    - "Trace the BK100 quick-start payload end-to-end. Confirm discipline_a='BK100' reaches clamp_bk_family_params!."
    - "Add top-level balls_goal hidden input to each BK-* quick-start button in _quick_game_buttons.html.erb so clamp_bk_family_params! can read it."
    - "OR change clamp_bk_family_params! to read balls_goal from balls_goal_a / balls_goal_b as a fallback when top-level balls_goal is absent."

- truth: "Detail-view layout matches the rest of the karambol scoreboard: BK-Variante row 8-col grid right-flushes the last button to the +/- counter column; Aufnahmebegrenzung row reads cleanly without crowding the MEHRSATZ row below."
  status: resolved
  reason: |
    User reported (P2): Detail page garbled. Screenshot at /Users/gullrich/Desktop/Bildschirmfoto 2026-04-25 um 18.53.14.png shows:
    - BK-Variante row buttons (BK50/BK100/BK-2/BK-2PLUS/BK2KOMBI) stretched across full width, last button does NOT right-flush to +/- counter column;
    - Aufnahmebegrenzung row "KEINE / 5 / 7 / counter-20" is mis-aligned;
    - MEHRSATZ row crowds against the row above (no spacing).
  severity: high
  test: round_3
  sub_id: P2
  flips: R3-2
  hypothesis: |
    Plan 10 changed BK-Variante to grid-cols-8 but the 5 buttons distribute as col-span-1 each across 8 cols (5 of 8 used; remaining 3 cols absorb stretch). The intended layout was 5×col-span-1 + 3-col-wide gap. Fix: explicit width / max-width constraint on each button OR change to grid-cols-5 with separate +/- counter outside the grid. Aufnahmebegrenzung row layout regression possibly comes from the merged DZ-max/SP-max consolidation in Plan 10 — _radio_select partial probably emits col-span-2 buttons when 2 values declared, but layout assumed col-span-1.
  artifacts:
    - path: app/views/locations/scoreboard_free_game_karambol_new.html.erb (BK-Variante row)
      issue: "5 buttons in grid-cols-8 leave 3 cols of empty stretch. Adjust column-span or shrink button width."
    - path: app/views/locations/scoreboard_free_game_karambol_new.html.erb (Aufnahmebegrenzung row)
      issue: "Mis-aligned KEINE / 5 / 7 / counter — column span ratios likely wrong after Plan 10 merge."
    - path: /Users/gullrich/Desktop/Bildschirmfoto 2026-04-25 um 18.53.14.png
      issue: "Reference screenshot."
  missing:
    - "Inspect BK-Variante row column-span — change grid layout so the 5 buttons take 5 cols and the +/- column takes 3 cols, with the last button right-flush."
    - "Verify Aufnahmebegrenzung row visual against canonical karambol row (any other touch-button row in the file)."
    - "Add a system test that checks DOM layout (e.g., expected button container class list) — the screenshot regression slipped past Plan 10 system tests because they only check text content."

- truth: "BK-2kombi quick-start canonical shortcut launches game with balls_goal=70 (the canonical default declared in carambus.yml.erb)."
  status: resolved
  reason: |
    User reported (P3): BK-2kombi 2/5/70+NS shortcut renders correctly, but tap-to-start launches with balls_goal=50 instead of 70.
    Note: P3 has the same root cause as P1 — quick-start path bypass of new clamp logic. Rest of P3 (Nachstoß behavior, dz_max/sp_max canonical) cannot be verified until P4 (off-by-one) is fixed.
  severity: blocker
  test: round_3
  sub_id: P3
  flips: R3-3
  related: [P1, P4]
  hypothesis: "Same as P1: quick-start payload doesn't include top-level balls_goal=70, so clamp falls back to allowed.first (50 for BK2-Kombi)."
  missing:
    - "Same fix as P1 — BK-2kombi quick-start button must emit top-level balls_goal=70."

- truth: "Nachstoß deferred-close — when leader reaches balls_goal and trailing player commits their equalizing inning, the trailing player can score UP TO AND INCLUDING balls_goal (off-by-one removed); set close logic resolves correctly at exactly balls_goal."
  status: resolved
  reason: |
    User reported (P4): "Nachstoß funktioniert, wenn start von der Detailseite, aber bei 49 ist immer noch Schluss." Nachstoß engages from the detail page, but the trailing player still caps at 49 — every increment beyond 49 is rejected, set never closes. Even manually entering 50 via Protokoll-Editor is rejected.
  severity: blocker
  test: round_3
  sub_id: P4
  flips: R3-1 (partial)
  hypothesis: |
    Plan 11 added Nachstoß deferred-close in `Bk2::AdvanceMatchState#close_set_if_reached!` but the SCORING path (`Bk2::ScoreShot` / `Bk2::CommitInning`) still enforces an off-by-one cap. Most likely:
    - `Bk2::ScoreShot` rejects increments when `current >= balls_goal - 1` (legacy "stop at balls_goal" rule that should be inverted to "stop at balls_goal + 1" for trailing-player Nachstoß path).
    - OR `Bk2::CommitInning` short-circuits at balls_goal-1 before calling AdvanceMatchState.
  artifacts:
    - path: app/services/bk2/score_shot.rb
      issue: "Increment cap likely at balls_goal-1 instead of balls_goal."
    - path: app/services/bk2/commit_inning.rb
      issue: "Verify CommitInning routes Nachstoß-pending state through to AdvanceMatchState before applying any per-discipline cap."
    - path: app/services/bk2/advance_match_state.rb
      issue: "close_set_if_reached! Nachstoß branch — check whether trailing player's score being equal to balls_goal triggers resolution path; check off-by-one in score comparison."
  missing:
    - "Inspect score_shot.rb increment cap — change to allow scoring AT balls_goal during Nachstoß; cap remains balls_goal otherwise."
    - "Add a test: trailing player at balls_goal-1 + 1 increment = balls_goal exactly; assert NO rejection and set closes."
    - "Manually test via Protokoll-Editor: trailing player at 49, edit to 50, verify accepted."

- truth: "nachstoss_allowed flag is scoped to BK-2kombi (id 107) ONLY — BK50, BK100, BK-2, BK-2plus do NOT carry nachstoss_allowed in Discipline.data."
  status: resolved
  reason: |
    User reported (P5): "Nicht ganz, was ich wollte. Bitte nur BK-2kombi. Parameter können ggf in carambus.yml angepasst werden. Die anderen drei weg." User wants nachstoss_allowed scoped to BK-2kombi only. The other 3-4 BK-* disciplines (BK50, BK100, BK-2, BK-2plus — likely "die anderen drei" = BK50/BK100/BK-2plus, plus BK-2 already lacks it) should NOT have nachstoss_allowed seeded; if those disciplines need similar parameters they should be configurable via carambus.yml, not hardcoded in seed data.
  severity: high
  test: round_3
  sub_id: P5
  flips: R3-4
  hypothesis: |
    Reverses Plan 11's seed change. New contract: nachstoss_allowed lives ONLY on BK-2kombi (id 107). The 4 disciplines created by Plan 04 seed (BK50, BK100, BK-2, BK-2plus) should not carry the flag. Investigate whether the user wants the BK50/BK100/BK-2/BK-2plus seed records to remain at all ("die anderen drei weg" — three away — possibly meaning remove 3 of the 4 new seed records, but unclear which 3). Ask user to clarify.
  artifacts:
    - path: script/seed_bk2_disciplines.rb
      issue: "Currently sets nachstoss_allowed:true on all 5 BK-* disciplines. Restrict to BK-2kombi only."
    - path: config/carambus.yml.erb
      issue: "User says parameters can be adjusted here if needed for BK50/BK100/BK-2/BK-2plus quick-game presets."
  missing:
    - "Edit script/seed_bk2_disciplines.rb to set nachstoss_allowed:true ONLY on the BK2-Kombi (id 107) record. Drop the flag from BK50/BK100/BK-2/BK-2plus seed entries."
    - "ASK USER: Does 'die anderen drei weg' mean (a) remove 3 of the 4 Plan-04 seed records entirely (which 3?), or (b) just remove the nachstoss_allowed flag from those 3?"
    - "If (a): identify which 3 of {BK50, BK100, BK-2, BK-2plus} should be removed; update seed script + Discipline::BK2_DISCIPLINE_MAP + Discipline::BK2_FREE_GAME_FORMS accordingly."
    - "Update Plan 11 service tests — Nachstoß tests for BK50/BK100 must be reframed: either skip-when-not-applicable or assert that Nachstoß does NOT engage for those disciplines."

# === Summary of round 3 verification ===
# Plans 10/11/12 closed O1-O8 in code (verifier confirmed 12/12 must-haves).
# Round 3 browser test surfaced 5 NEW round-4 gaps: P1, P2, P3, P4, P5.
# Common root cause for P1+P3: quick-start path bypasses new clamp_bk_family_params! balls_goal logic.
# P4: off-by-one in Bk2::ScoreShot scoring cap during Nachstoß window.
# P2: visual layout regression (system tests check text only).
# P5: contract change — nachstoss_allowed scope narrowed to BK-2kombi.

# === Round 5 — pending browser verification of round-4 closure (Plans 13-17) ===
# Plans 13/14/15/16/17 closed P1-P5 + CR-01/INFO-01/INFO-02 in code (verifier confirmed 17/17 must-haves).
# 5 items below need end-to-end human confirmation in a real browser session.

### R4-1. P1/P3 — BK100 + BK-2kombi quick-start launches with correct balls_goal end-to-end
expected: |
  Tap the BK100 quick-start button on the location scoreboard. Game launches with bk2_state.balls_goal=100.
  Tap the BK-2kombi 2/5/70+NS quick-start button. Game launches with bk2_state.balls_goal=70 (not 50).
  No silent coercion. clamp_bk_family_params! reads balls_goal_a / balls_goal_b fallback per BK_FAMILY_BALLZIEL_FALLBACK.
result: pending

### R4-2. P4 — BK-2kombi Nachstoß end-to-end: trailing player can score up to and including balls_goal
expected: |
  Start a BK-2kombi game with balls_goal=70. Score Player A to 70. Set transitions to Nachstoß state with Player B as trailing.
  Player B scores from current value up to 70. Each increment ≤ 70 is accepted (no rejection at 69 → 70 boundary).
  Manually edit Player B's score to 70 via Protokoll-Editor; write is accepted; set closes correctly with the resolution rule.
  Verify both +1 button path AND ProtokollEditor write path complete without rejection.
result: pending

### R4-3. P5 — BK50/BK100/BK-2/BK-2plus immediate-close (no Nachstoß)
expected: |
  Start a BK50 game. Player A reaches 50 → set closes IMMEDIATELY (no Nachstoß state, no equalizing inning).
  Same for BK100 (close at 100), BK-2 (close at chosen balls_goal), BK-2plus (close at chosen balls_goal).
  Only BK-2kombi triggers the deferred-close Nachstoß state machine.
  Confirm migration ran: existing BK-2/BK-2plus/BK50/BK100 disciplines no longer carry nachstoss_allowed:true.
result: pending

### R4-4. P2 — Detail-view visual layout pixel verification at 1024×768
expected: |
  Open detail-view for BK-* game. BK-Variante row (5 buttons in grid-cols-5) right-flushes to the +/- counter column.
  Aufnahmebegrenzung row "KEINE / 5 / 7 / counter-20" reads cleanly with cols=4 grid layout.
  MEHRSATZ row has visible breathing room above (h-4 spacer) — no crowding.
  Compare against baseline screenshot at /Users/gullrich/Desktop/Bildschirmfoto 2026-04-25 um 18.53.14.png — should look CLEANER, not garbled.
  Confirm whether h-4 spacer is sufficient or h-6 preferable.
result: pending

### R4-5. Production deploy operator playbook
expected: |
  Run master seed re-run (`bin/rails runner script/seed_bk2_disciplines.rb`) on the central API server.
  Run local migrations (`bin/rails db:migrate`) on each local server (carambus_bcw, carambus_phat, carambus_master).
  Verify Version sync propagation: the 4 new central Discipline records (BK50, BK100, BK-2, BK-2plus) appear on all local servers without manual unprotected=true writes.
  Verify migration 20260425090000_clear_nachstoss_allowed_for_non_bk2_kombi.rb runs cleanly and clears the flag from non-BK-2kombi local-id Discipline records.
  Verify CR-01 fix: Version.local_from_api no longer crashes with NameError on local_server.
result: pending

# === Round 6 — discovered 2026-04-25 23:45 during P11/P12 verification ===

### R5-1. BK-2 scoring counter jumps back on player switch (NEW BUG, circumstances unclear)
reported: 2026-04-25T23:45 — user observation during BK-2 detail-form testing.
symptom: |
  When the active player switches in BK-2, the score counter snaps back to a lower value.
  Exact reproduction conditions are not yet pinned down — needs investigation tomorrow.
hypothesis_starters: |
  - Could be related to score_engine.recompute_result discarding innings_redo_list[-1] mid-inning
  - Could be Bk2::AdvanceMatchState route dispatch mismatch for non-Nachstoß BK-* (BK-2 has no Discipline record locally → bk_family_with_nachstoss? = false → legacy karambol path)
  - Could be P9-related: removed result_recorder dispatch; verify nothing in live-scoring path was reading state set by that dead dispatch
  - Could be inning_redo_list rotation when player swaps mid-inning vs. at goal_reached
investigation_starting_points:
  - app/models/table_monitor/score_engine.rb#terminate_inning_data (line 1205+) — pop innings_redo_list, push to innings_list
  - app/models/table_monitor.rb#add_n_balls (line 884+) — :goal_reached dispatch routing
  - Bk2::CommitInning vs. legacy terminate_current_inning fork at line 907-911
session_handoff: |
  Last commit before pause: 324493e4 (P12). Browser-verified through P11.
  Next session: reproduce R5-1 with fresh BK-2 game, watch innings_list / innings_redo_list / result
  in tm.data after each click. The bug shows on player switch — likely innings_redo_list[-1]
  is being lost or recomputed wrong.
result: resolved
resolved_by:
  - "85e87a99 R5-1 step 1a: karambol_commit_inning! helper synchronizes BK-* commits"
  - "4c3abdf4 R5-1 step 1b-i: Bk2::CommitInning calls helper internally"
session_2026_04_26_resolved:
  R5-1: "Score-Sprung beim Spielerwechsel — pop redo_list, push to innings_list, recompute_result"
  R5-2: "ba_results.Sets1/2 Doppelzählung pro Satz (Match endete nach Satz 1)"
  R5-3: "Negativer Wert in DZ-Phase → opponent-credit auch karambol-seitig (eigener result bleibt 0, opponent.redo += abs)"
  R5-4: "S1: 70; ... statt S1: 70, ... Trennzeichen"
  R5-5: "Phase-Chip BK-2plus/BK-2 alterniert pro Satz (aus data['sets'].length statt stale bk2_state.current_phase)"
  R5-6: "Stöße-übrig-Badge in DZ-Phase entfernt (kein UI um Stoß abzuschließen)"
  R5-7: "innings_goal aus Form durchreichen — karambol's end_of_set? schließt jetzt nach Aufnahmen-Limit (BK50/BK100=1, andere=Form-Wert default 5)"

# === Round 6 deferred — tournament path (NOT yet covered) ===

### R6-T1. Tournament path uses tournament_monitor.balls_goal + TournamentPlan parameters
expected: |
  All R5-* fixes were tested in training/quick-game/detail-form path. Tournament path is different:
    - balls_goal comes from tournament_monitor.balls_goal (set via Ausschreibung + start-form on TournamentMonitor)
    - innings_goal source unknown — R5-7 only reads from p[:innings_choice] which is detail-form-specific
    - bk2_options defaults (DZ-max-shots, SP-max-innings, first_set_mode) — where set in tournament path?
    - nachstoss_allowed flag for BK-2kombi — Discipline.data or TournamentPlan override?
  Investigate before next club tournament. R5-* fixes likely partially apply but innings_goal will need
  a tournament-specific source (likely tournament_monitor.innings_goal or tournament_plan param).
result: pending
hypothesis: |
  Bk2::AdvanceMatchState#derive_balls_goal already prioritises tournament_monitor.balls_goal — that path works.
  But Aufnahmen-Limit needs analogous derivation. Check TournamentMonitor / TournamentPlan model fields
  for innings_goal or equivalent. May need a Bk2::AdvanceMatchState#derive_innings_goal helper.
investigation_starting_points:
  - app/models/tournament_monitor.rb (for balls_goal, innings_goal fields)
  - app/models/tournament_plan.rb (for parameter defaults)
  - app/services/table_monitor/game_setup.rb (how does tournament-game start_game inject params?)
  - Trace path: TournamentMonitor#assign_game → TableMonitor#start_game → bk2_options seeding
