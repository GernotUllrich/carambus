---
phase: 38.4-bk2-kombi-post-dry-run-gaps
plan: 03
plan_number: 03
type: execute
wave: 2
depends_on: ["38.4-01"]
files_modified:
  - config/locales/de.yml
  - config/locales/en.yml
autonomous: true
requirements: []
decisions: [D-03, D-08, D-09]
issues: [I2]

threats:
  - "T-38.4-03-01 (Tampering — T): accidental change to internal mode keys (e.g. renaming `direkter_zweikampf` → `bk_2plus`) would break bk2_state JSON round-trip for in-flight BK2-Kombi tournaments. Severity: HIGH. Disposition: MITIGATE. Enforcement: plan acceptance criteria verify internal mode keys in i18n YAML paths remain `direkter_zweikampf` / `serienspiel`; only the VALUES change. Add a code comment invariant noting display-label vs internal-key split."
  - "T-38.4-03-02 (Disclosure — I): no user data disclosure. Severity: N/A. Disposition: N/A."

must_haves:
  truths:
    - "de.yml `table_monitor.bk2_kombi.phase.direkter_zweikampf.label` value changed from 'Direkter Zweikampf' to 'BK-2plus' (and related sibling paths)"
    - "de.yml `table_monitor.bk2_kombi.phase.serienspiel.label` value changed from 'Serienspiel' to 'BK-2'"
    - "en.yml parallel changes: 'Direct duel'/'Series play' → 'BK-2plus'/'BK-2'"
    - "Phase-chip keys (`phase_chip.direkter_zweikampf`, `phase_chip.serienspiel`) updated to match"
    - "first_set_mode button-label keys updated — values use 'BK-2plus' / 'BK-2' (players' names come from Plan 05 interpolation, not i18n)"
    - "Internal mode keys `direkter_zweikampf` / `serienspiel` stay UNCHANGED throughout codebase (service constants, bk2_state JSON, AASM transitions, tests, fixtures)"
    - "A code-comment invariant is added at the top of the changed YAML subtree documenting the display-vs-internal split"
    - "DE + EN ship in parallel (no asymmetric rename)"
  artifacts:
    - path: "config/locales/de.yml"
      provides: "Updated DE values for phase labels, phase chips, and mode-button labels"
      contains: "label: BK-2plus"
    - path: "config/locales/en.yml"
      provides: "Updated EN values (mirrored)"
      contains: "label: BK-2plus"
  key_links:
    - from: "config/locales/de.yml (phase.direkter_zweikampf.label)"
      to: "UI render of phase chip in _scoreboard.html.erb"
      via: "t('table_monitor.bk2_kombi.phase.direkter_zweikampf.label')"
      pattern: "table_monitor\\.bk2_kombi\\.phase\\."
---

<objective>
Close I2 by renaming UI labels only: "Direkter Zweikampf" → "BK-2plus" and "Serienspiel" → "BK-2". Per D-08, this is i18n VALUES only — internal mode keys (`direkter_zweikampf`, `serienspiel`) stay unchanged throughout the codebase to eliminate bk2_state migration risk for in-flight BK2-Kombi tournaments. Per D-09, YAML key PATHS also stay unchanged — only VALUES change. DE + EN ship in parallel.

Purpose: Align the UI with BK-pro player + Landessportwart sign-off terminology. Shipping the rename in Wave 2 (before the I1/I7 restructure in Wave 3) means Plan 04 code touches already-renamed labels, avoiding rename-churn on in-flight code.

Output: `config/locales/de.yml` + `config/locales/en.yml` value updates (no ERB, no Ruby, no reflex changes).
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_bcw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-CONTEXT.md
@.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-RESEARCH.md
@config/locales/de.yml
@config/locales/en.yml

<interfaces>
<!-- RESEARCH Focus Area 12 confirms key surface. -->
<!-- de.yml around lines 598-655: -->
```yaml
de:
  table_monitor:
    bk2_kombi:
      phase:
        direkter_zweikampf:
          label: "Direkter Zweikampf"           # → CHANGE to "BK-2plus"
          # remaining_label: "Stöße übrig"      # stays (structural — how the badge reads)
        serienspiel:
          label: "Serienspiel"                   # → CHANGE to "BK-2"
      phase_chip:
        direkter_zweikampf: "Direkter Zweikampf" # → CHANGE to "BK-2plus"
        serienspiel: "Serienspiel"               # → CHANGE to "BK-2"
      first_set_mode:
        legend: "Erster Satz — Modus & Anstoß"  # stays
        button_direkter_zweikampf_a: "Spieler A stößt an — Direkter Zweikampf"  # see Plan 05 — deprecated for interpolation
        button_serienspiel_a: "Spieler A stößt an — Serienspiel"
        button_direkter_zweikampf_b: "Spieler B stößt an — Direkter Zweikampf"
        button_serienspiel_b: "Spieler B stößt an — Serienspiel"
```

<!-- en.yml around lines 565-616: same structure, "Direct duel" / "Series play" values -->

<!-- Internal mode keys that MUST stay unchanged -->
<!-- These appear in: -->
<!-- - app/services/bk2_kombi/advance_match_state.rb — "direkter_zweikampf", "serienspiel" string literals -->
<!-- - bk2_state JSON column values: state["current_phase"] in {"direkter_zweikampf", "serienspiel"} -->
<!-- - bk2_options["first_set_mode"] in same set -->
<!-- - AASM transitions (if any reference these names) -->
<!-- - tests and fixtures -->
<!-- - i18n KEY PATHS (YAML key names) also stay as "direkter_zweikampf" / "serienspiel" — only VALUES change -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename DE + EN i18n values; add display-vs-internal invariant comment</name>
  <files>
    - config/locales/de.yml
    - config/locales/en.yml
  </files>
  <read_first>
    - config/locales/de.yml (FULL FILE relevant section — grep for `table_monitor.bk2_kombi` and read the entire sub-tree)
    - config/locales/en.yml (same — EN sub-tree)
    - .planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-RESEARCH.md (Focus Area 12: full list of keys to update)
    - .planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-CONTEXT.md (D-08, D-09: rename depth)
  </read_first>
  <action>
Step 1 — Open `config/locales/de.yml`. Locate the `table_monitor: bk2_kombi:` sub-tree.

Apply these EXACT value substitutions (keep key paths unchanged):

```yaml
# BEFORE → AFTER (value changes only; YAML keys unchanged)
de:
  table_monitor:
    bk2_kombi:
      # --- ADD a comment at the top of this sub-tree ---
      # Display-label invariant (Phase 38.4 I2): the YAML keys below (direkter_zweikampf,
      # serienspiel) are INTERNAL mode keys that are ALSO stored in bk2_state["current_phase"]
      # and bk2_options["first_set_mode"]. Do NOT rename the keys — only their display VALUES.
      # Display values map: direkter_zweikampf ↔ "BK-2plus", serienspiel ↔ "BK-2" (BK-pro sign-off).

      phase:
        direkter_zweikampf:
          label: "BK-2plus"                                 # was: "Direkter Zweikampf"
        serienspiel:
          label: "BK-2"                                      # was: "Serienspiel"
      phase_chip:
        direkter_zweikampf: "BK-2plus"                       # was: "Direkter Zweikampf"
        serienspiel: "BK-2"                                   # was: "Serienspiel"
      first_set_mode:
        # Note: Plan 05 (D-18) replaces button_* keys with interpolated player names.
        # Here we only update the generic labels in case they remain as fallback.
        button_direkter_zweikampf_a: "Spieler A stößt an — BK-2plus"   # was: "… Direkter Zweikampf"
        button_serienspiel_a: "Spieler A stößt an — BK-2"              # was: "… Serienspiel"
        button_direkter_zweikampf_b: "Spieler B stößt an — BK-2plus"   # was: "… Direkter Zweikampf"
        button_serienspiel_b: "Spieler B stößt an — BK-2"              # was: "… Serienspiel"
```

If ANY sibling keys under `phase.direkter_zweikampf.*` or `phase.serienspiel.*` also contain the words "Direkter Zweikampf" / "Serienspiel" (e.g. a `description:` or `remaining_label:` using the phrases), apply the same rename. Do NOT rename structural phrases like "Stöße übrig" / "Aufnahmen übrig" — those are badge-reading labels, not phase display labels.

If any key named `shots_left_label` or `innings_left_label` has a value containing "Direkter Zweikampf" or "Serienspiel", update those too. Example grep before editing:
```bash
grep -n 'Direkter Zweikampf\|Serienspiel' config/locales/de.yml
```
Expected: shows ALL occurrences. Apply substitution only where the context is a USER-FACING display (phase chip, button label). NEVER touch YAML keys.

Step 2 — Open `config/locales/en.yml`. Apply parallel substitutions:

```yaml
en:
  table_monitor:
    bk2_kombi:
      # Display-label invariant (Phase 38.4 I2): see de.yml for explanation.
      # Display values: direkter_zweikampf ↔ "BK-2plus", serienspiel ↔ "BK-2".

      phase:
        direkter_zweikampf:
          label: "BK-2plus"                                 # was: "Direct duel"
        serienspiel:
          label: "BK-2"                                      # was: "Series play"
      phase_chip:
        direkter_zweikampf: "BK-2plus"                       # was: "Direct duel"
        serienspiel: "BK-2"                                   # was: "Series play"
      first_set_mode:
        button_direkter_zweikampf_a: "Player A breaks — BK-2plus"
        button_serienspiel_a: "Player A breaks — BK-2"
        button_direkter_zweikampf_b: "Player B breaks — BK-2plus"
        button_serienspiel_b: "Player B breaks — BK-2"
```

Again — grep for `Direct duel\|Series play` to find any sibling occurrences and update them. Stop at user-facing display values only.

Step 3 — CRITICAL GUARD. After edits, verify internal mode keys have NOT leaked into non-YAML code:

```bash
# Must find ZERO occurrences of "bk_2plus" / "bk_2" used as INTERNAL keys in production code
grep -rn '"bk_2plus"\|"bk_2"' app/services/bk2_kombi/ app/services/bk2/ app/models/ app/reflexes/
# Expected: empty, OR only in Plan 04 code (not yet written at this point)
```

```bash
# Must find zero renames of internal mode keys in YAML paths
grep -n 'bk_2plus:\|bk_2:' config/locales/de.yml config/locales/en.yml
# Expected: empty — YAML keys stay `direkter_zweikampf:` / `serienspiel:`
```

Step 4 — Run both locales' YAML parsers to catch syntax errors:
```bash
bundle exec i18n-tasks check-consistent-interpolations 2>/dev/null || true  # informational
bin/rails runner "puts YAML.load_file('config/locales/de.yml').keys; puts YAML.load_file('config/locales/en.yml').keys"
```
Must not raise. Keys should include `:de` / `:en`.

Step 5 — Boot the app briefly and render a BK2-Kombi scoreboard page (if a fixture is available) OR run:
```bash
bin/rails runner 'puts I18n.t("table_monitor.bk2_kombi.phase.direkter_zweikampf.label", locale: :de)'
# Expected: BK-2plus
bin/rails runner 'puts I18n.t("table_monitor.bk2_kombi.phase.serienspiel.label", locale: :de)'
# Expected: BK-2
bin/rails runner 'puts I18n.t("table_monitor.bk2_kombi.phase.direkter_zweikampf.label", locale: :en)'
# Expected: BK-2plus
```

Step 6 — Test regression guard. Existing tests may assert on literal strings "Direkter Zweikampf" / "Serienspiel" in system tests.
```bash
grep -rn '"Direkter Zweikampf"\|"Serienspiel"\|"Direct duel"\|"Series play"' test/ | grep -v CHANGELOG
```
If any tests assert on the OLD strings, update them to assert on "BK-2plus" / "BK-2" within THIS plan (single-line string swaps). Do NOT restructure tests — that's Plan 06 scope.

Any test assertions affected must be noted in the plan's SUMMARY as "touched by I2 rename: N test files".
  </action>
  <verify>
    <automated>bin/rails runner 'puts I18n.t("table_monitor.bk2_kombi.phase.direkter_zweikampf.label", locale: :de)' | grep -q "BK-2plus"</automated>
    <automated>bin/rails runner 'puts I18n.t("table_monitor.bk2_kombi.phase.serienspiel.label", locale: :de)' | grep -q "BK-2"</automated>
    <automated>bin/rails runner 'puts I18n.t("table_monitor.bk2_kombi.phase.direkter_zweikampf.label", locale: :en)' | grep -q "BK-2plus"</automated>
    <automated>bin/rails runner 'puts I18n.t("table_monitor.bk2_kombi.phase.serienspiel.label", locale: :en)' | grep -q "BK-2"</automated>
    <automated>! grep -n 'bk_2plus:\|bk_2:' config/locales/de.yml config/locales/en.yml</automated>
    <automated>bin/rails test test/system/bk2_kombi_scoreboard_test.rb 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `I18n.t("table_monitor.bk2_kombi.phase.direkter_zweikampf.label", locale: :de)` returns exactly `"BK-2plus"`.
    - `I18n.t("table_monitor.bk2_kombi.phase.serienspiel.label", locale: :de)` returns exactly `"BK-2"`.
    - `I18n.t("table_monitor.bk2_kombi.phase.direkter_zweikampf.label", locale: :en)` returns exactly `"BK-2plus"`.
    - `I18n.t("table_monitor.bk2_kombi.phase.serienspiel.label", locale: :en)` returns exactly `"BK-2"`.
    - `grep -rn '"direkter_zweikampf"' app/services/bk2/ app/services/bk2_kombi/ 2>/dev/null | wc -l` >= 1 (the internal key string must appear in at least one location — either namespace, depending on whether Plan 05's git mv has run — confirming the internal key was NOT renamed).
    - `grep -n 'bk_2plus:\|bk_2:' config/locales/de.yml config/locales/en.yml` returns ZERO (i18n key paths unchanged).
    - YAML files load without parse errors (`YAML.load_file`).
    - Display-invariant code-comment added at the top of both `de.yml`/`en.yml` `table_monitor.bk2_kombi:` subtrees.
    - Any test asserting on old strings updated to new strings OR noted for Plan 06 rewrite.
  </acceptance_criteria>
  <done>
    I2 closed at the i18n surface. UI labels display "BK-2plus" / "BK-2"; internal `direkter_zweikampf` / `serienspiel` keys survive unchanged. Plan 04 and Plan 05 can proceed on top of the renamed labels without further churn.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n YAML → view render | Static translation lookup; no user input. |
| bk2_state JSON (DB) → service read | Internal keys UNCHANGED; JSON round-trip unaffected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38.4-03-01 | T (Tampering internal contracts) | bk2_state["current_phase"] JSON value | mitigate | Hard rule: only YAML VALUES change; YAML key PATHS and internal code references stay. Enforced by acceptance criteria grep: `grep -n 'bk_2plus:' config/locales/` must return 0. Code-comment invariant at top of each changed YAML subtree documents the rule for future readers. |
| T-38.4-03-02 | I (Info disclosure) | i18n static values | accept | No user data in YAML values. |
</threat_model>

<verification>
- All 4 i18n lookups return "BK-2plus" / "BK-2" as specified.
- No internal keys renamed.
- YAML parses cleanly.
- `bin/rails test test/system/bk2_kombi_scoreboard_test.rb` either passes OR the string-literal assertions are updated inline (documented in SUMMARY).
</verification>

<success_criteria>
- Pure i18n-value rename: zero ERB changes, zero Ruby changes, zero reflex changes in this plan.
- Clean Wave 2 commit independent of Plans 01/02 (Wave 1) and Plans 04+ (Wave 3+).
- Code-comment invariant is in place so future readers understand the display-vs-internal split.
</success_criteria>

<output>
After completion, create `.planning/phases/38.4-bk2-kombi-post-dry-run-gaps/38.4-03-SUMMARY.md` documenting:
- I2 closed at i18n surface
- Exact list of value substitutions applied (DE + EN)
- Confirmation: zero internal-key renames
- Any test-assertion string updates included inline
- Guard-comment added to YAML
</output>
