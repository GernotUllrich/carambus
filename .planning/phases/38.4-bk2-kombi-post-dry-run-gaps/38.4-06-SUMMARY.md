---
phase: 38.4-bk2-kombi-post-dry-run-gaps
plan: "06"
subsystem: bk2-ui
tags: [bk2-family, detail-view, shootout, alpine, conditional-inputs, D-16, D-17, D-18, D-19]
dependency_graph:
  requires: ["38.4-03", "38.4-04", "38.4-05"]
  provides: ["D-16-detail-view-conditional-inputs", "D-17-bk2kombi-4btn-only", "D-18-player-names", "D-19-grid-resort", "is_bk2_kombi-predicate"]
  affects: ["app/views/locations/scoreboard_free_game_karambol_new.html.erb", "app/views/table_monitors/_shootout.html.erb", "app/views/table_monitors/_scoreboard.html.erb"]
tech_stack:
  added: []
  patterns:
    - "Alpine computed getters (is_bk_family, is_bk_fixed_goal, is_bk_dz_configurable, is_bk_sp_configurable, bk_ballziel_choices) in single x-data scope"
    - "is_bk2_kombi narrower predicate alongside Plan 05's 5-family is_bk2 for semantic split in scoreboard views"
    - "bk_selected_form radio selector drives free_game_form + balls_goal + discipline hidden inputs for all 5 BK-* forms"
key_files:
  created: []
  modified:
    - app/views/locations/scoreboard_free_game_karambol_new.html.erb
    - app/views/table_monitors/_shootout.html.erb
    - app/views/table_monitors/_scoreboard.html.erb
decisions:
  - "D-16: DZ-max input visible only for BK-2plus + BK-2kombi; SP-max visible only for BK-2 + BK-2kombi — implemented via is_bk_dz_configurable / is_bk_sp_configurable Alpine getters"
  - "D-07: Ballziel driven by bk_ballziel_choices getter; BK50/BK100 show read-only display; BK-2/BK-2plus/BK-2kombi show selectable dropdown"
  - "D-17: 4-button mode selector gated on is_bk2 == 'bk2_kombi' (single-value, unchanged); all other disciplines use Karambol-generic 2-button path"
  - "D-18: all 4 BK-2kombi shootout buttons use left_player[:fullname] / right_player[:fullname]"
  - "D-19: 4-button grid re-sorted to cols=player (A=start_game, B=switch+start), rows=mode (BK-2plus/amber top, BK-2/sky bottom)"
  - "is_bk2_kombi semantic split: phase-chip + bk2-kombi-scoreboard CSS hook narrowed to is_bk2_kombi; GD/HS hide + score display remain on is_bk2 (5-family) — Plan 05 predicate untouched"
  - "_player_score_panel.html.erb has zero changes — all its is_bk2 usages are correct 5-family semantics; no BK-2kombi-only narrowing needed there"
metrics:
  duration: ~7min
  completed: "2026-04-25"
  tasks_completed: 3
  files_changed: 3
---

# Phase 38.4 Plan 06: UI Polish — Detail-view Conditional Inputs + Shootout Grid Re-sort Summary

Wave 4 UI polish pass: replaced the hardcoded BK2-Kombi 50/60/70 radio selector with a 5-discipline Alpine-driven detail view; re-sorted the BK-2kombi shootout grid to cols=player/rows=mode with real player names; introduced `is_bk2_kombi` narrower predicate alongside Plan 05's 5-family `is_bk2` for BK-2kombi-only phase-chip semantics.

## Tasks Completed

| Task | Name | Commit | Key Files |
|------|------|--------|-----------|
| 1 | Detail-view D-16/D-07 — Ballziel dropdown + conditional DZ/SP inputs | 76b05a4c | app/views/locations/scoreboard_free_game_karambol_new.html.erb |
| 2 | Shootout D-17/D-18/D-19 — 4-btn BK-2kombi only, player names, grid re-sort | d74b2b2b | app/views/table_monitors/_shootout.html.erb |
| 3 | is_bk2_kombi narrower predicate for phase-chip in _scoreboard.html.erb | c215c8a1 | app/views/table_monitors/_scoreboard.html.erb |

## What Was Built

### Task 1: Detail-view restructure (D-16 + D-07)

**`scoreboard_free_game_karambol_new.html.erb`** — GAP-02 Alpine scope preserved (single `x-data=` wrapper, verified by grep count = 1).

**New Alpine state slots** added to the existing `x-data` object:
- `bk_selected_form: null` — tracks which of 5 BK-* disciplines is selected
- `bk_balls_goal: null` — tracks selected Ballziel value
- `is_bk_family` getter — true for any of the 5 BK-* forms
- `is_bk_fixed_goal` getter — true for BK50/BK100 (single fixed Ballziel)
- `is_bk_dz_configurable` getter — true for BK-2plus + BK-2kombi (D-16 DZ-max visibility)
- `is_bk_sp_configurable` getter — true for BK-2 + BK-2kombi (D-16 SP-max visibility)
- `bk_ballziel_choices` getter — returns the correct choices array for the selected form

**5-radio BK-family discipline selector** replaces the old `bk2_choice` 50/60/70 radio:
- Values: `bk50`, `bk100`, `bk_2`, `bk_2plus`, `bk2_kombi`
- Each radio sets `bk_selected_form` via `x-model` and resets `bk_balls_goal` via `@change`

**Ballziel UI** (D-07):
- BK50/BK100: read-only display showing the fixed value
- BK-2/BK-2plus/BK-2kombi: selectable `<select>` dropdown populated from `bk_ballziel_choices`

**Conditional DZ-max / SP-max inputs** (D-16):
- DZ-max (`bk2_dz_max_shots`): `x-show="is_bk_dz_configurable"` — visible for BK-2plus + BK-2kombi only
- SP-max (`bk2_sp_max_innings`): `x-show="is_bk_sp_configurable"` — visible for BK-2 + BK-2kombi only

**Hidden inputs updated**:
- `name='free_game_form'`: driven by `bk_selected_form` (5 BK-* forms) with fallback to `bk2_selected` (legacy) or `karambol`
- `name='balls_goal'`: replaces `set_target_points` (D-06); driven by `bk_balls_goal`
- `name='discipline_a'` / `name='discipline_b'`: map to 5 BK-* discipline name strings
- `name='sets_to_win'` / `name='sets_to_play'`: BK-2kombi → 2/3; others → 1/1
- `name='bk2_options[...]'` inputs: retained for DZ-max and SP-max

**`set_target_points` functional references**: zero (only in comments).

### Task 2: Shootout partial (D-17 + D-18 + D-19)

**`_shootout.html.erb`** — 4-button block replaced with D-19-sorted layout.

**D-17**: `is_bk2 = options[:free_game_form] == "bk2_kombi"` preserved as single-value check (unchanged). All other BK-* and non-BK disciplines use the existing 2-button Karambol-generic path (unchanged).

**D-19 grid re-sort**: `grid grid-cols-2` with:
- Row 1 (BK-2plus / amber): `[A stößt an — BK-2plus]` | `[B stößt an — BK-2plus]`
- Row 2 (BK-2 / sky): `[A stößt an — BK-2]` | `[B stößt an — BK-2]`

(Prior layout from 38.3-05 was cols=mode / rows=player — now cols=player / rows=mode.)

**D-18 player names**: all 4 buttons interpolate `left_player[:fullname]` / `right_player[:fullname]` from the existing locals (lines 1-28). The 2-button generic path already used player names — no change needed there.

**Reflex payloads** (unchanged from 38.3-05):
- Col 1 Row 1: `TableMonitorReflex#start_game` + `bk2_first_set_mode: "direkter_zweikampf"` + `first_break_choice: "1"`
- Col 2 Row 1: `TableMonitorReflex#switch_players_and_start_game` + `bk2_first_set_mode: "direkter_zweikampf"` + `first_break_choice: "2"`
- Col 1 Row 2: `TableMonitorReflex#start_game` + `bk2_first_set_mode: "serienspiel"` + `first_break_choice: "1"`
- Col 2 Row 2: `TableMonitorReflex#switch_players_and_start_game` + `bk2_first_set_mode: "serienspiel"` + `first_break_choice: "2"`

**i18n labels**: button labels use `t("table_monitor.bk2_kombi.phase.direkter_zweikampf")` and `t("table_monitor.bk2_kombi.phase.serienspiel")` — these were changed to "BK-2plus" and "BK-2" in Plan 03 (Wave 2). The interpolated format is `"#{player_name} stößt an — #{phase_label}"`.

### Task 3: is_bk2_kombi narrower predicate (scoreboard)

**`_scoreboard.html.erb`**:
- Added `is_bk2_kombi = options[:free_game_form] == "bk2_kombi"` immediately after the Plan 05 5-family `is_bk2` declaration
- Phase-chip block guard changed from `if is_bk2` → `if is_bk2_kombi` — BK50/BK100/BK-2/BK-2plus now fall through to the innings-counter `else` branch (correct: they have no phase chip)
- `bk2-kombi-scoreboard` CSS class (stable system-test selector hook) also narrowed to `is_bk2_kombi`
- Plan 05's 5-family `is_bk2` predicate unchanged (still present at line 53)

**`_player_score_panel.html.erb`**: zero changes. All `is_bk2` usages in that file are correct 5-family semantics (BK inning running score, bk2_set_target goal display, GD/HS hide, main score display). No BK-2kombi-only narrowing is needed there.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - ERBlint compliance] autocomplete attribute on DZ-max / SP-max number inputs**
- **Found during:** Task 1 ERBlint run
- **Issue:** ERBlint's `RequireInputAutocomplete` linter flagged the two new `<input type="number">` tags for missing `autocomplete` attribute.
- **Fix:** Added `autocomplete="off"` to both `bk2_dz_max_shots` and `bk2_sp_max_innings` number inputs.
- **Files modified:** app/views/locations/scoreboard_free_game_karambol_new.html.erb
- **Commit:** 76b05a4c

**2. [Pre-existing, out of scope] Alpine @change attribute ERBlint parse errors**
- Two new `@change` attributes introduce the same ERBlint "Non-whitespace @" error as the pre-existing `@click` attributes already in the file (47→49 total errors, 2 new).
- The `@change` shorthand is Alpine's standard event shorthand; the file already uses `@click` throughout. No fix applied — same pre-existing pattern.
- **Commit:** N/A (out of scope)

**3. [Scope clarification] shootout uses `content_tag "div"` not `<button>` elements**
- The plan's prescribed code used `<button>` HTML elements. The existing 4-button block (from 38.3-05) used `content_tag "div"` with StimulusReflex `data-reflex` attributes, which is the established pattern in this codebase. Kept `content_tag "div"` for consistency — same reflex payload, same visual result, ERBlint clean.

## Alpine Scope Invariant (GAP-02)

`grep -c 'x-data=' app/views/locations/scoreboard_free_game_karambol_new.html.erb` = **1**

The single `x-data` wrapper from Phase 38.2-02 is preserved. All new Alpine state slots (`bk_selected_form`, `bk_balls_goal`, computed getters) are added inside the same wrapper. No second `x-data` scope introduced.

## Predicate Semantic Split Summary

| Predicate | Scope | File | Purpose |
|-----------|-------|------|---------|
| `is_bk2` | 5-family (bk2_kombi/bk50/bk100/bk_2/bk_2plus) | _scoreboard.html.erb, _player_score_panel.html.erb | BK-family state setup, GD/HS hide, score display |
| `is_bk2_kombi` | Single-value (bk2_kombi only) | _scoreboard.html.erb | Phase chip, remaining badge, bk2-kombi-scoreboard CSS hook |
| `is_bk2` (shootout) | Single-value (bk2_kombi only — unchanged) | _shootout.html.erb | 4-button mode selector gating (D-17) |

## Known Stubs

None. All 5 BK-* forms are wired end-to-end through the detail view. The `bk_ballziel_choices` Alpine getter uses hardcoded arrays that match `Discipline#ballziel_choices` (Plan 04) — these are the same D-07 canonical values; no server roundtrip needed for the Alpine UI.

## Threat Flags

None. No new network endpoints, auth paths, or schema changes. The Alpine hidden inputs post to the existing `start_game` controller action whose CLAMP (Plan 04) already validates `balls_goal` and `bk2_options` server-side.

## Self-Check: PASSED
