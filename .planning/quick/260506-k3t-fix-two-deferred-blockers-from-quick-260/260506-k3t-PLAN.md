---
quick_id: 260506-k3t
type: execute
mode: quick-full
wave: 1
depends_on: []
autonomous: true
files_modified:
  - app/controllers/tournaments_controller.rb
  - app/views/tournaments/tournament_monitor.html.erb
  - test/integration/tournament_verification_payload_serialization_test.rb
  - test/fixtures/tournaments.yml
requirements:
  - DEFERRED-BLOCKER-1
  - DEFERRED-BLOCKER-2
must_haves:
  truths:
    - "build_verification_failure_payload returns a Hash with STRING keys ('failures', 'body_text') so the cookie-session JSON round-trip is a no-op"
    - "app/views/tournaments/tournament_monitor.html.erb:66 reads @verification_failure['body_text'] (string key) so the modal body is rendered correctly under the JSON cookie session serializer used in production / staging / test"
    - "A regression-guard Minitest integration test exists at test/integration/tournament_verification_payload_serialization_test.rb that builds a payload via the controller helper, JSON round-trips it, and asserts both 'body_text' and 'failures' are accessible — protecting against future symbol-key regressions even without running the full system suite"
    - "tournaments(:local) fixture state is 'tournament_mode_defined' — the AASM event start_tournament! transitions from %i[tournament_started tournament_mode_defined tournament_started_waiting_for_monitors] (app/models/tournament.rb:290-292), and tournament_mode_defined is the only one of those that satisfies the test setup gate at tournament_parameter_verification_test.rb:29 (which excludes tournament_started)."
    - "All 4 tests in test/system/tournament_parameter_verification_test.rb (36B-06) pass with 0 failures, 0 errors, 0 skips"
  artifacts:
    - path: "app/controllers/tournaments_controller.rb"
      provides: "build_verification_failure_payload returning string-keyed Hash"
      contains: "\"body_text\" =>"
    - path: "app/views/tournaments/tournament_monitor.html.erb"
      provides: "auto_open_body bound to @verification_failure['body_text'] (string key)"
      contains: "@verification_failure[\"body_text\"]"
    - path: "test/integration/tournament_verification_payload_serialization_test.rb"
      provides: "Regression-guard for the payload JSON round-trip contract"
      contains: "JSON.parse(JSON.dump"
    - path: "test/fixtures/tournaments.yml"
      provides: ":local fixture in a valid AASM pre-start state"
      contains: "state: \"tournament_mode_defined\""
  key_links:
    - from: "TournamentsController#start (line 327)"
      to: "build_verification_failure_payload (line 1028)"
      via: "flash[:verification_failure] assignment, then redirect_to tournament_monitor_tournament_path"
      pattern: "flash\\[:verification_failure\\] = build_verification_failure_payload"
    - from: "Cookie session JSON serializer (Rails 7.2 default)"
      to: "TournamentsController#tournament_monitor (line 284) reading flash[:verification_failure]"
      via: "JSON-stringified hash → @verification_failure"
      pattern: "@verification_failure = flash\\[:verification_failure\\]"
    - from: "tournament_monitor.html.erb:66"
      to: "shared/confirmation_modal partial"
      via: "auto_open_body local"
      pattern: "auto_open_body: @verification_failure\\[\"body_text\"\\]"
    - from: "TournamentParameterVerificationTest setup gate"
      to: "@tournament.start_tournament! (Tests 3 + 4)"
      via: "AASM event start_tournament! transitioning from a valid state"
      pattern: "start_tournament!"
---

<objective>
Fix the two DEFERRED-BLOCKERs from quick-260506-i6h's verification report so 36B-06
goes 4/4 green and the PRG refactor stack (commit 0ac7305a + downstream) is safe to push.

Bug 1 (production-affecting, 1 file in app/ + 1 file in app/views/): the PRG flash payload
uses symbol keys but Rails 7.2's default cookie session uses the :json serializer, which
stringifies symbol keys on the round-trip. View access via @verification_failure[:body_text]
returns nil → modal renders with empty body. Fix per LOCKED 1: write string keys at the
controller source, read string keys in the view.

Bug 2 (test-only, 1 fixture line): tournaments(:local).state == "registration" is not a
defined AASM state on Tournament. Tests 3 + 4 of 36B-06 call start_tournament! which raises
AASM::InvalidTransition (no transition from "registration"). Fix per the orchestrator-verified
override of LOCKED 3: change ONLY :local fixture's state to "tournament_mode_defined". The
AASM event start_tournament! transitions from %i[tournament_started tournament_mode_defined
tournament_started_waiting_for_monitors] (app/models/tournament.rb:290-292), and the test
setup gate at tournament_parameter_verification_test.rb:29 excludes tournament_started, so
tournament_mode_defined is the only viable pre-start state. Do not touch the other 4 fixtures
using state: "registration" (they may be referenced by tests that don't invoke AASM transitions).

Regression guard per LOCKED 2: a small Minitest integration test that builds the payload,
JSON-round-trips it, and asserts the round-tripped hash still has accessible body_text +
failures. Cheap insurance against a future contributor reverting back to symbol keys without
running the full system suite.

Purpose: Unblock the linear stack of 5 unpushed bcw commits so the user can push with
confidence after a final 4/4 green on the system test.

Output: 2 controller/view edits (Bug 1 fix), 1 new integration test (regression guard),
1 fixture edit (Bug 2 fix), one verification run.
</objective>

<execution_context>
@/Users/gullrich/DEV/carambus/carambus_bcw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gullrich/DEV/carambus/carambus_bcw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
# Project state and full bug analysis (read these in order):
@.planning/STATE.md
@.planning/quick/260506-i6h-fix-tournament-discipline-test-fixtures-/260506-i6h-VERIFICATION.md
@.planning/quick/260506-i6h-fix-tournament-discipline-test-fixtures-/260506-i6h-SUMMARY.md
@CLAUDE.md
@.agents/skills/scenario-management/SKILL.md

# Files this plan touches (read FIRST before editing):
@app/controllers/tournaments_controller.rb
@app/views/tournaments/tournament_monitor.html.erb
@app/models/tournament.rb
@test/fixtures/tournaments.yml
@test/system/tournament_parameter_verification_test.rb

# Environment config — confirms Bug 1 manifests in test, staging, production:
@config/environments/development.rb
@config/environments/test.rb

<interfaces>
<!-- Key contracts the executor needs. Extracted from the controller/view/model. -->
<!-- Use these directly — no codebase exploration needed. -->

From app/controllers/tournaments_controller.rb (current state, BUG):
```ruby
# Line 1028-1040 (CURRENT, BUGGY — symbol keys):
def build_verification_failure_payload(failures)
  body_lines = failures.map do |f|
    "#{f[:label]} = #{f[:value]} (üblich: #{f[:range].first}-#{f[:range].last})"
  end
  body_intro = I18n.t(
    "tournaments.monitor_form.verification.body_intro",
    default: "Die folgenden Werte liegen außerhalb des üblichen Bereichs für diese Disziplin. Bitte prüfen und bestätigen, wenn sie wirklich gewollt sind:"
  )
  {
    failures: failures,
    body_text: body_intro + "\n\n" + body_lines.join("\n")
  }
end

# Line 327 (call site — UNCHANGED in this task):
flash[:verification_failure] = build_verification_failure_payload(failures)

# Line 284 (read site in #tournament_monitor — UNCHANGED in this task):
@verification_failure = flash[:verification_failure]
```

From app/views/tournaments/tournament_monitor.html.erb (current state, BUG):
```erb
<!-- Line 55-71 (only line 66 changes; surrounding context for orientation) -->
<% if @verification_failure.present? %>
  <div class="mb-4 p-3 border border-orange-300 bg-orange-50 rounded">
    <p class="text-sm text-orange-900 font-semibold mb-2">
      <%= I18n.t("tournaments.monitor_form.verification.banner", default: "...") %>
    </p>
  </div>

  <%= render "shared/confirmation_modal",
             auto_open: true,
             auto_open_title: I18n.t("tournaments.monitor_form.verification.title", default: "..."),
             auto_open_body: @verification_failure[:body_text],   <!-- LINE 66, BUG: symbol key -->
             auto_open_confirm_label: I18n.t("tournaments.monitor_form.verification.confirm", default: "..."),
             auto_open_form_id: "start_tournament",
             hidden_override_name: "parameter_verification_confirmed",
             hidden_override_reset_on_cancel: true %>
<% end %>
```

From app/models/tournament.rb:271-281 (AASM state list — for Bug 2 reference):
```ruby
aasm column: "state", skip_validation_on_save: true do
  state :new_tournament, initial: true, after_enter: [:reset_tournament]
  state :accreditation_finished
  state :tournament_seeding_finished, after_enter: [:calculate_and_cache_rankings]
  state :tournament_mode_defined
  state :tournament_started_waiting_for_monitors
  state :tournament_started
  state :tournament_finished
  state :results_published
  state :closed
  # ...
end
```

From app/models/tournament.rb:290-292 (start_tournament! AASM event — load-bearing for Bug 2):
```ruby
event :start_tournament! do
  transitions from: %i[tournament_started tournament_mode_defined tournament_started_waiting_for_monitors],
              to: :tournament_started_waiting_for_monitors
end
```

"registration" is NOT in the state list. tournament_mode_defined is the only from-state of
start_tournament! that satisfies the test setup gate at
test/system/tournament_parameter_verification_test.rb:29 (which excludes tournament_started).

From test/fixtures/tournaments.yml (current state, BUG at line 63):
```yaml
local:
  id: 50_000_001
  # ...
  state: "registration"   # ← BUG: not an AASM state
  # ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Bug 1 — string keys at source (controller payload + view access)</name>
  <files>
    app/controllers/tournaments_controller.rb
    app/views/tournaments/tournament_monitor.html.erb
  </files>
  <action>
    Honor LOCKED 1 + LOCKED 5 (scope hygiene): two surgical edits, nothing else.

    EDIT 1 — app/controllers/tournaments_controller.rb at lines 1036-1039 (inside
    build_verification_failure_payload). Change the returned hash literal from symbol keys
    to STRING keys. Do NOT touch the body_lines / body_intro logic above; do NOT touch the
    failures.map block (failures themselves remain symbol-keyed Hashes — they're consumed
    only inside this method, never round-tripped through the cookie session).

      Before:
        {
          failures: failures,
          body_text: body_intro + "\n\n" + body_lines.join("\n")
        }
      After:
        # JSON cookie-session contract: string keys are the wire format. Symbol keys
        # would survive Marshal (development.rb redis_session_store) but become strings
        # under Rails 7.2's default :json serializer (test.rb / staging.rb / production-*.rb).
        # Writing strings here makes the round-trip a no-op and prevents nil reads in the view.
        # See quick-260506-k3t for the bug history.
        {
          "failures" => failures,
          "body_text" => body_intro + "\n\n" + body_lines.join("\n")
        }

    EDIT 2 — app/views/tournaments/tournament_monitor.html.erb at line 66. Change the
    bracket access from symbol to string. Surrounding ERB lines untouched.

      Before:
        auto_open_body: @verification_failure[:body_text],
      After:
        auto_open_body: @verification_failure["body_text"],

    DO NOT modify:
      - the redirect_to flow at line 327-328
      - the read site at line 284
      - the form_tag at line 72 or anything in the modal partial
      - the failures.map block (line 1029-1031) — internal, never round-tripped

    German comments per CLAUDE.md ("German comments for business logic, English for technical
    terms") — but a brief inline comment explaining "JSON cookie-session contract" is on the
    technical side; keep it short. Frozen string literal pragma already at top of controller.
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_bcw &amp;&amp; grep -n '"body_text"' app/controllers/tournaments_controller.rb &amp;&amp; grep -n '"failures"' app/controllers/tournaments_controller.rb &amp;&amp; grep -n '@verification_failure\["body_text"\]' app/views/tournaments/tournament_monitor.html.erb &amp;&amp; ! grep -nE '^\s*body_text:\s' app/controllers/tournaments_controller.rb &amp;&amp; ! grep -nF '@verification_failure[:body_text]' app/views/tournaments/tournament_monitor.html.erb</automated>
  </verify>
  <done>
    Controller's build_verification_failure_payload returns a Hash literal with string keys
    "failures" and "body_text". The view reads @verification_failure["body_text"]. No symbol
    forms remain in either file. Code compiles (no Ruby/ERB syntax errors).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression-guard integration test for JSON round-trip</name>
  <files>
    test/integration/tournament_verification_payload_serialization_test.rb
  </files>
  <behavior>
    - Test 1: After build_verification_failure_payload returns a Hash, JSON.parse(JSON.dump(payload))
      produces a Hash where ['body_text'] is a non-empty String AND ['failures'] is an Array.
      This is the regression guard for the symbol-key class of bug — if a future contributor
      re-introduces symbol keys, this test fails fast without needing the full Selenium suite.
    - Test 2 (optional, narrow): The body_text contains the discipline range hint format
      (e.g. "üblich: " substring) so we know the I18n body_intro path was exercised — keeps
      the test honest about what build_verification_failure_payload actually does, not just
      what shape it returns.

    Construction strategy: instantiate TournamentsController, call .send(:build_verification_failure_payload, fake_failures)
    where fake_failures is a hand-built array of Hash with the exact shape the real call site
    produces (failures.map symbol keys :label, :value, :range — see the failures.map block at
    line 1029). This avoids needing a real fixture / DB hit / route exercise. Pure-Ruby unit
    of the helper plus a JSON round-trip.

    Files: this is a NEW file under test/integration/ (alongside bk_param_latent_bugs_test.rb,
    tiebreak_modal_form_wiring_test.rb, users_test.rb). Use ActionDispatch::IntegrationTest as
    parent class to match existing convention. require "test_helper" at top. frozen_string_literal: true.
  </behavior>
  <action>
    Honor LOCKED 2 (Minitest, integration test, NOT system test, 1-2 test blocks tightly focused
    on the JSON-round-trip invariant). The point is regression-guard for the symbol-key class
    of bug, not full E2E.

    Create test/integration/tournament_verification_payload_serialization_test.rb with this
    exact structure:

      # frozen_string_literal: true
      require "test_helper"

      # Regression guard for quick-260506-k3t Bug 1: the PRG flash payload returned by
      # TournamentsController#build_verification_failure_payload (line 1028) is round-tripped
      # through the cookie session, which uses the :json serializer in Rails 7.2 by default
      # (test.rb / staging.rb / production-*.rb — only development.rb sets redis_session_store).
      # If the helper returns symbol keys, JSON serialization stringifies them and view-side
      # access via @verification_failure[:body_text] returns nil → empty modal body in production.
      #
      # This test pins the wire-format contract: build_verification_failure_payload MUST return
      # a Hash whose body_text and failures keys survive a JSON round-trip with string-key access.
      class TournamentVerificationPayloadSerializationTest < ActionDispatch::IntegrationTest
        test "build_verification_failure_payload returns a hash whose keys survive a JSON round-trip" do
          controller = TournamentsController.new
          fake_failures = [
            { label: "Bälle-Ziel", value: 99999, range: 10..80 },
            { label: "Innings", value: 0, range: 1..100 },
          ]
          payload = controller.send(:build_verification_failure_payload, fake_failures)

          # Simulate the cookie-session JSON serializer round-trip exactly.
          round_tripped = JSON.parse(JSON.dump(payload))

          assert_kind_of String, round_tripped["body_text"], "body_text must be readable via string key after JSON round-trip"
          assert_predicate round_tripped["body_text"], :present?, "body_text must not be empty after JSON round-trip"
          assert_kind_of Array, round_tripped["failures"], "failures must be readable via string key after JSON round-trip"
          assert_equal 2, round_tripped["failures"].length, "failures must round-trip with the same element count"
        end

        test "build_verification_failure_payload body_text includes the discipline range hint" do
          controller = TournamentsController.new
          fake_failures = [{ label: "Bälle-Ziel", value: 99999, range: 10..80 }]
          payload = controller.send(:build_verification_failure_payload, fake_failures)
          round_tripped = JSON.parse(JSON.dump(payload))

          # The hint format ("üblich: 10-80") is what the operator sees in the modal — proves
          # we exercised the body_intro + body_lines path, not just the empty hash shape.
          assert_match(/üblich:\s*10-80/, round_tripped["body_text"])
        end
      end

    Notes for executor:
      - DO NOT seed a Tournament fixture — this is a pure helper-call test.
      - DO NOT use post / get / follow_redirect! — those exercise routing, not what we're testing.
      - DO NOT touch tournaments_controller_test.rb (controller test); the new file lives in
        test/integration/ as locked.
      - If frozen_string_literal causes an issue with the Range literal (it doesn't, ranges
        are objects), no workaround needed.
      - Use .send(:build_verification_failure_payload, ...) since the method is private.
        This is acceptable in tests (existing pattern: e.g. ResultRecorder unit tests).
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_bcw &amp;&amp; bin/rails test test/integration/tournament_verification_payload_serialization_test.rb 2>&amp;1 | tee /tmp/k3t-task2.log &amp;&amp; grep -E "0 failures, 0 errors, 0 skips" /tmp/k3t-task2.log</automated>
  </verify>
  <done>
    test/integration/tournament_verification_payload_serialization_test.rb exists with 2 test blocks.
    bin/rails test on this file reports 2 runs / N assertions / 0 failures / 0 errors / 0 skips.
    The test would FAIL if Task 1's controller fix were reverted (i.e. if symbol keys came back) —
    this is the regression guard.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix Bug 2 — :local fixture state to "tournament_mode_defined"</name>
  <files>
    test/fixtures/tournaments.yml
  </files>
  <action>
    Orchestrator-verified override of original LOCKED 3: the correct value is
    "tournament_mode_defined", NOT "new_tournament". Justification (already independently
    confirmed by the orchestrator against app/models/tournament.rb:290-292):

      event :start_tournament! do
        transitions from: %i[tournament_started tournament_mode_defined tournament_started_waiting_for_monitors],
                    to: :tournament_started_waiting_for_monitors
      end

    Intersected with the test setup gate at test/system/tournament_parameter_verification_test.rb:29
    (which excludes tournament_started and the *_waiting_for_monitors variants from the
    pre-start fixture state), the only viable pre-start state for Tests 3 + 4 is
    `tournament_mode_defined`. "new_tournament" is the AASM initial state but is NOT in the
    from-list of start_tournament!, so leaving it would still cause AASM::InvalidTransition.

    Honor LOCKED 5 (scope hygiene): change ONLY :local. The other 4 fixtures using
    state: "registration" (wc_2024 line 17, wc_no_end_date line 34, imported line 76,
    scraped line 89) are out of scope — their callers don't invoke start_tournament! AASM,
    so the invalid state is harmless to those tests, and changing them broadens blast radius.

    SINGLE EDIT — test/fixtures/tournaments.yml inside the :local block (line ~50, the
    line that currently reads `state: "registration"`):

      Before:
        state: "registration"
      After:
        # AASM correctness (quick-260506-k3t): start_tournament! transitions from
        # %i[tournament_started tournament_mode_defined tournament_started_waiting_for_monitors]
        # per app/models/tournament.rb:290-292. Test setup gate at
        # tournament_parameter_verification_test.rb:29 excludes tournament_started, so
        # tournament_mode_defined is the only viable pre-start state for tests 3+4.
        # ("registration" was a stray label not in the AASM state list.)
        state: "tournament_mode_defined"

    DO NOT touch the other 4 fixtures using state: "registration" (LOCKED 5).
    DO NOT modify the FK rot history comment block from quick-260506-i6h (lines 46-54).
    DO NOT touch any other fixture line in the file.
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_bcw &amp;&amp; awk '/^local:/,/^[a-z]/' test/fixtures/tournaments.yml | grep -q 'state: "tournament_mode_defined"' &amp;&amp; ! awk '/^local:/,/^[a-z]/' test/fixtures/tournaments.yml | grep -q 'state: "registration"'</automated>
  </verify>
  <done>
    tournaments(:local) state is "tournament_mode_defined". Other 4 fixtures with
    state: "registration" are UNCHANGED. Test runs in Task 4 will validate empirically.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify 36B-06 system test goes 4/4 green</name>
  <files>
    (none — verification only)
  </files>
  <action>
    Honor LOCKED 7 (run only the targeted test files, do NOT run the full suite).

    Run the targeted system test:
      bin/rails test test/system/tournament_parameter_verification_test.rb

    Then run the new integration test (re-confirm Task 2):
      bin/rails test test/integration/tournament_verification_payload_serialization_test.rb

    Per LOCKED 4: the system test should report 4 runs / N assertions / 0 failures / 0 errors / 0 skips.

    EXECUTOR EXPECTATIONS BY TEST:
      - Test 1 ("out-of-range balls_goal opens verification modal") — was FAIL on i6h due
        to Bug 1 (modal body empty → assert_text "99999" failed). Should now PASS after Task 1.
      - Test 2 ("clicking Cancel keeps tournament un-started") — was already PASS. Should
        remain PASS.
      - Test 3 ("clicking Confirm starts the tournament with the override") — was FAIL on
        i6h due to Bug 1 (Confirm button click depends on modal body rendering correctly)
        AND Bug 2 (start_tournament! AASM transition). Should now PASS after Tasks 1 + 3.
      - Test 4 ("in-range values skip the modal and start the tournament directly") — was
        FAIL on i6h due to Bug 2 only (no modal, but start_tournament! still raises). Should
        now PASS after Task 3.

    INTERPRETING THE OUTPUT:
      - 4 runs / 0 failures / 0 errors / 0 skips → SUCCESS. Push stack ready.
      - 4 runs / >0 failures: surface the failure messages and stack traces in the SUMMARY.
      - 4 runs / >0 errors: read the stack trace; if it's a Capybara timeout or browser
        crash, retry once; if persistent, surface as system_test_caveat per CLAUDE.md.

    DO NOT run any other test file (no full suite, no regression sweep across other system
    tests). LOCKED 7: that's the verifier's job, not the executor's.

    If both verifies green: you are DONE. Skip the per-task commits handled by the orchestrator;
    this task is verification only — no files written.
  </action>
  <verify>
    <automated>cd /Users/gullrich/DEV/carambus/carambus_bcw &amp;&amp; bin/rails test test/system/tournament_parameter_verification_test.rb 2>&amp;1 | tee /tmp/k3t-36b06.log &amp;&amp; grep -E "4 runs.*0 failures.*0 errors.*0 skips" /tmp/k3t-36b06.log</automated>
  </verify>
  <done>
    bin/rails test test/system/tournament_parameter_verification_test.rb reports
    4 runs / N assertions / 0 failures / 0 errors / 0 skips.
    The new integration test reports 2 runs / 0 failures / 0 errors / 0 skips.
    Commit 0ac7305a's PRG refactor is now end-to-end correct; the linear stack of unpushed
    bcw commits (0ac7305a, 1de19400, 1c291731, 12652ae2, 57cc303c) plus the k3t commits
    is safe to push.
  </done>
</task>

</tasks>

<verification>
Targeted runs only (no full-suite regression sweep — that's the verifier's job per LOCKED 7):

1. Bug 1 surface: targeted system test passes 4/4 green
   bin/rails test test/system/tournament_parameter_verification_test.rb
   Expected: "4 runs, N assertions, 0 failures, 0 errors, 0 skips"

2. Bug 1 regression guard: new integration test passes
   bin/rails test test/integration/tournament_verification_payload_serialization_test.rb
   Expected: "2 runs, M assertions, 0 failures, 0 errors, 0 skips"

3. Bug 1 fix presence: string keys at source + view
   grep '"body_text"' app/controllers/tournaments_controller.rb         → match present
   grep '"failures"' app/controllers/tournaments_controller.rb           → match present
   grep '@verification_failure\["body_text"\]' app/views/tournaments/tournament_monitor.html.erb  → match present
   grep -nE '^\s*body_text:\s' app/controllers/tournaments_controller.rb → NO match (anchored, payload literal only)

4. Bug 2 fix presence: :local state is tournament_mode_defined
   awk '/^local:/,/^[a-z]/' test/fixtures/tournaments.yml | grep 'state:' → state: "tournament_mode_defined"
   (other 4 fixtures using state: "registration" remain UNCHANGED)
</verification>

<success_criteria>
1. Bug 1 fix shipped: build_verification_failure_payload returns string keys; view reads string keys.
2. Bug 1 regression guard: integration test exists, passes, would FAIL if symbol keys re-introduced.
3. Bug 2 fix shipped: tournaments(:local).state is "tournament_mode_defined" (orchestrator-verified
   override of original LOCKED 3 — narrowest scope, AASM-correct).
4. 4/4 green on test/system/tournament_parameter_verification_test.rb (LOCKED 4).
5. No other files modified (LOCKED 5 — scope hygiene): only the 4 listed in files_modified.
6. Commit 0ac7305a is now correct end-to-end → linear bcw stack safe to push (i.e. the linear
   stack of 5 unpushed bcw commits — 0ac7305a, 1de19400, 1c291731, 12652ae2, 57cc303c — plus
   the new commit(s) from k3t can be pushed with confidence).
</success_criteria>

<output>
After completion, create `.planning/quick/260506-k3t-fix-two-deferred-blockers-from-quick-260/260506-k3t-SUMMARY.md`
with:
  - The 4 tasks' status (DONE / BLOCKED-with-reason)
  - Final test counts for both targeted runs
  - Files modified (4 expected)
  - Confirmation that the 5 unpushed bcw commits + new k3t commits are safe to push
  - Any deviations from the plan
</output>
