---
phase: 260414-qb8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/tournaments/show.html.erb
  - app/views/tournaments/finalize_modus.html.erb
  - test/controllers/tournaments_controller_test.rb
autonomous: true
requirements:
  - QB8-01
must_haves:
  truths:
    - "tournaments#show renders without PG::UndefinedColumn for a local, not-yet-started, non-CC tournament"
    - "tournaments#finalize_modus no longer references a non-existent result_a column"
    - "Gespielte Spiele count reflects games with ended_at set (table monitor finished the game)"
  artifacts:
    - path: "app/views/tournaments/show.html.erb"
      provides: "reset-tournament modal body uses ended_at count"
      contains: "where.not(ended_at: nil)"
    - path: "app/views/tournaments/finalize_modus.html.erb"
      provides: "force-reset modal body uses ended_at count"
      contains: "where.not(ended_at: nil)"
    - path: "test/controllers/tournaments_controller_test.rb"
      provides: "regression test proving show renders in local, not-started, non-CC state"
      contains: "result_a"
  key_links:
    - from: "app/views/tournaments/show.html.erb"
      to: "games.ended_at"
      via: "where.not(ended_at: nil).count"
      pattern: "where\\.not\\(ended_at: nil\\)"
    - from: "app/views/tournaments/finalize_modus.html.erb"
      to: "games.ended_at"
      via: "where.not(ended_at: nil).count"
      pattern: "where\\.not\\(ended_at: nil\\)"
---

<objective>
Fix PG::UndefinedColumn: column games.result_a does not exist crash introduced by commit 872f92a3 in the reset-tournament and force-reset confirmation modals.

Purpose: Restore tournament show page for local, not-started tournaments — the page currently 500s the moment the reset modal body is precomputed, blocking volunteer tournament managers from opening any tournament that hasn't started yet.

Output: Two one-line view fixes plus a minimal controller regression test that renders tournaments#show in the exact gating state (local_server? && !has_clubcloud_results? && !tournament_started) where the bug fires.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/views/tournaments/show.html.erb
@app/views/tournaments/finalize_modus.html.erb
@db/schema.rb
@test/controllers/tournaments_controller_test.rb
@test/fixtures/tournaments.yml

<interfaces>
<!-- games table schema (db/schema.rb lines 315-336) — no result_a column exists -->
```
create_table "games" do |t|
  t.integer  "tournament_id"
  t.text     "roles"
  t.text     "data"
  t.integer  "seqno"
  t.string   "gname"
  t.integer  "group_no"
  t.integer  "table_no"
  t.integer  "round_no"
  t.datetime "started_at"
  t.datetime "ended_at"   # <-- THIS is the "game played" marker
  t.datetime "created_at"
  t.datetime "updated_at"
  t.string   "tournament_type"
  t.integer  "region_id"
  t.boolean  "global_context"
  t.string   "type"
end
```

<!-- Existing test fixture & scaffold already present in repo -->
- test/fixtures/tournaments.yml defines tournaments(:local) with id 50_000_001, state "registration" (not started)
- test/controllers/tournaments_controller_test.rb already sets up @tournament = tournaments(:local), signs in @user,
  and saves/restores Carambus.config.carambus_api_url per test (lines 23-38)
- Pattern for local-server mode: set Carambus.config.carambus_api_url = "http://local.test" inside the test body
- Existing tests already call `get tournament_url(@tournament)` and inspect response.status

<!-- Do NOT touch these unrelated result_a occurrences -->
- Local Ruby variables derived from table_monitor.data["playera"]["result"] — these are in-memory only and not ActiveRecord columns. Leave them alone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace result_a with ended_at in both confirmation modal bodies</name>
  <files>app/views/tournaments/show.html.erb, app/views/tournaments/finalize_modus.html.erb</files>
  <action>
Make exactly two edits, one per file. Both are one-line changes — nothing else in the ERB should move.

1) app/views/tournaments/show.html.erb, line 195:
   Change:
     reset_games_count = @tournament.games.where.not(result_a: nil).count
   To:
     reset_games_count = @tournament.games.where.not(ended_at: nil).count

2) app/views/tournaments/finalize_modus.html.erb, line 236:
   Change:
     fm_games_count = @tournament.games.where.not(result_a: nil).count
   To:
     fm_games_count = @tournament.games.where.not(ended_at: nil).count

Rationale (do not add as a comment — keep the diff minimal): the games table has no result_a column. A game is "played" when the table monitor sets ended_at. The `result_a`/`result_b` tokens elsewhere in the codebase are local Ruby variables read from `table_monitor.data["playera"]["result"]` and are NOT ActiveRecord column references — leave every such occurrence alone.

Do not rename the Ruby locals reset_games_count / fm_games_count. Do not change the I18n keys, modal copy, button wiring, or surrounding layout. No whitespace reflow. Keep the label "Gespielte Spiele" unchanged — ended_at semantics already match that label.
  </action>
  <verify>
    <automated>grep -n "result_a" app/views/tournaments/show.html.erb app/views/tournaments/finalize_modus.html.erb; grep -n "where.not(ended_at: nil)" app/views/tournaments/show.html.erb app/views/tournaments/finalize_modus.html.erb</automated>
  </verify>
  <done>
    - grep for `result_a` in both files returns zero matches
    - grep for `where.not(ended_at: nil)` returns exactly one match in each of the two files
    - No other lines in either file are modified (git diff shows ±1 line per file)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression test proving tournaments#show renders without PG::UndefinedColumn</name>
  <files>test/controllers/tournaments_controller_test.rb</files>
  <behavior>
    - Test renders `get tournament_url(@tournament)` with:
        * local-server context (Carambus.config.carambus_api_url = "http://local.test")
        * @tournament.tournament_started == false (fixture :local is in state "registration")
        * @tournament.has_clubcloud_results? stubbed to false (or fixture is naturally non-CC)
    - Test asserts response is :success (status 200) — NOT 500
    - Test asserts the response body includes the reset-modal trigger text (confirms the full modal block
      actually executed rather than being short-circuited upstream)
    - Before the fix: test would fail with PG::UndefinedColumn: column games.result_a does not exist
    - After the fix: test passes
  </behavior>
  <action>
Append one new test case to test/controllers/tournaments_controller_test.rb. Place it alongside the
existing "unauthenticated GET show is publicly accessible" test (around line 52-58) so related
show-rendering tests stay co-located.

Test name: "GET show renders reset modal for local not-started non-CC tournament (regression: result_a PG::UndefinedColumn)"

Test body requirements:
1. Set Carambus.config.carambus_api_url = "http://local.test" to force local server mode.
   (The setup block already captures @original_api_url and teardown restores it — no extra cleanup needed.)
2. Ensure the tournament is in the exact gating state the buggy code path requires:
     - @tournament.tournament_started must be falsy.
       The fixture tournaments(:local) has state "registration", so @tournament.tournament_started
       should already be false. If not, use @tournament.update_column(:state, "registration") to
       bypass any callbacks. Verify via `assert_not @tournament.tournament_started` before the GET.
     - @tournament.has_clubcloud_results? must return false.
       Stub it with @tournament.stub(:has_clubcloud_results?, false) wrapped around the GET, OR
       use Tournament.any_instance.stubs if Mocha is available, OR rely on the fixture if it is
       naturally non-CC. Check first: if tournaments(:local) already returns false for
       has_clubcloud_results?, no stub needed. Otherwise use Minitest's built-in `stub` via the
       tournament instance fetched inside the test.
3. Execute: get tournament_url(@tournament)
4. Assert: assert_response :success
5. Assert: assert_match /reset-tournament-form-#{@tournament.id}/, response.body
   This proves the modal body computation (the buggy line) actually ran and did not raise.
6. Do NOT use Carambus.config.carambus_api_url swapping beyond what setup already manages.

Use sign_in @user from setup (already done). Do not add fixtures. Do not modify the setup or teardown blocks.

If stubbing has_clubcloud_results? is needed and Mocha is not available, fetch a fresh instance
inside the test and use Ruby's Object#stub (provided by Minitest::Mock):

    tournament = Tournament.find(@tournament.id)
    tournament.stub(:has_clubcloud_results?, false) do
      Tournament.stub(:find, tournament) do  # so the controller loads our stubbed instance
        get tournament_url(tournament)
      end
    end

If that nested stubbing proves brittle, fall back to verifying the fixture's natural state by
first calling `assert_not tournaments(:local).has_clubcloud_results?` and relying on the fixture —
no stub needed. Pick whichever approach produces a green test with the fix applied and a red
test without the fix.
  </action>
  <verify>
    <automated>bin/rails test test/controllers/tournaments_controller_test.rb -n "/regression.*result_a/"</automated>
  </verify>
  <done>
    - New test exists in test/controllers/tournaments_controller_test.rb with "regression" and "result_a" in its name
    - Test passes with the Task 1 fix applied
    - (Manual sanity — not required in verify) Temporarily reverting Task 1 causes this test to fail with PG::UndefinedColumn, proving it actually guards the fixed line
    - Full file still parses: `bin/rails test test/controllers/tournaments_controller_test.rb` loads without syntax errors
  </done>
</task>

</tasks>

<verification>
Run after both tasks:

1. `grep -rn "result_a" app/views/tournaments/` returns zero matches
2. `bin/rails test test/controllers/tournaments_controller_test.rb -n "/regression.*result_a/"` passes
3. `bin/rails test test/controllers/tournaments_controller_test.rb` — the broader suite is still green (no regressions introduced)
4. `bundle exec erblint app/views/tournaments/show.html.erb app/views/tournaments/finalize_modus.html.erb` reports no new offenses

Optional spot check: open the two view files in a diff viewer and confirm the git diff is exactly ±1 line per file (plus the new test block).
</verification>

<success_criteria>
- Two view files contain `where.not(ended_at: nil)` and zero `result_a` references
- New regression test passes and lives under tests/controllers/tournaments_controller_test.rb
- Existing TournamentsControllerTest suite still green
- Diff is minimal: 2 single-line view edits + 1 new test method. No other files touched. No churn in unrelated code.
- Manual follow-up (out of scope for this plan, but noted): volunteer opens tournaments#show for a not-started local tournament and no longer sees the 500 page
</success_criteria>

<output>
After completion, create `.planning/quick/260414-qb8-fix-pg-undefinedcolumn-result-a-crash-in/260414-qb8-01-SUMMARY.md` recording:
- The two view diffs (before/after of the single changed line each)
- The new test name and the command that runs it in isolation
- Confirmation that Gespielte Spiele now counts games with ended_at set (semantic equivalence note: ended_at is the table monitor's "game finished" marker, which matches the label's intent more accurately than a non-existent result_a column ever could)
- Note that Ruby-local `result_a`/`result_b` tokens elsewhere in the codebase (derived from table_monitor.data) were intentionally left untouched
</output>
