---
phase: 260422-pz0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema.rb
autonomous: false
requirements:
  - MERGE-01  # Merge origin/master into feature/training-system (Claudia handoff 2026-04-22)
  - MERGE-02  # Resolve db/schema.rb conflict manually (master base + Training-System tables spliced)
  - MERGE-03  # Verify schema integrity, db:migrate (no SAFETY_ASSURED), and test suite
  - MERGE-04  # Capture reporting items for Claudia (SHA, db:migrate result, test numbers, grep)

must_haves:
  truths:
    - "feature/training-system HEAD is a merge commit with two parents: previous feature HEAD (d4fda370) and origin/master HEAD (7ac9990f)"
    - "db/schema.rb contains NO Jumpstart-Pro residue (no account_invitations, account_users, accounts, pay_*, action_text_*, action_mailbox_* tables)"
    - "db/schema.rb contains ALL Training-System tables (ball_configurations, ball_configuration_zones, table_zones, shot_events, training_concept_relations, training_concept_examples)"
    - "RAILS_ENV=test bin/rails db:migrate runs WITHOUT SAFETY_ASSURED=1 (strong_migrations initializer from master handles it)"
    - "RAILS_ENV=test bin/rails test produces 105 runs / 227 assertions / 0 failures (reference from Memory #95)"
    - "Four reporting items captured for Claudia feedback"
  artifacts:
    - path: "db/schema.rb"
      provides: "Merged schema — master baseline + Training-System tables spliced alphabetically"
      contains: "ball_configurations"
    - path: ".paul/HANDOFF-2026-04-22-merge-origin-master.md"
      provides: "Source of truth — referenced, not modified"
  key_links:
    - from: "feature/training-system"
      to: "origin/master"
      via: "git merge commit"
      pattern: "Merge origin/master into feature/training-system"
    - from: "db/schema.rb"
      to: "Training-System migrations (db/migrate/20260420*, 20260421*)"
      via: "create_table blocks preserved from feature side"
      pattern: "create_table \"(ball_configurations|table_zones|shot_events|training_concept_relations)\""
---

<objective>
Execute the Claudia → Paul merge handoff: merge `origin/master` into `feature/training-system`, manually resolving the single expected conflict in `db/schema.rb`, then verify schema integrity, migrations, and test suite. Capture the four reporting items for Claudia.

Purpose: Pulls the Jumpstart-Pro cleanup (sanitized baseline + cleaned follow-up migrations + `config/initializers/strong_migrations.rb`) onto the Training-System branch. After this merge, the schema.rb hand-curation pattern (Memory #94) and the `SAFETY_ASSURED=1` workaround (Memory #98) become obsolete.

Output: Merge commit on `feature/training-system`, clean `db/schema.rb`, verification artifacts, reporting items for Claudia.

Scope: Pure git + verification. NO new code, NO new migrations, NO ontology changes.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_gu/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_gu/.paul/HANDOFF-2026-04-22-merge-origin-master.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_gu/.paul/STATE.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_gu/CLAUDE.md

# The handoff (.paul/HANDOFF-2026-04-22-merge-origin-master.md) is THE authoritative procedure.
# This plan references the handoff — it does NOT duplicate it. Executor MUST re-read the handoff
# before every step to avoid drift.

# Expected state before execution:
#   - Working tree clean on feature/training-system
#   - HEAD = d4fda370 (Ontologie v0.9 Phase E)
#   - origin/master HEAD after fetch = 7ac9990f (docs(test): scenario testing pitfalls)
#   - merge-base = 2a4bababbaf81cf9dbfe0513d7b232884169f417
#   - 27 commits on master ahead, 16 on feature ahead
#
# Expected outcome per Memory #95:
#   Test suite reference: 105 runs / 227 assertions / 0 failures
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute merge and resolve db/schema.rb conflict per handoff</name>
  <files>db/schema.rb</files>
  <action>
Follow the handoff's "Merge-Ablauf" and "Echter Konflikt: genau eine Datei" sections exactly. Re-read `.paul/HANDOFF-2026-04-22-merge-origin-master.md` sections "Erwartete Konflikte" and "Merge-Ablauf" before starting.

Step-by-step (per handoff):

1. Verify clean working tree and starting point:
   ```bash
   cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_gu
   git status                                # must be clean
   git fetch origin
   git checkout feature/training-system      # must already be on this branch
   git log -1 --oneline                      # must show d4fda370
   git rev-parse origin/master               # should be 7ac9990f (or newer if fetch updated)
   git merge-base feature/training-system origin/master  # must be 2a4bababbaf81cf9dbfe0513d7b232884169f417
   ```
   If any precondition fails, STOP and report to user — do NOT proceed.

2. Capture feature-side schema.rb BEFORE starting merge (so we have the Training-System tables to splice in):
   ```bash
   git show feature/training-system:db/schema.rb > /tmp/feature_schema.rb
   ```

3. Execute the merge:
   ```bash
   git merge origin/master
   ```
   Expected: conflict ONLY in `db/schema.rb`. Any other conflict = STOP and report (handoff analysis says no other file is touched on both sides except the PDF which is a clean double-delete).

4. Resolve `db/schema.rb` per handoff strategy (NEVER use `bin/rails db:schema:dump` — it would re-pull Jumpstart tables from a possibly-dirty dev DB, per Memory #94):

   a. Take master-version as base (Jumpstart-clean):
      ```bash
      git checkout --theirs db/schema.rb
      ```
      Note: during a merge from origin/master INTO feature/training-system, `--theirs` refers to origin/master side (the side being merged in). Verify by checking that `account_invitations`, `accounts`, `pay_*` tables are GONE and Training-System tables are MISSING.

   b. Extract the Training-System `create_table` blocks from `/tmp/feature_schema.rb`. The tables to splice in (per handoff): `ball_configurations`, `ball_configuration_zones`, `table_zones`, `shot_events`, `training_concept_relations`, `training_concept_examples`. Also any Training-System `add_foreign_key` lines at the end of the file.

   c. Insert each `create_table` block ALPHABETICALLY at the correct position in `db/schema.rb`. Use the Edit tool — do NOT regenerate. Match existing indentation and style.

   d. Append the Training-System `add_foreign_key` lines at the end of the foreign-key block (alphabetical position within that block is fine if it fits).

   e. Raise the `ActiveRecord::Schema[X.Y].define(version: YYYY_MM_DD_HHMMSS)` header to the HIGHER of the two version values (compare master's version header vs feature's version header from `/tmp/feature_schema.rb`, take the max).

5. Stage and complete the merge using the handoff's commit message verbatim:
   ```bash
   git add db/schema.rb
   git merge --continue
   # or: git commit --no-edit if the editor opens with the template message
   ```

   If git asks for a commit message, use EXACTLY this (from handoff "Commit-Message-Vorschlag"):
   ```
   Merge origin/master into feature/training-system

   Holt den Jumpstart-Pro-Cleanup (sanierte Baseline + bereinigte
   Folge-Migrationen + strong_migrations-Initializer) auf den
   Training-System-Branch.

   schema.rb manuell aufgelöst: master-Version (Jumpstart-clean) als Basis,
   Training-System-Tabellen (ball_configurations, ball_configuration_zones,
   table_zones, shot_events, training_concept_relations,
   training_concept_examples) sowie die neuen FKs wieder eingefügt.
   ```

6. Capture the merge commit SHA for reporting:
   ```bash
   git rev-parse HEAD
   git log -1 --oneline
   ```

Avoid: `bin/rails db:schema:dump` (handoff forbids it), force pushes, amending, interactive rebase. If the conflict looks unmanageable, use `git merge --abort` and report to user (handoff "Bei Problemen" → "schema.rb-Konflikt ist unübersichtlich").
  </action>
  <verify>
    <automated>
git log -1 --pretty=%P | awk 'NF==2 {exit 0} {exit 1}' && \
grep -E "ball_configurations|table_zones|shot_events|training_concept_relations" db/schema.rb | wc -l | awk '$1 >= 4 {exit 0} {exit 1}' && \
! grep -qE "account_invitations|account_users|^\s+create_table \"accounts\"|pay_charges|action_text_embeds|action_mailbox" db/schema.rb
    </automated>
  </verify>
  <done>
- `git log -1 --pretty=%P` shows TWO parent SHAs (merge commit).
- `git status` is clean.
- `db/schema.rb` grep-check per handoff §Verifikation step 1: Jumpstart-pattern grep returns EMPTY; Training-System-pattern grep returns AT LEAST ONE hit per pattern.
- Merge commit SHA captured (will be reported to Claudia in Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Run verification suite from handoff §Verifikation nach dem Merge</name>
  <files></files>
  <action>
Execute the four verification steps from the handoff, in order. Collect output for Task 3 reporting.

1. **Schema integrity grep-check** (handoff §Verifikation step 1):
   ```bash
   cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_gu

   # Must be EMPTY:
   grep -E "account_invitations|account_users|^\s+create_table \"accounts\"|pay_charges|action_text_embeds|action_mailbox" db/schema.rb

   # Must each return >=1 hit:
   grep -E "ball_configurations|table_zones|shot_events|training_concept_relations" db/schema.rb
   ```
   Record: "sauber" (clean) or "nicht sauber" (dirty, list residues).

2. **db:migrate WITHOUT SAFETY_ASSURED** (handoff §Verifikation step 3 — Memory #98 workaround test):
   ```bash
   RAILS_ENV=test bin/rails db:migrate
   ```
   This is the KEY test — it tells us whether master's `config/initializers/strong_migrations.rb` makes the `SAFETY_ASSURED=1` workaround obsolete. Record: "ging" (passed without SAFETY_ASSURED) or "ging nicht" (blocked — include exact strong_migrations error message).

   If it blocks: per handoff §Bei Problemen, do NOT retry with SAFETY_ASSURED — just record the failure and move on. Claudia needs to know the cleanup didn't fully land.

3. **Test suite** (handoff §Verifikation step 3):
   ```bash
   RAILS_ENV=test bin/rails test
   ```
   Expected per Memory #95: 105 runs / 227 assertions / 0 failures. Record the actual numbers (runs / assertions / failures / errors / skips).

4. **schema.rb drift check** (handoff §Verifikation step 4) — OPTIONAL but recommended:
   ```bash
   bin/rails db:migrate        # normal dev DB run
   git diff db/schema.rb       # should be empty or only show new migrations
   ```
   If Jumpstart tables reappear after `db:migrate`, the cleanup on origin/master is incomplete — flag for Claudia.

SKIP the "frische Dev-DB" step (handoff §Verifikation step 2 — `bin/deploy-scenario carambus_gu` + `psql \dt`) unless the user explicitly requests it. It's a heavy teardown/rebuild and the grep-check above already validates the same invariant statically.

Do NOT fix any issues found during verification — this plan is pure merge + verify. Deviations get reported to Claudia in Task 3.
  </action>
  <verify>
    <automated>
RAILS_ENV=test bin/rails test 2>&1 | tee /tmp/260422-pz0-test.log | grep -E "^[0-9]+ runs?, [0-9]+ assertions?, [0-9]+ failures?" | awk '/0 failures/ {exit 0} {exit 1}'
    </automated>
  </verify>
  <done>
- Grep check results recorded (clean/dirty).
- `RAILS_ENV=test bin/rails db:migrate` outcome recorded (passed / blocked with exact error).
- Test suite numbers captured (runs / assertions / failures).
- Ideally: 0 failures. If failures exist, handoff §Bei Problemen → "Tests brechen" applies: capture diff of failures for Claudia, do NOT attempt fixes in this plan.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Capture 4 reporting items for Claudia and confirm handoff completion</name>
  <what-built>
    Merge commit on `feature/training-system` with Jumpstart-Pro cleanup from `origin/master` pulled in, `db/schema.rb` manually resolved (master base + Training-System tables spliced alphabetically + higher version header), and verification suite executed per handoff §Verifikation.
  </what-built>
  <how-to-verify>
Per handoff §"Nach dem Merge: Rückmeldung an Claudia" — confirm these four items are captured and ready to return to Claudia so Memories #94, #98, and #99 can be marked as resolved:

1. **Merge-Commit-SHA** — from `git rev-parse HEAD` after Task 1.

2. **`RAILS_ENV=test bin/rails db:migrate` ohne SAFETY_ASSURED** — "ging" or "ging nicht" (with exact strong_migrations error message if blocked). This determines whether Memory #98 becomes obsolete.

3. **`RAILS_ENV=test bin/rails test` result** — runs / assertions / failures / errors / skips. Reference expectation: 105 / 227 / 0 / 0 / ? per Memory #95.

4. **schema.rb grep-check** — "sauber" or "nicht sauber" (with list of residues if dirty). This determines whether Memory #94 becomes obsolete.

Sanity checks before signing off:
- [ ] `git log -1 --pretty=format:"%H %P"` shows merge commit with two parents (feature + master).
- [ ] `git status` is clean (no unstaged files, no untracked files from the merge).
- [ ] The commit message on the merge matches the handoff template (verbatim, including German).
- [ ] `db/schema.rb` contains ALL six Training-System tables listed in the handoff.
- [ ] `db/schema.rb` contains NONE of the Jumpstart-Pro tables listed in the handoff.
- [ ] No unpushed local branches or detached HEAD weirdness.

If any verification step failed (db:migrate blocked / tests red / schema.rb dirty), the handoff §"Bei Problemen" section applies — present findings to user; do NOT auto-fix.

Do NOT push to origin unless user explicitly requests it. Paul's lane per Memory #100 = merges in `carambus_gu` only; push decision is user's.
  </how-to-verify>
  <resume-signal>
Type "approved" to confirm the four reporting items are captured and return control to the orchestrator, OR describe issues / deviations found during verification.
  </resume-signal>
</task>

</tasks>

<verification>
Per handoff §Verifikation nach dem Merge (all four steps):

1. Schema-Integrität: `grep` shows Jumpstart patterns EMPTY, Training-System patterns present.
2. Frische Dev-DB: SKIPPED (heavy; grep-check covers the invariant). User may run manually.
3. Test-Suite: `RAILS_ENV=test bin/rails db:migrate` (no SAFETY_ASSURED) + `RAILS_ENV=test bin/rails test` (target 105/227/0).
4. schema.rb-Drift-Check: `bin/rails db:migrate` followed by `git diff db/schema.rb` shows no drift.

Plus merge-hygiene checks:
- `git log -1 --pretty=%P` → two parents.
- `git status` → clean.
- Commit message matches handoff template verbatim.
</verification>

<success_criteria>
- Merge commit exists on `feature/training-system` with parents d4fda370 + origin/master HEAD.
- `db/schema.rb` grep-check: Jumpstart-Pro patterns EMPTY, Training-System patterns present (≥1 each).
- `RAILS_ENV=test bin/rails db:migrate` outcome documented (ideally passes without SAFETY_ASSURED).
- `RAILS_ENV=test bin/rails test` result documented (ideally 105 runs / 227 assertions / 0 failures per Memory #95).
- Four reporting items ready for Claudia (merge SHA, db:migrate result, test numbers, grep cleanliness).
- NO ontology changes, NO new migrations, NO new code — pure git + verification.
</success_criteria>

<output>
After Task 3 sign-off, create `.planning/quick/260422-pz0-merge-origin-master-into-feature-trainin/260422-pz0-SUMMARY.md` containing:

- Merge commit SHA
- db/schema.rb resolution summary (which tables spliced, which version header won)
- The four reporting items (formatted for pasting to Claudia)
- Memory status: #94 obsolete? #98 obsolete? #99 done? (based on verification outcomes)
- Any deviations from handoff or issues encountered (per handoff §Bei Problemen)
</output>
