---
phase: quick-260507-m2z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mcp_server/tools/base_tool.rb
  - lib/mcp_server/tools/lookup_region.rb
  - lib/mcp_server/tools/lookup_league.rb
  - lib/mcp_server/tools/lookup_tournament.rb
  - lib/mcp_server/tools/lookup_teilnehmerliste.rb
  - lib/mcp_server/tools/lookup_team.rb
  - lib/mcp_server/tools/lookup_club.rb
  - lib/mcp_server/tools/lookup_spielbericht.rb
  - lib/mcp_server/tools/lookup_category.rb
  - lib/mcp_server/tools/lookup_serie.rb
  - lib/mcp_server/tools/search_player.rb
  - lib/mcp_server/tools/finalize_teilnehmerliste.rb
  - test/mcp_server/tools/lookup_region_test.rb
  - test/mcp_server/tools/cc_fed_id_env_default_test.rb
  - test/mcp_server/tools/finalize_teilnehmerliste_test.rb
  - docs/developers/clubcloud-mcp-server.de.md
autonomous: true
requirements:
  - QUICK-M2Z-01

must_haves:
  truths:
    - "Tools mit fed_id-Argument lesen ENV['CC_FED_ID'] als Default, wenn der Aufrufer fed_id nicht übergibt."
    - "Wenn weder fed_id noch ENV['CC_FED_ID'] gesetzt sind, bleibt der bestehende 'Missing fed_id'-Fehler erhalten."
    - "BaseTool.default_fed_id ist die einzige ENV-Lookup-Stelle (DRY)."
    - "Tool-Listing zeigt Claude in der input_schema-description, dass CC_FED_ID Default ist."
    - "Doku in Section 6 (Reference Manual → BaseTool) führt default_fed_id auf."
    - "Doku in Section 11 vermerkt, dass die ENV-Default-Lücke geschlossen ist."
  artifacts:
    - path: "lib/mcp_server/tools/base_tool.rb"
      provides: "Class-Helper default_fed_id"
      contains: "def self.default_fed_id"
    - path: "test/mcp_server/tools/cc_fed_id_env_default_test.rb"
      provides: "Regression-Tests für ENV-Default in 3 repräsentativen Tools"
      contains: "CC_FED_ID"
    - path: "docs/developers/clubcloud-mcp-server.de.md"
      provides: "BaseTool-Helper-Eintrag + ENV-Default-Closure-Notiz"
      contains: "default_fed_id"
  key_links:
    - from: "lib/mcp_server/tools/*.rb (11 Tools)"
      to: "BaseTool.default_fed_id"
      via: "fed_id ||= default_fed_id vor Validierung"
      pattern: "fed_id \\|\\|= default_fed_id"
    - from: "lib/mcp_server/tools/base_tool.rb"
      to: "ENV['CC_FED_ID']"
      via: "ENV-Lookup mit &.to_i"
      pattern: "ENV\\[.CC_FED_ID.\\]"
---

<objective>
Schließt die in der Spec dokumentierte CC_FED_ID-Lücke: `.mcp.json` und `claude mcp add --env CC_FED_ID=20` reichen die ENV-Variable an den MCP-Subprocess durch, aber kein Tool liest sie. Heute muss Claude `fed_id: 20` in jedem Tool-Call manuell wiederholen.

Purpose: Reduziert das mentale Overhead pro Tool-Call und macht ENV-basierte Default-Konfiguration nutzbar (heute werte- aber wirkungslos).

Output:
- BaseTool-Helper `default_fed_id` (eine ENV-Lookup-Stelle).
- 11 Tools mit `fed_id ||= default_fed_id`-Default-Lookup vor Validierung.
- 11 input_schema-Descriptions ergänzt um Default-Hinweis (`Defaults to ENV['CC_FED_ID'] if not provided.`).
- 1 neuer Test-File mit 6 Tests (3 Tools × {ENV gesetzt → kein Missing-Error / ENV unset → Missing-Error}).
- Doku-Update in Section 6 (BaseTool-Tabelle) + Section 11 (Closure-Notiz).
</objective>

<execution_context>
@/Users/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@lib/mcp_server/tools/base_tool.rb
@lib/mcp_server/tools/lookup_region.rb
@lib/mcp_server/tools/lookup_team.rb
@lib/mcp_server/tools/finalize_teilnehmerliste.rb
@test/mcp_server/tools/lookup_region_test.rb
@test/mcp_server/tools/finalize_teilnehmerliste_test.rb
@docs/developers/clubcloud-mcp-server.de.md

<interfaces>
<!-- Aus base_tool.rb: BaseTool ist class-method-only. Subklassen erben automatisch -->
<!-- alle .* Helper. default_fed_id wird identisch verwendbar wie .mock_mode? oder .cc_session. -->

From lib/mcp_server/tools/base_tool.rb:
```ruby
module McpServer
  module Tools
    class BaseTool < MCP::Tool
      def self.error(message); end       # → MCP::Tool::Response (error: true)
      def self.text(message); end        # → MCP::Tool::Response
      def self.validate_required!(args, required_keys); end  # nil oder Error-Response
      def self.mock_mode?; end           # → Boolean (ENV["CARAMBUS_MCP_MOCK"] == "1")
      def self.cc_session; end           # → McpServer::CcSession
    end
  end
end
```

Bestehende Validation-Patterns in den 11 Tools (alle vor Migration):
- `return error("Missing required parameter: \`fed_id\`") if fed_id.blank?`  (lookup_team, lookup_club, lookup_spielbericht, lookup_category, lookup_serie, lookup_tournament, lookup_teilnehmerliste — bei live-only oder im live_lookup-Pfad)
- `validate_required!(...)` mit `:fed_id` in required_keys (finalize_teilnehmerliste)
- `validate_required_anyof!(shortname:, fed_id:)` (lookup_region — fed_id ist optional, muss dort NICHT defaulten weil shortname-Pfad legitim ist; aber live_lookup(fed_id:) braucht den Default)
- `unless league_id.present? || (fed_id.present? && branch_id.present? && season.present?)` (lookup_league — anyof-Variante)
- `params[:fedId] = fed_id if fed_id.present?` (search_player — fed_id ist optional Scope-Filter)

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BaseTool.default_fed_id + 11 Tools auf ENV-Default umstellen + Regression-Tests</name>
  <files>
    lib/mcp_server/tools/base_tool.rb,
    lib/mcp_server/tools/lookup_region.rb,
    lib/mcp_server/tools/lookup_league.rb,
    lib/mcp_server/tools/lookup_tournament.rb,
    lib/mcp_server/tools/lookup_teilnehmerliste.rb,
    lib/mcp_server/tools/lookup_team.rb,
    lib/mcp_server/tools/lookup_club.rb,
    lib/mcp_server/tools/lookup_spielbericht.rb,
    lib/mcp_server/tools/lookup_category.rb,
    lib/mcp_server/tools/lookup_serie.rb,
    lib/mcp_server/tools/search_player.rb,
    lib/mcp_server/tools/finalize_teilnehmerliste.rb,
    test/mcp_server/tools/cc_fed_id_env_default_test.rb
  </files>
  <behavior>
    Test-Matrix in `test/mcp_server/tools/cc_fed_id_env_default_test.rb` (1 neue Datei, 6 Tests):

    Setup für ALLE Tests: `McpServer::CcSession.reset!` + `_client_override = MockClient.new` + `session_id = "TEST"` (kopiere setup-Pattern aus finalize_teilnehmerliste_test.rb damit MockClient genutzt wird; KEIN echter CC-Login).
    Teardown: `ENV["CC_FED_ID"] = nil`, `_client_override = nil`, `CcSession.reset!`.

    Repräsentativer Querschnitt (3 Tools, 6 Tests):

    1. **LookupRegion (DB-first; live_lookup-Pfad braucht Default)** — `force_refresh: true, shortname: nil, fed_id: nil`:
       - Test 1: ENV `CC_FED_ID=20`, Aufruf ohne fed_id → response.error? false (Live-Pfad reicht durch zu MockClient.get → 200), text matched `/fed_id=20/`.
       - Test 2: ENV unset, Aufruf ohne fed_id → response.error? true, text matched `/Missing required parameter for live lookup: fed_id/i` ODER `/Missing required parameter: provide at least one/i` (anyof-Validation feuert je nach Reihenfolge — beide akzeptabel, dokumentiert in Test-Kommentar).

    2. **LookupTeam (live-only; fed_id ist Pflicht)** — `team_id: 99`:
       - Test 3: ENV `CC_FED_ID=20`, Aufruf ohne fed_id → response.error? false, text matched `/fed_id=20/`.
       - Test 4: ENV unset, Aufruf ohne fed_id → response.error? true, text matched `/Missing required parameter: .fed_id./`.

    3. **FinalizeTeilnehmerliste (Write-Tool; fed_id im required-Array)** — `branch_id: 10, season: "2025/2026", meldeliste_id: 42, armed: false` (Dry-Run, kein Auth nötig):
       - Test 5: ENV `CC_FED_ID=20`, Aufruf ohne fed_id → response.error? false, text matched `/Would finalize Meldeliste 42/`.
       - Test 6: ENV unset, Aufruf ohne fed_id → response.error? true, text matched `/Missing required parameter.*fed_id/i`.
  </behavior>
  <action>
    **Schritt 1 — RED: Tests schreiben** (`test/mcp_server/tools/cc_fed_id_env_default_test.rb`).

    Setup-Pattern aus finalize_teilnehmerliste_test.rb übernehmen (MockClient-Pattern). Beispielblock:

    ```ruby
    # frozen_string_literal: true
    require "test_helper"

    # Regression-Tests für CC_FED_ID-ENV-Default in allen MCP-Tools mit fed_id-Argument.
    # BaseTool.default_fed_id liest ENV['CC_FED_ID'] und stellt es 11 Tools als Fallback bereit,
    # wenn der Aufrufer keine fed_id übergibt.
    class McpServer::Tools::CcFedIdEnvDefaultTest < ActiveSupport::TestCase
      setup do
        McpServer::CcSession.reset!
        McpServer::CcSession.session_id = "TEST_SESSION_ID"
        McpServer::CcSession.session_started_at = Time.now
        @mock = McpServer::Tools::MockClient.new
        McpServer::CcSession._client_override = @mock
      end

      teardown do
        ENV["CC_FED_ID"] = nil
        McpServer::CcSession._client_override = nil
        McpServer::CcSession.reset!
      end

      # ===== LookupRegion (DB-first; live_lookup-Pfad braucht Default) =====
      test "LookupRegion: ENV CC_FED_ID=20 wird als Default für force_refresh-Live-Pfad gelesen" do
        ENV["CC_FED_ID"] = "20"
        response = McpServer::Tools::LookupRegion.call(force_refresh: true, server_context: nil)
        refute response.error?, "Erwartet kein Fehler bei gesetztem ENV; got: #{response.content.first[:text]}"
        assert_match(/fed_id=20/, response.content.first[:text])
      end

      test "LookupRegion: ENV unset → bestehender Missing-fed_id-Fehler bleibt" do
        ENV["CC_FED_ID"] = nil
        response = McpServer::Tools::LookupRegion.call(force_refresh: true, server_context: nil)
        assert response.error?
        assert_match(/Missing required parameter|provide at least one/i, response.content.first[:text])
      end

      # ===== LookupTeam (live-only; fed_id ist Pflicht) =====
      test "LookupTeam: ENV CC_FED_ID=20 wird als Default verwendet" do
        ENV["CC_FED_ID"] = "20"
        response = McpServer::Tools::LookupTeam.call(team_id: 99, server_context: nil)
        refute response.error?, "Erwartet kein Fehler bei gesetztem ENV; got: #{response.content.first[:text]}"
        assert_match(/fed_id=20/, response.content.first[:text])
      end

      test "LookupTeam: ENV unset → Missing-fed_id-Fehler bleibt" do
        ENV["CC_FED_ID"] = nil
        response = McpServer::Tools::LookupTeam.call(team_id: 99, server_context: nil)
        assert response.error?
        assert_match(/Missing required parameter: .fed_id./, response.content.first[:text])
      end

      # ===== FinalizeTeilnehmerliste (Write-Tool; fed_id im required-Array) =====
      test "FinalizeTeilnehmerliste: ENV CC_FED_ID=20 wird im validate_required!-Pfad als Default verwendet (dry-run)" do
        ENV["CC_FED_ID"] = "20"
        response = McpServer::Tools::FinalizeTeilnehmerliste.call(
          branch_id: 10, season: "2025/2026", meldeliste_id: 42,
          server_context: nil
        )
        refute response.error?, "Erwartet kein Fehler bei gesetztem ENV; got: #{response.content.first[:text]}"
        assert_match(/Would finalize Meldeliste 42/, response.content.first[:text])
      end

      test "FinalizeTeilnehmerliste: ENV unset → Missing-fed_id-Fehler bleibt" do
        ENV["CC_FED_ID"] = nil
        response = McpServer::Tools::FinalizeTeilnehmerliste.call(
          branch_id: 10, season: "2025/2026", meldeliste_id: 42,
          server_context: nil
        )
        assert response.error?
        assert_match(/Missing required parameter.*fed_id/i, response.content.first[:text])
      end
    end
    ```

    Tests starten: `bin/rails test test/mcp_server/tools/cc_fed_id_env_default_test.rb` — alle 6 müssen RED sein (3× kein Default-Lookup → Missing-Error trotz ENV; 3× ENV-unset-Fall ist heute zufällig GREEN, falls auch in RED — auch ok).

    **Schritt 2 — GREEN: BaseTool-Helper hinzufügen** (`lib/mcp_server/tools/base_tool.rb`):

    Direkt nach `cc_session` einfügen:
    ```ruby
    # Liest ENV["CC_FED_ID"] als Default-Fallback für Tools, die fed_id nicht vom Aufrufer bekommen.
    # Eine ENV-Lookup-Stelle (DRY); kein per-Tool-Override-Pattern. Tools nutzen:
    #   fed_id ||= default_fed_id
    # vor der Validierung. Falls auch ENV nicht gesetzt ist (nil), bleibt der bestehende
    # "Missing required parameter: fed_id"-Fehler erhalten — keine Verhaltensänderung in dieser Bahn.
    def self.default_fed_id
      ENV["CC_FED_ID"]&.to_i
    end
    ```

    **Schritt 3 — GREEN: Default-Lookup in 11 Tools einbauen.** Pattern: `fed_id ||= default_fed_id` als ERSTE Zeile in `def self.call` (vor jeder Validierung). Schema-`description:` erweitern.

    | Datei | Stelle für `fed_id ||= default_fed_id` | Schema-description-Update |
    |-------|---------------------------------------|---------------------------|
    | `lookup_region.rb` | Zeile 22, NACH `def self.call(...)` (vor `validate_required_anyof!`) — Default-Lookup hier macht Sinn, weil sowohl shortname-only als auch fed_id-only Pfad weiterhin sauber funktionieren (anyof-Logik ist `OR`) | Zeile 15: `description: "ClubCloud federation ID. Defaults to ENV['CC_FED_ID'] if not provided."` |
    | `lookup_league.rb` | Zeile 22, vor der `unless league_id.present? \|\| (...)`-Validation | Zeile 12: `description: "ClubCloud federation ID. Defaults to ENV['CC_FED_ID'] if not provided."` |
    | `lookup_tournament.rb` | Zeile 22 (erste Zeile in `def self.call`) | Zeile 14: `description: "ClubCloud federation ID (required for live lookup). Defaults to ENV['CC_FED_ID'] if not provided."` |
    | `lookup_teilnehmerliste.rb` | Zeile 25 (erste Zeile in `def self.call`) | Zeile 18: `description: "ClubCloud federation ID (required for live lookup). Defaults to ENV['CC_FED_ID'] if not provided."` |
    | `lookup_team.rb` | Zeile 19 (erste Zeile in `def self.call`, vor team_id-Check) | Zeile 13: `description: "ClubCloud federation ID. Defaults to ENV['CC_FED_ID'] if not provided."` |
    | `lookup_club.rb` | Zeile 19 (erste Zeile in `def self.call`) | Zeile 12: `description: "ClubCloud federation ID. Defaults to ENV['CC_FED_ID'] if not provided."` |
    | `lookup_spielbericht.rb` | Zeile 19 (erste Zeile in `def self.call`) | Zeile 13: `description: "ClubCloud federation ID. Defaults to ENV['CC_FED_ID'] if not provided."` |
    | `lookup_category.rb` | Zeile 19 (erste Zeile in `def self.call`) | Zeile 13: `description: "ClubCloud federation ID. Defaults to ENV['CC_FED_ID'] if not provided."` |
    | `lookup_serie.rb` | Zeile 20 (erste Zeile in `def self.call`) | Zeile 13: `description: "ClubCloud federation ID. Defaults to ENV['CC_FED_ID'] if not provided."` |
    | `search_player.rb` | Zeile 20 (erste Zeile in `def self.call`) — Default-Lookup auch hier sinnvoll, dann ist fed_id immer scoped wenn ENV gesetzt | Zeile 14: `description: "ClubCloud federation ID to scope the search (optional). Defaults to ENV['CC_FED_ID'] if not provided."` |
    | `finalize_teilnehmerliste.rb` | Zeile 30 (erste Zeile in `def self.call`, VOR `validate_required!`) — wichtig, weil validate_required! sonst sofort feuert | Zeile 19: `description: "ClubCloud federation ID (e.g. 20 for BCW). Defaults to ENV['CC_FED_ID'] if not provided."` |

    **Konkrete Edit-Vorlage** (anwenden auf jede der 11 Dateien sinngemäß):

    ```ruby
    # VORHER:
    def self.call(team_id: nil, fed_id: nil, server_context: nil)
      return error("Missing required parameter: `team_id`") if team_id.blank?
      return error("Missing required parameter: `fed_id`") if fed_id.blank?

    # NACHHER:
    def self.call(team_id: nil, fed_id: nil, server_context: nil)
      fed_id ||= default_fed_id
      return error("Missing required parameter: `team_id`") if team_id.blank?
      return error("Missing required parameter: `fed_id`") if fed_id.blank?
    ```

    **Sonderfall lookup_region.rb**: Default-Lookup VOR `validate_required_anyof!` einfügen, damit bei ENV-gesetzt + shortname-nil + fed_id-nil der anyof-Check passiert (fed_id ist dann gesetzt → kein Error). Plus `live_lookup`'s eigener `if fed_id.blank?`-Check muss erhalten bleiben (defensive Doppel-Sicherung — `live_lookup` ist private API).

    **Sonderfall lookup_region_test.rb**: Test "force_refresh requires fed_id" (Zeile 35-39) prüft heute, dass `force_refresh: true` ohne fed_id einen Error gibt. Mit ENV-Default UND ENV gesetzt würde der Test brechen. **Lösung**: Setup-Block ergänzen mit `ENV["CC_FED_ID"] = nil` damit der Test unabhängig vom Host-ENV läuft. Bestehender Test bleibt sonst unverändert.

    **Schritt 4 — GREEN-Verifikation**: 6 neue Tests + alle bestehenden Tests grün:
    ```
    bin/rails test test/mcp_server/tools/cc_fed_id_env_default_test.rb
    bin/rails test test/mcp_server/tools/
    ```

    **WICHTIG — kein neues `default:`-Feld im input_schema** (constraint): nur die `description:`-Strings ändern. MCP-SDK validiert `default:` nicht zuverlässig; besser via Description signalisieren.

    **WICHTIG — kein per-Tool-Override** (constraint): keine eigene `default_fed_id`-Methode in einem der 11 Tools. Eine ENV-Lookup-Stelle nur in BaseTool.
  </action>
  <verify>
    <automated>bin/rails test test/mcp_server/tools/cc_fed_id_env_default_test.rb test/mcp_server/tools/ 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `BaseTool.default_fed_id` existiert und liest `ENV["CC_FED_ID"]&.to_i`.
    - 11 Tools haben `fed_id ||= default_fed_id` vor jeder Validierung.
    - 11 input_schema-descriptions enthalten `Defaults to ENV['CC_FED_ID']`.
    - Neue Test-Datei `test/mcp_server/tools/cc_fed_id_env_default_test.rb` mit 6 Tests, alle grün.
    - `lookup_region_test.rb` setup-Block setzt `ENV["CC_FED_ID"] = nil` damit "force_refresh requires fed_id"-Test stabil bleibt.
    - Alle bestehenden Tests in `test/mcp_server/tools/` weiterhin grün.
    - `grep -c "fed_id ||= default_fed_id" lib/mcp_server/tools/*.rb` zeigt mindestens 11 Treffer (1 pro Tool).
  </done>
</task>

<task type="auto">
  <name>Task 2: Doku-Update + mkdocs build + commit</name>
  <files>
    docs/developers/clubcloud-mcp-server.de.md
  </files>
  <action>
    **Schritt 1 — Section 6 (Reference Manual → BaseTool) erweitern.**

    In `docs/developers/clubcloud-mcp-server.de.md` ab Zeile 411-417 die Tabelle ergänzen. Direkt nach der `.cc_session`-Zeile (Zeile 417) eine neue Tabellenzeile einfügen:

    ```markdown
    | `.default_fed_id` | → `Integer` oder `nil` | Liest `ENV["CC_FED_ID"]&.to_i`. Tools nutzen `fed_id ||= default_fed_id` vor der Validierung — fällt der Aufrufer keine `fed_id` übergibt, wird der ENV-Default verwendet. Eine zentrale Lookup-Stelle (DRY); kein per-Tool-Override. |
    ```

    **Schritt 2 — Section 11 (Bekannte Issues) Closure-Notiz.**

    Da im Issue-Table aktuell KEIN expliziter Eintrag für die ENV-Default-Lücke existiert (WR-04 ist Reauth, WR-05 ist Validierungs-Reihenfolge), wird unterhalb der Info-Findings-Tabelle (nach Zeile 859) ein neuer Markdown-Block eingefügt:

    ```markdown
    **Geschlossen (Quick-Fix `260507-m2z`, 2026-05-07):**

    - **CC_FED_ID-ENV-Default-Lücke** — `.mcp.json` und `claude mcp add --env CC_FED_ID=20` reichten die ENV-Variable an den Subprocess durch, aber kein Tool las sie. Claude musste `fed_id` in jedem Tool-Call manuell mitgeben. Geschlossen via `BaseTool.default_fed_id` (`ENV["CC_FED_ID"]&.to_i`) + `fed_id ||= default_fed_id` in 11 Tools (alle mit `fed_id`-Argument). Schema-Descriptions weisen Claude in der Tool-Liste auf den Default hin. Bei nicht gesetztem ENV bleibt der bestehende `Missing fed_id`-Fehler erhalten — keine Verhaltensänderung an dieser Bahn. Regression-Tests in `test/mcp_server/tools/cc_fed_id_env_default_test.rb` (3 Tools × 2 Pfade = 6 Tests).
    ```

    **Schritt 3 — Pitfall 8-Folgenotiz** (optional, NUR falls noch Platz und es passt; sonst weglassen): keine Änderung — Pitfall 8 betrifft Trap-Kontext, nicht ENV-Lookup.

    **Schritt 4 — mkdocs build**:
    ```
    bin/rails mkdocs:build
    ```

    Erwarteter Effekt: `public/docs/` wird aktualisiert (CLAUDE.md erwähnt `config/initializers/docs_auto_rebuild.rb` mit Listen-Watcher in dev — falls `bin/rails mkdocs:build` nicht direkt verfügbar ist, prüfe `bin/rails -T | grep mkdocs` und nutze den korrekten Task-Namen; Fallback: `Listen` greift im Hintergrund nachdem die Datei in der dev-Session beobachtet wird).

    **Schritt 5 — git status + commit**.

    Files (in dieser Reihenfolge stagen):
    1. `lib/mcp_server/tools/base_tool.rb`
    2. 11 Tool-Dateien (`lib/mcp_server/tools/lookup_*.rb`, `lib/mcp_server/tools/search_player.rb`, `lib/mcp_server/tools/finalize_teilnehmerliste.rb`)
    3. `test/mcp_server/tools/cc_fed_id_env_default_test.rb`
    4. `test/mcp_server/tools/lookup_region_test.rb` (setup-Patch für ENV-Reset)
    5. `docs/developers/clubcloud-mcp-server.de.md`
    6. `public/docs/**` (von mkdocs build, falls vorhanden)

    Conventional commit message:
    ```
    feat(mcp-server): read CC_FED_ID env default in all 11 fed_id-aware tools

    Closes the gap between .mcp.json/claude mcp add --env CC_FED_ID=20 and the
    runtime: no tool was reading the env var, so Claude had to pass fed_id in
    every tool call. BaseTool.default_fed_id is the single ENV lookup site;
    11 tools now apply `fed_id ||= default_fed_id` before validation. Existing
    "Missing fed_id" error path preserved when neither caller nor ENV provides
    fed_id. Schema descriptions advertise the default to Claude in the tool list.

    - lib/mcp_server/tools/base_tool.rb: + default_fed_id class helper
    - 11 tools (10 lookup_*, finalize_teilnehmerliste): + ENV default lookup
    - test/mcp_server/tools/cc_fed_id_env_default_test.rb: 6 regression tests
    - test/mcp_server/tools/lookup_region_test.rb: setup pins ENV nil for
      force_refresh-requires-fed_id stability
    - docs/developers/clubcloud-mcp-server.de.md: BaseTool table row + Section 11
      closure note

    Quick: 260507-m2z

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>git diff --stat HEAD~1 HEAD 2>/dev/null | tail -20 && grep -c "default_fed_id" docs/developers/clubcloud-mcp-server.de.md</automated>
  </verify>
  <done>
    - Section 6 BaseTool-Tabelle hat einen neuen Eintrag `.default_fed_id`.
    - Section 11 hat einen Closure-Block für die CC_FED_ID-ENV-Default-Lücke (Quick-Fix `260507-m2z`).
    - `bin/rails mkdocs:build` lief durch (oder Listen-Watcher hat `public/docs/` aktualisiert) — `git status` zeigt entweder saubere `public/docs/`-Diffs oder keinen Drift.
    - `grep -c "default_fed_id" docs/developers/clubcloud-mcp-server.de.md` ≥ 2 (eine in Section 6, eine in Section 11).
    - Commit existiert mit conventional-commit-message + `Quick: 260507-m2z` + Co-Authored-By-Trailer.
  </done>
</task>

</tasks>

<verification>
**Phase-level checks:**

1. **Code-Path-Smoke**: `bin/rails runner 'puts McpServer::Tools::BaseTool.default_fed_id.inspect'` mit `CC_FED_ID=42` → `42`. Ohne ENV → `nil`.

2. **Tool-Coverage**: Genau 11 Tools mit `fed_id ||= default_fed_id`:
   ```
   grep -l "fed_id ||= default_fed_id" lib/mcp_server/tools/*.rb | wc -l
   # Expected: 11
   ```

3. **Schema-Description-Coverage**: Genau 11 Schema-descriptions mit `Defaults to ENV`:
   ```
   grep -c "Defaults to ENV\['CC_FED_ID'\]" lib/mcp_server/tools/*.rb | grep -v ":0$" | wc -l
   # Expected: 11
   ```

4. **Test-Suite**: `bin/rails test test/mcp_server/tools/` — alle Tests grün (Existing + 6 neue).

5. **No-per-tool-override** (constraint): KEINE Tool-Datei darf eine eigene `default_fed_id`-Definition enthalten:
   ```
   grep -l "def self.default_fed_id" lib/mcp_server/tools/*.rb
   # Expected: nur base_tool.rb (1 Datei)
   ```

6. **No new `default:` schema field** (constraint): keine Tool-Datei führt ein neues `default: ENV[...]` im input_schema ein:
   ```
   grep -n "default:.*ENV\[" lib/mcp_server/tools/*.rb
   # Expected: keine Treffer
   ```
</verification>

<success_criteria>
- BaseTool.default_fed_id liest ENV["CC_FED_ID"]&.to_i (1 Lookup-Stelle).
- 11 Tools wenden `fed_id ||= default_fed_id` an.
- 11 Schema-descriptions weisen auf den ENV-Default hin.
- 6 Regression-Tests grün; bestehende Tests in `test/mcp_server/tools/` unverändert grün.
- `lookup_region_test.rb` ist gegen Host-ENV-Drift abgesichert.
- Doku in Section 6 + Section 11 aktualisiert; mkdocs gebaut.
- Single conventional commit mit Quick-ID `260507-m2z`.
</success_criteria>

<output>
Nach Abschluss: Erstelle `.planning/quick/260507-m2z-cc-fed-id-env-default-in-allen-mcp-tools/260507-m2z-SUMMARY.md` (lokal, gitignored) mit:
- Commit-Hash
- 6 Test-Pfade + Status
- Stichprobenartiger Vergleich `claude --mcp-tools cc_lookup_region --shortname BCW` (vorher fed_id-Pflicht, nachher Default greift)
- Verbleibende Issues / Follow-ups (z.B. WR-04/05 aus Doku Section 11 sind weiterhin offen — nicht in Scope dieses Quick-Tasks)
</output>
