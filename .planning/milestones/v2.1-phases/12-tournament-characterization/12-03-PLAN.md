---
phase: 12-tournament-characterization
plan: "03"
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - test/models/tournament_scraping_test.rb
  - test/snapshots/vcr/tournament_scraping_nbv_2025.yml
autonomous: true
requirements: [CHAR-06]

must_haves:
  truths:
    - "scrape_single_tournament_public returns early for non-Region organizer without making HTTP calls"
    - "scrape_single_tournament_public returns early on API server (carambus_api_url present)"
    - "scrape_single_tournament_public with VCR cassette creates or updates seedings from the registration page"
    - "scrape_single_tournament_public with VCR cassette creates or updates games from the results page"
    - "parse_table_tr variant dispatch routes to the correct variant method based on header columns"
  artifacts:
    - path: "test/models/tournament_scraping_test.rb"
      provides: "Scraping pipeline characterization tests with VCR"
      contains: "class TournamentScrapingTest"
    - path: "test/snapshots/vcr/tournament_scraping_nbv_2025.yml"
      provides: "VCR cassette for NBV tournament scraping"
  key_links:
    - from: "test/models/tournament_scraping_test.rb"
      to: "app/models/tournament.rb"
      via: "scrape_single_tournament_public method"
      pattern: "scrape_single_tournament_public"
    - from: "test/snapshots/vcr/tournament_scraping_nbv_2025.yml"
      to: "test/models/tournament_scraping_test.rb"
      via: "VCR.use_cassette"
      pattern: "VCR\\.use_cassette"
---

<objective>
Create VCR-backed characterization tests for Tournament's scraping pipeline (scrape_single_tournament_public).

Purpose: Pin the scraping pipeline behavior (~420 lines) with VCR cassettes per D-03/D-04 before extraction into Tournament::PublicCcScraper in Phase 14. Tests verify guard conditions, HTTP call sequence, seeding/game creation, and variant dispatch.

Output: `test/models/tournament_scraping_test.rb` with VCR cassettes, all tests green.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-tournament-characterization/12-CONTEXT.md
@.planning/phases/12-tournament-characterization/12-RESEARCH.md

<interfaces>
<!-- Scraping pipeline entry point -->

From app/models/tournament.rb (lines 392-810) — scrape_single_tournament_public:
- Guard 1: returns early if `organizer_type != "Region"` (line 394)
- Guard 2: returns early if `Carambus.config.carambus_api_url.present?` (line 395)
- Makes 4 HTTP calls in sequence via Net::HTTP.get (in test env, not development)
- URL pattern: `{region.public_cc_url_base}/sb_{page}.php?p={region_cc_id}--{season}-{tournament_cc_id}----1-100000-`
- Pages: meisterschaft, meldeliste, einzelergebnisse, einzelrangliste
- opts[:tournament_doc] — pre-parsed Nokogiri doc (skips 1st HTTP call)
- opts[:reload_game_results] — destroys all games before re-scraping
- opts[:reload_seedings] — destroys all seedings before re-scraping

From test/fixtures/tournaments.yml:
- `scraped`: id 50_000_002, source_url "https://nbv.clubcloud.de/sb_meisterschaft.php?p=20--2025-123-0--2-1-100000-"

From research — variant dispatch (parse_table_tr):
- variant0: %w[Partie Begegnung Partien Erg.]
- variant7: %w[Partie Begegnung Aufn. HS GD Erg.]
- 12+ variants total based on header array matching

VCR config (test/support/vcr_setup.rb):
- cassette_library_dir: test/snapshots/vcr/
- record: :once, hook: :webmock
- match: [:method, :uri]
- allow_playback_repeats: true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tournament_scraping_test.rb — VCR-backed scraping pipeline characterization (per D-03, D-04, D-05, CHAR-06)</name>
  <files>test/models/tournament_scraping_test.rb, test/snapshots/vcr/tournament_scraping_nbv_2025.yml</files>
  <read_first>
    - app/models/tournament.rb (lines 392-810 for scrape_single_tournament_public, lines 1050-1300 for parse_table_tr variant dispatch)
    - app/models/tournament_cc.rb (schema — cc_id column)
    - app/models/region.rb (public_cc_url_base method, SHORTNAMES_CC constant)
    - test/support/vcr_setup.rb (VCR configuration)
    - test/fixtures/tournaments.yml (scraped fixture)
    - test/fixtures/regions.yml (nbv fixture)
    - test/fixtures/tournament_ccs.yml (if exists — check for scraped tournament's cc association)
    - test/snapshots/vcr/ (list existing cassettes for pattern reference)
  </read_first>
  <action>
Create `test/models/tournament_scraping_test.rb` with `frozen_string_literal: true`, requiring `test_helper`.

Class: `TournamentScrapingTest < ActiveSupport::TestCase` with `fixtures :all`.

**Important discovery step first**: Before writing VCR tests, the executor MUST:
1. Read the `scraped` fixture tournament's associations — it needs a `tournament_cc` record with a `cc_id` AND the organizer region needs `public_cc_url_base` set. Check if these fixtures exist.
2. If `tournament_ccs.yml` fixture does not exist, create it with a fixture entry for the scraped tournament.
3. Check if `regions(:nbv)` has a `public_cc_url_base` column/value. If not present in fixture, the executor must set it up in the test setup.
4. Read the `scraped` fixture's `source_url` to determine the actual ClubCloud URL pattern.

**Test groups to implement:**

1. **Guard: non-Region organizer returns early** (no HTTP):
   Create a tournament with `organizer_type: "Club"` (or use a Club organizer). Call `scrape_single_tournament_public`. Assert it returns without making HTTP calls (no VCR cassette needed — WebMock will block any unexpected calls).

2. **Guard: API server returns early** (no HTTP):
   Temporarily stub `Carambus.config.carambus_api_url` to return a value (e.g., `"https://api.example.com"`). Call `scrape_single_tournament_public` on a Region-organizer tournament. Assert early return. Restore config in ensure block.

3. **VCR-backed full scrape** (per D-03):
   This is the key test. Per D-03, VCR cassettes should be recorded from real ClubCloud URLs. The executor must:
   
   a. Determine the correct URL that the method will call by reading the tournament's `tournament_cc.cc_id`, the region's `public_cc_url_base`, and the season/region_cc_id parameters.
   
   b. Set up the tournament with all required associations (tournament_cc, region with public_cc_url_base).
   
   c. Use `VCR.use_cassette("tournament_scraping_nbv_2025", record: :new_episodes)` for the first run to record. On subsequent runs, VCR replays.
   
   d. If recording fails (e.g., real URL not accessible), use `record: :none` with a manually created cassette, OR use the `opts[:tournament_doc]` parameter to pass a pre-parsed Nokogiri document to skip the first HTTP call and provide minimal HTML for the remaining 3 calls.
   
   e. After scrape: assert `tournament.seedings.count > 0` (registration page parsed), assert presence of games if the results page had content, assert the tournament's state-related fields are consistent.
   
   **Fallback approach** if live recording is not possible: Create stub cassettes with minimal valid HTML responses for each of the 4 URLs. The HTML structure must match what Nokogiri expects (table rows with the right CSS selectors). Use existing ClubCloud HTML patterns from the codebase or from scraping test helpers.

4. **opts[:tournament_doc] skips first HTTP call**:
   Create a minimal Nokogiri document, pass it as `opts[:tournament_doc]`. Assert only 3 HTTP calls are made (registration, results, ranking — not the tournament page).

5. **opts[:reload_game_results] destroys games first**:
   Set up a tournament with existing games. Call with `reload_game_results: true`. Assert games were destroyed and re-created from the cassette.

6. **opts[:reload_seedings] destroys seedings first**:
   Similar to above for seedings.

7. **Variant dispatch tests** (per D-04 — at least most common variants):
   Test the private `parse_table_tr` method via `send(:parse_table_tr, ...)` with different header arrays. For each variant, create a minimal `<tr>` Nokogiri element with the right number of `<td>` cells and verify the method dispatches correctly. Test at least:
   - `variant0`: header `%w[Partie Begegnung Partien Erg.]`
   - `variant7`: header `%w[Partie Begegnung Aufn. HS GD Erg.]`
   - `Variant4` (capital V): header `%w[Partie Begegnung Pkt. Aufn. HS GD Erg.]`
   
   These test the dispatch logic, not the full parse — just verify the right variant method is called (use `assert_respond_to` or check return values).

**If VCR recording from real URLs is not feasible**: Document this clearly in the test file and use WebMock stubs directly instead of VCR cassettes. The key goal is pinning the behavior, not the recording mechanism. Stub all 4 HTTP calls with minimal valid HTML responses.

Note: The existing VCR config uses `record: :once` by default. For first-time recording, the executor may need to temporarily allow HTTP connections or use `record: :new_episodes`.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/models/tournament_scraping_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test/models/tournament_scraping_test.rb`
    - Contains: `class TournamentScrapingTest < ActiveSupport::TestCase`
    - Contains: `frozen_string_literal: true`
    - Contains tests for: guard (non-Region organizer), guard (API server), VCR or WebMock backed full scrape
    - Contains tests for: at least 2-3 variant dispatch methods via send(:parse_table_tr)
    - Contains VCR cassette or WebMock stubs for the 4 HTTP calls
    - All tests pass with 0 failures, 0 errors
  </acceptance_criteria>
  <done>The scraping pipeline is characterized — guard conditions, HTTP call sequence, seeding/game creation, and variant dispatch are all pinned by passing tests. VCR cassettes (or WebMock stubs as fallback) ensure reproducible HTTP responses.</done>
</task>

<task type="auto">
  <name>Task 2: Run full Phase 12 test suite verification and confirm no regressions</name>
  <files><!-- verification only — no files modified, reads all Phase 12 test outputs --></files>
  <read_first>
    - test/models/tournament_aasm_test.rb
    - test/models/tournament_attributes_test.rb
    - test/models/tournament_papertrail_test.rb
    - test/models/tournament_calendar_test.rb
    - test/models/tournament_scraping_test.rb
  </read_first>
  <action>
Run the full test suite to confirm no regressions were introduced by the five new test files.

```bash
cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api
bin/rails test test/models/tournament_aasm_test.rb test/models/tournament_attributes_test.rb test/models/tournament_papertrail_test.rb test/models/tournament_calendar_test.rb test/models/tournament_scraping_test.rb
```

If any failures, investigate and fix. Then run a broader test to check for interference:

```bash
bin/rails test test/models/
```

Confirm the existing `tournament_test.rb` (51 lines) still passes untouched per D-02.

Count total tests across all 5 new files. Document the count in the summary.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/models/tournament_aasm_test.rb test/models/tournament_attributes_test.rb test/models/tournament_papertrail_test.rb test/models/tournament_calendar_test.rb test/models/tournament_scraping_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - All 5 new tournament characterization test files pass with 0 failures, 0 errors
    - Existing tournament_test.rb passes unchanged
    - No regressions in test/models/ directory
  </acceptance_criteria>
  <done>All Phase 12 characterization tests pass. No regressions introduced. Total test count documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test env -> ClubCloud HTTP | VCR cassettes record real HTTP for replay; no live calls during test runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | I (Information Disclosure) | VCR cassettes | mitigate | VCR cassettes may contain real ClubCloud HTML — ensure no credentials or PII are in recorded responses; review cassette content before commit |
| T-12-04 | T (Tampering) | WebMock | accept | WebMock blocks all unrecorded HTTP in tests — no accidental live calls |
</threat_model>

<verification>
```bash
cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api
bin/rails test test/models/tournament_aasm_test.rb test/models/tournament_attributes_test.rb test/models/tournament_papertrail_test.rb test/models/tournament_calendar_test.rb test/models/tournament_scraping_test.rb
```
All 5 test files pass with 0 failures, 0 errors. No regressions in existing tests.
</verification>

<success_criteria>
- VCR cassette (or WebMock stubs) successfully replay scraping HTTP interactions
- Guard conditions (non-Region, API server) verified
- At least 2-3 variant dispatch methods tested
- Full suite verification confirms no regressions
- All 5 Phase 12 test files pass together
</success_criteria>

<output>
After completion, create `.planning/phases/12-tournament-characterization/12-03-SUMMARY.md`
</output>
