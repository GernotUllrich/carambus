---
phase: 14-medium-risk-extractions
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/tournament/public_cc_scraper.rb
  - test/services/tournament/public_cc_scraper_test.rb
  - app/models/tournament.rb
  - test/models/tournament_scraping_test.rb
autonomous: true
requirements: [TEXT-03]
must_haves:
  truths:
    - "Tournament::PublicCcScraper exists as an ApplicationService and executes the scraping pipeline"
    - "Tournament delegates scrape_single_tournament_public to PublicCcScraper with a 1-line wrapper"
    - "The scraping pipeline produces identical records to the Phase 12 characterization baseline"
    - "tournament_scraping_test.rb parse_table_tr tests are updated to call send on the service instance instead of Tournament"
    - "All Phase 12 characterization tests pass after extraction"
  artifacts:
    - path: "app/services/tournament/public_cc_scraper.rb"
      provides: "ApplicationService with scrape_single_tournament_public, parse_table_tr, handle_game, variant0-8, result_with_* methods"
      contains: "class Tournament::PublicCcScraper < ApplicationService"
    - path: "test/services/tournament/public_cc_scraper_test.rb"
      provides: "WebMock-backed tests for scraper service"
      contains: "class Tournament::PublicCcScraperTest"
    - path: "app/models/tournament.rb"
      provides: "1-line delegation wrapper for scrape_single_tournament_public"
      contains: "PublicCcScraper.call"
    - path: "test/models/tournament_scraping_test.rb"
      provides: "Updated parse_table_tr tests calling service instance instead of tournament"
      contains: "PublicCcScraper"
  key_links:
    - from: "app/models/tournament.rb"
      to: "app/services/tournament/public_cc_scraper.rb"
      via: "delegation wrapper"
      pattern: "PublicCcScraper\\.call"
    - from: "test/models/tournament_scraping_test.rb"
      to: "app/services/tournament/public_cc_scraper.rb"
      via: "parse_table_tr tests updated to call service"
      pattern: "PublicCcScraper\\.new"
---

<objective>
Extract PublicCcScraper ApplicationService from Tournament and wire delegation

Purpose: Remove ~800+ lines of scraping pipeline code from Tournament into a dedicated ApplicationService — the largest single extraction in the v2.1 milestone. This includes scrape_single_tournament_public, parse_table_tr, handle_game, all variant dispatch methods, and result parsing helpers. The delegation wrapper preserves the public API on Tournament. CRITICAL: tournament_scraping_test.rb's parse_table_tr tests use `@tournament.send(:parse_table_tr, ...)` which must be updated to call the service instance.

Output: `app/services/tournament/public_cc_scraper.rb`, updated tests, delegation wrapper on Tournament, Tournament reduced by ~800 lines
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-medium-risk-extractions/14-CONTEXT.md
@.planning/phases/14-medium-risk-extractions/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/services/tournament/table_reservation_service.rb (ApplicationService pattern):
```ruby
class Tournament::TableReservationService < ApplicationService
  def initialize(kwargs = {})
    @tournament = kwargs[:tournament]
  end

  def call
    # main logic
  end

  private
  # helper methods
end
```

From app/models/tournament.rb — methods to MOVE (per 14-RESEARCH.md complete method map):
```
scrape_single_tournament_public  (lines 392-810, ~419 lines) — becomes `call` method
parse_table_tr                   (lines 991-1096, ~106 lines)
handle_game                      (lines 1469-1581, ~113 lines)
parse_table_td                   (lines 1280-1467, ~188 lines) — DEAD CODE, move anyway
variant0                         (lines 1098-1106, 9 lines)
variant8                         (lines 1108-1117, 10 lines)
variant7                         (lines 1119-1130, 12 lines)
variant6                         (lines 1132-1142, 11 lines)
variant5                         (lines 1144-1153, 10 lines)
Variant4                         (lines 1155-1166, 12 lines) — capital V, keep as-is (Pitfall 2)
variant3                         (lines 1168-1196, 29 lines)
variant2                         (lines 1198-1207, 10 lines)
result_with_party_variant2       (lines 1209-1228, 20 lines)
result_with_party_variant        (lines 1230-1250, 21 lines)
result_with_party                (lines 1252-1259, 8 lines)
result_with_parties              (lines 1261-1268, 8 lines)
result_with_frames               (lines 1270-1278, 9 lines)
fix_location_from_location_text  (lines 812-819, 8 lines) — DEAD CODE, move with scraper
```

Methods that STAY on Tournament:
- deep_merge_data!, reset_tournament (AASM callbacks)
- calculate_and_cache_rankings (delegated to RankingCalculator)
- fallback_table_count (used by TableReservationService)
- before_all_events (AASM callback)
- scrape_single_tournament_public becomes 1-line delegation wrapper

CRITICAL: test/models/tournament_scraping_test.rb line 338 calls:
  @tournament.send(:parse_table_tr, ...)
After extraction, parse_table_tr lives on the service. This test must be updated.

Callers of scrape_single_tournament_public (all preserved by delegation wrapper):
- TournamentsController#reload_from_cc (line 140)
- VersionsController (line 72)
- Region#scrape_single_tournament_public (lines 573, 960)
- test/scraping/scraping_smoke_test.rb (lines 55, 67, 84, 101, 121, 171)
- test/scraping/tournament_scraper_test.rb (line 121)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PublicCcScraper ApplicationService with all scraping methods</name>
  <files>app/services/tournament/public_cc_scraper.rb</files>
  <read_first>
    - app/models/tournament.rb (lines 392-1581 — ALL methods being extracted. Read the entire file.)
    - app/services/tournament/table_reservation_service.rb (ApplicationService pattern to follow)
    - .planning/phases/14-medium-risk-extractions/14-RESEARCH.md (complete method map, self→@tournament conversions, pitfalls)
  </read_first>
  <action>
    Create `app/services/tournament/public_cc_scraper.rb` as an ApplicationService (per D-01).

    1. Class: `Tournament::PublicCcScraper < ApplicationService`
    2. Constructor: `def initialize(kwargs = {})` storing `@tournament = kwargs[:tournament]` and `@opts = kwargs[:opts] || {}`
    3. `def call` — copy the entire body of `scrape_single_tournament_public` (lines 392-810). Apply these conversions throughout:
       - `self.source_url =` → `@tournament.source_url =`
       - `self.shortname =` → `@tournament.shortname =`
       - `self.date =` → `@tournament.date =`
       - `self.location =` → `@tournament.location =`
       - `self.accredation_end =` → `@tournament.accredation_end =`
       - `self.discipline_id =` → `@tournament.discipline_id =`
       - `self.region_id =` → `@tournament.region_id =`
       - `self.id` → `@tournament.id`
       - All association calls: `seedings` → `@tournament.seedings`, `games` → `@tournament.games`, `tournament_cc` → `@tournament.tournament_cc`
       - All attribute reads: `organizer_type` → `@tournament.organizer_type`, `organizer` → `@tournament.organizer`, `season` → `@tournament.season`, `title` → `@tournament.title`, `discipline` → `@tournament.discipline`, `data` → `@tournament.data`, `source_url` → `@tournament.source_url`, etc.
       - `save!` → `@tournament.save!`, `reload` → `@tournament.reload`, `save` → `@tournament.save`, `changed?` → `@tournament.changed?`
       - `opts = {}` parameter already captured from constructor `@opts`; replace `opts` with `@opts` throughout
       - The rescue block (line 807-809): `reset_tournament` → `@tournament.reset_tournament` (Pitfall 3)
       - The `return if organizer_type != "Region"` guard → `return if @tournament.organizer_type != "Region"`
       - The `return if Carambus.config.carambus_api_url.present?` guard stays as-is (global config check)
    4. Private methods — copy verbatim from tournament.rb with self→@tournament conversions:
       - `parse_table_tr` (lines 991-1096) — receives explicit params, minimal self refs
       - `handle_game` (lines 1469-1581) — `self.id` → `@tournament.id`, `games` → `@tournament.games`, `discipline` → `@tournament.discipline`, `seedings` → `@tournament.seedings`
       - `parse_table_td` (lines 1280-1467) — DEAD CODE but move with scraper for completeness
       - `variant0` through `variant8` (lines 1098-1278) — no self refs, copy verbatim
       - `Variant4` (lines 1155-1166) — KEEP CAPITAL V (Pitfall 2)
       - `result_with_party_variant2`, `result_with_party_variant`, `result_with_party`, `result_with_parties`, `result_with_frames` — no self refs, copy verbatim
       - `fix_location_from_location_text` (lines 812-819) — DEAD CODE, `location_text` → `@tournament.location_text`, `location` → `@tournament.location`
    5. `frozen_string_literal: true` at top
    6. German docstring comment explaining the service purpose

    CRITICAL: Do NOT add any extra `@tournament.save!` in the delegation wrapper. The scraper's own save calls are the canonical saves (Pitfall 6 — PaperTrail double-version risk).

    CRITICAL: The `Variant4` method name starts with a capital V. Keep it exactly as-is. Ruby will treat it as a method call inside the class (Pitfall 2).
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && ruby -c app/services/tournament/public_cc_scraper.rb</automated>
  </verify>
  <acceptance_criteria>
    - File `app/services/tournament/public_cc_scraper.rb` exists with `class Tournament::PublicCcScraper < ApplicationService`
    - Constructor stores `@tournament` and `@opts`
    - `def call` contains the full scrape_single_tournament_public logic
    - All `self.x` references converted to `@tournament.x`
    - rescue block calls `@tournament.reset_tournament` (not bare `reset_tournament`)
    - `Variant4` method name preserved with capital V
    - `parse_table_tr`, `handle_game`, `parse_table_td` present as private methods
    - All variant methods (variant0-8) present
    - All result_with_* methods present
    - `fix_location_from_location_text` present
    - File is syntactically valid Ruby (`ruby -c` passes)
    - File has `frozen_string_literal: true` header
  </acceptance_criteria>
  <done>PublicCcScraper ApplicationService exists with all ~20 methods faithfully moved, syntactically valid, all self→@tournament conversions applied</done>
</task>

<task type="auto">
  <name>Task 2: Wire delegation in Tournament, update tests, verify full suite</name>
  <files>app/models/tournament.rb, test/models/tournament_scraping_test.rb, test/services/tournament/public_cc_scraper_test.rb</files>
  <read_first>
    - app/models/tournament.rb (full file — identify all methods to remove)
    - app/services/tournament/public_cc_scraper.rb (just created in Task 1)
    - test/models/tournament_scraping_test.rb (lines 316-354 — the call_parse_table_tr helper that needs updating)
    - test/models/tournament_scraping_test.rb (full file — understand all tests)
    - test/models/tournament_aasm_test.rb (must pass unchanged)
  </read_first>
  <action>
    **Part A: Wire delegation in tournament.rb**

    1. Replace `scrape_single_tournament_public` method (lines 392-810) with a 1-line delegation wrapper per D-02:
       ```ruby
       def scrape_single_tournament_public(opts = {})
         Tournament::PublicCcScraper.call(tournament: self, opts: opts)
       end
       ```
    2. Remove all private methods that moved to the service:
       - `parse_table_tr` (lines 991-1096)
       - `handle_game` (lines 1469-1581)
       - `parse_table_td` (lines 1280-1467)
       - `variant0` through `variant8`, `Variant4` (lines 1098-1278)
       - `result_with_party_variant2`, `result_with_party_variant`, `result_with_party`, `result_with_parties`, `result_with_frames`
       - `fix_location_from_location_text` (lines 812-819)
    3. Keep ALL other methods — `deep_merge_data!`, `reset_tournament`, `calculate_and_cache_rankings`, `fallback_table_count`, `before_all_events`, `match_location_from_location_text`, etc.

    **Part B: Update tournament_scraping_test.rb**

    The `call_parse_table_tr` helper (lines 316-354) uses `@tournament.send(:parse_table_tr, ...)`. After extraction, `parse_table_tr` is on the service. Update this helper to:
    ```ruby
    def call_parse_table_tr(header, tr)
      # Build service instance for calling private parse_table_tr
      scraper = Tournament::PublicCcScraper.new(tournament: @tournament)
      # ... same variable setup ...
      out = scraper.send(
        :parse_table_tr,
        region, frame1_lines, frame_points, frame_result, frames, gd, group, hb,
        header, hs, mp, innings, nbsp, no, player_list, playera_fl_name, playerb_fl_name,
        points, result, result_lines, result_url, td_lines, tr
      )
      # ... same hash mapping ...
    end
    ```

    **Part C: Create basic service-level test**

    Create `test/services/tournament/public_cc_scraper_test.rb` with at least one WebMock-backed test verifying that `Tournament::PublicCcScraper.call(tournament: t, opts: {})` executes without error for a tournament with the correct setup. Follow the WebMock stub pattern from tournament_scraping_test.rb.

    **Part D: Run full verification suite**

    Verify D-10: All Phase 11-12 characterization tests pass without modification.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/models/tournament_scraping_test.rb test/models/tournament_aasm_test.rb test/services/tournament/public_cc_scraper_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - tournament.rb `scrape_single_tournament_public` is exactly 3 lines (def, `PublicCcScraper.call(tournament: self, opts: opts)`, end)
    - Methods `parse_table_tr`, `handle_game`, `parse_table_td`, `variant0`-`variant8`, `Variant4`, all `result_with_*`, `fix_location_from_location_text` are NOT present in tournament.rb
    - `deep_merge_data!`, `reset_tournament`, `calculate_and_cache_rankings`, `fallback_table_count` REMAIN in tournament.rb
    - tournament.rb line count reduced by ~800 lines (from 1594 to ~794)
    - tournament_scraping_test.rb `call_parse_table_tr` uses `Tournament::PublicCcScraper.new(tournament: @tournament)` and `.send(:parse_table_tr, ...)` on the service instance
    - tournament_scraping_test.rb all tests pass
    - tournament_aasm_test.rb passes without modification
    - public_cc_scraper_test.rb exists and passes
    - No delegation wrapper adds extra `save!` (PaperTrail double-version risk per Pitfall 6)
  </acceptance_criteria>
  <done>Tournament delegates to PublicCcScraper, ~800 lines removed, all characterization tests pass, tournament_scraping_test.rb updated for service-based parse_table_tr access</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP→PublicCcScraper | Scraper fetches external HTML from ClubCloud — untrusted input parsed by Nokogiri |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | PublicCcScraper HTML parsing | accept | Existing Nokogiri parsing unchanged — faithful extraction preserves all existing input handling and error recovery |
| T-14-04 | Information Disclosure | PublicCcScraper error logging | accept | Existing logging pattern preserved — `Tournament.logger.info` with backtrace is existing behavior |
| T-14-05 | Denial of Service | PublicCcScraper HTTP requests | accept | Existing Net::HTTP usage unchanged — no new timeout or retry logic introduced |
</threat_model>

<verification>
1. `bin/rails test test/services/tournament/public_cc_scraper_test.rb` — new service tests pass
2. `bin/rails test test/models/tournament_scraping_test.rb` — characterization tests pass (with updated parse_table_tr helper)
3. `bin/rails test test/models/tournament_aasm_test.rb` — AASM tests pass unchanged
4. `wc -l app/models/tournament.rb` — reduced from 1594 to ~794 lines
5. `grep -c "parse_table_tr\|handle_game\|variant0\|Variant4" app/models/tournament.rb` — returns 0 (all methods removed)
6. `grep "PublicCcScraper.call" app/models/tournament.rb` — delegation wrapper present
</verification>

<success_criteria>
- Tournament::PublicCcScraper ApplicationService exists with all ~20 scraping methods (D-01)
- Tournament has 1-line delegation wrapper (D-02)
- Maximum line reduction achieved (D-03)
- tournament_scraping_test.rb parse_table_tr tests updated to call service instance
- All Phase 12 characterization tests pass (D-10)
- Tournament reduced from 1594 to ~794 lines
</success_criteria>

<output>
After completion, create `.planning/phases/14-medium-risk-extractions/14-02-SUMMARY.md`
</output>
