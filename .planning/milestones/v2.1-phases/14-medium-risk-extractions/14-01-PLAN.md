---
phase: 14-medium-risk-extractions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/tournament_monitor/ranking_resolver.rb
  - test/services/tournament_monitor/ranking_resolver_test.rb
  - app/models/tournament_monitor.rb
autonomous: true
requirements: [TMEX-02]
must_haves:
  truths:
    - "TournamentMonitor::RankingResolver exists as a PORO and resolves player IDs from ranking rule strings"
    - "TournamentMonitor delegates player_id_from_ranking to RankingResolver transparently"
    - "RankingResolver calls PlayerGroupDistributor.distribute_to_group directly (not through TournamentMonitor wrapper) per D-05"
    - "All Phase 11-12 characterization tests pass without modification after extraction"
  artifacts:
    - path: "app/services/tournament_monitor/ranking_resolver.rb"
      provides: "PORO with player_id_from_ranking, ko_ranking, group_rank, random_from_group_ranks, rank_from_group_ranks"
      contains: "class TournamentMonitor::RankingResolver"
    - path: "test/services/tournament_monitor/ranking_resolver_test.rb"
      provides: "Unit tests for ranking resolution algorithm"
      contains: "class TournamentMonitor::RankingResolverTest"
    - path: "app/models/tournament_monitor.rb"
      provides: "Delegation wrapper for player_id_from_ranking"
      contains: "RankingResolver.new(self)"
  key_links:
    - from: "app/models/tournament_monitor.rb"
      to: "app/services/tournament_monitor/ranking_resolver.rb"
      via: "delegation wrapper"
      pattern: "RankingResolver\\.new\\(self\\)"
    - from: "app/services/tournament_monitor/ranking_resolver.rb"
      to: "app/services/tournament_monitor/player_group_distributor.rb"
      via: "direct cross-service call in group_rank"
      pattern: "PlayerGroupDistributor\\.distribute_to_group"
---

<objective>
Extract RankingResolver PORO from TournamentMonitor and wire delegation

Purpose: Remove ~173 lines of regex rule parsing from TournamentMonitor into a dedicated PORO service, following the Phase 13 PlayerGroupDistributor extraction pattern. This is the second TournamentMonitor extraction and introduces the first cross-service dependency between extracted services (RankingResolver calls PlayerGroupDistributor directly per D-05).

Output: `app/services/tournament_monitor/ranking_resolver.rb`, unit tests, delegation wrapper on TournamentMonitor
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-medium-risk-extractions/14-CONTEXT.md
@.planning/phases/14-medium-risk-extractions/14-RESEARCH.md
@.planning/phases/13-low-risk-extractions/13-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/services/tournament_monitor/player_group_distributor.rb:
```ruby
class TournamentMonitor::PlayerGroupDistributor
  def self.distribute_to_group(players, ngroups, group_sizes = nil)
  def self.distribute_with_sizes(players, ngroups, group_sizes)
end
```

From app/models/tournament_monitor.rb (lines 135-163 + 179-337 — methods to extract):
```ruby
# CLASS METHOD — stays on model:
def self.ranking(hash, opts = {})

# PUBLIC — becomes delegation wrapper:
def player_id_from_ranking(rule_str, opts = {})

# PRIVATE — all move to RankingResolver:
def ko_ranking(rule_str)
def group_rank(match)
def random_from_group_ranks(match, ordered_ranking_nos, rule_str)
def rank_from_group_ranks(match, opts = {})
```

Key callers of player_id_from_ranking (all go through delegation wrapper — no changes needed):
- lib/tournament_monitor_support.rb:656, 724 — populate_tables
- lib/tournament_monitor_support.rb:284, 290 — update_ranking
- test/models/tournament_monitor_ko_test.rb:90 — characterization test
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RankingResolver PORO with unit tests</name>
  <files>app/services/tournament_monitor/ranking_resolver.rb, test/services/tournament_monitor/ranking_resolver_test.rb</files>
  <read_first>
    - app/models/tournament_monitor.rb (lines 145-337 — the methods being extracted)
    - app/services/tournament_monitor/player_group_distributor.rb (Phase 13 PORO pattern to follow)
    - test/services/tournament_monitor/player_group_distributor_test.rb (test pattern to follow)
    - test/models/tournament_monitor_ko_test.rb (characterization tests — understand what they verify)
  </read_first>
  <behavior>
    - Test 1: player_id_from_ranking resolves "sl.rk1" to first seeding player_id (seeding list resolution)
    - Test 2: player_id_from_ranking resolves "g1.2" to 2nd player in group 1 (group_rank path)
    - Test 3: player_id_from_ranking returns nil on invalid/unrecognized rule string (rescue path)
    - Test 4: player_id_from_ranking resolves "rule1" by following opts[:executor_params]["rules"]["rule1"] (recursive rule resolution)
    - Test 5: group_rank calls PlayerGroupDistributor.distribute_to_group directly (not TournamentMonitor.distribute_to_group) per D-05
  </behavior>
  <action>
    Create `app/services/tournament_monitor/ranking_resolver.rb` as a PORO (per D-04). Follow the PlayerGroupDistributor pattern exactly:

    1. Class: `TournamentMonitor::RankingResolver`
    2. Constructor: `def initialize(tournament_monitor)` storing `@tournament_monitor`
    3. Public method: `def player_id_from_ranking(rule_str, opts = {})` — copy from tournament_monitor.rb lines 145-163, converting:
       - `tournament` → `@tournament_monitor.tournament`
       - `data` → `@tournament_monitor.data`
       - Keep rescue block with `Tournament.logger.info` verbatim (behavior preservation)
    4. Private methods — copy verbatim from tournament_monitor.rb:
       - `ko_ranking` (lines 181-227): `data` → `@tournament_monitor.data`, `tournament` → `@tournament_monitor.tournament`
       - `group_rank` (lines 229-247): `tournament` → `@tournament_monitor.tournament`. CRITICAL per D-05: change `TournamentMonitor.distribute_to_group(...)` to `TournamentMonitor::PlayerGroupDistributor.distribute_to_group(...)` — direct cross-service call
       - `random_from_group_ranks` (lines 249-291): `tournament` → `@tournament_monitor.tournament`, `data` → `@tournament_monitor.data`
       - `rank_from_group_ranks` (lines 293-337): same conversions. The recursive `player_id_from_ranking(...)` call stays as a plain method call within the service object (no change needed per Pitfall 4)
    5. `TournamentMonitor.ranking(...)` class method calls stay as-is — it remains on the model (Pitfall 5)
    6. `frozen_string_literal: true` at top
    7. German docstring comment explaining the service purpose

    Then create unit tests in `test/services/tournament_monitor/ranking_resolver_test.rb`. Use fixtures to set up a TournamentMonitor with populated `data["rankings"]` hash. Test each resolution path. Verify D-05 by stubbing or asserting that PlayerGroupDistributor.distribute_to_group is called (not TournamentMonitor.distribute_to_group).
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/tournament_monitor/ranking_resolver_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - File `app/services/tournament_monitor/ranking_resolver.rb` exists with `class TournamentMonitor::RankingResolver`
    - File contains `def initialize(tournament_monitor)` and `@tournament_monitor = tournament_monitor`
    - Public method `player_id_from_ranking` present
    - Private methods `ko_ranking`, `group_rank`, `random_from_group_ranks`, `rank_from_group_ranks` present
    - `group_rank` contains `PlayerGroupDistributor.distribute_to_group` (NOT `TournamentMonitor.distribute_to_group`)
    - All `data` references use `@tournament_monitor.data`
    - All `tournament` references use `@tournament_monitor.tournament`
    - `TournamentMonitor.ranking(...)` calls remain fully qualified (class method stays on model)
    - rescue block in player_id_from_ranking uses `Tournament.logger.info` verbatim
    - Unit tests pass with zero failures
  </acceptance_criteria>
  <done>RankingResolver PORO exists with all 5 methods extracted, unit tests pass, cross-service call to PlayerGroupDistributor is direct per D-05</done>
</task>

<task type="auto">
  <name>Task 2: Wire delegation in TournamentMonitor and verify characterization tests</name>
  <files>app/models/tournament_monitor.rb</files>
  <read_first>
    - app/models/tournament_monitor.rb (current state — lines 145-337 to be replaced)
    - app/services/tournament_monitor/ranking_resolver.rb (just created in Task 1)
    - test/models/tournament_monitor_ko_test.rb (must pass unchanged)
    - test/models/tournament_monitor_t04_test.rb (must pass unchanged)
    - test/models/tournament_monitor_t06_test.rb (must pass unchanged)
  </read_first>
  <action>
    Replace the method bodies in tournament_monitor.rb with a delegation wrapper:

    1. Replace `player_id_from_ranking` (lines 145-163) with:
       ```ruby
       def player_id_from_ranking(rule_str, opts = {})
         TournamentMonitor::RankingResolver.new(self).player_id_from_ranking(rule_str, opts)
       end
       ```
    2. Remove private methods that moved to the service:
       - `ko_ranking` (lines 181-227) — DELETE
       - `group_rank` (lines 229-247) — DELETE
       - `random_from_group_ranks` (lines 249-291) — DELETE
       - `rank_from_group_ranks` (lines 293-337) — DELETE
    3. Keep `self.ranking` class method (line 135) — it stays on the model (Pitfall 5)
    4. Keep `next_seqno` (line 165) — not part of ranking resolver
    5. Keep `self.distribute_to_group` and `self.distribute_with_sizes` delegation wrappers (lines 171-177) — still needed by other callers
    6. Keep `before_all_events` and `broadcast_status_update` — unrelated

    Per D-06: TournamentMonitor gets a delegation wrapper for `player_id_from_ranking`. Private methods move entirely to the service.

    Verify all Phase 11-12 characterization tests pass without modification (D-10).
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/models/tournament_monitor_ko_test.rb test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb test/services/tournament_monitor/ranking_resolver_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `player_id_from_ranking` in tournament_monitor.rb is exactly 3 lines (def, delegation call, end)
    - Private methods `ko_ranking`, `group_rank`, `random_from_group_ranks`, `rank_from_group_ranks` are NOT present in tournament_monitor.rb
    - `self.ranking` class method REMAINS in tournament_monitor.rb
    - `self.distribute_to_group` and `self.distribute_with_sizes` delegation wrappers REMAIN
    - `next_seqno` REMAINS
    - tournament_monitor.rb line count reduced by ~157 lines (from 349 to ~192)
    - tournament_monitor_ko_test.rb passes without modification
    - tournament_monitor_t04_test.rb passes without modification
    - tournament_monitor_t06_test.rb passes without modification
    - ranking_resolver_test.rb passes
  </acceptance_criteria>
  <done>TournamentMonitor delegates to RankingResolver, ~157 lines removed, all characterization tests pass unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure internal refactoring — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | RankingResolver rule parsing | accept | Existing regex parsing unchanged — faithful extraction preserves existing input handling |
| T-14-02 | Denial of Service | Recursive player_id_from_ranking | accept | Existing recursion depth unchanged — rank_from_group_ranks calls player_id_from_ranking with finite rule set |
</threat_model>

<verification>
1. `bin/rails test test/services/tournament_monitor/ranking_resolver_test.rb` — new unit tests pass
2. `bin/rails test test/models/tournament_monitor_ko_test.rb test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb` — characterization tests pass unchanged
3. `wc -l app/models/tournament_monitor.rb` — reduced from 349 to ~192 lines
4. `grep -c "PlayerGroupDistributor.distribute_to_group" app/services/tournament_monitor/ranking_resolver.rb` — returns 1 (D-05 verified)
5. `grep -c "TournamentMonitor.distribute_to_group" app/services/tournament_monitor/ranking_resolver.rb` — returns 0 (D-05 verified — no indirect call)
</verification>

<success_criteria>
- TournamentMonitor::RankingResolver PORO exists with all 5 methods
- group_rank calls PlayerGroupDistributor directly (D-05)
- TournamentMonitor has 1-line delegation wrapper (D-06)
- All characterization tests pass without modification (D-10)
- TournamentMonitor reduced by ~157 lines
</success_criteria>

<output>
After completion, create `.planning/phases/14-medium-risk-extractions/14-01-SUMMARY.md`
</output>
