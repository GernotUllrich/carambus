---
phase: 16-controller-job-channel-coverage
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified: []
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "Tournament model is under 1000 lines"
    - "Full test suite passes with zero failures and zero errors"
    - "PaperTrail version counts match Phase 12 baselines"
  artifacts: []
  key_links:
    - from: "test/models/tournament_papertrail_test.rb"
      to: "app/models/tournament.rb"
      via: "PaperTrail version count assertions"
      pattern: "PaperTrail::Version"
---

<objective>
Verify all quality gates: Tournament line count under 1000, full test suite green, PaperTrail version baselines unchanged.

Purpose: QUAL-01, QUAL-02, QUAL-03 are the final quality verification gates for the v2.1 milestone. This plan runs the verification checks and documents the results.
Output: Verification results confirming all quality gates pass.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-controller-job-channel-coverage/16-01-SUMMARY.md
@.planning/phases/16-controller-job-channel-coverage/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quality gate verification</name>
  <files></files>
  <read_first>
    - app/models/tournament.rb (line count check)
    - test/models/tournament_papertrail_test.rb (PaperTrail baselines)
  </read_first>
  <action>
Run three verification checks and report results:

1. **QUAL-01 — Tournament line count:**
   ```bash
   wc -l app/models/tournament.rb
   ```
   Assert output shows fewer than 1000 lines. The research confirmed 575 lines — this is a sanity check that no regressions occurred.

2. **QUAL-02 — Full test suite green:**
   ```bash
   bin/rails test
   ```
   Assert 0 failures and 0 errors in the output. Record total run count and skip count.

3. **QUAL-03 — PaperTrail version baselines:**
   ```bash
   bin/rails test test/models/tournament_papertrail_test.rb -v
   ```
   Assert all PaperTrail baseline tests pass. These were established in Phase 12 and must remain unchanged.

If any check fails:
- QUAL-01 failure: Report the actual line count; this would indicate unexpected code was added to tournament.rb.
- QUAL-02 failure: Identify which tests fail. If failures are in the new Phase 16 test files, they must be fixed. If failures are in pre-existing tests, document and investigate.
- QUAL-03 failure: Report which PaperTrail assertion failed. Check if the new controller tests created unexpected PaperTrail versions (transactions should prevent this).

Document all three results in the SUMMARY.
  </action>
  <verify>
    <automated>wc -l app/models/tournament.rb | grep -E "^\s*[0-9]+" && bin/rails test 2>&1 | grep -E "runs.*failures.*errors" | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Tournament model line count is under 1000 (expected: ~575)
    - `bin/rails test` output contains "0 failures, 0 errors"
    - `bin/rails test test/models/tournament_papertrail_test.rb` passes with 0 failures
  </acceptance_criteria>
  <done>All three quality gates pass: Tournament under 1000 lines, full suite green, PaperTrail baselines unchanged. QUAL-01, QUAL-02, QUAL-03 are satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — verification only, no code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | N/A | verification only | accept | No production code changes |
</threat_model>

<verification>
- Tournament model < 1000 lines
- Full test suite: 0 failures, 0 errors
- PaperTrail baselines: all pass
</verification>

<success_criteria>
- All three QUAL requirements verified and passing
- v2.1 milestone quality gates met
</success_criteria>

<output>
After completion, create `.planning/phases/16-controller-job-channel-coverage/16-03-SUMMARY.md`
</output>
