---
phase: 15-high-risk-extractions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/tournament_monitor/result_processor.rb
  - test/services/tournament_monitor/result_processor_test.rb
  - app/models/tournament_monitor.rb
  - lib/tournament_monitor_support.rb
  - lib/tournament_monitor_state.rb
autonomous: true
requirements: [TMEX-03]
must_haves:
  truths:
    - "TournamentMonitor::ResultProcessor exists with all result pipeline methods extracted from lib modules"
    - "DB lock scope (game.with_lock) is preserved exactly — same 4 operations inside lock, everything else outside"
    - "AASM events fire on @tournament_monitor model reference, not on the service"
    - "accumulate_results is public on ResultProcessor and delegated from TournamentMonitor model"
    - "All Phase 11-12 characterization tests pass unchanged after extraction"
  artifacts:
    - path: "app/services/tournament_monitor/result_processor.rb"
      provides: "Result pipeline service with DB lock + AASM event firing"
      contains: "class TournamentMonitor::ResultProcessor"
    - path: "test/services/tournament_monitor/result_processor_test.rb"
      provides: "Unit tests for ResultProcessor public methods"
      contains: "class TournamentMonitor::ResultProcessorTest"
  key_links:
    - from: "app/models/tournament_monitor.rb"
      to: "app/services/tournament_monitor/result_processor.rb"
      via: "delegation wrappers for report_result, accumulate_results, update_ranking, update_game_participations"
      pattern: "ResultProcessor\\.new\\(self\\)"
    - from: "app/services/tournament_monitor/result_processor.rb"
      to: "app/models/tournament_monitor.rb"
      via: "@tournament_monitor AASM bang methods and deep_merge_data!"
      pattern: "@tournament_monitor\\."
---

<objective>
Extract ResultProcessor from TournamentMonitor lib modules — the result processing pipeline including report_result, write_game_result_data, finalize_game_result, accumulate_results, update_ranking, and supporting methods.

Purpose: Remove the highest-risk result pipeline code from lib modules into a testable service, preserving DB lock scope and AASM event firing behavior exactly.
Output: ResultProcessor service + unit tests + delegation wrappers on TournamentMonitor model
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-high-risk-extractions/15-CONTEXT.md
@.planning/phases/15-high-risk-extractions/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/models/tournament_monitor.rb (current, 181 lines):
- `include TournamentMonitorSupport` (lib/tournament_monitor_support.rb)
- `include TournamentMonitorState` (lib/tournament_monitor_state.rb)
- `cattr_accessor :current_admin, :allow_change_tables`
- `serialize :data, coder: JSON, type: Hash`
- `def deep_merge_data!(hash)` — stays on model
- `def current_round` / `incr_current_round!` / `decr_current_round!` — stay on model
- `def player_id_from_ranking(rule_str, opts)` — delegates to RankingResolver
- `def next_seqno` — stays on model
- `self.ranking(hash, opts)` — class method, stays on model

From app/services/tournament_monitor/ranking_resolver.rb (Phase 14 pattern):
- Plain class (no ApplicationService), `def initialize(tournament_monitor)`, `@tournament_monitor` accessor
- German docstring, `frozen_string_literal: true`
- Public: `player_id_from_ranking(rule_str, opts)`
- Private methods for specific resolution logic

From lib/tournament_monitor_support.rb — methods to extract (D-03, D-05):
- `report_result(table_monitor)` line 183 — main entry point
- `update_game_participations(tabmon)` line 5
- `update_game_participations_for_game(game, data)` line 11
- `accumulate_results` line 88 — SHARED: also called by populate_tables (Pitfall A)
- `add_result_to(gp, hash)` line 140
- `update_ranking` line 275
- `write_finale_csv_for_upload` line 301

From lib/tournament_monitor_state.rb — methods to extract (D-04):
- `write_game_result_data(table_monitor)` line 7
- `finalize_game_result(table_monitor)` line 45
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ResultProcessor service with all extracted methods + unit tests</name>
  <files>
    app/services/tournament_monitor/result_processor.rb
    test/services/tournament_monitor/result_processor_test.rb
  </files>
  <read_first>
    lib/tournament_monitor_support.rb (lines 1-310 for report_result chain, lines 88-180 for accumulate_results/add_result_to/update_ranking)
    lib/tournament_monitor_state.rb (lines 1-100 for write_game_result_data and finalize_game_result)
    app/services/tournament_monitor/ranking_resolver.rb (pattern reference — PORO with @tournament_monitor)
    app/models/tournament_monitor.rb (model interface — deep_merge_data!, current_round, etc.)
    test/services/tournament_monitor/ranking_resolver_test.rb (test pattern reference)
  </read_first>
  <behavior>
    - Test 1: ResultProcessor.new(tournament_monitor) creates instance with @tournament_monitor accessor
    - Test 2: report_result delegates to write_game_result_data, finalize_game_result, accumulate_results sequence
    - Test 3: accumulate_results (public) reads/writes @tournament_monitor.data["rankings"] and calls save!
    - Test 4: update_ranking calls player_id_from_ranking via @tournament_monitor delegation
    - Test 5: update_game_participations_for_game updates GameParticipation records
  </behavior>
  <action>
    Create `app/services/tournament_monitor/result_processor.rb` as a plain class (NOT ApplicationService — per research recommendation, follows RankingResolver pattern since multiple public entry points exist, not a single `call`).

    Per D-01: Receives TournamentMonitor instance in constructor. Store as `@tournament_monitor`.

    Per D-03, extract these methods from `lib/tournament_monitor_support.rb`:
    - `report_result(table_monitor)` — PUBLIC. The DB lock (`game.with_lock`) stays inside this method per D-01. Inside the lock: table_monitor.reload, game.reload, write_game_result_data(table_monitor), game.reload, table_monitor.reload, table_monitor.finish_match!. Outside the lock: finalize_game_result, accumulate_results, update_ranking, populate_tables delegation, AASM events.
    - `update_game_participations(tabmon)` — private
    - `update_game_participations_for_game(game, data)` — private
    - `accumulate_results` — PUBLIC (called by TablePopulator via model delegation per Pitfall A)
    - `add_result_to(gp, hash)` — private
    - `update_ranking` — PUBLIC (called by model delegation wrapper)

    Per D-05: `write_finale_csv_for_upload` — private (only called by report_result)

    Per D-04, extract from `lib/tournament_monitor_state.rb`:
    - `write_game_result_data(table_monitor)` — private
    - `finalize_game_result(table_monitor)` — private

    **self-to-@tournament_monitor conversion** (per D-09 and research Pattern 1):
    - Every bare `reload` -> `@tournament_monitor.reload`
    - Every bare `save!` -> `@tournament_monitor.save!`
    - Every bare `deep_merge_data!(h)` -> `@tournament_monitor.deep_merge_data!(h)`
    - Every bare `data` -> `@tournament_monitor.data`
    - Every bare `data_will_change!` -> `@tournament_monitor.data_will_change!`
    - Every bare `tournament` -> `@tournament_monitor.tournament`
    - Every bare `incr_current_round!` -> `@tournament_monitor.incr_current_round!`
    - Every bare `decr_current_round!` -> `@tournament_monitor.decr_current_round!`
    - Every bare `current_round` -> `@tournament_monitor.current_round`
    - Every bare `table_monitors` -> `@tournament_monitor.table_monitors`
    - Every bare `sets_to_play` -> `@tournament_monitor.sets_to_play`
    - Every bare `innings_goal` -> `@tournament_monitor.innings_goal`
    - Every bare `balls_goal` -> `@tournament_monitor.balls_goal`
    - AASM events per D-02: `end_of_tournament!` -> `@tournament_monitor.end_of_tournament!`, `start_playing_finals!` -> `@tournament_monitor.start_playing_finals!`, `start_playing_groups!` -> `@tournament_monitor.start_playing_groups!`
    - State queries stay on model (D-08): `all_table_monitors_finished?` -> `@tournament_monitor.all_table_monitors_finished?`, `finalize_round` -> `@tournament_monitor.finalize_round`, `group_phase_finished?` -> `@tournament_monitor.group_phase_finished?`, `finals_finished?` -> `@tournament_monitor.finals_finished?`
    - `populate_tables` called from report_result: -> `@tournament_monitor.populate_tables` (delegation to TablePopulator, will exist after Plan 02; for now calls the lib module method via model)
    - `player_id_from_ranking` -> `@tournament_monitor.player_id_from_ranking` (already delegates to RankingResolver)
    - `try do` blocks must be preserved exactly (Pitfall G — ActiveSupport Object#try, not begin/rescue)
    - `Tournament.logger` references kept verbatim (established pattern from Phase 13)
    - `self.class.current_admin` / `self.class.current_user` -> `TournamentMonitor.current_admin` / use `@tournament_monitor.class` for class-level accessors

    German docstring at top of file following RankingResolver pattern.
    `frozen_string_literal: true` as first line.

    Create `test/services/tournament_monitor/result_processor_test.rb` with unit tests verifying:
    - Instance creation and @tournament_monitor reference
    - accumulate_results reads data["rankings"]["groups"] and writes back
    - update_ranking calls player_id_from_ranking via @tournament_monitor
    - update_game_participations_for_game updates GP records
    - report_result contains game.with_lock block (structural verification)
    Use stubs/mocks for TournamentMonitor to avoid full DB setup in unit tests.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/tournament_monitor/result_processor_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - File `app/services/tournament_monitor/result_processor.rb` exists and contains `class TournamentMonitor::ResultProcessor`
    - `report_result` is a public method containing `game.with_lock` block
    - `accumulate_results` is a public method (not private)
    - `update_ranking` is a public method
    - All methods use `@tournament_monitor.` prefix for model calls, never bare `self` for model operations
    - No `ApplicationService` inheritance — plain class with `def initialize(tournament_monitor)`
    - `frozen_string_literal: true` as first line
    - Test file exists at `test/services/tournament_monitor/result_processor_test.rb`
    - All unit tests pass
  </acceptance_criteria>
  <done>ResultProcessor service exists with all 9 extracted methods (report_result, write_game_result_data, finalize_game_result, update_game_participations, update_game_participations_for_game, accumulate_results, add_result_to, update_ranking, write_finale_csv_for_upload). DB lock scope preserved. Unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ResultProcessor delegation in TournamentMonitor + remove lib module methods</name>
  <files>
    app/models/tournament_monitor.rb
    lib/tournament_monitor_support.rb
    lib/tournament_monitor_state.rb
  </files>
  <read_first>
    app/services/tournament_monitor/result_processor.rb (just created in Task 1)
    app/models/tournament_monitor.rb (current model — add delegation wrappers)
    lib/tournament_monitor_support.rb (identify methods to remove: lines 5-310)
    lib/tournament_monitor_state.rb (identify methods to remove: lines 7-100)
    test/models/tournament_monitor_t04_test.rb (characterization — must pass unchanged)
    test/models/tournament_monitor_t06_test.rb (characterization — must pass unchanged)
    test/models/tournament_monitor_ko_test.rb (characterization — must pass unchanged)
  </read_first>
  <action>
    **Step 1: Add delegation wrappers to `app/models/tournament_monitor.rb`** (BEFORE removing lib methods — Pitfall F):

    Add these public delegation methods after the existing `player_id_from_ranking` wrapper:

    ```ruby
    # Delegation wrappers — result pipeline lives in TournamentMonitor::ResultProcessor
    def report_result(table_monitor)
      TournamentMonitor::ResultProcessor.new(self).report_result(table_monitor)
    end

    def update_game_participations(tabmon)
      TournamentMonitor::ResultProcessor.new(self).update_game_participations(tabmon)
    end

    def accumulate_results
      TournamentMonitor::ResultProcessor.new(self).accumulate_results
    end

    def update_ranking
      TournamentMonitor::ResultProcessor.new(self).update_ranking
    end
    ```

    **Step 2: Run characterization tests WITH both lib methods AND delegation wrappers present** to verify delegation works before removing originals:
    ```bash
    bin/rails test test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb test/models/tournament_monitor_ko_test.rb
    ```

    **Step 3: Remove extracted methods from lib modules:**

    From `lib/tournament_monitor_support.rb` remove:
    - `update_game_participations(tabmon)` (line 5)
    - `update_game_participations_for_game(game, data)` (line 11)
    - `accumulate_results` (line 88)
    - `add_result_to(gp, hash)` (line 140)
    - `report_result(table_monitor)` (line 183)
    - `update_ranking` (line 275)
    - `write_finale_csv_for_upload` (line 301)

    From `lib/tournament_monitor_state.rb` remove:
    - `write_game_result_data(table_monitor)` (line 7)
    - `finalize_game_result(table_monitor)` (line 45)

    **Step 4: Run characterization tests again to verify removal did not break anything.**

    **Step 5: Run full ResultProcessor unit tests to confirm everything still works.**
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb test/models/tournament_monitor_ko_test.rb test/services/tournament_monitor/result_processor_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/tournament_monitor.rb` contains delegation wrappers for `report_result`, `update_game_participations`, `accumulate_results`, `update_ranking` — each calling `TournamentMonitor::ResultProcessor.new(self).method_name`
    - `lib/tournament_monitor_support.rb` no longer contains `report_result`, `accumulate_results`, `add_result_to`, `update_ranking`, `write_finale_csv_for_upload`, `update_game_participations`, `update_game_participations_for_game`
    - `lib/tournament_monitor_state.rb` no longer contains `write_game_result_data`, `finalize_game_result`
    - All 3 characterization tests pass unchanged (0 failures, 0 errors)
    - ResultProcessor unit tests pass
  </acceptance_criteria>
  <done>Delegation wrappers on TournamentMonitor delegate to ResultProcessor. Original lib module methods removed. All characterization + unit tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service-to-model | ResultProcessor calls AASM events and save! on @tournament_monitor — must preserve lock scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | ResultProcessor DB lock scope | mitigate | Preserve game.with_lock block exactly as in original — same 4 operations inside lock, everything else outside |
| T-15-02 | Denial of Service | Concurrent report_result calls | accept | Existing pessimistic lock handles concurrency — no change in behavior |
</threat_model>

<verification>
1. `bin/rails test test/services/tournament_monitor/result_processor_test.rb` — all unit tests pass
2. `bin/rails test test/models/tournament_monitor_t04_test.rb test/models/tournament_monitor_t06_test.rb test/models/tournament_monitor_ko_test.rb` — all characterization tests pass unchanged
3. `grep -c "def report_result" lib/tournament_monitor_support.rb` returns 0
4. `grep -c "def write_game_result_data" lib/tournament_monitor_state.rb` returns 0
5. `grep -c "ResultProcessor.new(self)" app/models/tournament_monitor.rb` returns 4
</verification>

<success_criteria>
- ResultProcessor service exists with all 9 methods from D-03, D-04, D-05
- DB lock scope in report_result is identical to the original
- AASM events fire on @tournament_monitor (D-02)
- accumulate_results is public for cross-service access (Pitfall A)
- All characterization tests pass unchanged (D-10)
- Delegation wrappers on model for report_result, accumulate_results, update_ranking, update_game_participations
</success_criteria>

<output>
After completion, create `.planning/phases/15-high-risk-extractions/15-01-SUMMARY.md`
</output>
