---
phase: 04-tablemonitor-gamesetup-optionspresenter
plan: 04
type: execute
wave: 1
depends_on: ["04-02"]
files_modified:
  - app/services/table_monitor/game_setup.rb
  - app/reflexes/table_monitor_reflex.rb
  - app/reflexes/game_protocol_reflex.rb
  - app/controllers/tournament_monitors_controller.rb
  - app/models/table_monitor.rb
  - test/services/table_monitor/game_setup_test.rb
  - test/characterization/table_monitor_char_test.rb
autonomous: true
gap_closure: true
requirements: [TMON-02, TMON-04]

must_haves:
  truths:
    - "The skip_update_callbacks name is completely gone from all production and test code"
    - "All call sites use suppress_broadcast instead of skip_update_callbacks"
    - "The alias_method shims are removed from TableMonitor"
    - "All existing tests pass with the renamed flag"
  artifacts:
    - path: "app/models/table_monitor.rb"
      provides: "suppress_broadcast attr_writer without alias shims"
      contains: "attr_writer :suppress_broadcast"
    - path: "app/services/table_monitor/game_setup.rb"
      provides: "GameSetup using suppress_broadcast directly"
      contains: "suppress_broadcast"
    - path: "app/reflexes/table_monitor_reflex.rb"
      provides: "Reflex using suppress_broadcast"
      contains: "suppress_broadcast"
    - path: "app/reflexes/game_protocol_reflex.rb"
      provides: "GameProtocol reflex using suppress_broadcast"
      contains: "suppress_broadcast"
    - path: "app/controllers/tournament_monitors_controller.rb"
      provides: "Controller using suppress_broadcast"
      contains: "suppress_broadcast"
  key_links:
    - from: "app/reflexes/table_monitor_reflex.rb"
      to: "app/models/table_monitor.rb"
      via: "suppress_broadcast= accessor"
      pattern: "\\.suppress_broadcast\\s*="
    - from: "app/services/table_monitor/game_setup.rb"
      to: "app/models/table_monitor.rb"
      via: "suppress_broadcast= accessor"
      pattern: "@tm\\.suppress_broadcast\\s*="
---

<objective>
Close the SC #2 verification gap: fully remove the `skip_update_callbacks` name from the codebase and replace all call sites with `suppress_broadcast`. This is a mechanical rename across 7 files (~79 occurrences total) followed by removing the alias_method shims from TableMonitor.

Purpose: SC #2 requires "The skip_update_callbacks flag is gone" — the current implementation kept it alive via alias shims. This plan completes the rename per D-04/D-05.
Output: Zero occurrences of `skip_update_callbacks` in app/ and test/ directories.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tablemonitor-gamesetup-optionspresenter/04-02-SUMMARY.md
@.planning/phases/04-tablemonitor-gamesetup-optionspresenter/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all skip_update_callbacks call sites to suppress_broadcast</name>
  <files>
    app/services/table_monitor/game_setup.rb,
    app/reflexes/table_monitor_reflex.rb,
    app/reflexes/game_protocol_reflex.rb,
    app/controllers/tournament_monitors_controller.rb
  </files>
  <read_first>
    app/services/table_monitor/game_setup.rb,
    app/reflexes/table_monitor_reflex.rb,
    app/reflexes/game_protocol_reflex.rb,
    app/controllers/tournament_monitors_controller.rb
  </read_first>
  <action>
    Perform a mechanical rename in all four files. Replace every occurrence of `skip_update_callbacks` with `suppress_broadcast`. The patterns to replace are:

    In game_setup.rb (3 occurrences):
    - Line 24: `@tm.skip_update_callbacks = true` -> `@tm.suppress_broadcast = true`
    - Line 28: `@tm.skip_update_callbacks = false` -> `@tm.suppress_broadcast = false`
    - Line 90: `@tm.skip_update_callbacks = false` -> `@tm.suppress_broadcast = false`

    In table_monitor_reflex.rb (46 occurrences):
    - Replace all `@table_monitor.skip_update_callbacks = true` with `@table_monitor.suppress_broadcast = true`
    - Replace all `@table_monitor.skip_update_callbacks = false` with `@table_monitor.suppress_broadcast = false`

    In game_protocol_reflex.rb (26 occurrences):
    - Replace all `@table_monitor.skip_update_callbacks = true` with `@table_monitor.suppress_broadcast = true`
    - Replace all `@table_monitor.skip_update_callbacks = false` with `@table_monitor.suppress_broadcast = false`

    In tournament_monitors_controller.rb (4 occurrences):
    - Replace all `table_monitor.skip_update_callbacks = true` with `table_monitor.suppress_broadcast = true`
    - Replace all `table_monitor.skip_update_callbacks = false` with `table_monitor.suppress_broadcast = false`

    This is a pure text substitution. Do NOT change any logic, line ordering, or surrounding code.
    Per D-04: replacing skip_update_callbacks with the explicit suppress_broadcast name.
  </action>
  <verify>
    <automated>grep -r "skip_update_callbacks" app/services/table_monitor/game_setup.rb app/reflexes/table_monitor_reflex.rb app/reflexes/game_protocol_reflex.rb app/controllers/tournament_monitors_controller.rb; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "skip_update_callbacks" app/services/ app/reflexes/ app/controllers/` returns zero matches
    - `grep -c "suppress_broadcast" app/reflexes/table_monitor_reflex.rb` returns 46
    - `grep -c "suppress_broadcast" app/reflexes/game_protocol_reflex.rb` returns 26
    - `grep -c "suppress_broadcast" app/services/table_monitor/game_setup.rb` returns at least 3
    - `grep -c "suppress_broadcast" app/controllers/tournament_monitors_controller.rb` returns 4
  </acceptance_criteria>
  <done>All production code call sites use suppress_broadcast; zero occurrences of skip_update_callbacks in app/ directory except the model (which still has alias shims until Task 2)</done>
</task>

<task type="auto">
  <name>Task 2: Remove alias shims from TableMonitor and update tests</name>
  <files>
    app/models/table_monitor.rb,
    test/services/table_monitor/game_setup_test.rb,
    test/characterization/table_monitor_char_test.rb
  </files>
  <read_first>
    app/models/table_monitor.rb,
    test/services/table_monitor/game_setup_test.rb,
    test/characterization/table_monitor_char_test.rb
  </read_first>
  <action>
    Step 1: In app/models/table_monitor.rb, remove the two alias_method lines and their comment:
    - Remove line 78: `# Transitional shim — reflex still uses skip_update_callbacks at 30+ call sites.`
    - Remove line 79: `# Remove when reflex is updated in a future phase.`
    - Remove line 80: `alias_method :skip_update_callbacks=, :suppress_broadcast=`
    - Remove line 81: `alias_method :skip_update_callbacks, :suppress_broadcast`

    Step 2: In test/services/table_monitor/game_setup_test.rb, rename all references:
    - Line 186 test name: change "skip_update_callbacks=true" to "suppress_broadcast=true"
    - Line 192: `callbacks_during << @tm.skip_update_callbacks` -> `callbacks_during << @tm.suppress_broadcast`
    - Line 199: "skip_update_callbacks muss" -> "suppress_broadcast muss"
    - Line 201: `@tm.skip_update_callbacks` -> `@tm.suppress_broadcast`
    - Line 240 test name: change "skip_update_callbacks=false" to "suppress_broadcast=false"
    - Line 250-251: `@tm.skip_update_callbacks` -> `@tm.suppress_broadcast` and update message string

    Step 3: In test/characterization/table_monitor_char_test.rb, rename references:
    - Line 255 test name: change "skip_update_callbacks" to "suppress_broadcast"
    - Line 258: `@tm.skip_update_callbacks = true` -> `@tm.suppress_broadcast = true`
    - Line 183 comment: update "skip_update_callbacks" to "suppress_broadcast" in the comment

    Do NOT change any test logic or assertions — only rename the accessor and update descriptive strings.
  </action>
  <verify>
    <automated>grep -r "skip_update_callbacks" app/ test/ --include="*.rb"; echo "EXIT:$?" && bin/rails test test/characterization/table_monitor_char_test.rb test/services/table_monitor/game_setup_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "skip_update_callbacks" app/ test/ --include="*.rb"` returns zero matches
    - `grep -c "alias_method" app/models/table_monitor.rb` does NOT include skip_update_callbacks aliases
    - `bin/rails test test/characterization/table_monitor_char_test.rb` passes (0 failures)
    - `bin/rails test test/services/table_monitor/game_setup_test.rb` passes (0 failures)
  </acceptance_criteria>
  <done>The skip_update_callbacks name is completely removed from the codebase. All tests pass using suppress_broadcast directly. SC #2 gap is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This is a pure rename refactoring.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | T (Tampering) | suppress_broadcast flag | accept | Same instance-variable flag as before, just renamed; no new attack surface |
</threat_model>

<verification>
1. `grep -r "skip_update_callbacks" app/ test/ --include="*.rb"` returns zero matches
2. `bin/rails test test/characterization/table_monitor_char_test.rb` — all 41 tests pass
3. `bin/rails test test/services/table_monitor/game_setup_test.rb` — all 10 tests pass
4. `ruby -c app/models/table_monitor.rb` — Syntax OK
5. `ruby -c app/reflexes/table_monitor_reflex.rb` — Syntax OK
6. `ruby -c app/reflexes/game_protocol_reflex.rb` — Syntax OK
</verification>

<success_criteria>
- Zero occurrences of `skip_update_callbacks` in app/ and test/ directories
- All characterization tests pass without modification of test logic
- All GameSetup unit tests pass with renamed accessor
- The alias_method shims are gone from table_monitor.rb
- SC #2 truth is now satisfiable: "The skip_update_callbacks flag is gone"
</success_criteria>

<output>
After completion, create `.planning/phases/04-tablemonitor-gamesetup-optionspresenter/04-04-SUMMARY.md`
</output>
