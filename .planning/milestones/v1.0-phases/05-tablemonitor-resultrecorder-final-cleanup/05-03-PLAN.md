---
phase: 05-tablemonitor-resultrecorder-final-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - .planning/reek_post_extraction_table_monitor.txt
autonomous: true
requirements:
  - TMON-06
must_haves:
  truths:
    - "All 4 extracted services (ScoreEngine, GameSetup, OptionsPresenter, ResultRecorder) have passing unit tests"
    - "Full test suite (characterization + unit) passes with zero failures"
    - "Reek post-extraction report shows measurable reduction from 781 baseline"
    - "TableMonitor line count is under 1550 lines"
  artifacts:
    - path: ".planning/reek_post_extraction_table_monitor.txt"
      provides: "Post-extraction Reek report"
  key_links:
    - from: ".planning/reek_post_extraction_table_monitor.txt"
      to: ".planning/reek_baseline_table_monitor.txt"
      via: "comparison"
      pattern: "warning count comparison"
---

<objective>
Verify full test coverage for all extracted services and run Reek final measurement.

Purpose: Confirm that all 4 extracted TableMonitor services have passing unit tests (TMON-06) and capture the final Reek measurement to quantify quality improvement from Phase 1 baseline (D-08). This is the final verification step for the entire refactoring project.

Output: Verified test suite, Reek post-extraction report committed to .planning/.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tablemonitor-resultrecorder-final-cleanup/05-02-SUMMARY.md
@.planning/reek_baseline_table_monitor.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify full test coverage for all 4 extracted services</name>
  <files></files>
  <read_first>
    - test/services/table_monitor/game_setup_test.rb (existing — verify passes)
    - test/services/table_monitor/result_recorder_test.rb (created in Plan 01 — verify passes)
    - test/models/table_monitor/score_engine_test.rb (existing — verify passes)
    - test/models/table_monitor/options_presenter_test.rb (existing — verify passes)
    - test/characterization/table_monitor_char_test.rb (regression safety net — verify passes)
  </read_first>
  <action>
    Run the complete test suite for all 4 extracted TableMonitor services plus characterization tests in a single command:

    ```bash
    bin/rails test test/models/table_monitor/ test/services/table_monitor/ test/characterization/table_monitor_char_test.rb -v
    ```

    Expected: All tests pass (0 failures, 0 errors).

    If any test fails:
    - Read the failure output carefully
    - If it's a delegation issue from Plan 01 or 02, fix the delegation wrapper
    - If it's a pre-existing failure, document it but do not block

    After all tests pass, record the test count summary (runs, assertions, failures, errors).

    Also run `wc -l app/models/table_monitor.rb` and confirm under 1550 lines.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/models/table_monitor/ test/services/table_monitor/ test/characterization/table_monitor_char_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - bin/rails test test/models/table_monitor/ test/services/table_monitor/ test/characterization/table_monitor_char_test.rb exits with 0
    - wc -l app/models/table_monitor.rb shows under 1550
    - Test output shows 0 failures, 0 errors
  </acceptance_criteria>
  <done>All 4 service test files + characterization tests pass; TableMonitor under 1550 lines confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Run Reek final measurement and save report</name>
  <files>.planning/reek_post_extraction_table_monitor.txt</files>
  <read_first>
    - .planning/reek_baseline_table_monitor.txt (Phase 1 baseline: 781 warnings)
  </read_first>
  <action>
    Run Reek on the post-extraction TableMonitor and save the report per D-08:

    ```bash
    reek app/models/table_monitor.rb > .planning/reek_post_extraction_table_monitor.txt 2>&1
    ```

    Note: Reek exit code 2 is expected (means smells found — this is a known god-object, just smaller now).

    After saving, compare:
    - Baseline: 781 warnings (from `.planning/reek_baseline_table_monitor.txt`)
    - Post-extraction: count warnings in new report with `grep -c "warning" .planning/reek_post_extraction_table_monitor.txt` or `wc -l`

    Document the reduction percentage in the summary. The reduction should be significant given ~1000+ lines removed.

    Also capture final line count: `wc -l app/models/table_monitor.rb`
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && test -f .planning/reek_post_extraction_table_monitor.txt && echo "PASS: Reek report exists" || echo "FAIL: Reek report missing"</automated>
  </verify>
  <acceptance_criteria>
    - test -f .planning/reek_post_extraction_table_monitor.txt (file exists)
    - wc -l .planning/reek_post_extraction_table_monitor.txt shows fewer lines than .planning/reek_baseline_table_monitor.txt
  </acceptance_criteria>
  <done>Reek post-extraction report saved; measurable reduction from 781-warning baseline confirmed; final TableMonitor line count documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan is verification-only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Integrity | Test coverage verification | accept | Tests verify behavior preservation, not exhaustive security coverage; characterization tests are the primary safety net |
</threat_model>

<verification>
1. `bin/rails test test/models/table_monitor/ test/services/table_monitor/ test/characterization/table_monitor_char_test.rb` — 0 failures
2. `.planning/reek_post_extraction_table_monitor.txt` exists with fewer warnings than baseline
3. `wc -l app/models/table_monitor.rb` — under 1550 lines
</verification>

<success_criteria>
- All 4 extracted service test suites pass
- Characterization tests pass (regression safety net)
- Reek report shows measurable improvement from 781-warning baseline
- TableMonitor confirmed under 1550 lines
- Phase 5 and the v1.0 milestone are complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-tablemonitor-resultrecorder-final-cleanup/05-03-SUMMARY.md`
</output>
