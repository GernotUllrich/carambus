---
phase: 24-data-source-investigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tmp/probe_umbevents.rb
  - tmp/probe_sooplive.rb
  - tmp/samples/umbevents/.keep
  - tmp/samples/cuesco/.keep
  - tmp/samples/sooplive/.keep
autonomous: true
requirements:
  - INVEST-01
  - INVEST-03

must_haves:
  truths:
    - "umbevents.umb-carom.org/Reports/ response format is documented (JSON or HTML-only) for at least 3 endpoint paths with multiple Accept header combinations"
    - "billiards.sooplive.com URL pattern probes are documented with HTTP status codes and content types for at least 3 guessed API paths"
    - "Probe script output is saved to tmp/ for consolidation into findings"
  artifacts:
    - path: "tmp/probe_umbevents.rb"
      provides: "Net::HTTP probe script for umbevents endpoints"
    - path: "tmp/probe_sooplive.rb"
      provides: "Net::HTTP probe script for SoopLive URL pattern guesses"
  key_links: []
---

<objective>
Probe umbevents.umb-carom.org and billiards.sooplive.com programmatically to determine whether structured JSON endpoints exist.

Purpose: These two sources are accessible from the dev environment. Script-based probing can confirm or rule out JSON API availability before any browser DevTools inspection is needed. Results feed into the findings document (Plan 02).
Output: Probe scripts in tmp/, console output documenting HTTP responses, sample responses saved if JSON found.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-data-source-investigation/24-CONTEXT.md
@.planning/phases/24-data-source-investigation/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe umbevents.umb-carom.org endpoints for JSON responses</name>
  <files>tmp/probe_umbevents.rb</files>
  <read_first>
    - app/services/umb_scraper_v2.rb (lines 57-91 for fetch_url pattern)
    - .planning/phases/24-data-source-investigation/24-RESEARCH.md (probe specifications)
  </read_first>
  <action>
Create `tmp/probe_umbevents.rb` — a throwaway Ruby script (NOT committed to app/) that probes the umbevents.umb-carom.org/Reports/ endpoints.

Per D-01 (Claude's discretion on approach) and D-07 (existence check only), the script must:

1. Define a `fetch_url` method copied from UmbScraperV2 (lines 57-91) with SSL VERIFY_NONE and 15s timeout
2. Probe these endpoint paths:
   - `/Reports/ViewAllRanks`
   - `/Reports/ViewTimetable`
   - `/Reports/ViewPlayers`
3. For each path, try these header combinations:
   - Default (no special headers)
   - `Accept: application/json`
   - `Accept: application/json` + `X-Requested-With: XMLHttpRequest`
   - `Accept: application/json, text/javascript, */*; q=0.01` + `X-Requested-With: XMLHttpRequest`
4. For each path, also try with query parameters: `?id=1`, `?event_id=1`, `?year=2025`
5. For each probe, print: URL, headers used, HTTP status code, Content-Type header, first 300 chars of response body
6. If any response has Content-Type containing "json", save the full response body to `tmp/samples/umbevents/`

Per D-08 (1-2 sample responses), do not exhaustively test — the goal is to determine if JSON is available at all.

Run the script with: `bundle exec ruby tmp/probe_umbevents.rb`

Capture the full console output and save it to `tmp/probe_umbevents_results.txt` for use in the findings document.

Create `tmp/samples/umbevents/` directory before running.
  </action>
  <verify>
    <automated>test -f tmp/probe_umbevents.rb && test -f tmp/probe_umbevents_results.txt && echo "PASS: probe script and results exist"</automated>
  </verify>
  <acceptance_criteria>
    - File `tmp/probe_umbevents.rb` exists and contains `fetch_url` method
    - File `tmp/probe_umbevents.rb` probes at least 3 URL paths with at least 3 header combinations each
    - File `tmp/probe_umbevents_results.txt` exists with HTTP status codes and Content-Type headers for each probe
    - Console output shows whether any endpoint returned JSON (Content-Type containing "application/json")
  </acceptance_criteria>
  <done>umbevents probe complete: every endpoint path tested with multiple Accept headers and query params; results captured to tmp/probe_umbevents_results.txt; JSON or HTML-only verdict clear from output</done>
</task>

<task type="auto">
  <name>Task 2: Probe billiards.sooplive.com for structured API endpoints</name>
  <files>tmp/probe_sooplive.rb</files>
  <read_first>
    - app/services/sooplive_scraper.rb (existing SoopLive scraper patterns)
    - .planning/phases/24-data-source-investigation/24-RESEARCH.md (SoopLive probe specifications, Task C section)
  </read_first>
  <action>
Create `tmp/probe_sooplive.rb` — a throwaway Ruby script that probes billiards.sooplive.com for hidden JSON API endpoints.

Per D-01, D-07 (existence check), and D-08 (1-2 samples):

1. Copy the same `fetch_url` method from Task 1
2. Try these URL pattern guesses (from RESEARCH.md Pattern C):
   - `https://billiards.sooplive.com/api/schedule/127`
   - `https://billiards.sooplive.com/schedule/127/matches`
   - `https://billiards.sooplive.com/schedule/127.json`
   - `https://billiards.sooplive.com/api/schedule/127/matches`
   - `https://billiards.sooplive.com/schedule/api/127`
   - `https://api.sooplive.com/schedule/127`
3. For each URL, try with default headers and with `Accept: application/json` + `X-Requested-With: XMLHttpRequest`
4. Also fetch `https://billiards.sooplive.com/schedule/127?sub1=result` with WebFetch-style inspection:
   - Look for any `<script>` tags that contain API URL patterns (e.g., `/api/`, `fetch(`, `$.ajax`, `$.get`)
   - Look for `data-seq` and `data-broad_no` attribute patterns in the HTML
5. Print: URL, HTTP status, Content-Type, first 300 chars of body for each probe
6. If any JSON response found, save to `tmp/samples/sooplive/`

Additionally, use WebFetch on `https://billiards.sooplive.com/schedule/127?sub1=result` to extract any JavaScript that reveals API endpoint URLs. Look specifically for:
- `fetch(` or `$.ajax` or `$.get` calls in inline scripts
- References to API paths like `/api/`, `/schedule/`, `/matches/`
- jsRender template data-source URLs

Run with: `bundle exec ruby tmp/probe_sooplive.rb`

Save output to `tmp/probe_sooplive_results.txt`.

Create `tmp/samples/sooplive/` directory before running.
  </action>
  <verify>
    <automated>test -f tmp/probe_sooplive.rb && test -f tmp/probe_sooplive_results.txt && echo "PASS: SoopLive probe script and results exist"</automated>
  </verify>
  <acceptance_criteria>
    - File `tmp/probe_sooplive.rb` exists and probes at least 6 URL pattern guesses
    - File `tmp/probe_sooplive_results.txt` exists with HTTP status codes for each probed URL
    - Results document whether any URL returned JSON content
    - HTML inspection results document whether inline scripts reveal API endpoint URLs
  </acceptance_criteria>
  <done>SoopLive probe complete: URL pattern guesses tested, HTML inspected for API URL patterns, results captured to tmp/probe_sooplive_results.txt; clear indication of whether programmatic API discovery succeeded or browser DevTools are required</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> external sites | Outbound HTTP to third-party tournament sites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | I (Information Disclosure) | tmp/probe scripts | accept | Scripts are throwaway, not committed; tmp/ is gitignored; no credentials involved |
| T-24-02 | D (Denial of Service) | external sites | mitigate | Probe scripts use 15s timeout, try at most ~30 requests total, no retry loops |
</threat_model>

<verification>
- tmp/probe_umbevents.rb exists and runs without error
- tmp/probe_sooplive.rb exists and runs without error
- tmp/probe_umbevents_results.txt contains HTTP response data for umbevents endpoints
- tmp/probe_sooplive_results.txt contains HTTP response data for SoopLive URL guesses
</verification>

<success_criteria>
- umbevents endpoints probed with multiple Accept headers and query params; JSON or HTML-only verdict clear
- SoopLive URL patterns probed; API discovery succeeded or documented as requiring browser DevTools
- All probe results saved to tmp/ for consolidation in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/24-data-source-investigation/24-01-SUMMARY.md`
</output>
