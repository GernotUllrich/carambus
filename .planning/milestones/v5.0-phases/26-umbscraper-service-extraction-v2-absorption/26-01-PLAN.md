---
phase: 26-umbscraper-service-extraction-v2-absorption
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/umb/http_client.rb
  - app/services/umb/player_resolver.rb
  - app/services/umb/discipline_detector.rb
  - app/services/umb/date_helpers.rb
  - test/services/umb/player_resolver_test.rb
  - test/services/umb/discipline_detector_test.rb
  - test/services/umb/date_helpers_test.rb
  - test/services/umb/http_client_test.rb
autonomous: true
requirements: [SCRP-06, SCRP-07]
must_haves:
  truths:
    - "Umb::HttpClient can download PDF content and return extracted text"
    - "Umb::PlayerResolver finds existing players by caps+mixed name strategy (Western and Asian name orders)"
    - "Umb::PlayerResolver creates new InternationalPlayer records when no match found"
    - "Umb::DisciplineDetector.detect returns a Discipline record from tournament name string"
    - "Date parsing helpers correctly handle UMB date formats (ranges, single dates, month names)"
  artifacts:
    - path: "app/services/umb/http_client.rb"
      provides: "fetch_pdf_text method added to existing HttpClient"
      contains: "def fetch_pdf_text"
    - path: "app/services/umb/player_resolver.rb"
      provides: "Player lookup and creation from PDF data"
      contains: "class Umb::PlayerResolver"
    - path: "app/services/umb/discipline_detector.rb"
      provides: "Tournament name to Discipline record mapping"
      contains: "class Umb::DisciplineDetector"
    - path: "app/services/umb/date_helpers.rb"
      provides: "Shared date parsing used by FutureScraper and ArchiveScraper"
      contains: "module Umb::DateHelpers"
  key_links:
    - from: "app/services/umb/player_resolver.rb"
      to: "Player model"
      via: "ActiveRecord queries"
      pattern: "Player\\.where"
    - from: "app/services/umb/discipline_detector.rb"
      to: "Discipline model"
      via: "ActiveRecord ILIKE lookup"
      pattern: "Discipline\\."
---

<objective>
Extract foundation services from UmbScraper: PlayerResolver (absorbing V2's superior caps+mixed name strategy per D-09), DisciplineDetector (consolidating duplicate discipline methods), DateHelpers (shared date parsing), and add `fetch_pdf_text` to HttpClient. These are leaf-node dependencies used by all higher-level services.

Purpose: Establish independently testable foundation layer before extracting the HTML scrapers and PDF parsers that depend on them.
Output: Four service files + tests, all green.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-umbscraper-service-extraction-v2-absorption/26-RESEARCH.md

<interfaces>
<!-- Existing Umb::HttpClient (Phase 25) — to be extended with fetch_pdf_text -->
From app/services/umb/http_client.rb:
```ruby
class Umb::HttpClient
  TIMEOUT = 30
  def self.ssl_verify_mode
  def fetch_url(url, follow_redirects: true, max_redirects: 5)
end
```

<!-- ApplicationService pattern -->
From app/services/application_service.rb:
```ruby
class ApplicationService
  def self.call(kwargs = {})
    new(kwargs).call
  end
end
```

<!-- Key UmbScraper methods to extract -->
# find_or_create_international_player (line 1772) -> Umb::PlayerResolver
# find_discipline_from_name (line 1211) -> Umb::DisciplineDetector (comprehensive DB lookup)
# determine_discipline_from_name (line 1468) -> Umb::DisciplineDetector (simple string map — consolidate)
# parse_date_range (line 1064), parse_single_date (1094), etc. -> Umb::DateHelpers

<!-- V2 methods to absorb -->
# find_player_by_caps_and_mixed (line 431) -> Umb::PlayerResolver (superior strategy)
# find_player_by_name (line 447) -> Umb::PlayerResolver
# find_or_create_player (line 459) -> Umb::PlayerResolver
# parse_date_range (line 543), parse_single_date (line 519) -> Umb::DateHelpers (duplicate, delete)
# determine_discipline_from_name (line 570) -> delete (use DisciplineDetector)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Umb::HttpClient fetch_pdf_text + Umb::DisciplineDetector + Umb::DateHelpers</name>
  <files>
    app/services/umb/http_client.rb,
    app/services/umb/discipline_detector.rb,
    app/services/umb/date_helpers.rb,
    test/services/umb/http_client_test.rb,
    test/services/umb/discipline_detector_test.rb,
    test/services/umb/date_helpers_test.rb
  </files>
  <read_first>
    app/services/umb/http_client.rb,
    app/services/umb_scraper.rb (lines 1064-1220 for date helpers and discipline detection),
    app/services/umb_scraper.rb (lines 1468-1477 for determine_discipline_from_name),
    app/services/umb_scraper.rb (lines 1560-1580 for download_pdf),
    test/services/umb/http_client_test.rb
  </read_first>
  <behavior>
    - fetch_pdf_text("https://example.com/test.pdf") returns extracted text from PDF pages joined by newline
    - fetch_pdf_text returns nil on HTTP error (non-200)
    - fetch_pdf_text returns nil on malformed PDF (rescue StandardError)
    - DisciplineDetector.detect("World Cup 3-Cushion") returns Discipline record for 3-Cushion
    - DisciplineDetector.detect("unknown gibberish") returns nil
    - DisciplineDetector.detect consolidates BOTH find_discipline_from_name (ILIKE DB lookup) AND determine_discipline_from_name (string map) into one method
    - DateHelpers.parse_date_range("15 - 17 January 2025") returns [Date(2025-01-15), Date(2025-01-17)]
    - DateHelpers.parse_single_date("15 January 2025") returns Date(2025-01-15)
    - DateHelpers.parse_date_range handles month-spanning ranges like "28 January - 2 February 2025"
  </behavior>
  <action>
    1. Add `fetch_pdf_text(url)` instance method to existing `Umb::HttpClient`:
       - Call `fetch_url(url)` to get raw bytes
       - Return nil if blank
       - `PDF::Reader.new(StringIO.new(raw)).pages.map(&:text).join("\n")`
       - Rescue StandardError, log, return nil
       - Add `require "pdf-reader"` and `require "stringio"` at top

    2. Create `app/services/umb/discipline_detector.rb` as a PORO (per D-03 — stateless pure algorithm):
       - `class Umb::DisciplineDetector`
       - Class method `self.detect(tournament_name)` returns a `Discipline` record or nil
       - First try: ILIKE DB query (from V1's `find_discipline_from_name` line 1211):
         `Discipline.where("name ILIKE ?", "%#{extracted_term}%").first`
       - Fallback: string-to-name map from V1's `determine_discipline_from_name` (line 1468) then `Discipline.find_by(name: mapped_name)`
       - Extract discipline terms from tournament name using the regex patterns from both V1 methods
       - `frozen_string_literal: true`, German business-logic comments

    3. Create `app/services/umb/date_helpers.rb` as a module:
       - `module Umb::DateHelpers` with module_function methods
       - Extract from UmbScraper: `parse_date_range` (line 1064), `parse_single_date` (line 1094), `parse_day_range_with_month` (line 1106), `parse_month_day_range` (line 1153), `parse_month_name` (line 1189), `parse_date` (line 1438), `enhance_date_with_context` (line 898)
       - Delete `parse_full_month_range` (dead code — always returns nil)
       - All methods become module_function so they can be called as `Umb::DateHelpers.parse_date_range(...)`

    4. Write tests:
       - `test/services/umb/http_client_test.rb`: Add tests for `fetch_pdf_text` using WebMock stubs (stub URL returning binary PDF content built from a minimal PDF fixture)
       - `test/services/umb/discipline_detector_test.rb`: Test with WebMock-free unit tests using fixture Discipline records
       - `test/services/umb/date_helpers_test.rb`: Pure unit tests for all date parsing variants
  </action>
  <verify>
    <automated>bin/rails test test/services/umb/http_client_test.rb test/services/umb/discipline_detector_test.rb test/services/umb/date_helpers_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def fetch_pdf_text" app/services/umb/http_client.rb` returns 1
    - `grep -c "class Umb::DisciplineDetector" app/services/umb/discipline_detector.rb` returns 1
    - `grep -c "module Umb::DateHelpers" app/services/umb/date_helpers.rb` returns 1
    - `grep -c "def self.detect" app/services/umb/discipline_detector.rb` returns 1
    - `grep "parse_date_range\|parse_single_date\|parse_day_range_with_month\|parse_month_name" app/services/umb/date_helpers.rb | wc -l` is at least 4
    - All three test files pass with 0 failures
  </acceptance_criteria>
  <done>HttpClient has fetch_pdf_text, DisciplineDetector consolidates both discipline lookup methods, DateHelpers extracts all date parsing. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Umb::PlayerResolver — absorbing V2's caps+mixed name strategy</name>
  <files>
    app/services/umb/player_resolver.rb,
    test/services/umb/player_resolver_test.rb
  </files>
  <read_first>
    app/services/umb_scraper.rb (lines 1772-1817 for find_or_create_international_player),
    app/services/umb_scraper_v2.rb (lines 431-490 for find_player_by_caps_and_mixed, find_player_by_name, find_or_create_player),
    app/models/player.rb (for Player model schema and associations)
  </read_first>
  <behavior>
    - resolve("SMITH", "John", nationality: "GB") returns existing Player if "John Smith" found (Western order)
    - resolve("TANAKA", "Hiroshi", nationality: "JP") returns existing Player if "Hiroshi Tanaka" found (Asian order swap)
    - resolve("UNKNOWN", "Player", nationality: "XX") creates new Player with type InternationalPlayer when no match
    - resolve with umb_player_id: 106 stores external reference for cross-reference
    - find_by_caps_and_mixed("SMITH", "John") tries both name orders before giving up
  </behavior>
  <action>
    1. Create `app/services/umb/player_resolver.rb`:
       - `class Umb::PlayerResolver` (ApplicationService per D-03 — has DB side effects: creates Player records)
       - Primary method: `resolve(caps_name, mixed_name, nationality: nil, umb_player_id: nil)` returns Player
       - Absorb V2's `find_player_by_caps_and_mixed` (line 431) as private method — this is the superior strategy that handles Asian name ordering
       - Absorb V2's `find_player_by_name` (line 447) — swaps first/last name and tries both
       - Absorb V1's `find_or_create_international_player` (line 1772) for the creation path (creates `InternationalPlayer` with nationality, umb_player_id)
       - Handle edge cases: blank names, special characters in names
       - `frozen_string_literal: true`

    2. Write `test/services/umb/player_resolver_test.rb`:
       - Test finding existing player by Western name order (caps=LASTNAME, mixed=Firstname)
       - Test finding existing player by Asian name order (caps=FIRSTNAME, mixed=Lastname)
       - Test creating new player when not found
       - Test umb_player_id storage
       - Use fixtures or factory for test Player records
  </action>
  <verify>
    <automated>bin/rails test test/services/umb/player_resolver_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class Umb::PlayerResolver" app/services/umb/player_resolver.rb` returns 1
    - `grep "def resolve" app/services/umb/player_resolver.rb | wc -l` is at least 1
    - `grep "find_player_by_caps_and_mixed\|find_player_by_name" app/services/umb/player_resolver.rb | wc -l` is at least 2
    - `bin/rails test test/services/umb/player_resolver_test.rb` passes with 0 failures
  </acceptance_criteria>
  <done>PlayerResolver consolidates V1 and V2 player resolution strategies into one service. V2's caps+mixed strategy is the primary lookup. Tests verify both name ordering scenarios and creation path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HttpClient -> UMB servers | Outbound HTTPS to files.umb-carom.org for PDF download |
| PDF content -> PDF::Reader | Untrusted binary content parsed by pdf-reader gem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | T (Tampering) | fetch_pdf_text | accept | UMB PDFs are public read-only documents; no authentication; SSL VERIFY_PEER in production already handled by Phase 25 |
| T-26-02 | D (Denial of Service) | PDF::Reader | mitigate | Rescue StandardError around PDF::Reader.new to prevent malformed PDFs from crashing the process |
| T-26-03 | I (Information Disclosure) | PlayerResolver | accept | Player data is public tournament records; no PII beyond names and nationality |
</threat_model>

<verification>
- `bin/rails test test/services/umb/` passes all tests in the Umb:: namespace
- `grep "class Umb::" app/services/umb/*.rb` shows HttpClient, PlayerResolver, DisciplineDetector
- `grep "module Umb::" app/services/umb/*.rb` shows DateHelpers
</verification>

<success_criteria>
- Four new service files exist in `app/services/umb/`
- Each has a corresponding test file in `test/services/umb/`
- `fetch_pdf_text` replaces the need for per-scraper `download_pdf` methods
- DisciplineDetector consolidates the two duplicate discipline methods
- PlayerResolver absorbs V2's superior name resolution strategy
- `bin/rails test test/services/umb/` is green
</success_criteria>

<output>
After completion, create `.planning/phases/26-umbscraper-service-extraction-v2-absorption/26-01-SUMMARY.md`
</output>
