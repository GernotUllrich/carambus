---
phase: 26-umbscraper-service-extraction-v2-absorption
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/umb/pdf_parser/player_list_parser.rb
  - app/services/umb/pdf_parser/group_result_parser.rb
  - app/services/umb/pdf_parser/ranking_parser.rb
  - test/services/umb/pdf_parser/player_list_parser_test.rb
  - test/services/umb/pdf_parser/group_result_parser_test.rb
  - test/services/umb/pdf_parser/ranking_parser_test.rb
  - test/fixtures/files/umb_player_list.txt
  - test/fixtures/files/umb_group_results.txt
  - test/fixtures/files/umb_ranking.txt
autonomous: true
requirements: [SCRP-06, SCRP-07]
must_haves:
  truths:
    - "PlayerListParser extracts player names, nationalities, and seeding positions from PDF text"
    - "GroupResultParser extracts match pairs with scores from group results PDF text"
    - "RankingParser extracts final ranking positions from ranking PDF text"
    - "All three parsers are POROs that return structured data hashes, not ActiveRecord objects (per D-03, D-08)"
    - "GroupResultParser uses V2's pair-accumulator approach as base (per research recommendation)"
  artifacts:
    - path: "app/services/umb/pdf_parser/player_list_parser.rb"
      provides: "PDF text -> seeding data array"
      contains: "class Umb::PdfParser::PlayerListParser"
    - path: "app/services/umb/pdf_parser/group_result_parser.rb"
      provides: "PDF text -> match data array"
      contains: "class Umb::PdfParser::GroupResultParser"
    - path: "app/services/umb/pdf_parser/ranking_parser.rb"
      provides: "PDF text -> ranking data array"
      contains: "class Umb::PdfParser::RankingParser"
  key_links:
    - from: "Umb::PdfParser::GroupResultParser"
      to: "Video::TournamentMatcher (Phase 27)"
      via: "Structured match data output per D-08"
      pattern: "player_a.*player_b.*winner"
---

<objective>
Create three PORO PDF text parsers that convert extracted PDF text into structured data hashes. These are pure-function parsers (per D-01 decision to split by PDF type, per D-03 PORO for stateless algorithms). They absorb V2's PDF parsing logic (per D-04, D-05, D-06) and implement ranking parsing as new work (per D-07, RANK-01). Output format satisfies D-08 for Phase 27 consumption.

Purpose: Separate text parsing (pure logic) from side effects (DB writes). Parsers return data structures; callers decide what to persist.
Output: Three parser classes + test fixtures + tests.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-umbscraper-service-extraction-v2-absorption/26-RESEARCH.md

<interfaces>
<!-- V2's player list parsing (line 236-288) — base for PlayerListParser -->
<!-- V2's group results parsing (line 290-358) — base for GroupResultParser (pair-accumulator) -->
<!-- V2's final ranking stub (line 362-367) — to be implemented as RankingParser -->
<!-- V1's scrape_players_from_pdf (line 1588) — secondary reference -->
<!-- V1's scrape_results_from_pdf (line 1637) — partial ranking impl (uses deleted InternationalResult model) -->
<!-- V1's parse_group_results_pdf (line 1879) — alternative approach (each_slice), NOT recommended -->

<!-- Data output contracts per D-08 (from 26-RESEARCH.md): -->
<!-- PlayerListParser: { player_name:, nationality:, position:, caps_name:, mixed_name: } -->
<!-- GroupResultParser: { group:, player_a: {name:, points:, innings:, average:, hs:, match_points:}, player_b: {...}, winner_name: } -->
<!-- RankingParser: { position:, player_name:, nationality:, points:, average: } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlayerListParser + GroupResultParser POROs with test fixtures</name>
  <files>
    app/services/umb/pdf_parser/player_list_parser.rb,
    app/services/umb/pdf_parser/group_result_parser.rb,
    test/services/umb/pdf_parser/player_list_parser_test.rb,
    test/services/umb/pdf_parser/group_result_parser_test.rb,
    test/fixtures/files/umb_player_list.txt,
    test/fixtures/files/umb_group_results.txt
  </files>
  <read_first>
    app/services/umb_scraper_v2.rb (lines 236-360 for V2's player list and group results parsing),
    app/services/umb_scraper.rb (lines 1588-1636 for V1's scrape_players_from_pdf),
    app/services/umb_scraper.rb (lines 1879-2046 for V1's parse_group_results_pdf),
    app/services/umb_scraper.rb (lines 1960-2046 for create_games_from_matches),
    test/characterization/umb_scraper_v2_char_test.rb (to see what behavior V2 char tests pinned — per D-05)
  </read_first>
  <behavior>
    - PlayerListParser.new(pdf_text).parse returns array of hashes with :caps_name, :mixed_name, :nationality, :position
    - PlayerListParser handles empty/nil input returning empty array
    - PlayerListParser skips header lines and malformed entries
    - GroupResultParser.new(pdf_text).parse returns array of match hashes with :group, :player_a, :player_b
    - GroupResultParser detects group headers ("Group A", "Group B", etc.)
    - GroupResultParser uses V2's pair-accumulator pattern (pending_player + next player = match)
    - GroupResultParser player data includes :name, :nationality, :points, :innings, :average, :hs, :match_points
    - GroupResultParser identifies winner (player with higher match_points)
  </behavior>
  <action>
    1. Create test fixture files by extracting representative text from VCR cassettes or V2 char test expectations:
       - `test/fixtures/files/umb_player_list.txt` — sample player list PDF text (3-5 players with caps names, mixed names, nationality codes)
       - `test/fixtures/files/umb_group_results.txt` — sample group results PDF text (2 groups, 2-3 matches each)

    2. Create `app/services/umb/pdf_parser/player_list_parser.rb`:
       - `class Umb::PdfParser::PlayerListParser` — pure PORO, no DB access
       - `initialize(pdf_text)` stores the text
       - `parse` method returns Array of hashes: `[{ caps_name: "SMITH", mixed_name: "John", nationality: "GB", position: 1 }, ...]`
       - Use V2's regex pattern from `scrape_players_list_pdf` (line ~250-270) as the base
       - Skip header lines, handle edge cases (blank lines, page breaks)
       - `frozen_string_literal: true`

    3. Create `app/services/umb/pdf_parser/group_result_parser.rb`:
       - `class Umb::PdfParser::GroupResultParser` — pure PORO, no DB access
       - `initialize(pdf_text)` stores the text
       - `parse` returns Array of match hashes per D-08 contract:
         `[{ group: "A", player_a: { name:, nationality:, points:, innings:, average:, hs:, match_points: }, player_b: {...}, winner_name: "Firstname Lastname" }, ...]`
       - Use V2's pair-accumulator approach (line 290-358) as base — NOT V1's each_slice
       - Detect group headers via `/^Group\s+([A-Z])/i`
       - Skip header/label lines via `/^\s*(Players|Nat|Group)/i`
       - Use V2's regex for player stat lines (line 529)
       - winner_name = player with higher match_points value
       - `frozen_string_literal: true`

    4. Write tests using the fixture files:
       - `test/services/umb/pdf_parser/player_list_parser_test.rb`: parse returns expected structure, handles empty input, skips headers
       - `test/services/umb/pdf_parser/group_result_parser_test.rb`: parse returns match pairs with correct groups, player data, winner identification
  </action>
  <verify>
    <automated>bin/rails test test/services/umb/pdf_parser/player_list_parser_test.rb test/services/umb/pdf_parser/group_result_parser_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class Umb::PdfParser::PlayerListParser" app/services/umb/pdf_parser/player_list_parser.rb` returns 1
    - `grep -c "class Umb::PdfParser::GroupResultParser" app/services/umb/pdf_parser/group_result_parser.rb` returns 1
    - `grep "def parse" app/services/umb/pdf_parser/player_list_parser.rb | wc -l` returns 1
    - `grep "def parse" app/services/umb/pdf_parser/group_result_parser.rb | wc -l` returns 1
    - `grep "winner_name" app/services/umb/pdf_parser/group_result_parser.rb | wc -l` is at least 1
    - `grep "pending_player" app/services/umb/pdf_parser/group_result_parser.rb | wc -l` is at least 1 (V2 accumulator pattern)
    - Both test files pass with 0 failures
  </acceptance_criteria>
  <done>PlayerListParser and GroupResultParser are pure POROs returning structured data. V2's pair-accumulator is the GroupResultParser base. Test fixtures provide repeatable input. Per D-05, these tests cover the same behavior V2 char tests pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RankingParser PORO — new implementation per D-06, D-07 (RANK-01)</name>
  <files>
    app/services/umb/pdf_parser/ranking_parser.rb,
    test/services/umb/pdf_parser/ranking_parser_test.rb,
    test/fixtures/files/umb_final_ranking.txt,
    test/fixtures/files/umb_weekly_ranking.txt
  </files>
  <read_first>
    app/services/umb_scraper.rb (lines 1637-1678 for V1's scrape_results_from_pdf — partial ranking implementation),
    app/services/umb_scraper_v2.rb (lines 362-367 for V2's ranking stub),
    app/services/umb_scraper.rb (lines 137-149 for scrape_rankings method and URL pattern comments)
  </read_first>
  <behavior>
    - RankingParser.new(pdf_text, type: :final).parse returns array of { position:, player_name:, nationality:, points:, average: }
    - RankingParser.new(pdf_text, type: :weekly).parse returns array of { rank:, player_name:, nationality:, points: }
    - RankingParser handles empty/nil input returning empty array
    - RankingParser skips header rows and non-data lines
    - Both tournament final rankings and weekly UMB world rankings are supported (per D-07)
  </behavior>
  <action>
    1. Create test fixtures by constructing representative ranking text:
       - `test/fixtures/files/umb_final_ranking.txt` — Tournament final ranking format (position, name, nationality, points, average). Construct from V1's regex pattern at line 1637 and known UMB PDF structures.
       - `test/fixtures/files/umb_weekly_ranking.txt` — Weekly world ranking format (rank, name, nationality, points). The URL pattern is `files.umb-carom.org/Public/Ranking/1_WP_Ranking/YEAR/WWEEK_YEAR.pdf`.

       NOTE: If real PDF structure is unknown, create a best-effort fixture based on V1's regex pattern: `/(\d+)\s+([A-Z\s]+)\s+([A-Z][a-z]+(?:\s+[A-Z][a-z]+)*)\s+(\w{2,3})\s+(\d+)\s+(\d+)\s+([\d.]+)\s+(\d+)/`
       Mark the test with a comment that fixture format should be verified against a real downloaded PDF if available.

    2. Create `app/services/umb/pdf_parser/ranking_parser.rb`:
       - `class Umb::PdfParser::RankingParser` — pure PORO
       - `initialize(pdf_text, type: :final)` — `type` is `:final` (tournament result) or `:weekly` (UMB world ranking)
       - `parse` returns Array of hashes:
         - For `:final`: `[{ position: 1, player_name: "Firstname Lastname", nationality: "NL", points: 150, average: 2.5 }, ...]`
         - For `:weekly`: `[{ rank: 1, player_name: "Firstname Lastname", nationality: "NL", points: 1200 }, ...]`
       - Use V1's regex from `scrape_results_from_pdf` (line 1637) as a starting point but output structured hashes instead of creating InternationalResult records (that model no longer exists)
       - NOTE: InternationalResult model does NOT exist — ranking data returns as plain hashes per D-08
       - `frozen_string_literal: true`

    3. Write tests:
       - `test/services/umb/pdf_parser/ranking_parser_test.rb`: test both :final and :weekly types, empty input, header skipping
  </action>
  <verify>
    <automated>bin/rails test test/services/umb/pdf_parser/ranking_parser_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class Umb::PdfParser::RankingParser" app/services/umb/pdf_parser/ranking_parser.rb` returns 1
    - `grep "type.*final\|type.*weekly" app/services/umb/pdf_parser/ranking_parser.rb | wc -l` is at least 1
    - `grep "def parse" app/services/umb/pdf_parser/ranking_parser.rb | wc -l` returns 1
    - Test file passes with 0 failures
    - No reference to `InternationalResult` anywhere in the new parser file
  </acceptance_criteria>
  <done>RankingParser handles both tournament final rankings and weekly UMB world rankings as pure PORO returning structured data. RANK-01 is fulfilled. InternationalResult model (deleted) is not referenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF text content -> Parser regex | Untrusted text from external PDFs parsed by regex patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | T (Tampering) | PDF parsers | accept | PDFs are public UMB documents; parsers return data hashes, no DB writes; consumers validate before persisting |
| T-26-05 | D (Denial of Service) | Regex parsing | mitigate | Use non-greedy regex patterns; limit line iteration to pdf_text.each_line (bounded by PDF size) |
</threat_model>

<verification>
- `bin/rails test test/services/umb/pdf_parser/` passes all PDF parser tests
- `find app/services/umb/pdf_parser/ -name "*.rb" | wc -l` returns 3
- All parsers return plain Ruby hashes, no ActiveRecord dependency in parser files
</verification>

<success_criteria>
- Three PDF parser POROs exist in `app/services/umb/pdf_parser/`
- Each has corresponding test file with text fixtures
- PlayerListParser and GroupResultParser absorb V2's logic (per D-04, D-05, D-06)
- RankingParser is new implementation (per D-07, RANK-01)
- Output format matches D-08 contract for Phase 27 consumption
- No reference to deleted InternationalResult model
</success_criteria>

<output>
After completion, create `.planning/phases/26-umbscraper-service-extraction-v2-absorption/26-02-SUMMARY.md`
</output>
