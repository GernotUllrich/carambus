---
phase: 30-content-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/developers/umb-scraping-implementation.md
  - docs/developers/umb-scraping-implementation.de.md
  - docs/developers/umb-scraping-implementation.en.md
  - docs/developers/umb-scraping-methods.md
  - docs/developers/umb-scraping-methods.de.md
  - docs/developers/umb-scraping-methods.en.md
autonomous: true
requirements:
  - UPDATE-01

must_haves:
  truths:
    - "umb-scraping-implementation.de.md describes the Umb:: namespace with all 10 services and contains no reference to UmbScraperV2"
    - "umb-scraping-methods.de.md lists the 6 key entry points (3 scraper .call methods + 3 PdfParser .parse methods) with no reference to UmbScraperV2 or old Rake tasks"
    - "Both docs have .en.md counterparts with equivalent content in English"
    - "The original single .md files are removed (replaced by .de.md pairs)"
  artifacts:
    - path: "docs/developers/umb-scraping-implementation.de.md"
      provides: "German Umb:: architecture overview"
      contains: "Umb::HttpClient"
    - path: "docs/developers/umb-scraping-implementation.en.md"
      provides: "English Umb:: architecture overview"
      contains: "Umb::HttpClient"
    - path: "docs/developers/umb-scraping-methods.de.md"
      provides: "German Umb:: method/entry-point inventory"
      contains: "Umb::DetailsScraper"
    - path: "docs/developers/umb-scraping-methods.en.md"
      provides: "English Umb:: method/entry-point inventory"
      contains: "Umb::DetailsScraper"
  key_links:
    - from: "docs/developers/umb-scraping-implementation.de.md"
      to: "app/services/umb/"
      via: "class name references"
      pattern: "Umb::(HttpClient|DisciplineDetector|DateHelpers|PlayerResolver|FutureScraper|ArchiveScraper|DetailsScraper)"
    - from: "docs/developers/umb-scraping-methods.de.md"
      to: "app/services/umb/"
      via: "entry point references"
      pattern: "Umb::(FutureScraper|ArchiveScraper|DetailsScraper)"
---

<objective>
Rewrite the two stale UMB scraping docs to reflect the current Umb:: namespace architecture, converting them from single German-only files to bilingual .de.md/.en.md pairs.

Purpose: These docs currently describe the deleted UmbScraperV2 monolith. After this plan, they accurately describe the 10-service Umb:: namespace.
Output: 4 new doc files (2 DE + 2 EN), 2 old files removed.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-content-updates/30-CONTEXT.md
@.planning/phases/30-content-updates/30-RESEARCH.md

<interfaces>
<!-- The executor MUST read the actual source files to write accurate one-liners. -->
<!-- The RESEARCH.md contains the full verified service inventory with descriptions. -->
<!-- Key data flow diagram from RESEARCH.md: -->

```
UMB Website
  +-- FutureTournaments.aspx --> Umb::FutureScraper
  |                                  +-- Umb::HttpClient (fetch HTML)
  |                                  +-- Umb::DateHelpers (parse dates)
  |                                  +-- Umb::DisciplineDetector (match discipline)
  |                                       -> InternationalTournament (upsert)
  |
  +-- TournametDetails.aspx?ID=N --> Umb::ArchiveScraper
  |                                      +-- Umb::HttpClient (fetch HTML)
  |                                      +-- Umb::DateHelpers
  |                                      +-- Umb::DisciplineDetector
  |                                           -> InternationalTournament (create)
  |
  +-- TournametDetails.aspx?ID=N --> Umb::DetailsScraper
           +-- Umb::HttpClient (fetch HTML + PDFs)
           +-- Umb::PlayerResolver (find/create Player)
           +-- PDF pipeline (optional, parse_pdfs: true):
           |     +-- PdfParser::PlayerListParser -> Seeding records
           |     +-- PdfParser::GroupResultParser -> InternationalGame + GameParticipation
           |     +-- PdfParser::RankingParser -> Seeding with final position
           +-- InternationalGame records (HTML-based, create_games: true)
```

<!-- 10 Umb:: services (verified): -->
<!-- 1. Umb::HttpClient - Stateless HTTP transport -->
<!-- 2. Umb::DisciplineDetector - Maps tournament names to Discipline records -->
<!-- 3. Umb::DateHelpers - Module with module_function, parses UMB date range strings -->
<!-- 4. Umb::PlayerResolver - Finds/creates Player records from UMB name pairs -->
<!-- 5. Umb::FutureScraper - Scrapes FutureTournaments.aspx -->
<!-- 6. Umb::ArchiveScraper - Sequential ID scan of historical tournaments -->
<!-- 7. Umb::DetailsScraper - Scrapes tournament detail page, orchestrates PDF pipeline -->
<!-- 8. Umb::PdfParser::PlayerListParser - Parses player seeding list PDFs -->
<!-- 9. Umb::PdfParser::GroupResultParser - Parses group result PDFs -->
<!-- 10. Umb::PdfParser::RankingParser - Parses ranking PDFs -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite umb-scraping-implementation as bilingual pair</name>
  <files>
    docs/developers/umb-scraping-implementation.md,
    docs/developers/umb-scraping-implementation.de.md,
    docs/developers/umb-scraping-implementation.en.md
  </files>
  <read_first>
    docs/developers/umb-scraping-implementation.md,
    app/services/umb/http_client.rb,
    app/services/umb/discipline_detector.rb,
    app/services/umb/date_helpers.rb,
    app/services/umb/player_resolver.rb,
    app/services/umb/future_scraper.rb,
    app/services/umb/archive_scraper.rb,
    app/services/umb/details_scraper.rb,
    app/services/umb/pdf_parser/player_list_parser.rb,
    app/services/umb/pdf_parser/group_result_parser.rb,
    app/services/umb/pdf_parser/ranking_parser.rb,
    .planning/phases/30-content-updates/30-RESEARCH.md
  </read_first>
  <action>
Per D-01, D-02, D-03, D-05:

1. Read the existing `docs/developers/umb-scraping-implementation.md` (727 lines, entirely stale UmbScraperV2 content — nothing is salvageable).

2. Read ALL 10 Umb:: service source files listed in read_first to write accurate descriptions.

3. Create `docs/developers/umb-scraping-implementation.de.md` (German primary, per D-03) with this structure:
   - Title: `# UMB Scraping — Architektur`
   - Opening paragraph: Brief description that the Umb:: namespace handles scraping of tournament data from the UMB (Union Mondiale de Billard) website. Mention 10 services across 2 sub-namespaces (Umb:: and Umb::PdfParser::).
   - `## Namespace-Übersicht`: A table listing all 10 services with columns: Klasse, Datei, Beschreibung. Use one-liner descriptions per D-07. Group the PdfParser services visually (sub-heading or indentation).
   - `## Architektur-Entscheidungen`: Document these 5 points:
     a. PORO vs ApplicationService split — pure algorithms (DisciplineDetector, DateHelpers, PdfParser parsers) are POROs; scrapers with DB side effects inherit ApplicationService
     b. Module vs Class — Umb::DateHelpers is a `module` with `module_function`, all others are classes
     c. Delegation pattern — FutureScraper, ArchiveScraper, DetailsScraper delegate to HttpClient, DateHelpers, DisciplineDetector
     d. Optional PDF pipeline — DetailsScraper has `parse_pdfs: false` by default; when enabled runs all 3 PdfParser services independently
     e. InternationalGame STI — games created by DetailsScraper must use `type: 'InternationalGame'`
   - `## Datenfluss`: Include a text-based data flow diagram showing the 3 entry points (FutureScraper, ArchiveScraper, DetailsScraper) and how they connect to shared services and DB models. Use the diagram from RESEARCH.md as the basis.
   - `## Einstiegspunkte`: List the 3 main entry points:
     - `Umb::FutureScraper.new.call` — scrapes upcoming tournaments
     - `Umb::ArchiveScraper.new.call(start_id:, end_id:)` — scans historical tournament IDs
     - `Umb::DetailsScraper.new.call(tournament_id_or_record)` — enriches one tournament

4. Create `docs/developers/umb-scraping-implementation.en.md` — translate the German content to English per D-04. Same structure, same technical accuracy.

5. Delete the original `docs/developers/umb-scraping-implementation.md` using `git rm`.

6. Commit per D-08: `git commit` with message `docs(30): rewrite umb-scraping-implementation as bilingual pair` including both new `.de.md` and `.en.md` files and the removal of the old `.md` file.

CRITICAL: The new docs must contain ZERO references to `UmbScraperV2`, `UmbScraper` (V1), `InternationalParticipation`, `InternationalResult`, or any Rake tasks like `rake umb:update` or `rake umb_v2:scrape`. These are all deleted/obsolete.

CRITICAL: Use 10 services / 35 total (not 37). The verified count is 35 services across 7 namespaces.

NOTE: Per RESEARCH.md, no mkdocs.yml nav changes are needed — the `mkdocs-static-i18n` plugin with `docs_structure: suffix` resolves `.de.md`/`.en.md` automatically from the existing nav entry `developers/umb-scraping-implementation.md`.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && test -f docs/developers/umb-scraping-implementation.de.md && test -f docs/developers/umb-scraping-implementation.en.md && ! test -f docs/developers/umb-scraping-implementation.md && ! grep -q "UmbScraperV2" docs/developers/umb-scraping-implementation.de.md && ! grep -q "UmbScraperV2" docs/developers/umb-scraping-implementation.en.md && grep -q "Umb::HttpClient" docs/developers/umb-scraping-implementation.de.md && grep -q "Umb::HttpClient" docs/developers/umb-scraping-implementation.en.md && grep -q "Umb::PdfParser::PlayerListParser" docs/developers/umb-scraping-implementation.de.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/developers/umb-scraping-implementation.de.md` exists and contains all 10 service class names: `Umb::HttpClient`, `Umb::DisciplineDetector`, `Umb::DateHelpers`, `Umb::PlayerResolver`, `Umb::FutureScraper`, `Umb::ArchiveScraper`, `Umb::DetailsScraper`, `Umb::PdfParser::PlayerListParser`, `Umb::PdfParser::GroupResultParser`, `Umb::PdfParser::RankingParser`
    - `docs/developers/umb-scraping-implementation.en.md` exists and contains the same 10 class names
    - Neither file contains `UmbScraperV2`, `UmbScraper` (without namespace prefix), `InternationalParticipation`, `InternationalResult`, `rake umb:`, or `rake umb_v2:`
    - `docs/developers/umb-scraping-implementation.md` (original) no longer exists
    - Both files contain a data flow diagram section
    - `git log --oneline -1` shows a commit containing both `.de.md` and `.en.md` changes
  </acceptance_criteria>
  <done>umb-scraping-implementation rewritten as bilingual pair with all 10 Umb:: services documented, no stale references, old file removed</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite umb-scraping-methods as bilingual pair</name>
  <files>
    docs/developers/umb-scraping-methods.md,
    docs/developers/umb-scraping-methods.de.md,
    docs/developers/umb-scraping-methods.en.md
  </files>
  <read_first>
    docs/developers/umb-scraping-methods.md,
    app/services/umb/future_scraper.rb,
    app/services/umb/archive_scraper.rb,
    app/services/umb/details_scraper.rb,
    app/services/umb/pdf_parser/player_list_parser.rb,
    app/services/umb/pdf_parser/group_result_parser.rb,
    app/services/umb/pdf_parser/ranking_parser.rb,
    .planning/phases/30-content-updates/30-RESEARCH.md
  </read_first>
  <action>
Per D-01, D-02, D-03, D-05:

1. Read the existing `docs/developers/umb-scraping-methods.md` (307 lines, entirely stale Rake task reference for UmbScraperV2 — nothing is salvageable).

2. Read the 6 service files that have public entry points (3 scrapers + 3 PdfParsers).

3. Create `docs/developers/umb-scraping-methods.de.md` (German primary, per D-03) with this structure:
   - Title: `# UMB Scraping — Methoden-Referenz`
   - Opening paragraph: This page documents the public entry points and key methods of the Umb:: namespace services.
   - `## Scraper-Einstiegspunkte`: For each of the 3 scrapers, document:
     - Class name and file path
     - `#call` method signature with parameters
     - What it returns / what DB records it creates
     - Key options (e.g., `DetailsScraper#call` has `parse_pdfs:` and `create_games:` keyword args)
   - Specifically document:
     - `Umb::FutureScraper.new.call` — no parameters, scrapes FutureTournaments.aspx, upserts InternationalTournament records
     - `Umb::ArchiveScraper.new.call(start_id:, end_id:)` — scans tournament IDs in range, creates InternationalTournament records
     - `Umb::DetailsScraper.new.call(tournament)` — accepts InternationalTournament ID or record, options: `parse_pdfs: false` (default), `create_games: true` (default). Creates InternationalGame + GameParticipation records
   - `## PDF-Parser`: For each of the 3 PdfParser services, document:
     - Class name and file path
     - `#parse` or `#call` method signature
     - Input format (PDF text string)
     - Output format (array of hashes with specific keys)
   - Specifically document:
     - `Umb::PdfParser::PlayerListParser.new(text).parse` — returns `[{caps_name:, mixed_name:, nationality:, position:}]`
     - `Umb::PdfParser::GroupResultParser.new(text).parse` — returns match pair arrays
     - `Umb::PdfParser::RankingParser.new(text, type:).parse` — type is `:final` or `:weekly`, returns ranking arrays
   - `## Hilfs-Services`: Brief reference to HttpClient, DisciplineDetector, DateHelpers, PlayerResolver as supporting services (link to umb-scraping-implementation for details).

4. Create `docs/developers/umb-scraping-methods.en.md` — translate the German content to English per D-04. Same structure and technical accuracy.

5. Delete the original `docs/developers/umb-scraping-methods.md` using `git rm`.

6. Commit per D-08: `git commit` with message `docs(30): rewrite umb-scraping-methods as bilingual pair` including both new files and the removal.

CRITICAL: No references to `UmbScraperV2`, `UmbScraper` (V1), `rake umb:update`, `rake umb_v2:scrape`, or any old Rake task names.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && test -f docs/developers/umb-scraping-methods.de.md && test -f docs/developers/umb-scraping-methods.en.md && ! test -f docs/developers/umb-scraping-methods.md && ! grep -q "UmbScraperV2" docs/developers/umb-scraping-methods.de.md && ! grep -q "rake umb" docs/developers/umb-scraping-methods.de.md && grep -q "Umb::DetailsScraper" docs/developers/umb-scraping-methods.de.md && grep -q "PdfParser::PlayerListParser" docs/developers/umb-scraping-methods.de.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/developers/umb-scraping-methods.de.md` exists and contains `Umb::FutureScraper`, `Umb::ArchiveScraper`, `Umb::DetailsScraper`, `Umb::PdfParser::PlayerListParser`, `Umb::PdfParser::GroupResultParser`, `Umb::PdfParser::RankingParser`
    - `docs/developers/umb-scraping-methods.en.md` exists and contains the same 6 class names
    - Neither file contains `UmbScraperV2`, `rake umb:`, or `rake umb_v2:`
    - `docs/developers/umb-scraping-methods.md` (original) no longer exists
    - Both files document `parse_pdfs:` parameter on DetailsScraper
    - `git log --oneline -1` shows a commit containing both `.de.md` and `.en.md` changes
  </acceptance_criteria>
  <done>umb-scraping-methods rewritten as bilingual pair with 6 entry points documented, no stale references, old file removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only phase — no code changes, no user input, no API endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | docs/ directory | accept | Documentation is public-facing by design; no secrets in service descriptions |
| T-30-02 | Tampering | mkdocs nav | accept | Nav entries unchanged; verified mkdocs-static-i18n handles suffix resolution transparently |
</threat_model>

<verification>
After both tasks complete:
1. `bin/check-docs-coderef.rb` reports no stale references in the new files
2. `bin/check-docs-translations.rb` shows umb-scraping-implementation and umb-scraping-methods have complete DE/EN pairs
3. `grep -r "UmbScraperV2" docs/developers/umb-scraping-*` returns nothing
</verification>

<success_criteria>
- umb-scraping-implementation and umb-scraping-methods are rewritten as bilingual pairs
- All 10 Umb:: services are accurately described
- Zero references to UmbScraperV2 or old architecture
- Each doc pair committed together per D-08
</success_criteria>

<output>
After completion, create `.planning/phases/30-content-updates/30-01-SUMMARY.md`
</output>
