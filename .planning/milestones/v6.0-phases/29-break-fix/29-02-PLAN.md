---
phase: 29-break-fix
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - docs/administrators/streaming-production-deployment.md
  - docs/developers/developer-guide.en.md
  - docs/developers/operations/tournament-game-protection.de.md
  - docs/developers/rake-tasks-debugging.de.md
  - docs/developers/rake-tasks-debugging.en.md
  - docs/developers/scenario-workflow.md
  - docs/developers/testing/fixture-collection-guide.md
  - docs/developers/testing/testing-quickstart.md
  - docs/international/umb_scraper.md
  - docs/managers/clubcloud_upload_feedback.md
  - docs/players/ai-search.de.md
  - docs/players/ai-search.en.md
  - docs/reference/config-lock-files.de.md
  - docs/reference/config-lock-files.en.md
  - docs/reference/glossary.de.md
  - docs/reference/glossary.en.md
  - docs/training_database.md
  - docs/developers/umb-scraping-methods.md
  - docs/developers/clubcloud-upload.de.md
  - docs/developers/clubcloud-upload.en.md
autonomous: true
requirements: [FIX-01, FIX-02]

must_haves:
  truths:
    - "bin/check-docs-links.rb --exclude-archives reports zero broken links"
    - "bin/check-docs-coderef.rb --exclude-archives reports zero stale references"
    - "No active doc references UmbScraperV2, tournament_monitor_support, or pre-refactoring god-object descriptions"
    - "mkdocs build --strict completes with zero missing-file warnings for nav entries"
  artifacts:
    - path: "docs/developers/umb-scraping-methods.md"
      provides: "UmbScraperV2 reference removed/updated"
      contains: "Umb::"
    - path: "docs/developers/clubcloud-upload.de.md"
      provides: "tournament_monitor_support updated to app/services/tournament_monitor/"
    - path: "docs/developers/clubcloud-upload.en.md"
      provides: "tournament_monitor_support updated to app/services/tournament_monitor/"
  key_links:
    - from: "bin/check-docs-links.rb"
      to: "all active docs"
      via: "link validation"
      pattern: "Broken links found: 0"
    - from: "bin/check-docs-coderef.rb"
      to: "all active docs"
      via: "class name validation"
      pattern: "Stale references found: 0"
---

<objective>
Fix all 31 remaining broken links manually and update 3 stale code references so that the active docs site has zero broken internal links and zero references to deleted code — achieving the Phase 29 goal of a clean structural baseline.

Purpose: Per D-04, this is the second pass after automation. Completes FIX-01 (all 75 broken links) and FIX-02 (all stale code references).
Output: 20 documentation files with all broken links and stale references resolved; clean verification from both checker scripts.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-break-fix/29-01-SUMMARY.md
@docs/audit.json

<interfaces>
<!-- Phase 29 findings remaining after Plan 01 -->
<!-- 31 broken links (non-screenshot, non-template) + 3 stale refs -->

Broken link fixes needed (by file):

docs/administrators/streaming-production-deployment.md:
  FIND-001: link to administrators/systemd-streaming-services.md (nonexistent)

docs/developers/developer-guide.en.md:
  FIND-002: link to developers/enhanced_mode_system.md (moved to obsolete/)
  FIND-003: link to developers/enhanced_mode_system.md (second occurrence)

docs/developers/operations/tournament-game-protection.de.md:
  FIND-004: link to developers/operations/FLASH_MESSAGES_SCOREBOARD.md (nonexistent)

docs/developers/rake-tasks-debugging.de.md:
  FIND-005: link to developers/scenario-system-workflow.md (nonexistent)

docs/developers/rake-tasks-debugging.en.md:
  FIND-006: link to developers/scenario-system-workflow.md (nonexistent)

docs/developers/scenario-workflow.md:
  FIND-007: link to CONTRIBUTING.md (outside docs root)
  FIND-008: link to README.md (outside docs root)
  FIND-009: link to carambus_master/docs/developers/ (external path)

docs/developers/testing/fixture-collection-guide.md:
  FIND-010: link to test/FIXTURES_QUICK_START.md (outside docs root)
  FIND-011: link to test/FIXTURES_SAMMELN.md (outside docs root)
  FIND-012: link to test/fixtures/html/README.md (outside docs root)
  FIND-013: link to test/FIXTURE_WORKFLOW.md (outside docs root)
  FIND-014: link to test/FIXTURES_QUICK_START.md (duplicate)
  FIND-015: link to test/FIXTURES_QUICK_START.md (duplicate)

docs/developers/testing/testing-quickstart.md:
  FIND-016: link to developers/testing/testing-quickstart.md (self-link with wrong path)
  FIND-017: link to developers/testing-strategy.md (nonexistent)
  FIND-018: link to test/snapshots/README.md (outside docs root)
  FIND-019: link to developers/testing/testing-quickstart.md (self-link, duplicate)
  FIND-020: link to developers/testing-strategy.md (duplicate)

docs/international/umb_scraper.md:
  FIND-021: link to international/international_videos.md (nonexistent)
  FIND-022: link to international/youtube_scraper.md (nonexistent)

docs/managers/clubcloud_upload_feedback.md:
  FIND-023: link to bin/test-cc-name-mapping.rb (outside docs root)
  FIND-024: link to managers/logging_conventions.md (nonexistent)

docs/players/ai-search.de.md:
  FIND-025: link to search.md (nonexistent)

docs/players/ai-search.en.md:
  FIND-026: link to search.md (nonexistent)

docs/reference/config-lock-files.de.md:
  FIND-059: link to reference/PRODUCTION_SETUP.md (nonexistent)

docs/reference/config-lock-files.en.md:
  FIND-060: link to reference/PRODUCTION_SETUP.md (nonexistent)

docs/reference/glossary.de.md:
  FIND-061: link to search.md (nonexistent)

docs/reference/glossary.en.md:
  FIND-062: link to search.md (nonexistent)

docs/training_database.md:
  FIND-075: link to README.md (outside docs root)

Stale reference fixes:

docs/developers/umb-scraping-methods.md:
  FIND-076: UmbScraperV2 at line 73 — class deleted in v5.0; action DELETE per audit.json

docs/developers/clubcloud-upload.de.md:
  FIND-077: tournament_monitor_support.rb at line 194 — moved to app/services/tournament_monitor/

docs/developers/clubcloud-upload.en.md:
  FIND-078: tournament_monitor_support.rb at line 194 — moved to app/services/tournament_monitor/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 31 remaining broken links across 17 documentation files</name>
  <files>
    docs/administrators/streaming-production-deployment.md,
    docs/developers/developer-guide.en.md,
    docs/developers/operations/tournament-game-protection.de.md,
    docs/developers/rake-tasks-debugging.de.md,
    docs/developers/rake-tasks-debugging.en.md,
    docs/developers/scenario-workflow.md,
    docs/developers/testing/fixture-collection-guide.md,
    docs/developers/testing/testing-quickstart.md,
    docs/international/umb_scraper.md,
    docs/managers/clubcloud_upload_feedback.md,
    docs/players/ai-search.de.md,
    docs/players/ai-search.en.md,
    docs/reference/config-lock-files.de.md,
    docs/reference/config-lock-files.en.md,
    docs/reference/glossary.de.md,
    docs/reference/glossary.en.md,
    docs/training_database.md
  </files>
  <read_first>
    docs/audit.json,
    bin/check-docs-links.rb,
    docs/administrators/streaming-production-deployment.md,
    docs/developers/developer-guide.en.md,
    docs/developers/operations/tournament-game-protection.de.md,
    docs/developers/rake-tasks-debugging.de.md,
    docs/developers/rake-tasks-debugging.en.md,
    docs/developers/scenario-workflow.md,
    docs/developers/testing/fixture-collection-guide.md,
    docs/developers/testing/testing-quickstart.md,
    docs/international/umb_scraper.md,
    docs/managers/clubcloud_upload_feedback.md,
    docs/players/ai-search.de.md,
    docs/players/ai-search.en.md,
    docs/reference/config-lock-files.de.md,
    docs/reference/config-lock-files.en.md,
    docs/reference/glossary.de.md,
    docs/reference/glossary.en.md,
    docs/training_database.md
  </read_first>
  <action>
Fix each broken link per D-02: when the target file does not exist, remove the link markup and keep the surrounding text. The link text becomes plain text. Do NOT delete the sentence — just de-link it.

Per D-06: before deleting any FILE (not link), grep all active docs for references to it. Log the grep result. This task does not delete files — it only modifies link markup.

**Fix instructions by file:**

**docs/administrators/streaming-production-deployment.md** (FIND-001):
- Line 571: link to `administrators/systemd-streaming-services.md` does not exist. Remove link markup, keep link text as plain text. E.g., `[systemd Streaming Services](administrators/systemd-streaming-services.md)` becomes `systemd Streaming Services`.

**docs/developers/developer-guide.en.md** (FIND-002, FIND-003):
- Lines 366, 458: links to `developers/enhanced_mode_system.md` (moved to obsolete/). Per D-02, remove link markup, keep text. The file exists in obsolete/ but we do not link to obsolete content.

**docs/developers/operations/tournament-game-protection.de.md** (FIND-004):
- Line 254: link to `developers/operations/FLASH_MESSAGES_SCOREBOARD.md` does not exist. Remove link, keep text.

**docs/developers/rake-tasks-debugging.de.md** (FIND-005):
- Line 423: link to `developers/scenario-system-workflow.md`. Check if `docs/developers/scenario-workflow.md` exists (note: without "system-"). If it does, update the link target to `scenario-workflow.md`. If not, remove link markup.

**docs/developers/rake-tasks-debugging.en.md** (FIND-006):
- Line 419: same fix as FIND-005 — update to `scenario-workflow.md` if it exists, otherwise remove link markup.

**docs/developers/scenario-workflow.md** (FIND-007, FIND-008, FIND-009):
- FIND-007: link to `CONTRIBUTING.md` outside docs root. Remove link, keep text.
- FIND-008: link to `README.md` outside docs root. Remove link, keep text.
- FIND-009: link to `carambus_master/docs/developers/`. Remove link, keep text.

**docs/developers/testing/fixture-collection-guide.md** (FIND-010 to FIND-015):
- 6 links to `test/` paths outside docs root (FIXTURES_QUICK_START.md, FIXTURES_SAMMELN.md, fixtures/html/README.md, FIXTURE_WORKFLOW.md). All targets are outside docs/ and cannot be linked from mkdocs. Remove all 6 link markups, keep text.

**docs/developers/testing/testing-quickstart.md** (FIND-016 to FIND-020):
- FIND-016, FIND-019: self-referencing links to `developers/testing/testing-quickstart.md`. These are circular — remove link markup, keep text.
- FIND-017, FIND-020: links to `developers/testing-strategy.md` (nonexistent, note: NOT in testing/ subdirectory). Check if `docs/developers/testing-strategy.de.md` or similar exists. If yes, fix the path. If no, remove link markup.
- FIND-018: link to `test/snapshots/README.md` outside docs root. Remove link, keep text.

**docs/international/umb_scraper.md** (FIND-021, FIND-022):
- FIND-021: link to `international/international_videos.md` (nonexistent). Remove link, keep text.
- FIND-022: link to `international/youtube_scraper.md` (nonexistent). Remove link, keep text.

**docs/managers/clubcloud_upload_feedback.md** (FIND-023, FIND-024):
- FIND-023: link to `bin/test-cc-name-mapping.rb` outside docs root. Remove link, keep text. Consider adding inline note: `(Skript: bin/test-cc-name-mapping.rb)` so the script name is still visible.
- FIND-024: link to `managers/logging_conventions.md` (nonexistent). Remove link, keep text.

**docs/players/ai-search.de.md** (FIND-025):
- Link to `search.md` (nonexistent). Remove link, keep text.

**docs/players/ai-search.en.md** (FIND-026):
- Link to `search.md` (nonexistent). Remove link, keep text.

**docs/reference/config-lock-files.de.md** (FIND-059):
- Link to `reference/PRODUCTION_SETUP.md` (nonexistent). Remove link, keep text.

**docs/reference/config-lock-files.en.md** (FIND-060):
- Link to `reference/PRODUCTION_SETUP.md` (nonexistent). Remove link, keep text.

**docs/reference/glossary.de.md** (FIND-061):
- Link to `search.md` (nonexistent). Remove link, keep text.

**docs/reference/glossary.en.md** (FIND-062):
- Link to `search.md` (nonexistent). Remove link, keep text.

**docs/training_database.md** (FIND-075):
- Link to `README.md` outside docs root. Remove link, keep text.

**Pattern for link removal:**
- `[link text](broken/path.md)` becomes `link text`
- `[link text](broken/path.md "title")` becomes `link text`
- Preserve surrounding punctuation and sentence structure
  </action>
  <verify>
    <automated>ruby bin/check-docs-links.rb --exclude-archives 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `ruby bin/check-docs-links.rb --exclude-archives` exits with code 0 (zero broken links)
    - `grep -r "enhanced_mode_system" docs/developers/developer-guide.en.md` returns no matches containing `](` link syntax
    - `grep -r "FLASH_MESSAGES_SCOREBOARD" docs/developers/operations/tournament-game-protection.de.md` returns no matches containing `](` link syntax
    - `grep -r "FIXTURES_QUICK_START\|FIXTURES_SAMMELN\|FIXTURE_WORKFLOW" docs/developers/testing/fixture-collection-guide.md` returns no matches containing `](` link syntax
    - `grep -r "PRODUCTION_SETUP" docs/reference/config-lock-files.de.md docs/reference/config-lock-files.en.md` returns no matches containing `](` link syntax
    - All 17 files still contain their original non-link content (text preserved, only markup removed)
  </acceptance_criteria>
  <done>All 31 remaining broken links resolved. Link checker reports zero broken links across all active docs. No file deletions performed — only link markup removals.</done>
</task>

<task type="auto">
  <name>Task 2: Fix 3 stale code references and run final verification (per D-03, D-04)</name>
  <files>
    docs/developers/umb-scraping-methods.md,
    docs/developers/clubcloud-upload.de.md,
    docs/developers/clubcloud-upload.en.md
  </files>
  <read_first>
    docs/audit.json,
    bin/check-docs-coderef.rb,
    docs/developers/umb-scraping-methods.md,
    docs/developers/clubcloud-upload.de.md,
    docs/developers/clubcloud-upload.en.md
  </read_first>
  <action>
**Per D-03:** Update stale references to current names rather than deleting context.

**FIND-076: docs/developers/umb-scraping-methods.md line 73**
The audit.json action is "DELETE" but D-03 overrides this: update the reference rather than deleting context. At line 73, replace `UmbScraperV2` with the current scraper architecture name. The correct replacement is `Umb::` namespace services (specifically the relevant service name in context — likely `Umb::DetailsScraper` or `Umb::ArchiveScraper` or simply `Umb:: services`). Read the surrounding context at line 73 to determine the best replacement. The key point: the reader should understand what the current scraper is called.

Note: This is a MINIMAL structural fix per Phase 29 scope. The full content rewrite of umb-scraping-methods.md is Phase 30 (UPDATE-01). Only change the class name reference, not the surrounding description.

**FIND-077: docs/developers/clubcloud-upload.de.md line 194**
A code comment references `lib/tournament_monitor_support.rb` which no longer exists. Per D-03, update the path to `app/services/tournament_monitor/` (the current location after v2.1 refactoring). Change `lib/tournament_monitor_support.rb` to `app/services/tournament_monitor/` in the code comment.

**FIND-078: docs/developers/clubcloud-upload.en.md line 194**
Same fix as FIND-077 — update `lib/tournament_monitor_support.rb` to `app/services/tournament_monitor/` in the code comment.

**Final verification (per D-04):**
After all fixes, run both verification tools:
1. `ruby bin/check-docs-links.rb --exclude-archives` — must report zero broken links
2. `ruby bin/check-docs-coderef.rb --exclude-archives` — must report zero stale references
3. `bundle exec rake mkdocs:check` — must pass (per D-05: only link-related warnings matter, but check anyway)

If any verification fails, investigate and fix the remaining issues before marking done.
  </action>
  <verify>
    <automated>ruby bin/check-docs-links.rb --exclude-archives && ruby bin/check-docs-coderef.rb --exclude-archives && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `ruby bin/check-docs-coderef.rb --exclude-archives` exits with code 0 (zero stale references)
    - `ruby bin/check-docs-links.rb --exclude-archives` exits with code 0 (zero broken links)
    - `grep -n "UmbScraperV2" docs/developers/umb-scraping-methods.md` returns zero matches
    - `grep -n "tournament_monitor_support" docs/developers/clubcloud-upload.de.md` returns zero matches
    - `grep -n "tournament_monitor_support" docs/developers/clubcloud-upload.en.md` returns zero matches
    - `docs/developers/umb-scraping-methods.md` line ~73 contains `Umb::` (the replacement namespace)
    - `docs/developers/clubcloud-upload.de.md` line ~194 contains `app/services/tournament_monitor/`
    - `docs/developers/clubcloud-upload.en.md` line ~194 contains `app/services/tournament_monitor/`
  </acceptance_criteria>
  <done>All 3 stale references updated to current names. Both checker scripts report zero findings. Phase 29 goal achieved: zero broken internal links and zero stale code references in active docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bin/ scripts -> docs/ files | Scripts validate documentation files on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | T (Tampering) | docs/ content edits | accept | Documentation is version-controlled; all changes are committed and reviewable via git diff. No executable code is modified. |
| T-29-04 | I (Info Disclosure) | docs/ content | accept | Documentation is public-facing; no credentials or secrets in doc files. |
</threat_model>

<verification>
- `ruby bin/check-docs-links.rb --exclude-archives` exits 0 — zero broken links
- `ruby bin/check-docs-coderef.rb --exclude-archives` exits 0 — zero stale references
- `bundle exec rake mkdocs:check` passes (zero missing-file warnings for nav entries, per D-05)
- No active doc contains `UmbScraperV2` as a current class reference
- No active doc contains `tournament_monitor_support` as a current file path
</verification>

<success_criteria>
- All 75 broken links resolved (44 in Plan 01 + 31 in Plan 02) — FIX-01 complete
- All 3 stale code references updated — FIX-02 complete
- Both checker scripts report zero findings
- mkdocs build passes for nav entries
</success_criteria>

<output>
After completion, create `.planning/phases/29-break-fix/29-02-SUMMARY.md`
</output>
