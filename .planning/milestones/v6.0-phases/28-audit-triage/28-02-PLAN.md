---
phase: 28-audit-triage
plan: 02
type: execute
wave: 2
depends_on:
  - "28-01"
files_modified:
  - docs/audit.json
  - docs/DOCS-AUDIT-REPORT.md
autonomous: true
requirements:
  - AUDIT-01
must_haves:
  truths:
    - "A machine-parseable audit.json exists with every finding classified by category, action, severity, and target phase"
    - "A human-readable DOCS-AUDIT-REPORT.md exists with sections for broken links, stale refs, coverage gaps, and bilingual gaps"
    - "Every finding has an action classification: FIX, DELETE, UPDATE, or CREATE"
    - "Every finding has a target phase assignment (29, 30, 31, or 32)"
    - "Archive directory indexing status is confirmed and documented"
  artifacts:
    - path: "docs/audit.json"
      provides: "Machine-parseable staleness inventory"
      contains: "findings"
    - path: "docs/DOCS-AUDIT-REPORT.md"
      provides: "Human-readable audit summary"
      contains: "Broken Links"
  key_links:
    - from: "docs/audit.json"
      to: "Phases 29-32"
      via: "phase field on each finding"
      pattern: "\"phase\":"
---

<objective>
Run all audit tools from Plan 01, collect findings, and produce the dual-output staleness inventory (audit.json + DOCS-AUDIT-REPORT.md) per D-01/D-02. This inventory gates all subsequent documentation phases 29-32.

Purpose: Create the single source of truth for all documentation problems, classified and assigned to phases, so that Phases 29-32 can execute without ambiguity.
Output: docs/audit.json (machine-parseable), docs/DOCS-AUDIT-REPORT.md (human-readable).
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-audit-triage/28-CONTEXT.md
@.planning/phases/28-audit-triage/28-RESEARCH.md
@.planning/phases/28-audit-triage/28-01-SUMMARY.md

<interfaces>
<!-- Scripts created in Plan 01 that this plan runs -->

bin/check-docs-links.rb:
  - Accepts --exclude-archives flag
  - Reports broken links with file:line:link_text:link_url format
  - Exit 0 = clean, exit 1 = findings

bin/check-docs-translations.rb:
  - Accepts --exclude-archives, --nav-only flags
  - Reports MISSING_EN: and MISSING_DE: lines
  - Exit 0 = clean, exit 1 = gaps

bin/check-docs-coderef.rb:
  - Accepts --exclude-archives, --json flags
  - Reports STALE_REF: file:line - identifier
  - Uses git diff v1.0..v5.0 for stale identifier discovery
  - Exit 0 = clean, exit 1 = stale refs

<!-- audit.json schema from RESEARCH.md -->
```json
{
  "generated_at": "ISO8601",
  "summary": {
    "total_findings": 0,
    "by_action": { "DELETE": 0, "UPDATE": 0, "CREATE": 0, "FIX": 0 },
    "by_category": { "broken_link": 0, "stale_ref": 0, "coverage_gap": 0, "bilingual_gap": 0 }
  },
  "findings": [
    {
      "id": "FIND-001",
      "category": "broken_link|stale_ref|coverage_gap|bilingual_gap",
      "action": "FIX|DELETE|UPDATE|CREATE",
      "severity": "high|medium|low",
      "file": "docs/relative/path.md",
      "line": 42,
      "description": "Human-readable description",
      "target": "optional target path or identifier",
      "phase": 29
    }
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all audit tools and collect raw findings</name>
  <files>docs/audit.json, docs/DOCS-AUDIT-REPORT.md</files>
  <read_first>
    bin/check-docs-links.rb
    bin/check-docs-translations.rb
    bin/check-docs-coderef.rb
    docs/BROKEN_LINKS_REPORT.txt
    .planning/phases/28-audit-triage/28-RESEARCH.md
  </read_first>
  <action>
Run each audit tool and capture output. Then manually enumerate coverage gaps (37 undocumented services). Collect ALL findings into a single in-memory data structure before writing any files.

**Step 1 — Run existing link checker:**
```bash
ruby bin/check-docs-links.rb --exclude-archives 2>&1
```
Parse the output to extract each broken link as a finding. Each broken link becomes a finding with:
- category: `broken_link`
- action: `FIX`
- severity: `high` for developers/, `medium` for managers/administrators/, `low` for reference/mkdocs_dokumentation placeholder links
- phase: `29`
- Note: The 8 placeholder links in reference/mkdocs_dokumentation.*.md (links to `file.md`, `datei.md`, `assets/image.png` etc.) should get `severity: low` and `note: "intentional documentation syntax example"`

**Step 2 — Run translation checker (both modes):**
```bash
ruby bin/check-docs-translations.rb --exclude-archives 2>&1
ruby bin/check-docs-translations.rb --nav-only 2>&1
```
Parse output. Each MISSING_EN/MISSING_DE becomes a finding with:
- category: `bilingual_gap`
- action: `CREATE` (for missing EN files that should exist) or `FIX` (for nav-linked gaps)
- severity: `high` for nav-linked gaps (managers/table-reservation.de.md), `low` for non-nav gaps
- phase: `32` for nav-linked gaps, `32` for non-nav gaps (or note as out-of-scope per TRANSLATE-01 deferral)
- For the 26 de-only non-nav files: mark with `note: "deferred — TRANSLATE-01"` and `phase: null`

**Step 3 — Run code reference checker:**
```bash
ruby bin/check-docs-coderef.rb --exclude-archives --json 2>&1
```
Parse JSON output. Each stale ref becomes a finding with:
- category: `stale_ref`
- action: `DELETE` (if the reference should simply be removed) or `UPDATE` (if surrounding content needs rewriting)
- severity: `high` (actively misleads developers about current architecture)
- phase: `29` for simple deletions, `30` for content that needs rewriting
- Known findings per RESEARCH.md:
  - `UmbScraperV2` in developers/umb-scraping-methods.md -> action: `DELETE`, phase: 29
  - `tournament_monitor_support` in developers/clubcloud-upload.de.md -> action: `UPDATE`, phase: 29
  - `tournament_monitor_support` in developers/clubcloud-upload.en.md -> action: `UPDATE`, phase: 29
  - `TournamentMonitorSupport` in developers/tournament-architecture-overview.en.md -> action: `UPDATE`, phase: 29

**Step 4 — Enumerate coverage gaps (37 undocumented services):**
Build findings for each undocumented service namespace. These are CREATE items for Phase 31. The 37 services across 8 namespaces from FEATURES.md research:
- TableMonitor:: (4 services: CommandHandler, DiagramBuilder, RefreshService, SoundService)
- RegionCc:: (10 services: BundScraper, LandScraper, BezirkScraper, VereinScraper, ClubSeasonScraper, SeasonScraper, GeneralScraper, Matchers, NameNormalizer, RegionDetector)
- Tournament:: (3 services: StatusUpdater, RegistrationService, DuplicateResolver)
- TournamentMonitor:: (4 services: TablePopulator, ResultProcessor, ScoreCalculator, SoundNotifier)
- League:: (4 services: StandingsCalculator, GamePlanReconstructor, ClubCloudScraper, BbvScraper)
- PartyMonitor:: (2 services: TablePopulator, ResultProcessor)
- Umb:: (10 services: HttpClient, DisciplineDetector, DateHelpers, PlayerResolver, PlayerListParser, GroupResultParser, RankingParser, FutureScraper, ArchiveScraper, DetailsScraper)
- Video:: (3 services: TournamentMatcher, MetadataExtractor, SoopliveBilliardsClient — technically only 2 are Video:: namespaced)

Each namespace gets ONE finding with:
- category: `coverage_gap`
- action: `CREATE`
- severity: `medium`
- phase: `31`
- description listing the services in that namespace

**Step 5 — Confirm archive indexing status:**
After D-05 was applied in Plan 01 (exclude_docs in mkdocs.yml), verify by checking mkdocs.yml contains `exclude_docs` with `archive/**` and `obsolete/**`. Add one informational finding:
- category: `coverage_gap` (or new subcategory)
- action: `FIX`
- severity: `low`
- phase: `28` (already fixed)
- note: `"Archive exclusion applied via exclude_docs in mkdocs.yml — archive/ and obsolete/ no longer built or indexed"`
  </action>
  <verify>
    <automated>ruby bin/check-docs-links.rb --exclude-archives 2>&1 | tail -3 && ruby bin/check-docs-translations.rb --nav-only 2>&1 | tail -3 && ruby bin/check-docs-coderef.rb --exclude-archives 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All three scripts run without error (may exit 1 due to findings, but no Ruby crashes)
    - Output from each script is captured for processing in Task 2
  </acceptance_criteria>
  <done>
    - All audit tools have been run and raw findings collected
    - Coverage gap enumeration for 37 services across 8 namespaces is complete
    - Archive indexing status is confirmed
  </done>
</task>

<task type="auto">
  <name>Task 2: Write docs/audit.json and docs/DOCS-AUDIT-REPORT.md (D-01, D-02)</name>
  <files>docs/audit.json, docs/DOCS-AUDIT-REPORT.md</files>
  <read_first>
    .planning/phases/28-audit-triage/28-RESEARCH.md
    .planning/phases/28-audit-triage/28-CONTEXT.md
    docs/BROKEN_LINKS_REPORT.txt
  </read_first>
  <action>
Using all findings collected in Task 1, write both output files in a single pass. Do NOT write partial files.

**docs/audit.json (D-01, D-02):**

Write valid JSON with this exact schema:
```json
{
  "generated_at": "2026-04-12T...",
  "phase": "28-audit-triage",
  "summary": {
    "total_findings": N,
    "by_action": { "FIX": N, "DELETE": N, "UPDATE": N, "CREATE": N },
    "by_category": { "broken_link": N, "stale_ref": N, "coverage_gap": N, "bilingual_gap": N },
    "by_phase": { "28": N, "29": N, "30": N, "31": N, "32": N, "null": N },
    "archive_indexing": "excluded via mkdocs.yml exclude_docs"
  },
  "findings": [
    {
      "id": "FIND-001",
      "category": "broken_link",
      "action": "FIX",
      "severity": "high",
      "file": "docs/developers/some-file.md",
      "line": 42,
      "description": "Link to nonexistent file",
      "target": "developers/missing-file.md",
      "phase": 29,
      "note": null
    }
  ]
}
```

Finding ID format: `FIND-001` through `FIND-NNN`, sequential across all categories.

Phase assignment rules:
- broken_link + FIX -> phase 29
- stale_ref + DELETE -> phase 29
- stale_ref + UPDATE -> phase 29 (simple reference removal) or phase 30 (content rewrite)
- coverage_gap + CREATE -> phase 31
- bilingual_gap + CREATE (nav-linked) -> phase 32
- bilingual_gap + CREATE (non-nav, deferred) -> phase null with note "deferred — TRANSLATE-01"

**docs/DOCS-AUDIT-REPORT.md:**

Write a structured markdown report with these exact sections:

```markdown
# Documentation Audit Report

Generated: 2026-04-12
Phase: 28-audit-triage

## Summary

| Category | Count | Target Phase |
|----------|-------|-------------|
| Broken Links | N | Phase 29 |
| Stale Code References | N | Phase 29 |
| Coverage Gaps (services) | N namespaces | Phase 31 |
| Bilingual Gaps (nav-linked) | N | Phase 32 |
| Bilingual Gaps (non-nav, deferred) | N | TRANSLATE-01 |
| **Total Findings** | **N** | |

## Archive Indexing Status

[Status: FIXED in Phase 28]
- `mkdocs.yml` `exclude_docs` now excludes `archive/**` and `obsolete/**`
- Archive pages are no longer built into the site or indexed by search

## Broken Links (Phase 29)

[Table with columns: File | Line | Link Text | Target | Severity]
[Grouped by directory: players/, developers/, reference/, managers/, international/, administrators/]

## Stale Code References (Phase 29)

[Table with columns: File | Line | Identifier | Action | Severity]

## Coverage Gaps — Undocumented Services (Phase 31)

[List each namespace with its services]
- TableMonitor:: (4 services) — CommandHandler, DiagramBuilder, RefreshService, SoundService
- RegionCc:: (10 services) — ...
- ...

## Bilingual Gaps

### Nav-Linked (Phase 32)
[Table: Nav Entry | Missing File | Severity]

### Non-Nav (Deferred — TRANSLATE-01)
[Count and list of 26 de-only files not in nav]

## Phase Workload Summary

| Phase | FIX | DELETE | UPDATE | CREATE | Total |
|-------|-----|--------|--------|--------|-------|
| 29 | N | N | N | 0 | N |
| 30 | 0 | 0 | N | 0 | N |
| 31 | 0 | 0 | 0 | N | N |
| 32 | 0 | 0 | 0 | N | N |
```

Ensure the JSON is valid (test with `ruby -rjson -e 'JSON.parse(File.read("docs/audit.json"))'`).
  </action>
  <verify>
    <automated>ruby -rjson -e 'j = JSON.parse(File.read("docs/audit.json")); puts "Total: #{j["summary"]["total_findings"]}"; puts "Categories: #{j["summary"]["by_category"]}"' && head -30 docs/DOCS-AUDIT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/audit.json is valid JSON (ruby -rjson parse does not error)
    - docs/audit.json contains `"generated_at"` field
    - docs/audit.json contains `"summary"` with `"total_findings"`, `"by_action"`, `"by_category"`, `"by_phase"` keys
    - docs/audit.json contains `"findings"` array with at least 74 broken_link entries (known baseline)
    - docs/audit.json contains findings with `"category": "stale_ref"` (at least 4 — UmbScraperV2 + tournament_monitor_support entries)
    - docs/audit.json contains findings with `"category": "coverage_gap"` (8 namespace entries)
    - docs/audit.json contains findings with `"category": "bilingual_gap"` (at least 1 nav-linked)
    - docs/audit.json every finding has `"id"`, `"category"`, `"action"`, `"severity"`, `"file"`, `"phase"` fields
    - docs/DOCS-AUDIT-REPORT.md contains `# Documentation Audit Report`
    - docs/DOCS-AUDIT-REPORT.md contains `## Broken Links`
    - docs/DOCS-AUDIT-REPORT.md contains `## Stale Code References`
    - docs/DOCS-AUDIT-REPORT.md contains `## Coverage Gaps`
    - docs/DOCS-AUDIT-REPORT.md contains `## Bilingual Gaps`
    - docs/DOCS-AUDIT-REPORT.md contains `## Archive Indexing Status`
    - docs/DOCS-AUDIT-REPORT.md contains `## Phase Workload Summary`
  </acceptance_criteria>
  <done>
    - docs/audit.json exists with all findings from all four audit categories
    - docs/DOCS-AUDIT-REPORT.md exists with all six sections populated
    - JSON is parseable and contains correct summary counts
    - Every known stale identifier (UmbScraperV2, tournament_monitor_support, TournamentMonitorSupport) appears in findings
    - All 8 namespace coverage gaps are listed
    - The nav-linked bilingual gap (managers/table-reservation.de.md) is recorded
    - Archive indexing status is confirmed as FIXED
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system write | Plan writes docs/audit.json and docs/DOCS-AUDIT-REPORT.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | I (Information Disclosure) | docs/audit.json | accept | Contains only file paths and line numbers within docs/; no secrets, credentials, or PII |
| T-28-05 | T (Tampering) | docs/audit.json | accept | Developer tool output, not serving user-facing content; committed to git for auditability |
</threat_model>

<verification>
- `ruby -rjson -e 'JSON.parse(File.read("docs/audit.json"))'` exits 0 (valid JSON)
- `grep -c '"category"' docs/audit.json` returns count > 80 (74 broken links + stale refs + gaps)
- `grep "## Broken Links" docs/DOCS-AUDIT-REPORT.md` finds the section
- `grep "UmbScraperV2" docs/audit.json` finds at least one stale_ref finding
- `grep "Phase Workload Summary" docs/DOCS-AUDIT-REPORT.md` finds the section
</verification>

<success_criteria>
- Complete staleness inventory exists in both machine and human formats
- Every finding has category, action, severity, and phase assignment
- Inventory covers all four categories: broken links, stale refs, coverage gaps, bilingual gaps
- Archive indexing status is confirmed and documented
- Downstream phases (29-32) can parse audit.json to scope their work
</success_criteria>

<output>
After completion, create `.planning/phases/28-audit-triage/28-02-SUMMARY.md`
</output>
