---
phase: 31-new-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/developers/services/video-crossref.de.md
  - docs/developers/services/video-crossref.en.md
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "Video:: cross-referencing page exists in DE and EN documenting TournamentMatcher confidence scoring with 0.75 threshold"
    - "Video:: page documents MetadataExtractor regex-first + AI fallback strategy"
    - "Video:: page documents SoopliveBilliardsClient replay_no linking with replay_no==0 guard"
    - "Video:: page documents operational workflow for backfill vs incremental matching"
  artifacts:
    - path: "docs/developers/services/video-crossref.de.md"
      provides: "Video:: cross-referencing documentation (German)"
      contains: "CONFIDENCE_THRESHOLD"
    - path: "docs/developers/services/video-crossref.en.md"
      provides: "Video:: cross-referencing documentation (English)"
      contains: "CONFIDENCE_THRESHOLD"
  key_links:
    - from: "docs/developers/services/video-crossref.de.md"
      to: "app/models/video/tournament_matcher.rb"
      via: "documents public interface"
      pattern: "TournamentMatcher"
    - from: "docs/developers/services/video-crossref.de.md"
      to: "app/models/video/metadata_extractor.rb"
      via: "documents extraction strategy"
      pattern: "MetadataExtractor"
    - from: "docs/developers/services/video-crossref.de.md"
      to: "app/services/sooplive_billiards_client.rb"
      via: "documents replay_no linking"
      pattern: "SoopliveBilliardsClient"
---

<objective>
Create the Video:: cross-referencing documentation page as a bilingual DE+EN pair, fulfilling the specific requirements of DOC-02 and SC-2.

Purpose: Satisfy DOC-02 — developers can understand the video cross-referencing system: TournamentMatcher confidence scoring, MetadataExtractor extraction strategy, SoopliveBilliardsClient replay_no linking, and the operational workflow.
Output: 2 markdown files (1 DE + 1 EN) in `docs/developers/services/`.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-new-documentation/31-CONTEXT.md
@.planning/phases/31-new-documentation/31-RESEARCH.md

Source files (source of truth for all content):
@app/models/video/tournament_matcher.rb
@app/models/video/metadata_extractor.rb
@app/services/sooplive_billiards_client.rb

Pattern reference:
@docs/developers/umb-scraping-implementation.de.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Video:: cross-referencing page (DE+EN)</name>
  <files>
    docs/developers/services/video-crossref.de.md,
    docs/developers/services/video-crossref.en.md
  </files>
  <read_first>
    .planning/phases/31-new-documentation/31-RESEARCH.md (Video:: section lines 449-558 — read the FULL section),
    app/models/video/tournament_matcher.rb,
    app/models/video/metadata_extractor.rb,
    app/services/sooplive_billiards_client.rb
  </read_first>
  <action>
**Video:: cross-referencing page (video-crossref.de.md, ~300-400 lines):**

This is the most specified page (D-04 + SC-2). It covers EXACTLY: TournamentMatcher confidence scoring, MetadataExtractor extraction strategy, SoopliveBilliardsClient replay_no linking, and operational workflow. No broader video system context (per D-04).

IMPORTANT: Video:: classes are in `app/models/video/`, NOT `app/services/video/`. The page must note this location difference (Pitfall 4 from RESEARCH.md).

Page structure:

1. `# Video:: — Cross-Referencing System`
   - Intro paragraph: Links Video records to InternationalTournament or InternationalGame records. Uses confidence-scored matching (TournamentMatcher) and metadata extraction (MetadataExtractor) for UMB/Kozoom videos, and direct replay_no linking (SoopliveBilliardsClient) for SoopLive videos.
   - Note: Unlike other namespace pages, Video:: classes live in `app/models/video/`, not `app/services/video/`. SoopliveBilliardsClient is a standalone service in `app/services/`.

2. `## Komponenten-Uebersicht`
   - Table with 3 entries: TournamentMatcher (app/models/video/), MetadataExtractor (app/models/video/), SoopliveBilliardsClient (app/services/)

3. `## Video::TournamentMatcher`
   - Type: ApplicationService
   - Public interface: `.call(video_scope:)` returning `{ assigned_count:, skipped_count:, results: }` (from RESEARCH.md lines 461-467)
   - **Confidence Scoring** subsection with the weights table (from RESEARCH.md lines 469-476):
     - Date overlap: weight 0.40, `date_overlap_score` method, 3-day grace period, nil end_date defaults to date + 7 days
     - Player intersection: weight 0.35, `player_intersection_score`, Jaccard similarity
     - Title similarity: weight 0.25, `title_similarity_score`, normalized Levenshtein
   - Threshold: `CONFIDENCE_THRESHOLD = 0.75` — auto-assign at or above, no review tier
   - `confidence_score(video, tournament, metadata)` is public (testable directly)

4. `## Video::MetadataExtractor`
   - Type: Pure PORO
   - Public interface: `.new(video)`, `extract_all`, `extract_players`, `extract_round`, `extract_tournament_type`, `extract_year`, `extract_with_ai_fallback(ai_extraction_enabled:)` (from RESEARCH.md lines 496-509)
   - **Regex-First Strategy** subsection:
     - ROUND_PATTERNS from `Umb::DetailsScraper::GAME_TYPE_MAPPINGS` keys (shared constant — cross-namespace dependency)
     - TOURNAMENT_TYPE_PATTERNS: named regexes for tournament type keywords
     - Year pattern: `/\b(20[12]\d)\b/`
   - **AI Fallback** subsection:
     - Model: gpt-4o-mini with JSON response format
     - Default: disabled (ai_extraction_enabled: false)
     - Rescue guard: failures return empty hash

5. `## SoopliveBilliardsClient`
   - Type: Plain Ruby class (not ApplicationService)
   - Location: `app/services/sooplive_billiards_client.rb` (standalone, not in video/ namespace)
   - Public interface: `fetch_games`, `fetch_matches(game_no)`, `fetch_results(game_no)`, `vod_url(replay_no)` class method, `link_match_vods(game_no, international_game:)`, `cross_reference_kozoom_videos` class method (from RESEARCH.md lines 527-553)
   - IMPORTANT: Document the `replay_no == 0` guard explicitly (Pitfall 5): `link_match_vods` skips replay_no == 0; callers of `vod_url` must check. `record_yn != "Y"` also skipped.
   - `link_match_vods` does NOT create new Video records — links pre-existing ones only.

6. `## Operativer Workflow`
   - Three operational modes (from RESEARCH.md lines 556-558):
     1. **Inkrementell (neues Turnier):** `fetch_matches(game_no)` then `link_match_vods(game_no, international_game:)` for each game — VOD URLs assigned directly
     2. **Backfill (bestehende Videos):** `Video::TournamentMatcher.call` over `Video.unassigned` — confidence scoring for SoopLive + Kozoom + other source videos
     3. **Kozoom Cross-Reference:** `SoopliveBilliardsClient.cross_reference_kozoom_videos` — batch operation matching via `json_data["eventId"]` to `InternationalTournament.external_id`
   - Include a simple flow description showing which path applies when.

7. `## Querverweise`
   - Link to `../developer-guide.md#extrahierte-services`
   - Link to Umb:: page for shared `GAME_TYPE_MAPPINGS` constant: `./umb.md`

Create `video-crossref.en.md` as a FULL English translation (not a stub — SC-3). Same structure, all technical content, translated headings and prose. Keep code blocks, class names, method names, and constants untranslated.

Per D-08: one commit for this bilingual pair.
  </action>
  <verify>
    <automated>test -f docs/developers/services/video-crossref.de.md && test -f docs/developers/services/video-crossref.en.md && grep -q "CONFIDENCE_THRESHOLD" docs/developers/services/video-crossref.de.md && grep -q "0.75" docs/developers/services/video-crossref.de.md && grep -q "0.40" docs/developers/services/video-crossref.de.md && grep -q "0.35" docs/developers/services/video-crossref.de.md && grep -q "0.25" docs/developers/services/video-crossref.de.md && grep -q "gpt-4o-mini" docs/developers/services/video-crossref.de.md && grep -q "replay_no" docs/developers/services/video-crossref.de.md && grep -q "CONFIDENCE_THRESHOLD" docs/developers/services/video-crossref.en.md && grep -q "replay_no" docs/developers/services/video-crossref.en.md && grep -q "app/models/video" docs/developers/services/video-crossref.de.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - video-crossref.de.md contains "CONFIDENCE_THRESHOLD", "0.75", weights (0.40, 0.35, 0.25)
    - video-crossref.de.md contains "MetadataExtractor", "gpt-4o-mini", "ai_extraction_enabled"
    - video-crossref.de.md contains "SoopliveBilliardsClient", "replay_no", "vod_url", guard note about replay_no == 0
    - video-crossref.de.md contains operational workflow section with 3 modes (incremental, backfill, kozoom)
    - video-crossref.de.md notes that Video:: lives in app/models/video/ (not app/services/)
    - video-crossref.de.md links to umb.md for GAME_TYPE_MAPPINGS
    - video-crossref.en.md is full English (not stub), contains all the same technical content
    - One git commit for this bilingual pair
  </acceptance_criteria>
  <done>2 files created (1 DE + 1 EN), Video:: cross-referencing page with TournamentMatcher confidence scoring, MetadataExtractor regex+AI strategy, SoopliveBilliardsClient replay_no linking, and operational workflow — committed as 1 bilingual-pair commit</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation content | No untrusted input — all content from verified source files |
| OpenAI API key reference | MetadataExtractor documents AI fallback but does not expose API keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Information Disclosure | video-crossref.*.md | accept | Documents public interface only; no API keys or credentials. AI model name (gpt-4o-mini) is not sensitive. |
</threat_model>

<verification>
- Both files exist in `docs/developers/services/`
- DE file contains all SC-2 required elements: confidence threshold 0.75, three weights, two-path matching, regex+AI, replay_no, operational workflow
- EN file is complete translation (not stub) with same technical depth
- `grep -c "replay_no" docs/developers/services/video-crossref.de.md` returns 3+ occurrences
</verification>

<success_criteria>
Video:: cross-referencing page exists as bilingual DE+EN pair documenting exactly what SC-2 requires: TournamentMatcher confidence scoring (0.75 threshold, weighted signals), MetadataExtractor (regex-first + AI fallback), SoopliveBilliardsClient (replay_no linking with guards), and operational workflow (backfill vs incremental vs Kozoom cross-reference).
</success_criteria>

<output>
After completion, create `.planning/phases/31-new-documentation/31-03-SUMMARY.md`
</output>
