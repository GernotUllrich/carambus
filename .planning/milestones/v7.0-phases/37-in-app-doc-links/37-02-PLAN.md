---
phase: 37-in-app-doc-links
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/managers/tournament-management.de.md
  - docs/managers/tournament-management.en.md
autonomous: true
requirements:
  - LINK-02
  - LINK-04
user_setup: []

must_haves:
  truths:
    - "#seeding-list anchor resolves to Schritt 3 / Step 3 in both locales"
    - "#participants anchor resolves to Schritt 4 / Step 4 in both locales"
    - "#mode-selection anchor resolves to Schritt 6 / Step 6 in both locales"
    - "#start-parameters anchor resolves to Schritt 7 / Step 7 in both locales"
    - "Only heading lines are edited — no prose, no reordering, no new sections"
  artifacts:
    - path: "docs/managers/tournament-management.de.md"
      provides: "4 stable {#anchor} attrs on existing Schritt headings"
      contains: "{#seeding-list}"
    - path: "docs/managers/tournament-management.en.md"
      provides: "4 stable {#anchor} attrs on matching Step headings (identical IDs)"
      contains: "{#seeding-list}"
  key_links:
    - from: "Wizard/form Ruby callers in Plans 03/04"
      to: "Doc section anchors"
      via: "mkdocs_link(path, anchor: 'seeding-list'|'participants'|'mode-selection'|'start-parameters')"
      pattern: "\\{#[a-z-]+\\}"
---

<objective>
Add exactly 4 stable `{#anchor}` attr_list attributes to the existing `### Schritt N / ### Step N` headings in both `tournament-management.de.md` and `tournament-management.en.md`. This is the ONLY content change to the doc files in Phase 37. The `attr_list` markdown extension is already enabled in `mkdocs.yml:248`, so the anchors become real `id=` attributes when mkdocs builds.

Purpose: LINK-04 requires ≥3 deep-link anchors. Adding 4 gives a safety margin and covers the wizard steps that have distinct doc sections. Sharing identical kebab-case IDs between DE and EN lets the Ruby helper stay locale-agnostic for anchors.

Output: 8 heading-line edits total (4 anchors × 2 language files). Prose unchanged.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-in-app-doc-links/37-CONTEXT.md
@docs/managers/tournament-management.de.md
@docs/managers/tournament-management.en.md
@mkdocs.yml

<interfaces>
<!-- The 4 target headings as they currently exist (extracted via grep). -->
<!-- Executor MUST NOT change the visible heading text, only append the {#id}. -->

docs/managers/tournament-management.de.md:
```
Line 52: ### Schritt 3: Setzliste übernehmen oder erzeugen        → append {#seeding-list}
Line 63: ### Schritt 4: Teilnehmerliste prüfen und ergänzen (Wizard Schritt 3)  → append {#participants}
Line 104: ### Schritt 6: Turniermodus auswählen                   → append {#mode-selection}
Line 118: ### Schritt 7: Start-Parameter und Tischzuordnung ausfüllen → append {#start-parameters}
```

docs/managers/tournament-management.en.md:
```
Line 59: ### Step 3: Take over or generate the seeding list       → append {#seeding-list}
Line 70: ### Step 4: Review and add participants (Wizard Step 3)  → append {#participants}
Line 111: ### Step 6: Select tournament mode                      → append {#mode-selection}
Line 125: ### Step 7: Start parameters and table assignment       → append {#start-parameters}
```

<!-- attr_list syntax: append a single space then {#id} to the end of the heading line. -->
<!-- Example: "### Schritt 3: Setzliste übernehmen oder erzeugen {#seeding-list}" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 4 stable anchors to tournament-management.de.md</name>
  <files>docs/managers/tournament-management.de.md</files>
  <read_first>
    - docs/managers/tournament-management.de.md (full file — verify heading text BEFORE editing; do not blindly edit line numbers if prior phases shifted them)
    - mkdocs.yml (line 248 area — confirm `attr_list` is under `markdown_extensions:`)
    - .planning/phases/37-in-app-doc-links/37-CONTEXT.md (D-06, D-07, D-08, D-09)
  </read_first>
  <action>
Append a single space + `{#id}` to the end of EXACTLY these 4 heading lines. Do not modify the heading prose. Do not add blank lines. Do not change section order. Do not touch any other heading.

Edit 1 — Schritt 3:
- Find: `### Schritt 3: Setzliste übernehmen oder erzeugen`
- Replace with: `### Schritt 3: Setzliste übernehmen oder erzeugen {#seeding-list}`

Edit 2 — Schritt 4:
- Find: `### Schritt 4: Teilnehmerliste prüfen und ergänzen (Wizard Schritt 3)`
- Replace with: `### Schritt 4: Teilnehmerliste prüfen und ergänzen (Wizard Schritt 3) {#participants}`

Edit 3 — Schritt 6:
- Find: `### Schritt 6: Turniermodus auswählen`
- Replace with: `### Schritt 6: Turniermodus auswählen {#mode-selection}`

Edit 4 — Schritt 7:
- Find: `### Schritt 7: Start-Parameter und Tischzuordnung ausfüllen`
- Replace with: `### Schritt 7: Start-Parameter und Tischzuordnung ausfüllen {#start-parameters}`

Notes:
- Use the Edit tool with find-and-replace on the exact heading string (no line-number reliance — Phase 36a may have shifted lines).
- If the Find string does not match verbatim (e.g., Phase 36a changed "übernehmen oder erzeugen" to "übernehmen"), STOP and report the mismatch in the SUMMARY rather than guessing.
- Do NOT introduce `{: .class}` or other attr_list syntax — plain `{#id}` only.
- Do NOT change any Step 1, 2, 5, 8–14 headings.
- Do NOT touch glossary, troubleshooting, or appendix headings.
  </action>
  <verify>
    <automated>grep -c '^### Schritt [3467]:.*{#\(seeding-list\|participants\|mode-selection\|start-parameters\)}$' docs/managers/tournament-management.de.md | grep -qE '^4$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '{#seeding-list}' docs/managers/tournament-management.de.md` returns exactly `1`
    - `grep -c '{#participants}' docs/managers/tournament-management.de.md` returns exactly `1`
    - `grep -c '{#mode-selection}' docs/managers/tournament-management.de.md` returns exactly `1`
    - `grep -c '{#start-parameters}' docs/managers/tournament-management.de.md` returns exactly `1`
    - `grep '{#seeding-list}' docs/managers/tournament-management.de.md` shows line starting with `### Schritt 3:` (anchor is on Schritt 3, not any other heading)
    - `grep '{#mode-selection}' docs/managers/tournament-management.de.md` shows line starting with `### Schritt 6:`
    - `grep '{#start-parameters}' docs/managers/tournament-management.de.md` shows line starting with `### Schritt 7:`
    - `grep '{#participants}' docs/managers/tournament-management.de.md` shows line starting with `### Schritt 4:`
    - `git diff docs/managers/tournament-management.de.md | grep -c '^+' ` should be small (≈5: 4 header edits + diff header line). If >10 added lines, executor has touched prose → halt.
    - No line is removed from the file (`git diff | grep -c '^-[^-]'` ≈ 4, matching the 4 pre-edit heading lines).
  </acceptance_criteria>
  <done>All 4 anchors appended to correct Schritt headings; no prose changes; `grep` counts match exactly 1 each.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 4 stable anchors to tournament-management.en.md</name>
  <files>docs/managers/tournament-management.en.md</files>
  <read_first>
    - docs/managers/tournament-management.en.md (full file — verify heading text before editing)
    - .planning/phases/37-in-app-doc-links/37-CONTEXT.md (D-08: identical IDs across locales)
  </read_first>
  <action>
Append `{#id}` to exactly these 4 English headings, mirroring Task 1. IDs are IDENTICAL to the DE file (per D-08).

Edit 1 — Step 3:
- Find: `### Step 3: Take over or generate the seeding list`
- Replace with: `### Step 3: Take over or generate the seeding list {#seeding-list}`

Edit 2 — Step 4:
- Find: `### Step 4: Review and add participants (Wizard Step 3)`
- Replace with: `### Step 4: Review and add participants (Wizard Step 3) {#participants}`

Edit 3 — Step 6:
- Find: `### Step 6: Select tournament mode`
- Replace with: `### Step 6: Select tournament mode {#mode-selection}`

Edit 4 — Step 7:
- Find: `### Step 7: Start parameters and table assignment`
- Replace with: `### Step 7: Start parameters and table assignment {#start-parameters}`

Notes:
- If Find string does not match verbatim, STOP and report — do NOT guess an alternative heading.
- IDs are English kebab-case, identical to DE file. No `en-` prefix.
- No other heading, no prose, no glossary/troubleshooting edits.
  </action>
  <verify>
    <automated>grep -c '^### Step [3467]:.*{#\(seeding-list\|participants\|mode-selection\|start-parameters\)}$' docs/managers/tournament-management.en.md | grep -qE '^4$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '{#seeding-list}' docs/managers/tournament-management.en.md` returns exactly `1`
    - `grep -c '{#participants}' docs/managers/tournament-management.en.md` returns exactly `1`
    - `grep -c '{#mode-selection}' docs/managers/tournament-management.en.md` returns exactly `1`
    - `grep -c '{#start-parameters}' docs/managers/tournament-management.en.md` returns exactly `1`
    - `grep '{#seeding-list}' docs/managers/tournament-management.en.md` shows line starting with `### Step 3:`
    - `grep '{#mode-selection}' docs/managers/tournament-management.en.md` shows line starting with `### Step 6:`
    - `grep '{#start-parameters}' docs/managers/tournament-management.en.md` shows line starting with `### Step 7:`
    - `grep '{#participants}' docs/managers/tournament-management.en.md` shows line starting with `### Step 4:`
    - `git diff docs/managers/tournament-management.en.md | grep -c '^+' ` is small (≈5)
  </acceptance_criteria>
  <done>All 4 anchors appended to correct Step headings; IDs match DE file; no prose changes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run mkdocs build --strict and verify no new warnings</name>
  <files></files>
  <read_first>
    - mkdocs.yml (confirm attr_list extension is enabled around line 248)
  </read_first>
  <action>
Run `mkdocs build --strict` from the repo root and capture exit code and warning count.

```bash
cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api
mkdocs build --strict 2>&1 | tee /tmp/mkdocs-37-02-build.log
echo "EXIT=$?"
grep -cE 'WARNING' /tmp/mkdocs-37-02-build.log || echo "0"
```

The Phase 35-36a baseline was 191 WARNING log lines (per STATE.md). This phase MUST NOT introduce any new warnings — delta 0 is the gate.

If the build fails or warning count increases:
1. Check the warning details — is it about attr_list parsing? Unclosed `{`?
2. Check that the anchor IDs match `^[a-z][a-z0-9-]*$` (they do: seeding-list, participants, mode-selection, start-parameters).
3. Report the specific failure in the SUMMARY and halt.

If `mkdocs` command is not installed in the working directory, document it in the SUMMARY and defer the strict build to the phase-level verification (Plan 05 test plan will re-check).
  </action>
  <verify>
    <automated>bash -c 'cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && (mkdocs build --strict 2>&1 | grep -cE "^WARNING" || echo 0) | awk "{ if (\$1 > 191) exit 1; else exit 0 }"'</automated>
  </verify>
  <acceptance_criteria>
    - `mkdocs build --strict` exits 0 OR the command is not installed (and this is documented in SUMMARY)
    - WARNING count in build output is ≤191 (baseline from Phase 35)
    - No new warning mentions the 4 new anchor IDs (`seeding-list`, `participants`, `mode-selection`, `start-parameters`)
    - If build runs: `site/managers/tournament-management/index.html` exists with `id="seeding-list"` on a heading element (grep the generated HTML)
  </acceptance_criteria>
  <done>mkdocs strict build passes with delta 0 warnings from baseline 191, OR build unavailable is documented for Plan 05 follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static mkdocs HTML → browser | anchors are static content; no dynamic behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Tampering | markdown attr_list | accept | IDs are hardcoded constants, not user-derived; any malformed attr_list would be caught by mkdocs --strict build. |
| T-37-05 | Denial of Service | mkdocs build | accept | Four single-line edits cannot affect build time meaningfully. |
</threat_model>

<verification>
- All 4 anchor IDs appear exactly once in each of the 2 doc files (8 grep hits total)
- Each anchor is attached to the correct Schritt/Step heading (Schritt 3→seeding-list, 4→participants, 6→mode-selection, 7→start-parameters)
- `git diff` shows only heading-line edits (no prose, no new/removed lines in paragraph bodies)
- `mkdocs build --strict` passes with warning delta 0 from baseline 191
</verification>

<success_criteria>
- Phase 37 heading-edits gate satisfied: exactly 8 heading-line edits, no prose changes
- Identical kebab-case IDs across DE and EN enabling locale-agnostic anchor Ruby helper
- mkdocs strict build clean (no new warnings)
</success_criteria>

<output>
After completion, create `.planning/phases/37-in-app-doc-links/37-02-SUMMARY.md`
</output>
