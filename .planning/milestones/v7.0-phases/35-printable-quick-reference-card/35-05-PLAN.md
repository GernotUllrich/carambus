---
phase: 35-printable-quick-reference-card
plan: 05
type: execute
wave: 5
depends_on:
  - 35-01
  - 35-02
  - 35-03
  - 35-04
files_modified:
  - .planning/phases/35-printable-quick-reference-card/35-VERIFICATION.md
  - .planning/phases/35-printable-quick-reference-card/35-01-BASELINE.txt
autonomous: false
requirements:
  - QREF-01
  - QREF-02
  - QREF-03
user_setup: []
tags:
  - validation
  - mkdocs-strict

must_haves:
  truths:
    - "`mkdocs build --strict` final warning count is <= the Plan 01 pre-edit baseline recorded in 35-01-BASELINE.txt (D-09 zero-new-warnings gate)"
    - "All 4 ROADMAP Phase 35 success criteria are verified by automated checks (file existence, nav reachability, print layout, shortcut cheat sheet presence, strict build)"
    - "All 4 F-NN references in the card (F-09, F-12, F-14, F-19) cite F-IDs that exist in 33-UX-FINDINGS.md"
    - "All walkthrough deep-links in the card resolve to anchors that exist in tournament-management.{de,en}.md"
    - "A human verification checkpoint confirms the printed-page smoke test: open the card in a browser, use File → Print → Preview (A4), and confirm mkdocs chrome is hidden and the page fits one sheet"
  artifacts:
    - path: ".planning/phases/35-printable-quick-reference-card/35-VERIFICATION.md"
      provides: "Phase 35 verification report with all 4 success criteria checked and final strict build counts"
      contains: "## Goal Achievement"
  key_links:
    - from: "35-VERIFICATION.md"
      to: "35-01-BASELINE.txt counts"
      via: "baseline vs final comparison"
      pattern: "baseline"
---

<objective>
Run the Phase 35 D-09 strict-build gate, verify all 4 ROADMAP success criteria with automated checks, verify all cross-references (F-NN citations + walkthrough deep-links), and produce `35-VERIFICATION.md`. Then pause for a single `checkpoint:human-verify` smoke-test of the printed layout (print preview in a browser, A4).

Purpose: Close Phase 35 with evidence. Guard against late strict-build regressions. Keep the human-in-the-loop step minimal and focused on what Claude literally cannot automate — the visual inspection of the print preview.

Output: 35-VERIFICATION.md with a passed status; 35-01-BASELINE.txt annotated with final counts.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-printable-quick-reference-card/35-CONTEXT.md
@.planning/phases/35-printable-quick-reference-card/35-01-BASELINE.txt
@.planning/phases/34-task-first-doc-rewrite/34-VERIFICATION.md
@.planning/phases/33-ux-review-wizard-audit/33-UX-FINDINGS.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/tournament-quick-reference.de.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/tournament-quick-reference.en.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/mkdocs.yml
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/stylesheets/print.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final strict-build gate and automated success-criteria checks</name>
  <files>
    .planning/phases/35-printable-quick-reference-card/35-01-BASELINE.txt
    .planning/phases/35-printable-quick-reference-card/35-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/35-printable-quick-reference-card/35-01-BASELINE.txt (all prior counts)
    - .planning/phases/35-printable-quick-reference-card/35-CONTEXT.md §D-09 / §D-09a (strict-build gate)
    - .planning/ROADMAP.md §Phase 35 success criteria (4 items)
  </read_first>
  <action>
    **Step 1 — Final strict build.** Run mkdocs strict one last time and capture the full output:
    ```bash
    cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master && mkdocs build --strict 2>&1 | tee /tmp/35-05-mkdocs-final.log
    final_exit_code=$?
    final_warnings=$(grep -c "^WARNING" /tmp/35-05-mkdocs-final.log)
    ```

    Append the final values to `.planning/phases/35-printable-quick-reference-card/35-01-BASELINE.txt`:
    ```
    final_mkdocs_build_strict_exit_code: <final_exit_code>
    final_warning_log_lines: <final_warnings>
    final_measured_at: <ISO-8601>
    delta_vs_baseline: <final_warnings minus baseline warning_log_lines — must be 0 or negative>
    ```

    **Step 2 — Automated Success Criteria Checks.** Run these checks and capture the result of each (PASS / FAIL):

    **SC-1: Files exist, reachable from nav, Before/During/After sections with checkboxes**
    ```bash
    test -f /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/tournament-quick-reference.de.md
    test -f /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/tournament-quick-reference.en.md
    grep -q "managers/tournament-quick-reference.md" /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/mkdocs.yml
    grep -q "Tournament Quick Reference: Turnier-Schnellreferenz" /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/mkdocs.yml
    for f in tournament-quick-reference.de.md tournament-quick-reference.en.md; do
      count=$(grep -c '^- \[ \]' /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/$f)
      [ "$count" -ge 19 ] && [ "$count" -le 25 ] || echo "SC-1 FAIL: $f has $count task-list items (expected 19-25)"
    done
    for anchor in before during after; do
      for f in tournament-quick-reference.de.md tournament-quick-reference.en.md; do
        grep -q "<a id=\"$anchor\">" /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/$f || echo "SC-1 FAIL: $anchor missing in $f"
      done
    done
    ```

    **SC-2: print.css hides mkdocs chrome and sets A4 layout**
    ```bash
    test -f /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/stylesheets/print.css
    grep -q "@media print" /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/stylesheets/print.css
    for sel in .md-header .md-sidebar .md-footer .md-tabs .md-search .md-nav .md-top; do
      grep -q "$sel" /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/stylesheets/print.css || echo "SC-2 FAIL: $sel missing from print.css"
    done
    grep -q "size: A4" /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/stylesheets/print.css || echo "SC-2 FAIL: no A4 page size rule"
    grep -q "stylesheets/print.css" /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/mkdocs.yml || echo "SC-2 FAIL: print.css not in extra_css"
    # Print-layout visual verification is deferred to Task 2 (human checkpoint)
    ```

    **SC-3: Scoreboard shortcut cheat sheet present (table + ASCII strip)**
    ```bash
    de_strip=$(sed -n '228p' /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/players/scoreboard-guide.de.md)
    en_strip=$(sed -n '227p' /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/players/scoreboard-guide.en.md)
    grep -Fxq "$de_strip" /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/tournament-quick-reference.de.md || echo "SC-3 FAIL: DE ASCII strip missing/altered"
    grep -Fxq "$en_strip" /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/tournament-quick-reference.en.md || echo "SC-3 FAIL: EN ASCII strip missing/altered"
    grep -q '^| Taste | Aktion | Wann |' /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/tournament-quick-reference.de.md || echo "SC-3 FAIL: DE shortcut table missing"
    grep -q '^| Key | Action | When |' /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/tournament-quick-reference.en.md || echo "SC-3 FAIL: EN shortcut table missing"
    ```

    **SC-4: `mkdocs build --strict` zero new warnings vs baseline**
    ```bash
    baseline=$(grep -E '^warning_log_lines: [0-9]+$' .planning/phases/35-printable-quick-reference-card/35-01-BASELINE.txt | awk '{print $2}')
    [ "$final_warnings" -le "$baseline" ] && echo "SC-4 PASS: final=$final_warnings baseline=$baseline (delta=$((final_warnings - baseline)))" || echo "SC-4 FAIL: final=$final_warnings baseline=$baseline"
    ```

    **Step 3 — Cross-reference checks.** Verify F-NN citations and deep-links:
    ```bash
    # F-NN refs must exist in 33-UX-FINDINGS.md
    for f in F-09 F-12 F-14 F-19; do
      grep -q "^| $f " .planning/phases/33-ux-review-wizard-audit/33-UX-FINDINGS.md || echo "CROSS-REF FAIL: $f missing from 33-UX-FINDINGS.md"
    done

    # Every deep-link target must exist in Phase 34 files
    for lang in de en; do
      card=/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/tournament-quick-reference.$lang.md
      walkthrough=/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master/docs/managers/tournament-management.$lang.md
      for target in $(grep -oE 'tournament-management\.md#[a-z0-9-]+' "$card" | sed 's/.*#//' | sort -u); do
        grep -q "<a id=\"$target\">" "$walkthrough" || echo "CROSS-REF FAIL: #$target not in tournament-management.$lang.md"
      done
    done
    ```

    **Step 4 — Write `.planning/phases/35-printable-quick-reference-card/35-VERIFICATION.md`** with this structure:
    ```markdown
    ---
    phase: 35-printable-quick-reference-card
    verified: <ISO-8601>
    status: <passed | human_needed | failed>
    score: <N>/4 success criteria verified
    human_verification:
      - <if Task 2 checkpoint outstanding, list it here>
    gaps: []
    deferred: []
    ---

    # Phase 35: printable-quick-reference-card Verification Report

    ## Goal Achievement

    ### Success Criteria (from ROADMAP)

    | # | Criterion | Status | Evidence |
    |---|-----------|--------|----------|
    | 1 | Files exist, reachable from mkdocs nav, Before/During/After sections with checkbox items | <PASS/FAIL> | <grep counts> |
    | 2 | Print preview hides mkdocs chrome + A4 layout + legible font | <PASS/PENDING> | <automated: print.css contents; visual: Task 2 checkpoint> |
    | 3 | Scoreboard keyboard shortcut cheat sheet included | <PASS/FAIL> | <ASCII strip byte match, table row counts> |
    | 4 | mkdocs build --strict zero new warnings vs baseline | <PASS/FAIL> | baseline=<N> final=<N> delta=<N> |

    ### Cross-Reference Integrity

    | Check | Status | Evidence |
    |-------|--------|----------|
    | F-09/F-12/F-14/F-19 exist in 33-UX-FINDINGS.md | <PASS/FAIL> | grep output |
    | All card deep-links resolve to Phase 34 anchors | <PASS/FAIL> | per-lang loop output |

    ### Baseline Counts (from 35-01-BASELINE.txt)

    | Stage | Warning count |
    |-------|---------------|
    | Pre-edit baseline (Plan 01 Task 1) | <N> |
    | After Plan 01 (print.css + extra_css) | <N> |
    | After Plan 02 (skeleton + nav) | <N> |
    | After Plan 03 DE prose | <N> |
    | After Plan 03 EN prose | <N> |
    | After Plan 04 DE shortcuts | <N> |
    | After Plan 04 EN shortcuts | <N> |
    | Final (Plan 05) | <N> |

    **D-09 gate:** <PASSED/FAILED> — final warnings (<N>) <= baseline (<N>).

    ## Human Verification Required

    See Task 2 below — printed-page visual smoke test is the only step Claude cannot automate.
    ```

    Fill in every `<...>` placeholder with actual captured values from Steps 1-3.
  </action>
  <verify>
    <automated>test -f .planning/phases/35-printable-quick-reference-card/35-VERIFICATION.md && grep -q "^## Goal Achievement" .planning/phases/35-printable-quick-reference-card/35-VERIFICATION.md && grep -q "final_warning_log_lines:" .planning/phases/35-printable-quick-reference-card/35-01-BASELINE.txt && grep -q "delta_vs_baseline:" .planning/phases/35-printable-quick-reference-card/35-01-BASELINE.txt</automated>
  </verify>
  <acceptance_criteria>
    - `35-VERIFICATION.md` exists
    - File contains frontmatter with `status:`, `score:`, `human_verification:` fields
    - File contains a `## Goal Achievement` section with a 4-row success criteria table
    - File contains the `D-09 gate` line with a PASSED or FAILED marker
    - `35-01-BASELINE.txt` contains the `final_warning_log_lines:` line with a numeric value
    - `35-01-BASELINE.txt` contains the `delta_vs_baseline:` line with a value <= 0 (D-09 gate)
    - All cross-reference checks (F-NN refs + deep-links) report PASS (zero `CROSS-REF FAIL` lines when Step 3 runs)
    - SC-1 through SC-3 report PASS (zero `SC-* FAIL` lines)
    - SC-4 reports PASS (final <= baseline) OR status is marked `failed` in 35-VERIFICATION.md frontmatter
  </acceptance_criteria>
  <done>35-VERIFICATION.md written with all 4 success criteria checked, baseline file finalized, D-09 gate result recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test of printed layout (browser print preview)</name>
  <files>.planning/phases/35-printable-quick-reference-card/35-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/35-printable-quick-reference-card/35-VERIFICATION.md (Task 1 output — status should be `human_needed` awaiting this check)
  </read_first>
  <what-built>
    - `docs/managers/tournament-quick-reference.{de,en}.md` — bilingual printable checklist with Before / During / After sections, 4 UX-bug callouts, and a scoreboard keyboard shortcut cheat sheet
    - `docs/stylesheets/print.css` — `@media print` stylesheet that hides mkdocs navigation chrome (`.md-header`, `.md-sidebar`, `.md-footer`, `.md-tabs`, `.md-nav`, `.md-top`, `.md-search`) and enforces A4 layout (15mm top/bottom, 12mm left/right, 11pt body, 16pt H1, 14pt H2)
    - `mkdocs.yml` — new nav entry + nav_translations DE label + extra_css print.css reference
  </what-built>
  <how-to-verify>
    1. Start a local mkdocs dev server:
       ```bash
       cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_master && mkdocs serve -a localhost:8000
       ```
    2. Open DE version: `http://localhost:8000/de/managers/tournament-quick-reference/`
       - Confirm the page loads cleanly, the Before / During / After sections render with checkboxes
       - Confirm the scoreboard shortcut table and ASCII strip render
       - Confirm the 4 warning/tip callouts render as mkdocs-material admonitions
    3. Open the browser Print Preview (Chrome/Firefox: Ctrl+P / Cmd+P, A4 portrait, default margins)
       - Confirm the mkdocs header, left sidebar, right ToC sidebar, tabs, footer, and search bar are ALL HIDDEN in the preview
       - Confirm the card content expands to full A4 page width
       - Confirm the page fits on ONE A4 sheet (or at most two — soft ceiling per D-04a)
       - Confirm fonts are legible at "arm's length" (roughly 11pt body, 14-16pt headings)
       - Confirm checkboxes render as visible square boxes (pymdownx.tasklist with `custom_checkbox: true`)
       - Confirm the ASCII keycap strip is not clipped or wrapped mid-line
       - Confirm there is NO dependency on color for meaning — the preview would still be readable printed on a black-and-white printer
    4. Repeat for EN version: `http://localhost:8000/en/managers/tournament-quick-reference/`
    5. Stop the dev server: Ctrl+C
  </how-to-verify>
  <action>
    This task is a human checkpoint: Claude pauses execution and presents the `<what-built>` and `<how-to-verify>` blocks to the user. The user runs the steps above, inspects the two print previews, and replies with one of the `<resume-signal>` options. Claude does NOT write any files in this task; it only waits for the human response.

    On `approved` or `approved-with-notes`, Claude then edits `.planning/phases/35-printable-quick-reference-card/35-VERIFICATION.md` to set `status:` to `passed`, updates the SC-2 row to `PASS`, and appends any observations from `approved-with-notes` to the `## Human Verification Required` section.

    On `rejected: <reason>`, Claude leaves 35-VERIFICATION.md `status:` as `human_needed` and creates an entry in the `gaps:` frontmatter array describing the rejected aspect, then escalates to the orchestrator for a gap-closure plan.
  </action>
  <resume-signal>
    After verification, type one of:
    - `approved` — layout looks good in both languages
    - `approved-with-notes: <observations>` — acceptable but with notes for a future iteration
    - `rejected: <what's wrong>` — specify the issue and which file needs revision

    On `approved` or `approved-with-notes`, update `35-VERIFICATION.md` frontmatter `status:` to `passed` and append any notes to the `## Human Verification Required` section. On `rejected`, create a gap closure plan entry for the failing aspect.
  </resume-signal>
  <verify>
    <automated>grep -E '^status:\s*(passed|human_needed|failed)' .planning/phases/35-printable-quick-reference-card/35-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - User has provided a resume-signal (`approved`, `approved-with-notes: ...`, or `rejected: ...`)
    - `35-VERIFICATION.md` frontmatter `status:` reflects the signal (`passed` on approved*, `human_needed` still on rejected)
    - If rejected, `gaps:` frontmatter contains at least one entry describing the rejected observation
    - If approved-with-notes, the `## Human Verification Required` section contains the user's observations
  </acceptance_criteria>
  <done>Human verification signal captured, 35-VERIFICATION.md status updated accordingly, any rejection tracked in gaps array.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer laptop → mkdocs dev server | localhost-only preview; no network exposure. |
| browser → mkdocs static site | Public print rendering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-15 | Denial of Service | `mkdocs build --strict` | mitigated by design | Command has a deterministic bounded runtime on this repo (~30s). No infinite loops, no network I/O required. Failure to run blocks Task 1 acceptance — this is intentional, not a DoS surface. |
| T-35-16 | Repudiation | D-09 gate evidence | mitigated by design | All counts captured to `35-01-BASELINE.txt` with ISO-8601 timestamps; final report persists in `35-VERIFICATION.md` under version control. Any future regression is traceable to a specific post-Plan-05 commit. |
| T-35-17 | Information Disclosure | localhost mkdocs serve (Task 2) | accepted | Dev server binds to `localhost:8000` during human verification only; developer Ctrl+Cs after the check. No public exposure. Cited: Task 2 step 5 explicit shutdown. |
</threat_model>

<verification>
1. 35-VERIFICATION.md exists with populated success-criteria table
2. 35-01-BASELINE.txt contains final counts and D-09 delta
3. D-09 gate: final_warning_log_lines <= baseline warning_log_lines
4. Zero CROSS-REF FAIL lines
5. Task 2 human verification signal captured and reflected in frontmatter
</verification>

<success_criteria>
- All 4 ROADMAP Phase 35 success criteria PASS in 35-VERIFICATION.md
- D-09 gate satisfied: zero new mkdocs strict warnings vs Plan 01 pre-edit baseline
- All F-NN references and walkthrough deep-links are sound
- Human smoke test confirms the printed layout is clean, A4-sized, chrome-free, and legible
- Phase 35 can be marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/35-printable-quick-reference-card/35-05-SUMMARY.md` with:
- Final strict build count and delta vs baseline
- All 4 success-criteria results
- Human verification signal (approved / approved-with-notes / rejected)
- A "Known follow-ups" section listing any approved-with-notes observations for future phases
- Pointer to 35-VERIFICATION.md
</output>
