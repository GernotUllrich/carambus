---
phase: 20-characterization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/models/league_team_test.rb
  - test/models/party_test.rb
  - test/fixtures/league_teams.yml
  - test/fixtures/parties.yml
autonomous: true
requirements: [CHAR-05, CHAR-06]

must_haves:
  truths:
    - "LeagueTeam associations (league, club, parties_a, parties_b, seedings) are verified by tests"
    - "LeagueTeam cc_id_link returns a URL containing the correct parameters"
    - "Party associations (league, league_team_a, league_team_b, seedings, games) are verified"
    - "Party computed properties (name, party_nr, intermediate_result) return expected values"
    - "Party boolean flags (manual_assignment, allow_follow_up, continuous_placements) are pinned"
  artifacts:
    - path: "test/models/league_team_test.rb"
      provides: "LeagueTeam characterization tests"
    - path: "test/models/party_test.rb"
      provides: "Party characterization tests"
    - path: "test/fixtures/league_teams.yml"
      provides: "LeagueTeam fixture data for tests"
    - path: "test/fixtures/parties.yml"
      provides: "Party fixture data for tests"
  key_links:
    - from: "test/models/league_team_test.rb"
      to: "app/models/league_team.rb"
      via: "characterization assertions"
    - from: "test/models/party_test.rb"
      to: "app/models/party.rb"
      via: "characterization assertions"
---

<objective>
Pin LeagueTeam (63 lines) and Party (216 lines) critical paths with characterization tests.

Purpose: These are the simpler models in the Phase 20 scope. Pinning their behavior first provides fixture infrastructure (league_teams.yml, parties.yml) that other plans in this phase can reference. Per D-06, Party has no AASM — test associations, computed properties, and boolean flags. Per D-07, LeagueTeam is 63 lines — test associations, cc_id_link, and the empty scrape method.
Output: Two test files, two fixture files, all tests green.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-characterization/20-CONTEXT.md
@.planning/phases/20-characterization/20-RESEARCH.md
@app/models/league_team.rb
@app/models/party.rb
@test/fixtures/leagues.yml
@test/fixtures/regions.yml
@test/fixtures/seasons.yml
@test/fixtures/disciplines.yml
@test/test_helper.rb
</context>

<tasks>

<task type="auto">
  <name>Task 1: LeagueTeam characterization tests + fixtures</name>
  <files>test/fixtures/league_teams.yml, test/models/league_team_test.rb</files>
  <read_first>app/models/league_team.rb, test/fixtures/leagues.yml, test/fixtures/regions.yml, app/models/region.rb</read_first>
  <action>
Create `test/fixtures/league_teams.yml` with two fixture records using local IDs (>= 50_000_000):

```yaml
team_alpha:
  id: 50_000_010
  league: one
  name: "Team Alpha"
  cc_id: 101

team_beta:
  id: 50_000_011
  league: one
  name: "Team Beta"
  cc_id: 102
```

Create `test/models/league_team_test.rb` with `frozen_string_literal: true`. Test class inherits `ActiveSupport::TestCase`. Setup loads `league_teams(:team_alpha)`.

Tests to write (per D-07):

1. **Association tests:**
   - `test "belongs to league"` — assert `league_team.league` is not nil, is a League
   - `test "has many parties_a (home)"` — assert `league_team.respond_to?(:parties_a)` and it returns an ActiveRecord relation
   - `test "has many parties_b (guest)"` — assert `league_team.respond_to?(:parties_b)` and it returns an ActiveRecord relation
   - `test "has many seedings"` — assert `league_team.respond_to?(:seedings)`

2. **cc_id_link test:**
   - Read `app/models/region.rb` to check if `public_cc_url_base` is a column or method
   - Read `league_team.rb` line 58 for the exact `cc_id_link` implementation
   - If region fixture lacks `public_cc_url_base`, stub it on the organizer: `league_team.league.organizer.stubs(:public_cc_url_base).returns("https://nbv.club-cloud.de/")`
   - Assert the returned URL contains expected parameters (the `cc_id` value)
   - If `cc_id_link` returns nil when `public_cc_url_base` is missing, pin that nil behavior too

3. **scrape_players_from_ba_league_team test:**
   - `test "scrape_players_from_ba_league_team returns nil without raising"` — assert_nothing_raised, assert_nil

4. **name attribute test:**
   - `test "name returns the team name"` — assert_equal "Team Alpha", league_team.name

Verify all tests pass with: `bin/rails test test/models/league_team_test.rb`
  </action>
  <verify>
    <automated>bin/rails test test/models/league_team_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class LeagueTeamTest" test/models/league_team_test.rb
    - grep -q "frozen_string_literal: true" test/models/league_team_test.rb
    - grep -q "belongs to league" test/models/league_team_test.rb
    - grep -q "cc_id_link" test/models/league_team_test.rb
    - grep -q "scrape_players_from_ba_league_team" test/models/league_team_test.rb
    - grep -q "team_alpha" test/fixtures/league_teams.yml
    - grep -q "50_000_010" test/fixtures/league_teams.yml
  </acceptance_criteria>
  <done>LeagueTeam fixture file exists with local IDs. LeagueTeam test file has 5+ tests covering associations, cc_id_link, scrape stub, and name. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Party characterization tests + fixtures</name>
  <files>test/fixtures/parties.yml, test/models/party_test.rb</files>
  <read_first>app/models/party.rb, test/fixtures/league_teams.yml, test/fixtures/leagues.yml, test/test_helper.rb</read_first>
  <action>
Create `test/fixtures/parties.yml` with two fixture records using local IDs (>= 50_000_000). Parties reference league and league_teams from Task 1 fixtures:

```yaml
party_one:
  id: 50_000_020
  league: one
  league_team_a: team_alpha
  league_team_b: team_beta
  sets_to_play: 1
  sets_to_win: 1
  team_size: 4
  data: '{"result": "3:1"}'

party_two:
  id: 50_000_021
  league: one
  league_team_a: team_beta
  league_team_b: team_alpha
  sets_to_play: 1
  sets_to_win: 1
  team_size: 4
  data: '{"result": "2:2"}'
```

Create `test/models/party_test.rb` with `frozen_string_literal: true`. Test class inherits `ActiveSupport::TestCase`. Setup loads `parties(:party_one)`.

Tests to write (per D-06):

1. **Association tests:**
   - `test "belongs to league"` — assert party.league is present
   - `test "belongs to league_team_a"` — assert party.league_team_a is present and is a LeagueTeam
   - `test "belongs to league_team_b"` — assert party.league_team_b is present and is a LeagueTeam
   - `test "has many seedings"` — assert party.respond_to?(:seedings)

2. **Computed properties (per D-06):**
   - `test "name returns 'TeamA - TeamB' format"` — Read party.rb line 177 for exact format. Assert party.name includes both team names separated by " - "
   - `test "intermediate_result returns [0, 0]"` — Per RESEARCH Pitfall: `intermediate_result` returns `[0, 0]` unconditionally (dead code after early return). Assert `assert_equal [0, 0], party.intermediate_result`
   - `test "party_nr assigns party_no if blank and returns it"` — Per RESEARCH Pitfall 5: this method has a side effect (saves). Create a party programmatically with blank party_no, call party_nr, assert it returns a number and the record is saved. Use `unprotected = true` pattern.

3. **Boolean flags (per D-06):**
   - `test "manual_assignment returns true"` — Per RESEARCH: hardcoded to return `true` regardless of column. Assert `assert_equal true, party.manual_assignment`
   - `test "allow_follow_up responds to boolean"` — assert party.respond_to?(:allow_follow_up)
   - `test "continuous_placements responds to boolean"` — assert party.respond_to?(:continuous_placements)

4. **Data access:**
   - `test "data stores result hash"` — assert party.data is a Hash (or parsed from JSON), contains "result" key

Verify: `bin/rails test test/models/party_test.rb`
  </action>
  <verify>
    <automated>bin/rails test test/models/party_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class PartyTest" test/models/party_test.rb
    - grep -q "frozen_string_literal: true" test/models/party_test.rb
    - grep -q "intermediate_result" test/models/party_test.rb
    - grep -q "manual_assignment" test/models/party_test.rb
    - grep -q "party_nr" test/models/party_test.rb
    - grep -q "belongs to league" test/models/party_test.rb
    - grep -q "party_one" test/fixtures/parties.yml
    - grep -q "50_000_020" test/fixtures/parties.yml
  </acceptance_criteria>
  <done>Party fixture file exists with local IDs referencing league_teams fixtures. Party test file has 8+ tests covering associations, computed properties (name, intermediate_result, party_nr), boolean flags (manual_assignment, allow_follow_up, continuous_placements), and data access. All tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — test-only phase, no production code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | n/a | n/a | accept | Test-only phase — no new attack surface |
</threat_model>

<verification>
```bash
bin/rails test test/models/league_team_test.rb test/models/party_test.rb
```
All tests green, 0 failures, 0 errors.
</verification>

<success_criteria>
- LeagueTeam has 5+ characterization tests covering associations, cc_id_link, scrape stub
- Party has 8+ characterization tests covering associations, computed properties, boolean flags
- Both fixture files exist with local IDs >= 50_000_000
- All new tests pass
- Full test suite remains green (751+ runs, 0 failures)
</success_criteria>

<output>
After completion, create `.planning/phases/20-characterization/20-01-SUMMARY.md`
</output>
