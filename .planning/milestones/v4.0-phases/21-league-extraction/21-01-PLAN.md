---
phase: 21-league-extraction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/league/standings_calculator.rb
  - app/services/league/game_plan_reconstructor.rb
  - app/models/league.rb
  - test/services/league/standings_calculator_test.rb
  - test/services/league/game_plan_reconstructor_test.rb
autonomous: true
requirements: [EXTR-01, EXTR-03]
must_haves:
  truths:
    - "League#standings_table_karambol delegates to League::StandingsCalculator and returns identical results"
    - "League#standings_table_snooker delegates to League::StandingsCalculator and returns identical results"
    - "League#standings_table_pool delegates to League::StandingsCalculator and returns identical results"
    - "League#schedule_by_rounds delegates to League::StandingsCalculator and returns identical results"
    - "League#reconstruct_game_plan_from_existing_data delegates to League::GamePlanReconstructor"
    - "League.reconstruct_game_plans_for_season delegates to League::GamePlanReconstructor"
    - "All Phase 20 characterization tests pass without modification"
  artifacts:
    - path: "app/services/league/standings_calculator.rb"
      provides: "PORO standings calculator with karambol, snooker, pool, schedule_by_rounds methods"
      contains: "class League::StandingsCalculator"
    - path: "app/services/league/game_plan_reconstructor.rb"
      provides: "ApplicationService for game plan reconstruction operations"
      contains: "class League::GamePlanReconstructor < ApplicationService"
    - path: "test/services/league/standings_calculator_test.rb"
      provides: "Tests for StandingsCalculator service"
      contains: "class League::StandingsCalculatorTest"
    - path: "test/services/league/game_plan_reconstructor_test.rb"
      provides: "Tests for GamePlanReconstructor service"
      contains: "class League::GamePlanReconstructorTest"
  key_links:
    - from: "app/models/league.rb"
      to: "app/services/league/standings_calculator.rb"
      via: "delegation wrapper methods"
      pattern: "League::StandingsCalculator\\.new\\(self\\)"
    - from: "app/models/league.rb"
      to: "app/services/league/game_plan_reconstructor.rb"
      via: "delegation wrapper methods"
      pattern: "League::GamePlanReconstructor\\.call"
---

<objective>
Extract StandingsCalculator (PORO, ~226 lines) and GamePlanReconstructor (ApplicationService, ~426 lines) from League model, reducing it by ~650 lines while keeping all public API stable via thin delegation wrappers.

Purpose: First two extractions per D-02 ordering (easiest first). StandingsCalculator is pure calculation with no side effects. GamePlanReconstructor has DB writes but well-defined boundaries.
Output: Two new service classes with tests, League model reduced by ~650 lines.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-league-extraction/21-CONTEXT.md
@.planning/phases/21-league-extraction/21-RESEARCH.md

<interfaces>
<!-- ApplicationService base class -->
From app/services/application_service.rb:
```ruby
class ApplicationService
  def self.call(kwargs = {})
    new(kwargs).call
  end
end
```

<!-- PORO pattern reference -->
From app/services/tournament/ranking_calculator.rb:
```ruby
class Tournament::RankingCalculator
  def initialize(tournament)
    @tournament = tournament
  end
  # public instance methods for each calculation
end
```

<!-- Delegation wrapper pattern (from Tournament model) -->
```ruby
def calculate_and_cache_rankings
  Tournament::RankingCalculator.new(self).calculate_and_cache_rankings
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract League::StandingsCalculator PORO</name>
  <files>
    app/services/league/standings_calculator.rb,
    test/services/league/standings_calculator_test.rb,
    app/models/league.rb
  </files>
  <read_first>
    app/models/league.rb,
    app/services/tournament/ranking_calculator.rb,
    test/models/league_standings_test.rb,
    test/services/tournament/ranking_calculator_test.rb
  </read_first>
  <action>
1. Create directory `app/services/league/` (mkdir -p).

2. Create `app/services/league/standings_calculator.rb` as a PORO (per D-05):
   - `# frozen_string_literal: true`
   - Class `League::StandingsCalculator` (no inheritance from ApplicationService)
   - `def initialize(league)` storing `@league`
   - Extract these method bodies from `league.rb` into public instance methods:
     - `def karambol` — body of `League#standings_table_karambol`
     - `def snooker` — body of `League#standings_table_snooker`
     - `def pool` — body of `League#standings_table_pool`
     - `def schedule_by_rounds` — body of `League#schedule_by_rounds`
   - Any private helper methods called exclusively by these standings methods also move into the service. Read league.rb carefully to identify them.
   - Replace `self` references within moved methods with `@league` where they reference the League instance.

3. In `league.rb`, replace the extracted method bodies with thin one-liner delegation wrappers (per D-11, D-12):
   ```ruby
   def standings_table_karambol
     League::StandingsCalculator.new(self).karambol
   end

   def standings_table_snooker
     League::StandingsCalculator.new(self).snooker
   end

   def standings_table_pool
     League::StandingsCalculator.new(self).pool
   end

   def schedule_by_rounds
     League::StandingsCalculator.new(self).schedule_by_rounds
   end
   ```

4. Create `test/services/league/standings_calculator_test.rb` (mkdir -p `test/services/league/`):
   - `# frozen_string_literal: true`
   - `require "test_helper"`
   - Class `League::StandingsCalculatorTest < ActiveSupport::TestCase`
   - Use `TEST_ID_BASE = 50_000_000` and `ID_OFFSET = 70_000` (per RESEARCH.md)
   - Test that `League::StandingsCalculator.new(league).karambol` returns an Array
   - Test that `League::StandingsCalculator.new(league).snooker` returns an Array
   - Test that `League::StandingsCalculator.new(league).pool` returns an Array
   - Test that `League::StandingsCalculator.new(league).schedule_by_rounds` returns results
   - Each test creates a League with a local ID (>= TEST_ID_BASE), using `regions(:nbv)` as organizer and `seasons(:current)` / appropriate discipline fixtures

5. Run `bin/rails test test/services/league/standings_calculator_test.rb` — must pass.
6. Run `bin/rails test test/models/league_standings_test.rb` — must pass (characterization tests unchanged).
7. Run `bundle exec standardrb app/services/league/standings_calculator.rb app/models/league.rb` — must pass.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/league/standings_calculator_test.rb test/models/league_standings_test.rb test/models/league_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - app/services/league/standings_calculator.rb exists and contains `class League::StandingsCalculator`
    - app/services/league/standings_calculator.rb contains `def initialize(league)`
    - app/services/league/standings_calculator.rb contains `def karambol`, `def snooker`, `def pool`, `def schedule_by_rounds`
    - app/services/league/standings_calculator.rb contains `frozen_string_literal: true`
    - app/models/league.rb contains `League::StandingsCalculator.new(self).karambol`
    - app/models/league.rb contains `League::StandingsCalculator.new(self).snooker`
    - app/models/league.rb contains `League::StandingsCalculator.new(self).pool`
    - app/models/league.rb contains `League::StandingsCalculator.new(self).schedule_by_rounds`
    - test/services/league/standings_calculator_test.rb exists and contains `class League::StandingsCalculatorTest`
    - `bin/rails test test/services/league/standings_calculator_test.rb` exits 0
    - `bin/rails test test/models/league_standings_test.rb` exits 0 (characterization tests unmodified)
  </acceptance_criteria>
  <done>StandingsCalculator PORO extracted, ~226 lines moved out of league.rb, delegation wrappers in place, new service tests + all characterization tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Extract League::GamePlanReconstructor ApplicationService</name>
  <files>
    app/services/league/game_plan_reconstructor.rb,
    test/services/league/game_plan_reconstructor_test.rb,
    app/models/league.rb
  </files>
  <read_first>
    app/models/league.rb,
    app/services/league/standings_calculator.rb,
    app/services/region_cc/league_syncer.rb,
    test/models/league_test.rb
  </read_first>
  <action>
1. Create `app/services/league/game_plan_reconstructor.rb` (per D-06):
   - `# frozen_string_literal: true`
   - Class `League::GamePlanReconstructor < ApplicationService`
   - `def initialize(kwargs = {})` extracting `@league = kwargs[:league]`, `@season = kwargs[:season]`, `@operation = kwargs[:operation] || :reconstruct`, `@opts = kwargs.except(:league, :season, :operation)`
   - `def call` with dispatcher:
     ```ruby
     case @operation
     when :reconstruct then reconstruct
     when :reconstruct_for_season then reconstruct_for_season
     when :delete_for_season then delete_for_season
     else raise ArgumentError, "Unknown operation: #{@operation}"
     end
     ```
   - Extract these method bodies from `league.rb` as private methods:
     - `reconstruct` — body of `League#reconstruct_game_plan_from_existing_data` (~238 lines)
     - `reconstruct_for_season` — body of `League.reconstruct_game_plans_for_season` (~54 lines, class method becomes instance method using `@season`)
     - `delete_for_season` — body of `League#delete_game_plans_for_season`
     - `analyze_game_plan_structure` — private helper called only from `reconstruct`
     - `find_leagues_with_same_gameplan` — private helper (zero external callers per RESEARCH.md)
     - `find_or_create_shared_gameplan` — private helper (zero external callers per RESEARCH.md)
   - Replace `self` references with `@league` in extracted methods. For class-level methods that iterate leagues, use `League.where(...)` directly (the service is not tied to a single league instance for `:reconstruct_for_season`).

2. In `league.rb`, replace extracted method bodies with delegation wrappers (per D-11):
   ```ruby
   def reconstruct_game_plan_from_existing_data
     League::GamePlanReconstructor.call(league: self, operation: :reconstruct)
   end

   def self.reconstruct_game_plans_for_season(season, opts = {})
     League::GamePlanReconstructor.call(season: season, operation: :reconstruct_for_season, **opts)
   end

   def delete_game_plans_for_season(season)
     League::GamePlanReconstructor.call(league: self, season: season, operation: :delete_for_season)
   end
   ```
   - Remove `analyze_game_plan_structure`, `find_leagues_with_same_gameplan`, `find_or_create_shared_gameplan` from league.rb entirely (no external callers).

3. Create `test/services/league/game_plan_reconstructor_test.rb`:
   - `# frozen_string_literal: true`
   - `require "test_helper"`
   - Class `League::GamePlanReconstructorTest < ActiveSupport::TestCase`
   - Use `TEST_ID_BASE = 50_000_000` and `ID_OFFSET = 80_000` (unique range per RESEARCH.md)
   - Test `League::GamePlanReconstructor.call(league: league, operation: :reconstruct)` returns nil/GamePlan for empty league
   - Test `League::GamePlanReconstructor.call(season: season, operation: :reconstruct_for_season)` returns Hash with :success, :failed, :errors keys
   - Test `League::GamePlanReconstructor.call(league: league, season: season, operation: :delete_for_season)` does not raise
   - Test unknown operation raises `ArgumentError`

4. Run `bin/rails test test/services/league/game_plan_reconstructor_test.rb` — must pass.
5. Run `bin/rails test test/models/league_test.rb` — must pass (characterization tests unchanged).
6. Run `bundle exec standardrb app/services/league/game_plan_reconstructor.rb app/models/league.rb` — must pass.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/league/game_plan_reconstructor_test.rb test/models/league_test.rb test/models/league_standings_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - app/services/league/game_plan_reconstructor.rb exists and contains `class League::GamePlanReconstructor < ApplicationService`
    - app/services/league/game_plan_reconstructor.rb contains `def initialize(kwargs = {})`
    - app/services/league/game_plan_reconstructor.rb contains `when :reconstruct then reconstruct`
    - app/services/league/game_plan_reconstructor.rb contains `when :reconstruct_for_season`
    - app/services/league/game_plan_reconstructor.rb contains `when :delete_for_season`
    - app/services/league/game_plan_reconstructor.rb contains `frozen_string_literal: true`
    - app/models/league.rb contains `League::GamePlanReconstructor.call(league: self, operation: :reconstruct)`
    - app/models/league.rb contains `League::GamePlanReconstructor.call(season: season, operation: :reconstruct_for_season`
    - app/models/league.rb does NOT contain `def analyze_game_plan_structure` (moved to service)
    - app/models/league.rb does NOT contain `def find_leagues_with_same_gameplan` (moved to service)
    - test/services/league/game_plan_reconstructor_test.rb exists and contains `class League::GamePlanReconstructorTest`
    - `bin/rails test test/services/league/game_plan_reconstructor_test.rb` exits 0
    - `bin/rails test test/models/league_test.rb` exits 0 (characterization tests unmodified)
  </acceptance_criteria>
  <done>GamePlanReconstructor ApplicationService extracted, ~426 lines moved out of league.rb, delegation wrappers in place, utility methods removed from model, new service tests + all characterization tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Pure internal refactoring — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | T (Tampering) | league.rb delegation | accept | Delegation wrappers call service classes with `self` — same trust level as original inline code |
| T-21-02 | I (Info Disclosure) | Service classes | accept | No new data exposure — services access same ActiveRecord models with same authorization |
</threat_model>

<verification>
- `bin/rails test test/services/league/ test/models/league_test.rb test/models/league_standings_test.rb` — all pass
- `wc -l app/models/league.rb` shows reduction from 2221 lines (target: ~650 lines removed)
- `bundle exec standardrb app/services/league/ app/models/league.rb` — no violations
</verification>

<success_criteria>
- League::StandingsCalculator PORO exists with 4 public methods
- League::GamePlanReconstructor ApplicationService exists with dispatcher pattern
- League model has thin delegation wrappers for all extracted methods
- All Phase 20 characterization tests pass without modification
- New service tests pass
- League model reduced by ~650 lines
</success_criteria>

<output>
After completion, create `.planning/phases/21-league-extraction/21-01-SUMMARY.md`
</output>
