---
phase: 10-final-pass-green-suite
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - test/models/tournament_auto_reserve_test.rb
  - test/tasks/auto_reserve_tables_test.rb
  - test/models/tournament_ko_integration_test.rb
  - test/models/tournament_plan_ko_test.rb
  - test/controllers/table_locals_controller_test.rb
  - test/controllers/uploads_controller_test.rb
  - test/controllers/party_monitors_controller_test.rb
  - test/controllers/game_plans_controller_test.rb
  - test/controllers/club_locations_controller_test.rb
  - test/controllers/application_controller_test.rb
  - test/controllers/users/registrations_controller_test.rb
  - test/controllers/table_monitors_controller_test.rb
autonomous: true
requirements: [QUAL-02, QUAL-03, PASS-01]

must_haves:
  truths:
    - "tournament_auto_reserve_test.rb runs without PG::UniqueViolation errors"
    - "auto_reserve_tables_test.rb runs without PG::UniqueViolation errors"
    - "TournamentKoIntegrationTest passes or failures are documented with justification"
    - "All scaffold controller tests either pass or are deleted with commit justification"
    - "bin/rails test shows 0 failures and 0 errors"
  artifacts:
    - path: "test/models/tournament_auto_reserve_test.rb"
      provides: "Fixed season creation avoiding unique constraint violations"
    - path: "test/tasks/auto_reserve_tables_test.rb"
      provides: "Fixed season creation avoiding unique constraint violations"
  key_links:
    - from: "test/models/tournament_auto_reserve_test.rb"
      to: "test/fixtures/seasons.yml"
      via: "Season.create! must not conflict with fixture names"
      pattern: "Season.create!"
---

<objective>
Fix all remaining test failures and errors after infrastructure fixes from Plan 10-01.

Purpose: Achieve the green suite target (0 failures, 0 errors) by fixing the remaining categories: PG::UniqueViolation in auto_reserve tests (30 errors), controller scaffold test failures (auth, redirects, ApiProtector rollbacks), KO integration test failures, and registration test failures. This is iterative work — fix a category, re-run, fix the next.

Output: All test files passing. `bin/rails test` shows 0 failures, 0 errors.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-final-pass-green-suite/10-CONTEXT.md
@.planning/phases/10-final-pass-green-suite/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PG::UniqueViolation in auto_reserve and tournament_auto_reserve tests</name>
  <files>test/models/tournament_auto_reserve_test.rb, test/tasks/auto_reserve_tables_test.rb</files>
  <read_first>
    - .planning/phases/10-final-pass-green-suite/10-01-SUMMARY.md (current failure state after Plan 01)
    - test/models/tournament_auto_reserve_test.rb (full file — understand Season.create! in setup)
    - test/tasks/auto_reserve_tables_test.rb (full file — understand Season.create! in setup)
    - test/fixtures/seasons.yml (see existing fixture names that cause uniqueness conflicts)
    - db/schema.rb (check index_seasons_on_name unique constraint)
  </read_first>
  <action>
    The root cause: both test files call `Season.create!(name: "2025/2026")` in their setup blocks, but the seasons fixture already has an entry with name "2025/2026". The unique index on seasons.name causes PG::UniqueViolation.

    **Fix approach:** In each test file's setup block, replace `Season.create!(name: "2025/2026")` with a reference to the existing fixture: `@season = seasons(:current)`. This uses the fixture that already has name "2025/2026".

    Read each test file FULLY to understand:
    1. What the setup creates and why
    2. Whether the Season is used beyond just being a foreign key reference
    3. Whether other Model.create! calls might also conflict with fixtures (check Region, Discipline, Location, Table, etc.)

    If the tests create other records that might conflict with fixtures, use unique names (e.g., append test-specific suffixes) or use `find_or_create_by!` patterns.

    After fixing, run each test file individually to verify:
    ```
    bin/rails test test/models/tournament_auto_reserve_test.rb
    bin/rails test test/tasks/auto_reserve_tables_test.rb
    ```
  </action>
  <verify>
    <automated>bin/rails test test/models/tournament_auto_reserve_test.rb test/tasks/auto_reserve_tables_test.rb 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - bin/rails test test/models/tournament_auto_reserve_test.rb shows 0 errors, 0 failures
    - bin/rails test test/tasks/auto_reserve_tables_test.rb shows 0 errors, 0 failures
    - grep -c "UniqueViolation" from running these tests returns 0
  </acceptance_criteria>
  <done>All 30 PG::UniqueViolation errors in tournament_auto_reserve_test.rb (18) and auto_reserve_tables_test.rb (12) are resolved. Tests pass with fixture-based season references.</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining controller, KO integration, and miscellaneous test failures</name>
  <files>test/controllers/table_locals_controller_test.rb, test/controllers/uploads_controller_test.rb, test/controllers/party_monitors_controller_test.rb, test/controllers/game_plans_controller_test.rb, test/controllers/club_locations_controller_test.rb, test/controllers/application_controller_test.rb, test/controllers/users/registrations_controller_test.rb, test/controllers/table_monitors_controller_test.rb, test/models/tournament_ko_integration_test.rb, test/models/tournament_plan_ko_test.rb</files>
  <read_first>
    - .planning/phases/10-final-pass-green-suite/10-01-SUMMARY.md (current failure state)
    - test/controllers/table_locals_controller_test.rb (3 failures: create/destroy count wrong, update 422)
    - test/controllers/uploads_controller_test.rb (6 failures: redirects, auth, count issues)
    - test/controllers/party_monitors_controller_test.rb (4 failures: index/edit/destroy/show)
    - test/controllers/game_plans_controller_test.rb (4 failures: index/edit/destroy/show)
    - test/controllers/club_locations_controller_test.rb (2 failures: create/update)
    - test/controllers/application_controller_test.rb (2 failures: title string mismatch, preferences)
    - test/controllers/users/registrations_controller_test.rb (2 failures: registration form)
    - test/controllers/table_monitors_controller_test.rb (any remaining after fixture fix)
    - test/models/tournament_ko_integration_test.rb (5 failures: game counts doubled, state wrong)
    - test/models/tournament_plan_ko_test.rb (2 failures)
    - app/controllers/table_locals_controller.rb (understand create/update/destroy actions)
    - app/controllers/uploads_controller.rb (understand auth requirements)
    - app/controllers/game_plans_controller.rb (understand auth/routing)
    - app/controllers/party_monitors_controller.rb (understand auth/routing)
    - app/controllers/application_controller.rb (understand preferences, page title)
    - config/carambus.yml (check app_name for title assertion)
    - test/support/ko_tournament_test_helper.rb (understand KO tournament helper)
  </read_first>
  <action>
    This task is ITERATIVE. After each fix category, re-run `bin/rails test` to check for cascading resolutions.

    **Category 1: Controller scaffold tests (table_locals, uploads, party_monitors, game_plans, club_locations)**

    These are Rails scaffold-generated controller tests that fail because:
    - They lack proper auth (no `sign_in` before requests) — add `sign_in users(:admin)` or appropriate user in setup
    - ApiProtector/LocalProtector causes silent rollbacks on create/destroy — the ApiProtectorTestOverride from Plan 01 should fix this, but verify
    - They expect wrong redirect paths — check actual controller redirect targets
    - They send invalid params — check what the controller's strong params permit

    For each failing controller test, read the controller source to understand:
    1. What auth is required (Devise, Pundit, CanCanCan)
    2. What the correct redirect path is after create/update/destroy
    3. What params are required

    Then fix the test to match the actual controller behavior.

    **Category 2: ApplicationControllerTest**

    Two failures:
    1. Title assertion expects "Willkommen - Carambus" but actual is "Willkommen - Carambus API Server" — fix the expected string to match the actual app title from config/carambus.yml
    2. Preferences test — read the test and controller to understand the failure

    **Category 3: RegistrationsControllerTest**

    Two failures related to Devise registration form rendering and InvisibleCaptcha. Read the test and controller to understand what setup is missing.

    **Category 4: TournamentKoIntegrationTest (5 failures)**

    Failures show doubled game counts (30 vs expected 15) and wrong state ("playing_finals" vs expected "new_tournament_monitor"). This suggests `initialize_tournament_monitor` or `create_ko_games` is being called twice, or the helper creates games during setup that the test then re-creates. Read the helper and test carefully to understand the doubling.

    **Category 5: TournamentPlanKoTest (2 failures)**

    Read the test file to understand what's failing and fix accordingly.

    **Category 6: RegionCcCharTest (1 failure)**

    There was 1 non-skip failure in RegionCcCharTest. Read the test output to understand what's failing.

    **Decision guidance per D-01:** Fix everything possible. If a scaffold controller test cannot be made to work because the controller has complex auth/routing not expressible in a simple integration test, consider whether the test provides value. Per QUAL-03 (dead/redundant tests), scaffold tests that test nothing meaningful can be deleted with a commit message explaining why. However, prefer fixing over deleting.

    After all fixes, run the FULL suite: `bin/rails test`
  </action>
  <verify>
    <automated>bin/rails test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - bin/rails test shows 0 failures and 0 errors
    - If any tests were deleted, the deletion is documented in the SUMMARY with justification
    - Skipped count remains at 7 (VCR cassettes — addressed in Plan 10-03)
  </acceptance_criteria>
  <done>All test failures and errors resolved. `bin/rails test` reports 0 failures, 0 errors. Any tests that could not be fixed were deleted with documented justification per QUAL-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — test-only changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | T (Tampering) | Test fixture data | accept | Fixtures are test-only; no production data affected |
</threat_model>

<verification>
Run `bin/rails test` — must show 0 failures, 0 errors.
Run `bin/rails test test/models/tournament_auto_reserve_test.rb` — must show 0 failures, 0 errors.
Run `bin/rails test test/controllers/` — must show 0 failures, 0 errors.
</verification>

<success_criteria>
- PG::UniqueViolation errors in auto_reserve tests are eliminated
- All controller test failures are resolved (fixed or justified deletions)
- KO integration test failures are resolved
- `bin/rails test` shows 0 failures, 0 errors
- Only 7 VCR-related skips remain (handled by Plan 10-03)
</success_criteria>

<output>
After completion, create `.planning/phases/10-final-pass-green-suite/10-02-SUMMARY.md`
</output>
