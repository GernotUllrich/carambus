---
phase: 08-service-tests-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/services/region_cc/branch_syncer_test.rb
  - test/services/region_cc/competition_syncer_test.rb
  - test/services/region_cc/game_plan_syncer_test.rb
  - test/services/region_cc/metadata_syncer_test.rb
  - test/services/region_cc/party_syncer_test.rb
  - test/services/region_cc/registration_syncer_test.rb
  - test/services/region_cc/tournament_syncer_test.rb
  - test/services/region_cc/club_cloud_client_test.rb
autonomous: true
requirements: [SRVC-01]

must_haves:
  truths:
    - "Every assert_nothing_raised block in the 7 syncer test files is followed by at least one outcome assertion"
    - "club_cloud_client_test.rb response checks verify structure, not just presence"
    - "The useless PATH_MAP constant test is removed from club_cloud_client_test.rb"
    - "All 10 RegionCc service tests pass after changes"
    - "All 12 service test files have frozen_string_literal: true as their first line (per D-09)"
    - "All syncer tests use injected ClubCloudClient doubles and VCR cassettes — no live HTTP calls (ROADMAP criterion 2)"
  artifacts:
    - path: "test/services/region_cc/branch_syncer_test.rb"
      provides: "Post-condition assertion after assert_nothing_raised"
    - path: "test/services/region_cc/competition_syncer_test.rb"
      provides: "Post-condition assertions on 2 tests"
    - path: "test/services/region_cc/game_plan_syncer_test.rb"
      provides: "Post-condition assertions on 2 tests"
    - path: "test/services/region_cc/metadata_syncer_test.rb"
      provides: "Post-condition assertions on 3 tests"
    - path: "test/services/region_cc/party_syncer_test.rb"
      provides: "Post-condition assertion on 1 test"
    - path: "test/services/region_cc/registration_syncer_test.rb"
      provides: "Post-condition assertion on 1 test"
    - path: "test/services/region_cc/tournament_syncer_test.rb"
      provides: "Post-condition assertions on 3 tests"
    - path: "test/services/region_cc/club_cloud_client_test.rb"
      provides: "Strengthened response assertions, PATH_MAP test removed"
  key_links:
    - from: "test/services/region_cc/*_syncer_test.rb"
      to: "app/services/region_cc/*_syncer.rb"
      via: "Post-condition assertions must match what the syncer actually does"
      pattern: "assert_equal|assert_includes|assert_difference"
---

<objective>
Strengthen all RegionCc service test assertions — add post-condition checks after every sole assert_nothing_raised in the 7 syncer test files, strengthen response checks in club_cloud_client_test.rb, remove the useless PATH_MAP constant test, verify frozen_string_literal compliance (D-09), and confirm injected-doubles consistency (ROADMAP criterion 2).

Purpose: These tests currently prove only "no crash" but not "correct outcome." Adding 1-2 meaningful assertions per case makes them trustworthy regression guards.
Output: 8 modified test files with stronger assertions, all passing.
</objective>

<execution_context>
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/workflows/execute-plan.md
@/Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audit-baseline-standards/STANDARDS.md
@.planning/phases/06-audit-baseline-standards/AUDIT-REPORT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen 7 RegionCc syncer test files — add post-condition assertions</name>
  <files>
    test/services/region_cc/branch_syncer_test.rb
    test/services/region_cc/competition_syncer_test.rb
    test/services/region_cc/game_plan_syncer_test.rb
    test/services/region_cc/metadata_syncer_test.rb
    test/services/region_cc/party_syncer_test.rb
    test/services/region_cc/registration_syncer_test.rb
    test/services/region_cc/tournament_syncer_test.rb
  </files>
  <read_first>
    test/services/region_cc/branch_syncer_test.rb
    test/services/region_cc/competition_syncer_test.rb
    test/services/region_cc/game_plan_syncer_test.rb
    test/services/region_cc/metadata_syncer_test.rb
    test/services/region_cc/party_syncer_test.rb
    test/services/region_cc/registration_syncer_test.rb
    test/services/region_cc/tournament_syncer_test.rb
    app/services/region_cc/branch_syncer.rb
    app/services/region_cc/competition_syncer.rb
    app/services/region_cc/game_plan_syncer.rb
    app/services/region_cc/metadata_syncer.rb
    app/services/region_cc/party_syncer.rb
    app/services/region_cc/registration_syncer.rb
    app/services/region_cc/tournament_syncer.rb
    test/services/region_cc/league_syncer_test.rb
    test/services/region_cc/club_syncer_test.rb
  </read_first>
  <action>
For each of the 7 syncer test files, find every test where `assert_nothing_raised` is the SOLE assertion. Per D-01/D-02/D-03: keep the `assert_nothing_raised` block, then add 1-2 post-condition assertions AFTER it.

To determine what assertions to add, read both the test file AND the corresponding service source file. Also read league_syncer_test.rb and club_syncer_test.rb as examples of good assertion patterns (these are clean per audit).

Approach for each file:
1. Read the source syncer to understand what records it creates/updates and what side effects it produces
2. Read the test to understand the VCR cassette context and what data is available
3. After each `assert_nothing_raised` block, add assertions that verify the outcome — e.g., record count changed, specific attribute set, association created
4. Use `assert_equal`, `assert_includes`, `assert_difference`, `assert_predicate`, or `refute_nil` with a follow-up value check — NOT bare `assert_not_nil`

Specific files and their E02 locations from AUDIT-REPORT.md:
- branch_syncer_test.rb: Line 53 — `assert_not_nil branch_cc` — add `assert_equal` on cc_id or name attribute
- competition_syncer_test.rb: Lines 40, 83 — 2 tests with sole assert_nothing_raised
- game_plan_syncer_test.rb: Lines 68, 84 — 2 tests with sole assert_nothing_raised
- metadata_syncer_test.rb: Lines 77, 93, 109 — 3 tests with sole assert_nothing_raised
- party_syncer_test.rb: Line 78 — 1 test with sole assert_nothing_raised
- registration_syncer_test.rb: Line 47 — 1 test with sole assert_nothing_raised
- tournament_syncer_test.rb: Lines 38, 74, 117 — 3 tests with sole assert_nothing_raised

IMPORTANT: These tests use VCR cassettes. Do NOT change what code runs inside the assert_nothing_raised blocks. Only ADD assertions after them. The VCR cassettes replay recorded HTTP interactions — changing the call sequence would break cassette matching.

Do NOT touch league_syncer_test.rb or club_syncer_test.rb — they are clean per D-02.

Per D-09: While editing each file, verify it has `# frozen_string_literal: true` as its first line. If any file is missing it, add it. (Initial audit found all 12 already have it, but confirm and fix if needed.)

ROADMAP success criterion 2 verification: While reviewing each syncer test file, confirm that it uses injected ClubCloudClient doubles (not instantiating a real client that makes live HTTP) and VCR cassettes for any HTTP interactions. If any test lacks this pattern, flag it in the summary.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/region_cc/branch_syncer_test.rb test/services/region_cc/competition_syncer_test.rb test/services/region_cc/game_plan_syncer_test.rb test/services/region_cc/metadata_syncer_test.rb test/services/region_cc/party_syncer_test.rb test/services/region_cc/registration_syncer_test.rb test/services/region_cc/tournament_syncer_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "assert_nothing_raised" in each file matches the original count (none removed)
    - grep -c "assert_equal\|assert_includes\|assert_difference\|assert_predicate\|assert_operator" shows at least 1 new assertion per file (15 total new assertions across 7 files minimum)
    - No test uses bare `assert_not_nil` as a sole post-condition (branch_syncer line 53 strengthened)
    - All 7 test files pass: bin/rails test test/services/region_cc/{branch,competition,game_plan,metadata,party,registration,tournament}_syncer_test.rb
    - Per D-09: `grep -rL "frozen_string_literal: true" test/services/region_cc/*_syncer_test.rb` returns no results (all files have it)
    - ROADMAP criterion 2: `grep -rL "VCR\|vcr_cassette\|WebMock\|stub" test/services/region_cc/*_syncer_test.rb` returns no results (all syncer tests use VCR/WebMock, no live HTTP)
  </acceptance_criteria>
  <done>Every sole assert_nothing_raised in the 7 syncer test files has at least one meaningful post-condition assertion after it. All files have frozen_string_literal: true. All syncer tests use injected doubles / VCR — no live HTTP. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen club_cloud_client_test.rb — response structure checks + remove PATH_MAP test</name>
  <files>
    test/services/region_cc/club_cloud_client_test.rb
  </files>
  <read_first>
    test/services/region_cc/club_cloud_client_test.rb
    app/services/region_cc/club_cloud_client.rb
  </read_first>
  <action>
Per D-04: Strengthen the 7 `assert_not_nil res` / `assert_not_nil doc` assertions (lines 48, 49, 65, 94, 111, 137, 138) to check response structure. Read the club_cloud_client.rb source to understand what each method returns:
- For methods returning HTTP responses: assert on response class, status code, or body content
- For methods returning parsed Nokogiri documents: assert on document class (`Nokogiri::HTML::Document` or `Nokogiri::XML::Document`), or check for expected CSS selectors / node counts
- For methods returning parsed data: assert on data type and expected keys

Replace each bare `assert_not_nil res` with a more specific check. Keep `assert_not_nil` as a precondition if desired, but add at least one structural assertion after it.

Per D-05: Remove the test at line 146 that asserts `assert_not_nil RegionCc::ClubCloudClient::PATH_MAP`. Constants are never nil unless the class failed to load — this tests nothing. Delete the entire test method.

Per D-09: Verify this file has `# frozen_string_literal: true` as its first line. Add it if missing.

Do NOT change the test setup, VCR cassettes, or HTTP stubs — only strengthen assertions and remove the useless test.
  </action>
  <verify>
    <automated>cd /Volumes/EXT2TB/gullrich/DEV/carambus/carambus_api && bin/rails test test/services/region_cc/club_cloud_client_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "assert_not_nil res\b" club_cloud_client_test.rb returns 0 (all bare res checks replaced)
    - grep -c "assert_not_nil doc\b" club_cloud_client_test.rb returns 0 (all bare doc checks replaced)
    - grep "PATH_MAP" club_cloud_client_test.rb returns no test method (test removed)
    - grep -c "assert_equal\|assert_instance_of\|assert_kind_of\|assert_includes\|assert_respond_to\|assert_operator" club_cloud_client_test.rb shows at least 7 structural assertions
    - Per D-09: `head -1 test/services/region_cc/club_cloud_client_test.rb` shows `# frozen_string_literal: true`
    - Test file passes: bin/rails test test/services/region_cc/club_cloud_client_test.rb
  </acceptance_criteria>
  <done>All 7 response checks verify structure not just presence. PATH_MAP test is deleted. frozen_string_literal: true confirmed. Tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected. This plan modifies test files only — no production code changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T (Tampering) | VCR cassettes | accept | Tests only add assertions after existing blocks; VCR cassette replay is not modified |
</threat_model>

<verification>
After both tasks complete:
```bash
# All RegionCc tests pass
bin/rails test test/services/region_cc/

# D-09: All 12 service test files have frozen_string_literal: true
grep -rL "frozen_string_literal: true" test/services/region_cc/ test/services/table_monitor/game_setup_test.rb test/services/table_monitor/result_recorder_test.rb

# ROADMAP criterion 2: No syncer test makes live HTTP calls
grep -rL "VCR\|vcr_cassette\|WebMock\|stub" test/services/region_cc/*_syncer_test.rb
```
All 10 RegionCc service test files pass (including unchanged league_syncer and club_syncer).
D-09 grep returns no results (all files compliant).
ROADMAP criterion 2 grep returns no results (all syncer tests use doubles/VCR).
</verification>

<success_criteria>
- All 15+ sole assert_nothing_raised / assert_not_nil cases across 8 files now have meaningful post-condition assertions
- PATH_MAP constant test removed from club_cloud_client_test.rb
- All 10 RegionCc service test files pass
- No VCR cassette behavior broken
- All 12 service test files have frozen_string_literal: true (D-09)
- All syncer tests confirmed to use injected doubles and VCR — no live HTTP (ROADMAP criterion 2)
</success_criteria>

<output>
After completion, create `.planning/phases/08-service-tests-review/08-01-SUMMARY.md`
</output>
