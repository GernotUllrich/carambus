local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# For TrainingSource source_files - local only, not synced
local_sources:
  service: Disk
  root: <%= Rails.root.join("storage_local") %>

test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %> 