<%# ⚠️ EINE Meldung fuer alle Faelle — erfunden, abgelaufen, bereits verbraucht.
    Hier steht bewusst KEIN Name, KEINE Adresse und kein Hinweis darauf, ob es ein Konto
    ueberhaupt gibt. Jede Unterscheidung waere eine Auskunft an einen Unbekannten. %>

<div class="max-w-xl mx-auto p-4 space-y-4">
  <h1 class="text-2xl font-semibold text-gray-900 dark:text-gray-100">
    <%= t("pin_setups.invalid.title") %>
  </h1>
  <p class="text-gray-600 dark:text-gray-400"><%= t("pin_setups.invalid.body") %></p>
</div>
