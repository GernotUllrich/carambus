<%= form_for(@season_participation) do |f| %>
  <% if @season_participation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@season_participation.errors.count, "error") %> prohibited this season_participation from being saved:</h2>

      <ul>
      <% @season_participation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %><br>
    <%= f.number_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :season_id %><br>
    <%= f.number_field :season_id %>
  </div>
  <div class="field">
    <%= f.label :remarks %><br>
    <%= f.text_area :remarks %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
