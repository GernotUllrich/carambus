<%= form_for(@table_kind) do |f| %>
  <% if @table_kind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@table_kind.errors.count, "error") %> prohibited this table_kind from being saved:</h2>

      <ul>
      <% @table_kind.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :short %><br>
    <%= f.text_field :short %>
  </div>
  <div class="field">
    <%= f.label :measures %><br>
    <%= f.text_area :measures %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
