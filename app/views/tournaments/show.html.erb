<%= render :partial => "show", locals: { tournament: @ba_tournament, t_tournament: @t_tournament, subtitle: "", show_edit_back: true } %>
<div class="container mx-auto my-8">
  <div class="max-w-6xl mx-auto">
    <div class="flex flex-wrap bg-white dark:bg-black rounded shadow p-8 pb-4">
      <hr>
      <div class="flex flex-wrap">
        <div class="flex flex-wrap align-items-start pb-4">
          <%= button_to I18n.t("tournaments.show.reload_from_billard_area"), reload_from_ba_tournament_path(@ba_tournament), method: :post, style: "float: left; margin-right: 10px;", class: "btn btn-flat btn-primary" %>
        </div>
        <div class="flex flex-wrap align-items-start pb-4">
          <span class="strong flex" style="padding-top: 7px;">==&gt;</span>
          <%- disabled = !%w{new_tournament accreditation_finished}.include?(@ba_tournament.state) %>
          <%= button_to I18n.t("tournaments.show.order_by_ranking"), order_by_ranking_tournament_path(@ba_tournament), method: :post, class: "btn btn-flat btn-primary #{"opacity-25" if disabled}", style: "float: left; margin-right: 10px;", disabled: disabled %>
        </div>
        <%- if @t_tournament.present? %>
          <div class="flex flex-wrap align-items-start pb-4">
            <span class="strong flex" style="padding-top: 7px;">==&gt;</span>
            <%- disabled = !%w{accreditation_finished tournament_seeding_finished}.include?(@t_tournament.state) %>
            <%= button_to I18n.t("tournaments.show.finalize_tournament_modus"), finalize_modus_tournament_path(@t_tournament), method: :get, class: "btn btn-flat btn-primary #{"opacity-25" if disabled}", style: "float: left; margin-right: 10px;", disabled: disabled %>
          </div>
          <div class="flex flex-wrap align-items-start pb-4">
            <span class="strong flex" style="padding-top: 7px;">==&gt;</span>
            <%- disabled = %w{new_tournament accreditation_finished tournament_seeding_finished finals_finished results_published closed}.include?(@t_tournament.state) %>
            <%= button_to I18n.t("tournaments.show.mangage_tournament_group_phase"), tournament_monitor_tournament_path(@t_tournament), method: :get, class: "btn btn-flat btn-primary #{"opacity-25" if disabled}", style: "float: left; margin-right: 10px;", disabled: disabled %>
          </div>
        <%- end %>
      </div>
    </div>
  </div>
</div>
<hr class="mt-4 mb-6">
<div class="container mx-auto my-8 px-4">
  <div class="max-w-2xl mx-auto">
    <h2><%= I18n.t("tournaments.show.registrations") %></h2>
    <div class="flex flex-wrap space-y-5 bg-white dark:bg-black rounded shadow p-8">
      <table class="flex-1">
        <thead>
        <tr>
          <th><%= I18n.t("tournaments.show.position") %></th>
          <th colspan="2"><%= I18n.t("tournaments.show.reorder") %></th>
          <th><%= I18n.t("tournaments.show.name") %></th>
          <th><%= I18n.t("tournaments.show.club") %></th>
          <th>Rank<br><%= @ba_tournament.discipline.andand.name %><br>Season <%= @ba_tournament.season.previous.andand.name %>
          </th>
        </tr>
        </thead>
        <tbody>
        <%- @x_tournament.seedings.joins(:player).order(@x_tournament.tournament_seeding_finished? ? "#{@x_tournament.is_a?(Tournament) ? "seedings" : "t_seedings"}.position" : "players.lastname asc, players.firstname asc").each do |seeding| %>
          <%- club = @ba_tournament.season.season_participations.where("season_participations.player_id = ?", seeding.player.id).first.club %>
          <tr>
            <td><%= seeding.position %></td>
            <td>
              <%= link_to image_tag("up-icon.png"), up_seeding_path(seeding), method: :post %>
            </td>
            <td>
              <%= link_to image_tag("down-icon.png"), down_seeding_path(seeding), method: :post %>
            </td>
            <td><%= link_to("#{seeding.player.lastname}, #{seeding.player.firstname}", player_path(seeding.player)) %></td>
            <td><%= link_to("#{club.shortname}", club_path(club)) %></td>
            <td class="text-center"><%= seeding.player.player_rankings.where(discipline_id: Discipline.find_by_name("Freie Partie klein"), season_id: Season.find_by_ba_id(Season.current_season.ba_id - 1)).first.andand.rank.presence || 999 %></td>
          </tr>
        <%- end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
<div class="container mx-auto my-8 px-4">
  <div class="max-w-2xl mx-auto">
    <h2 class="mt-4 mb-6"><%= I18n.t("tournaments.show.rankings") %></h2>
    <div class="flex-1 flex-wrap space-y-5 bg-white dark:bg-black rounded shadow p-8">
      <%- lists = @x_tournament.seedings.joins(:player).order("players.lastname, players.firstname").map { |s| s.data["result"].keys rescue [] }.inject([]) { |a, memo| memo |= a; memo } %>
      <%- lists.each do |list| %>
        <div class="flex flex-col">
          <h3><%= list %></h3>
          <table class="flex-1">
            <thead>
            <tr>
              <%- @x_tournament.seedings.select { |seeding| seeding.data["result"].andand[list].present? }.first.data["result"][list].keys.each do |k| %>
                <%- next if k == "Rank" %>
                <th><%= k %></th>
              <%- end %>
            </tr>
            </thead>
            <tbody>
            <%- @x_tournament.seedings.select { |seeding| seeding.data["result"].andand[list].present? }.sort_by { |seeding| seeding.data["result"][list]["#"].to_i }.each do |seeding| %>
              <tr>
                <%- seeding.data["result"][list].each do |k, v| %>
                  <%- next if k == "Rank" %>
                  <td><%= v %></td>
                <%- end %>
              </tr>
            <%- end %>
            </tbody>
          </table>
        </div>
      <%- end %>
    </div>
  </div>
</div>
<div class="container mx-auto my-8 px-4">
  <div class="max-w-5xl mx-auto">
    <h2 class="mt-4 mb-6"><%= I18n.t("tournaments.show.games") %></h2>
    <div class="flex flex-wrap space-y-5 bg-white dark:bg-black rounded shadow p-8">
      <%- if @x_tournament.games.present? %>
        <table class="flex-1">
          <thead>
          <tr>
            <%- @x_tournament.games.first.data.keys.each do |k| %>
              <th><%= k %></th>
            <%- end %>
          </tr>
          </thead>
          <tbody>
          <%- @x_tournament.games.each do |game| %>
            <tr>
              <%- skip = false %>
              <%- game.data.each do |k, v| %>
                <%- skip = true if %w{Heim Gast}.include?(k) && v.blank? %>
                <%- unless skip %>
                  <td><%= v %></td>
                <%- end %>
              <%- end %>
            </tr>
          <%- end %>
          </tbody>
        </table>
      <%- end %>
    </div>
  </div>
</div>
<%- games = @x_tournament.games.order(seqno: :asc) %>
<%- if !@t_tournament.andand.tournament_started %>
  <%= button_to I18n.t("tournaments.show.reset_tournament_monitor"), reset_tournament_path(@ba_tournament), method: :post, class: "btn btn-flat btn-primary", data: { confirm: 'Are you sure?' }, style: "float: left; margin-right: 10px;" %>
<%- end %>
<%- if current_user.andand.email.andand.downcase == "gernot.ullrich@gmx.de" %>
  <%= button_to I18n.t("tournaments.show.debugging_force_reset_tournament_monitor"), reset_tournament_path(@ba_tournament, force_reset: true), method: :post, class: "btn btn-flat btn-primary", style: "float: left; margin-right: 10px;" %>
<% end %>
