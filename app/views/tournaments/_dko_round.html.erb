<%#
  Eine Runden-Spalte im Doppel-K.-o.-Diagramm. Locals: label, games.
  Überschrift fest oben; Matches vertikal gleichmäßig verteilt, damit die
  Verbindungslinien des Controllers sauber zwischen den Runden verlaufen.
%>
<div class="flex flex-col min-w-[14rem]">
  <h4 class="text-xs font-bold uppercase tracking-wider text-gray-500 dark:text-gray-400 text-center mb-2">
    <%= label %>
  </h4>
  <div class="flex flex-col justify-around gap-6 flex-1">
    <% games.each_with_index do |game, idx| %>
      <%= render partial: "tournaments/ko_match", locals: { game: game, number: idx + 1, label: label } %>
    <% end %>
  </div>
</div>
