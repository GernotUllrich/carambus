<div class="container mx-auto my-8 px-4">
  <div class="max-w-6xl mx-auto">

    <!-- Header -->
    <div class="flex items-center justify-between mb-6">
      <h1 class="text-3xl font-bold">
        Setzliste automatisch erkannt
      </h1>
      <%= link_to '← Zurück', compare_seedings_tournament_path(@tournament), class: 'btn btn-secondary' %>
    </div>

    <% if @extraction_result[:success] %>
      <!-- Erfolgreiche Extraktion -->
      <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-6">
        <h3 class="font-semibold text-green-900 mb-2">
          ✅ <%= @extraction_result[:count] %> Spieler automatisch erkannt!
          <% if @extraction_result[:group_assignment].present? %>
            <span class="ml-2 text-sm">+ Gruppenbildung extrahiert 🎯</span>
          <% end %>
        </h3>
        <p class="text-green-800">
          Die Setzliste wurde aus der Einladung extrahiert.
          <% if @extraction_result[:group_assignment].present? %>
            <br><strong>Bonus:</strong> Die Gruppenzuordnung wurde ebenfalls erkannt und wird automatisch verwendet.
          <% end %>
          Bitte prüfen Sie die erkannten Spieler und bestätigen Sie die Übernahme.
        </p>
      </div>

      <!-- Gruppenbildung Preview (wenn extrahiert) -->
      <% if @extraction_result[:group_assignment].present? %>
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
          <h3 class="font-semibold text-blue-900 mb-3">
            🎯 Extrahierte Gruppenbildung
          </h3>
          <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
            <% @extraction_result[:group_assignment].each do |group_no, player_positions| %>
              <div class="bg-white p-3 rounded border">
                <h4 class="font-bold text-blue-900 mb-2">Gruppe <%= group_no %></h4>
                <ol class="list-decimal list-inside text-sm">
                  <% player_positions.each do |pos| %>
                    <% matched = @match_result[:matched].find { |m| m[:position] == pos } %>
                    <li class="text-blue-800">
                      <% if matched %>
                        <%= matched[:player].fullname %>
                      <% else %>
                        Spieler <%= pos %>
                      <% end %>
                    </li>
                  <% end %>
                </ol>
              </div>
            <% end %>
          </div>
          <p class="text-sm text-blue-700 mt-3">
            💡 Diese Zuordnung wird in Schritt 5 (Turniermodus) verwendet und überschreibt den Standard-Algorithmus.
          </p>
        </div>
      <% end %>

      <!-- Matched Players -->
      <div class="bg-white  dark:bg-slate-900 rounded-lg shadow p-6 mb-6">
        <h2 class="text-xl font-bold mb-4">
          Erkannte Spieler (<%= @match_result[:match_rate] %>% Übereinstimmung)
        </h2>

        <%= form_with url: apply_seeding_order_tournament_path(@tournament), method: :post do |f| %>
          <table class="w-full">
            <thead>
              <tr class="bg-gray-100 dark:bg-slate-900">
                <th class="p-3 text-left">Position</th>
                <th class="p-3 text-left">Erkannter Name</th>
                <th class="p-3 text-left">Zugeordneter Spieler</th>
                <th class="p-3 text-left">Vorgabe (Pkt)</th>
                <th class="p-3 text-left">Status</th>
              </tr>
            </thead>
            <tbody>
              <% @match_result[:matched].each do |match| %>
                <% player = match[:player] %>
                <% club = @tournament.season.season_participations.where("season_participations.player_id = ?", player.id).first&.club %>
                <tr class="border-b">
                  <td class="p-3 font-bold"><%= match[:position] %></td>
                  <td class="p-3"><%= match[:extracted_name] %></td>
                  <td class="p-3">
                    <div class="flex items-center gap-2">
                      <% if club&.logo.present? %>
                        <%= image_tag club.logo, class: "h-6 w-auto object-contain", alt: club.shortname %>
                      <% end %>
                      <span><%= player.fullname %></span>
                      <% if player.dbu_nr.present? && player.dbu_nr > 0 %>
                        <span class="text-xs text-gray-500">(DBU: <%= player.dbu_nr %>)</span>
                      <% end %>
                    </div>
                    <%= hidden_field_tag 'seeding_order[]', player.id %>
                    <%= hidden_field_tag "balls_goal[#{player.id}]", match[:balls_goal] if match[:balls_goal].present? %>
                  </td>
                  <td class="p-3">
                    <% if match[:balls_goal].present? %>
                      <span class="font-semibold text-blue-600"><%= match[:balls_goal] %> Pkt</span>
                    <% else %>
                      <span class="text-gray-400">–</span>
                    <% end %>
                  </td>
                  <td class="p-3">
                    <% if match[:confidence] == :high %>
                      <span class="text-green-600 font-semibold">✓ Sicher</span>
                    <% else %>
                      <span class="text-yellow-600 font-semibold">⚠️ Vermutung</span>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>

          <% if @match_result[:unmatched].any? %>
            <div class="mt-6 p-4 bg-yellow-50 border border-yellow-200 rounded">
              <h4 class="font-semibold text-yellow-900 mb-2">
                ⚠️ Nicht zugeordnet (<%= @match_result[:unmatched].count %>):
              </h4>
              <ul class="list-disc list-inside text-yellow-800">
                <% @match_result[:unmatched].each do |um| %>
                  <li><%= um[:full_name] %> (Position <%= um[:position] %>)</li>
                <% end %>
              </ul>
              <p class="text-sm text-yellow-700 mt-2">
                Diese Spieler sind nicht in der Meldeliste. Bitte fügen Sie sie manuell hinzu.
              </p>
            </div>
          <% end %>

          <div class="flex justify-between items-center mt-6">
            <%= link_to 'Abbrechen', compare_seedings_tournament_path(@tournament),
                class: 'btn btn-secondary' %>
            <%= f.submit 'Setzliste übernehmen',
                class: 'btn btn-success btn-lg',
                data: { confirm: "Setzliste mit #{@match_result[:matched].count} Spielern übernehmen?" } %>
          </div>
        <% end %>
      </div>

      <!-- Raw Text Preview (für Debugging) -->
      <details class="bg-gray-50 dark:bg-gray rounded-lg p-4">
        <summary class="cursor-pointer font-semibold text-gray-700">
          🔍 Extrahierter Text anzeigen (Debugging)
        </summary>
        <pre class="mt-4 p-4 bg-white border rounded text-xs overflow-auto max-h-96"><%= @extraction_result[:raw_text] %></pre>
      </details>

    <% else %>
      <!-- Fehler bei Extraktion -->
      <div class="bg-red-50 border border-red-200 rounded-lg p-6">
        <h3 class="font-semibold text-red-900 mb-2">
          ❌ Automatische Extraktion fehlgeschlagen
        </h3>
        <p class="text-red-800 mb-4">
          <%= @extraction_result[:error] %>
        </p>

        <div class="space-y-3">
          <p class="text-red-700">Alternativen:</p>
          <div class="flex gap-3">
            <%= link_to 'Andere Datei hochladen', compare_seedings_tournament_path(@tournament),
                class: 'btn btn-primary' %>
            <%= link_to 'Manuell sortieren', tournament_path(@tournament),
                class: 'btn btn-secondary' %>
          </div>
        </div>

        <% if @extraction_result[:raw_text].present? %>
          <details class="mt-4">
            <summary class="cursor-pointer text-red-700">Extrahierter Text anzeigen</summary>
            <pre class="mt-2 p-4 bg-white border rounded text-xs overflow-auto max-h-96"><%= @extraction_result[:raw_text] %></pre>
          </details>
        <% end %>
      </div>
    <% end %>

  </div>
</div>

