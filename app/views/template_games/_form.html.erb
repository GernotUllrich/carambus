<%= form_for(@template_game) do |f| %>
  <% if @template_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@template_game.errors.count, "error") %> prohibited this template_game from being saved:</h2>

      <ul>
      <% @template_game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :template_id %><br>
    <%= f.number_field :template_id %>
  </div>
  <div class="field">
    <%= f.label :remarks %><br>
    <%= f.text_area :remarks %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
