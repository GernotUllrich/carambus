<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <%= link_to "← Back to Dashboard", scraping_monitor_path, class: "text-primary-600 hover:text-primary-800" %>
  </div>
  
  <h1 class="text-3xl font-bold mb-6">📊 <%= @operation %></h1>
  
  <!-- Time Range Filter -->
  <div class="mb-6 flex gap-2">
    <%= link_to "7 Days", scraping_monitor_operation_path(@operation, days: 7), 
        class: "px-4 py-2 rounded #{@time_range == 7 ? 'bg-primary-600 text-white' : 'bg-gray-200 dark:bg-gray-700 dark:text-gray-200'}" %>
    <%= link_to "30 Days", scraping_monitor_operation_path(@operation, days: 30), 
        class: "px-4 py-2 rounded #{@time_range == 30 ? 'bg-primary-600 text-white' : 'bg-gray-200 dark:bg-gray-700 dark:text-gray-200'}" %>
    <%= link_to "90 Days", scraping_monitor_operation_path(@operation, days: 90), 
        class: "px-4 py-2 rounded #{@time_range == 90 ? 'bg-primary-600 text-white' : 'bg-gray-200 dark:bg-gray-700 dark:text-gray-200'}" %>
  </div>
  
  <!-- Stats Cards -->
  <div class="grid grid-cols-1 md:grid-cols-5 gap-4 mb-8">
    <div class="bg-white dark:bg-gray-800 border-l-4 border-purple-500 p-4 shadow">
      <div class="text-sm text-gray-600 dark:text-gray-300">Total Runs</div>
      <div class="text-2xl font-bold text-gray-900 dark:text-gray-100"><%= @stats[:total_runs] %></div>
    </div>
    
    <div class="bg-white dark:bg-gray-800 border-l-4 border-indigo-500 p-4 shadow">
      <div class="text-sm text-gray-600 dark:text-gray-300">Avg Duration</div>
      <div class="text-2xl font-bold text-gray-900 dark:text-gray-100"><%= "#{@stats[:avg_duration]}s" %></div>
    </div>
    
    <div class="bg-white dark:bg-gray-800 border-l-4 border-green-500 p-4 shadow">
      <div class="text-sm text-gray-600 dark:text-gray-300">Total Created</div>
      <div class="text-2xl font-bold text-green-600"><%= @stats[:total_created] %></div>
    </div>
    
    <div class="bg-white dark:bg-gray-800 border-l-4 border-primary-500 p-4 shadow">
      <div class="text-sm text-gray-600 dark:text-gray-300">Total Updated</div>
      <div class="text-2xl font-bold text-primary-600"><%= @stats[:total_updated] %></div>
    </div>
    
    <div class="bg-white dark:bg-gray-800 border-l-4 border-red-500 p-4 shadow">
      <div class="text-sm text-gray-600 dark:text-gray-300">Success Rate</div>
      <div class="text-2xl font-bold <%= @stats[:success_rate] >= 95 ? 'text-green-600' : 'text-red-600' %>">
        <%= "#{@stats[:success_rate]}%" %>
      </div>
    </div>
  </div>
  
  <!-- Last Run Info -->
  <div class="bg-primary-50 dark:bg-primary-900/30 border border-primary-200 dark:border-primary-700 rounded p-4 mb-8">
    <strong class="text-primary-900 dark:text-primary-100">Last Run:</strong>
    <span class="text-primary-700 dark:text-primary-300"><%= @stats[:last_run]&.strftime('%Y-%m-%d %H:%M:%S') || 'N/A' %></span>
  </div>
  
  <!-- Logs Table -->
  <div class="bg-white dark:bg-gray-800 shadow rounded-lg">
    <div class="px-6 py-4 border-b border-gray-200 dark:border-gray-700">
      <h2 class="text-xl font-semibold">📝 Execution History (Last 100 Runs)</h2>
    </div>
    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200 dark:divide-gray-700">
        <thead class="bg-gray-50 dark:bg-gray-700">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Timestamp</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Context</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Duration</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Created</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Updated</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Deleted</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Unchanged</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider">Errors</th>
          </tr>
        </thead>
        <tbody class="bg-white dark:bg-gray-800 divide-y divide-gray-200 dark:divide-gray-700">
          <% @logs.each do |log| %>
            <tr class="<%= log.error_count > 0 ? 'bg-red-50 dark:bg-red-900/30' : 'hover:bg-gray-50 dark:hover:bg-gray-700' %>">
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 dark:text-gray-100">
                <%= log.executed_at.strftime('%Y-%m-%d %H:%M:%S') %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600 dark:text-gray-300">
                <%= log.context || '-' %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm">
                <span class="<%= log.duration.to_f > 120 ? 'text-orange-600 font-bold' : 'text-gray-900 dark:text-gray-100' %>">
                  <%= "#{log.duration&.round(2)}s" %>
                </span>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-green-600">
                <%= log.created_count %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-primary-600">
                <%= log.updated_count %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-yellow-600">
                <%= log.deleted_count %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600 dark:text-gray-300">
                <%= log.unchanged_count %>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-sm">
                <% if log.error_count > 0 %>
                  <details class="cursor-pointer">
                    <summary class="px-2 py-1 text-xs font-bold bg-red-100 text-red-800 dark:bg-red-900/30 dark:text-red-200 rounded hover:bg-red-200 dark:hover:bg-red-800">
                      <%= log.error_count %> errors
                    </summary>
                    <div class="mt-2 p-2 bg-red-50 dark:bg-red-900/30 rounded text-xs">
                      <% log.errors_parsed.each do |error| %>
                        <div class="mb-2">
                          <strong><%= error.dig('error', 'class') %>:</strong>
                          <%= error.dig('error', 'message') %>
                        </div>
                      <% end %>
                    </div>
                  </details>
                <% else %>
                  <span class="text-green-600">✓</span>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
