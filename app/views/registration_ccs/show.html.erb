<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= custom_link_to 'Registration Ccs', registration_ccs_path %> > Registration Cc #<%= @registration_cc.id %></h1>
      <%= custom_link_to 'Edit', edit_registration_cc_path(@registration_cc), class: "btn btn-link" %>
    </div>

    <div class=" rounded shadow p-8">
            <div class="mb-4">
        <h5>Registration list cc</h5>
        <%= @registration_cc.registration_list_cc_id %>
      </div>
            <div class="mb-4">
        <h5>Player</h5>
        <%= @registration_cc.player_id %>
      </div>
            <div class="mb-4">
        <h5>Status</h5>
        <%= @registration_cc.status %>
      </div>
          </div>
  </div>
</div>
