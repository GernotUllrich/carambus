<div class=" rounded shadow table-responsive">
  <table class="table">
    <% @league_teams ||= records %>
    <% @pagy ||= pagy %>
    <thead>
    <tr>
      <th class="p-3 uppercase text-left text-xs"><%= sortable @league_teams, :name,'Name', params, class: 'text-gray-700' %></th>
      <th class="p-3 uppercase text-left text-xs"><%= sortable @league_teams, :shortname,'Shortname', params, class: 'text-gray-700' %></th>
      <th class="p-3 uppercase text-left text-xs"><%= sortable @league_teams, :league_id,'League', params, class: 'text-gray-700' %></th>
      <th class="p-3 uppercase text-left text-xs"><%= sortable @league_teams, :season_id,'Season', params, class: 'text-gray-700' %></th>
<!--      <th class="p-3 uppercase text-left text-xs"><%#= sortable @league_teams, :ba_id,'Ba', params, class: 'text-gray-700' %></th>-->
      <th class="p-3 uppercase text-left text-xs"><%= sortable @league_teams, :cc_id,'Cc', params, class: 'text-gray-700' %></th>
      <th class="p-3 uppercase text-left text-xs"><%= sortable @league_teams, :club_id,'Club', params, class: 'text-gray-700' %></th>
      <th class="p-3 uppercase text-left text-xs"><%= sortable @league_teams, :source_url,'Source', params, class: 'text-gray-700' %></th>
      <th class="p-3 uppercase text-left text-xs"><%= sortable @league_teams, :sync_date,'Sync Date', params, class: 'text-gray-700' %></th>
      <th class="p-3 uppercase text-left text-xs text-gray-700 w-20">Actions</th>
    </tr>
    </thead>

    <tbody>
    <% @league_teams.each do |league_team| %>
      <tr class="group border-t border-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700">
        <td class="p-3"><%= league_team.name %></td>
        <td class="p-3"><%= league_team.shortname %></td>
        <td class="p-3"><%= custom_link_to league_team.league.name, league_path(league_team.league) %></td>
        <td class="p-3"><%= custom_link_to league_team.league.season.name, season_path(league_team.league.season) %></td>
        <td class="p-3"><%= link_to_if league_team.cc_id.present?, league_team.cc_id, league_team.cc_id_link, target: :_blank %></td>
        <td class="p-3"><%= custom_link_to league_team.club.shortname, club_path(league_team.club) if league_team.club.present? %></td>
        <td class="p-3"><%= link_to_if league_team.source_url.present?, league_team.source_url.present? ? URI.parse(league_team.source_url).host : "", league_team.source_url, target: :_blank %></td>
        <td class="p-3"><%= league_team.sync_date.andand.to_date %></td>

        <td>
          <%= custom_link_to "View", league_team, class: "btn btn-link no-underline" %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>

<% if @pagy.pages > 1 %>
  <div class="text-center my-6">
    <%== pagy_nav(@pagy) %>
  </div>
<% end %>
