<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Carambus Stream Overlay</title>
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  
  <style>
    /* Custom animations and gradient backgrounds */
    @keyframes pulse-dot {
      0%, 100% { opacity: 1; transform: scale(1); }
      50% { opacity: 0.5; transform: scale(0.85); }
    }
    
    .live-indicator {
      animation: pulse-dot 2s infinite;
    }
    
    .overlay-gradient {
      background: linear-gradient(135deg, rgba(0, 97, 120, 0.95) 0%, rgba(0, 60, 80, 0.95) 100%);
    }
  </style>
  
  <%= javascript_importmap_tags %>
  <%= action_cable_meta_tag %>
</head>

<body class="w-full h-full m-0 p-0 font-sans text-white overflow-hidden antialiased" style="background-color: #000000;">
  <%= yield %>
</body>
</html>
