<%= form_for(@game_participation) do |f| %>
  <% if @game_participation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_participation.errors.count, "error") %> prohibited this game_participation from being saved:</h2>

      <ul>
      <% @game_participation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.number_field :game_id %>
  </div>
  <div class="field">
    <%= f.label :player_id %><br>
    <%= f.number_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :role %><br>
    <%= f.text_field :role %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
