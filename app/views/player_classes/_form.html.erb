<%= form_for(@player_class) do |f| %>
  <% if @player_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_class.errors.count, "error") %> prohibited this player_class from being saved:</h2>

      <ul>
      <% @player_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :discipline_id %><br>
    <%= f.number_field :discipline_id %>
  </div>
  <div class="field">
    <%= f.label :shortname %><br>
    <%= f.text_field :shortname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
