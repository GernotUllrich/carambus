<%# 09-02: generisches Row-List-Shell (Foundation v0.3) — ersetzt die bespoke Scaffold-Tabelle. %>
<%= render "shared/scaffold_index", model_class: Country, records: @countries, title: t('country.index.countries'), new_path: new_country_path %>
<div class="container mx-auto px-4 mb-8 text-sm">
  -> <%= custom_link_to t('home.index.region'), regions_path, class: "text-primary-600 hover:text-primary-700" %>
</div>
