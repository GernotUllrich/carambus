<%= content_for :title, "Discipline Phases ##{@discipline_phase.id}" %>
<%= turbo_stream_from @discipline_phase %>

<div class="container px-4 mx-auto my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= custom_link_to "Discipline Phases", discipline_phases_path, class: "text-black" %>
      <span class="text-gray-400 font-light mx-2">\</span>
      Discipline Phase #<%= @discipline_phase.id %>
    </h1>
    <%= custom_link_to t("edit"), edit_discipline_phase_path(@discipline_phase), class: "btn btn-white" %>
  </div>

  <div class="p-8  rounded shadow">
    <%= render @discipline_phase %>
  </div>
</div>
